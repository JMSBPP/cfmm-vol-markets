---
phase: 24-gams-invocation-toolchain-identity
plan: 05
type: execute
wave: 5
depends_on: ["24-04"]
files_modified:
  - cfmm-replicationPlank-rpc-api.cabal
  - offchain/lib/Gams/Invoke.hs
  - offchain/app/GamsConformance.hs
  - offchain/rig/capture-gams-conformance.sh
  - offchain/rig/gams-conformance.json
  - offchain/rig/README.md
  - offchain/test/Main.hs
autonomous: true
requirements: [GAMS-01, GAMS-02, GAMS-03, GAMS-04, GAMS-05, GAMS-06, BYTE-04]

must_haves:
  truths:
    - "The real GAMS 54.1 binary was driven, and the committed artifact records what it did — exit codes, banners, digests, and the bytes under four environments"
    - "The artifact records action=c exiting 0 with volume_path.json ABSENT — the real binary producing GAMS-02's firing input"
    - "The artifact records the true CONOPT version alongside both decoys, and the true one is asserted DIFFERENT from the link version"
    - "The artifact records the leading-zero argv token producing a different sha256 at exit 0 with every gate green"
    - "Editing Gams/Argv.hs without re-capturing reddens the suite — the freshness oracle recomputes the half it can"
    - "Every leaf of the artifact is either asserted by a check or pardoned in absorbed_by_design with a written reason"
  artifacts:
    - path: "offchain/rig/gams-conformance.json"
      provides: "the committed real-GAMS evidence every Tier-C check reads"
      contains: "gc_complete"
    - path: "offchain/rig/capture-gams-conformance.sh"
      provides: "the only script that drives the real solver; refuses to emit a partial artifact"
    - path: "offchain/lib/Gams/Invoke.hs"
      provides: "the real-prover composition; the one module cabal test may not name"
    - path: "offchain/app/GamsConformance.hs"
      provides: "the capture executable, aeson in the executable only"
  key_links:
    - from: "offchain/test/Main.hs"
      to: "offchain/rig/gams-conformance.json"
      via: "gams_conformance_path, resolved through GAMS_CONFORMANCE"
      pattern: "gams_conformance_path"
    - from: "offchain/test/Main.hs"
      to: "offchain/lib/Gams/Argv.hs"
      via: "the freshness oracle recomputes the module digest the capture recorded"
      pattern: "argv_module_sha256"
---

<objective>
Drive the real GAMS 54.1 / CONOPT 4.39 toolchain once, out of band, and turn what it did into
committed evidence that `cabal test` asserts over while never touching the solver.

Purpose: every guard this phase installs was designed against MEASURED behaviour. Tier A and Tier B
prove the guards fire; only Tier C proves they were aimed at the real thing. This is the
`store-conformance.json` pattern applied to a solver — a fresh checkout has the artifact, there is
nothing to provision and nothing to skip.

Output: `Gams.Invoke`, the capture executable and script, the committed
`offchain/rig/gams-conformance.json`, nine Tier-C checks, and the artifact's registration in the
sentinel sweep with all three floors re-measured.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-gams-invocation-toolchain-identity/24-RESEARCH.md
@.planning/phases/24-gams-invocation-toolchain-identity/24-04-SUMMARY.md
@offchain/rig/capture-store-conformance.sh
@offchain/lib/Gams/Run.hs
@offchain/lib/Gams/Config.hs
@offchain/test/Main.hs

<interfaces>
<!-- The capture idiom this plan copies, from offchain/rig/capture-store-conformance.sh: -->

```sh
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
OUT=offchain/rig/store-conformance.json
CFMM_REQUIRE_DB="${CFMM_REQUIRE_DB:-1}"
# "This capture has exactly one mode. There is no skip path, because a capture that skips
#  writes a stale artifact and reports success."
if [ "$CFMM_REQUIRE_DB" != "1" ]; then … exit 1; fi
if ! command -v docker >/dev/null 2>&1; then … "emits NO artifact rather than a partial one" … fi
```

Its gate is a VALUE, not a green build, and the committed artifact is left exactly as it was when
the capture cannot run.

<!-- From offchain/test/Main.hs — the sentinel sweep this plan registers a sixth artifact with: -->

```haskell
swept_artifacts       :: [MutableArtifact]      -- line 5228, five entries
artifact_field_floors :: [(String, Int)]        -- line 5705, five entries, ALL re-measured at 23-05
sentinel_pair_floor   :: Int                    -- line 5695, currently 3250
absorbed_by_design    :: [(String, [(String, Int)], String)]  -- line 5517, field/sentinel/reason
unswept_artifacts     :: [FilePath]             -- line 5243, the honest gap, asserted to exist
expensive_checks      :: [String]               -- line 5293
```

Budget arithmetic of record, from the 23-05 haddock: `store-conformance.json` enumerates **134**
leaves, six sentinels, 804 possible pairs, **793 exercised** (11 identity skips), and the four
older artifacts still contribute exactly 2457 — `3250 - 2457 = 793`. That is what says none of them
silently shrank. Note also that `jq 'paths(scalars)'` OMITS JSON nulls and the harness does not, so
the harness's number is the larger one and it is the one to budget with.

<!-- MEASURED values this capture must reproduce (24-RESEARCH M1a/M1b/M3/M4/M5/M6/M7/M8/M9/M13): -->

| observation | measured |
|---|---|
| clean solve exit / sha256 | 0 / `e7b14f384ab4c027be5450218a52040110d45dbaddbbfb0bb7bd5ab707d0d884`, 606 bytes |
| `action=c` | exit 0, `volume_path.json` **ABSENT** |
| no arguments | exit **0**, stdout 1239 bytes / 27 lines, stderr 0, line 1 `--- Job ? Start …` |
| `--version` | exit **6**, stdout 275 bytes, stderr 0, line 1 `--- Job --version Start …` |
| syntax error / `abort$` / `1/0` / missing file | 2 / 3 / 3 / 6 |
| CONOPT true | `    C O N O P T   version 4.39.0` (probe line 38, real run line 47) |
| CONOPT decoys | `CONOPT 4         54.1.0 37378ce0 …` and `libconopt464.so` |
| leading zero `079228…` | exit 0, all §4 gates pass, sha256 `d64a7b32…14b9e650` ≠ golden |
| `28e18` vs `2.8e19` | byte-identical, both golden |
| hostile env (`GAMSTHREADS=8 GDXCOMPRESS=1 LC_NUMERIC=de_DE.UTF-8 GAMSDIR=/nonexistent`) | golden |
| `gams` binary | 1,822,256 bytes, sha256 `79cd3a57…` — MACHINE-SPECIFIC: record, never pin in Haskell |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gams.Invoke, the capture executable, and the capture script</name>
  <files>cfmm-replicationPlank-rpc-api.cabal, offchain/lib/Gams/Invoke.hs, offchain/app/GamsConformance.hs, offchain/rig/capture-gams-conformance.sh, offchain/rig/README.md</files>

  <read_first>
    - `offchain/rig/capture-store-conformance.sh` in full — the refuse-to-emit-a-partial-artifact
      discipline, the preconditions that fail loudly and never default, the per-run naming, and the
      "the gate is a VALUE, not a green build" header.
    - `cfmm-replicationPlank-rpc-api.cabal` lines 195-225 — the `store-conformance` executable
      stanza and its comment explaining why `aeson` is in the executable only.
    - `offchain/lib/Gams/{Run,Config,Version,Argv,Env}.hs`.
    - `.planning/phases/24-gams-invocation-toolchain-identity/24-RESEARCH.md` M3 (the 8-line CONOPT
      probe), M6, M7, M9, M13.
  </read_first>

  <action>
    **1. `offchain/lib/Gams/Invoke.hs`** — the real-prover composition, and the one module
    `offchain/test/Main.hs` may not name (plan 24-04's structural grep). It:
    - resolves `gams_bin` from `Gams.Config`; when the value contains no path separator it resolves
      it with `System.Directory.findExecutable` and FAILS LOUDLY naming `GAMS_BIN` if it is not
      found. The result must be ABSOLUTE — a `PATH`-resolved name is a shadow surface, and invoking
      by absolute path removes it entirely (MEASURED: the GAMS directory need not be on `PATH` at
      all);
    - resolves `gams_model` from `Gams.Config` and fails loudly naming `GAMS_MODEL` when the file is
      absent — **and it IS absent in this worktree**; `volume_path.gms` lives in the sibling
      `cfmm-wt/gams` worktree, so the override is mandatory here;
    - computes the binary's sha256 with `Store.Types.sha256_hex`, BARE (a `0x`-prefixed 64-hex
      literal under `offchain/` reddens `sc3_literal_purge`);
    - builds a `RunRequest` with `rr_env = Just (whitelist_for home)` and calls
      `Gams.Run.run_prover`.

    **2. `.cabal`.** Add `Gams.Invoke` to `exposed-modules` and a new
    `executable gams-conformance` stanza in the `store-conformance` family, with `aeson` **in the
    executable only** — it writes the JSON REPORT, a description of an experiment, and never touches
    an artifact byte, which is exactly why BYTE-03's scan is scoped to the library's storage
    modules. Copy that reasoning into the new stanza's comment.

    **3. `offchain/app/GamsConformance.hs`.** The ONLY real-GAMS caller. It performs, in one run,
    every observation the artifact records, then writes `offchain/rig/gams-conformance.json` (path
    resolved through `gams_conformance_path`). Design the document NARROW — every leaf will be
    either asserted by a Tier-C check or pardoned with a written reason, and the sentinel sweep
    re-runs `core_checks` once per (leaf × 6 sentinels). Target **50-70 leaves** and record the
    enumerated count in the summary. The shape:

    ```json
    {
      "gc_complete": true,
      "generatedAt": "<UTC>",
      "gams_path": "<absolute>", "gams_sha256": "<64 bare hex>", "gams_size": 1822256,
      "gams_version": "54.1.0", "gams_build": "37378ce0",
      "gams_version_method": "<how it was read>",
      "conopt_version": "4.39.0", "conopt_method": "<how it was read>",
      "conopt_link_version": "54.1.0", "conopt_so_name": "libconopt464.so",
      "conopt_true_line_index_probe": 38, "conopt_true_line_index_real": 47,
      "exit_codes": {"clean":0,"compile_error":2,"abort":3,"exec_error":3,
                     "missing_file":6,"no_args":0,"action_c":0},
      "action_c": {"exit":0,"artifact_present":false},
      "no_args":  {"exit":0,"stdout_len":1239,"stderr_len":0,"line1":"…","parser_verdict":"…"},
      "version_flag": {"exit":6,"stdout_len":275,"stderr_len":0,"line1":"…","parser_verdict":"…"},
      "golden": {"sha256":"e7b14f38…","len":606},
      "whitelist_run": {"vars":["HOME","LC_ALL","PATH"],"sha256":"…"},
      "minimal_run":   {"vars":["LC_ALL","PATH"],"sha256":"…"},
      "hostile_env_run": {"vars":["GAMSTHREADS=8","GDXCOMPRESS=1","LC_NUMERIC=de_DE.UTF-8",
                                  "GAMSDIR=/nonexistent"],"sha256":"…"},
      "leading_zero_run": {"token":"079228162514264337593543950336","exit":0,"sha256":"…"},
      "voltgt_rendering": {"a":"28e18","b":"2.8e19","sha256_a":"…","sha256_b":"…"},
      "model_sources": [{"path":"<absolute>","sha256":"…"}],
      "argv_module_sha256": "<sha256 of offchain/lib/Gams/Argv.hs>",
      "artifact_module_sha256": "<sha256 of offchain/lib/Gams/Artifact.hs>",
      "observations": [{"name":"…","verdict":"pass"}, …]
    }
    ```

    Two design rules, each with its reason written into the module's haddock:
    - `argv_module_sha256` and `artifact_module_sha256` are the FRESHNESS ORACLE'S subject. The
      model source lives in another worktree and the suite cannot recompute its digest offline —
      a NAMED GAP, handled in task 2 — but the Haskell half IS recomputable from this repository, so
      editing the renderer without re-capturing reddens. This mirrors 23-05's computed freshness
      oracle, which recomputes migration digests from the repo's own `.sql` files.
    - `observations` is a list of NAMED verdicts, so the Tier-C check asserts a SET and never a
      count. 23-05 MEASURED a deleted law passing a count while the SET caught it.

    Every version and parser verdict recorded here must be produced by calling the SHIPPED parsers
    (`parse_gams_version`, `parse_conopt_version`), never by a second implementation inside the
    executable — a recorded field derived from a different expression than the one under test is
    the tautology defect this repository has already been bitten by.

    **4. `offchain/rig/capture-gams-conformance.sh`.** Copy `capture-store-conformance.sh`'s
    discipline exactly:
    - `set -euo pipefail`; `cd "$(git rev-parse --show-toplevel)"`;
    - `CFMM_REQUIRE_GAMS="${CFMM_REQUIRE_GAMS:-1}"` and refuse any other value. **This variable
      lives HERE and nowhere near `cabal test`** — gating the suite on it fails OPEN, which is the
      advertised-and-dead defect one layer up. That is 23-RESEARCH's `CFMM_REQUIRE_DB` ruling
      applied verbatim;
    - fail loudly, NAMING the tool, when `gams`, `jq` or `sha256sum` is absent, and emit NO artifact
      rather than a partial one — the committed file is left exactly as it was;
    - resolve `GAMS_BIN` with `command -v gams` when unset, and EXPORT it; require `GAMS_MODEL`, and
      say in the failure text that `volume_path.gms` lives in the sibling `cfmm-wt/gams` worktree;
    - write the **8-line CONOPT probe** into its own scratch directory — **never under `model/`**,
      which is another workstream's territory:
      ```gams
      variable z, x;  equation e;
      e.. z =e= sqr(x-1) + 1;
      x.lo = -10; x.up = 10;
      model m /e/;  option nlp = conopt;
      solve m using nlp minimizing z;
      ```
    - run every observation in scratch directories, never in the tree, and never write into
      `model/`;
    - end with a VALUE GATE before the artifact is written, in the store capture's shape:
      `jq -r '.action_c.artifact_present'` must print `false` while `jq -r '.action_c.exit'` prints
      `0`; `jq -r '.leading_zero_run.sha256'` must DIFFER from `jq -r '.golden.sha256'`; and
      `jq -r '.conopt_version'` must differ from `jq -r '.conopt_link_version'`. A capture whose
      gate cannot be evaluated must not write an artifact.

    **5. `offchain/rig/README.md`.** Document the new capture beside the existing ones: what it
    produces, the three environment variables, and the one-line command.
  </action>

  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/24-05-t1.log | tail -3; test "$(grep -ciE 'warning' /tmp/24-05-t1.log)" = 0 &amp;&amp; bash -n offchain/rig/capture-gams-conformance.sh &amp;&amp; echo SCRIPT_PARSES</automated>
  </verify>

  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with zero warning lines in `/tmp/24-05-t1.log`.
    - `bash -n offchain/rig/capture-gams-conformance.sh` exits 0.
    - `grep -c 'CFMM_REQUIRE_GAMS' offchain/rig/capture-gams-conformance.sh` is at least `2` (the
      default assignment and the refusal), and
      `grep -rc 'CFMM_REQUIRE_GAMS' offchain/test/Main.hs` is `0`.
    - `grep -c 'model/' offchain/rig/capture-gams-conformance.sh` shows no WRITE into `model/`:
      `grep -cE '> *"?\$?\{?[A-Za-z_]*model/' offchain/rig/capture-gams-conformance.sh` is `0`.
    - `grep -c 'import Gams.Invoke' offchain/app/GamsConformance.hs` is `1`, and
      `grep -rl 'Gams.Invoke' offchain/ --include=*.hs` names exactly two files
      (`offchain/lib/Gams/Invoke.hs` and `offchain/app/GamsConformance.hs`).
    - `grep -cE '^ +aeson,' cfmm-replicationPlank-rpc-api.cabal` increased by exactly 1 relative to
      the previous commit (the new executable stanza), and the library stanza is untouched in that
      respect.
    - `grep -c '0x[0-9a-fA-F]\{64\}' offchain/app/GamsConformance.hs offchain/rig/capture-gams-conformance.sh`
      is `0` — digests are written BARE.
  </acceptance_criteria>

  <done>The capture executable and script exist, the real prover is reachable from exactly two Haskell files, and no `0x`-prefixed digest and no write into `model/` was introduced.</done>
</task>

<task type="auto">
  <name>Task 2: Run the capture, commit the evidence, and make nine checks rest on it</name>
  <files>offchain/rig/gams-conformance.json, offchain/test/Main.hs</files>

  <read_first>
    - `offchain/test/Main.hs` — the eleven `store_conformance_*` checks added at 23-05 (search for
      `store_conformance_is_present_and_fresh` and `store_conformance_verdicts_are_all_pass`);
      they are the exact shape the nine below copy, including the SET-not-count assertion and the
      "name the capture command in the failure" convention.
    - `offchain/rig/store-conformance.json` — the committed sibling artifact's field layout.
    - `.planning/phases/24-gams-invocation-toolchain-identity/24-RESEARCH.md`, the
      Requirement → Test Map rows marked Tier C.
  </read_first>

  <action>
    **1. Run the capture.**

    ```bash
    export GAMS_MODEL=/home/jmsbpp/cfmms-playground/cfmm-wt/gams/model/mev_tax_model_one/volume_path.gms
    bash offchain/rig/capture-gams-conformance.sh
    ```

    The `GAMS_MODEL` path above is machine-specific and belongs in the shell, never in a tracked
    file. Commit the resulting `offchain/rig/gams-conformance.json`. If any recorded value differs
    from the MEASURED table in `<interfaces>`, **record the difference and investigate it — do not
    edit the artifact by hand.** A hand-edited capture is evidence about the editor.

    **2. Nine Tier-C checks**, every one registered in `core_checks`, every one FAILING (never
    skipping) when the artifact is absent and naming
    `bash offchain/rig/capture-gams-conformance.sh` in the failure text.

    - **`gams_conformance_is_present_and_fresh`** — the artifact decodes; `gc_complete` is `true`;
      and the FRESHNESS ORACLE: `argv_module_sha256` and `artifact_module_sha256` equal the digests
      the check RECOMPUTES from `offchain/lib/Gams/Argv.hs` and `offchain/lib/Gams/Artifact.hs`.
      The failure message names the file whose digest moved and says a re-capture is required.
      **The model-source half is a NAMED GAP**, not an omission: add
      `gams_conformance_unrecomputable :: [(String, String)]` carrying
      `("model_sources", <reason>)` with a reason over 200 characters explaining that
      `volume_path.gms` lives in the sibling `cfmm-wt/gams` worktree and is unreachable from this
      repository offline, that the recorded list is asserted on SHAPE instead (non-empty, absolute
      path, 64 bare hex per entry), and that this follows 23-05's `PGSTORE_DSN` precedent — a gap
      that is written down and asserted where it can be, never manufactured a subject for.
    - **`gams_conformance_records_the_measured_exit_codes`** — `exit_codes` equals the map
      `{clean:0, compile_error:2, abort:3, exec_error:3, missing_file:6, no_args:0, action_c:0}`
      as a SET of key/value pairs in BOTH directions. A missing key is a set mismatch, never a
      shorter list. The failure message states the finding this encodes: exit `3` does NOT
      distinguish a named §4 abort from an unhandled execution error.
    - **`gams_conformance_records_action_c_exit_zero_with_no_artifact`** —
      `action_c.exit == 0` AND `action_c.artifact_present == false`. Either one alone is
      satisfiable by an exhibit that stopped exercising the case. This is GAMS-02's firing input
      produced by the REAL binary.
    - **`gams_conformance_records_the_wrong_subject_banners`** — `no_args.exit == 0`,
      `no_args.stdout_len == 1239`, `no_args.stderr_len == 0`, `no_args.parser_verdict` names a
      rejection; `version_flag.exit == 6`, `version_flag.stdout_len == 275`,
      `version_flag.stderr_len == 0`, `version_flag.parser_verdict` names a rejection. The two
      `stderr_len == 0` assertions are the point: a detector reading stderr gets `""` in every mode.
    - **`gams_conformance_records_the_resolved_binary_and_its_digest`** — `gams_path` is absolute
      (`isPrefixOf "/"`), `gams_sha256` is exactly 64 characters all in `[0-9a-f]` with NO `0x`
      prefix, `gams_size > 0`, and `gams_version` equals the `gams_version` recorded elsewhere in
      the document. **SHAPE and cross-consistency, never the machine-specific value** — pinning
      `79cd3a57…` in Haskell would make `cabal test` fail on every other install.
    - **`gams_conformance_records_conopt_and_the_method_that_found_it`** —
      `conopt_version` matches `4.39.*`; `conopt_method` is non-empty; and
      `conopt_version /= conopt_link_version` and `conopt_version /= conopt_so_name`. Equality on
      either would mean the exhibit lost the decoy it exists to distinguish. Also assert
      `conopt_true_line_index_probe /= conopt_true_line_index_real`, which is what retires
      positional parsing as a recorded fact rather than a claim.
    - **`gams_conformance_records_byte_identity_under_a_hostile_environment`** —
      `hostile_env_run.sha256 == golden.sha256` AND `length hostile_env_run.vars >= 4`. An empty
      hostile set makes the exhibit vacuous, which is why the second conjunct exists.
    - **`gams_conformance_records_the_minimal_whitelist_reproducing_the_golden_bytes`** —
      `whitelist_run.sha256 == golden.sha256`, `minimal_run.sha256 == golden.sha256`, and every
      variable named in `whitelist_run.vars` appears in `Gams.Env.whitelist_keys` (in both
      directions). A recorded whitelist containing a variable `Gams.Env` does not name means the
      capture and the library have drifted.
    - **`gams_conformance_records_the_leading_zero_changing_the_bytes`** —
      `leading_zero_run.exit == 0` AND `leading_zero_run.sha256 /= golden.sha256`, plus
      `voltgt_rendering.sha256_a == golden.sha256` and
      `voltgt_rendering.sha256_a == voltgt_rendering.sha256_b`. Together these record the asymmetry
      that makes the renderer load-bearing: the two echoed STRING fields are token-sensitive and
      the `Scalar` is not. Assert the INEQUALITY rather than pinning `d64a7b32…` — the digest is a
      product of this machine's run and the claim is that it DIFFERS.
    - **`gams_conformance_verdicts_are_all_pass`** — every entry of `observations` has verdict
      `pass`, AND the observation-name SET equals a pinned `expected_gams_observations` list, in
      both directions. A count would pass a deletion; 23-05 MEASURED exactly that.
  </action>

  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/24-05-t2.log | tail -3; test "$(grep -ciE 'warning' /tmp/24-05-t2.log)" = 0 &amp;&amp; cabal test 2>&amp;1 | tail -4</automated>
  </verify>

  <acceptance_criteria>
    - `offchain/rig/gams-conformance.json` exists, is tracked, and `jq -e '.gc_complete' ` prints
      `true`.
    - `jq -r '.action_c.exit, .action_c.artifact_present'` prints `0` then `false`.
    - `jq -r '.golden.sha256'` prints
      `e7b14f384ab4c027be5450218a52040110d45dbaddbbfb0bb7bd5ab707d0d884` and `jq -r '.golden.len'`
      prints `606`.
    - `[ "$(jq -r '.leading_zero_run.sha256')" != "$(jq -r '.golden.sha256')" ]` is true, and
      `jq -r '.leading_zero_run.exit'` prints `0`.
    - `[ "$(jq -r '.conopt_version')" != "$(jq -r '.conopt_link_version')" ]` is true.
    - `cabal test` exits 0 with `grep -c '^FAIL '` = `0`, and nine `^PASS gams_conformance_*` lines
      appear.
    - **FIRING OBSERVATIONS**, each recorded verbatim, each restoring the artifact to its committed
      sha256 afterwards (record that sha256 before starting):
      1. move the artifact aside → all nine checks FAIL, each naming the capture command, and NONE
         skips;
      2. set `action_c.artifact_present` to `true` in a scratch copy reached through
         `GAMS_CONFORMANCE` → `gams_conformance_records_action_c_exit_zero_with_no_artifact` FAILS;
      3. set `conopt_link_version` equal to `conopt_version` in a scratch copy →
         `gams_conformance_records_conopt_and_the_method_that_found_it` FAILS;
      4. DELETE one entry from `observations` in a scratch copy →
         `gams_conformance_verdicts_are_all_pass` FAILS as a SET mismatch — record whether a count
         over the same document would still have passed;
      5. append one space to `offchain/lib/Gams/Argv.hs` →
         `gams_conformance_is_present_and_fresh` FAILS naming that file; revert the file
         sha256-identically.
    - Every mutation is performed on a SCRATCH COPY reached through `GAMS_CONFORMANCE`, never on the
      committed file. `git status --porcelain offchain/rig/gams-conformance.json` is empty after
      each observation.
  </acceptance_criteria>

  <done>The committed artifact exists, records the real toolchain, and nine checks fail loudly when it is absent, stale, or has lost the decoy it exists to distinguish.</done>
</task>

<task type="auto">
  <name>Task 3: The sixth swept artifact, three re-measured floors, and every leaf accounted for</name>
  <files>offchain/test/Main.hs</files>

  <read_first>
    - `offchain/test/Main.hs` lines 5202-5250 (`sentinels`, `MutableArtifact`, `swept_artifacts`,
      `unswept_artifacts`), 5293-5301 (`expensive_checks`, `cheap_first`), 5348-5432
      (`reader_set`, `sweep_one` — read the `readable` filter and the `hot` heuristic), 5517-5600
      (`absorbed_by_design` and its reason constants), 5680-5712 (`sentinel_pair_floor` and
      `artifact_field_floors`, with the 23-05 arithmetic).
    - `.planning/phases/23-postgres-foundation-byte-exact-schema/23-05-SUMMARY.md`, the
      "BARE PATH TURNED OUT TO BE PREDICTABLE" section — how a field that the harness reported as
      absorbed was turned into an assertion rather than a pardon.
  </read_first>

  <action>
    **1. Register the sixth artifact.** Add to `swept_artifacts`:

    ```haskell
    , MutableArtifact "GAMS_CONFORMANCE" gams_conformance_path "gams-conformance.json"
    ```

    with a comment in the 23-05 shape: this is the first artifact whose subject is a SOLVER, and
    every real-GAMS observation in this phase is a recorded value here and nowhere else — so a leaf
    of it that nothing reads is a solver-only claim that survived the whole phase unasserted.

    **2. Run the harness and read what it says.** `cabal test` will report the artifact's absorbed
    pairs. For each one, make a DELIBERATE choice, in this order of preference:
    a. **assert the field** — add or extend a Tier-C check so the pair is caught. Prefer this. 23-05
       turned three of four reported-absorbed fields into assertions rather than pardons, and the
       one it pardoned (`generatedAt`) is the only one with no comparand;
    b. **pardon it** with an `absorbed_by_design` entry carrying the field path, the per-sentinel
       occurrence counts, and a WRITTEN reason. `gams-conformance.json.generatedAt` is expected to
       be the archetypal entry and may reuse the existing `reason_generated_at`.

    An entry with no reason, or a reason that says "not needed", is not acceptable — the whole value
    of the list is that it is a written record of a decision somebody had to defend.

    **3. Re-measure all three floors. RAISE UNTIL THE HARNESS REPORTS WHAT IT REACHED; never do
    arithmetic.**
    - `artifact_field_floors` — add `("gams-conformance.json", <measured>)` and **re-measure the
      other five in the SAME run**, recording whether any came back different. A new entry beside
      five stale ones records the tree as it was on the day someone last thought about it.
    - `sentinel_pair_floor` — currently `3250`. Raise it to the measured total and write the
      arithmetic check into the haddock the way 23-05 did: `<new> - 3250 = <the new artifact's
      exercised pairs>`, with the leaf count and the identity-skip count, which is what says the
      five older artifacts still contribute exactly 3250.
    - `purge_file_floor` and `credential_scan_floor` — this plan adds `Gams/Invoke.hs`,
      `app/GamsConformance.hs` and `rig/capture-gams-conformance.sh` (all SCANNED types) plus
      `rig/gams-conformance.json` (scanned by the credential scan only). Re-measure both cold with
      the two `find … | wc -l` commands and update both haddocks with the new extension census.

    **4. Guard the wall.** If any of the new checks is slow, add it to `expensive_checks` so
    `cheap_first` defers it — the ordering matters because `sweep_one` stops at the FIRST objection.
    Record the `cabal test` wall before and after this plan against the 900 s budget, and state the
    per-artifact delta.

    **5. The honest gap.** If any Tier-C observation could not be captured on this machine, add it
    to a named list with a written reason rather than dropping it — 23-05's guard #13 precedent: a
    guard with no observation is reported as a phase-level finding, named, never omitted.
  </action>

  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/24-05-t3.log | tail -3; test "$(grep -ciE 'warning' /tmp/24-05-t3.log)" = 0 &amp;&amp; { time cabal test; } 2>&amp;1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with zero warnings; `cabal test` exits 0 with
      `grep -c '^FAIL '` = `0`, including `sentinel_falsification_harness`.
    - `grep -c 'gams-conformance.json' offchain/test/Main.hs` is at least `2` (the swept-artifact
      entry and the field floor).
    - `grep -c 'sentinel_pair_floor = 3250' offchain/test/Main.hs` is `0` — the floor moved, and the
      haddock carries the arithmetic check with the measured leaf count.
    - `grep -c '("gams-conformance.json", ' offchain/test/Main.hs` is `1`, and the summary states
      whether each of the other five `artifact_field_floors` entries came back unchanged.
    - Neither `purge_file_floor` nor `credential_scan_floor` holds the value it had before this
      plan, and the summary pastes both `find … | wc -l` commands with their outputs.
    - Every absorbed pair the harness reports is either now CAUGHT or listed in
      `absorbed_by_design` with a reason of at least 120 characters; the summary lists each decision
      and which way it went.
    - The `cabal test` wall is recorded before and after, with the delta attributed.
  </acceptance_criteria>

  <done>The sixth artifact is swept, every leaf is asserted or pardoned with a written reason, and all four tree-derived floors carry values measured in this run.</done>
</task>

</tasks>

<verification>
- `cabal build --enable-tests -j all` — zero warnings. The bare `cabal build -j all` appears nowhere.
- `cabal test` — all checks pass, `FAIL` count 0, including the sentinel harness.
- Both structural greps over `offchain/test/Main.hs` return `0` (DB-free and GAMS-free).
- `bash offchain/rig/capture-gams-conformance.sh` re-run from scratch produces an artifact whose
  `observations` are all `pass`.
- Fourteen firing observations across the three tasks recorded verbatim; the committed artifact's
  sha256 is byte-identical before and after all of them.
</verification>

<success_criteria>
- The real toolchain's behaviour is committed evidence, and nine checks fail rather than skip when
  it is absent.
- The exhibit still contains the decoys it exists to distinguish, asserted as inequalities.
- The leading-zero run is on record as exit 0, gates green, different bytes.
- Editing the renderer without re-capturing reddens the suite; the un-recomputable half is a named,
  asserted gap rather than an omission.
- No leaf of the new artifact is unread by every check without a written pardon.
</success_criteria>

<output>
After completion, create
`.planning/phases/24-gams-invocation-toolchain-identity/24-05-SUMMARY.md` with the fourteen firing
observations, the enumerated leaf count, every absorbed-pair decision and which way it went, all
four re-measured floors with the commands that produced them, and the `cabal test` wall before and
after.
</output>
