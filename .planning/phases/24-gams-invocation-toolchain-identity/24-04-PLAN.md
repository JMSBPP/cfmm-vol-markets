---
phase: 24-gams-invocation-toolchain-identity
plan: 04
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - offchain/lib/Gams/Run.hs
  - offchain/test/Main.hs
autonomous: true
requirements: [GAMS-03, GAMS-05, GAMS-06]

must_haves:
  truths:
    - "A hung GRANDCHILD — not a direct child — is terminated and reaped, and the negative control shows the same stub SURVIVING when only the direct child is signalled"
    - "A child writing more than 1 MB to stderr completes without deadlock"
    - "A timed-out run yields Aborted and can never yield an artifact"
    - "A run that exits 0 with a valid artifact but no version banner in its log ABORTS — a completed run with an empty version component is unreachable"
    - "The whitelisted child's environment is the whitelist; the inherited child's environment names variables the whitelist excludes"
    - "offchain/test/Main.hs cannot name the real solver: a three-token grep, with a proven positive control, returns 0"
  artifacts:
    - path: "offchain/test/Main.hs"
      provides: "seven Tier-B/structural checks, the GAMS-free grep, and the three GAMS override registrations"
      contains: "a_hung_grandchild_is_terminated_and_reaped"
  key_links:
    - from: "offchain/test/Main.hs"
      to: "Gams.Config"
      via: "the three GAMS variables registered in advertised_overrides / unprobed_overrides and asserted in both directions"
      pattern: "gams_conformance_env_var"
    - from: "the suite"
      to: "the real prover"
      via: "NOTHING — asserted by the GAMS-free structural grep"
      pattern: "Gams\\\\.Invoke\\|CFMM_REQUIRE_GAMS"
---

<objective>
The three hazards that survive a naive test, and the structural guarantee that `cabal test` cannot
reach the real solver.

Purpose: MEASURED — `System.Timeout.timeout` around `readProcessWithExitCode` terminates AND reaps a
direct child with no orphan, so a GAMS-05 test written against `sh -c 'exec sleep 300'`
**cannot fail**. Against `sh -c 'sleep 297 & wait'` the grandchild SURVIVES at PPID 1, and GAMS runs
CONOPT at `Solvelink=2` as a separate process — verbatim in the log,
`--- Executing CONOPT (Solvelink=2)` — so the grandchild is not hypothetical, it is the actual case.
A check that passes here while a `conopt4` process burns a core is the defect class relocated into
the test.

Output: seven checks, the GAMS-free structural grep with a proven positive control, and three
override registrations following 23-05's `PGSTORE_DSN` ruling.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-gams-invocation-toolchain-identity/24-RESEARCH.md
@.planning/phases/24-gams-invocation-toolchain-identity/24-03-SUMMARY.md
@offchain/lib/Gams/Run.hs
@offchain/lib/Gams/Env.hs
@offchain/lib/Gams/Config.hs
@offchain/test/Main.hs

<interfaces>
<!-- From offchain/test/Main.hs — the override sweep this plan extends. -->

```haskell
data OverrideProbe = OverrideProbe
  { ov_var :: String, ov_resolve :: IO FilePath, ov_probe :: IO (Maybe String) }
advertised_overrides :: [OverrideProbe]      -- line 3639. Five entries at Phase 23's end.
data UnprobedOverride = UnprobedOverride { uo_var :: String, uo_resolve :: …, uo_reason :: String }
unprobed_overrides :: [UnprobedOverride]     -- line 3785. One entry: PGSTORE_DSN.
```

`store_overrides_are_probed_or_named_as_gaps` (line 3804) asserts four things, and this plan
extends the FIRST of them — the variable names in both lists are compared to the constants in the
config module, so a rename there and nowhere else reddens:

```haskell
store_vars = [store_conformance_env_var, pgstore_dsn_env_var]
uncovered  = [v | v <- store_vars, v `notElem` probed, v `notElem` unprobed]
both       = [v | v <- probed, v `elem` unprobed]
thin       = [uo_var o | o <- unprobed_overrides, length (uo_reason o) < 200]
```

23-05's ruling, verbatim in `reason_dsn_has_no_offline_consumer`: an entry belongs in
`unprobed_overrides` when its consumer is unreachable from `cabal test` BY CONSTRUCTION, and
**a validator written only to be probed is worse than the gap, because it looks right** — its
rejection would prove only that a function written to reject rejects.

From `.planning/phases/.../24-RESEARCH.md` M10, the four MEASURED probe outcomes:

| Probe | Result |
|---|---|
| child writes 2,000,000 bytes to stderr via `readProcessWithExitCode` | `ExitSuccess`, `err=2000000`, no deadlock |
| `timeout 2s` around a DIRECT child `exec sleep 300` | terminated AND reaped, no orphan |
| `timeout 2s` around `sh -c 'sleep 297 & echo $! >f; wait'` | grandchild **SURVIVES**, `PPID 1` |
| `/usr/bin/timeout -k 1 2` around the same | exits **124**, grandchild **gone** |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The timeout owns the process group — proven on a grandchild</name>
  <files>offchain/test/Main.hs</files>

  <read_first>
    - `.planning/phases/24-gams-invocation-toolchain-identity/24-RESEARCH.md` M10 in full, and M3's
      rider (`--- Executing CONOPT (Solvelink=2)` — the reason the grandchild is the real case).
    - `offchain/lib/Gams/Run.hs` — the `/usr/bin/timeout -k` invocation and the backstop.
    - The stub helper `write_stub` added by plan 24-03.
  </read_first>

  <behavior>
    - a stub writing 2,000,000 bytes to stderr and 5 to stdout, then writing a valid artifact and
      exiting 0 → `Produced`, with `BS.length (cs_stderr streams) == 2000000`
    - a stub that backgrounds `sleep 300` and `wait`s, with `rr_budget_s = 2` → `Aborted` with the
      exit code `124`, and the backgrounded pid is GONE within 500 ms
    - the same stub, driven through a direct-child-only kill → the backgrounded pid SURVIVES (the
      negative control, observed once and then cleaned up)
    - a stub that writes a valid artifact and then hangs → `Aborted`, never `Produced`
  </behavior>

  <action>
    Three checks, all registered in `core_checks`.

    **`a_stderr_flood_completes_without_deadlock`.** The stub:

    ```sh
    #!/bin/sh
    for a in "$@"; do case "$a" in curdir=*) D=${a#curdir=};; esac; done
    printf 'hello'                                  # 5 bytes to stdout
    yes x | head -c 2000000 >&2                     # 2,000,000 bytes to stderr
    cat > "$D/volume_path.json" <<'JSON'
    <the 606 golden bytes, read from offchain/rig/volume-path-golden.json and interpolated>
    JSON
    cat > "$D/volume_path.log" <<'LOG'
    --- Job volume_path.gms Start 08/16/26 15:52:25 54.1.0 37378ce0 LEX-LEG x86 64bit/Linux
        C O N O P T   version 4.39.0
    LOG
    exit 0
    ```

    Assert the call RETURNS (an implementation that waits before draining hangs here and the check
    times out), the outcome is `Produced`, and `BS.length (cs_stderr …)` is exactly `2000000` —
    an equality, so a truncating drain is caught too. Give the check its own generous
    `System.Timeout.timeout` so a regression fails the check rather than hanging the suite.

    **`a_hung_grandchild_is_terminated_and_reaped`.** THE CHECK THIS PHASE IS MOST AT RISK OF
    WRITING WRONGLY. **The stub MUST be a grandchild**, not a direct child:

    ```sh
    #!/bin/sh
    sleep 300 &
    echo $! > <PIDFILE>          # an ABSOLUTE path OUTSIDE the run directory, interpolated by the
    wait                         # check when it writes the stub -- the run directory is removed.
    ```

    Drive it with `rr_budget_s = 2`, `rr_kill_after_s = 1` (the hung-child budget is **≤ 2 s**;
    this check is run once per full `core_checks` pass and `reader_set` makes one pass per swept
    artifact, so a larger budget multiplies). Then:
    1. read the pid from `<PIDFILE>`;
    2. `threadDelay 500000`;
    3. assert `doesDirectoryExist ("/proc/" ++ pid)` is `False`. If it is `True`, read
       `/proc/<pid>/stat` and put its contents in the failure message, and kill the pid so the
       check does not leak a process on its way to failing;
    4. assert the outcome is `Aborted` with recorded exit code `124` — `timeout(1)`'s expiry code,
       which collides with nothing in the mod-256 image of GAMS's table.

    **THE NEGATIVE CONTROL — a ONE-TIME OBSERVATION, not a permanent check.** During execution,
    drive the SAME grandchild stub through a variant that uses `System.Timeout.timeout` around
    `readProcessWithExitCode` directly (no `/usr/bin/timeout` wrapper) and OBSERVE the backgrounded
    pid still alive at `PPID 1`. Record `ps -o pid,ppid,stat,cmd -p <pid>` verbatim in the summary,
    then kill it. Without this observation the check is green for a reason nobody has verified, and
    MEASURED, that is precisely the shape a direct-child test has: it cannot fail.

    **`a_timed_out_run_yields_Aborted_and_no_artifact`.** A stub that writes a VALID artifact and
    a valid log FIRST and then hangs. Assert the outcome is `Aborted`, never `Produced` — a
    timeout after the bytes exist is the case where a layer that checks artifact presence before
    the exit code would wrongly succeed. Assert also that the run directory is gone. Pair it with
    the compile-level fact recorded in 24-03: there is no `ProverOutcome -> ProverArtifact`, so
    "never an output row" is unrepresentable rather than merely untested.
  </action>

  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/24-04-t1.log | tail -3; test "$(grep -ciE 'warning' /tmp/24-04-t1.log)" = 0 &amp;&amp; { time cabal test; } 2>&amp;1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with zero warnings; `cabal test` exits 0,
      `grep -c '^FAIL '` is `0`.
    - Three `^PASS ` lines: `a_stderr_flood_completes_without_deadlock`,
      `a_hung_grandchild_is_terminated_and_reaped`, `a_timed_out_run_yields_Aborted_and_no_artifact`.
    - `grep -c 'sleep 300 &' offchain/test/Main.hs` is at least `1` — the stub backgrounds its
      sleep, so the process under test is a GRANDCHILD.
    - `grep -c '/proc/' offchain/test/Main.hs` is at least `1` — liveness is read from procfs, not
      inferred.
    - The negative control's `ps` output is quoted verbatim in the summary, showing `PPID 1` and the
      surviving `sleep`, and the pid was killed afterwards.
    - `pgrep -a 'sleep 3' | wc -l` is `0` after `cabal test` — the suite leaks no process.
    - The `cabal test` wall is recorded before and after; the delta attributable to the ≤ 2 s
      hung-child check is stated.
    - **FIRING OBSERVATION:** change `Gams.Run` to spawn the binary directly instead of through
      `/usr/bin/timeout` (keeping only the Haskell backstop) → `a_hung_grandchild_is_terminated_and_reaped`
      FAILS, quoting `/proc/<pid>/stat`. Record the verbatim FAIL line, restore, confirm
      `git status --porcelain offchain/lib/Gams/` is empty.
  </acceptance_criteria>

  <done>The grandchild is the subject, its death is read from procfs, the negative control has been observed leaving it alive, and the suite leaks no process.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The environment vector, and a version that cannot be missing</name>
  <files>offchain/test/Main.hs</files>

  <read_first>
    - `.planning/phases/24-gams-invocation-toolchain-identity/24-RESEARCH.md` M9 and **Correction 2**
      — why the "inheriting differs" half is discharged against the child's environment vector and
      not against artifact bytes.
    - `offchain/lib/Gams/Env.hs` and its haddock recording the honest limit.
  </read_first>

  <behavior>
    - the env-printing stub run with `rr_env = Just (whitelist_for home)`: every whitelist pair is
      present with its exact value, and the child's remaining keys are a subset of a MEASURED,
      named shell-injected set
    - the same stub run with `rr_env = Nothing`: the child's environment is a strict SUPERSET and
      names at least one variable that is in neither the whitelist nor the shell-injected set
    - a stub that exits 0 and writes a VALID artifact but a log with NO job banner →
      `Aborted (VersionUnreadable …)`
    - a stub that writes the banner to STDERR while the log file has none → `Aborted` as well
  </behavior>

  <action>
    Three checks, all registered in `core_checks`.

    **`the_child_environment_is_exactly_the_whitelist`.** The stub:

    ```sh
    #!/bin/sh
    env > <ENVFILE>     # an ABSOLUTE path outside the run directory, interpolated by the check
    exit 0
    ```

    Run it with `rr_env = Just (whitelist_for home)`. Parse `<ENVFILE>` into `[(String,String)]`.
    Assert:
    1. every pair in the whitelist is present with its EXACT value (`LC_ALL=C`, `PATH=/usr/bin`,
       `HOME=<home>`);
    2. the child's keys minus the whitelist keys are a subset of
       `shell_injected_env_keys :: [String]` — **MEASURE this set at execution time** by printing
       the actual difference, and write it into the constant with the reason (`/bin/sh` exports
       these to its own children; they are the shell's, not the caller's). If any key appears that
       the list does not name, the check FAILS naming it. Do NOT copy a set from this plan.

    **`an_inherited_environment_is_observed_to_differ`.** The SAME stub, run with
    `rr_env = Nothing`. Assert the captured environment is a strict superset of the whitelisted
    one, and that it names at least one variable that is in neither the whitelist nor
    `shell_injected_env_keys`. Name that variable in the success path's recorded evidence.
    This is Correction 2's honest form: it is what actually proves the whitelist is IN FORCE, and
    a whitelist that silently fell back to inheritance is exactly what it catches. The failure
    message must state why this is not asserted on bytes — MEASURED, four hostile ambient variables
    changed nothing, and `locale -a` on this machine offers only `C`, `C.utf8`, `en_US.utf8` and
    `POSIX`, so there is no comma-decimal locale to observe a byte difference with.

    **`version_detection_failure_aborts_the_invocation`.** Two stubs:
    1. exits 0, writes the VALID 606-byte artifact, writes a `volume_path.log` containing only
       `"--- Executing CONOPT (Solvelink=2)\n"` — no job banner;
    2. exits 0, writes the valid artifact, writes NO log at all, and prints the real banner line to
       STDERR instead.

    Both must give `Aborted (VersionUnreadable …)`. Accepting either means a run COMPLETED with an
    empty version component, which is the poisoned-row scenario this phase's sequencing exists to
    prevent — and MEASURED, the real tool leaves stderr at 0 bytes in every mode, so a detector
    that read stderr would get `""` always. Say both sentences in the failure message.
  </action>

  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/24-04-t2.log | tail -3; test "$(grep -ciE 'warning' /tmp/24-04-t2.log)" = 0 &amp;&amp; cabal test 2>&amp;1 | tail -4</automated>
  </verify>

  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with zero warnings; `cabal test` exits 0 with
      `grep -c '^FAIL '` = `0`.
    - Three `^PASS ` lines for the three check names.
    - `grep -c 'shell_injected_env_keys' offchain/test/Main.hs` is at least `2` (the constant and
      its consumer), and its haddock states the measurement that produced it, with the date.
    - The summary quotes the two captured environment vectors: the whitelisted one in full (it is
      short) and the count plus one named excluded variable from the inherited one.
    - **FIRING OBSERVATIONS**, applied, RED observed with verbatim FAIL lines, restored
      sha256-identical:
      1. change `Gams.Run` to pass `env = Nothing` unconditionally →
         `the_child_environment_is_exactly_the_whitelist` FAILS naming an inherited variable;
      2. delete `LC_ALL` from `Gams.Env.whitelist_for` → both
         `the_whitelist_pins_LC_ALL_C_and_admits_no_GAMS_variable` (plan 24-02) and
         `the_child_environment_is_exactly_the_whitelist` FAIL;
      3. make `Gams.Run` fall back to a placeholder version when the banner is missing →
         `version_detection_failure_aborts_the_invocation` FAILS.
  </acceptance_criteria>

  <done>The whitelist is proven in force by comparing two real environment vectors, and a missing banner aborts the run rather than yielding an empty key component.</done>
</task>

<task type="auto">
  <name>Task 3: The suite cannot name the real solver, and the three GAMS overrides are registered or named as gaps</name>
  <files>offchain/test/Main.hs</files>

  <read_first>
    - `offchain/test/Main.hs` lines 3620-3700 (`OverrideProbe`, `advertised_overrides`,
      `every_advertised_override_is_honoured` and its three assertions) and 3740-3840
      (`UnprobedOverride`, `reason_dsn_has_no_offline_consumer`,
      `store_overrides_are_probed_or_named_as_gaps` with its four assertions and the 200-character
      reason floor).
    - `offchain/lib/Gams/Config.hs` — the three variables and their constants.
    - `.planning/phases/23-postgres-foundation-byte-exact-schema/23-05-SUMMARY.md`, the
      `PGSTORE_DSN` ruling — the precedent this task follows and must not weaken.
  </read_first>

  <action>
    **1. The GAMS-free structural grep — the DB-free grep's twin.** Add
    `the_suite_never_names_the_real_solver`:

    ```haskell
    gams_free_pattern :: String     -- BUILT, never written as one literal, so this file stays
    gams_free_pattern =             -- inside its own scan the way credential_pattern does
      intercalate "|" ["Gams" ++ "\\.Invoke", "CFMM_REQUIRE" ++ "_GAMS", "/usr/" ++ "gams"]
    ```

    Scan `offchain/test/Main.hs` for it; the result must be EMPTY. Three tokens, exactly mirroring
    the DB-free grep (`Store\.Postgres|CFMM_REQUIRE_DB|connectPostgreSQL`), and each is a real
    reachability statement:
    - `Gams.Invoke` is the module that resolves the REAL binary and model from `Gams.Config`; it is
      imported ONLY by `offchain/app/GamsConformance.hs` (plan 24-05). `Gams.Run` — the testable IO
      edge, which takes the binary path explicitly — is deliberately NOT on the list, because the
      Tier-B checks drive it against stubs and that is the whole design.
    - `CFMM_REQUIRE_GAMS` belongs to the capture script and nowhere near `cabal test`; the
      23-RESEARCH `CFMM_REQUIRE_DB` ruling applies verbatim — gating the suite on it fails OPEN.
    - `/usr/gams` is the absolute path of the real binary on this machine.

    Give it a PROVEN POSITIVE CONTROL in `aeson_positive_control`'s shape: seed one bait file
    containing all three tokens and one clean file, assert grep NAMES the bait and does NOT name the
    clean file, and order the control FIRST. Absence may not read as success until the pattern has
    been shown matching.

    **2. `GAMS_CONFORMANCE` → `advertised_overrides`.** Same shape as `STORE_CONFORMANCE` exactly:
    it resolves a `FilePath` and the Tier-C checks of plan 24-05 read it through the shared JSON
    reader, so all three of `probe_override`'s assertions have a real subject. Add the
    `OverrideProbe` with a `json_probe` naming the capture command
    (`bash offchain/rig/capture-gams-conformance.sh`).

    **3. `GAMS_BIN` and `GAMS_MODEL` → `unprobed_overrides`, each with a written reason.** Follow
    23-05's ruling and do NOT weaken `probe_override`: their consumer is `Gams.Invoke`, which the
    GAMS-free grep above makes unreachable from `cabal test` BY CONSTRUCTION, so the third assertion
    (the consumer fails NAMING the resolved value) has no subject. The two ways to manufacture one
    are both rejected: importing `Gams.Invoke` breaks the GAMS-free property on its way to
    enforcing it, and a validator written only to be probed is a registered-but-vacuous probe — the
    exact defect the sweep exists to catch, installed to close the sweep's own list. Each reason
    must exceed the 200-character floor and say which of the two rejections applies, plus where the
    real evidence lives: the capture exports both variables and records a resolved absolute path
    and a binary digest that no unconsumed variable could have produced. `GAMS_MODEL`'s reason must
    additionally record that **`volume_path.gms` does not exist in this worktree** — it lives in the
    sibling `cfmm-wt/gams` worktree — so this variable is not optional here.

    **4. Extend the both-directions assertion.** In
    `store_overrides_are_probed_or_named_as_gaps`, extend the variable list from
    `[store_conformance_env_var, pgstore_dsn_env_var]` to also carry `gams_bin_env_var`,
    `gams_model_env_var` and `gams_conformance_env_var`, read from `Gams.Config` — the only place
    each is named. The disjointness and reason-floor assertions then cover them without new code.
    Update the check's haddock to say the list is now five variables across two config modules.

    **5. Floors.** No new file lands in this plan, but re-measure `purge_file_floor` and
    `credential_scan_floor` anyway and record the values — the rule is that a floor is re-measured
    whenever a plan is already editing this file, and the two previous plans moved the tree.
  </action>

  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/24-04-t3.log | tail -3; test "$(grep -ciE 'warning' /tmp/24-04-t3.log)" = 0 &amp;&amp; cabal test 2>&amp;1 | tail -4</automated>
  </verify>

  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with zero warnings; `cabal test` exits 0 with
      `grep -c '^FAIL '` = `0`.
    - `cabal test 2>&1 | grep -c '^PASS the_suite_never_names_the_real_solver$'` is `1`.
    - `grep -cE 'Gams\.Invoke|CFMM_REQUIRE_GAMS|/usr/gams' offchain/test/Main.hs` is `0` — and the
      pattern in the source is BUILT by concatenation, so this file stays inside its own scan.
    - `grep -cE 'Store\.Postgres|CFMM_REQUIRE_DB|connectPostgreSQL' offchain/test/Main.hs` is still
      `0` — the DB-free twin is intact.
    - `grep -c 'GAMS_CONFORMANCE' offchain/test/Main.hs` is `0` — the variable is referenced through
      `gams_conformance_env_var`, never re-spelled (which is what makes a rename in the config
      module redden the sweep).
    - `cabal test 2>&1 | grep -c '^PASS every_advertised_override_is_honoured$'` is `1` and the same
      for `store_overrides_are_probed_or_named_as_gaps`.
    - **FIRING OBSERVATIONS**, applied, RED observed with verbatim FAIL lines, restored:
      1. add the literal `Gams.Invoke` in a comment in `Main.hs` →
         `the_suite_never_names_the_real_solver` FAILS naming the line;
      2. remove `gams_conformance_env_var` from the extended variable list while leaving the
         `OverrideProbe` in place → the disjointness/coverage assertion behaviour is exercised;
         re-add it and record what was observed;
      3. shorten one `unprobed_overrides` reason below 200 characters →
         `store_overrides_are_probed_or_named_as_gaps` FAILS on the `thin` arm.
  </acceptance_criteria>

  <done>A three-token grep with a proven positive control says the suite cannot name the real solver, and all five config variables are each in exactly one override list with a written reason.</done>
</task>

</tasks>

<verification>
- `cabal build --enable-tests -j all` — zero warnings. The bare `cabal build -j all` appears nowhere.
- `cabal test` — all checks pass, `FAIL` count 0, wall recorded against the 900 s budget.
- Both structural greps over `offchain/test/Main.hs` return `0`: the DB-free three-token grep and
  the new GAMS-free three-token grep.
- `pgrep -a 'sleep 3' | wc -l` is `0` after the suite.
- Seven firing observations recorded verbatim with sources restored sha256-identical.
</verification>

<success_criteria>
- The timeout is proven against a GRANDCHILD, with a negative control that OBSERVED the same stub
  surviving a direct-child-only kill.
- A 2,000,000-byte stderr flood completes and its length is asserted as an equality.
- A run with a valid artifact and no banner ABORTS.
- The whitelist is proven in force by comparing two captured environment vectors.
- `cabal test` is structurally incapable of naming the real solver.
</success_criteria>

<output>
After completion, create
`.planning/phases/24-gams-invocation-toolchain-identity/24-04-SUMMARY.md` with the seven firing
observations, the negative control's verbatim `ps` output, the measured
`shell_injected_env_keys` set, and the `cabal test` wall before and after.
</output>
