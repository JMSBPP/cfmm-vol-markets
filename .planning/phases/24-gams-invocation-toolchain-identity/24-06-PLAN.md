---
phase: 24-gams-invocation-toolchain-identity
plan: 06
type: execute
wave: 6
depends_on: ["24-05"]
files_modified:
  - offchain/migrations/003_version_columns_nonempty.sql
  - offchain/lib/Store/Schema.hs
  - offchain/rig/store-conformance.json
  - offchain/test/Main.hs
autonomous: true
requirements: [GAMS-03]

must_haves:
  truths:
    - "An empty gams_ver or conopt_ver is REJECTED by the database, observed against a real server — `NOT NULL` does not forbid `''` and MEASURED, the schema would have stored it"
    - "The migration list is three long, gapless, and matches the directory in both directions"
    - "The store conformance artifact is re-captured, its computed freshness oracle is green, and all its verdicts still pass"
  artifacts:
    - path: "offchain/migrations/003_version_columns_nonempty.sql"
      provides: "a CHECK constraint making an empty version string unstorable"
      contains: "check ("
    - path: "offchain/rig/store-conformance.json"
      provides: "the re-captured evidence, now covering three migrations and the new constraint"
  key_links:
    - from: "offchain/lib/Store/Schema.hs"
      to: "offchain/migrations/"
      via: "expected_migrations, compared to the directory in both directions"
      pattern: "003_version_columns_nonempty.sql"
    - from: "offchain/test/Main.hs"
      to: "offchain/rig/store-conformance.json"
      via: "the computed freshness oracle recomputing every migration's md5 from the repo's own .sql files"
      pattern: "expected_migrations"
---

<objective>
Close the empty version string one layer below the Haskell guard.

Purpose: MEASURED — `offchain/migrations/001_model_run.sql` declares `gams_ver text not null` and
`conopt_ver text not null`, and **`NOT NULL` does not forbid `''`**. The schema will happily store
the empty string, which is precisely the poisoned-row scenario this phase's sequencing exists to
prevent, sitting one layer below the guard that prevents it. `Gams.Version`'s abstract newtype is
the primary defence and it is already in place; this is the layer that outlives every Haskell
refactor, and the phase is already moving the floors it disturbs.

This plan is deliberately LAST. Adding a migration forces a re-capture, because 23-05's freshness
oracle RECOMPUTES each migration's md5 from the repository's own `.sql` files — so the re-capture
should happen once, after the tree has stopped moving.

Output: migration `003`, a three-entry `expected_migrations`, a re-captured
`store-conformance.json`, and the observation that the constraint rejects.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-gams-invocation-toolchain-identity/24-RESEARCH.md
@.planning/phases/24-gams-invocation-toolchain-identity/24-05-SUMMARY.md
@offchain/lib/Store/Schema.hs
@offchain/rig/capture-store-conformance.sh
@offchain/test/Main.hs

<interfaces>
<!-- From offchain/lib/Store/Schema.hs. The manifest is PURE DATA and is deliberately NOT derived
     by globbing the directory: a globbed manifest agrees with whatever is on disk by construction,
     which is a statement about listDirectory and not about the schema. -->

```haskell
expected_migrations :: [(Int, FilePath)]
expected_migrations = [ (1, "001_model_run.sql"), (2, "002_byte_corpus.sql") ]
identity_constraint_name    = "model_run_identity"
identity_constraint_columns = ["model", "key_scheme", "key"]
```

Its haddock records the source-read that makes the directory check strict: `postgresql-migration`
resolves through `scriptsInDirectory dir = sort <$> listDirectory dir` and applies
`executeMigration` to EVERY entry it finds — **there is no extension filter anywhere on that path**.
So the check asserts the directory's WHOLE contents against the manifest, not only its `.sql` files.

<!-- The MEASURED wart this migration is defence-in-depth against, from 24-RESEARCH M14: -->

`offchain/migrations/001_model_run.sql` declares `gams_ver text not null` and
`conopt_ver text not null`. `NOT NULL` does not forbid `''`. There is no run-log table at all —
STORE-07 is Phase 25.

<!-- Environment, verified at plan time: docker 29.5.2 and jq 1.8.1 are on PATH. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 003, the manifest, and the floors it moves</name>
  <files>offchain/migrations/003_version_columns_nonempty.sql, offchain/lib/Store/Schema.hs, offchain/test/Main.hs</files>

  <read_first>
    - `offchain/migrations/001_model_run.sql` — the existing `gams_ver`/`conopt_ver` declarations
      and the `model_run_identity` constraint, so the new constraint is named in the same style.
    - `offchain/lib/Store/Schema.hs` in full, including the haddock explaining why the manifest is
      written out rather than globbed and why the version numbers are asserted to be `[1 .. n]`.
    - `offchain/test/Main.hs` — `migration_list_is_ordered_and_gapless` and
      `store_conformance_digests_match_the_pinned_source_digest` (the computed freshness oracle),
      plus `purge_file_floor` at line 916 and `credential_scan_floor` at line 6917.
  </read_first>

  <action>
    **1. `offchain/migrations/003_version_columns_nonempty.sql`.** One statement:

    ```sql
    alter table model_run
      add constraint model_run_versions_nonempty
      check (length(gams_ver) > 0 and length(conopt_ver) > 0);
    ```

    The file's leading comment must state the measurement it answers: `NOT NULL` does not forbid
    `''`, the empty version was storable, and every toolchain would then hash to the same key
    component — so GAMS 54.1's bytes would be served to a GAMS 55 request, the precise scenario
    `VOLUME_PATH.md` §3 warns about ("a different CONOPT version may select a different member of
    the underdetermined path family — still passing every gate"). And state that afterwards the
    poisoned rows are INDISTINGUISHABLE from good ones, because the only evidence of which
    toolchain produced them is the column that was emptied.

    No credential, host, port, user or DSN literal appears in the file, including in the prose —
    `no_credential_is_present_in_a_tracked_file` scans `.sql`.

    **2. `Store.Schema.expected_migrations`.** Add `(3, "003_version_columns_nonempty.sql")`.
    `migration_list_is_ordered_and_gapless` then asserts `[1..3]` and the directory's whole contents
    in both directions, with no new code.

    **3. Floors.** This adds one `.sql` file, a SCANNED type for both scans. RE-MEASURE
    `purge_file_floor` and `credential_scan_floor` cold with the two `find … | wc -l` commands and
    update both haddocks with the new census. Do not increment by one.
  </action>

  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/24-06-t1.log | tail -3; test "$(grep -ciE 'warning' /tmp/24-06-t1.log)" = 0 &amp;&amp; echo ZERO_WARNINGS</automated>
  </verify>

  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with zero warning lines.
    - `ls offchain/migrations/ | wc -l` is `3`, and
      `grep -c '003_version_columns_nonempty.sql' offchain/lib/Store/Schema.hs` is `1`.
    - `grep -c 'length(gams_ver) > 0' offchain/migrations/003_version_columns_nonempty.sql` is `1`
      and `grep -c 'length(conopt_ver) > 0'` is `1`.
    - `grep -cE 'postgres://|PGPASSWORD|password=[^"$]' offchain/migrations/003_version_columns_nonempty.sql` is `0`.
    - Both floors hold values produced by the `find` commands at execution time, pasted with their
      output into the summary.
    - `cabal test` at this point is EXPECTED to be RED on the freshness oracle — the artifact has
      not been re-captured yet. Record the verbatim FAIL line: it is the firing observation for
      23-05's computed freshness oracle against a real edit, and it is the reason task 2 exists.
  </acceptance_criteria>

  <done>Migration 003 exists, the manifest names it, and the freshness oracle has been OBSERVED reddening against the un-re-captured artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Re-capture the store evidence, and observe the constraint rejecting</name>
  <files>offchain/rig/store-conformance.json, offchain/rig/capture-store-conformance.sh, offchain/test/Main.hs</files>

  <read_first>
    - `offchain/rig/capture-store-conformance.sh` in full — in particular the observation list it
      drives and the value gate it evaluates before writing the artifact.
    - `offchain/test/Main.hs` — the eleven `store_conformance_*` checks, so a new observation is
      registered in `expected_store_laws` / the observation SET rather than only recorded.
    - `.planning/phases/23-postgres-foundation-byte-exact-schema/23-05-SUMMARY.md` — the observation
      SET discipline and the recorded fact that a COUNT passed a deletion the SET caught.
  </read_first>

  <action>
    **1. Add ONE observation to the capture.** In `capture-store-conformance.sh` (and its
    executable), after migrations are applied, attempt an insert into `model_run` with
    `gams_ver = ''` and record the outcome:

    ```
    "empty_version_rejected": { "attempted": true, "sqlstate": "23514", "rejected": true }
    ```

    `23514` is the `check_violation` SQLSTATE. Record what the server ACTUALLY returned; if it is
    not `23514`, record the real value and investigate rather than editing the artifact. Also
    attempt the symmetric case with `conopt_ver = ''`, and record BOTH — a constraint that only
    covers one column is exactly the shape a copy-paste produces.

    **The insert must go through the same `Binary`-wrapped path the store uses**, not a bespoke
    statement, so the observation is about the schema this repository writes through.

    **2. Register the observation in the SET**, not merely in the document: add its name to the
    pinned observation list the Tier-C check compares against in both directions, so deleting it is
    a set mismatch. A count would pass the deletion — MEASURED at 23-05.

    **3. Add `store_conformance_records_the_empty_version_rejection`** to `core_checks`: assert
    `empty_version_rejected.attempted == true`, `rejected == true`, and `sqlstate` non-empty. The
    `attempted` field is not decoration — without it, an exhibit that never ran the insert and an
    exhibit whose insert was refused look identical, which is this repository's defect class in the
    evidence rather than in the code.

    **4. Re-run the capture.**

    ```bash
    bash offchain/rig/capture-store-conformance.sh
    ```

    Record the artifact's sha256 before and after. The capture provisions and removes its own
    container; docker 29.5.2 and jq 1.8.1 were verified present at plan time. If the capture cannot
    run, it emits NO artifact and leaves the committed one exactly as it was — in that case the
    task FAILS and is reported, and the artifact is NOT hand-edited.

    **5. Re-run the sentinel harness.** The re-captured artifact may enumerate a different number
    of leaves (it gains the new observation). RE-MEASURE `artifact_field_floors`'s
    `store-conformance.json` entry and `sentinel_pair_floor` by raising them until the harness
    reports what it reached, and update the arithmetic in both haddocks. Any newly absorbed pair is
    either asserted or pardoned with a written reason, in 24-05 task 3's discipline.
  </action>

  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/24-06-t2.log | tail -3; test "$(grep -ciE 'warning' /tmp/24-06-t2.log)" = 0 &amp;&amp; { time cabal test; } 2>&amp;1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with zero warnings.
    - `cabal test` exits 0 with `grep -c '^FAIL '` = `0`, including
      `sentinel_falsification_harness` and every `store_conformance_*` check — the freshness oracle
      that was RED at the end of task 1 is now green, and the summary quotes both states.
    - `jq -r '.empty_version_rejected.attempted, .empty_version_rejected.rejected'` prints `true`
      then `true`, and `jq -r '.empty_version_rejected.sqlstate'` prints a non-empty value (expected
      `23514`; record whatever the server returned).
    - `jq -r '.migrations | length'` (or the equivalent field the capture uses) reports `3`.
    - `cabal test 2>&1 | grep -c '^PASS store_conformance_records_the_empty_version_rejection$'`
      is `1`.
    - `cabal test 2>&1 | grep -c '^PASS migration_list_is_ordered_and_gapless$'` is `1`.
    - **FIRING OBSERVATIONS**, each on a SCRATCH COPY reached through `STORE_CONFORMANCE`, never on
      the committed file:
      1. set `empty_version_rejected.rejected` to `false` →
         `store_conformance_records_the_empty_version_rejection` FAILS;
      2. DELETE the new observation from the name list in a scratch copy →
         `store_conformance_verdicts_are_all_pass` FAILS as a SET mismatch;
      3. drop `(3, …)` from `expected_migrations` → `migration_list_is_ordered_and_gapless` FAILS
         naming the unlisted file on disk; restore.
    - `git status --porcelain offchain/rig/store-conformance.json` is empty after every observation,
      and the committed artifact's sha256 is recorded before and after the whole task.
  </acceptance_criteria>

  <done>The database now refuses an empty version string, the refusal has been OBSERVED against a real server, and the re-captured evidence carries it as a named observation in the asserted SET.</done>
</task>

</tasks>

<verification>
- `cabal build --enable-tests -j all` — zero warnings. The bare `cabal build -j all` appears nowhere.
- `cabal test` — all checks pass, `FAIL` count 0, including the sentinel harness.
- Both structural greps over `offchain/test/Main.hs` return `0` (DB-free and GAMS-free).
- `bash offchain/rig/capture-store-conformance.sh` re-run from scratch; both captures' verdicts pass.
- Four firing observations recorded verbatim, including the computed freshness oracle reddening
  against a real migration edit before the re-capture.
</verification>

<success_criteria>
- An empty `gams_ver` or `conopt_ver` is unstorable, and the rejection was OBSERVED with its
  SQLSTATE rather than argued from the DDL.
- The migration manifest is three long and agrees with the directory in both directions.
- The re-captured store evidence is green, and its new observation is in the asserted SET so a
  deletion is a set mismatch rather than a shorter list.
- All four tree-derived floors carry values measured in this run.
</success_criteria>

<output>
After completion, create
`.planning/phases/24-gams-invocation-toolchain-identity/24-06-SUMMARY.md` with the four firing
observations, the store artifact's sha256 before and after, the re-measured floors with their
commands, and the phase-closing guard ledger: one row per guard in `24-RESEARCH.md`'s 41-row table,
naming the input that fired it and where the observation is recorded. **A guard with no observation
is reported as a phase-level finding, named, never omitted** — 23-05's guard #13 precedent.
</output>
