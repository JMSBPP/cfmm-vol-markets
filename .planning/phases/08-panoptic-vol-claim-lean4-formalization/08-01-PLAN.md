---
phase: 08-panoptic-vol-claim-lean4-formalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/panoptic.md
  - spec/refs/cfmm-discrete/FINANCE.md
  - spec/refs/cfmm-discrete/STREAMING_PREMIUM.md
  - spec/refs/cfmm-discrete/DIFFERENTIATION.md
  - spec/refs/cfmm-discrete/BINARY_TREES.md
  - spec/refs/cfmm-discrete/INTEGRATION.md
  - spec/refs/cfmm-discrete/COORDINATES.md
  - spec/refs/README.md
autonomous: true
requirements:
  - CTX-HYGIENE   # 08-CONTEXT: fix θ sign typo + Demeterfi citekey + remove ~/ path, commit spec
  - CTX-VENDOR    # 08-CONTEXT: vendor load-bearing cfmm-discrete notes, keep citekey canonical

must_haves:
  truths:
    - "spec/panoptic.md is git-tracked and committed"
    - "The θ kernel exponent in the spec is negative (Gaussian decays)"
    - "The Demeterfi reference is a URL/citekey, not a dangling ../refs/*.pdf link"
    - "No home-absolute or ~/ path appears anywhere under spec/"
    - "The load-bearing cfmm-discrete notes exist under spec/refs/cfmm-discrete/"
  artifacts:
    - path: "spec/panoptic.md"
      provides: "Pinned, tracked source spec the Lean formalization proceeds from"
      contains: "exp"
    - path: "spec/refs/cfmm-discrete/STREAMING_PREMIUM.md"
      provides: "Lattice θ_ATM derivation source (center-column, kσ/√(8πτ))"
      contains: "Theta"
    - path: "spec/refs/cfmm-discrete/FINANCE.md"
      provides: "CRR backward-induction operator + σ-bridge source"
      contains: "CRR"
  key_links:
    - from: "spec/panoptic.md"
      to: "spec/refs/cfmm-discrete/"
      via: "in-tree relative reference replacing the ~/ NOTE path"
      pattern: "spec/refs/cfmm-discrete"
---

<objective>
Pin the phase's source of truth. Correct the θ sign typo in `spec/panoptic.md`, replace the
dangling Demeterfi PDF link with a URL/citekey, remove the `~/`-absolute NOTE path, commit the
(currently untracked) spec, and vendor the load-bearing `cfmm-discrete` calculus notes into
`spec/refs/cfmm-discrete/` with dangling cross-tree links neutralized.

Purpose: All downstream Lean formalization (Panoptic.lean, Upsilon.lean, the Aristotle θ
derivation) must proceed from a corrected, tracked spec and in-tree proof-source notes — never
from home-absolute paths (Phase-1 sanitization rule) or an uncommitted file.
Output: A committed `spec/panoptic.md` and a populated `spec/refs/cfmm-discrete/`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-CONTEXT.md
@.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix θ sign, Demeterfi citekey, and ~/ NOTE path in spec/panoptic.md; commit</name>
  <read_first>
    - spec/panoptic.md (the file being modified — read current state; it is currently UNTRACKED)
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-CONTEXT.md (Spec hygiene & references decisions)
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-RESEARCH.md (Pitfall 3: sign typo)
  </read_first>
  <files>spec/panoptic.md</files>
  <action>
    Make exactly three edits to `spec/panoptic.md`, then commit it.

    1. **θ sign fix (Pitfall 3).** The current θ kernel (the `\theta(...)` display block) has a
       POSITIVE exponent inside `exp`:
       `\exp \Big (\frac{\Big [- \ln (\frac{p \, (t_0)}{K}) \, + \, \frac{\sigma^2 \, t}{2} \Big ]^2}{2\, \sigma^2 \, (\cdot)\, t}\Big)`
       A Gaussian kernel must DECAY, so the exponent must be negative. Replace it with the
       negated form (insert a leading minus inside `exp`):
       `\exp \Big (-\,\frac{\Big [- \ln (\frac{p \, (t_0)}{K}) \, + \, \frac{\sigma^2 \, t}{2} \Big ]^2}{2\, \sigma^2 \, (\cdot)\, t}\Big)`
       This is the only form consistent with STREAMING_PREMIUM.md's `Θ_ATM(τ)=kσ/√(8πτ)` and
       `∫₀ᵀ Θ_ATM dτ = kσ√(T/2π)`. Do NOT change the prefactor `p(·)σ(·)/√(8πt)`.

    2. **Demeterfi reference.** Replace the dangling markdown link
       `[VOL_SWAPS](../refs/DemeterfietalVarianceSwaps.pdf)`
       with a URL + citekey (do NOT vendor the PDF — public repo, redistribution rights unclear):
       `[VOL_SWAPS](https://emanuelderman.com/wp-content/uploads/1999/02/gs-volatility_swaps.pdf) (Demeterfi, Derman, Kamal, Zou 1999, "More Than You Ever Wanted To Know About Volatility Swaps", Goldman Sachs Quantitative Strategies Research Notes)`
       If that exact URL cannot be confirmed live, use the citekey text alone with the Goldman
       Sachs QS Research Notes attribution and NO file link — never leave a `../refs/*.pdf` link.

    3. **`~/` NOTE path (Phase-1 rule).** The top NOTE line contains
       `[CALCULUS IS THIS ONE](~/learning/cfmm-theory/cfmm-discrete/**)`. Replace the home-absolute
       target with the in-tree vendored path produced by Task 2:
       `[CALCULUS IS THIS ONE](refs/cfmm-discrete/) (canonical KB: cfmm-theory `cfmm-discrete/` — cited, not vendored-as-authority)`
       No `~/`, `$HOME`, or `/home/...` substring may remain in the file.

    Then stage and commit ONLY this file:
    `git add spec/panoptic.md && git commit -m "docs(spec): pin panoptic.md — fix θ sign, Demeterfi citekey, de-path NOTE"`
    (do not use gsd-tools commit here — this is a spec content commit, not a planning-doc commit).
  </action>
  <verify>
    <automated>git ls-files --error-unmatch spec/panoptic.md && ! grep -nE '/home/|\$HOME|~/' spec/panoptic.md && grep -q 'exp \\Big (-' spec/panoptic.md && ! grep -q 'DemeterfietalVarianceSwaps.pdf' spec/panoptic.md</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch spec/panoptic.md` exits 0 (file is tracked)
    - `grep -nE '/home/|\$HOME|~/' spec/panoptic.md` returns nothing (exit 1)
    - `grep -c 'DemeterfietalVarianceSwaps.pdf' spec/panoptic.md` returns 0 (dangling PDF link gone)
    - The negated exponent is present: `grep -n 'exp \\Big (-' spec/panoptic.md` returns a match
    - `grep -n 'spec/refs/cfmm-discrete\|refs/cfmm-discrete' spec/panoptic.md` returns a match (NOTE re-pathed)
    - `git log --oneline -1 -- spec/panoptic.md` shows the pin commit
  </acceptance_criteria>
  <done>spec/panoptic.md is tracked and committed with the corrected negative-exponent θ kernel, a URL/citekey Demeterfi reference, and no home-absolute paths.</done>
</task>

<task type="auto">
  <name>Task 2: Vendor cfmm-discrete notes into spec/refs/cfmm-discrete/, neutralize dangling links, commit</name>
  <read_first>
    - spec/panoptic.md (references the vendored notes; read to confirm the NOTE path target)
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-CONTEXT.md (cfmm-discrete notes decision: vendor + keep citekey canonical)
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-RESEARCH.md (Open Q3: which notes are load-bearing; dangling sibling links warning)
  </read_first>
  <files>spec/refs/cfmm-discrete/FINANCE.md, spec/refs/cfmm-discrete/STREAMING_PREMIUM.md, spec/refs/cfmm-discrete/DIFFERENTIATION.md, spec/refs/cfmm-discrete/BINARY_TREES.md, spec/refs/cfmm-discrete/INTEGRATION.md, spec/refs/cfmm-discrete/COORDINATES.md, spec/refs/README.md</files>
  <action>
    Vendor the load-bearing cfmm-discrete notes (the user's own writing) from the local KB into
    the repo, then neutralize any cross-tree links that would dangle once vendored.

    1. Create `spec/refs/cfmm-discrete/` and copy these SIX files from the local KB source
       `/home/jmsbpp/learning/cfmm-theory/cfmm-discrete/` (read each with the Read tool, then
       write the identical content into the vendored path — do NOT introduce edits beyond the
       link neutralization in step 2):
       - `FINANCE.md`           (CRR operator §6.15/6.18, q, σ-bridge — load-bearing)
       - `STREAMING_PREMIUM.md` (lattice Θ, center-column ATM, kσ/√(8πτ), ∫ = kσ√(T/2π) — load-bearing)
       - `DIFFERENTIATION.md`   (discrete Itô ∂_t/∂_i² — load-bearing)
       - `BINARY_TREES.md`      (Δt=(Δi)² heat-operator collapse, geometric-grid⟹CRR — load-bearing)
       - `INTEGRATION.md`       (referenced by FINANCE.md — vendor so its ./INTEGRATION.md link stays live)
       - `COORDINATES.md`       (supporting lattice calculus; vendor so sibling links resolve)

    2. **Neutralize dangling cross-tree links.** These notes contain relative links that point
       OUTSIDE the vendored set and will dangle. In the vendored copies only, rewrite each such
       link so it no longer resolves to a missing in-tree file — convert the link target to a
       plain-text citekey annotation `(cfmm-theory KB — not vendored)`. Specifically:
       - In `STREAMING_PREMIUM.md`: `../lp-derivatives/notes/CFMM_DISCRETE.md` → `CFMM_DISCRETE (cfmm-theory KB — not vendored)`
       - In `FINANCE.md` and `STREAMING_PREMIUM.md`: `../cfmm-options/notes/NOTATION.md` → `NOTATION (cfmm-theory KB — not vendored)`
       Leave IN-TREE sibling links (`./FINANCE.md`, `./BINARY_TREES.md`, `./INTEGRATION.md`) intact —
       those resolve because their targets are now vendored alongside.
       No `~/`, `$HOME`, or `/home/...` substring may appear in any vendored file.

    3. Write `spec/refs/README.md` with a one-paragraph provenance note: these are vendored copies
       of the author's `cfmm-discrete/` notes from the cfmm-theory knowledge base, kept in-tree as
       the load-bearing proof source for Phase 8; the canonical citekey remains the cfmm-theory KB.

    4. Commit:
       `git add spec/refs/cfmm-discrete/ spec/refs/README.md && git commit -m "docs(spec): vendor load-bearing cfmm-discrete notes; neutralize dangling cross-tree links"`
  </action>
  <verify>
    <automated>test -f spec/refs/cfmm-discrete/FINANCE.md && test -f spec/refs/cfmm-discrete/STREAMING_PREMIUM.md && test -f spec/refs/cfmm-discrete/DIFFERENTIATION.md && test -f spec/refs/cfmm-discrete/BINARY_TREES.md && ! grep -rnE '/home/|\$HOME|~/' spec/refs/ && ! grep -rn '\.\./lp-derivatives\|\.\./cfmm-options' spec/refs/cfmm-discrete/</automated>
  </verify>
  <acceptance_criteria>
    - All six vendored files exist: `ls spec/refs/cfmm-discrete/{FINANCE,STREAMING_PREMIUM,DIFFERENTIATION,BINARY_TREES,INTEGRATION,COORDINATES}.md` exits 0
    - `grep -rnE '/home/|\$HOME|~/' spec/refs/` returns nothing (exit 1) — no home-absolute paths
    - `grep -rn '\.\./lp-derivatives\|\.\./cfmm-options' spec/refs/cfmm-discrete/` returns nothing (dangling cross-tree links neutralized)
    - `grep -q 'Theta\|theta' spec/refs/cfmm-discrete/STREAMING_PREMIUM.md` matches (content copied, not empty)
    - `git ls-files spec/refs/cfmm-discrete/ | wc -l` returns 6
    - `spec/refs/README.md` exists and is tracked: `git ls-files --error-unmatch spec/refs/README.md` exits 0
  </acceptance_criteria>
  <done>The six cfmm-discrete notes are vendored and tracked under spec/refs/cfmm-discrete/ with no home-absolute paths and no dangling cross-tree links; a provenance README is committed.</done>
</task>

</tasks>

<verification>
- `git ls-files --error-unmatch spec/panoptic.md` exits 0
- `git grep -InE '/home/[a-z0-9_-]+/|\$HOME|~/' -- spec/` returns nothing
- `ls spec/refs/cfmm-discrete/` lists the six vendored notes
</verification>

<success_criteria>
spec/panoptic.md is pinned and tracked with the corrected θ sign, a URL/citekey Demeterfi
reference, and no home-absolute paths; the load-bearing cfmm-discrete notes are vendored in-tree
with dangling cross-tree links neutralized. Downstream Lean plans can reference these paths.
</success_criteria>

<output>
After completion, create `.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-01-SUMMARY.md`
</output>
