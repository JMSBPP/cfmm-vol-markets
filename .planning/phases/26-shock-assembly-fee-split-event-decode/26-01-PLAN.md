---
phase: 26-shock-assembly-fee-split-event-decode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - offchain/lib/Fee/Split.hs
  - cfmm-replicationPlank-rpc-api.cabal
  - offchain/test/Main.hs
autonomous: true
requirements: [FEE-01, FEE-02]
must_haves:
  truths:
    - "A (pool fee, delta*) request yields a (phi_X, phi_M) pair whose COMPOSED fee is recorded exactly as an Integer, never approximated"
    - "The admissibility verdict is the prover's own ellTest, evaluated in Integer arithmetic, agreeing with an independent Rational evaluation on every pinned triple"
    - "Exact-split rarity is asserted in BOTH directions: f=6497 has 2 exact pairs, f=3000 has 0"
    - "No floating value, no Rational and no IO exists anywhere on the fee path"
  artifacts:
    - path: "offchain/lib/Fee/Split.hs"
      provides: "pips_denominator, compose_scaled, residual_scaled, nearest_partner, exact_pairs_for, ellipse_test, is_admissible, min_admissible_dstar, FeeSplit, SplitRefusal, splitter_version"
      contains: "ellipse_test"
    - path: "offchain/test/Main.hs"
      provides: "seven new Tier-A checks wired into core_checks"
      contains: "admissibility_is_the_provers_own_ellipse_test"
  key_links:
    - from: "offchain/test/Main.hs"
      to: "Fee.Split"
      via: "import Fee.Split"
      pattern: "import Fee\\.Split"
    - from: "offchain/test/Main.hs aeson_storage_path"
      to: "offchain/lib/Fee/Split.hs"
      via: "the float/aeson scan list, extended in the SAME commit that creates the module"
      pattern: "offchain/lib/Fee/Split\\.hs"
    - from: "offchain/test/Main.hs artifact_path_directories"
      to: "offchain/lib/Fee"
      via: "the both-directions directory-coverage arm"
      pattern: "offchain/lib/Fee"
---

<objective>
Build `Fee.Split` — the pure, all-`Integer` core of the fee splitter — and prove its two
arithmetic claims: the level constraint is exact over pips, and the admissibility gate is the GAMS
prover's own `ellTest` rather than any closed form.

Purpose: FEE-01's exactness and FEE-02's Tier-A half. Everything later in this phase (the ninth
refusal, the seeded band, the Tier-C differential) calls into this module, so it is the interface
that lands first.

Output: `offchain/lib/Fee/Split.hs`, its `.cabal` registration, its entry in the float/aeson scan
and in the directory-coverage arm, and seven new checks in `core_checks`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-shock-assembly-fee-split-event-decode/26-RESEARCH.md
@.planning/phases/26-shock-assembly-fee-split-event-decode/26-VALIDATION.md
@offchain/lib/Gams/Argv.hs
</context>

<locked_decisions>
These were open in `26-RESEARCH.md`'s "Wave 0 Gaps". They are DECIDED here and are binding on every
later plan in this phase. Do not revisit.

1. **Rounding design = (B) round-and-report.** `nearest_partner` picks `m` nearest to the exact
   solution; the realized fee and the exact residual are FIRST-CLASS recorded fields; a pair is
   refused only when `|residual_scaled| >= pips_denominator` (one pip). Exact-or-refuse is REJECTED:
   it refuses every canonical Uniswap tier (MEASURED: f = 100, 500, 3000, 10000 each admit ZERO
   exact integer-pip pairs).
2. **The admissibility refusal lives in `Gams.Argv.render_argv`** as the ninth refusal. That is plan
   26-03's work; this plan supplies only the predicate.
3. **The band is restricted to `rho > 1`, i.e. `phi_M > phi_X`** — the fixture's orientation.
   `VOLUME_PATH.md` section 2 says `phiXpips` comes from the DynamicFeeHook and `phiMpips` from
   `MevTaxModelOneFees`, so the legs are not interchangeable downstream even though the model is
   symmetric. Recorded as a splitter-version decision, so a later widening ORPHANS keys rather than
   silently changing them. **`exact_pairs_for` is deliberately NOT restricted** — it enumerates both
   orientations, which is what keeps its MEASURED count at 2 for f = 6497.
4. **The splitter is authoritative for (phi_X, phi_M) in v6.0.** ROADMAP SC-1: "the derived pips
   (not `f`) are what reach the key". Phase 27's chain read will surface the same pair and the two
   must then be reconciled. Record this in the module haddock now.
5. **The seed to index function is a documented integer mixer, not `mwc-random`.** That is 26-03's
   work; declared here so `Fee.Split` never acquires a `GenIO` or an `ST` dependency.
</locked_decisions>

<planning_corrections>
Two of `26-RESEARCH.md`'s numbers were re-derived independently at plan time; one of its framings
does not survive. Everything below was recomputed on this machine on 2026-08-17.

**CORRECTION A — the residual headroom is 2x, not ~10^3, for an arbitrary band member.**
M4 reports "the best nearest-m residual is +8e-6 pip at f = 3000" and guard 5 generalises that to
"real headroom MEASURED at ~10^3". The +8e-6 figure is the residual of the BEST member of the band,
not of an arbitrary one. MEASURED over the whole `rho > 1` band at `delta* = 490000`:

| f | band size | max abs residual_scaled | in pips | min abs residual_scaled |
|---|---|---|---|---|
| 100 | 44 | 2464 | 0.0025 | 99 |
| 500 | 224 | 61824 | 0.0618 | 499 |
| 3000 | 1344 | **499671** | **0.4997** | 8 |
| 6497 | 2900 | **499240** | **0.4992** | 0 |
| 10000 | 4447 | **499527** | **0.4995** | 100 |

Nearest rounding bounds `abs residual_scaled` by `(pips_denominator - phi_X) / 2`, strictly below
`pips_denominator / 2`. The one-pip alarm therefore has **2.0x** headroom. State 2x. Never repeat
the 10^3 figure without saying it is the band MINIMUM.

**CORRECTION B — research guard 5's named firing input DOES NOT FIRE the bound.**
Guard 5 names "a rounder using floor at f = 3000, x = 998". MEASURED: a floor rounder's worst
residual over the whole band is **999799** at f = 10000 — strictly BELOW `pips_denominator`, so the
bound never trips for it at any f measured. `m + 1` fires for **0 of 44** members at f = 100 and
only 568 of 1344 at f = 3000. The input that fires for EVERY member at every f measured is
**`m + 2`**: minimum abs residual over the band is 1997448 (f=100), 1499790 (f=3000), 1497248
(f=6497), 1493931 (f=10000) — all at least 1.49x `pips_denominator`.

The residual therefore gets **two independent assertions**, and only the second uses the bound:
- the residual is a RECORDED VALUE equal to `compose_scaled x m - pips_denominator * f` recomputed
  — this is what a floor rounder breaks, at PINNED values;
- `abs residual_scaled < pips_denominator` — the bound, whose firing input is **`m + 2`**.
</planning_corrections>

<pinned_values>
Every value here was computed on this machine on 2026-08-17 in exact `Integer` arithmetic and
independently reproduces `26-RESEARCH.md`. `D` denotes `pips_denominator = 1000000`.

**The transcription (`volume_path.gms:100-108`, multiplied through by `D^6`):**

```
Pn = D*(x + m) - x*m
E  = Pn^2 * d^2  +  D^2 * (m - x)^2 * d^2  -  D^2 * (x + m) * Pn * d  +  D^4 * x * m
admissible  <=>  E <= 0
```

`phiBar` is the **COMPOSED** fee `1 - (1-phi_X)(1-phi_M)`; `dphi` is the **FULL** gap
`phi_M - phi_X`. The arithmetic-mean / semi-axis reading is WRONG: exactly 2x too large, disagrees
with the prover on 36 of 70 near-boundary points, and falsely refuses 82713 pips of admissible
`delta*` at the fixture fees. Its corollary "no target reachable unless rho >= 2+sqrt(3)" is FALSE.

**Level constraint:**
- `compose_scaled 500 6000 == 6497000000` (= `D * 6497`) — the fixture recomposes EXACTLY.
  Provenance: `999500 * 994000 = 993503000000 = D * 993503`.
- `compose_scaled` with the `- x*m` term deleted gives `D * 6500` at the fixture, not `D * 6497`.

**Exact-pair census (both orientations, unrestricted):**

| f | length (exact_pairs_for f) | members |
|---|---|---|
| 100 | **0** | none |
| 500 | **0** | none |
| 3000 | **0** | none |
| 10000 | **0** | none |
| 6497 | **2** | (500,6000) and (6000,500) |

**Ellipse test, pinned as exact Integers:**
- `ellipse_test 500 6000 490000 == -295056739100000000000000000000` (admitted)
- `ellipse_test 500 6000 82803 == 4991723980281000000000000` (**refused**, one pip below the boundary)
- `ellipse_test 500 6000 82804 == -25238725702256000000000000` (admitted, the boundary)
- `ellipse_test 500 6000 0 == 3000000000000000000000000000000` (> 0, so `delta* = 0` is refused for
  free — this closes `Gams/Argv.hs`'s live `txlVolumeRate = 0` hole without a second bound)

**min_admissible_dstar (least integer delta* in [0, D-1] with E <= 0):**

| phi_X | phi_M | result |
|---|---|---|
| 500 | 6000 | Just 82804 |
| 100 | 900 | Just 109769 |
| 1000 | 3000 | Just 300361 |
| 50 | 9950 | Just 5026 |
| 700 | 800 | Just 495953 |
| **3000** | **3000** | **Nothing** |

The last row is section 1.2 as a machine-checked fact and is STRONGER than the prose: at equal fees
the quadratic has a DOUBLE root at `1/(2-phi) = 0.50075...`, so **no integer delta* at all** is
admissible. The prose only says the root exceeds one half.
</pinned_values>

<tasks>

<task type="auto">
  <name>Task 1: Author Fee.Split — the whole type surface and the exact arithmetic</name>
  <files>offchain/lib/Fee/Split.hs, cfmm-replicationPlank-rpc-api.cabal</files>
  <read_first>
    - `offchain/lib/Gams/Argv.hs` in full — the house haddock voice, the `Either <Error>` refusal
      shape, `in_range`'s "name the field, name the bound" message discipline, and the fact that the
      module imports `Data.Char` and nothing else.
    - `cfmm-replicationPlank-rpc-api.cabal` lines 80-140 — `exposed-modules` and the package-count
      comment discipline. `common warnings` is `ghc-options: -Wall`; the compiler is GHC 9.10.3.
    - `26-RESEARCH.md` sections M2, M3, M4 and "Architecture Patterns" patterns 1-3.
  </read_first>
  <action>
Create `offchain/lib/Fee/Split.hs`. It imports **`Data.Word (Word32)` and nothing else** — no
`Data.Ratio`, no `Data.Bits` (that arrives in 26-03), no `System.*`, no `Control.*`, no IO.

Export exactly:

```
module Fee.Split
  ( pips_denominator
  , compose_scaled
  , residual_scaled
  , nearest_partner
  , exact_pairs_for
  , ellipse_test
  , is_admissible
  , min_admissible_dstar
  , FeeSplit (..)
  , SplitRefusal (..)
  , refusal_message
  , refusal_boundary
  , splitter_version
  ) where
```

Definitions, all `Integer`, all total:

- `pips_denominator :: Integer` = `1000000`. Haddock: this is `volume_path.gms`'s `PIPS` and the
  Algebra `FEE_DENOMINATOR = 1e6`; KEY-05 requires it in the key preimage, which is Phase 25's work
  and is named here so the two cannot drift.

- `compose_scaled :: Integer -> Integer -> Integer` =
  `\x m -> pips_denominator * (x + m) - x * m`. Haddock: this is `pips_denominator * f'` where `f'`
  is the fee the pair ACTUALLY composes to, exactly; equality with `pips_denominator * f` holds iff
  `pips_denominator` divides `x*m`, true for **4.93 %** of f in [1, 20000] and for **NONE** of
  100 / 500 / 3000 / 10000. Pin `compose_scaled 500 6000 == 6497000000` with its provenance
  `999500 * 994000 = 993503000000`.

- `residual_scaled :: Integer -> Integer -> Integer -> Integer` =
  `\f x m -> compose_scaled x m - pips_denominator * f`. Haddock: units are
  `pips_denominator * pips`, so one whole pip is `pips_denominator`. Record CORRECTION A's measured
  bound — nearest rounding gives `abs residual_scaled < pips_denominator / 2`, so the one-pip alarm
  has **2x** headroom (max measured over the whole `rho > 1` band: 499671 at f=3000, 499240 at
  f=6497, 499527 at f=10000). Do NOT write 1000x; that is the band minimum.

- `nearest_partner :: Integer -> Integer -> Integer`. `nearest_partner f x` = the integer nearest
  the exact solution `D*(f-x)/(D-x)`, computed with `divMod` and a `2*r >= (D - x)` tie rule (ties
  round UP), never with `/`, `round`, `fromIntegral` into a fractional type, or any `Double`.
  Haddock the tie rule explicitly: it is the rounding rule SC-1 requires "pinned in writing".

- `exact_pairs_for :: Integer -> [(Integer, Integer)]`. All `(x, m)` with
  `1 <= x < pips_denominator`, `1 <= m < pips_denominator` and
  `compose_scaled x m == pips_denominator * f`, found by enumerating divisors `a` of
  `n = pips_denominator * (pips_denominator - f)` in the OPEN window
  `(pips_denominator - f, pips_denominator)` and returning `(D - a, D - n `div` a)`.
  **Deliberately UNRESTRICTED in orientation** — it returns both `(500,6000)` and `(6000,500)`.
  Haddock says so and pins the census: 0 / 0 / 0 / 0 for 100 / 500 / 3000 / 10000 and 2 for 6497.

- `ellipse_test :: Integer -> Integer -> Integer -> Integer` — the transcription in
  `<pinned_values>`, term for term, in exactly that term order. The haddock MUST carry (a) the
  verbatim four GAMS lines from `volume_path.gms:100-108`; (b) that `phiBar` is the COMPOSED fee and
  `dphi` the FULL gap, with the MEASURED cost of the mean/semi-axis misreading — 36 of 70
  near-boundary points flip, 82713 pips falsely refused at the fixture, and the "rho >= 2+sqrt(3)"
  corollary is FALSE; (c) `E == D^6 * ellTest` in sign and value, VERIFIED on 20000 random triples;
  (d) `ellipse_test x m 0 = D^4 * x * m > 0`, which is why `delta* = 0` needs no separate bound.

- `is_admissible :: Integer -> Integer -> Integer -> Bool` = `\x m d -> ellipse_test x m d <= 0`.

- `min_admissible_dstar :: Integer -> Integer -> Maybe Integer`. The least integer `d` in
  `[0, pips_denominator - 1]` with `is_admissible x m d`, else `Nothing`. Implement by BISECTION on
  the interval where the predicate is monotone — never by linear scan, never through a floating
  root:
  - `pn = pips_denominator * (x + m) - x * m`
  - `a = pn*pn + pips_denominator^2 * (m - x)^2`   (the positive leading coefficient)
  - `vertex = (pips_denominator^2 * (x + m) * pn) `div` (2 * a)`   (floor of `-B / 2A`)
  - `hi = min vertex (pips_denominator - 1)`; `ellipse_test` is DECREASING on `[0, hi]`, so
    `is_admissible` is monotone there
  - if `not (is_admissible x m hi)` then `Nothing`, else binary-search the least admissible `d`
    in `[0, hi]`.
  Haddock why the bisection is valid (convex in `d`, positive leading coefficient) and pin the six
  values from `<pinned_values>`, **including `min_admissible_dstar 3000 3000 == Nothing`** with the
  sentence: at equal fees the discriminant vanishes, the single root is `1/(2-phi) = 0.50075`, and
  NO integer delta* is admissible — strictly stronger than section 1.2's prose.

- `data FeeSplit` — ONE constructor, record syntax (so every selector is total), strict fields:
  `fs_pool_fee_pips, fs_dstar_pips, fs_phi_x_pips, fs_phi_m_pips :: !Integer`;
  `fs_realized_scaled, fs_residual_scaled :: !Integer`; `fs_is_exact :: !Bool`;
  `fs_ellipse_e, fs_boundary_pips :: !Integer`; `fs_band_size :: !Int`;
  `fs_seed :: !Word32`; `fs_splitter_version :: !String`. `deriving (Eq, Show)`.
  Haddock `fs_seed` and `fs_splitter_version` as **exactly what Phase 25's run log and `key_scheme`
  consume** (ROADMAP:1202) — this phase owes Phase 25 these two fields and nothing else.
  Construction is 26-03's work; the record lands here so the interface is fixed before its consumers.

- `data SplitRefusal` — **POSITIONAL constructors, NO record syntax.** GHC 9.10's
  `-Wincomplete-record-selectors` is in `-Wall` and a multi-constructor record would emit it:

```
  = EqualFees Integer
  | OutsideEllipse Integer Integer Integer Integer (Maybe Integer)
  | EmptyBand Integer Integer Int
  | ResidualTooLarge Integer Integer Integer Integer
  deriving (Eq, Show)
```

  Haddock names each positional field. `OutsideEllipse phi_x phi_m dstar e boundary` — the fifth is
  `Maybe` because `min_admissible_dstar` is `Maybe`, and the equal-fee case genuinely has no
  boundary.

- `refusal_message :: SplitRefusal -> String` — total, one arm per constructor, each naming its
  subject AND its number in the `Gams.Argv.in_range` voice. `OutsideEllipse` must spell the pair,
  the requested `delta*`, the exact `E` and the boundary (or say there is none). `EqualFees` must
  cite `VOLUME_PATH.md` section 1.2 by name.

- `refusal_boundary :: SplitRefusal -> Maybe Integer` — total; `Just b` only for `OutsideEllipse`
  carrying a boundary.

- `splitter_version :: String` = `"fee-split-1"`. Haddock: it names the ruling SET —
  round-and-report, `rho > 1`, nearest-with-ties-up, the prover's own `ellTest` — so widening any of
  them bumps this string and ORPHANS Phase 25 keys rather than silently changing them.

Register `Fee.Split` in the library stanza's `exposed-modules` immediately after `Gams.Invoke`, with
a comment in the established voice stating **`Downloading = 0` as a CONFIRMED observation** of
`cabal build --enable-tests -j all` (base only; `Data.Word` is base). Never write it as an estimate.

**Pitfall E, instance 19 — prose inside a grep's blast radius.** `sc3_literal_purge` scans
`.hs`/`.sh`/`.sql` under `offchain/` for `0x` followed by exactly 8, 40 or 64 hex characters at a
word boundary. Write every constant in this module in DECIMAL, and do not spell a `0x`-prefixed hex
literal anywhere in the file including in haddock prose.
  </action>
  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/26-01-t1.log | tail -20; echo "WARN=$(grep -ciE '\bwarning\b' /tmp/26-01-t1.log)"; echo "DL=$(grep -c Downloading /tmp/26-01-t1.log)"; echo "HEXLIT=$(grep -cE '0x[0-9a-fA-F]{40}\b|0x[0-9a-fA-F]{64}\b|0x[0-9a-fA-F]{8}\b' offchain/lib/Fee/Split.hs)"; echo "FLOATS=$(grep -cE 'Double|Float|realToFrac|fromRational|Data\.Ratio|Rational|sqrt|System\.|unsafePerformIO' offchain/lib/Fee/Split.hs)"; cabal test 2>&1 | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with `WARN=0`. The variant of it that omits `--enable-tests` is
      VACUOUS and must appear NOWHERE in the summary or the commit message.
    - `DL=0` — no `Downloading` line. Record it in the `.cabal` comment as a CONFIRMED observation.
    - `HEXLIT=0` and `FLOATS=0` from the commands above.
    - `grep -c '^import' offchain/lib/Fee/Split.hs` returns exactly `1`, and that import is
      `Data.Word (Word32)`.
    - `grep -cE 'ellipse_test|min_admissible_dstar|compose_scaled|nearest_partner|exact_pairs_for|refusal_message|refusal_boundary|splitter_version' offchain/lib/Fee/Split.hs` is at least 8.
    - `grep -n 'Fee.Split' cfmm-replicationPlank-rpc-api.cabal` shows it in `exposed-modules`.
    - `cabal test` still reports the 151-check baseline with FAIL 0 (no check added yet).
  </acceptance_criteria>
  <done>`Fee.Split` compiles warning-free with the whole type surface exported and the six pinned
  `min_admissible_dstar` values reproducible by hand from the source.</done>
</task>

<task type="auto">
  <name>Task 2: FEE-01 — four checks over the level constraint, plus the scan-scope growth</name>
  <files>offchain/test/Main.hs</files>
  <read_first>
    - `offchain/test/Main.hs` `pure_check` (~:525), `expect` (~:530), `core_checks` (~:11042).
    - `aeson_storage_path` (~:7807), `aeson_scan_exemptions`, `artifact_path_directories` (~:8960)
      and `the_artifact_path_scan_covers_every_module_on_it` (~:8993) — read the whole check; its
      haddock records that `Store/Schema.hs` sat unlisted for two commits and nothing reddened.
    - `artifact_float_path` / `artifact_float_pattern` (~:8864) and
      `no_Double_and_no_aeson_on_the_artifact_path` (~:8918).
    - `purge_file_floor` (~:1058) and `credential_scan_floor` (~:7634) — read BOTH haddocks. The
      rule is that they are re-measured as a PAIR, cold, by running the two commands, never by
      adding one to the number written beside them.
  </read_first>
  <action>
Extend the scan scope FIRST, in this same commit — `26-RESEARCH.md` pitfall F, and 24-02's finding
that a scan whose scope fails to grow is worse than no scan:

- add `"offchain/lib/Fee/Split.hs"` to `aeson_storage_path` (which `artifact_float_path` aliases,
  so it covers the float pattern too);
- add `"offchain/lib/Fee"` to `artifact_path_directories`. **Do NOT add `offchain/lib/Chain`** —
  that directory does not exist until 26-02, and
  `the_artifact_path_scan_covers_every_module_on_it` fails loudly on an absent directory, by
  design. 26-02 adds it in the commit that creates the module.

Then add these four checks and wire ALL of them into `core_checks`. A check not in that list does
not exist.

1. `compose_is_the_exact_level_constraint` (`pure_check`) — over the pinned corpus
   `[(500,6000),(6000,500),(100,900),(998,2004),(1,99),(752,2250)]` assert
   `compose_scaled x m == pips_denominator*(x+m) - x*m` recomputed inline in the check (an
   independent second expression, not a call to the same function), and assert
   `residual_scaled f x m == compose_scaled x m - pips_denominator * f` for `f` derived from each.
   **Firing input**: deleting the `- x*m` term from `compose_scaled` turns the fixture's
   `6497000000` into `6500000000`.

2. `the_fixture_pair_recomposes_to_6497_pips` (`pure_check`) — assert
   `compose_scaled 500 6000 == 6497000000` and, as the independent provenance,
   `(pips_denominator - 500) * (pips_denominator - 6000) == 993503000000` and
   `993503000000 == pips_denominator * 993503`. Three `Integer` equalities; no tolerance can absorb
   any of them. **Firing input**: any change to `compose_scaled`'s algebra.

3. `exact_split_existence_is_measured_in_both_directions` (`pure_check`) — assert
   `length (exact_pairs_for 6497) == 2` AND
   `sort (exact_pairs_for 6497) == [(500,6000),(6000,500)]` AND
   `exact_pairs_for f == []` for each of `f <- [100,500,3000,10000]`. Haddock: a one-directional
   assertion passes on a splitter that stopped searching, AND on one that started claiming
   exactness — this is `26-RESEARCH.md` pitfall G. **Firing inputs**: an implementation returning
   `[]` for 6497 (positive arm reddens); one returning a pair for 3000 (negative arm reddens).

4. `rounding_residual_is_recorded_and_under_a_pip` (`pure_check`) — over the corpus
   `[(100,490000),(500,490000),(3000,490000),(6497,490000),(10000,490000)]`, for every `x` in
   `[1 .. f-1]` with `m = nearest_partner f x`, `m > x` and `is_admissible x m dstar`:
   - assert `residual_scaled f x m == compose_scaled x m - pips_denominator * f` (the VALUE arm);
   - assert `abs (residual_scaled f x m) < pips_denominator` (the BOUND arm);
   - assert the MEASURED extrema as pinned constants, so a rounder that changed would move them:
     `(f, band size, max abs residual, min abs residual)` =
     `(100,44,2464,99)`, `(500,224,61824,499)`, `(3000,1344,499671,8)`,
     `(6497,2900,499240,0)`, `(10000,4447,499527,100)`.
   Haddock CORRECTION A verbatim — the headroom is **2x**, not 10^3, because 10^3 is the band
   MINIMUM. Haddock CORRECTION B: **`m + 2` is the firing input for the bound arm**; a `floor`
   rounder does NOT trip it (worst floor residual measured 999799 < 1000000) and `m + 1` trips it
   for 0 of 44 members at f = 100. A `floor` rounder IS caught, by the extrema arm.

RE-MEASURE both tree-derived floors COLD, as a pair, by running the two `find` commands — never by
arithmetic. Expected after this plan: `purge_file_floor` **60** (this plan adds one `.hs` under
`offchain/`), `credential_scan_floor` **69**. If the measurement disagrees with the expectation, the
MEASUREMENT WINS and the discrepancy is recorded in the summary; two phase-24 summaries misreported
this pair.
  </action>
  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/26-01-t2.log | tail -20; echo "WARN=$(grep -ciE '\bwarning\b' /tmp/26-01-t2.log)"; cabal test 2>&1 | tee /tmp/26-01-t2-test.log | tail -6; echo "FAIL=$(grep -c '^FAIL' /tmp/26-01-t2-test.log)"; echo "PURGE=$(find offchain -type f \( -name '*.hs' -o -name '*.sh' -o -name '*.sql' \) | wc -l)"; echo "CRED=$(find offchain -type f \( -name '*.hs' -o -name '*.sh' -o -name '*.sql' -o -name '*.json' \) | wc -l)"; echo "DBFREE=$(grep -cE 'Store\.Postgres|CFMM_REQUIRE_DB|connectPostgreSQL' offchain/test/Main.hs)"; echo "GAMSFREE=$(grep -cE 'Gams\.Invoke|CFMM_REQUIRE_GAMS|/usr/gams' offchain/test/Main.hs)"</automated>
  </verify>
  <acceptance_criteria>
    - `WARN=0`, `FAIL=0`, `DBFREE=0`, `GAMSFREE=0`.
    - `cabal test` total is **155** (151 baseline + 4).
    - All four check names appear in `core_checks`:
      `grep -c 'compose_is_the_exact_level_constraint\|the_fixture_pair_recomposes_to_6497_pips\|exact_split_existence_is_measured_in_both_directions\|rounding_residual_is_recorded_and_under_a_pip' offchain/test/Main.hs` is at least 8 (definition + registration for each).
    - `grep -n 'offchain/lib/Fee/Split.hs' offchain/test/Main.hs` shows it inside `aeson_storage_path`;
      `grep -n 'offchain/lib/Fee"' offchain/test/Main.hs` shows it inside `artifact_path_directories`;
      `grep -c 'offchain/lib/Chain' offchain/test/Main.hs` is **0**.
    - `PURGE` and `CRED` were both RUN and their outputs are what `purge_file_floor` and
      `credential_scan_floor` are set to — zero slack on both, recorded in the summary as measured
      values with the date.
    - **Firing observations, each with the source restored from a SAVED COPY verified by sha256
      (never `git checkout`), recorded verbatim in the summary:**
      (a) `- x*m` deleted from `compose_scaled` reddens checks 1 and 2 and prints `6500000000`;
      (b) `exact_pairs_for` returning `[]` unconditionally reddens check 3's positive arm naming
      f = 6497;
      (c) `nearest_partner` returning `q + 2` reddens check 4's BOUND arm;
      (d) `nearest_partner` returning `q` (floor) leaves the BOUND arm GREEN and reddens the
      EXTREMA arm — this observation is what proves CORRECTION B and MUST be recorded.
  </acceptance_criteria>
  <done>Four FEE-01 checks green in `core_checks`, four firing observations recorded, both floors
  re-measured cold and the scan scope grown in the same commit as the module.</done>
</task>

<task type="auto">
  <name>Task 3: FEE-02 Tier A — the prover's own ellipse, the integer/rational agreement, the float scan</name>
  <files>offchain/test/Main.hs</files>
  <read_first>
    - `26-RESEARCH.md` M1 (the 70-point differential table), M2 (the derivation and the 20000-triple
      verification) and M5 (the exact-vs-double margin).
    - `offchain/test/Main.hs` `no_Double_and_no_aeson_on_the_artifact_path` (~:8918) and
      `gams_version_scan` — the ONE-argument-vector scan idiom with a proven positive control.
    - `offchain/test/Main.hs` `sc3_literal_purge` and `purge_control_literal` — the built-not-spelled
      bait idiom.
  </read_first>
  <action>
Add three checks and wire all of them into `core_checks`.

5. `admissibility_is_the_provers_own_ellipse_test` (`pure_check`). Assert, as exact `Integer`
   equalities and `Bool`s:
   - `ellipse_test 500 6000 490000 == -295056739100000000000000000000`
   - `ellipse_test 500 6000 82803 == 4991723980281000000000000` and `not (is_admissible 500 6000 82803)`
   - `ellipse_test 500 6000 82804 == -25238725702256000000000000` and `is_admissible 500 6000 82804`
   - `ellipse_test 500 6000 0 == 3000000000000000000000000000000` and `not (is_admissible 500 6000 0)`
   - `min_admissible_dstar` equals `Just 82804 / Just 109769 / Just 300361 / Just 5026 / Just 495953`
     for `(500,6000) / (100,900) / (1000,3000) / (50,9950) / (700,800)` respectively
   - **`min_admissible_dstar 3000 3000 == Nothing`** — section 1.2 as a machine-checked fact
   - the boundary is bracketed by one pip on BOTH sides for all five pairs:
     `not (is_admissible x m (b-1)) && is_admissible x m b` for `Just b`.
   Haddock the mean/semi-axis misreading and its MEASURED cost (36 of 70 flips, 82713 pips falsely
   refused, "rho >= 2+sqrt(3)" FALSE). **Firing input**: replacing `pn` with
   `pips_denominator*(x+m) `div` 2` (the arithmetic-mean reading) or `(m-x)` with `(m-x) `div` 2`
   (the semi-axis reading) — either flips at least the `82803`/`82804` pair.

6. `the_integer_form_equals_the_rational_evaluation` (`pure_check`). Evaluate `ellTest` a SECOND,
   INDEPENDENT way inside the check — as a pair of `Integer`s `(numerator, denominator)` with
   `denominator = pips_denominator^6 > 0`, built directly from the GAMS expression's own shape
   (`phiBar`, `dphi`, `dStar` each as an explicit `(num, den)` pair combined by hand) — and assert
   for every triple in a pinned corpus that (a) the SIGNS agree and (b)
   `numerator == ellipse_test x m d`. **`Data.Ratio` must NOT be imported**; the second evaluation
   is hand-built numerator arithmetic, which is why it is independent rather than a rename.
   Corpus: the five boundary pairs above at `{b-1, b, b+1}`, plus `(3,7,362071)`,
   `(998,2004,399519)`, `(752,2250,300912)`, plus a deterministic pseudo-random sweep of **at least
   2000** triples generated inside the check from a fixed integer recurrence (no `mwc-random`, no
   IO). Haddock: the full 20000-triple verification was done at research time; 2000 in-suite triples
   are the standing sample and the check states its own sample size.
   **Firing input**: any single sign or coefficient change in `ellipse_test` — record which one was
   used and the first disagreeing triple it printed.

7. `no_floating_value_is_on_the_fee_path` (`pure_check` or `Check`, using the existing
   `gams_version_scan` one-argument-vector idiom). Two arms, POSITIVE CONTROL FIRST:
   - the pattern (built by concatenation, not spelled, so this file does not match its own scan)
     `Double|Float|realToFrac|fromRational|Data\.Ratio|Rational|sqrt` must MATCH a seeded bait file
     written into a scratch directory outside the repository — and the bait's NAME must appear in
     the grep output (`-H` is load-bearing);
   - the same argument vector over `artifact_float_path` must return exit 1 (no match), and the
     scanned set must CONTAIN `offchain/lib/Fee/Split.hs`, asserted explicitly so this check cannot
     pass by scanning a list the module was never added to.
   Haddock: `26-RESEARCH.md` M5 — the exact-vs-double margin at `phi = (3,7)` pips is 4.4e-18,
   only 4e4 times the double noise floor, which is why the grid stays at `phi >= 100` and why no
   floating value is permitted here at all.
   **Firing inputs**: (a) seed `budget :: Double` into `Fee/Split.hs` and observe the scan naming
   the file and line; (b) delete `"offchain/lib/Fee/Split.hs"` from `aeson_storage_path` and observe
   BOTH this check's membership arm AND
   `the_artifact_path_scan_covers_every_module_on_it` redden, the latter naming the unlisted module.

Update `Fee/Split.hs`'s module haddock with a "what this module is NOT" paragraph if any of the
firing observations above revealed an unstated assumption. Do not relax any pattern to make a check
pass — every one of the eighteen prior instances of prose-inside-a-grep's-blast-radius was resolved
by MOVING the prose.
  </action>
  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/26-01-t3.log | tail -20; echo "WARN=$(grep -ciE '\bwarning\b' /tmp/26-01-t3.log)"; cabal test 2>&1 | tee /tmp/26-01-t3-test.log | tail -8; echo "FAIL=$(grep -c '^FAIL' /tmp/26-01-t3-test.log)"; echo "PASS=$(grep -c '^PASS' /tmp/26-01-t3-test.log)"; echo "RATIO=$(grep -c 'Data.Ratio' offchain/test/Main.hs)"; echo "DBFREE=$(grep -cE 'Store\.Postgres|CFMM_REQUIRE_DB|connectPostgreSQL' offchain/test/Main.hs)"; echo "GAMSFREE=$(grep -cE 'Gams\.Invoke|CFMM_REQUIRE_GAMS|/usr/gams' offchain/test/Main.hs)"</automated>
  </verify>
  <acceptance_criteria>
    - `WARN=0`, `FAIL=0`, `DBFREE=0`, `GAMSFREE=0`.
    - `PASS` is **158** (151 baseline + 4 from task 2 + 3 here).
    - `RATIO=0` — `Data.Ratio` is imported nowhere in the suite.
    - All three check names are defined AND registered in `core_checks`.
    - The wall-clock time of `cabal test` is recorded before and after this task; the total remains
      under the **900 s** budget and is reported against the 149.5 s baseline. If the 2000-triple
      sweep costs more than 10 s, reduce the sweep to the smallest size that still passes and state
      the size in the check's own failure message.
    - **Firing observations recorded verbatim, each restored from a saved copy verified by sha256:**
      (a) `pn` replaced by the arithmetic-mean reading reddens check 5 and prints the
      `82803`/`82804` flip;
      (b) one coefficient sign flipped in `ellipse_test` reddens check 6, naming the first
      disagreeing triple;
      (c) `budget :: Double` seeded into `Fee/Split.hs` reddens check 7 by file and line;
      (d) `"offchain/lib/Fee/Split.hs"` removed from `aeson_storage_path` reddens BOTH check 7's
      membership arm and `the_artifact_path_scan_covers_every_module_on_it`, the latter naming the
      unlisted module.
  </acceptance_criteria>
  <done>FEE-01's four clauses and FEE-02's Tier-A clauses are asserted, seven checks are green in
  `core_checks`, and every guard added by this plan has been OBSERVED rejecting its named input.</done>
</task>

</tasks>

<verification>
- `cabal build --enable-tests -j all` exits 0 with ZERO `-Wall` warnings. The bare
  `cabal build` WITHOUT `--enable-tests` is VACUOUS and appears nowhere in this plan, its commits or its summary.
- `cabal test`: **158/158**, FAIL 0. Wall recorded before and after against the 149.5 s baseline and
  the 900 s ceiling.
- Both structural greps over `offchain/test/Main.hs` return **0** (DB-free and GAMS-free).
- `purge_file_floor` and `credential_scan_floor` re-measured COLD as a pair by running the two `find`
  commands; expected 60 / 69, zero slack; the measurement wins over the expectation.
- `git status --porcelain src test foundry-scripts Makefile foundry.toml .github` is EMPTY — this
  phase's territory is `offchain/` and `.planning/` only.
- Seven guards, seven firing observations, each restored from a sha256-verified saved copy.
</verification>

<success_criteria>
1. `Fee.Split` exists, is `-Wall`-clean, imports only `Data.Word`, and contains no `0x`-prefixed
   8/40/64-hex literal and no floating or `Rational` type.
2. `ellipse_test` reproduces all six pinned `min_admissible_dstar` values including
   `min_admissible_dstar 3000 3000 == Nothing`, and its integer form agrees in sign and value with
   an independently hand-built rational evaluation on at least 2000 triples.
3. Exact-split existence is asserted in BOTH directions with the MEASURED constants 2 (f=6497) and
   0 (f=3000, 100, 500, 10000).
4. The rounding rule is pinned in writing, the residual is a recorded value AND bounded under one
   pip, and CORRECTION B's measurement (a `floor` rounder does not trip the bound) is OBSERVED and
   recorded.
5. The float/aeson scan and its both-directions directory arm cover `offchain/lib/Fee/` in the same
   commit that created the module, and removing the module from the list was OBSERVED reddening two
   independent checks.
</success_criteria>

<output>
After completion, create
`.planning/phases/26-shock-assembly-fee-split-event-decode/26-01-SUMMARY.md` in the 24-06 shape,
including: the measured suite counts before and after, the wall before and after, the two
re-measured floors with the commands that produced them, the seven-guard firing ledger with
verbatim FAIL lines, and any guard added but NOT observed firing reported BY NAME as a
phase-level finding.
</output>
