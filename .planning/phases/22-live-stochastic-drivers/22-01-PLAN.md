---
phase: 22-live-stochastic-drivers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - offchain/rig/import-paths.txt
  - offchain/rig/import-ref.txt
  - offchain/rig/check-upstream.sh
  - offchain/rig/rig-pins.json
  - .planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md
  - foundry-scripts/deploy/DeployDynamicFeeHook.s.sol
  - foundry-scripts/deploy/InitSwappableRig.s.sol
  - src/modules/pos_spec/VolOrderManagerMod.plk
  - .planning/phases/22-live-stochastic-drivers/FORGE-DELTA.md
  - .planning/phases/22-live-stochastic-drivers/22-CROSS-TRACK-FINDINGS.md
autonomous: true
requirements: [DRIV-01, DRIV-02]

must_haves:
  truths:
    - "The 37 imported paths are byte-identical to origin/develop @ 2039f27 and sha256-matched"
    - "verify-import.sh exits 0 at 37 paths and exits 1 under an injected digest fault"
    - "check-upstream.sh cannot go green on a develop that lacks InitSwappableRig.s.sol"
    - "forge build --via-ir exits 0 with PoolSwapTest/PoolModifyLiquidityTest now reachable from a tracked script"
    - "notes/VOLATILITY_INSTRUMENTS.md was NOT imported"
  artifacts:
    - path: "offchain/rig/import-paths.txt"
      provides: "37 pinned import paths"
      contains: "foundry-scripts/deploy/InitSwappableRig.s.sol"
    - path: "foundry-scripts/deploy/InitSwappableRig.s.sol"
      provides: "the swappable-rig init script, imported verbatim"
      min_lines: 199
    - path: ".planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md"
      provides: "37 sha256 rows against ref 2039f27"
    - path: ".planning/phases/22-live-stochastic-drivers/FORGE-DELTA.md"
      provides: "post-import forge/plank baseline vs Phase 20's 85/27/112"
  key_links:
    - from: "offchain/rig/import-ref.txt"
      to: "offchain/rig/generate-pins.sh + offchain/rig/deploy-rig.sh"
      via: "IMPORT_REF / REF_FILE read at run time -> generatedFrom in both manifest halves"
      pattern: "import-ref\\.txt"
    - from: "offchain/rig/verify-import.sh"
      to: ".planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md"
      via: "PIN= constant, unchanged on purpose"
      pattern: "IMPORT-PIN\\.md"
---

<objective>
Re-import the plank track's source-of-truth artifacts from `origin/develop @ 2039f27` (PR #18)
by verbatim checkout, re-pin all 37 paths by sha256, and PROVE that the two vendored v4-core
routers — which no tracked file in this repo has ever pulled into a build — compile under
`--via-ir`.

Purpose: the standing rig is STALE (F2 moved `TICK_SPACING` 10 -> 20, which changes the PoolKey
hash and therefore the poolId), and `InitSwappableRig.s.sol` — the only thing that can make a
swap enter `DynamicFeeHook.beforeSwap` — does not exist on this branch yet. Every other plan in
Phase 22 is unexecutable until this lands.

Output: 37 pinned paths, a re-pinned `IMPORT-PIN.md`, an updated upstream gate, a measured
forge/plank delta, and the phase's cross-track findings file.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-live-stochastic-drivers/22-CONTEXT.md
@.planning/phases/22-live-stochastic-drivers/22-RESEARCH.md
@.planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md
@offchain/rig/import-paths.txt
@offchain/rig/verify-import.sh
@offchain/rig/check-upstream.sh

<interfaces>
<!-- The exact facts this plan consumes. All MEASURED on 2026-08-02 against the working tree. -->

Recorded ref today:   offchain/rig/import-ref.txt = 9f5ccba92ddf89d80efe81bae1dcd1d0a1c10e2d
Target ref:           origin/develop = 2039f2783598866a337115df4a265a75e8842e82
Ref subject:          "Merge pull request #18 from JMSBPP/feat/plank"

Measured delta inside the pinned set (exactly two files changed):
  foundry-scripts/deploy/DeployDynamicFeeHook.s.sol   +5 -1   (F2: TICK_SPACING 10 -> 20)
  src/modules/pos_spec/VolOrderManagerMod.plk         +18 -13 (F1: comment block only)

Measured delta over foundry-scripts/ src/ notes/ (one ADD, one file that MUST NOT be imported):
  foundry-scripts/deploy/InitSwappableRig.s.sol       +199   ADD to the pin set
  notes/VOLATILITY_INSTRUMENTS.md                     +753   NOT in import-paths.txt. DO NOT IMPORT.

sha256 @ 2039f27 (computed by `git show 2039f27:<path> | sha256sum`, NOT transcribed):
  f282e0942d0f013e04541957d74245bcd932bb3cbdb4f8628812bea219082fa0  foundry-scripts/deploy/DeployDynamicFeeHook.s.sol
  d9d4e228da9f96d96d40d526f851bcc5f10b3c54e4a3bb04e286fba66caa5c42  src/modules/pos_spec/VolOrderManagerMod.plk
  fba060b988086e3c81d150fefd9e43e1bcbf0ec1b5041917fd8cff8efcbb75e1  foundry-scripts/deploy/InitSwappableRig.s.sol

verify-import.sh reads:
  REF = $(cat offchain/rig/import-ref.txt)
  PIN = .planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md   <- DO NOT change this path
  rows matched by:  grep -F "| \`$p\` |" "$PIN" | grep -oE '[0-9a-f]{64}'

generate-pins.sh:35   REF_FILE="offchain/rig/import-ref.txt"   -> rig-pins.json .generatedFrom
deploy-rig.sh:39      IMPORT_REF=$(cat offchain/rig/import-ref.txt) -> rig-manifest.json .generatedFrom

InitSwappableRig's transitive closure — VERIFIED PRESENT, nothing further to import:
  univ4-core/ -> lib/panoptic-v2-core/lib/v4-core/src/   (remappings.txt:33)
  lib/panoptic-v2-core/lib/v4-core/src/test/PoolSwapTest.sol            exists
  lib/panoptic-v2-core/lib/v4-core/src/test/PoolModifyLiquidityTest.sol exists
  lib/panoptic-v2-core/lib/v4-core/test/utils/CurrencySettler.sol       exists
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the delta, import by checkout, re-pin 37 paths</name>
  <files>offchain/rig/import-paths.txt, offchain/rig/import-ref.txt, offchain/rig/check-upstream.sh, offchain/rig/rig-pins.json, .planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md, foundry-scripts/deploy/DeployDynamicFeeHook.s.sol, foundry-scripts/deploy/InitSwappableRig.s.sol, src/modules/pos_spec/VolOrderManagerMod.plk</files>
  <read_first>
    - offchain/rig/import-paths.txt (the 36-path list)
    - offchain/rig/import-ref.txt
    - offchain/rig/verify-import.sh (the two independent checks and the PIN path)
    - .planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md (header + row format)
    - offchain/rig/check-upstream.sh (REQUIRED_PATHS array, lines 34-45)
    - 22-RESEARCH.md section 1 (the measured delta table)
  </read_first>
  <action>
RE-MEASURE FIRST — do not trust the table above. Research's own metadata says
"Re-run `git diff --stat <import-ref> origin/develop -- $(cat offchain/rig/import-paths.txt)` at
execution start"; Phase 20 already had one research measurement expire this way.

    git fetch origin --quiet
    git rev-parse origin/develop
    bash offchain/rig/verify-import.sh                  # must exit 0 against the OLD ref FIRST
    git diff --stat "$(cat offchain/rig/import-ref.txt)" origin/develop -- $(tr '\n' ' ' < offchain/rig/import-paths.txt)
    git diff --stat "$(cat offchain/rig/import-ref.txt)" origin/develop -- foundry-scripts/ src/ notes/

If the measured delta differs from the `<interfaces>` block above (more files moved, a different
sha, a different ref), STOP and record the discrepancy in the summary before proceeding — then
proceed against the MEASURED values, not the planned ones.

Then, with `NEWREF=$(git rev-parse origin/develop)`:

1. IMPORT BY CHECKOUT (never re-type, never hand-edit):
       git checkout "$NEWREF" -- foundry-scripts/deploy/DeployDynamicFeeHook.s.sol
       git checkout "$NEWREF" -- src/modules/pos_spec/VolOrderManagerMod.plk
       git checkout "$NEWREF" -- foundry-scripts/deploy/InitSwappableRig.s.sol
   Do NOT use a `foundry-scripts/` or `notes/` glob — `notes/VOLATILITY_INSTRUMENTS.md` changed on
   develop by +753 lines and belongs to the Lean4/math track. It is NOT in `import-paths.txt` and
   must NOT appear in `git status` after this task.

2. `offchain/rig/import-paths.txt`: append the line
       foundry-scripts/deploy/InitSwappableRig.s.sol
   directly after `foundry-scripts/deploy/DeployDynamicFeeHook.s.sol` (keep the file's grouping;
   the count goes 36 -> 37).

3. `offchain/rig/import-ref.txt`: replace its single line with `$NEWREF` via
       printf '%s\n' "$NEWREF" > offchain/rig/import-ref.txt

4. `.planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md` — edit IN PLACE. This is
   THE pin file; `verify-import.sh`'s `PIN=` constant stays pointing at it (a second pin file in
   the Phase-22 directory would mean two sources of truth for the same fact). Changes:
   - header `Source ref:` -> `` `origin/develop` @ `<NEWREF>` ``
   - header `Ref subject:` -> the output of `git log -1 --format='%s' "$NEWREF"`
   - header `Imported:` -> the current UTC timestamp from `date -u +%Y-%m-%dT%H:%M:%SZ`
   - header `Path list:` -> `(37 paths)`
   - add a `Re-pinned:` line naming Phase 22 and the superseded ref `9f5ccba9…`
   - UPDATE the two digest rows and ADD one row, all three GENERATED mechanically — regenerate the
     whole table with a loop so no digest is transcribed:
         while read -r p; do
           printf '| `%s` | `%s` |\n' "$(sha256sum "$p" | cut -d' ' -f1)" "$p"
         done < offchain/rig/import-paths.txt
     and splice the result in, preserving the existing `| sha256 | path |` header row.

5. `offchain/rig/check-upstream.sh`: add `foundry-scripts/deploy/InitSwappableRig.s.sol` to the
   `REQUIRED_PATHS` array (after `DeployDynamicFeeHook.s.sol`) so the gate cannot go green on a
   develop that lacks it. Change nothing else in that file — its selector reads stay `jq`-sourced.

6. Regenerate the pin file so `generatedFrom` follows the ref:
       bash offchain/rig/generate-pins.sh
   EXPECT: `git diff --stat offchain/rig/rig-pins.json` shows exactly ONE changed line
   (`generatedFrom`). No selector and no topic0 may move — the interface files did not change in
   this delta, and `VolOrderManagerMod.plk`'s change is comment-only. If any pin VALUE moves, that
   is a FINDING: stop and record it.
  </action>
  <verify>
    <automated>bash offchain/rig/verify-import.sh</automated>
    <automated>git status --porcelain notes/ | tee /dev/stderr | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `bash offchain/rig/verify-import.sh` exits 0 and its last line reads
      `SC-1 OK: 37 imported paths content-identical to 2039f278… and sha256-matched to IMPORT-PIN.md`
      (the count is literally 37, printed by the script from the file it read).
    - `wc -l < offchain/rig/import-paths.txt` = `37`
    - `grep -c 'InitSwappableRig.s.sol' offchain/rig/import-paths.txt` = `1`
    - `grep -c 'InitSwappableRig.s.sol' offchain/rig/check-upstream.sh` = `1`
    - `cat offchain/rig/import-ref.txt` equals `git rev-parse origin/develop` exactly
    - `grep -c '^| `[0-9a-f]\{64\}` |' .planning/phases/20-deploy-rig-source-of-truth-import/IMPORT-PIN.md` = `37`
    - `sha256sum foundry-scripts/deploy/InitSwappableRig.s.sol` = `fba060b988086e3c81d150fefd9e43e1bcbf0ec1b5041917fd8cff8efcbb75e1`
    - `sha256sum foundry-scripts/deploy/DeployDynamicFeeHook.s.sol` = `f282e0942d0f013e04541957d74245bcd932bb3cbdb4f8628812bea219082fa0`
    - `sha256sum src/modules/pos_spec/VolOrderManagerMod.plk` = `d9d4e228da9f96d96d40d526f851bcc5f10b3c54e4a3bb04e286fba66caa5c42`
    - `grep -c 'TICK_SPACING = 20' foundry-scripts/deploy/DeployDynamicFeeHook.s.sol` >= `1`
    - `git status --porcelain notes/` prints NOTHING (VOLATILITY_INSTRUMENTS.md was not pulled in)
    - `git diff --stat offchain/rig/rig-pins.json` reports `1 insertion(+), 1 deletion(-)` and
      `git diff offchain/rig/rig-pins.json | grep -c 'generatedFrom'` = `2`
    - FALSIFIED: flip one hex character of any digest row in IMPORT-PIN.md ->
      `bash offchain/rig/verify-import.sh` exits 1 naming that path; `git checkout` restores exit 0.
      Separately delete the new `InitSwappableRig.s.sol` row -> exits 1 with
      `no pin row in ... for foundry-scripts/deploy/InitSwappableRig.s.sol`; restore.
      BOTH falsifications must be RUN and their output quoted in the summary.
  </acceptance_criteria>
  <done>37 paths pinned to 2039f27, verify-import.sh green and twice-falsified, rig-pins.json's generatedFrom moved and nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the vendored routers compile under --via-ir and record the delta</name>
  <files>.planning/phases/22-live-stochastic-drivers/FORGE-DELTA.md, .planning/phases/22-live-stochastic-drivers/22-CROSS-TRACK-FINDINGS.md</files>
  <read_first>
    - foundry-scripts/deploy/InitSwappableRig.s.sol (the file just imported — read all 199 lines)
    - remappings.txt (line 33, the univ4-core mapping)
    - .planning/phases/20-deploy-rig-source-of-truth-import/FORGE-DELTA.md if it exists, else
      .planning/phases/20-deploy-rig-source-of-truth-import/FORGE-BASELINE.md (the 85/27/112 shape)
    - offchain/rig/README.md ("Clean-machine sequence" — the preflight this must reproduce)
  </read_first>
  <action>
This is the LOWEST-confidence item in the whole phase and it is deliberately isolated here rather
than assumed inside the rig plan. `grep -rln 'PoolSwapTest\|PoolModifyLiquidityTest' test/ src/
foundry-scripts/` returned NOTHING before this import: no tracked file in this repository has ever
pulled the two v4-core routers into a build. `--via-ir` behaviour on them is unmeasured.

1. Preflight exactly as the README prescribes (these are idempotent and cheap if already done):
       npm ci --ignore-scripts
       git -c submodule.lib/panoptic-helper.update=none submodule update --init --recursive

2. Prove the closure:
       forge build --via-ir
   It MUST exit 0. If it does not, capture the full error and do NOT patch
   `foundry-scripts/` or `remappings.txt` — those belong to the plank track (CLAUDE.md). Record it
   as a BLOCKER finding in 22-CROSS-TRACK-FINDINGS.md and stop.

3. Prove the two routers are actually IN the build now (a green `forge build` proves nothing about
   them if the compiler skipped the file):
       ls out/PoolSwapTest.sol/PoolSwapTest.json
       ls out/PoolModifyLiquidityTest.sol/PoolModifyLiquidityTest.json
       jq -r '.bytecode.object | length' out/PoolSwapTest.sol/PoolSwapTest.json
   Both artifacts must exist with non-empty `bytecode.object`.

4. Prove `forge script` can resolve the target without touching the chain:
       forge script foundry-scripts/deploy/InitSwappableRig.s.sol --tc InitSwappableRig --via-ir
   With no `--rpc-url` and no `--broadcast` it will compile, simulate against a default local
   backend and fail on the REQUIRED `vm.envAddress("POOL_MANAGER")` — that specific failure is the
   PASS signal for this task (it proves compile + target resolution). A COMPILE error is a fail.
   Record the exact message.

5. Re-measure the forge and plank baselines the way Phase 20 did and write
   `.planning/phases/22-live-stochastic-drivers/FORGE-DELTA.md`:
       forge test 2>&1 | tail -30
       make compile-plank 2>&1 | tail -20   # if the target exists; otherwise say so explicitly
   Record: the numbers, Phase 20's post-import baseline (85 passed / 27 failed / 112 total) for
   comparison, and an explicit attribution sentence for every moved number. This is a RECORD, not
   a gate — `test/` belongs to the Solidity-testing session and nothing here may be fixed.

6. Create `.planning/phases/22-live-stochastic-drivers/22-CROSS-TRACK-FINDINGS.md` and open it with
   the finding research already identified (report, never edit — `notes/` is plank-owned):
     - `notes/DATA_CONTRACT.md` says "A same-block second write emits NOTHING." G1 establishes the
       guard is per distinct uint32 TIMESTAMP, not per block
       (`src/lib/market_state_measurements/RealizedVolatilityStateLib.plk:114`,
       `if vol_state.lastTimepointTimestamp == now { return false; }` — an equality test on the
       timestamp with no block number anywhere). The two coincide on most chains and DIVERGE on
       anvil, which mines several blocks per wall second. Wording is imprecise, behaviour is not
       wrong. Report to the plank track.
   Add any further finding surfaced by steps 2-5 to the same file.
  </action>
  <verify>
    <automated>forge build --via-ir && test -s out/PoolSwapTest.sol/PoolSwapTest.json && test -s out/PoolModifyLiquidityTest.sol/PoolModifyLiquidityTest.json</automated>
  </verify>
  <acceptance_criteria>
    - `forge build --via-ir` exits 0.
    - `out/PoolSwapTest.sol/PoolSwapTest.json` and
      `out/PoolModifyLiquidityTest.sol/PoolModifyLiquidityTest.json` both exist and
      `jq -r '.bytecode.object|length'` on each is > 2.
    - `forge script foundry-scripts/deploy/InitSwappableRig.s.sol --tc InitSwappableRig --via-ir`
      fails on a MISSING ENVIRONMENT VARIABLE (message contains `POOL_MANAGER`), never on a
      compilation or "no matching contract" error. The exact message is quoted in the summary.
    - `.planning/phases/22-live-stochastic-drivers/FORGE-DELTA.md` exists and contains the literal
      string `85` (Phase 20's baseline) alongside the newly measured numbers.
    - `.planning/phases/22-live-stochastic-drivers/22-CROSS-TRACK-FINDINGS.md` exists and
      `grep -c 'DATA_CONTRACT' 22-CROSS-TRACK-FINDINGS.md` >= 1.
    - `git status --porcelain src/ test/ foundry-scripts/ Makefile foundry.toml remappings.txt`
      shows ONLY the three checked-out import paths from Task 1 — no edit, no new file.
  </acceptance_criteria>
  <done>The routers are proven to compile and to be present as build artifacts; the delta and the DATA_CONTRACT finding are recorded; no other track's file was edited.</done>
</task>

</tasks>

<verification>
- `bash offchain/rig/verify-import.sh` exits 0 at 37 paths, and exits 1 under BOTH injected faults
- `forge build --via-ir` exits 0 with both router artifacts present
- `cabal build --enable-tests -j all && cabal test` still exits 0 (this plan touches no Haskell; a
  regression here means the pin regeneration moved a value it should not have)
- `git status --porcelain notes/` is empty
</verification>

<success_criteria>
- 37 paths content-identical to `2039f27` and sha256-matched, twice falsified
- `InitSwappableRig.s.sol` present, 199 lines, digest `fba060b9…`
- `check-upstream.sh` names it in `REQUIRED_PATHS`
- `rig-pins.json` differs from HEAD in `generatedFrom` ONLY
- The two vendored routers compile under `--via-ir` and emit bytecode artifacts
- `FORGE-DELTA.md` and `22-CROSS-TRACK-FINDINGS.md` written
</success_criteria>

<output>
After completion, create `.planning/phases/22-live-stochastic-drivers/22-01-SUMMARY.md`.
Record: the re-measured delta (and whether it matched the plan), both falsification outputs
verbatim, the `forge script` env-failure message, and the forge/plank numbers with attribution.
</output>
