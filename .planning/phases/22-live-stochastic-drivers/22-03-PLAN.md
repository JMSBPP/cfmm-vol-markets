---
phase: 22-live-stochastic-drivers
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - offchain/rig/deploy-rig.sh
  - offchain/rig/verify-rig.sh
  - offchain/rig/README.md
  - offchain/lib/Rig/Manifest.hs
  - offchain/lib/VolOrder/Decode.hs
  - offchain/test/Main.hs
autonomous: true
requirements: [DRIV-01, DRIV-02]

must_haves:
  truths:
    - "One command takes a machine with no anvil to a SWAPPABLE rig whose probe swap provably wrote a timepoint"
    - "The rig's pool has tickSpacing 20 (F2), so the standing tickSpacing-10 rig is provably rebuilt"
    - "The manifest carries nine contracts including both routers, and Rig.Manifest refuses a manifest missing either"
    - "Two from-scratch runs still produce a byte-identical manifest once generatedAt is excluded"
    - "The chain clock and the RealizedVolatilityMod seed share one deterministic origin"
  artifacts:
    - path: "offchain/rig/deploy-rig.sh"
      provides: "6-script rig lifecycle incl. InitSwappableRig, anvil --timestamp, probe-ts assertion"
      contains: "InitSwappableRig"
    - path: "offchain/rig/verify-rig.sh"
      provides: "liveness probes for nine contracts incl. both routers' poolManager()"
    - path: "offchain/lib/Rig/Manifest.hs"
      provides: "required_contracts of nine"
      contains: "PoolSwapTest"
  key_links:
    - from: "offchain/rig/deploy-rig.sh"
      to: "foundry-scripts/deploy/InitSwappableRig.s.sol"
      via: "forge script --tc InitSwappableRig --broadcast --via-ir with POOL_MANAGER/HOOK/TOKEN0/TOKEN1 env"
      pattern: "InitSwappableRig"
    - from: "offchain/rig/deploy-rig.sh"
      to: "offchain/rig/rig-manifest.json .contracts.PoolSwapTest"
      via: "broadcast JSON select(.contractName==\"PoolSwapTest\") cross-checked against the console line"
      pattern: "PoolSwapTest"
---

<objective>
Extend the rig lifecycle so one command produces a SWAPPABLE pool: the two vendored v4-core
routers deployed, one full-range position minted, and a probe swap whose passing assertion is
proof that timepoints self-write through `DynamicFeeHook.beforeSwap`.

Purpose: this is the whole of the corrected DRIV-01 mechanism on the rig side. Until
`InitSwappableRig` runs, `DynamicFeeHook`'s pool has ZERO liquidity and there is no
unlock-callback router anywhere on chain — a swap from an EOA is impossible, so no timepoint can
ever be written by the hook. It also rebuilds the STALE rig: F2 moved `TICK_SPACING` 10 -> 20,
which changes the PoolKey hash and therefore the poolId.

Output: a 6-step `deploy-rig.sh` with a deterministic clock origin, a 9-contract manifest, router
liveness probes, an SC-5 reproducibility re-measurement, and a README that is no longer wrong in
three places.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-live-stochastic-drivers/22-CONTEXT.md
@.planning/phases/22-live-stochastic-drivers/22-RESEARCH.md
@.planning/phases/22-live-stochastic-drivers/22-01-SUMMARY.md
@offchain/rig/deploy-rig.sh
@offchain/rig/verify-rig.sh
@offchain/rig/README.md
@offchain/lib/Rig/Manifest.hs
@foundry-scripts/deploy/InitSwappableRig.s.sol

<interfaces>
<!-- Read directly out of the file imported by 22-01. Use verbatim. -->

INVOCATION (no --ffi: pure Solidity; --broadcast REQUIRED for the manifest workflow):
  env POOL_MANAGER=$PM HOOK=$HOOK TOKEN0=$CURRENCY0 TOKEN1=$CURRENCY1 \
    forge script foundry-scripts/deploy/InitSwappableRig.s.sol --tc InitSwappableRig \
      --rpc-url local --broadcast --via-ir

  All four env vars are vm.envAddress and REQUIRED (no envOr default).
  require(t0 < t1, "TOKEN0/TOKEN1 must be the SORTED manifest currencies") — deploy-rig.sh's
  CURRENCY0/CURRENCY1 already come out sorted from DeployDynamicFeeHook's console.
  Approvals are deployer -> ROUTERS. Do NOT approve the PoolManager (the plank track corrected our
  issue's wording on exactly this: settlement is CurrencySettler.settle -> transferFrom pulled BY
  the router).

THE NINE CONSOLE LINES (label spacing is exact; deploy-rig.sh's check() greps a label prefix and
extracts 0x[0-9a-fA-F]{40}, so the two router labels work verbatim):
  swapRouter            : 0x…
  modifyLiquidityRouter : 0x…
  tickLower             : -887260
  tickUpper             : 887260
  liquidity             : 1000000000000000000000
  probe delta0          : …
  probe delta1          : …
  timepoint ts before   : …
  timepoint ts after    : …

BROADCAST SHAPE: both routers are plain `new X(...)` under startBroadcast, so foundry records them
as TOP-LEVEL CREATE with a POPULATED contractName. Key them by name —
  jq -r '[.transactions[] | select(.contractName=="PoolSwapTest")][0].contractAddress'
— the `PriceSetterHook` pattern, NOT the nameless-Plank-CREATE pattern used by addr_of().
Record path: broadcast/InitSwappableRig.s.sol/31337/run-latest.json

THE SCRIPT'S OWN REQUIREs (a passing run already proves all of these):
  code at all four env addresses; t0 < t1;
  reconstructed PoolKey (currency0, currency1, 0x800000, TICK_SPACING=20, hook) hashes to
    hook.poolId()  — a pre-registered KEY-DRIFT detector;
  tsAfter > tsBefore ("probe swap wrote NO timepoint (same-second B1 no-op?) - rig NOT proven");
  getAverageVolatility(int24(0), uint32(target)) returns 32 bytes.

IT ADVANCES THE CLOCK ITSELF BY 5s: vm.warp(block.timestamp + 5) plus, on chainid 31337, an
evm_increaseTime of 5 through a low-level address(vm).call. Consequence: after this script the
chain clock is at least deploy_ts + 5 and ONE timestamp has already been consumed by the probe.
The driver's first step must be strictly later — plan 22-05 owns that guard.

CURRENT deploy-rig.sh anchors (line numbers as of HEAD):
  :30  INIT_TS=1700000000        :38  CHAIN_ID=31337       :88  nohup anvil --silent …
  :106 rm -rf broadcast/…        :130 FS=(--rpc-url … --broadcast --ffi --via-ir)
  :146 the 5th script            :158-162 the B_* broadcast paths
  :192 PM=…select(.contractName=="PoolManager")…B_HOOK
  :199 the address-shape loop    :231 check()  :260 the jq -n manifest emitter

MEASURED (anvil 1.5.1, research section 5): `anvil --timestamp 1700000000` works; the chain clock
is anchored at that value and then advances with wall clock (13 s of real time -> 1700000013).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deploy-rig.sh gains a deterministic clock origin and the InitSwappableRig step</name>
  <files>offchain/rig/deploy-rig.sh</files>
  <read_first>
    - offchain/rig/deploy-rig.sh (whole file — every anchor listed in <interfaces>)
    - foundry-scripts/deploy/InitSwappableRig.s.sol (whole file — the env contract, the nine
      console lines, the requires)
    - offchain/rig/rig-manifest.json (the current 7-contract shape this extends)
  </read_first>
  <action>
Edit `offchain/rig/deploy-rig.sh` ONLY (never `foundry-scripts/`). Five changes:

1. LINE 88 — give the chain a deterministic origin:
       nohup anvil --silent --timestamp "$INIT_TS" >"$LOG_DIR/anvil.log" 2>&1 &
   Add a comment recording WHY, because it is not obvious: `INIT_TS` already seeds
   `RealizedVolatilityMod`'s module-global series, but `DeployDynamicFeeHook` seeds the HOOK's
   buffer at `uint32(block.timestamp)`. Without `--timestamp` those are two unrelated clocks — the
   module at 1.7e9 and the hook at wall clock (~1.78e9 in Aug 2026) — and any driver computing
   `INIT_TS + k*stride` would be setting timestamps ~2.7 YEARS in the past, which anvil rejects
   outright. With it, both series share one origin. NOTE HONESTLY in the comment that anvil's
   clock still ADVANCES with wall time from that anchor (measured: 13 s of real time -> block
   timestamp 1700000013), so `--timestamp` fixes the ORIGIN, not the rate; the driver must still
   read the chain head rather than assume `INIT_TS`.

2. LINE 106 — add `broadcast/InitSwappableRig.s.sol` to the stale-record `rm -rf` list. Not
   optional: a failed run leaves the previous run's `run-latest.json` and the extractor below would
   emit a stale router address.

3. AFTER line 147 (the PriceSetterHook step) — the 6th step. It runs LAST because it consumes the
   4th script's outputs, and it takes a DIFFERENT flag set (no `--ffi`, and `--tc`):

       run_deploy "$LOG_DIR/06-swappable.log" \
         env POOL_MANAGER="$PM" HOOK="$HOOK" TOKEN0="$CURRENCY0" TOKEN1="$CURRENCY1" \
         forge script foundry-scripts/deploy/InitSwappableRig.s.sol --tc InitSwappableRig \
           --rpc-url "$RPC_ALIAS" --broadcast --via-ir

   PROBLEM TO SOLVE, not to work around: `PM`, `HOOK`, `CURRENCY0` and `CURRENCY1` are extracted in
   Step 7/`console_field`, which currently runs AFTER all the scripts. Move the extraction of those
   four values (the `B_HOOK` existence check, `HOOK=`, `PM=`, `CURRENCY0=`, `CURRENCY1=`) to
   immediately after the 4th script's `run_deploy`, and leave the remaining extraction and ALL of
   the Step 8 cross-checks where they are. Do NOT duplicate the extraction logic — move it.

4. AFTER the 6th step — the probe assertion, which turns the script's internal `require` into a
   rig-level acceptance check we own (the same move Step 6 already makes for `seeded : true`):

       TS_BEFORE=$(console_field 'timepoint ts before   :' "$LOG_DIR/06-swappable.log")
       TS_AFTER=$(console_field 'timepoint ts after    :' "$LOG_DIR/06-swappable.log")
       case "$TS_BEFORE$TS_AFTER" in ''|*[!0-9]*) FATAL … ;; esac
       [ "$TS_AFTER" -gt "$TS_BEFORE" ] || {
         echo "FATAL: probe swap did not advance the hook's timepoint clock ($TS_BEFORE -> $TS_AFTER)." >&2
         echo "       The rig is NOT proven swappable: beforeSwap either did not run or hit the" >&2
         echo "       G1 same-second no-op. E3 is the ground truth of what landed, never the swap count." >&2
         exit 1; }
       echo "  probe swap wrote a timepoint: $TS_BEFORE -> $TS_AFTER"

   `console_field` is defined at line 211; it must be moved above its first use if the reordering
   in (3) puts a call earlier than the definition.

5. STEP 7/8/9 — the two routers:
       B_SWAP=broadcast/InitSwappableRig.s.sol/31337/run-latest.json
       (add to the existence loop)
       SWAPR=$(jq -r '[.transactions[] | select(.contractName=="PoolSwapTest")][0].contractAddress' "$B_SWAP" | tr 'A-Z' 'a-z')
       LIQR=$(jq -r '[.transactions[] | select(.contractName=="PoolModifyLiquidityTest")][0].contractAddress' "$B_SWAP" | tr 'A-Z' 'a-z')
       (add "PoolSwapTest:$SWAPR" and "PoolModifyLiquidityTest:$LIQR" to the address-shape loop)
       check 'swapRouter            :'    "$LOG_DIR/06-swappable.log" "$SWAPR"
       check 'modifyLiquidityRouter :'    "$LOG_DIR/06-swappable.log" "$LIQR"
   and in the `jq -n` manifest emitter add `--arg swapr` / `--arg liqr` and the two entries
       PoolSwapTest: $swapr,
       PoolModifyLiquidityTest: $liqr
   inside `contracts`. Add both to the Step-10 summary `printf`.

   DECISION, record it in a comment: only the two ADDRESSES go into the manifest. The tick range,
   liquidity and probe deltas stay on the console and in the log — they are proof the script
   produced, not values any driver reads, and every mandatory manifest field is a field
   `Rig.Manifest` will refuse to load without.

Do NOT touch the `FS=(…)` array — the other five scripts still need `--ffi`.
  </action>
  <verify>
    <automated>bash offchain/rig/deploy-rig.sh && jq -r '.contracts | keys | length' offchain/rig/rig-manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - `bash offchain/rig/deploy-rig.sh` exits 0 and its output contains the line
      `probe swap wrote a timepoint: <before> -> <after>` with after > before
    - `grep -c 'InitSwappableRig' offchain/rig/deploy-rig.sh` >= 2 (the run + the broadcast path)
    - `grep -c '\-\-ffi' "$(grep -n 'InitSwappableRig.s.sol --tc' offchain/rig/deploy-rig.sh | head -1)"`
      — restated concretely: the line invoking `InitSwappableRig` must NOT contain `--ffi`
      (`grep -n 'InitSwappableRig.s.sol' offchain/rig/deploy-rig.sh | grep -c ffi` = 0)
    - `grep -c 'anvil --silent --timestamp' offchain/rig/deploy-rig.sh` = 1
    - `jq -r '.contracts | keys | length' offchain/rig/rig-manifest.json` = `9`
    - `jq -r '.contracts.PoolSwapTest, .contracts.PoolModifyLiquidityTest' offchain/rig/rig-manifest.json`
      prints two distinct 42-char `0x…` addresses, neither equal to any other contract's address
    - `jq -r '.pool.tickSpacing' offchain/rig/rig-manifest.json` = `20` (THE proof the stale
      tickSpacing-10 rig was rebuilt, not patched)
    - `jq -r '.generatedFrom' offchain/rig/rig-manifest.json` equals `cat offchain/rig/import-ref.txt`
    - `cast block --rpc-url local 0 --field timestamp` = `1700000000`
    - `cast code "$(jq -r .contracts.PoolSwapTest offchain/rig/rig-manifest.json)" --rpc-url local`
      returns more than 2 characters
    - FALSIFIED: temporarily change the probe assertion to compare `-lt`, re-run, and confirm the
      script exits 1 with the FATAL message; restore and confirm exit 0. RUN this, quote the output.
    - `git status --porcelain foundry-scripts/ src/ test/ Makefile foundry.toml remappings.txt` empty
  </acceptance_criteria>
  <done>One command produces a swappable rig with nine contracts, a genesis timestamp of 1700000000, tickSpacing 20, and an owned assertion that the probe swap actually wrote a timepoint.</done>
</task>

<task type="auto">
  <name>Task 2: Rig.Manifest requires nine, verify-rig.sh probes the routers, stale tickSpacing comments corrected</name>
  <files>offchain/lib/Rig/Manifest.hs, offchain/rig/verify-rig.sh, offchain/test/Main.hs, offchain/lib/VolOrder/Decode.hs</files>
  <read_first>
    - offchain/lib/Rig/Manifest.hs lines 224-244 (required_contracts and its haddock)
    - offchain/rig/verify-rig.sh (the probe structure and the final "N contracts live" line)
    - offchain/test/Main.hs lines 460-505 (sc3_load_succeeds asserts `n == 7`;
      sc3_corrupted_manifest_fails deletes VolOrderManagerMod from a copy)
    - offchain/lib/VolOrder/Decode.hs lines 126-158 (the storage-layout haddock whose closing
      paragraph claims the pool's tickSpacing is 10)
    - offchain/test/Main.hs, the `module_tick_spacing` binding and its comment
  </read_first>
  <action>
1. `offchain/lib/Rig/Manifest.hs` — add `"PoolSwapTest"` and `"PoolModifyLiquidityTest"` to
   `required_contracts`. Update the haddock's closing sentence ("Extra contracts are always
   accepted; these seven are not optional") to say NINE, and add one sentence explaining why the
   routers are mandatory rather than optional: the cheat-swap driver cannot send a swap without
   the router address, and a rig whose `InitSwappableRig` step did not run is a rig with a
   zero-liquidity pool where no timepoint can ever be written — precisely the "broken rig, not a
   rig with a zero address" case this list exists to catch.

2. `offchain/rig/verify-rig.sh` — Probe 1 already loops over every contract in the manifest, so
   the two routers get a bytecode probe for free and `N` becomes 9. Add ONE new probe that a
   live-vs-empty check cannot fake, in the style of Probe 5:

       # --- Probe 6: both routers point at the SAME PoolManager the hook is bound to -------
   For each of `PoolSwapTest` and `PoolModifyLiquidityTest`, `cast call <addr> "manager()(address)"`
   and require it equals `.contracts.PoolManager` (lowercased both sides, as the file already
   does). `PoolTestBase` exposes the immutable as `manager`. If the accessor name turns out to be
   different, READ it out of
   `lib/panoptic-v2-core/lib/v4-core/src/test/PoolTestBase.sol` and use the real one — do not
   guess, and do not fall back to a bytecode-only probe.
   This is the router analogue of the fault Phase 20 measured: a router bound to the OTHER live
   PoolManager is live, has bytecode, and is useless.

3. `offchain/test/Main.hs` — `sc3_load_succeeds` asserts `n == 7`. Change to `9` and update its
   message. Confirm `sc3_corrupted_manifest_fails` still passes unchanged (it deletes
   `VolOrderManagerMod`, which is still required).

4. TWO STALE COMMENTS, now factually wrong after F2, both in OUR territory:
   - `offchain/lib/VolOrder/Decode.hs` (the `unpack_vol_order_storage` haddock) states "That
     constant is TICK_SPACING = 20, while the rig's own deployed pool has tickSpacing = 10 …
     That is a real discrepancy". F2 changed `DeployDynamicFeeHook.s.sol` to `TICK_SPACING = 20`,
     so module and pool now AGREE. Rewrite the paragraph to say the discrepancy was RESOLVED
     upstream in PR #18 / ref `2039f27`, name F2, and keep the explanation of WHY tickSpacing is
     read-and-discarded (that part is still true and still load-bearing).
   - `offchain/test/Main.hs`'s `module_tick_spacing` comment carries the same stale claim. Same
     correction. The VALUE `20` does not change; only the claim about the pool does.
   Do NOT silently delete these paragraphs — the record of a resolved discrepancy is worth more
   than a clean comment.
  </action>
  <verify>
    <automated>cabal build --enable-tests -j all && cabal test 2>&1 | grep -E 'sc3_load_succeeds|checks passed' && bash offchain/rig/verify-rig.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash offchain/rig/verify-rig.sh` exits 0 and its final line reads
      `SC-2 OK: 9 contracts live, RealizedVolatilityMod seeded (packed=…)`
    - `verify-rig.sh` output contains a PASS line for the router->PoolManager binding of BOTH
      routers
    - `grep -c 'PoolSwapTest' offchain/lib/Rig/Manifest.hs` >= 1 and
      `grep -c 'PoolModifyLiquidityTest' offchain/lib/Rig/Manifest.hs` >= 1
    - `cabal test` prints `PASS sc3_load_succeeds` and exits 0;
      `cabal build --enable-tests -j all 2>&1 | grep -ci warning` = 0
    - `grep -c 'tickSpacing = 10' offchain/lib/VolOrder/Decode.hs offchain/test/Main.hs` = 0 in both
    - `grep -c '2039f27\|PR #18\|F2' offchain/lib/VolOrder/Decode.hs` >= 1
    - FALSIFIED: with the rig standing, copy the manifest, delete `.contracts.PoolSwapTest` from
      the copy, and run `RIG_MANIFEST=<copy> cabal test` — `sc3_load_succeeds` must FAIL naming the
      missing contract. Also run `RIG_MANIFEST=<copy> bash offchain/rig/verify-rig.sh` and confirm
      it no longer reports 9. RUN both; quote the output.
    - FALSIFIED 2: point one router's manifest entry at the OTHER live `PriceSetterPoolManager`
      address in a copy and confirm Probe 6 exits 1. This is the fault a bytecode probe cannot see.
  </acceptance_criteria>
  <done>Nine contracts are mandatory and probed for real binding, not just for bytecode; the two now-false tickSpacing comments record the resolution instead of the stale claim.</done>
</task>

<task type="auto">
  <name>Task 3: SC-5 reproducibility re-measured, README's three stale spots fixed</name>
  <files>offchain/rig/README.md</files>
  <read_first>
    - offchain/rig/README.md (whole file — the clean-machine sequence, "What each step needs" table
      row for `cabal test`, the "What the last step does and does not prove" section, "The seven
      contracts", "Reproducibility")
    - .planning/phases/22-live-stochastic-drivers/22-01-SUMMARY.md (the ref now pinned)
    - .planning/STATE.md, the 21-05 paragraph (the measured chain-independence and the mining demo
      order — the two facts the README currently contradicts)
  </read_first>
  <action>
1. RE-MEASURE SC-5. Phase 20's byte-identical-manifest property must not be INHERITED across the
   `--timestamp` change and the new sixth script — re-run it:
       bash offchain/rig/deploy-rig.sh && jq -S 'del(.generatedAt)' offchain/rig/rig-manifest.json > /tmp/rig-run1.json
       bash offchain/rig/deploy-rig.sh && jq -S 'del(.generatedAt)' offchain/rig/rig-manifest.json > /tmp/rig-run2.json
       diff -u /tmp/rig-run1.json /tmp/rig-run2.json
       sha256sum /tmp/rig-run1.json /tmp/rig-run2.json
   The diff MUST be empty and the two digests equal, with `generatedAt` differing between the two
   raw files (that difference is what proves run 2 actually regenerated rather than the comparison
   reading one file twice). If ANY field moves — especially `pool.poolId` or either router — record
   exactly which and why before proceeding; a nondeterministic address is a finding, not noise.
   Record the normalised sha256 in the summary the way 20-03 did.

2. README FIXES — three stale spots, all measured wrong today:
   (a) The clean-machine sequence prints `cabal build -j all && cabal test`. That gate is VACUOUS —
       Phase 21 confirmed it FOUR times (exit 0 against a test suite that would not compile).
       Replace with `cabal build --enable-tests -j all && cabal test` and add a one-line note
       saying why, so nobody "simplifies" it back.
   (b) The "What each step needs" row for `cabal test` says "a running rig — it does NOT skip when
       the rig is down, it fails". Phase 21 MEASURED the suite chain-independent: rig stopped,
       `pgrep anvil` empty, `cast block-number` erroring, `cabal test` still exit 0. Rewrite the
       row: `cast` on PATH is still needed (`sc4_cast_agreement`); a running rig is needed only for
       the manifest-consuming checks (`sc3_load_succeeds`, `sc3_corrupted_manifest_fails`) because
       `rig-manifest.json` is gitignored — and those FAIL rather than skip. State both halves.
   (c) The "What the last step does and does not prove" section says `cabal run` does not place a
       vol order and comes back `status reverted`. Phase 21 (21-05) measured the demo order MINING,
       with an `ORDER_CREATED` log carrying `target_vega 1000000000000000000` and the batch at
       2 succeeded / 0 failed. Replace the stale paragraph with what is now true.

3. README ADDITIONS:
   - the sixth step in the clean-machine sequence, with its distinct flag set (`--tc`,
     `--broadcast --via-ir`, and explicitly NO `--ffi`) and one sentence on what its probe proves;
   - "The seven contracts" -> NINE, listing `PoolSwapTest` and `PoolModifyLiquidityTest` and what
     each is for;
   - a short "Why the rig owns the clock" paragraph: `anvil --timestamp "$INIT_TS"` gives the chain
     the same origin as the `RealizedVolatilityMod` seed, and the timepoint write guard is one
     write per distinct uint32 TIMESTAMP (not per block) — so a driver that does not advance the
     clock silently loses writes while its receipts stay status 1.
   - update the "Reproducibility" section with the newly measured normalised sha256.
  </action>
  <verify>
    <automated>bash offchain/rig/deploy-rig.sh && jq -S 'del(.generatedAt)' offchain/rig/rig-manifest.json | sha256sum</automated>
  </verify>
  <acceptance_criteria>
    - Two from-scratch `deploy-rig.sh` runs produce `diff -u` EMPTY over
      `jq -S 'del(.generatedAt)'`, and the two raw files' `generatedAt` values DIFFER. Both facts
      quoted in the summary with the normalised sha256.
    - `grep -c 'cabal build -j all' offchain/rig/README.md` = 0
    - `grep -c 'cabal build --enable-tests -j all' offchain/rig/README.md` >= 1
    - `grep -c 'status reverted' offchain/rig/README.md` = 0
    - `grep -c 'does NOT skip when the rig is down' offchain/rig/README.md` = 0
    - `grep -c 'InitSwappableRig' offchain/rig/README.md` >= 1 and
      `grep -c 'PoolSwapTest' offchain/rig/README.md` >= 1
    - `grep -c 'seven contracts' offchain/rig/README.md` = 0; `grep -ci 'nine contracts' README` >= 1
    - `grep -ci 'timestamp' offchain/rig/README.md` >= 1 covering the clock paragraph
    - Every command in the README's clean-machine sequence RUN top to bottom, each exiting 0, with
      the exit codes recorded (the Phase-20 SC-5 precedent). If any step fails, that is the finding.
  </acceptance_criteria>
  <done>SC-5 re-measured under the new clock and the new sixth script; the README's three measured-wrong statements are corrected and the new step documented.</done>
</task>

</tasks>

<verification>
- `bash offchain/rig/deploy-rig.sh` exits 0 with the probe-timepoint line
- `bash offchain/rig/verify-rig.sh` exits 0 at 9 contracts, with router binding probes
- `bash offchain/rig/verify-import.sh` still exits 0 at 37 paths (this plan touches no imported path)
- `cabal build --enable-tests -j all && cabal test` exits 0, zero `-Wall` warnings
- `git status --porcelain src/ test/ foundry-scripts/ Makefile foundry.toml remappings.txt` empty
- LEAVE THE RIG RUNNING — plan 22-04 needs a standing swappable rig. Note the pid and block height
  in the summary.
</verification>

<success_criteria>
- Nine contracts in the manifest, both routers cross-checked broadcast-vs-console
- `pool.tickSpacing` = 20 and genesis timestamp = 1700000000
- The probe swap's timepoint advance is asserted BY US, and the assertion is falsified
- SC-5 byte-identical manifest re-measured, not inherited
- README correct in all three previously-stale places, and documents the sixth step
</success_criteria>

<output>
After completion, create `.planning/phases/22-live-stochastic-drivers/22-03-SUMMARY.md`.
Record: the observed `timepoint ts before -> after` values, the SC-5 normalised sha256 and the two
`generatedAt` values, the router `manager()` accessor actually found, both falsification outputs,
and the rig's pid + block height at exit.
</output>
