---
phase: 21-v2-abi-re-pin-targetvega-generation
plan: 05
type: execute
wave: 4
depends_on: ["21-02", "21-03", "21-04"]
files_modified:
  - offchain/test/Main.hs
  - offchain/rig/peer-haskell-bytes.json
  - .planning/phases/21-v2-abi-re-pin-targetvega-generation/21-CROSS-TRACK-FINDINGS.md
  - docs/superpowers/verification/2026-07-22-stochastic-order-gen-verification.md
autonomous: true
requirements: [RPIN-05]
must_haves:
  truths:
    - "cabal test asserts the captured live bytes against the v4.0 external-encoder golden, including N=0 at exactly 64 bytes"
    - "The captured bytes decode through the SHIPPED decode_create_orders_result"
    - "A stale capture reddens — the assertion checks the capture's provenance against the current manifest"
    - "cabal test still runs green with anvil DOWN, so chain-independence is preserved"
    - "The three cross-track findings are REPORTED in a durable artifact and no other track's file was edited"
  artifacts:
    - path: "offchain/test/Main.hs"
      provides: "RPIN-05 assertions over the committed capture"
      contains: "rpin05_"
    - path: "offchain/rig/peer-haskell-bytes.json"
      provides: "Haskell-produced decode results for the golden fixture cases, offered to the Solidity-testing track"
      contains: "_offer_to"
    - path: ".planning/phases/21-v2-abi-re-pin-targetvega-generation/21-CROSS-TRACK-FINDINGS.md"
      provides: "F1, the tickSpacing discrepancy and the obsolete follow-up, addressed to their owning tracks"
      min_lines: 40
  key_links:
    - from: "offchain/test/Main.hs"
      to: "offchain/rig/batch-return-capture.json"
      via: "eitherDecodeFileStrict, diffed against the golden and fed to the shipped decoder"
      pattern: "batch-return-capture.json"
    - from: "offchain/test/Main.hs"
      to: "offchain/rig/rig-manifest.json"
      via: "freshness assertion — the capture's manager must match the current manifest"
      pattern: "rig_contracts"
---

<objective>
Turn plan 21-02's live capture into `cabal test` assertions WITHOUT regressing the suite's
chain-independence, and discharge the phase's report-don't-edit obligations.

Purpose: RPIN-05 says *verified byte-unchanged against the live module*. Research section 5.4 named
the design tension and this plan takes option (a): assert against a provenance-bearing committed
artifact rather than making a live call inside the suite. `cabal test` is chain-independent TODAY
(measured 44/44 with anvil down); a live call would make every future contributor's first
`cabal test` red for an unrelated reason, and the whole value of the pin suite is that it is cheap
and always runnable.

Output: `rpin05_` checks in the single suite; a Haskell-produced bytes artifact offered to the
Solidity-testing track; one findings document carrying F1, the `TICK_SPACING` discrepancy and the
obsolete v4.0 follow-up; and the phase gate run end to end.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-v2-abi-re-pin-targetvega-generation/21-CONTEXT.md
@.planning/phases/21-v2-abi-re-pin-targetvega-generation/21-RESEARCH.md
@.planning/phases/21-v2-abi-re-pin-targetvega-generation/21-02-SUMMARY.md
@.planning/phases/21-v2-abi-re-pin-targetvega-generation/21-03-SUMMARY.md
@.planning/phases/21-v2-abi-re-pin-targetvega-generation/21-04-SUMMARY.md
@offchain/test/Main.hs
@offchain/lib/VolOrder/Decode.hs
@offchain/lib/Rig/Manifest.hs
@docs/superpowers/verification/2026-07-22-stochastic-order-gen-verification.md
</context>

<interfaces>
THE COMMITTED CAPTURE, written by plan 21-02 (`offchain/rig/batch-return-capture.json`):

    { "generatedAt": "...Z", "chainId": 31337, "manager": "0x...", "blockNumber": N,
      "_provenance": "...", "_scope_limit": "...", "_golden_diff": { ... },
      "cases": [ { "name": "N0_empty",             "n": 0, "calldata": "...",
                   "returndata": "...", "returndata_bytes": 64 },
                 { "name": "N1_success",           "n": 1, ... },
                 { "name": "N2_success_then_fail", "n": 2, ... },
                 { "name": "N1_dirty_vega",        "n": 1, ... } ] }

THE v4.0 GOLDEN (`test/pos_spec/fixtures/vol_order_return_golden.json`) — the Solidity-testing
track's territory, READ ONLY:

    names  = ["N0_empty","N1_success","N2_success_then_fail","N3_mixed_seeded_C5","N3_all_invalid"]
    ns     = [0, 1, 2, 3, 3]
    expected[0] = the 64-byte empty encoding (outer offset word 0x20, length word 0)
    peer_haskell_bytes = five "PLACEHOLDER -- NOT-PEER-VERIFIED" entries
    _peer_status       = records that this rpc_api track never delivered Haskell-produced bytes

The `expected` bytes were produced by `cast abi-encode` (alloy, cast 1.5.1-stable) — an encoder
OUTSIDE this repo. Its INPUTS are 3-field and predate V2, but the return encoding is a function of
`(success, id)` only, so the `expected` strings remain valid across the version change. That is what
makes this a legitimate cross-version, cross-implementation check.

THE RETURN LAYOUT (`src/modules/pos_spec/VolOrderManagerMod.plk:214-216, 249, 265-269`):

    0x00  0x20    outer offset word
    0x20  count   length in ELEMENTS
    0x40  success[0] (canonical 0 or 1) / 0x60 orderId[0] / ...
    TOTAL = 64 + 64*count.  N = 0 is EXACTLY 64 bytes -- not 0 and not 32.

THE SHIPPED DECODER, unchanged by this phase and to be REUSED, never re-implemented:

    decode_create_orders_result :: HexString -> Either String [(Bool, Integer)]

It already computes `expected_length = 64 + 64 * count` and already rejects non-canonical bool
words. v4.0 measured that solc's `abi.decode` REVERTS outright on a success word of 2; a lenient
decoder would silently disagree with `abi.decode` about the same bytes, which is why the strictness
is a consumer-side contract and not a test detail.

`Rig.Manifest` accessors for the freshness assertion: `load_rig_from`, `Rig (..)`,
`RigAddresses (..)`, `rig_addrs`, `rig_contracts`. The suite already calls
`load_rig_from pins_file manifest_file` inside `sc3_load_succeeds` (Main.hs:415-426).

CARRIED CONSUMER CONTRACTS from v4.0's exit record — still live, do not re-litigate: the canonical
array offset `0x40` at byte 36 is a HARD encoding requirement (a legally padded head is rejected
with an empty revert); `N = 0` returns exactly 64 bytes; success words are canonically 0 or 1.

HAZARD F1, for the findings document: `src/modules/pos_spec/VolOrderManagerMod.plk:177-188` carries
a stale V1 comment block that contradicts that same file's V2 code at lines 221-235. Plank's
territory. REPORT, never edit.
</interfaces>

<constraints>
CHAIN-INDEPENDENCE IS THIS PLAN'S DESIGN POINT. The new checks read the COMMITTED capture file. They
must not open a socket, must not shell out to `cast call`, and must not require anvil. Prove it by
running `cabal test` with anvil DOWN as an explicit acceptance step.

`sc3_literal_purge` scans `offchain/**/*.{hs,sh}`. The captured byte strings live in `.json` files
and must be READ from them — never pasted into `Main.hs`.

Other tracks' files are READ-ONLY: `test/` (Solidity-testing), `src/` and `foundry-scripts/`
(plank), `Makefile` / `foundry.toml` / `remappings.txt`. `../plank/` is read-only. The peer bytes go
into an `offchain/`-side artifact and are OFFERED; the fixture's placeholders are not edited here.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: RPIN-05 assertions over the committed capture — golden diff, N=0 at 64 bytes, shipped-decoder decode, freshness guard</name>
  <read_first>
    - offchain/rig/batch-return-capture.json (as produced by 21-02 — the exact key names)
    - test/pos_spec/fixtures/vol_order_return_golden.json (READ ONLY: names, ns, expected)
    - offchain/lib/VolOrder/Decode.hs (decode_create_orders_result:81-102 and its header comment)
    - offchain/test/Main.hs (the aeson Value-walking idiom at 440-470, the Check plumbing at 256-275, sc3_load_succeeds at 415-426)
    - .planning/phases/21-v2-abi-re-pin-targetvega-generation/21-02-SUMMARY.md (what the capture actually contains, and any golden disagreement already recorded)
  </read_first>
  <files>offchain/test/Main.hs</files>
  <action>
Decode both JSON files with the `Data.Aeson (Value)` + `Data.Aeson.KeyMap` idiom the module already
uses at lines 440-470. Do NOT add a `FromJSON` instance or a new type — two small `Value`-walking
helpers keep the suite's dependency surface flat.

Constants to add:

    capture_file :: FilePath
    capture_file = "offchain/rig/batch-return-capture.json"

    golden_file :: FilePath
    golden_file = "test/pos_spec/fixtures/vol_order_return_golden.json"
    -- READ ONLY. The Solidity-testing track's fixture. Its `expected` bytes come from
    -- cast abi-encode (alloy), an encoder OUTSIDE this repo, which is what makes the diff below
    -- a cross-implementation check rather than a self-agreement.

    capture_command :: String
    capture_command = "bash offchain/rig/capture-batch-return.sh"

CHECK 1 — `rpin05_capture_is_present_and_fresh`. FAIL, never skip, when the capture is missing: the
same discipline as `sc3_load_succeeds`, because a suite that goes quietly green when an artifact is
absent is worse than one that goes red. Name the resolved path AND `capture_command` in the message.
Then assert freshness:
  - the capture's `chainId` equals the manifest's `chainId`;
  - the capture's `manager` lowercased equals the manifest's `contracts.VolOrderManagerMod`
    lowercased, obtained through `load_rig_from pins_file manifest_file` and
    `rig_contracts . rig_addrs` — not by re-parsing the manifest by hand;
  - `cases` has length 4 and the names are exactly
    `["N0_empty","N1_success","N2_success_then_fail","N1_dirty_vega"]`.
This is what makes a STALE capture redden instead of passing quietly. If `rig-manifest.json` is
absent, fail naming `bash offchain/rig/deploy-rig.sh` — the manifest is gitignored, so that is the
one legitimate not-runnable case and it must say so loudly rather than skipping.

CHECK 2 — `rpin05_live_bytes_match_the_external_golden`, the requirement's core claim:
  - `N0_empty`: the captured `returndata` lowercased must equal `expected[0]` lowercased EXACTLY,
    and its byte length must be exactly 64. Assert BOTH — the length assertion is what catches a
    consumer that treats an empty batch as empty returndata, a failure invisible on-chain and named
    in v4.0's exit record as the clause most likely to break `StochasticOrderGen`.
  - `N1_success` and `N2_success_then_fail`: compare against `expected[1]` / `expected[2]`
    STRUCTURALLY, word by word. Assert word 0 (the outer offset) is `0x20`, word 1 (the length in
    ELEMENTS) equals `ns[i]`, and every SUCCESS word (words 2, 4, 6, ...) matches the golden's. Any
    difference must be confined to the ORDER-ID words, because the golden was generated against a
    fresh registry while the live rig's `orderCount` has already advanced — that is not a
    return-encoding disagreement. A difference ANYWHERE ELSE must FAIL, with a message printing both
    words and their index. Per the requirement: a live/golden disagreement is a FINDING to report,
    not something to paper over. Do not adjust the golden and do not relax the check.

CHECK 3 — `rpin05_capture_decodes_through_the_shipped_decoder`. Import
`VolOrder.Decode (decode_create_orders_result)` and feed it each captured `returndata`:
  - `N0_empty` decodes to `Right []`
  - `N1_success` decodes to `Right [(True, id)]` with `id > 0`
  - `N2_success_then_fail` decodes to `Right [(True, id), (False, 0)]`
  - `N1_dirty_vega` decodes to `Right [(False, 0)]`
  For each, also assert the decoded element count equals that case's `n`.
  Add an in-file comment on the last one: a `targetVega` above the u96 bound was SKIPPED on a REAL
  chain and its result is indistinguishable from an ordinary business rejection. That is the live
  evidence for why `pack_vol_order_input`'s `in_range 96` guard has to exist client-side — the chain
  gives the client no signal here.

CHECK 4 — `rpin05_no_canonical_bool_violation`. Read every success word out of every captured case
directly from the bytes and assert it is exactly 0 or 1. The decoder already enforces this; reading
the words separately means the check does not ride on the thing it is checking.

Register all four in `main`'s check list.
  </action>
  <verify>
    <automated>cabal build -j all &amp;&amp; cabal test 2>&amp;1 | tee /tmp/21-05-t1.log; grep -c 'PASS rpin05_' /tmp/21-05-t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `cabal test` exits 0 and `grep -c 'PASS rpin05_' ` over its output returns 4
    - `grep -q 'batch-return-capture.json' offchain/test/Main.hs` and `grep -q 'vol_order_return_golden.json' offchain/test/Main.hs` both succeed
    - `grep -q 'decode_create_orders_result' offchain/test/Main.hs` succeeds — the shipped decoder is reused, not re-implemented
    - `grep -cE 'cast call|HttpProvider|8545' offchain/test/Main.hs` returns 0 — the suite opens no socket
    - CHAIN-INDEPENDENCE PROVEN: after `bash offchain/rig/deploy-rig.sh --stop` with `pgrep anvil` empty, `cabal test` still exits 0. Recorded verbatim, then the rig is brought back up.
    - OBSERVED RED recorded verbatim: one hex character of `N0_empty`'s `returndata` flipped in `offchain/rig/batch-return-capture.json` makes `cabal test` exit nonzero naming `rpin05_live_bytes_match_the_external_golden`; `git checkout` restores green
    - OBSERVED RED recorded verbatim: `.manager` in the capture altered by one character makes `cabal test` exit nonzero naming `rpin05_capture_is_present_and_fresh`; restored after
    - `grep -rnE '0x[0-9a-fA-F]{40}\b|0x[0-9a-fA-F]{64}\b|0x[0-9a-fA-F]{8}\b' offchain --include='*.hs' --include='*.sh'` produces NO output
    - `git status --porcelain test/ src/ foundry-scripts/ Makefile foundry.toml remappings.txt` produces NO output
  </acceptance_criteria>
  <done>Live-captured bytes are asserted byte-for-byte against an external-encoder golden including the 64-byte N=0 case, decoded through the shipped decoder, guarded against staleness, and the suite still runs green with no chain.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the Haskell-side bytes artifact and file the three cross-track findings — report, never edit</name>
  <read_first>
    - test/pos_spec/fixtures/vol_order_return_golden.json (READ ONLY: peer_haskell_bytes, _peer_status, _scope_limit, names, ns, expected)
    - docs/superpowers/verification/2026-07-22-stochastic-order-gen-verification.md (the tracked follow-ups, specifically #5 whole-word storage verification and #7 "the deployed pos_spec module emits no logs")
    - src/modules/pos_spec/VolOrderManagerMod.plk lines 175-190 (F1 — quote it verbatim, do not paraphrase)
    - src/types/pos_spec/VolOrder.plk (the module's TICK_SPACING) and offchain/rig/rig-manifest.json (`.pool.tickSpacing`)
    - src/lib/events/VolEventsLib.plk lines 47-54 (the emitter that makes follow-up #7 obsolete)
  </read_first>
  <files>offchain/rig/peer-haskell-bytes.json, .planning/phases/21-v2-abi-re-pin-targetvega-generation/21-CROSS-TRACK-FINDINGS.md, docs/superpowers/verification/2026-07-22-stochastic-order-gen-verification.md</files>
  <action>
PART A — `offchain/rig/peer-haskell-bytes.json`.

The golden fixture's `peer_haskell_bytes` holds five `PLACEHOLDER -- NOT-PEER-VERIFIED` entries and
`_peer_status` records that this rpc_api track never delivered Haskell-produced bytes. Phase 21 is
the first moment that gap CAN close — but `test/` is the Solidity-testing track's territory.
Produce the values on our side and OFFER them; do NOT edit the fixture.

Write, for each of the golden's five case names:
  - `name`, `n`
  - `haskell_decode` — `decode_create_orders_result` run over that case's golden `expected` bytes,
    rendered as a list of `{ "success": bool, "orderId": integer }`
  - `haskell_agrees_with_golden` — true when the decode succeeded AND its element count equals `ns[i]`
  - `live_capture_agrees` — for the three names the live rig also covered, whether the captured
    bytes agreed; `null` for the two it did not (`N3_mixed_seeded_C5`, `N3_all_invalid`)

Top level: `_generated_by` naming this plan and the exact command used; `_scope` stating plainly
that this closes the CROSS-LANGUAGE gap for the `(bool,uint256)[]` RETURN ONLY — it does not
exercise a Haskell ENCODER, so the canonical-array-offset requirement on the INPUT side remains
unexercised from this side; and `_offer_to` naming the Solidity-testing track and the exact fixture
field (`peer_haskell_bytes`) these values can fill.

Generate the file mechanically — a short `cabal exec`/`runghc` snippet, or `jq` assembling a printed
decode. Never hand-type bytes. Record the exact command in the summary.

PART B — `.planning/phases/21-v2-abi-re-pin-targetvega-generation/21-CROSS-TRACK-FINDINGS.md`.

One document, three findings. Each carries: the exact file and line range, the verbatim text or
value at issue, why it matters, who owns it, and what this track did (report) and did not do (edit).

F1 — STALE V1 COMMENT INSIDE THE V2 MODULE, `src/modules/pos_spec/VolOrderManagerMod.plk:177-188`.
Quote both offending sentences verbatim: *"INPUT WORD: skew@0..15 | strike@16..103 | width@104..127
| bits >=128 MUST BE ZERO"* and *"width IS DELIBERATELY UNMASKED. It is the TOP field"*. Both are V1
and both are false of that same file's own V2 code at lines 221-235, which says the opposite and
implements the V2 form. The VALUES are unaffected — the code, the handoff, `notes/DATA_CONTRACT.md`
and `notes/UNITS_AND_SCALES.md` all agree — so this is a documentation defect, not a behaviour
defect. Why it matters: an implementer who reads that block instead of the code ships a V1 packer
that passes every off-chain test and is SILENTLY SKIPPED on-chain, because the batch path skips
rather than reverts. Same rot class this milestone exists to stop, one layer up: a comment cannot be
tested. Owner: the plank track. Action taken: reported here, and warned about inline in
`Encoding.hs`, `capture-batch-return.sh` and every Phase 21 plan; the file was NOT edited.

F2 — `TICK_SPACING = 20` (module constant) vs the rig pool's `tickSpacing = 10`. Record both values
with their sources. Nothing in Phase 21 breaks: the client reads the stored `tickSpacing` and
DISCARDS it. But plan 21-01's storage round-trip asserts `== 20` against a pool whose spacing is 10,
written deliberately against the MODULE CONSTANT so a change reddens loudly. Which value is intended
once placement lands is an OPEN question for the plank track. Owner: plank. Action: reported, out of
scope, expectation pinned to the module constant with an in-file comment naming the discrepancy.

F3 — the v4.0 verification record's tracked follow-up #7 is OBSOLETE. It says *"the deployed
pos_spec module emits no logs"* and proposes deleting the decoder as legacy. V2 added
`emit_vol_order_created` on BOTH the strict and the batch path
(`src/lib/events/VolEventsLib.plk:47-54`), so the decoder is live again and was re-pinned in plan
21-03. Owner: this workstream's own doc — correct it in Part C. Also record that follow-up #5
(whole-word storage verification including `tickSpacing`) was ADDRESSED by plan 21-01's storage
round-trip check, since the re-pin landed on exactly those lines and CONTEXT permits touching a
deferred item in that case.

Close with a "how to consume this" line: the plank track's items are F1 and F2; the
Solidity-testing track's item is the `peer_haskell_bytes` offer in Part A.

PART C — `docs/superpowers/verification/2026-07-22-stochastic-order-gen-verification.md`. This doc
is THIS workstream's. Two minimal, surgical edits; do not restructure:
  1. Mark follow-up #7 OBSOLETE with a one-line reason plus pointers to
     `src/lib/events/VolEventsLib.plk:47-54` and plan 21-03. Do NOT delete the original text —
     supersede it in place, the way this repo's STATE.md supersedes stale entries.
  2. Mark follow-up #5 ADDRESSED, naming plan 21-01's storage round-trip check and its
     `tickSpacing == 20` assertion.
Leave every other follow-up untouched.

PART D — THE PHASE GATE. Run the documented sequence end to end, recording every exit code:

    bash offchain/rig/deploy-rig.sh
    bash offchain/rig/verify-rig.sh
    bash offchain/rig/capture-batch-return.sh
    bash offchain/rig/generate-pins.sh
    git diff --exit-code offchain/rig/rig-pins.json
    cabal build -j all
    cabal test

Then run `cabal run` once against the standing rig and record the output. 20-05's summary recorded
that the demo order REVERTS because `Encoding.hs` still built the retired 3-arg `create_order` — the
exact defect this phase exists to fix. Report whether the demo order now succeeds with a status-1
receipt and an ORDER_CREATED log carrying `target_vega`. If it still fails, that is a finding,
reported with the tx hash, never a silent omission.
  </action>
  <verify>
    <automated>cabal build -j all &amp;&amp; cabal test &amp;&amp; jq -e '.cases | length == 5' offchain/rig/peer-haskell-bytes.json &amp;&amp; grep -q '177-188' .planning/phases/21-v2-abi-re-pin-targetvega-generation/21-CROSS-TRACK-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - `jq -r '.cases | length' offchain/rig/peer-haskell-bytes.json` returns `5`, and every entry has a non-null `haskell_decode`
    - `jq -r '._offer_to' offchain/rig/peer-haskell-bytes.json` names the Solidity-testing track and the string `peer_haskell_bytes`
    - `jq -r '._scope' offchain/rig/peer-haskell-bytes.json` states that only the RETURN side is covered and that a Haskell ENCODER remains unexercised
    - `.planning/phases/21-v2-abi-re-pin-targetvega-generation/21-CROSS-TRACK-FINDINGS.md` exists, is at least 40 lines, and contains the strings `177-188`, `TICK_SPACING`, `tickSpacing = 10`, `VolEventsLib.plk:47-54` and `peer_haskell_bytes`
    - `grep -q 'OBSOLETE' docs/superpowers/verification/2026-07-22-stochastic-order-gen-verification.md` succeeds, and the original follow-up #7 text is still present (superseded in place, not deleted)
    - `git status --porcelain src/ test/ foundry-scripts/ Makefile foundry.toml remappings.txt ../plank/` produces NO output
    - The full phase-gate sequence in PART D is recorded in the summary with an exit code per command; `git diff --exit-code offchain/rig/rig-pins.json` is clean
    - The `cabal run` outcome is recorded verbatim, explicitly stating whether the demo order's receipt status is 1 and whether an ORDER_CREATED log with a `target_vega` line appeared
  </acceptance_criteria>
  <done>Haskell-produced decode results for all five golden cases are committed and offered to the owning track, the three cross-track findings are filed against their owners with verbatim quotations, this workstream's own stale follow-up is superseded in place, and the whole phase gate has been run with every exit code recorded.</done>
</task>

</tasks>

<verification>
1. `cabal build -j all` — exit 0, zero warnings.
2. `cabal test` — exit 0, `SC-3 and SC-4 OK`, four `PASS rpin05_` lines.
3. `bash offchain/rig/deploy-rig.sh --stop && cabal test` — still exit 0 with `pgrep anvil` empty.
   This is the chain-independence proof; bring the rig back up afterwards.
4. `bash offchain/rig/generate-pins.sh && git diff --exit-code offchain/rig/rig-pins.json` — clean.
5. `grep -rnE '0x[0-9a-fA-F]{40}\b|0x[0-9a-fA-F]{64}\b|0x[0-9a-fA-F]{8}\b' offchain --include='*.hs' --include='*.sh'` — no output.
6. `git status --porcelain src/ test/ foundry-scripts/ Makefile foundry.toml remappings.txt` — no output.
7. Both observed-RED demos (flipped capture byte, altered manager) recorded verbatim before any
   green is reported.
</verification>

<success_criteria>
- `decode_create_orders_result` is verified byte-unchanged against a `(bool,uint256)[]` return
  CAPTURED FROM THE LIVE Phase-20 module, including the `N = 0` case at exactly 64 bytes, with any
  disagreement reported rather than papered over.
- The assertion runs inside `cabal test` and the suite remains chain-independent — proven with
  anvil down.
- A stale capture reddens, proven by an observed RED on an altered manager field.
- Haskell-produced decode results for all five golden cases exist in an `offchain/`-side artifact
  and are offered to the Solidity-testing track; the fixture itself is untouched.
- F1, the `TICK_SPACING` discrepancy and the obsolete follow-up are filed against their owners; no
  other track's file was edited.
</success_criteria>

<output>
After completion, create `.planning/phases/21-v2-abi-re-pin-targetvega-generation/21-05-SUMMARY.md`.
Record verbatim: both observed-RED runs; the anvil-down `cabal test` run with `pgrep anvil` output
alongside it; the final `cabal test` check count; each phase-gate command with its exit code; and
the `cabal run` outcome including the receipt status and whether an ORDER_CREATED log with
`target_vega` appeared. Note any live/golden disagreement as a FINDING with the exact words and
indices, never reconciled.
</output>
