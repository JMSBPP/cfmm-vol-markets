---
phase: 19-differential-mutation-battery-consumer-fixture
plan: 04
type: execute
wave: 3
depends_on: ["19-03"]
files_modified:
  - .planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md
autonomous: true
requirements: [MVER-02]

must_haves:
  truths:
    - "Each of the three calldata guards, deleted INDEPENDENTLY, produces an observed RED on the current tree with its verbatim FAIL line recorded"
    - "Guard 3's kill is recorded from the REVERT assertion, never from a state assertion — a state assertion stays green under it and would record a fake kill"
    - "The return-head 0x40 -> 0x20 mutant and the non-canonical success-word mutant each produce an observed RED against a BYTE-EQUALITY assertion"
    - "The head mutant's N=0 blindness is re-confirmed by observation, not cited"
    - "Every mutated source is restored sha256 byte-identical and the pos_spec suites are green afterwards"
  artifacts:
    - path: ".planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md"
      provides: "Part B of the battery: guards 1/2/3 + the two encoder mutants, plus the consolidated MVER-02 tally"
      contains: "PART B TALLY"
  key_links:
    - from: "19-MUTATION-BATTERY.md"
      to: "test/pos_spec/VolOrderManagerBatch.t.sol"
      via: "each mutant's recorded kill site names the test that reddened"
      pattern: "test__unit__truncatedCalldataReverts"
---

<objective>
MVER-02, part B: the three calldata guards deleted INDEPENDENTLY, plus the two return-encoder
mutants — return head `0x40` -> `0x20` and the non-canonical success word.

Purpose: these five traverse the surface 18b CHANGED. 18b's own observations predate 19-01's
sequence differential and 19-02's golden fixture, both of which are now candidate kill sites.
Re-observation is the whole requirement.

Output: part B appended to `19-MUTATION-BATTERY.md`, plus the consolidated MVER-02 tally covering
parts A and B together.

THE SOURCES ARE MUTATED AND RESTORED, NEVER IMPROVED. A surviving mutant is REPORTED, never
dropped, and never a reason to weaken a test.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-differential-mutation-battery-consumer-fixture/19-CONTEXT.md
@.planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md
@.planning/phases/18b-typed-return-encoding/18b-01-SUMMARY.md
@src/modules/pos_spec/VolOrderManagerMod.plk
@test/pos_spec/VolOrderManagerBatch.t.sol

<baselines>
  be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787  src/modules/pos_spec/VolOrderManagerMod.plk

Plan 19-03 restored this file; re-measure at the start of Task 1 and confirm it matches before
applying anything. If it does not, STOP — 19-03's restoration failed and that is a finding.
</baselines>

<procedure>
IDENTICAL to plan 19-03's `<procedure>` block. Restating the two clauses most often skipped:

  - `rm -rf cache/fuzz` before EVERY observation. A `runs: 0` fuzz line is a CACHE REPLAY, not a
    kill; if one appears, the observation is void — clear again and re-run.
  - Record the VERBATIM `[FAIL: ...]` text and the test name, copied character for character.
    "It reddened" is not a record.

FORGE FLAGS: `--via-ir --optimize`. There is NO `--skip` flag any more (removed at `8b11d73`
together with the untracked `PriceSetterHook.sol` sketch). Every prior phase's documented command
is stale on this point. Do NOT reintroduce it.

THE COMMAND: `forge test --match-path 'test/pos_spec/*' --via-ir --optimize`
</procedure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: M5/M6/M7 — the three calldata guards, deleted independently</name>

  <read_first>
    - src/modules/pos_spec/VolOrderManagerMod.plk lines 150-166 (the four guards and the load-bearing ORDER comment)
    - test/pos_spec/VolOrderManagerBatch.t.sol lines 258-338 (VolOrderManagerBatchGuardTest — one isolated corpus per guard)
    - test/pos_spec/VolOrderManagerBatch.t.sol lines 298-322 (test__unit__truncatedCalldataReverts and its READ-THIS-BEFORE-CHANGING-IT header)
    - .planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md (equivalence ledger entry 4 — guard 3's state-invisibility)
  </read_first>

  <action>
Three mutants, applied and observed ONE AT A TIME. Independence is the requirement: never delete
two guards in one run, or a single red cannot be attributed.

GUARD NAMING is fixed project-wide and must be used in the record: **guard 1 = offset,
guard 2 = length, guard 3 = calldatasize.** The `MAX_BATCH` bound is NOT one of MCAL-02's three
calldata guards and is named separately.

---

**M5 — GUARD 1 DELETED (canonical array offset).**
`src/modules/pos_spec/VolOrderManagerMod.plk:158`, delete the line:
```
        require(@evm_calldataload(36) == 0x40);
```
Expected kill site: `test__unit__nonCanonicalOffsetReverts`. The corpus is
`encodeBatchRaw(1, 0x2000, 1, words)` — a hostile offset pointing far past the array. This is the
PHANTOM-ORDER hole, not merely "a wrong value": with the guard gone the loop keeps reading the
fixed byte-100 region and stores tuples the caller's encoder never placed there. The test's
FIRST assertion is `assertFalse(ok, "guard 1: ...")`, so expect a revert-shaped red there.

---

**M6 — GUARD 2 DELETED (array length agrees with count).**
`src/modules/pos_spec/VolOrderManagerMod.plk:159`, delete the line:
```
        require(@evm_calldataload(68) == count);
```
Expected kill site: `test__unit__lengthCountMismatchReverts`. Its corpus is deliberately
constructed so guard 3 CANNOT be what fires — `count = 2`, `length = 1`, and a full 164-byte
payload, with `assertEq(cd.length, 164, ...)` asserted as a precondition. That isolation is what
makes the red attributable to guard 2 alone.

---

**M7 — GUARD 3 DELETED (calldatasize covers the elements).**
`src/modules/pos_spec/VolOrderManagerMod.plk:160`, delete the line:
```
        require(@evm_calldatasize() >= 100 + 32 * count);
```

**READ THIS BEFORE RECORDING THE RESULT — THIS MUTANT IS INVISIBLE TO EVERY STATE ASSERTION.**
Deleting the calldatasize guard does NOT corrupt state on a truncated payload: a `calldataload`
past the end returns ZERO-PADDED words, `build_vol_order(0, 0, 0)` fails validation, the tuple is
SKIPPED, and state stays clean. A state assertion therefore stays GREEN under this mutant and
would record a FAKE KILL.

The ONLY discriminating assertion is `assertFalse(ok, "guard 3: calldatasize must cover
100 + 32*count")` in `test__unit__truncatedCalldataReverts`. Record the red from THAT line. The
`orderCount` assertion that follows it in the same test is completeness, NOT the kill site — if
your recorded FAIL line is the orderCount one, the observation is wrong and must be redone.

Additionally OBSERVE and record the green half: run
`forge test --match-contract VolOrderManagerBatchStateTest --via-ir --optimize` under M7 and
confirm it stays GREEN. That measured green is what substantiates the state-invisibility claim
instead of merely repeating it from the ledger.
  </action>

  <verify>
    <automated>sha256sum src/modules/pos_spec/VolOrderManagerMod.plk</automated>
    <automated>rm -rf cache/fuzz && forge test --match-path 'test/pos_spec/*' --via-ir --optimize 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `19-MUTATION-BATTERY.md` contains sections headed `M5`, `M6`, `M7`
    - The `M5` section's `VERBATIM FAIL LINE:` names `test__unit__nonCanonicalOffsetReverts`
    - The `M6` section's `VERBATIM FAIL LINE:` names `test__unit__lengthCountMismatchReverts`
    - The `M7` section's `VERBATIM FAIL LINE:` contains the string `guard 3: calldatasize` — i.e.
      the REVERT assertion, not the orderCount assertion
    - The `M7` section additionally records `VolOrderManagerBatchStateTest` observed GREEN under
      the mutant, with the pass tally
    - Each section records PRE and POST-RESTORATION sha256, and they are EQUAL
    - No recorded forge output contains `runs: 0`
    - `sha256sum src/modules/pos_spec/VolOrderManagerMod.plk` outputs
      `be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787`
    - `git diff --stat src/` produces NO output
  </acceptance_criteria>

  <done>
    All three guards deleted independently, each with a verbatim RED attributable to its own
    isolated corpus, guard 3's kill taken from the revert assertion and its state-invisibility
    re-measured, and the module restored byte-identical.
  </done>
</task>

<task type="auto">
  <name>Task 2: M8 return head 0x40 -> 0x20, M9 non-canonical success word</name>

  <read_first>
    - src/modules/pos_spec/VolOrderManagerMod.plk lines 193-247 (the pinned return layout comment, the buffer allocation, `let base = 64 + 64 * i;` and the two `@mstore32` pairs)
    - test/pos_spec/VolOrderManagerBatch.t.sol lines 536-580 (why BYTES and not decoded values; assertion order as mutation-evidence design)
    - test/pos_spec/VolOrderManagerBatch.t.sol lines 582-613 (test__unit__emptyReturnIsExactlySixtyFourBytes — the N=0 edge where M8 is BLIND)
    - test/pos_spec/VolOrderManagerBatch.t.sol lines 726-745 (test__unit__successWordsAreCanonicallyZeroOrOne — reads RAW WORDS, deliberately not through bool)
    - .planning/STATE.md (the 18b-01 MEASURED entries on M8's N=0 blindness and M9's abi.decode rejection)
  </read_first>

  <action>
**M8 — RETURN HEAD `0x40` -> `0x20`.** The element base shift: emitting the length word but
placing element 0 where the length word sits. `src/modules/pos_spec/VolOrderManagerMod.plk:231`:
```
            let base = 64 + 64 * i;
```
becomes:
```
            let base = 32 + 64 * i;
```

**M8 IS BLIND AT N=0 AND THAT MUST BE RE-CONFIRMED BY OBSERVATION, NOT CITED.** 18b MEASURED that
`test__unit__emptyReturnIsExactlySixtyFourBytes` stays GREEN under this mutant — with no elements
there is nothing to misplace and the total is 64 bytes either way. Record TWO observations:
  (a) `forge test --match-test test__unit__emptyReturnIsExactlySixtyFourBytes --via-ir --optimize`
      -> expect GREEN. Record it. This is the measured proof that an N=0-only corpus would miss a
      real encoder bug.
  (b) `forge test --match-path 'test/pos_spec/*' --via-ir --optimize` -> expect REDs at the
      N >= 1 byte-equality assertions: `test__unit__oneAndTwoElementReturnsAreByteExact`,
      `test__unit__mixedBatchReturnIsByteExact`, 19-01's `test__unit__fixedAnchorSequenceDiffers`
      and 19-02's fixture cases `N1_success` onward. Record the verbatim FAIL line from a
      KECCAK BYTE-EQUALITY assertion — a length-only or word-level red is a localisation aid, not
      the kill site.

If (a) comes back RED instead of GREEN, that is a DIVERGENCE from 18b's measurement on a tree
whose module hash is unchanged — investigate and report it rather than recording the convenient
result.

---

**M9 — NON-CANONICAL SUCCESS WORD.** `src/modules/pos_spec/VolOrderManagerMod.plk:240`:
```
                @mstore32(out +% base, 1);
```
becomes:
```
                @mstore32(out +% base, 2);
```

Expected shape, and it is unusual enough to state up front so the record is not misread:
18b-01 CORROBORATED that solc's `abi.decode` **REJECTS a non-canonical bool outright**, so the
whole 18a suite reddens with `EvmError: Revert` rather than with wrong values — because
`callBatch` decodes. The DISCRIMINATING kill site is
`test__unit__successWordsAreCanonicallyZeroOrOne`, which reads RAW WORDS via `wordAt` and
asserts `w < 2`; it does NOT go through `bool`, precisely so the pinned property is
"the word is canonical" and not "solc's decoder rejected it" — those are different failures.
Record the verbatim red from THAT test as the kill, and record the `EvmError: Revert` cascade
separately as corroboration.

This mutant is also the milestone's HARD REQUIREMENT ON THE HASKELL CONSUMER: a lenient Haskell
decoder would accept a truthy 2 while `abi.decode` rejects it, so the two consumers would
silently disagree about the same bytes. Note that in the record — it is a consumer-side contract,
not a test detail, and it belongs in the peer hand-off.

---

**Then write PART B TALLY and the CONSOLIDATED MVER-02 TALLY** into `19-MUTATION-BATTERY.md`:

  - Part B: attempted / OBSERVED RED / SURVIVOR / UNCONSTRUCTIBLE.
  - Consolidated (parts A + B): the full MVER-02 list — deleted validation branch, missing strike
    upper bound, count-advance-on-failure, ring-mask reintroduction, each of the three calldata
    guards, return-head `0x40` -> `0x20`, non-canonical success word — each with its status and
    the test that killed it.
  - A SURVIVORS section, present even if empty, stating the count explicitly.
  - A NEW-KILL-SITES section naming which of 19-01's and 19-02's tests appeared in any red. That
    is the direct evidence those plans strengthened the suite rather than merely adding green,
    and it is worth more than the kill count itself.
  </action>

  <verify>
    <automated>sha256sum src/modules/pos_spec/VolOrderManagerMod.plk</automated>
    <automated>rm -rf cache/fuzz && forge test --match-path 'test/pos_spec/*' --via-ir --optimize 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `19-MUTATION-BATTERY.md` contains sections headed `M8` and `M9`
    - The `M8` section records BOTH the GREEN observation of
      `test__unit__emptyReturnIsExactlySixtyFourBytes` under the mutant AND a verbatim FAIL line
      from a keccak byte-equality assertion at N >= 1
    - The `M9` section's primary `VERBATIM FAIL LINE:` names
      `test__unit__successWordsAreCanonicallyZeroOrOne`, with the `EvmError: Revert` cascade
      recorded separately as corroboration
    - `grep -c 'PART B TALLY' 19-MUTATION-BATTERY.md` outputs `1`
    - `grep -c 'CONSOLIDATED MVER-02 TALLY' 19-MUTATION-BATTERY.md` outputs `1`
    - `grep -c 'SURVIVORS' 19-MUTATION-BATTERY.md` outputs at least `1`
    - The consolidated tally section names all nine MVER-02 mutants
    - Each section records PRE and POST-RESTORATION sha256, and they are EQUAL
    - `sha256sum src/modules/pos_spec/VolOrderManagerMod.plk` outputs
      `be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787`
    - `git diff --stat src/` produces NO output
    - `rm -rf cache/fuzz && forge test --match-path 'test/pos_spec/*' --via-ir --optimize` exits 0
  </acceptance_criteria>

  <done>
    M8 and M9 each carry a verbatim observation with the correct kill site, M8's N=0 blindness is
    re-measured rather than cited, the consolidated MVER-02 tally covers all nine mutants with an
    explicit survivor count, and the module is byte-identical to its baseline.
  </done>
</task>

</tasks>

<verification>
1. `sha256sum src/modules/pos_spec/VolOrderManagerMod.plk` equals
   `be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787`.
2. `git diff --stat src/` is empty.
3. `rm -rf cache/fuzz && forge test --match-path 'test/pos_spec/*' --via-ir --optimize` exits 0.
4. `19-MUTATION-BATTERY.md` covers M1a, M1b, M2, M3, M4, M5, M6, M7, M8, M9 with verbatim
   observations, the equivalence-masked ledger, both part tallies and the consolidated tally.
5. No recorded forge output contains `runs: 0`.
</verification>

<success_criteria>
- All nine MVER-02 mutants re-applied and re-observed against the CURRENT tree; nothing cited.
- Guard 3's kill comes from the revert assertion and its state-invisibility is re-measured.
- M8's N=0 blindness is re-measured, so the corpus-design lesson is evidence rather than folklore.
- M9's kill comes from the raw-word canonicality assertion, with the decoder cascade recorded
  separately as the consumer-contract finding.
- Survivors are reported with an explicit count, never engineered away.
- Every source restored sha256 byte-identical; `src/types/pos_spec/` untouched.
</success_criteria>

<output>
After completion, create
`.planning/phases/19-differential-mutation-battery-consumer-fixture/19-04-SUMMARY.md`.

Record: the consolidated MVER-02 tally, the explicit survivor count, which of 19-01's and 19-02's
new tests appeared as kill sites, the restoration hash, and any divergence from 18b's recorded
measurements (in particular whether M8 was still blind at N=0 on this tree).
</output>
