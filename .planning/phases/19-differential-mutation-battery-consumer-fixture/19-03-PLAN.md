---
phase: 19-differential-mutation-battery-consumer-fixture
plan: 03
type: execute
wave: 2
depends_on: ["19-01", "19-02"]
files_modified:
  - .planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md
autonomous: true
requirements: [MVER-02]

must_haves:
  truths:
    - "Each of the four semantic mutants (validation branch deleted, strike upper bound removed, count-advance-on-failure, ring-mask reintroduction) is applied to the CURRENT tree and OBSERVED red, with the verbatim FAIL line recorded"
    - "Each mutated source is restored and proven sha256 byte-identical to its pre-mutation baseline"
    - "After every restoration the pos_spec suites are green again"
    - "No fuzz kill is accepted from a warm cache — cache/fuzz is cleared before each observation and the runs: figure is recorded"
    - "Equivalence-masked mutants are documented and explicitly NOT counted as kills"
  artifacts:
    - path: ".planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md"
      provides: "Part A of the observed-RED battery record: 4 mutants, verbatim FAIL lines, sha256 restorations"
      contains: "VERBATIM FAIL LINE"
  key_links:
    - from: "19-MUTATION-BATTERY.md"
      to: "src/modules/pos_spec/VolOrderManagerMod.plk"
      via: "recorded sha256 baseline and post-restoration hash"
      pattern: "be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787"
---

<objective>
MVER-02, part A: re-run the four SEMANTIC mutants fresh against the CURRENT tree — deleted
validation branch, missing strike upper bound, count-advance-on-failure, ring-mask reintroduction.

Purpose, and it is not ceremony. Every prior kill in this milestone was observed against OLDER
code, and Phase 18b CHANGED THE RETURN TYPE of `create_orders` — the surface several of these
mutants traverse. A kill recorded in Phase 17 is a historical fact, not evidence about today's
tree. Plans 19-01 and 19-02 have since added new suites that are themselves candidate kill sites.
**The battery is an OBSERVATION or it is a bibliography.**

WHAT MAY BE REUSED FROM PRIOR PHASES AND WHAT MAY NOT:
  - The EDIT TEXT of a mutant — reuse freely. Re-deriving Plank syntax wastes effort.
  - The OBSERVATION (that it reddened, and where) — NEVER reuse. Re-run and re-record.

Output: `19-MUTATION-BATTERY.md` containing part A.

THE SOURCES ARE MUTATED AND RESTORED, NEVER IMPROVED. If a mutant does NOT redden, that is a
SURVIVOR: report it, keep it in the record, and do NOT weaken or reshape any test to manufacture
a kill. A surviving mutant is a finding about test coverage, and reporting it is the whole point.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-differential-mutation-battery-consumer-fixture/19-CONTEXT.md
@.planning/phases/18b-typed-return-encoding/18b-01-SUMMARY.md
@.planning/phases/18a-batch-input-state-effects/18a-01-SUMMARY.md
@src/modules/pos_spec/VolOrderManagerMod.plk
@src/lib/pos_spec/VolOrderValidationLib.plk

<baselines>
PRE-MUTATION sha256, measured 2026-07-21 on this tree. Re-measure at the start of Task 1 and use
YOUR measured values if they differ (a different value means another track edited the file — that
is a FINDING to report before proceeding, not something to paper over).

  be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787  src/modules/pos_spec/VolOrderManagerMod.plk
  5fe71f30e4820d230a6d15b30e440ae78a33875d0d9a66e60f4e0d7d73fe8f35  src/lib/pos_spec/VolOrderValidationLib.plk

The module hash matches the one 18b-VERIFICATION.md recorded, so the module is unchanged since
18b — but the TEST TREE is not, which is exactly why the observations must be redone.
</baselines>

<procedure>
THE OBSERVATION LOOP, applied identically to every mutant. Deviating from it invalidates the
evidence.

  1. `sha256sum <file>`  -> record as the PRE hash; confirm it equals the baseline.
  2. `cp <file> /tmp/mut_backup_<name>.plk`
  3. Apply the mutant edit EXACTLY as specified.
  4. `rm -rf cache/fuzz`   <- MANDATORY. `deployPlank` recompiles the `.plk` over FFI at test
     time, so the mutant reaches the deployed bytecode without `make compile-plank`. But a warm
     fuzz cache replays a prior corpus: a `runs: 0` line in the output is a CACHE REPLAY, not a
     kill. If any fuzz test reports `runs: 0`, the observation is void — clear again and re-run.
  5. Run the named command. Capture stdout.
  6. Record the VERBATIM FAIL LINE — the exact `[FAIL: ...]` text and the test name, copied
     character for character, not paraphrased. Also record the failing/passing tallies.
  7. `cp /tmp/mut_backup_<name>.plk <file>`
  8. `sha256sum <file>` -> MUST equal the PRE hash exactly. If it does not, restore from git
     (`git checkout -- <file>`) and re-verify before continuing.
  9. Re-run the same command and confirm GREEN.

THE PROJECT-WIDE FORGE FLAGS ARE `--via-ir --optimize`. There is NO `--skip` flag any more: as of
commit `8b11d73` the untracked `PriceSetterHook.sol` sketch was deleted and
`--skip 'src/modules/protocol_integrations/PriceSetterHook.sol'` was removed from all nine
Makefile recipes. Every prior phase's documented command is STALE on this point. Do NOT
reintroduce the flag.

THE POS_SPEC COMMAND used throughout (covers all five suites — the single-call surface, the batch
+ return surface, 19-01's differential and 19-02's fixture):
  `forge test --match-path 'test/pos_spec/*' --via-ir --optimize`
Where a mutant's sole kill site is the Phase-16 pure-lib suite, additionally run:
  `forge test --match-path 'test/types/pos_spec/VolOrderValidation.t.sol' --via-ir --optimize`
</procedure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: M1 deleted validation branch, M2 missing strike upper bound</name>

  <read_first>
    - src/modules/pos_spec/VolOrderManagerMod.plk lines 66-92 (the strict path) and lines 209-248 (the batch loop)
    - src/lib/pos_spec/VolOrderValidationLib.plk lines 34-72 (MAX_STRIKE, strike_fits_packed, validate_order)
    - .planning/phases/18a-batch-input-state-effects/18a-01-SUMMARY.md (the M-VAL mutant's EDIT TEXT — reuse the edit, never the observation)
    - test/pos_spec/VolOrderManagerBatch.t.sol lines 193-256 (the state kill sites)
    - test/pos_spec/VolOrderManager.diff.t.sol (19-01's new kill sites)
  </read_first>

  <action>
Create `.planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md`
with a header stating: every entry below was OBSERVED on the CURRENT tree on {date}; no kill is
cited from a prior phase; prior kills are historical facts, not evidence about this tree. Then
append part A as you go — write each mutant's record IMMEDIATELY after observing it, so a crash
loses at most one observation.

---

**M1 — DELETED VALIDATION BRANCH.** Two independent applications; observe each SEPARATELY.

*M1a — the BATCH path.* `src/modules/pos_spec/VolOrderManagerMod.plk:233`:
```
            if validate_order(order) {
```
becomes a tautology so every tuple, valid or not, is stored and reported as `(true, id)`:
```
            if 1 == 1 {
```
If Plank rejects that form, try in order and RECORD which one compiled:
  (i) `if true {`
  (ii) `if validate_order(order) | (1 == 1) {`
  (iii) `let _v = validate_order(order); if 1 == 1 {`
If none compiles, record the verbatim compiler errors and treat M1a as UNCONSTRUCTIBLE — an
unconstructible mutant is documented and explicitly NOT counted as a kill, exactly as 18b handled
its M7 scoping case.

EXPECTED SHAPE OF THE RED, and this is a PREDICTION to check, not a result to assume:
18a-01 MEASURED that deleting validation **cannot** produce a batch revert — `pack_vol_order` is
pure `@evm_shl`/`&`/`|` and `@evm_sstore` cannot revert on this path, so an unvalidated tuple is
STORED WRONG and COUNTED. So expect VALUE/STATE reds, not `EvmError: Revert`:
`test__unit__dirtyHighBitsAreSkippedNotStored`, `test__unit__mixedBatchFootprintAndContiguity`,
`test__unit__allInvalidBatchReturnsAllFalseZero`, plus 19-01's
`test__unit__fixedAnchorSequenceDiffers` (the mock still rejects, so the mock and module diverge).
If a REVERT is observed instead, that is an MCAL-04 FINDING — a step in the structural enumeration
is not total — and must be recorded and investigated, not smoothed over.

*M1b — the STRICT path.* `src/modules/pos_spec/VolOrderManagerMod.plk:77`, delete the line:
```
        validate_order_strict(order);
```
Expected kill sites: `test__unit__invalidSkewRevertsAndLeavesStateUntouched`,
`test__unit__invalidSkewAfterAValidOrderDoesNotDisturbIt` (both use `vm.expectRevert`), plus
19-01's `_singleExpectRevertBoth` step.

---

**M2 — MISSING STRIKE UPPER BOUND (silent truncation).** `src/lib/pos_spec/VolOrderValidationLib.plk`,
the body of `strike_fits_packed`:
```
      let res = tick_volatility_is_complete(self) & (self.vol <= MAX_STRIKE);
```
becomes:
```
      let res = tick_volatility_is_complete(self);
```
This is the AUTHORED half of the bound removed, leaving only the pre-existing `vol > 0`. The
failure it models is a VALUE CHANGE, never a revert: an oversized strike passes validation and is
then silently masked to 88 bits by `pack_vol_order`, storing a different value than the caller
supplied. Nothing on-chain would surface it.

Kill sites to check: the Phase-16 pure-lib suite (`test/types/pos_spec/VolOrderValidation.t.sol`)
AND 19-01's differential (the mock's `isValid` still applies `strike <= 0xFFFFFFFFFFFFFFFFFFFFFF`,
so an oversized strike makes module and mock disagree). Run BOTH commands and record both.

NOTE: `VolOrderValidationLib.plk` is a DIFFERENT file from the module — back it up and restore it
separately, and verify BOTH hashes at the end.
  </action>

  <verify>
    <automated>sha256sum src/modules/pos_spec/VolOrderManagerMod.plk src/lib/pos_spec/VolOrderValidationLib.plk</automated>
    <automated>forge test --match-path 'test/pos_spec/*' --via-ir --optimize 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `19-MUTATION-BATTERY.md` exists and contains a section headed `M1a`, `M1b`, and `M2`
    - Each of those three sections contains a line beginning `VERBATIM FAIL LINE:` followed by a
      copied `[FAIL:` string, OR the explicit word `SURVIVOR` / `UNCONSTRUCTIBLE` with the
      compiler or test output that establishes it
    - Each section records the PRE sha256 and the POST-RESTORATION sha256, and they are EQUAL
    - `grep -c 'runs: 0' ` over each recorded forge output is `0` (a `runs: 0` fuzz line voids the
      observation — clear `cache/fuzz` and re-observe)
    - `sha256sum src/modules/pos_spec/VolOrderManagerMod.plk` outputs
      `be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787`
    - `sha256sum src/lib/pos_spec/VolOrderValidationLib.plk` outputs
      `5fe71f30e4820d230a6d15b30e440ae78a33875d0d9a66e60f4e0d7d73fe8f35`
    - `git diff --stat src/` produces NO output
    - `forge test --match-path 'test/pos_spec/*' --via-ir --optimize` exits 0
  </acceptance_criteria>

  <done>
    M1a, M1b and M2 each carry a verbatim observation (RED, SURVIVOR, or UNCONSTRUCTIBLE) taken on
    this tree with a cold fuzz cache, and both source files are byte-identical to their baselines.
  </done>
</task>

<task type="auto">
  <name>Task 2: M3 count-advance-on-failure, M4 ring-mask reintroduction</name>

  <read_first>
    - src/modules/pos_spec/VolOrderManagerMod.plk lines 209-255 (the loop, the id advance inside the `if`, and the trailing single write-back)
    - src/modules/pos_spec/VolOrderManagerMod.plk lines 79-91 (the strict path's id and array_slot call)
    - test/pos_spec/VolOrderManager.t.sol lines 120-143 (test__unit__idAt65536IsNotMaskedIntoSlotZero — the SOLE ring-mask kill site)
    - test/pos_spec/VolOrderManagerBatch.t.sol lines 196-239 (the contiguity assertion, deliberately ordered BEFORE the count assertion)
    - .planning/STATE.md (the 17-01 entry recording that M5 was equivalence-masked in the strict path and became a real kill in the batch)
  </read_first>

  <action>
**M3 — COUNT-ADVANCE-ON-FAILURE.** Hoist the id increment out of the success branch so a SKIPPED
tuple consumes an id. In `src/modules/pos_spec/VolOrderManagerMod.plk`, lines 231-246, change:
```
            let base = 64 + 64 * i;

            if validate_order(order) {
                id = id + 1;
```
to:
```
            let base = 64 + 64 * i;
            id = id + 1;

            if validate_order(order) {
```
(i.e. move `id = id + 1;` above the `if`, and delete it from inside the branch — the branch keeps
its `@evm_sstore` and its two `@mstore32` calls unchanged.)

WHY THE DISCRIMINATING RED IS THE CONTIGUITY ASSERTION AND NOT THE COUNT. 18a-01 recorded this as
a FINDING that binds every mutation gate here: **forge reports only the FIRST failing assertion per
test**, and under this mutant BOTH the contiguity and the `orderCount` assertions redden.
`test__unit__mixedBatchFootprintAndContiguity` deliberately places the contiguity assertion FIRST
for exactly this reason. Expect and record:
`id contiguity: third valid order at C+2: 0 != <packed>` — slot C+2 holds ZERO because the skipped
middle tuple consumed the id and pushed valid_B to C+3. If the recorded red is a count-only red,
the observation is WEAKER than it should be — note that, do not accept it silently.

Additional expected kill sites on this tree: 19-01's `test__unit__fixedAnchorSequenceDiffers`
(step 2's mixed batch, then step 3's interleaved single, which now lands at a different id from
the mock) and 19-02's `N3_mixed_seeded_C5` fixture case (its golden bytes pin `(true,6)`,
`(false,0)`, `(true,7)` — the mutant emits `(true,6)`, `(false,0)`, `(true,8)`).

---

**M4 — RING-MASK REINTRODUCTION.** Apply the 16-bit wraparound mask at BOTH `array_slot` call
sites. `src/modules/pos_spec/VolOrderManagerMod.plk:89` (strict path):
```
        @evm_sstore(array_slot(SLOT_ORDERS_BASE, id), pack_vol_order(order));
```
becomes:
```
        @evm_sstore(array_slot(SLOT_ORDERS_BASE, id & 0xFFFF), pack_vol_order(order));
```
and line 235 (batch path) identically.

**THIS MUTANT IS A NO-OP AT SMALL IDS AND THAT IS DOCUMENTED, NOT DISCOVERED HERE.**
`1 & 0xFFFF == 1`, so it only bites at 65536, where it folds the order onto slot 0. Its SOLE kill
site is `test__unit__idAt65536IsNotMaskedIntoSlotZero`, which reaches id 65536 by
`vm.store`-ing the counter to 65535 rather than issuing 65536 transactions. Every other test in
the repo stays GREEN under M4 — that was MEASURED at 17-01 and is the reason the test exists.

Record TWO observations for M4, because the pair is the evidence:
  (a) `forge test --match-test test__unit__idAt65536IsNotMaskedIntoSlotZero --via-ir --optimize`
      -> RED, verbatim line.
  (b) `forge test --match-path 'test/pos_spec/*' --match-contract VolOrderManagerBatchStateTest --via-ir --optimize`
      -> still GREEN under the mutant. Record this explicitly. It is not a failure of the battery;
      it is the measured proof that small-id tests are provably insufficient, and stating it is
      what keeps the record honest.

The 19-CONTEXT.md ledger already classifies M4 as a NO-OP at small ids. It is still a REAL KILL
(the 65536 test genuinely reddens) and IS counted — it is the equivalence-MASKED mutants in
Task 3's ledger that are not counted. Do not confuse the two.
  </action>

  <verify>
    <automated>sha256sum src/modules/pos_spec/VolOrderManagerMod.plk</automated>
    <automated>forge test --match-path 'test/pos_spec/*' --via-ir --optimize 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `19-MUTATION-BATTERY.md` contains sections headed `M3` and `M4`
    - The `M3` section contains a `VERBATIM FAIL LINE:` naming a CONTIGUITY assertion (the string
      `id contiguity` appears in it), or an explicit note that only a count-level red was observed
      and why
    - The `M4` section contains BOTH a verbatim RED from
      `test__unit__idAt65536IsNotMaskedIntoSlotZero` AND the recorded observation that
      `VolOrderManagerBatchStateTest` stayed GREEN under the same mutant
    - Both sections record PRE and POST-RESTORATION sha256, and they are EQUAL
    - `sha256sum src/modules/pos_spec/VolOrderManagerMod.plk` outputs
      `be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787`
    - `git diff --stat src/` produces NO output
    - `forge test --match-path 'test/pos_spec/*' --via-ir --optimize` exits 0
  </acceptance_criteria>

  <done>
    M3 and M4 each carry a verbatim observation on this tree; M4 additionally carries the measured
    green that establishes its sole-kill-site claim; the module is byte-identical to its baseline.
  </done>
</task>

<task type="auto">
  <name>Task 3: Restoration audit and the equivalence-masked ledger</name>

  <read_first>
    - .planning/phases/19-differential-mutation-battery-consumer-fixture/19-CONTEXT.md (the four known equivalence-masked mutants and why each must stay excluded)
    - .planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md (parts written in Tasks 1-2)
    - .planning/STATE.md (the 18b-01 DECIDED entry: M7 is unconstructible at the SCOPING level; the count reads 6, not 7)
  </read_first>

  <action>
**(a) Restoration audit.** Run:
```
git status --short src/
git diff --stat src/
sha256sum src/modules/pos_spec/VolOrderManagerMod.plk src/lib/pos_spec/VolOrderValidationLib.plk
```
Both hashes must equal their baselines. `git status --short src/` must show no modification to
`src/modules/pos_spec/` or `src/lib/pos_spec/` (an unrelated `M src/lib/exposure/VegaIssuanceLib.plk`
from another track's in-progress draft is EXPECTED and is not ours — name it, do not touch it).
Then run the FULL pos_spec sweep green:
```
rm -rf cache/fuzz && forge test --match-path 'test/pos_spec/*' --via-ir --optimize
forge test --match-path 'test/types/pos_spec/VolOrderValidation.t.sol' --via-ir --optimize
```

**(b) Write the EQUIVALENCE-MASKED LEDGER** into `19-MUTATION-BATTERY.md` as its own section.
These are DOCUMENTED and explicitly NOT COUNTED as kills. Each entry states the mutant, why it is
masked, and — critically — that it must not be quietly re-counted in a later phase:

  1. **v3.0 — h-bound `<` -> `<=`.** Masked by `full_math`'s zero-denominator revert. NOT COUNTED.
  2. **v3.0 — unset-`p_risk` guard.** Same mask, same reason. NOT COUNTED.
  3. **Phase 17 — the ring-mask at SMALL IDS.** `1 & 0xFFFF = 1` makes it a NO-OP below 65536.
     The mutant itself IS a real kill (see M4 above), but a small-id observation of it is NOT
     evidence — its only kill site is the `vm.store`-to-65535 test. Recorded so a future phase
     cannot claim a kill from a small-id test.
  4. **Phase 18a — guard-3's deletion is invisible to STATE assertions.** A `calldataload` past the
     end returns zero-padded words, so `build_vol_order(0,0,0)` fails validation, the tuple is
     skipped, and state stays clean. A state assertion stays GREEN under it and would record a
     FAKE kill. Killable ONLY by the REVERT assertion in `test__unit__truncatedCalldataReverts`.
     (Observed in plan 19-04, which owns the guard mutants — this ledger entry exists so the
     constraint is stated once, in one place.)
  5. **Phase 18b — M7, pure allocation reordering.** UNCONSTRUCTIBLE at the SCOPING level: moving
     the buffer allocation inside the loop makes the trailing `@evm_return(out, ...)` fail to
     compile with `error: unresolved identifier 'out'`. Excluded. **18b's count reads 6, not 7.**
  6. **Phase 18b — the element-base-shift mutant is BLIND at N=0** and the **stride mutant is
     blind at N <= 1** (i=0 makes `64 + stride*i` independent of the stride). Both were MEASURED.
     A kill for either must come from an N >= 1 / N >= 2 corpus respectively. Recorded here so
     plan 19-04 cannot accept an N=0 green as evidence.
  7. **Phase 18b — the `(false, id)` leak mutant is NOT killable by an all-invalid batch on a
     fresh registry**, because `id` never leaves 0 there so `(false, id)` IS `(false, 0)`. The
     SEEDED mixed corpus is the SOLE kill site.

**(c) Write a PART A TALLY** stating: mutants attempted, OBSERVED RED, SURVIVORS, UNCONSTRUCTIBLE.
State the survivor count explicitly even when it is zero. A survivor is a FINDING to carry into
the phase summary and the milestone exit record — never a reason to weaken a test, and never
something to drop from the record.
  </action>

  <verify>
    <automated>git diff --stat src/modules/pos_spec/ src/lib/pos_spec/</automated>
    <automated>rm -rf cache/fuzz && forge test --match-path 'test/pos_spec/*' --via-ir --optimize 2>&1 | tail -8</automated>
    <automated>sha256sum src/modules/pos_spec/VolOrderManagerMod.plk src/lib/pos_spec/VolOrderValidationLib.plk</automated>
  </verify>

  <acceptance_criteria>
    - `git diff --stat src/modules/pos_spec/ src/lib/pos_spec/` produces NO output
    - `sha256sum src/modules/pos_spec/VolOrderManagerMod.plk` outputs
      `be196dcb0a524ea548480cdbd55f0f70d458fe3cbead078d9fed27d1cc9b8787`
    - `sha256sum src/lib/pos_spec/VolOrderValidationLib.plk` outputs
      `5fe71f30e4820d230a6d15b30e440ae78a33875d0d9a66e60f4e0d7d73fe8f35`
    - `rm -rf cache/fuzz && forge test --match-path 'test/pos_spec/*' --via-ir --optimize` exits 0
    - `forge test --match-path 'test/types/pos_spec/VolOrderValidation.t.sol' --via-ir --optimize` exits 0
    - `grep -c 'NOT COUNTED' .planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md`
      outputs at least `2`
    - `grep -c 'PART A TALLY' .planning/phases/19-differential-mutation-battery-consumer-fixture/19-MUTATION-BATTERY.md`
      outputs `1`
    - `git status --short src/types/pos_spec/` produces NO output
  </acceptance_criteria>

  <done>
    Both mutated sources are byte-identical to their baselines, the pos_spec suites are green from
    a cold fuzz cache, the seven-entry equivalence-masked ledger is written, and part A's tally
    names its survivors explicitly.
  </done>
</task>

</tasks>

<verification>
1. `sha256sum src/modules/pos_spec/VolOrderManagerMod.plk src/lib/pos_spec/VolOrderValidationLib.plk`
   equals the two baselines exactly.
2. `git diff --stat src/modules/pos_spec/ src/lib/pos_spec/` is empty.
3. `rm -rf cache/fuzz && forge test --match-path 'test/pos_spec/*' --via-ir --optimize` exits 0.
4. `19-MUTATION-BATTERY.md` has one section per mutant (M1a, M1b, M2, M3, M4), each with a
   verbatim observation and matching PRE/POST hashes, plus the equivalence-masked ledger and the
   part A tally.
</verification>

<success_criteria>
- Four semantic mutants re-applied to the CURRENT tree, each observed fresh with a cold fuzz cache,
  each verbatim FAIL line recorded, each source restored sha256 byte-identical.
- No kill is cited from a prior phase.
- M4's sole-kill-site claim is backed by a MEASURED green elsewhere, not asserted.
- Equivalence-masked and unconstructible mutants are documented and explicitly not counted.
- Survivors, if any, are reported rather than engineered away.
- `src/types/pos_spec/` is untouched.
</success_criteria>

<output>
After completion, create
`.planning/phases/19-differential-mutation-battery-consumer-fixture/19-03-SUMMARY.md`.

Record: the part A tally (attempted / RED / SURVIVOR / UNCONSTRUCTIBLE), the two restoration
hashes, whether the M1a red was a value red or a revert (a revert would be an MCAL-04 finding),
and whether any of 19-01's or 19-02's NEW tests appeared as kill sites — that is direct evidence
those plans strengthened the suite rather than merely adding green.
</output>
