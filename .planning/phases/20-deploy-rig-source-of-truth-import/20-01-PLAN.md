---
phase: 20-deploy-rig-source-of-truth-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - offchain/rig/check-upstream.sh
  - offchain/rig/import-ref.txt
  - .planning/phases/20-deploy-rig-source-of-truth-import/FORGE-BASELINE.md
autonomous: true
requirements: [RIG-01]

must_haves:
  truths:
    - "A single command answers, from LIVE remote state, whether the plank->develop merge carrying the V2 rig artifacts has landed"
    - "If the merge has NOT landed the phase halts with a committed BLOCKED note naming PR #15 and issue #13 — nothing downstream is attempted"
    - "If the merge HAS landed, the exact develop commit sha used for the import is recorded on disk before any file is imported"
    - "forge build exits 0 on this branch BEFORE any import, so a post-import build failure is unambiguously attributable to the import"
    - "The pre-import forge test and make compile-plank counts are MEASURED cold and written down, so the post-import delta is a measurement and not an assumption"
  artifacts:
    - path: "offchain/rig/check-upstream.sh"
      provides: "Executable BLOCKING upstream gate; exit 0 = open, exit 2 = blocked"
      contains: "0x98d950ec"
    - path: "offchain/rig/import-ref.txt"
      provides: "The 40-char origin/develop sha the import is pinned to"
      contains: "[0-9a-f]{40}"
    - path: ".planning/phases/20-deploy-rig-source-of-truth-import/FORGE-BASELINE.md"
      provides: "Cold pre-import forge test + make compile-plank counts"
      contains: "PRE-IMPORT BASELINE"
  key_links:
    - from: "offchain/rig/check-upstream.sh"
      to: "origin/develop:src/interfaces/pos_spec/VolOrderManagerInterface.plk"
      via: "git fetch + git show piped to grep -q 0x98d950ec"
      pattern: "git show .*VolOrderManagerInterface"
    - from: "offchain/rig/check-upstream.sh"
      to: "offchain/rig/import-ref.txt"
      via: "writes git rev-parse origin/develop on success"
      pattern: "import-ref.txt"
---

<objective>
Open (or provably close) the upstream gate, then make the tree buildable and take the cold
pre-import measurements the rest of the phase is judged against.

Purpose: Phase 20's entire deliverable is an IMPORT from `origin/develop`. If PR #15
(`feat/plank` to `develop`) has not merged, there is nothing to import and every subsequent task
in this phase is unexecutable. Research §1 measured the gate CLOSED at 2026-07-31 14:20 UTC
(`origin/develop` = `1c41935`, PR #15 OPEN). That measurement EXPIRES the moment PR #15 merges —
it must be re-taken live, by a command, at execution time.
Separately, research §7.1 MEASURED that `forge build` fails on this branch today (`node_modules/`
absent, four tracked test files remap into it). Fixing that BEFORE the import is what makes a
post-import failure attributable.

Output: `offchain/rig/check-upstream.sh` (the gate), `offchain/rig/import-ref.txt` (the pinned
sha), `FORGE-BASELINE.md` (cold counts).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-deploy-rig-source-of-truth-import/20-CONTEXT.md
@.planning/phases/20-deploy-rig-source-of-truth-import/20-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write and RUN the blocking upstream gate</name>
  <files>offchain/rig/check-upstream.sh, offchain/rig/import-ref.txt</files>

  <read_first>
    - `.planning/phases/20-deploy-rig-source-of-truth-import/20-RESEARCH.md` section 1 (the
      measured gate state and the prescribed commands — the `0x98d950ec` grep is the sharpest
      single discriminator between "develop has the files" and "develop has the v1 files")
    - `.planning/phases/20-deploy-rig-source-of-truth-import/20-CONTEXT.md`, section
      "Import mechanism — VIA DEVELOP (upstream gate)"
    - `CLAUDE.md` (multi-instance ownership map — the plank track is claude-peers agent `ul2inqpl`)
  </read_first>

  <action>
Create `offchain/rig/` and write `offchain/rig/check-upstream.sh` with EXACTLY this behaviour,
then `chmod +x` it:

    #!/usr/bin/env bash
    # Phase 20 BLOCKING upstream gate.
    # Exit 0 = the plank->develop merge carrying the V2 rig artifacts HAS landed; the 40-char
    #          origin/develop sha is written to offchain/rig/import-ref.txt and echoed.
    # Exit 2 = BLOCKED. PR #15 (feat/plank -> develop) has not merged. Nothing in Phase 20 is
    #          executable. This is cross-track coordination (issue #13 / peer ul2inqpl), never a
    #          local problem to work around.
    set -euo pipefail
    cd "$(git rev-parse --show-toplevel)"

    git fetch origin --quiet
    DEV=$(git rev-parse origin/develop)

    REQUIRED_PATHS=(
      foundry-scripts/deploy/PlankDeployBase.s.sol
      foundry-scripts/deploy/DeployVolOrderManagerMod.s.sol
      foundry-scripts/deploy/DeployRealizedVolatilityMod.s.sol
      foundry-scripts/deploy/DeployDynamicFeeMod.s.sol
      foundry-scripts/deploy/DeployDynamicFeeHook.s.sol
      notes/DATA_CONTRACT.md
      notes/UNITS_AND_SCALES.md
      .planning/rpc-api-volorder-v2-HANDOFF.md
      src/interfaces/premium/DynamicFeeInterface.plk
      src/interfaces/pos_spec/VolOrderManagerInterface.plk
    )
    missing=0
    for p in "${REQUIRED_PATHS[@]}"; do
      git cat-file -e "$DEV:$p" 2>/dev/null || { echo "ABSENT on $DEV: $p"; missing=1; }
    done

    # The V1-vs-V2 discriminator. A path-existence check alone CANNOT tell a merged V2 interface
    # from the stale v1 file (selector 0x6501fe94, no event block).
    if ! git show "$DEV:src/interfaces/pos_spec/VolOrderManagerInterface.plk" 2>/dev/null \
         | grep -q '0x98d950ec'; then
      echo "STALE/ABSENT: VolOrderManagerInterface.plk on $DEV lacks the V2 selector 0x98d950ec"
      missing=1
    fi

    if [ "$missing" -ne 0 ]; then
      echo "BLOCKED: PR #15 (feat/plank -> develop) has NOT merged."
      echo "  origin/develop = $DEV"
      echo "  Phase 20 imports its artifacts FROM origin/develop (20-CONTEXT.md, locked decision)."
      echo "  Coordination: GitHub issue #13 / claude-peers agent ul2inqpl (plank development)."
      echo "  Do NOT import from origin/feat/plank as a workaround - the recorded develop ref IS"
      echo "  the SC-1 acceptance target."
      exit 2
    fi

    printf '%s\n' "$DEV" > offchain/rig/import-ref.txt
    echo "OPEN: origin/develop = $DEV carries the V2 rig artifacts (0x98d950ec present)."
    echo "recorded -> offchain/rig/import-ref.txt"

Then RUN it: `bash offchain/rig/check-upstream.sh; echo "gate_exit=$?"`.

HALT PROTOCOL — this is the only branch in Phase 20 that stops the phase.

If `gate_exit` is 2:
  1. Write `.planning/phases/20-deploy-rig-source-of-truth-import/20-BLOCKED.md` containing: the
     verbatim script output, the current `git rev-parse origin/develop`, the current output of
     `gh pr list --repo JMSBPP/cfmm-replicationPlank --state open`, and the sentence
     "Phase 20 is BLOCKED on the plank->develop merge (PR #15). No local workaround exists: the
     recorded develop ref is the SC-1 acceptance target."
  2. Commit `offchain/rig/check-upstream.sh` and `20-BLOCKED.md`.
  3. STOP. Do not execute Task 2, Task 3, or any task in plans 20-02 through 20-05. Report
     BLOCKED to the orchestrator, naming PR #15 and issue #13.

If `gate_exit` is 0: continue to Task 2.
  </action>

  <verify>
    <automated>bash offchain/rig/check-upstream.sh && test -x offchain/rig/check-upstream.sh && grep -qE '^[0-9a-f]{40}$' offchain/rig/import-ref.txt</automated>
  </verify>

  <acceptance_criteria>
    - `test -x offchain/rig/check-upstream.sh` exits 0
    - `grep -c '0x98d950ec' offchain/rig/check-upstream.sh` is at least 1
    - `grep -c 'exit 2' offchain/rig/check-upstream.sh` is at least 1
    - `grep -c 'issue #13' offchain/rig/check-upstream.sh` is at least 1
    - Gate OPEN path: `grep -qE '^[0-9a-f]{40}$' offchain/rig/import-ref.txt` exits 0 AND
      `git cat-file -e "$(cat offchain/rig/import-ref.txt):notes/DATA_CONTRACT.md"` exits 0
    - Gate BLOCKED path: `20-BLOCKED.md` exists, `grep -q 'PR #15' 20-BLOCKED.md` exits 0, and no
      other task in this phase has been executed
  </acceptance_criteria>

  <done>The gate is a command, not a note. It either runs green (develop ref recorded on disk) or it halts the phase with a committed BLOCKED record.</done>
</task>

<task type="auto">
  <name>Task 2: npm/submodule preflight — make forge build exit 0 BEFORE any import</name>
  <files>node_modules/ (untracked), lib/panoptic-v2-core/ (submodule checkout) — NO tracked file is modified by this task</files>

  <read_first>
    - `.planning/phases/20-deploy-rig-source-of-truth-import/20-RESEARCH.md` section 7.1 (the
      MEASURED `forge build` failure and its root cause) and section 4 "Installation / preflight"
    - `foundry.toml` (the `@cryptoalgebra/...` remappings into `node_modules/`, and the
      `[rpc_endpoints] local = "http://127.0.0.1:8545"` alias the rig relies on)
    - `package.json`
  </read_first>

  <action>
Run, from the repo root, in this order:

    npm ci --ignore-scripts
    git submodule update --init lib/panoptic-v2-core
    git -C lib/panoptic-v2-core config submodule.lib/panoptic-helper.update none
    git submodule update --init --recursive
    forge build

`--ignore-scripts` is not optional: it is the exact invocation plank's own `develop-gate.yml`
uses, with the in-file comment "node_modules became a HARD forge dependency with the premium
port". The `submodule.lib/panoptic-helper.update none` line is the infinite-recursion guard,
lifted verbatim from that workflow.

The observed failure this fixes (research section 7.1, MEASURED in this worktree 2026-07-31):

    Error (6275): Source "node_modules/@cryptoalgebra/volatility-oracle-plugin/contracts/
    VolatilityOraclePluginImplementation.sol" not found --> test/MarketStatisticsTest.t.sol:8:1

`forge build` MUST exit 0. If it does not, do NOT edit `foundry.toml`, `remappings.txt`, or any
file under `test/` or `src/` — all are other tracks' territory per `CLAUDE.md`. Record the exact
error verbatim and stop the plan.

Do not commit `node_modules/` (already gitignored). This task changes no tracked file; its
outputs are a working build environment plus the recorded exit codes for the summary.
  </action>

  <verify>
    <automated>forge build; echo "forge_build_exit=$?"</automated>
  </verify>

  <acceptance_criteria>
    - `test -d node_modules/@cryptoalgebra` exits 0
    - `forge build; echo "forge_build_exit=$?"` prints `forge_build_exit=0`
    - `test -f lib/panoptic-v2-core/lib/v4-core/src/PoolManager.sol` exits 0
    - `git status --porcelain foundry.toml remappings.txt` produces NO output
  </acceptance_criteria>

  <done>forge build exits 0 on a pre-import tree, so any later build failure is attributable to the import and to nothing else.</done>
</task>

<task type="auto">
  <name>Task 3: Take the cold PRE-IMPORT forge test and plank compile baselines</name>
  <files>.planning/phases/20-deploy-rig-source-of-truth-import/FORGE-BASELINE.md</files>

  <read_first>
    - `.planning/phases/20-deploy-rig-source-of-truth-import/20-RESEARCH.md` section 7.5
      (importing the V2 `src/` closure WILL turn `test/pos_spec/*.t.sol` red at runtime; that is
      expected, is the Solidity-testing session's territory, and must be MEASURED and ATTRIBUTED,
      never repaired)
    - `.planning/STATE.md`, the `[19-05 MEASURED...]` and `[19-05 FINDING...]` entries. The last
      cold measurement there was 102 passed / 18 failed / 120 total (44 suites) and
      `make compile-plank` 11 ok / 2 failed. Those are from a DIFFERENT branch state and are a
      SANITY REFERENCE ONLY — re-measure cold, never copy them forward.
    - `Makefile` (read-only — this workstream never edits it)
  </read_first>

  <action>
Take BOTH baselines cold. Do not infer either from a prior phase's record.

    forge test --via-ir --fuzz-seed 4880 2>&1 | tee /tmp/forge-test-pre-import.log | tail -40
    make compile-plank 2>&1 | tee /tmp/plank-compile-pre-import.log | tail -20

Then create `.planning/phases/20-deploy-rig-source-of-truth-import/FORGE-BASELINE.md` with this
exact structure, filling every angle-bracket slot from the logs and never from memory:

    # PRE-IMPORT BASELINE (Phase 20, plan 20-01 task 3)

    Measured cold: <output of `date -u +%FT%TZ`>
    Tree state: HEAD = <git rev-parse HEAD>
    Import ref (origin/develop) = <cat offchain/rig/import-ref.txt>
    Command: `forge test --via-ir --fuzz-seed 4880`

    ## forge test
    - total: <N>
    - passed: <N>
    - failed: <N>
    - suites: <N>

    ### Failing tests (verbatim identifiers, one per line)
    <paste every failing test identifier>

    ## make compile-plank
    - ok: <N>
    - failed: <N>
    - skipped: <N>

    ### Failing entrypoints (verbatim)
    <paste>

    ## Attribution note
    These are the PRE-import numbers. Plan 20-02 task 3 re-measures with the identical commands
    and records the delta. Per research section 7.5 and CLAUDE.md, `test/` belongs to the
    Solidity-testing session: new reds caused by the V2 import are REPORTED and ATTRIBUTED,
    never repaired.

Known measurement trap, from `.planning/STATE.md` [19-05 FINDING]: a naive
`grep FAIL | grep -c pos_spec` counts the `--dep pos_spec=...` flag echoed inside
`[FAIL: vm.ffi: ffi command [...]]` lines and reports a wildly wrong number. Take counts from
forge's own summary line and list failing test identifiers verbatim; never derive counts by
grepping paths out of failure text.
  </action>

  <verify>
    <automated>grep -q "PRE-IMPORT BASELINE" .planning/phases/20-deploy-rig-source-of-truth-import/FORGE-BASELINE.md && grep -qE '^- total: [0-9]+' .planning/phases/20-deploy-rig-source-of-truth-import/FORGE-BASELINE.md && grep -qE '^- ok: [0-9]+' .planning/phases/20-deploy-rig-source-of-truth-import/FORGE-BASELINE.md</automated>
  </verify>

  <acceptance_criteria>
    - `FORGE-BASELINE.md` exists and `grep -q 'PRE-IMPORT BASELINE'` exits 0
    - It contains lines matching `^- total: [0-9]+`, `^- passed: [0-9]+`, `^- failed: [0-9]+`, `^- suites: [0-9]+`
    - It contains lines matching `^- ok: [0-9]+`, `^- failed: [0-9]+`, `^- skipped: [0-9]+` under the compile-plank heading
    - The `Import ref` line contains the same 40-char sha as `offchain/rig/import-ref.txt`
    - No angle-bracket placeholder `<` remains: `grep -c '<' FORGE-BASELINE.md` is 0
    - `git status --porcelain test/ src/ Makefile` produces NO output
  </acceptance_criteria>

  <done>Both pre-import counts exist on disk as measured numbers with named failing tests, ready for 20-02's delta.</done>
</task>

</tasks>

<verification>
1. `bash offchain/rig/check-upstream.sh` exits 0 and `offchain/rig/import-ref.txt` holds a 40-char sha
2. `forge build` exits 0
3. `FORGE-BASELINE.md` contains cold measured counts for both `forge test` and `make compile-plank`
4. `git status --porcelain src/ test/ Makefile foundry.toml remappings.txt` produces NO output
</verification>

<success_criteria>
- The upstream gate is executable and was RUN, not merely written
- The import ref is a recorded 40-char sha on disk, not a value carried in prose
- The tree builds before any import
- Both baselines are measured numbers with verbatim failing-test names, not inherited figures
</success_criteria>

<output>
After completion, create
`.planning/phases/20-deploy-rig-source-of-truth-import/20-01-SUMMARY.md`.
Record: the gate result and the exact `origin/develop` sha (or the BLOCKED record), the
`forge build` exit code, and both baseline count sets verbatim.
</output>
