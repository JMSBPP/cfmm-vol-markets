---
phase: 15-differential-verification-mutation-battery-plank-skip-exit
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/lib/exposure/VegaIssuanceLib.plk
  - src/modules/exposure/VegaAccountMod.plk
  - Makefile
autonomous: true
requirements:
  - VVER-02
must_haves:
  truths:
    - "Each of the FIVE killable mutants (p_risk ceil->floor lib, shares floor->ceil lib, slot-constant aliasing module, dust-guard deletion module, raw checked cross-product guard module) produces an OBSERVED RED at the e2e/reused kill sites with cache/fuzz cleared before each"
    - "After each mutant is restored, sha256sum matches the recorded baseline (lib 2ee071627e25f4fe07b6e78cb5e163435cdfb737b4dcf293939c5a8ae7bfc7e3, module 555a7a100b97f41bcdf3604141065fc2fe3a1e2d63a5ec9ffcb12b9172818120) and the suite re-runs GREEN"
    - "The two equivalence-masked mutants (h-bound <-><= lib, unset-price guard deletion module) are documented as equivalence-checked defense-in-depth, NEVER counted as kills"
    - "make compile-plank reports 12 ok, 0 failed, 0 skipped — VegaAccountMod is out of PLANK_SKIP and compiles standalone"
    - "make test compiles (PriceSetterHook.sol skipped) and runs; all vega + e2e tests pass; the 5 pre-existing pos_spec failures REMAIN visible (never skipped/filtered)"
  artifacts:
    - path: "Makefile"
      provides: "PLANK_SKIP with VegaAccountMod removed + updated rescue-queue comment; test: target with the PriceSetterHook --skip guard + updated CURRENTLY-RED comment counts; a dedicated test-vega-e2e target"
      contains: "test-vega-e2e"
    - path: "src/modules/exposure/VegaAccountMod.plk"
      provides: "Restored to the recorded baseline (net-unchanged after the battery)"
      contains: "SLOT_RISK_WEIGHTED_SHARES"
    - path: "src/lib/exposure/VegaIssuanceLib.plk"
      provides: "Restored to the recorded baseline (net-unchanged after the battery)"
      contains: "mulDivRoundingUp"
  key_links:
    - from: "Makefile test: target"
      to: "the whole forge suite incl. the vega + e2e tests"
      via: "forge test with --skip PriceSetterHook.sol (un-breaks make test for the local tree)"
      pattern: "forge test .*--skip 'src/modules/protocol_integrations/PriceSetterHook.sol'"
    - from: "Makefile PLANK_SKIP"
      to: "make compile-plank 12 ok / 0 skipped"
      via: "VegaAccountMod.plk line deleted"
      pattern: "PLANK_SKIP    :="
---

<objective>
Prove the whole VegaAccountMod surface FALSIFIABLE (VVER-02): an observed-RED mutation battery of five
killable mutants over the end-to-end differential and the reused Phase-13/14 tests, each with cache/fuzz
cleared and restored sha256-identical to the recorded baselines. Then EXIT PLANK_SKIP (VegaAccountMod
now compiles standalone -> 12 ok, 0 skipped) and FOLD the vega + e2e suites into `make test` (un-breaking
it via the PriceSetterHook skip), recording verbatim `make compile` and `make test` tails as the runs of
record.

Purpose: this is the milestone acceptance gate. No green is trusted until every killable mutant is
observed red; PLANK_SKIP shrinks only when the module is proven, never on compile alone.
Output: net-unchanged source files (battery restores them), an updated Makefile, and the two runs of record.

DO NOT run gsd:complete-milestone — the milestone close/audit is a SEPARATE user decision after the phase
verifier passes.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-differential-verification-mutation-battery-plank-skip-exit/15-CONTEXT.md

# The mutation targets (the exact lines to flip):
@src/lib/exposure/VegaIssuanceLib.plk
@src/modules/exposure/VegaAccountMod.plk

# The kill sites the battery reddens (both the new e2e file AND the reused Phase-13/14 tests):
@test/exposure/VegaAccount.e2e.t.sol
@test/exposure/VegaAccount.t.sol
@test/exposure/VegaIssuance.diff.t.sol

# The Makefile blocks being edited: PLANK_SKIP (line ~145), the test: target (line ~37-38) and its
# CURRENTLY-RED comment (lines ~27-36):
@Makefile

<interfaces>
<!-- CRITICAL PRE-CONDITION — the working-tree baseline has diverged. -->
As of planning, the working-tree copies of BOTH source files carry uncommitted BEHAVIOR-NEUTRAL
comment-only WIP edits (verified 14-02/13 docblocks replaced by scratch `todo:` comments). Their
sha256 does NOT match the recorded baselines, but `git show HEAD:<file>` DOES:
    git show HEAD:src/modules/exposure/VegaAccountMod.plk | sha256sum  -> 555a7a10...818120  (baseline)
    git show HEAD:src/lib/exposure/VegaIssuanceLib.plk    | sha256sum  -> 2ee07162...bfc7e3  (baseline)
The battery's sha256 restoration check requires the PRISTINE baseline on disk. Task 1 restores HEAD
FIRST (behaviour-neutral — the code is byte-identical modulo comments; the verified docblocks are the
better text). If a peer/track is found to own those WIP comments, confirm before discarding; otherwise
`git checkout HEAD -- <files>` is the correct restore.

The exact mutation edits (apply, observe RED, restore):
- p_risk ceil->floor (LIB, VegaIssuanceLib.plk:16): mulDivRoundingUp(oracleX96, TWO_96, denom) -> mulDiv(oracleX96, TWO_96, denom)
- shares floor->ceil (LIB, VegaIssuanceLib.plk:22): mulDiv(deposit, TWO_96, pRiskX96.val) -> mulDivRoundingUp(deposit, TWO_96, pRiskX96.val)
- slot-constant aliasing (MODULE, VegaAccountMod.plk:14): SLOT_RISK_WEIGHTED_SHARES value := the SLOT_TOTAL_SHARES value (0x8d1d621c...c31c)
- dust-guard deletion (MODULE, VegaAccountMod.plk:32): delete `if @evm_iszero(shares) { revert_empty(); }`
- cross-product guard (MODULE, VegaAccountMod.plk:43): replace `require(new_total_deposits >= collateral);` with a raw CHECKED cross-product, e.g. `let _x = collateral * storedRiskPrice;` (checked `*` overflow-reverts at ~2^200)

Equivalence-documented, NEVER a kill:
- h-bound (LIB, VegaIssuanceLib.plk:12): `>=` -> `>` (hX96.val > TWO_96) — at hX96==2^96 the denom is 0 and mulDivRoundingUp reverts regardless; baseline and mutant both revert empty.
- unset-price guard deletion (MODULE, VegaAccountMod.plk:27): delete `if @evm_iszero(storedRiskPrice) { revert_empty(); }` — the lib's own mulDiv zero-denominator revert masks it; both revert empty, state unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Observed-RED mutation battery over the e2e + reused surface (baseline-restored first)</name>
  <read_first>
    - 15-CONTEXT.md §The battery — the five killable mutants, the two equivalence-documented non-kills, and the per-mutant apply -> rm -rf cache/fuzz -> observe RED verbatim -> restore -> sha256 == baseline -> green protocol.
    - test/exposure/VegaAccount.e2e.t.sol (15-01) — the primary e2e kill site.
    - test/exposure/VegaAccount.t.sol — VegaAccountSlotDistinctnessTest (vm.load kill for slot-aliasing), VegaAccountGuardTest.test__unit__dustDepositReverts, VegaAccountCrossProductMutantTest (the 2^200 overflow kill).
    - test/exposure/VegaIssuance.diff.t.sol — VegaIssuanceKernelProbeTest (the inexact anchor: cache-independent kill for both rounding flips).
    - STATE.md Accumulated Context — the cached-replay rule (a runs:0 kill is a REPLAY; clear cache/fuzz) and the FFI-recompile fact (no make compile-plank needed between mutants).
  </read_first>
  <files>src/lib/exposure/VegaIssuanceLib.plk, src/modules/exposure/VegaAccountMod.plk</files>
  <action>
    STEP 0 — establish the pristine baseline (see the pre-condition note in context):
      git checkout HEAD -- src/lib/exposure/VegaIssuanceLib.plk src/modules/exposure/VegaAccountMod.plk
      sha256sum src/lib/exposure/VegaIssuanceLib.plk src/modules/exposure/VegaAccountMod.plk
      # MUST print 2ee071627e25f4fe07b6e78cb5e163435cdfb737b4dcf293939c5a8ae7bfc7e3 (lib)
      #       and 555a7a100b97f41bcdf3604141065fc2fe3a1e2d63a5ec9ffcb12b9172818120 (module).
      # If either differs, STOP — the baseline is wrong and every restore check below is void.

    The suite run used per mutant (reddens the e2e AND the reused Phase-13/14 files in one shot):
      rm -rf cache/fuzz
      forge test --match-path 'test/exposure/*.t.sol' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize
    Precondition: with the pristine baseline this run is GREEN (record the green tail once, up front).

    For EACH of the five killable mutants, execute the protocol and RECORD the verbatim failing line:
      1. apply the edit (Edit tool — the exact edits are in the context <interfaces> block)
      2. rm -rf cache/fuzz                       # a runs:0 "kill" is a cached REPLAY, not a kill
      3. run the suite above -> OBSERVE exit non-zero (RED); copy the verbatim failing assertion + values
      4. restore: git checkout HEAD -- <the mutated file>
      5. sha256sum <file> == the recorded baseline for that file
      6. re-run the suite -> GREEN (exit 0)

    Expected RED signatures (each MUST be observed, not inferred):
      (a) p_risk ceil->floor (lib): e2e `p_risk ceil: module preview vs mock` fails
          (module previewRiskPrice floors to 60944740395587951995033807950 vs mock 60944740395587951995033807951);
          VegaIssuanceKernelProbeTest `p_risk ceil at inexact anchor` also fails.
      (b) shares floor->ceil (lib): e2e after-write assert fails at the Phase-12 step (totalShares 1013 vs mirror 1012)
          and/or the 2.5->3 step; VegaIssuanceKernelProbeTest `shares floor at inexact anchor` fails (13 != 12).
      (c) slot-constant aliasing (module): e2e first deposit -> totalShares doubles (2000 vs mirror 1000);
          VegaAccountSlotDistinctnessTest `riskWeightedShares slot` vm.load reads 0 (the real slot never written).
      (d) dust-guard deletion (module): e2e `_depositExpectRevertBoth(1)` -> the module does NOT revert
          (banks 0 shares) so vm.expectRevert fails; VegaAccountGuardTest.test__unit__dustDepositReverts fails.
      (e) cross-product guard (module): e2e 2^200 weight-one deposit -> checked-multiply overflow (Panic 0x11)
          where the baseline accepts; VegaAccountCrossProductMutantTest fails.

    THEN document the two EQUIVALENCE-MASKED non-kills (apply each, observe the suite STAYS GREEN, restore):
      - h-bound `>=`->`>` (lib): suite stays green (denom-0 revert masks it). NOT a kill.
      - unset-price guard deletion (module): suite stays green (lib zero-price revert masks it). NOT a kill.
      Record both as equivalence-checked defense-in-depth. Counting either as a kill is the
      false-verification pattern this project catalogues.

    FINAL: sha256sum both files == the recorded baselines (net-unchanged); one last GREEN suite run.
  </action>
  <verify>
    <automated>sha256sum src/lib/exposure/VegaIssuanceLib.plk src/modules/exposure/VegaAccountMod.plk | tee /dev/stderr | grep -q '2ee071627e25f4fe07b6e78cb5e163435cdfb737b4dcf293939c5a8ae7bfc7e3' && rm -rf cache/fuzz && forge test --match-path 'test/exposure/*.t.sol' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five killable mutants was OBSERVED red (verbatim failing line recorded in the SUMMARY) with cache/fuzz cleared before the run — a runs:0 replay is NOT accepted as a kill.
    - After every mutant the file restored to its recorded sha256 (lib 2ee07162...bfc7e3, module 555a7a10...818120) and the suite re-ran green.
    - The two equivalence-masked mutants were applied, the suite STAYED green, and both are documented as NON-kills — never counted.
    - Final state: `sha256sum src/lib/exposure/VegaIssuanceLib.plk src/modules/exposure/VegaAccountMod.plk` equals the two recorded baselines and the whole `test/exposure/*.t.sol` suite is green.
  </acceptance_criteria>
  <done>All five killable mutants observed red then restored sha256-identical to the recorded baselines with the suite green; the two equivalence-masked mutants documented as non-kills; source files net-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Exit PLANK_SKIP, fold the vega + e2e suites into make test, record the runs of record</name>
  <read_first>
    - 15-CONTEXT.md §PLANK_SKIP exit and §make test fold-in — the exact edits and the honest framing.
    - Makefile — the PLANK_SKIP block (rescue-queue comment ~138-145 + the value at ~145), the `test:` target (~37-38) and its CURRENTLY-RED comment (~27-36), and the existing focused targets test-vega-account/test-vega-issuance (~103-112) to mirror for test-vega-e2e.
  </read_first>
  <files>Makefile</files>
  <action>
    PLANK_SKIP exit:
      - Delete the `src/modules/exposure/VegaAccountMod.plk` value from `PLANK_SKIP :=` (line ~145) so the
        variable is now empty (`PLANK_SKIP    :=`).
      - Update the rescue-queue comment (lines ~138-145): remove the VegaAccountMod entry; note the queue
        is now EMPTY — VegaAccountMod went green in Phase 15 (VVER-02) and left the queue as designed
        ("this list should only ever shrink").
      - Run `make compile-plank`. EXPECT the tail `compile-plank: 12 ok, 0 failed, 0 skipped`.
        If VegaAccountMod FAILS standalone compile (an import quirk surfacing only via the entrypoint path),
        that is a REAL finding — FIX the module forward (record the fix), NEVER re-add the skip.

    make test fold-in:
      - Edit the `test:` recipe (line ~38) from
          forge test --via-ir --optimize
        to
          forge test --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize
        with a loud inline comment: the skip routes around an UNTRACKED parallel-track stray
        (src/modules/protocol_integrations/PriceSetterHook.sol, PR #11) whose empty imports break
        `forge build` of the whole tree and thus `make test` for everyone; it is a NO-OP once the owning
        track removes the file. HONEST framing: this un-breaks make test for the local tree without
        touching another track's file.
      - Update the "THIS TARGET IS CURRENTLY RED" comment block (lines ~27-36) counts to the MEASURED truth
        from the `make test` run below (do NOT guess): previous 50 pass / 5 fail, now + the vega suites
        (VegaIssuance.diff + VegaAccount + the new VegaAccount.e2e) all passing. The 5 pre-existing
        pos_spec harness failures REMAIN visible (never skipped/filtered) — restate them.
      - Add a dedicated focused target mirroring the siblings (VVER-02 "dedicated make target"):
          test-vega-e2e:
              forge test --match-path 'test/exposure/VegaAccount.e2e.t.sol' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize
        and add test-vega-e2e to the relevant `.PHONY` line.

    Runs of record (the phase's last acts):
      - Run `make compile` and capture the verbatim tail (expect `compile-plank: 12 ok, 0 failed, 0 skipped`).
      - Run `make test` and capture the verbatim tail (the measured pass/fail counts; the 5 pos_spec
        failures still present and named). Write BOTH verbatim tails into the SUMMARY.
      - DO NOT run gsd:complete-milestone.
  </action>
  <verify>
    <automated>make compile 2>&1 | tail -3 | grep -q 'compile-plank: 12 ok, 0 failed, 0 skipped' && grep -q "skip 'src/modules/protocol_integrations/PriceSetterHook.sol'" Makefile && grep -q 'test-vega-e2e' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'PLANK_SKIP    :=' Makefile` shows the value EMPTY (no VegaAccountMod.plk).
    - `make compile` tail reads exactly `compile-plank: 12 ok, 0 failed, 0 skipped`.
    - The `test:` recipe contains `--skip 'src/modules/protocol_integrations/PriceSetterHook.sol'` with the loud comment; `make test` compiles and runs (no forge build break).
    - `make test` passes the vega + e2e tests and STILL shows the 5 pos_spec failures (they are named in the updated comment block and NOT filtered).
    - `test-vega-e2e` target exists and is in `.PHONY`.
    - Verbatim `make compile` and `make test` tails are recorded in the SUMMARY.
  </acceptance_criteria>
  <done>VegaAccountMod is out of PLANK_SKIP with make compile-plank at 12 ok / 0 skipped; make test compiles via the PriceSetterHook skip and runs the folded-in vega + e2e suites green with the 5 pos_spec failures still visible; both runs of record captured.</done>
</task>

</tasks>

<verification>
- All five killable mutants observed red (cache/fuzz cleared) then restored sha256-identical; two equivalence-masked mutants documented as non-kills.
- `sha256sum` of both source files == the recorded baselines (net-unchanged by the battery).
- `make compile` tail: `compile-plank: 12 ok, 0 failed, 0 skipped`.
- `make test` compiles (PriceSetterHook skipped) and runs; vega + e2e green; the 5 pos_spec failures remain visible and named.
- gsd:complete-milestone NOT run.
</verification>

<success_criteria>
VVER-02 satisfied: every KILLABLE mutant (rounding-direction flips at the inexact anchor, slot-constant
aliasing, dust-guard deletion, raw checked cross-product guard) is observed red with cache/fuzz cleared
and restored sha256-identical; the two equivalence-masked mutants are documented, never counted;
VegaAccountMod leaves PLANK_SKIP (make compile-plank 12 ok / 0 skipped) after deposit is CALLED green; the
suite is wired into a dedicated make target and folded into make test, which runs green under
--via-ir --optimize with the 5 pos_spec failures still visible. Milestone close is a separate user decision.
</success_criteria>

<output>
After completion, create `.planning/phases/15-differential-verification-mutation-battery-plank-skip-exit/15-02-SUMMARY.md`.
Record: the per-mutant verbatim RED lines + restored-green confirmations; the sha256 restorations; and the
verbatim `make compile` and `make test` tails as the runs of record.
</output>
