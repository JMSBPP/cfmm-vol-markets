---
phase: 15-differential-verification-mutation-battery-plank-skip-exit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/exposure/VegaAccount.e2e.t.sol
autonomous: true
requirements:
  - VVER-01
must_haves:
  truths:
    - "Driving identical (setRiskPrice, deposit) sequences into VegaAccountMod (FFI-deployed) and the IssuanceRefMock-backed mirror, all three accumulators (totalDeposits, totalShares, riskWeightedShares) are equal at tolerance 0 AFTER EVERY write, asserted INSIDE the driver helpers"
    - "The fixed anchor reproduces the Phase-12 value (deposit=10 at the P12 price mints exactly 12 shares) and derives that price via the module's previewRiskPrice diffed tolerance-0 against mock.haircutRiskPrice — so the p_risk ceil rounding site is sensitive in the e2e"
    - "A mid-sequence dust deposit (shares floor to 0 at pRisk=2^97) reverts on the module and leaves all three accumulators synced with the mirror"
    - "riskWeightedShares == totalShares is asserted as a d==1 consequence, using two SEPARATE mirror accumulators (never conflated in the driver's bookkeeping)"
    - "The corpus is CONSTRUCTED (bound only, no vm.assume), non-vacuous (>=1 nonzero mint), with mid-sequence setRiskPrice changes so later deposits price at the NEW price, and includes a ~2^200 weight-one deposit point that the baseline accepts"
  artifacts:
    - path: "test/exposure/VegaAccount.e2e.t.sol"
      provides: "End-to-end (setRiskPrice, deposit) sequence differential: module vs stateful mirror over IssuanceRefMock; assertion inside _setPriceBoth/_depositBoth; fixed anchor + constructed fuzz"
      contains: "contract VegaAccountE2EDiffTest"
      min_lines: 120
  key_links:
    - from: "test/exposure/VegaAccount.e2e.t.sol"
      to: "src/modules/exposure/VegaAccountMod.plk"
      via: "IVegaAccount deposit/setRiskPrice/readers via deployPlank (FFI)"
      pattern: "deployPlank\\(\"src/modules/exposure/VegaAccountMod.plk\"\\)"
    - from: "test/exposure/VegaAccount.e2e.t.sol"
      to: "test/mocks/IssuanceRefMock.sol"
      via: "mock.issueShares / mock.haircutRiskPrice as the mirror's expected-value source"
      pattern: "mock\\.(issueShares|haircutRiskPrice)"
---

<objective>
Build the milestone acceptance driver (VVER-01): an end-to-end differential that drives IDENTICAL
`(setRiskPrice, deposit)` sequences into `VegaAccountMod` (FFI-deployed) and a trivially-simple
stateful mirror backed by `IssuanceRefMock`, asserting all three accumulators equal at TOLERANCE 0
AFTER EVERY write, with the assertion INSIDE the driver helpers so it aborts at the earliest write a
mutant can diverge.

Purpose: This is the differential the Phase-15 mutation battery (15-02) reddens. It must be
rounding-sensitive at BOTH lib sites (p_risk ceil, shares floor), guard-sensitive (dust revert), and
overflow-sensitive (~2^200 point) so 15-02's killable mutants each have a green to redden.
Output: `test/exposure/VegaAccount.e2e.t.sol` — one new contract, green under
`forge test --match-path 'test/exposure/VegaAccount.e2e.t.sol' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-differential-verification-mutation-battery-plank-skip-exit/15-CONTEXT.md

# THE after-every-write driver pattern to MIRROR (assertion inside _writeBoth; fixed anchor +
# constructed fuzz; abort-at-earliest-divergence rationale in its docblock):
@test/market_state_measurements/RealizedVolatility.diff.t.sol

# The pieces being assembled:
@test/exposure/VegaAccount.t.sol
@test/mocks/IssuanceRefMock.sol
@src/modules/exposure/VegaAccountMod.plk
@src/interfaces/exposure/VegaAccountInterface.plk

<interfaces>
<!-- Executor uses these directly — no codebase exploration needed. -->

The module ABI (identical to IVegaAccount in test/exposure/VegaAccount.t.sol — REUSE that
declaration by importing it, or re-declare it locally verbatim):
```solidity
interface IVegaAccount {
    function deposit(uint256 collateralAmt) external;
    function setRiskPrice(uint256 pRiskX96) external;
    function previewDeposit(uint256 amt) external view returns (uint256);
    function previewRiskPrice(uint256 oracleX96, uint256 hX96) external view returns (uint256);
    function totalDeposits() external view returns (uint256);
    function totalShares() external view returns (uint256);
    function riskWeightedShares() external view returns (uint256);
    function riskPrice() external view returns (uint256);
}
```

The reference mock (test/mocks/IssuanceRefMock.sol) — the mirror's expected-value source:
```solidity
// floor: shares = floor(deposit * 2^96 / pRiskX96); reverts pRiskX96==0
function issueShares(uint256 deposit, uint256 pRiskX96) public pure returns (uint256);
// ceil: p_risk = ceil(oracleX96 * 2^96 / (2^96 - hX96)); reverts oracle==0 or h>=2^96
function haircutRiskPrice(uint256 oracleX96, uint256 hX96) public pure returns (uint256);
```

Module storage semantics (from VegaAccountMod.plk run{}): setRiskPrice(p) stores p RAW into
SLOT_RISK_PRICE (no haircut applied). deposit(c) computes shares = issue_shares(c, storedRiskPrice)
= mulDiv(c, 2^96, storedRiskPrice) FLOOR, then adds c to totalDeposits and shares to BOTH
totalShares and riskWeightedShares (d==1). Guards: deposit(0) reverts; storedRiskPrice==0 reverts;
shares==0 (dust) reverts.

deployPlank(path) is provided by PlankTestBase (already imported by the sibling test files); it
FFI-compiles and deploys the .plk fresh each run — no make compile-plank needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: E2E differential driver — mirror, helpers, and the fixed hand-checkable anchor</name>
  <read_first>
    - test/market_state_measurements/RealizedVolatility.diff.t.sol — RealizedVolatilityTimepointDiffTest (lines ~769-890): the _initBoth/_writeBoth pattern with the assertion INSIDE the helper. Mirror its shape exactly.
    - test/exposure/VegaAccount.t.sol — the IVegaAccount interface (reuse it) and the existing anchors (PRISK=2^96, the 2^97 dust price, the previewRiskPrice inexact anchor at oracle=10*2^92, h=3*2^92 -> 60944740395587951995033807951).
    - test/exposure/VegaIssuance.diff.t.sol — VegaIssuanceKernelProbeTest: the Phase-12/13 anchor (deposit=10, P12 price, shares=12) is reused verbatim; do NOT invent a new anchor.
  </read_first>
  <files>test/exposure/VegaAccount.e2e.t.sol</files>
  <action>
    Create test/exposure/VegaAccount.e2e.t.sol with ONE contract `VegaAccountE2EDiffTest is PlankTestBase`.

    WHY A NEW FILE (justify in a header docblock): VegaAccount.t.sol is the MODULE dispatch/storage/
    guard surface (its header says one-file-per-surface); VegaIssuance.diff.t.sol is the LIB arithmetic
    surface. The end-to-end (setRiskPrice, deposit) SEQUENCE differential vs the Solidity reference mock
    is a distinct THIRD surface, and the repo already separates differential files by the `.diff`/named
    convention (RealizedVolatility.diff.t.sol, VegaIssuance.diff.t.sol). `VegaAccount.e2e.t.sol` (the
    name 15-CONTEXT names) keeps VegaAccount.t.sol pure. The 15-02 battery runs BOTH files.

    State (the TRIVIALLY-SIMPLE mirror lives in the test contract — visible in one file, per 15-CONTEXT):
      IVegaAccount acct;            // deployPlank("src/modules/exposure/VegaAccountMod.plk")
      IssuanceRefMock mock;         // new IssuanceRefMock()
      uint256 mDeposits;            // three SEPARATE mirror accumulators
      uint256 mShares;
      uint256 mRiskWeighted;        // NEVER conflated with mShares in bookkeeping
      uint256 mRiskPrice;           // mirror of the stored price

    setUp(): acct = IVegaAccount(deployPlank("src/modules/exposure/VegaAccountMod.plk"));
             mock = new IssuanceRefMock();  // all four mirror uints start 0.

    Helper _setPriceBoth(uint256 p) — assertion INSIDE:
      acct.setRiskPrice(p); mRiskPrice = p;
      assertEq(acct.riskPrice(), p, "riskPrice stored == mirror");
      _assertAccumulatorsMatch();  // accumulators UNCHANGED across a price set

    Helper _depositBoth(uint256 collateral) — assertion INSIDE, expected value from the mock:
      uint256 expShares = mock.issueShares(collateral, mRiskPrice); // floor, SAME as module's mulDiv
      // update the mirror BEFORE the call so the post-call assert compares the new totals
      mDeposits += collateral; mShares += expShares; mRiskWeighted += expShares;
      acct.deposit(collateral);
      _assertAccumulatorsMatch();

    Helper _depositExpectRevertBoth(uint256 collateral) — proves reverts leave state synced:
      assertEq(mock.issueShares(collateral, mRiskPrice), 0, "precondition: this IS a dust point");
      vm.expectRevert(); acct.deposit(collateral);           // module reverts; mirror UNCHANGED
      _assertAccumulatorsMatch();                            // all three still synced

    Helper _assertAccumulatorsMatch() — the three-field tolerance-0 assertion, plus the d==1 consequence:
      assertEq(acct.totalDeposits(),      mDeposits,     "totalDeposits: module vs mirror, tol 0");
      assertEq(acct.totalShares(),        mShares,       "totalShares: module vs mirror, tol 0");
      assertEq(acct.riskWeightedShares(), mRiskWeighted, "riskWeightedShares: module vs mirror, tol 0");
      assertEq(mRiskWeighted, mShares, "d==1 consequence: riskWeightedShares == totalShares (mirror kept separate)");

    THE FIXED ANCHOR TEST — test__unit__fixedAnchorSequenceDiffers() — hand-checkable, all values in a
    docblock. It MUST make BOTH lib rounding sites and the module guards sensitive:

      // 1. weight-one; accumulators must still be 0
      _setPriceBoth(1 << 96); _assertAccumulatorsMatch();
      // 2. first real deposit at weight-one: expShares = 1000
      _depositBoth(1000);                              // mirror: D=1000 S=1000 RW=1000
      // 3. mid-sequence PRICE CHANGE derived via the module's previewRiskPrice, diffed vs the mock.
      //    This is what makes the p_risk CEIL site sensitive in the e2e (a ceil->floor lib mutant
      //    makes previewRiskPrice floor and this assertEq reddens):
      uint256 p12 = acct.previewRiskPrice(10 * (2 ** 92), 3 * (2 ** 92));
      assertEq(p12, mock.haircutRiskPrice(10 * (2 ** 92), 3 * (2 ** 92)), "p_risk ceil: module preview vs mock, tol 0");
      assertEq(p12, 60944740395587951995033807951, "p_risk hand-derived anchor (Phase-12/13)");
      _setPriceBoth(p12);                              // accumulators unchanged (1000,1000,1000)
      // 4. deposit priced at the NEW price — inherits the Phase-12 anchor shares=12 (INEXACT: floor).
      //    A shares floor->ceil lib mutant makes this 13 and reddens the after-write assert.
      _depositBoth(10);                                // mirror: D=1010 S=1012 RW=1012
      // 5. dust regime, then a mid-sequence DUST REVERT proving state stays synced
      _setPriceBoth(2 * (1 << 96));                    // pRisk = 2^97
      _depositExpectRevertBoth(1);                     // floor(1*2^96/2^97)=0 -> revert; state (1010,1012,1012)
      // 6. another INEXACT deposit after the revert (5*2^96/2^97 = 2.5 -> 2)
      _depositBoth(5);                                 // mirror: D=1015 S=1014 RW=1014
      // 7. back to weight-one; a ~2^200 deposit the BASELINE accepts (the cross-product mutant's kill
      //    site in the e2e — 15-02 mutant (c) overflow-reverts here)
      _setPriceBoth(1 << 96);
      _depositBoth(1 << 200);                          // mirror: D=1015+2^200 S=1014+2^200 RW=1014+2^200
      // non-vacuity: at least one nonzero mint happened
      assertTrue(acct.totalShares() != 0, "corpus non-vacuous: shares minted");

    Import PlankTestBase from "../PlankTestBase.sol" and IssuanceRefMock from "../mocks/IssuanceRefMock.sol";
    re-declare the IVegaAccount interface locally (verbatim from VegaAccount.t.sol) OR import it.
    pragma solidity ^0.8.0; SPDX MIT.
  </action>
  <verify>
    <automated>forge test --match-path 'test/exposure/VegaAccount.e2e.t.sol' --match-test 'test__unit__fixedAnchorSequenceDiffers' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize</automated>
  </verify>
  <acceptance_criteria>
    - The fixed-anchor test passes (exit 0): module and mirror agree on all three accumulators after every write.
    - The docblock records the hand-checkable expected values: step 2 -> (1000,1000,1000); p12 == 60944740395587951995033807951; step 4 -> (1010,1012,1012) with shares=12; step 6 -> (1015,1014,1014); the 2^200 step accepted.
    - Grep-checkable: `grep -c 'assertEq(p12, mock.haircutRiskPrice' test/exposure/VegaAccount.e2e.t.sol` >= 1 (p_risk site sensitive); `grep -c '_depositExpectRevertBoth' test/exposure/VegaAccount.e2e.t.sol` >= 1 (dust revert proves sync); `grep -c '1 << 200' test/exposure/VegaAccount.e2e.t.sol` >= 1 (cross-product kill site).
    - The assertion is INSIDE the helpers: `grep -n '_assertAccumulatorsMatch' test/exposure/VegaAccount.e2e.t.sol` shows calls inside _setPriceBoth/_depositBoth/_depositExpectRevertBoth.
  </acceptance_criteria>
  <done>The fixed anchor sequence drives module vs mirror with the assertion inside the helpers, reproduces the Phase-12 shares=12 via a previewRiskPrice-derived price diffed vs the mock, and proves a mid-sequence dust revert leaves state synced — all green.</done>
</task>

<task type="auto">
  <name>Task 2: Constructed fuzz sequences (no vm.assume), mid-sequence re-pricing</name>
  <read_first>
    - test/market_state_measurements/RealizedVolatility.diff.t.sol — test__fuzz__randomPathVarianceFieldsMatch: the CONSTRUCTED corpus (bound + keccak-derived, repair-not-reject) with the assertion inside the driver. Mirror its corpus discipline.
    - test/exposure/VegaAccount.t.sol — test__fuzz__previewDepositEqualsDepositDelta: the `forge-config: default.fuzz.runs = 256` annotation and the bound()-only (never assume) convention.
  </read_first>
  <files>test/exposure/VegaAccount.e2e.t.sol</files>
  <action>
    Add test__fuzz__randomSequenceDiffers(uint256 seed, uint8 nRaw) to the SAME contract.

    Corpus is CONSTRUCTED (bound only, NO vm.assume; repair-not-reject), and every run is a LIVE
    assertion (no dust reverts in the fuzz path — keep it clean):
      uint256 n = bound(uint256(nRaw), 2, 8);          // small: FFI deploy is once per test, keep 256 runs tractable
      // /// forge-config: default.fuzz.runs = 256
      for i in 0..n:
        // derive a NEW price each iteration so later deposits price differently (catches stale-price caching)
        uint256 price = bound(uint256(keccak256(abi.encode(seed, i, "price"))), 1 << 96, 2 ** 120);
        _setPriceBoth(price);
        // deposit LARGE relative to price so shares >= 1 ALWAYS (no dust, no repair needed):
        //   deposit in [2^120, 2^160], price <= 2^120  => shares = deposit*2^96/price >= 2^96 >> 0
        uint256 deposit = bound(uint256(keccak256(abi.encode(seed, i, "amt"))), 2 ** 120, 2 ** 160);
        _depositBoth(deposit);                          // assertion INSIDE

    After the loop:
      assertTrue(acct.totalShares() != 0, "fuzz corpus non-vacuous: >=1 nonzero mint");
      // three accumulators were asserted INDEPENDENTLY (never a quotient) inside every _depositBoth,
      // so compensating errors cannot cancel.

    NOTE the overflow-safety of the bounds in an inline comment: with price <= 2^120 and deposit <= 2^160,
    issue_shares = mulDiv(deposit, 2^96, price) has result <= 2^160*2^96/2^96 = 2^160 < 2^256 (no revert),
    and the mirror totals over <=8 iterations stay < 2^164 < 2^256. Every run is live.
  </action>
  <verify>
    <automated>forge test --match-path 'test/exposure/VegaAccount.e2e.t.sol' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize</automated>
  </verify>
  <acceptance_criteria>
    - Both the fixed anchor and the fuzz pass (exit 0), 256 fuzz runs, zero counterexamples.
    - No vm.assume anywhere: `grep -c 'vm.assume' test/exposure/VegaAccount.e2e.t.sol` == 0.
    - Mid-sequence re-pricing present: the fuzz sets a new price each iteration (grep the "price" keccak key).
    - `grep -c 'forge-config: default.fuzz.runs' test/exposure/VegaAccount.e2e.t.sol` >= 1.
  </acceptance_criteria>
  <done>The constructed fuzz drives distinct (setRiskPrice, deposit) sequences with mid-sequence re-pricing, asserting all three accumulators independently at tolerance 0 after every write, 256 runs green, no vm.assume.</done>
</task>

</tasks>

<verification>
- `forge test --match-path 'test/exposure/VegaAccount.e2e.t.sol' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize` is green (anchor + fuzz).
- The assertion lives INSIDE the driver helpers (grep _assertAccumulatorsMatch inside _setPriceBoth/_depositBoth/_depositExpectRevertBoth).
- No vm.assume; corpus constructed via bound() only.
- The p_risk ceil site (previewRiskPrice vs mock.haircutRiskPrice), the shares floor site (Phase-12 anchor 12 + the 2.5->2 point), the dust revert, and the ~2^200 point are all present — so 15-02's five killable mutants each have a green to redden.
</verification>

<success_criteria>
VVER-01 satisfied: an end-to-end differential drives identical (setRiskPrice, deposit) sequences into
VegaAccountMod and the IssuanceRefMock-backed mirror, asserting all three accumulators equal at tolerance
0 after EVERY write, with the assertion inside the driver, in ONE test file with a non-fuzz unit anchor
alongside the fuzz. Corpus constructed, non-vacuous, three accumulators asserted independently.
</success_criteria>

<output>
After completion, create `.planning/phases/15-differential-verification-mutation-battery-plank-skip-exit/15-01-SUMMARY.md`.
Record the verbatim green tail of the e2e run and the hand-checked anchor totals.
</output>
