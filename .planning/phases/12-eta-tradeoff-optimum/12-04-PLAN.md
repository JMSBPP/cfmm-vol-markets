---
phase: 12-eta-tradeoff-optimum
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - model/vol_markets/LEAN_TRACEABILITY.md
  - model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md
  - ../plank/notes/VOLATILITY_INSTRUMENTS.md
  - ../plank/todo.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CTX-TRACE]

must_haves:
  truths:
    - "LEAN_TRACEABILITY gains §0 notation rows for every new symbol plus a binding paragraph recording the Capponi remaps and the η protection, mirroring the MMR collision paragraph"
    - "A new §7.2 carries one claim row per E-block, with statuses taken from the fidelity record and from nowhere else"
    - "Every backticked identifier in the NEW rows is grep-verified to be a real declaration — the check is scoped to the new section so it cannot false-fail on pre-existing rows"
    - "§6(b), the demand-elasticity gap, is AMENDED rather than deleted: it now has a partial carrier, and what remains OPEN is named"
    - "The master document is back-annotated with > LEAN lines per E-block, and the annotation's invalidation of the 12-01 sha pin is recorded rather than left to be discovered"
    - "The plank todo #227 answer is written as a short quotable controller law with its honest caveats — no on-chain proxy for ϱ_I is invented"
    - "If the factor-share half of the η identity came back absent, the record says the user's decision is PARTIALLY discharged and never that it is closed"
    - "ROADMAP and STATE record the phase outcome in the sanctioned status words, and no Lean file is touched"
  artifacts:
    - path: "model/vol_markets/LEAN_TRACEABILITY.md"
      provides: "§0 notation rows and the new §7.2 claim table for the ETA layer, plus the amended §6(b)"
      contains: "curvIndex"
    - path: "../plank/notes/VOLATILITY_INSTRUMENTS.md"
      provides: "The ## ETA section back-annotated with per-block > LEAN lines"
      contains: "> LEAN"
    - path: "../plank/todo.md"
      provides: "The todo #227 answer — the η controller law and what plank must estimate to use it"
      contains: "227"
  key_links:
    - from: "model/vol_markets/LEAN_TRACEABILITY.md"
      to: "lean/vol_markets/EtaCurvature.lean"
      via: "§7.2 claim rows naming real declarations"
      pattern: "etaStar"
    - from: "../plank/todo.md"
      to: "the η* controller law"
      via: "the todo #227 answer paragraph"
      pattern: "227"
---

<objective>
Close the cycle: make the ETA layer auditable by a future consumer who cannot re-derive it, and
answer the question plank actually asked.

This plan moves no mathematics. Its only real risk is softening — writing the record in a way that
reads better than the result. Phase 11 closed with two refutations and said so in the sanctioned
status words rather than dressing either up, and that discipline is why its record is usable. The
same standard applies here, with one specific exposure: the user issued a binding decision that
`priceEta`'s η and `eta.md`'s η are the same parameter, and this phase may have discharged only half
of it. If the factor-share identification came back absent, the record must say **PARTIALLY
discharged**, not "the bridge is proven".

Purpose: an auditable record and a usable answer.
Output: traceability rows, the back-annotated master doc, the todo #227 answer, and the close-out.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-eta-tradeoff-optimum/12-03-SUMMARY.md
@.planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md
@model/vol_markets/LEAN_TRACEABILITY.md
@model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md
@lean/vol_markets/EtaCurvature.lean

<constants>
STATUS WORDS COME FROM THE FIDELITY RECORD AND NOWHERE ELSE:
  PROVEN / CORRECTED then PROVEN / REFUTED / OPEN / OMITTED (OPTIONAL) / NOT IDENTIFIED.
  A row's text states the RESULT, not the aspiration. An executor argument that is not
  machine-checked is LABELLED as not machine-checked and is merged into no claim.

THE §0 ADDITIONS — new symbol rows plus ONE binding paragraph, mirroring the MMR collision
  paragraph that §0 already carries:
    chi / curvIndex        the discrete curvature index (Capponi's k)
    varrho_I / premInv     investor private-use premium — NOT a probability, may exceed 1
    varrho_S / premShock   price-shock magnitude — NOT a probability
    varpi_A, _I, _H, _D    the four absorbed equilibrium constants
    chi* / kphiStar         the interior optimum in the curvature index
    eta* / etaStar         THE DELIVERABLE — ln((1+varrho_I)/(1+phi))/(Delta_i^2 * ln lam)
  The binding paragraph records: Capponi's `k`, `alpha`, `beta` are REMAPPED and his `theta`,
  `kappa_*` are ABSORBED (never named), because `theta` collides with the doc's option theta and
  `kappa` with the Phase-11 scalarization weight; `f` is IDENTIFIED with `phi` rather than renamed;
  **`eta` is PROTECTED end-to-end and was the REQUIRED symbol of this phase's notation gate, which
  INVERTS the Phase-11 gate's Rule 1**; and `nu` was never introduced because M6b binds it.
  It must ALSO record the standing distinction, in the same voice §0 uses for `lambda_ARB` vs
  `lambda_MEV`: **Capponi's `arbLossRatio` and `MevOptimization.mevMulti` are NOT IDENTIFIED** —
  different models (two-period discrete-shock vs MMR fast-block diffusion), different units (a
  per-period ratio of pool value vs a discrete hazard sum over `D_t`).

THE §6(b) AMENDMENT — amend, do not delete (the 11-06 discipline). §6(b) currently records the
  missing "demand-elasticity / optimal-fee equilibrium layer". Capponi's `varrho_I` IS that demand
  parameter, so the gap now has a PARTIAL carrier. The amendment states what is carried (a
  demand-side valuation premium entering the curvature optimum in closed form) and what remains
  OPEN (the equilibrium transfer — that our tick-grid AMM has these closed forms is an assumption;
  and MMR §7.3 eq. (27)'s optimal-fee layer, which this phase does not touch).

THE SHA-PIN INVALIDATION — disclose it, do not let it be discovered:
  Back-annotating the `## ETA` section with `> LEAN` lines CHANGES the bytes and therefore
  INVALIDATES `APPROVED-ETA-SHA256` in 12-01-REVIEW.md and `BUNDLED-ETA-SHA256` in
  12-02-RUN-RECORD.md. This is acceptable ONLY because both gates have already been consumed and
  passed — 12-02 Task 1 and 12-02 Task 3 both compared the bundled copy to the approved bytes, and
  no downstream check reads either hash again. Record this in the addendum header, exactly as 11-06
  recorded its own intentional pin invalidation, so a later reader does not treat the mismatch as
  drift.

THE TODO #227 ANSWER — deliver the LAW, do not invent a PROXY:
  The controller law is `eta* = ln((1+varrho_I)/(1+phi)) / (Delta_i^2 * ln lam)`, with:
  `eta* > 0` iff the investor's private-use premium exceeds the fee; strictly increasing in that
  premium; **strictly decreasing in the fee**, so the Phase-11 fee corner LOWERS the optimal
  curvature; strictly decreasing in `Delta_i^2`.
  Three caveats that must travel with it:
   (a) `varrho_I` is UNOBSERVABLE — Capponi's private-use premium. **Do not invent an on-chain
       proxy.** Naming "estimating varrho_I" as the follow-up is the correct deliverable; inventing
       a proxy would repeat the upsilon econometric null-result failure mode, which cost this
       project two phases.
   (b) The plank todo's phrasing "substitution elasticity" is LOOSE and should be tightened rather
       than propagated: for a weighted-geometric CFMM the elasticity of substitution is 1 and `eta`
       is the FACTOR SHARE. Say so.
   (c) The equilibrium transfer is an ASSUMPTION: Capponi's closed forms are proven for
       `lpExcess . curvIndex`; that `lpExcess` describes our tick-grid AMM is the modelling
       hypothesis and is labelled OPEN.
  Also record the hook-location consequence honestly: this phase produces the economic controller
  answer; mapping it to `beforeSwap`/`afterSwap` is plank's work and is not attempted here.

OWNERSHIP: `../plank/notes/VOLATILITY_INSTRUMENTS.md` and `../plank/todo.md` belong to agent
  `ul2inqpl` — EDIT, do NOT commit there; verify the plank HEAD sha before and after.
  `lean/exp/` and `model/exp/` belong to another workstream and are READ-ONLY.
  NO `.lean` FILE MAY BE TOUCHED by this plan: `git status --porcelain lean/` must be empty
  throughout.

TWO KNOWN MECHANICAL-CRITERION DEFECTS FROM 11-06, FIXED HERE RATHER THAN REPEATED:
  1. Its identifier-existence loop scanned the WHOLE traceability file and therefore flagged a
     pre-existing identifier from an unrelated section. **Scope the loop to the NEW §7.2 rows.**
  2. Its `! grep -rE '/home/'` criterion false-failed on ROADMAP rows that legitimately QUOTE a
     home-path scrub command. **Scope the path check to the NEW rows**, not the whole file.
</constants>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LEAN_TRACEABILITY — §0 notation rows, the binding paragraph, the new §7.2, and the §6(b) amendment</name>

  <read_first>
    - .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md IN FULL — the T1'–T31' verdicts, the added-hypotheses record and the NOT ESTABLISHED section. **Every status word in every new row comes from this file.** In particular: whether T24' returned as an equality, whether T27' was constructed, whether T28'b was delivered or omitted, and whether T18'/T18'b landed
    - lean/vol_markets/EtaCurvature.lean — the ACTUAL declaration names, for the identifier-existence check. Use the landed names, not the requested ones, wherever the prover renamed something
    - model/vol_markets/LEAN_TRACEABILITY.md — §0 (the notation dictionary, the η reservation at line 16, and the MMR collision paragraph whose SHAPE the new paragraph mirrors), §6 item (b) (the gap to amend), and §7.1 (the MEV claim-row format to reproduce)
    - model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md — the approved E0–E8 blocks, one claim row per block
    - .planning/phases/11-mev-hazard-inf-program/11-06-SUMMARY.md — the close-out standard: what was recorded, how the two refutations were worded, and the two mechanical-criterion defects this plan fixes rather than repeats
  </read_first>

  <files>model/vol_markets/LEAN_TRACEABILITY.md</files>

  <action>
1. **§0 — add the symbol rows** listed in `<constants>`, in the existing table format
   (`| symbol | Lean identifier | meaning | module |`). Each names `EtaCurvature` as its module.
   State in the `ϱ_I`/`ϱ_S` rows, in the row text itself, that they are **premia, not
   probabilities** — the single most consequential misreading available here (`12-CONTEXT.md`
   contains it), because under it `κ_φ* = 1 − √((1+φ)/(1+ϱ_I))` is uninterpretable and the demand-side
   link to §6(b) is lost.

2. **§0 — add the binding paragraph** described in `<constants>`, mirroring the MMR collision
   paragraph. It records the remaps, the absorptions and their reasons, the `f ≡ φ` identification,
   the η protection and the gate inversion, the ν avoidance, and the `arbLossRatio` / `mevMulti`
   NON-IDENTIFICATION.
   Add one sentence on the **λ overload**: in the ETA layer an unsubscripted `λ` inside an
   exponential is the tick base `PosSpec.lam = 1.0001`, while in `### MEV` every `λ` is a
   subscripted hazard. Both usages are legitimate and neither was renamed.

3. **§0 — record the η-identity outcome precisely.** Extend the existing `η` reservation with the
   result: the exponent identity `priceEta η Δi i = CFMM.Eta.p_eta lam Δi (η/2) i` is PROVEN (naming
   the landed theorem), the factor 2 being the sqrt-price normalization; and the factor-share
   identification with `CFMM.Eta.L_eta` is **either PROVEN (naming the theorem) or OPEN**, taken
   from the fidelity record. **If it is OPEN, write that the user's 2026-07-31 decision is
   PARTIALLY discharged** — exponent identity proven, factor-share identification open — and cite
   `exp/eta.lean`'s `P_half` docstring ("η does not enter the tick→price map") as the reason the
   second half is a modelling claim rather than a rewriting. Do not write it as closed.

4. **Add `### 7.2 `## ETA` — the curvature controller and the interior η*`** immediately after
   §7.1, same table shape (`| Claim | Lean carrier | Status |`), one row per E-block E0–E8, plus
   rows for the headline objects. Statuses from the fidelity record only. Rows that must exist and
   must be worded carefully:
   - **E1 / the curvature index** — `curvIndex`, `priceEta_step_ratio`, `curvIndex_strictMono`.
     The row must state that tick-independence is the content, and must carry the caveat that
     `η = 1` is the sqrt-price grid and **NOT** Capponi's `k = 1`.
   - **E2, E3 / Lemma 3** — the two strict antitonicities, with the verdict on STRICTNESS explicit
     (`StrictAntiOn` delivered, or narrowed and named as narrowed).
   - **E4 / Proposition 5** — the interior optimum, with `0 < c₁` named as the standing hypothesis
     and the `c₁ ≤ 0` freeze case recorded per the fidelity verdict (REFUTED if T18'b landed, OPEN
     otherwise). The row must carry the words **branch point / kink** on their own line and state
     that there is **no first-order condition** — the single most likely thing for a downstream
     reader to assume.
   - **E6 / the bridge** — `etaStar`, `curvIndex_etaStar`, and the three comparative statics. The
     row must state the CLOSED FORM explicitly and must record that `η*` is obtained by INVERTING a
     bijection, which is what distinguishes it from `exp/DynamicsOptimization.optimal_controls`'s
     hypothesized interior optimum. Name that module in the row so nobody downstream reads this as
     a duplicate.
   - **E7 / de-degeneration** — `eta_no_common_argmax`, `joint_corner_and_interior`,
     `etaStar_coupled_to_fee_corner`, each with the Phase-11 theorem it contrasts against, named.
   - **E8 / caveats** — one row per OPEN item, each `OPEN`, including the equilibrium transfer,
     welfare, the `arbLossRatio` non-identification, gas, and Phase 11's inherited `Θ_φ`-restricted
     σ-varying case (which this phase does NOT touch and must not appear to).

5. **VERIFY EVERY IDENTIFIER IN THE NEW ROWS**, scoped to §7.2 so the check cannot false-fail on
   pre-existing rows elsewhere (11-06's defect):
   ```bash
   [ -n "$(awk '/^### 7\.2/{f=1} f&&/^## /{f=0} f' model/vol_markets/LEAN_TRACEABILITY.md \
     | grep -oE '`[A-Za-z_][A-Za-z0-9_.]*`')" ] || { echo "VACUOUS: no identifiers found"; exit 1; }
   awk '/^### 7\.2/{f=1} f&&/^## /{f=0} f' model/vol_markets/LEAN_TRACEABILITY.md \
     | grep -oE '`[A-Za-z_][A-Za-z0-9_.]*`' | tr -d '`' | sed 's/.*\.//' | sort -u \
     | while read -r n; do
         grep -qrE "^(theorem|lemma|noncomputable def|def) $n" lean/vol_markets/ lean/exp/ \
           || echo "UNRESOLVED: $n"
       done
   ```
   The outer grep MUST be nonempty first (an empty identifier list is a vacuous pass and FAILS),
   then the loop must print nothing. **A row naming a non-existent lemma is worse than no row** — that is why
   this check exists. Names that are prose rather than identifiers must not be backticked.

6. **Amend §6(b)** as described in `<constants>` — amend, do not delete. State what is now carried
   and what remains OPEN.

7. **Scoped path check on the new content only** (11-06's other defect — the whole-file version
   false-fails on rows that legitimately quote a home path):
   ```bash
   awk '/^### 7\.2/{f=1} f&&/^## /{f=0} f' model/vol_markets/LEAN_TRACEABILITY.md \
     | grep -nE '/home/|\$HOME' && exit 1 || true
   ```

8. Touch NO `.lean` file and nothing under `../plank/` in this task.
  </action>

  <verify>
    <automated>grep -qF 'curvIndex' model/vol_markets/LEAN_TRACEABILITY.md && grep -qF 'etaStar' model/vol_markets/LEAN_TRACEABILITY.md && grep -qE '^### 7\.2' model/vol_markets/LEAN_TRACEABILITY.md && [ -n "$(awk '/^### 7\.2/{f=1} f&&/^## /{f=0} f' model/vol_markets/LEAN_TRACEABILITY.md | grep -oE '`[A-Za-z_][A-Za-z0-9_.]*`')" ] && [ -z "$(awk '/^### 7\.2/{f=1} f&&/^## /{f=0} f' model/vol_markets/LEAN_TRACEABILITY.md | grep -oE '`[A-Za-z_][A-Za-z0-9_.]*`' | tr -d '`' | sed 's/.*\.//' | sort -u | while read -r n; do grep -qrE "^(theorem|lemma|noncomputable def|def) $n" lean/vol_markets/ lean/exp/ || echo "UNRESOLVED: $n"; done)" ] && [ "$(git status --porcelain lean/ | wc -l)" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qE '^### 7\.2' model/vol_markets/LEAN_TRACEABILITY.md`
    - The new §0 symbol rows exist: `for s in curvIndex premInv premShock kphiStar etaStar; do grep -qF "$s" model/vol_markets/LEAN_TRACEABILITY.md || exit 1; done`
    - `ϱ_I` is declared not-a-probability in its own row: `grep -qiE 'not a probability|premium, not a probability' model/vol_markets/LEAN_TRACEABILITY.md`
    - The binding paragraph records the remaps and the gate inversion: `grep -qF '2103.08842' model/vol_markets/LEAN_TRACEABILITY.md` and `grep -qiE 'absorbed|never named' model/vol_markets/LEAN_TRACEABILITY.md` and `grep -qi 'invert' model/vol_markets/LEAN_TRACEABILITY.md`
    - The non-identification is recorded: `grep -qi 'NOT IDENTIFIED' model/vol_markets/LEAN_TRACEABILITY.md`
    - The η-identity outcome is recorded at the right strength: `grep -qF 'p_eta' model/vol_markets/LEAN_TRACEABILITY.md` AND either the factor-share row reads `PROVEN` naming a real theorem, or `grep -qi 'PARTIALLY discharged' model/vol_markets/LEAN_TRACEABILITY.md`
    - The kink is recorded: `grep -qiE 'kink|branch point' model/vol_markets/LEAN_TRACEABILITY.md`
    - No FOC is claimed in the new section: `awk '/^### 7\.2/{f=1} f&&/^## /{f=0} f' model/vol_markets/LEAN_TRACEABILITY.md | grep -iE 'first-order condition' | grep -qvE -i 'no |not a |superseded|hypothesiz' && exit 1 || true`
    - `exp/DynamicsOptimization` is named so the headline is not read as a duplicate: `grep -qF 'DynamicsOptimization' model/vol_markets/LEAN_TRACEABILITY.md`
    - **IDENTIFIER EXISTENCE, scoped to §7.2:** the step-5 loop prints nothing
    - §6(b) was amended, not deleted: `grep -qi 'demand-elasticity' model/vol_markets/LEAN_TRACEABILITY.md` and the §6 region also matches `grep -qiE 'partial carrier|now has a carrier|partly closed'`
    - The OPEN items are present in §7.2: `[ "$(awk '/^### 7\.2/{f=1} f&&/^## /{f=0} f' model/vol_markets/LEAN_TRACEABILITY.md | grep -c 'OPEN')" -ge 4 ]`
    - Phase 11's §7.1 rows were not deleted: `git diff model/vol_markets/LEAN_TRACEABILITY.md | grep '^-' | grep -qE 'M6a|M6b|lambda_ARB' && exit 1 || true`
    - **SCOPED path check:** the step-7 command exits 0
    - `[ "$(git status --porcelain lean/ | wc -l)" = "0" ]` and `git status --porcelain` shows nothing under `../plank/`
  </acceptance_criteria>

  <done>LEAN_TRACEABILITY carries §0 rows for every new symbol with the premia explicitly marked as non-probabilities, a binding paragraph recording the remaps, the absorptions, the gate inversion, the λ overload and the arbLossRatio/mevMulti non-identification, an η-identity entry stated at exactly the strength the fidelity record supports, a new §7.2 with one status row per E-block naming the kink and citing DynamicsOptimization so the headline is not read as a duplicate, an amended §6(b), and every backticked identifier in the new rows verified to exist by a check scoped to the new section.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Back-annotate the master document, answer the η note at ../plank/todo.md line 227, notify the owner</name>

  <read_first>
    - .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md — the per-item verdicts that the `> LEAN` annotations state
    - model/vol_markets/VOLATILITY_INSTRUMENTS_LEAN_ADDENDUM.md — the `> LEAN` annotation STYLE precedent; and model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md for how the M-blocks were annotated at 11-06
    - ../plank/notes/VOLATILITY_INSTRUMENTS.md — the `## ETA` section as inserted at 12-01, and the M-block annotations already present (the format to match)
    - ../plank/todo.md — the η note at LINE 227 (a line number, not an item number — the η controller application question this phase answers), and the `## LEAN4 - MATH` handoff section
    - .planning/phases/12-eta-tradeoff-optimum/12-CONTEXT.md — the plank-consumer clause: this phase produces the ECONOMIC CONTROLLER ANSWER; plank maps it to implementation
  </read_first>

  <files>../plank/notes/VOLATILITY_INSTRUMENTS.md, ../plank/todo.md, model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md</files>

  <action>
0. Record the plank HEAD first:
   `git -C ../plank rev-parse HEAD > /tmp/plank-head-before-1204.txt`
   and the current M-block hash, so step 6 can prove the Phase-11 pins survived:
   `awk '/\*\*M0\./{f=1} f&&/^## \*\*M9\./{f=0} f{print}' ../plank/notes/VOLATILITY_INSTRUMENTS.md | sha256sum > /tmp/mblocks-before-1204.txt`
   (M0 → end of M8 ONLY — stop BEFORE `## **M9.`, never M0→EOF: placement-independent and immune
   to the parallel JIT/TAU edits below M8.)

1. **Back-annotate the `## ETA` section** in `../plank/notes/VOLATILITY_INSTRUMENTS.md`: append a
   `> LEAN` line to each of E1–E8 naming the carrier declarations and the status, in the style the
   M-blocks already use. Where a block's content came back narrowed, absent or refuted, the
   annotation says so — these annotations are read by people who will not open the fidelity record.
   Apply the identical annotations to this tree's
   `model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` so the two copies do not drift.

2. **Record the sha-pin invalidation** in the addendum header, in the same paragraph as the
   APPROVED & APPLIED status: the back-annotation changes the `## ETA` bytes and therefore
   invalidates `APPROVED-ETA-SHA256` (12-01-REVIEW.md) and `BUNDLED-ETA-SHA256`
   (12-02-RUN-RECORD.md); this is acceptable because **both gates were already consumed and both
   passed**, and no downstream check reads either hash again. Say it plainly so a later reader does
   not diagnose drift.

3. **Answer the η note at ../plank/todo.md line 227** (a line number, not an item number). Append
   to `../plank/todo.md` under `## LEAN4 - MATH` a short, quotable block containing:
   - the controller law `η* = ln((1+ϱ_I)/(1+φ)) / (Δi² · ln λ)`, with `λ = 1.0001` the tick base;
   - the three comparative statics, with the fee one called out: **raising the fee LOWERS the
     optimal curvature**, so the Phase-11 fee corner and the curvature choice are COUPLED;
   - the carrier: `EtaCurvature.etaStar` / `EtaCurvature.curvIndex_etaStar` in
     `lean/vol_markets/EtaCurvature.lean`, machine-checked and axiom-clean;
   - **caveat (a): `ϱ_I` is UNOBSERVABLE** — Capponi's investor private-use premium. The named
     follow-up for plank is ESTIMATING it. **No on-chain proxy is proposed here, deliberately** —
     inventing one would repeat the υ econometric null-result failure mode;
   - **caveat (b): the todo's "substitution elasticity" phrasing is loose** — for a
     weighted-geometric CFMM the elasticity of substitution is 1 and `η` is the FACTOR SHARE;
   - **caveat (c): the equilibrium transfer is an ASSUMPTION** — the results are proven for
     `lpExcess ∘ curvIndex`; that `lpExcess` describes our tick-grid AMM is the modelling hypothesis
     and is labelled OPEN;
   - the scope line: this phase produced the economic controller answer; the
     `beforeSwap`/`afterSwap` mapping is plank's work and is not attempted here.

4. **Notify `ul2inqpl`** via `claude-peers send_message`: the ETA section is back-annotated and
   the line-227 η-note answer is appended, both UNCOMMITTED on their side; the 12-01 sha pin is
   intentionally invalidated and why; and the M-block bytes were not touched.

5. **Verify the plank worktree was not committed to:**
   `[ "$(git -C ../plank rev-parse HEAD)" = "$(cat /tmp/plank-head-before-1204.txt)" ]`

6. **Prove the M-blocks are still byte-identical** — this plan must not disturb the Phase-11 pins:
   ```bash
   awk '/\*\*M0\./{f=1} f&&/^## \*\*M9\./{f=0} f{print}' ../plank/notes/VOLATILITY_INSTRUMENTS.md | sha256sum
   ```
   (the same M0 → end-of-M8 scope, stopping BEFORE `## **M9.`) must equal
   `/tmp/mblocks-before-1204.txt`. Record the comparison.

7. Touch NO `.lean` file, and nothing under `model/exp/` or `lean/exp/`.
  </action>

  <verify>
    <automated>grep -q '> LEAN' ../plank/notes/VOLATILITY_INSTRUMENTS.md && grep -qF '227' ../plank/todo.md && grep -qF 'etaStar' ../plank/todo.md && grep -qi 'APPROVED-ETA-SHA256' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md && [ "$(git -C ../plank rev-parse HEAD)" = "$(cat /tmp/plank-head-before-1204.txt)" ] && [ "$(git status --porcelain lean/ model/exp/ | wc -l)" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
    - The ETA section is annotated: `awk '/\*\*E0\./{f=1} f{print} /<!-- END ETA -->/{f=0}' ../plank/notes/VOLATILITY_INSTRUMENTS.md | grep -c '> LEAN'` is at least 6 (the END-marker-delimited extraction is placement-independent — the section header may be `## FLAIR & MEV` or `## ETA` per the 12-01 decision)
    - The two copies agree: the `> LEAN` line count in the addendum's E-blocks equals the count in the plank `## ETA` section
    - **THE M-BLOCKS SURVIVED:** the step-6 hash equals `/tmp/mblocks-before-1204.txt`, recorded in the plan summary
    - The pin invalidation is disclosed: `grep -qi 'APPROVED-ETA-SHA256' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` and that line's context matches `grep -qiE 'invalidat|superseded|already consumed'`
    - The todo #227 answer exists with the law and its carrier: `grep -qF '227' ../plank/todo.md` and `grep -qF 'etaStar' ../plank/todo.md` and `grep -qF 'EtaCurvature' ../plank/todo.md`
    - The unobservability caveat is present and NO proxy was invented: `grep -qiE 'unobservable|cannot be observed' ../plank/todo.md` and `! grep -qiE 'proxy for .*premInv|proxy for .*varrho|use realized taker surplus as' ../plank/todo.md`
    - The loose-phrasing correction is present: `grep -qi 'factor share' ../plank/todo.md`
    - The scope line is present: `grep -qiE 'beforeSwap|afterSwap' ../plank/todo.md`
    - This session did NOT commit in the plank worktree: `[ "$(git -C ../plank rev-parse HEAD)" = "$(cat /tmp/plank-head-before-1204.txt)" ]`
    - `[ "$(git status --porcelain lean/ model/exp/ | wc -l)" = "0" ]`
  </acceptance_criteria>

  <done>The `## ETA` section and this tree's addendum carry identical per-block `> LEAN` annotations stating the landed carriers and their real statuses, the intentional sha-pin invalidation is disclosed in the addendum header with the reason it is safe, the Phase-11 M-block bytes are proven unchanged, plank's todo #227 has a quotable controller law with its unobservability, factor-share and equilibrium-transfer caveats and no invented proxy, the owner is notified, and the plank HEAD is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: ROADMAP and STATE close-out, memory, commit</name>

  <read_first>
    - .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md and the 12-01/12-02/12-03 SUMMARY files — the outcomes to record, in their own status words
    - .planning/ROADMAP.md Phase 12 entry — the `**Requirements**` line and the plan list to close out
    - .planning/STATE.md — the format of the Phase-11 close-out entries, including how NOT ESTABLISHED and OPEN items were recorded
    - .planning/phases/12-eta-tradeoff-optimum/12-CONTINGENCY.md — whether it was invoked, which must be recorded either way
  </read_first>

  <files>.planning/ROADMAP.md, .planning/STATE.md</files>

  <action>
1. **ROADMAP Phase 12** — update the entry:
   - mark each of the four plans complete with a one-line per-plan outcome;
   - correct the Goal clause IN PLACE where this phase's results differ from the brief, without
     deleting the original intent — the brief says "first-order characterization where provable",
     and the result is that **there is no first-order condition: `κ_φ*` is a kink and `η*` comes from
     inverting a bijection in closed form**. Record that as a planning correction, in the same voice
     the Phase-11 entry uses;
   - confirm the `**Requirements**` line carries the seven CTX-* ids;
   - record whether `12-CONTINGENCY.md` was invoked and, if so, its outcome.

2. **STATE.md** — write the Phase 12 close-out in the established voice:
   - the headline: the closed form `η* = ln((1+ϱ_I)/(1+φ))/(Δi²·ln λ)`, the FIRST interior optimum
     in this program where everything in `Θ_φ` was a corner or a saturation limit — **with the
     honest qualification that `exp/DynamicsOptimization` already carries an interior-η claim in a
     different model, and that what is new here is CONSTRUCTION (existence and closed form) versus
     that module's HYPOTHESIZED maximizer and first-order characterization**;
   - the de-degeneration and the coupling (the fee corner lowers optimal curvature);
   - the η-identity outcome at exactly the strength the fidelity record supports — **PARTIALLY
     discharged if the factor-share half is OPEN**;
   - a NOT ESTABLISHED paragraph naming every OPEN item, the equilibrium transfer first;
   - any added hypotheses the prover supplied and what they mean;
   - the deviations, recorded rather than papered over, in the manner every prior plan's summary
     did.
   Update the `progress` frontmatter counters.

3. **Memory** — update the phase entry: the landed module, the controller law, the OPEN items, the
   η-identity partial discharge if applicable, and the fact that the queue is free.

4. **Commit this tree only:**
   ```bash
   git add model/vol_markets/ .planning/
   git commit -m "docs(12-04): ETA traceability, doc back-annotation, todo #227 answer, phase close-out"
   ```
   Do NOT commit in `../plank/`.

5. Final checks: `git status --porcelain lean/` empty; `cd lean && lake build` still exits 0 (this
   plan changed no Lean file, so a failure here means something else moved and must be reported).
  </action>

  <verify>
    <automated>grep -q 'CTX-CURVDOC' .planning/ROADMAP.md && grep -q 'CTX-TRACE' .planning/ROADMAP.md && grep -qF '12-04' .planning/ROADMAP.md && grep -qF 'etaStar' .planning/STATE.md && git log --oneline -2 --name-only | grep -q 'LEAN_TRACEABILITY.md' && [ "$(git status --porcelain lean/ | wc -l)" = "0" ] && (cd lean && lake build)</automated>
  </verify>

  <acceptance_criteria>
    - The ROADMAP Phase 12 requirements line carries all seven ids: `for r in CTX-CURVDOC CTX-CAPTRANS CTX-INTERIOR CTX-ETABRIDGE CTX-DEGEN CTX-REVIEW CTX-TRACE; do grep -q "$r" .planning/ROADMAP.md || exit 1; done`
    - The plan list is closed out: `for p in 12-01 12-02 12-03 12-04; do grep -qF "$p" .planning/ROADMAP.md || exit 1; done` and no `TBD` remains in the Phase 12 entry
    - The no-FOC planning correction is recorded: `grep -qiE 'kink|no first-order' .planning/ROADMAP.md`
    - STATE records the headline and the honest qualification: `grep -qF 'etaStar' .planning/STATE.md` and `grep -qF 'DynamicsOptimization' .planning/STATE.md`
    - STATE has a NOT ESTABLISHED paragraph: `grep -qi 'NOT ESTABLISHED' .planning/STATE.md`
    - The η-identity strength matches the fidelity record: if `12-03-FIDELITY.md` records T28'b absent, then `grep -qi 'PARTIALLY discharged' .planning/STATE.md`
    - The contingency disposition is recorded either way: `grep -qi 'contingency' .planning/ROADMAP.md` or `grep -qi 'contingency' .planning/STATE.md`
    - Committed: `git log --oneline -2 --name-only | grep -q 'LEAN_TRACEABILITY.md'` and `git log --oneline -2 --name-only | grep -q 'STATE.md'`
    - Nothing committed in the plank worktree: `[ "$(git -C ../plank rev-parse HEAD)" = "$(cat /tmp/plank-head-before-1204.txt)" ]`
    - `[ "$(git status --porcelain lean/ | wc -l)" = "0" ]` and `cd lean && lake build` exits 0
  </acceptance_criteria>

  <done>ROADMAP's Phase 12 entry carries the seven requirement ids, four closed-out plans with per-plan outcomes, and the no-first-order-condition planning correction recorded in place; STATE carries the close-out in the established voice with the headline properly qualified against the pre-existing `exp/` interior-η claim, a NOT ESTABLISHED paragraph, and the η-identity recorded at exactly the strength the fidelity record supports; memory is updated and only this tree was committed.</done>
</task>

</tasks>

<verification>
- `LEAN_TRACEABILITY.md` §0 has rows for `curvIndex`, `premInv`, `premShock`, `kphiStar`, `etaStar`
  and the `ϖ_*` constants, plus the binding remap/absorption paragraph and the non-identification.
- §7.2 exists with one status row per E-block, every backticked identifier verified to exist by a
  check scoped to that section.
- §6(b) is amended, not deleted, and §7.1's Phase-11 rows are untouched.
- The `## ETA` section and the addendum carry identical `> LEAN` annotations; the M-block bytes are
  proven unchanged; the intentional sha-pin invalidation is disclosed.
- `../plank/todo.md` carries the #227 answer with the controller law, its carrier, and the
  unobservability / factor-share / equilibrium-transfer caveats — and no invented proxy.
- The plank HEAD sha is unchanged by this plan.
- `git status --porcelain lean/` is empty throughout and `lake build` is still green.
- ROADMAP and STATE record the outcome in the sanctioned status words, including the η-identity's
  partial discharge if that is what the fidelity record says.
</verification>

<success_criteria>
The ETA layer is auditable without re-derivation: every claim has a status traceable to the fidelity
record, every named identifier exists, every OPEN item is labelled OPEN, the demand-elasticity gap is
amended to show a partial carrier and a named remainder, plank has a quotable controller law with the
honest caveats that make it usable, and nothing in the record reads stronger than what was proved.
</success_criteria>

<output>
After completion, create `.planning/phases/12-eta-tradeoff-optimum/12-04-SUMMARY.md`.
</output>
