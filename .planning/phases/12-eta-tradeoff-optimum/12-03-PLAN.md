---
phase: 12-eta-tradeoff-optimum
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - lean/vol_markets/EtaCurvature.lean
  - lean/lakefile.toml
  - .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md
autonomous: true
requirements: [CTX-CAPTRANS, CTX-INTERIOR, CTX-ETABRIDGE, CTX-DEGEN]

must_haves:
  truths:
    - "All SEVENTEEN bundled modules returned byte-identical, checked BEFORE anything is integrated"
    - "The returned module is integrated with the import rewrite as the ONLY edit, and the rewrite is driven by the module map because it is NOT uniform"
    - "The module is registered as a lakefile root by APPENDING, and the build log's own 'Built vol_markets.EtaCurvature' line is the evidence it was actually elaborated"
    - "lake build exits 0 for both vol_markets and the full project"
    - "Every declaration is axiom-clean, and the sweep file is GENERATED by grep from the module so it cannot silently omit a declaration"
    - "Every T1'-T31' item has an explicit disposition — delivered, delivered-with-added-hypotheses, narrowed, or absent — diffed against the sha-pinned prompt"
    - "The anti-narrowing watch list is checked item by item: the four strict monotonicities, the whole-interval maximum, the headline equality, the constructed maximizer, the de-degeneration conjunction"
    - "An absent OPTIONAL item is recorded as OPEN and is never written up as a success"
    - "Both remotes carry the landed module, and the mirror push was verified a fast-forward before pushing"
  artifacts:
    - path: "lean/vol_markets/EtaCurvature.lean"
      provides: "The proven curvature layer, interior optimum, η bridge and de-degeneration — the 15th vol_markets root"
      contains: "etaStar"
      min_lines: 300
    - path: ".planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md"
      provides: "The T1'-T31' disposition table, the byte-identity evidence, the axiom sweep result and the added-hypothesis record"
      contains: "curvIndex_etaStar"
    - path: "lean/lakefile.toml"
      provides: "The vol_markets root registration that makes the green build non-vacuous"
      contains: "vol_markets.EtaCurvature"
  key_links:
    - from: "lean/vol_markets/EtaCurvature.lean"
      to: "VolInstrument.priceEta"
      via: "the step-ratio theorem the curvature index is built from"
      pattern: "priceEta"
    - from: "lean/vol_markets/EtaCurvature.lean"
      to: "MevJointProgram.joint_corner_degeneracy"
      via: "the de-degeneration docstring naming the Phase-11 contrast"
      pattern: "joint_corner_degeneracy"
    - from: "lean/lakefile.toml"
      to: "lean/vol_markets/EtaCurvature.lean"
      via: "the appended root, without which the green build is vacuous"
      pattern: "vol_markets.EtaCurvature"
---

<objective>
Land the returned module: prove the bundle came back unmodified, integrate `EtaCurvature.lean` with
the import rewrite as the only edit, register it, build green, sweep the axioms, diff every
T1'–T31' statement against the sha-pinned prompt, and push to both remotes.

**The T1'–T31' diff IS the deliverable of this plan.** The mathematics was proved by the prover; what
this plan produces is the auditable record of exactly what came back and exactly what did not — and
its only real risk is softening. Phase 11 landed one bundle whose T15 was FALSE as specified and
another whose T24 came back as a machine-checked REFUTATION; both were recorded as such and both
records are more valuable than the theorems around them.

The specific integration trap here is new: **the import rewrite is NOT uniform.** This bundle
carries two source libraries, so `import RequestProject.eta` must become `import exp.eta` while
`import RequestProject.VolInstrument` must become `import vol_markets.VolInstrument`. A blanket
`s/RequestProject./vol_markets./` — the sed every prior plan used, correctly, for a single-library
bundle — produces `import vol_markets.eta`, which does not exist.

Purpose: land the proofs and the honest record of them.
Output: the registered, green, axiom-clean module on both remotes, plus the fidelity record.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-eta-tradeoff-optimum/12-02-SUMMARY.md
@.planning/phases/12-eta-tradeoff-optimum/12-02-RUN-RECORD.md
@.planning/phases/12-eta-tradeoff-optimum/12-RESEARCH.md
@model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md

<constants>
PROJECT/TASK  : read the ids from 12-02-RUN-RECORD.md. Poll with `aristotle tasks`, NEVER
                `aristotle show` (it streams and blocks — 11-02 hung a two-minute call).
DOWNLOAD      : `aristotle download --destination <path>` writes an ARCHIVE FILE, not a directory,
                and extracts one directory deeper than expected (11-05). Rename to `.tar.gz` and
                extract under `scratch/`, following the existing convention
                (`scratch/aristotle-*-result.tar.gz`).

THE NON-UNIFORM IMPORT REWRITE — the one thing in this plan that will silently break the build:
  Drive it from `.planning/phases/12-eta-tradeoff-optimum/12-02-MODULE-MAP.txt`, which pairs every
  bundled module with its origin library:
      eta exp
      CESLongVolPayoff exp
      EtaReplication exp
      VolInstrument vol_markets
      TauMevAlgebra vol_markets
      ... (17 lines total)
  For each returned `import RequestProject.X`, look X up in the map and emit
  `import <origin>.X`. Do NOT use a blanket sed.
  Namespaces need NO rewriting: they are independent of module paths (`CFMM.Eta` for all three exp
  modules, `VolInstrument`/`MevJointProgram`/... for vol_markets), so in-body references survive
  the round trip untouched.
  THE IMPORT REWRITE IS THE ONLY PERMITTED EDIT. Hand-editing a returned proof voids its
  verification (the standing rule since 11-03).

BYTE IDENTITY: check ALL SEVENTEEN bundled modules BEFORE integrating anything, and corroborate from
  the repo side with `git status --porcelain lean/` showing exactly the intended entries. Every
  prior bundle returned its dependencies byte-identical; a diff is a red flag that the prover
  edited an input, and an edited input invalidates the whole run.
  Compare with the forward rewrite undone via the SAME map.

LAKEFILE: APPEND `"vol_markets.EtaCurvature"` to the `vol_markets` roots list in `lean/lakefile.toml`
  (currently 14 roots, ending `"vol_markets.TauMevAlgebra"` — the new root is appended AFTER
  TauMevAlgebra). REMOVE NOTHING — 11-03 found the
  anchor had moved under an independent parallel run. **A missing root makes a green build VACUOUS**;
  the build log's own `Built vol_markets.EtaCurvature` line is the evidence the module was actually
  elaborated, and both 11-03 and 11-05 used exactly that line for exactly that reason.
  Do NOT touch the `exp` roots list — that library belongs to another workstream.

AXIOM SWEEP: the sweep file is GENERATED BY GREP from the landed module, never hand-authored, so it
  cannot silently omit a declaration:
    grep -oE '^(theorem|lemma|noncomputable def|def) [A-Za-z_][A-Za-z0-9_'"'"']*' lean/vol_markets/EtaCurvature.lean
  Expected result for EVERY declaration: `[propext, Classical.choice, Quot.sound]`.
  NOTE (11-03/11-06): private declarations have mangled names and cannot be printed individually;
  their cleanliness is a transitive INFERENCE from the clean public prints and must be LABELLED as
  one, not asserted.
  NOTE (11-03's self-contradiction): a fidelity file that greps itself for forbidden axiom names
  will match its own text. State the clean sweep by DESCRIPTION, or scope the check to the sweep
  output file rather than the record.

THE ANTI-NARROWING WATCH LIST (from the prompt; every item gets an explicit verdict):
  T4', T7', T11', T12'  must be STRICT (`StrictAntiOn`/`StrictMonoOn`), never the non-strict forms
  T13'                  must be over the WHOLE `Set.Icc 0 1`
  T24' `curvIndex_etaStar` must be an EQUALITY, with `etaStar`'s closed form in the statement
  T27'                  must be CONSTRUCTED from T24', never an assumed maximizer under an
                        `IsMaxOn` hypothesis — that would be a restatement of
                        `exp/DynamicsOptimization.optimal_controls`
  T29'                  must keep its CONJUNCTION (both ratios), never a single-objective monotonicity
  T28'b                 must not have been merged into T28'a
  T30'                  must REFERENCE the prior theorems' statements (the Phase-11 corner results
                        and T27') as an explicit conjunction — never restate weakened forms

THE OPTIONAL ITEMS — an absence here is an OUTCOME, not a failure, and must be recorded as OPEN:
  T18'  (reduced welfare)                          OPTIONAL
  T18'b (the c₁ ≤ 0 refutation)                    OPTIONAL-but-preferred
  T28'b (the factor-share half of the η identity)  OPTIONAL — if absent, `LEAN_TRACEABILITY` gets an
        OPEN row at 12-04 and the user's η-identity decision is recorded as PARTIALLY discharged:
        the exponent identity proven, the factor-share identification OPEN. That is the honest
        result and it is not to be dressed up as full discharge.

CONTINGENCY: if Section C or Section D comes back absent, or if any watch-list item comes back
  narrowed and cannot be repaired without hand-editing a proof, STOP and invoke the named
  contingency in `.planning/phases/12-eta-tradeoff-optimum/12-CONTINGENCY.md` (a second bundle
  12-02b/12-03b). **Do not accept narrowed statements silently and do not hand-prove the gap.**

PUSH: git push origin feat/lean4-spec ; git subtree push --prefix=lean lean4-spec main
  Verify the mirror is a FAST-FORWARD before pushing (11-05's procedure).
</constants>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Download, prove byte identity of all 17 modules, integrate via the module map, register, build</name>

  <read_first>
    - .planning/phases/12-eta-tradeoff-optimum/12-02-RUN-RECORD.md — the project id, task id, `PROMPT-SHA256`, `BUNDLED-ETA-SHA256`, the 17-module inventory and the inline module map
    - .planning/phases/12-eta-tradeoff-optimum/12-02-MODULE-MAP.txt — the module→origin pairs that DRIVE the import rewrite. Read it before writing any sed
    - .planning/phases/11-mev-hazard-inf-program/11-05-SUMMARY.md — the landing procedure that worked: byte-identity first, import rewrite as the only edit, append the lakefile root, use the `Built ...` log line as elaboration evidence, and the three CLI gotchas (archive-not-directory, one-directory-deeper, `tasks` not `show`)
    - lean/lakefile.toml lines 24–33 — the `vol_markets` roots list to APPEND to, and the `exp` roots list to LEAVE ALONE
  </read_first>

  <files>lean/vol_markets/EtaCurvature.lean, lean/lakefile.toml</files>

  <action>
1. **Confirm terminal status and download.** `aristotle tasks --api-key "$(grep -m1 '^ARISTOTLE_API_KEY=' .env | cut -d= -f2-)"`.
   Download to `scratch/`, rename the archive to `scratch/aristotle-eta-curvature-result.tar.gz`,
   extract, and locate the returned tree (it sits one directory deeper than the flag suggests).

2. **PROVE BYTE IDENTITY OF ALL SEVENTEEN MODULES, BEFORE INTEGRATING ANYTHING.** Undo the forward
   rewrite using the map, then diff against the committed sources:
   ```bash
   RET=<the extracted RequestProject dir>
   MAP=.planning/phases/12-eta-tradeoff-optimum/12-02-MODULE-MAP.txt
   fail=0
   while read -r m lib; do
     [ "$m" = "EtaCurvature" ] && continue
     src="lean/$lib/$m.lean"
     tmp="$(mktemp)"
     # rebuild the origin imports from the map, then compare
     cp "$RET/$m.lean" "$tmp"
     while read -r m2 lib2; do
       sed -i "s|^import RequestProject\.$m2\$|import $lib2.$m2|" "$tmp"
     done < "$MAP"
     diff -q "$tmp" "$src" || { echo "MODIFIED INPUT: $m"; fail=1; }
     rm -f "$tmp"
   done < "$MAP"
   [ "$fail" = "0" ] || { echo "STOP: the prover modified a bundled input"; exit 1; }
   ```
   Also verify the returned doc copy is unchanged — pin the comparison to the BUNDLED copy: the
   returned doc's END-marker-delimited ETA extraction must hash to the `BUNDLED-ETA-SHA256`
   recorded in 12-02-RUN-RECORD.md at submit time. NEVER compare against the live plank file,
   which may have moved (a parallel JIT/TAU edit there is expected and irrelevant).
   **If any input differs, STOP** — an edited input
   invalidates the run; record the diff and escalate rather than integrating.

3. **Integrate `EtaCurvature.lean` with the map-driven rewrite as the ONLY edit:**
   ```bash
   cp "$RET/EtaCurvature.lean" lean/vol_markets/EtaCurvature.lean
   while read -r m lib; do
     sed -i "s|^import RequestProject\.$m\$|import $lib.$m|" lean/vol_markets/EtaCurvature.lean
   done < .planning/phases/12-eta-tradeoff-optimum/12-02-MODULE-MAP.txt
   ```
   Then PROVE no stale import survived:
   `! grep -q 'import RequestProject' lean/vol_markets/EtaCurvature.lean`
   and that every import resolves to a real file:
   ```bash
   grep -oE '^import (vol_markets|exp)\.[A-Za-z0-9_]+' lean/vol_markets/EtaCurvature.lean \
     | sed 's/^import //' | tr '.' '/' | while read -r p; do test -f "lean/$p.lean" || exit 1; done
   ```
   **Change nothing else in the file** — no formatting, no docstring edits, no renames.

4. **Register the root by APPENDING.** Edit `lean/lakefile.toml` so the `vol_markets` roots list
   gains `"vol_markets.EtaCurvature"` after `"vol_markets.TauMevAlgebra"` (the current last root).
   Remove nothing.
   Do not touch the `exp` or `tao` lists.

5. **Build, and capture the elaboration evidence:**
   ```bash
   cd lean && lake build vol_markets 2>&1 | tee /tmp/eta-build-vm.log
   cd lean && lake build 2>&1 | tee /tmp/eta-build-all.log
   ```
   Both must exit 0, and `/tmp/eta-build-vm.log` must contain a line matching
   `Built vol_markets.EtaCurvature` — **without it the green build proves nothing**, because an
   unregistered root is simply skipped. If the module is not in the log, the root registration
   failed; fix the lakefile, do not proceed.

6. **Sanity-check the deliverables are present** before declaring the task done:
   `grep -cE '^(theorem|lemma|noncomputable def|def) ' lean/vol_markets/EtaCurvature.lean` and
   confirm `curvIndex`, `etaStar`, `curvIndex_etaStar`, `arbLossRatio`, `surplusRatio`, `lpExcess`,
   `eta_no_common_argmax` all appear as declarations. Record which of the OPTIONAL items
   (T18', T18'b, T28'b) are present — Task 2 turns this into dispositions.

7. `! grep -q 'sorry\|admit' lean/vol_markets/EtaCurvature.lean` (excluding docstring mentions —
   check the hits, do not just count them; `exp/eta.lean:602` has a `sorry` string inside a
   docstring and is not a gap).
  </action>

  <verify>
    <automated>test -f lean/vol_markets/EtaCurvature.lean && ! grep -q 'import RequestProject' lean/vol_markets/EtaCurvature.lean && grep -q 'vol_markets.EtaCurvature' lean/lakefile.toml && (cd lean && lake build vol_markets) && (cd lean && lake build) && grep -q 'Built vol_markets.EtaCurvature' /tmp/eta-build-vm.log</automated>
  </verify>

  <acceptance_criteria>
    - `test -f lean/vol_markets/EtaCurvature.lean` and `[ "$(wc -l < lean/vol_markets/EtaCurvature.lean)" -ge 300 ]`
    - **BYTE IDENTITY:** the step-2 loop exits 0 — all 16 bundled input modules match their committed sources after the map-driven inverse rewrite
    - The rewrite left no stale import: `! grep -q 'import RequestProject' lean/vol_markets/EtaCurvature.lean`
    - **The non-uniform rewrite was applied correctly:** if the module imports an exp module, it does so as `exp.`, not `vol_markets.` — `! grep -qE '^import vol_markets\.(eta|CESLongVolPayoff|EtaReplication)$' lean/vol_markets/EtaCurvature.lean`
    - Every import resolves to a real file (the step-3 loop exits 0)
    - Root registered by appending: `grep -q 'vol_markets.EtaCurvature' lean/lakefile.toml` and `grep -q 'vol_markets.TauMevAlgebra' lean/lakefile.toml` and `[ "$(grep -o 'vol_markets\.[A-Za-z]*' lean/lakefile.toml | wc -l)" -ge 15 ]` (`grep -o | wc -l` counts OCCURRENCES — `grep -c` counts LINES, and the roots list spans ~6 lines with several roots per line)
    - The `exp` roots were not touched: `git diff lean/lakefile.toml | grep '^[-+]' | grep -q 'exp\.' && exit 1 || true`
    - `cd lean && lake build vol_markets` exits 0 and `cd lean && lake build` exits 0
    - **ELABORATION EVIDENCE:** `grep -q 'Built vol_markets.EtaCurvature' /tmp/eta-build-vm.log`
    - No gaps: `! grep -nE '\bsorry\b|\badmit\b' lean/vol_markets/EtaCurvature.lean` (or every hit is inside a docstring and is recorded as such)
    - The headline declarations exist: `for n in curvIndex etaStar curvIndex_etaStar arbLossRatio surplusRatio lpExcess eta_no_common_argmax; do grep -qE "^(theorem|lemma|noncomputable def|def) $n" lean/vol_markets/EtaCurvature.lean || exit 1; done`
    - Only the intended files changed: `git status --porcelain lean/` lists exactly `lean/vol_markets/EtaCurvature.lean` and `lean/lakefile.toml`
    - `[ "$(git status --porcelain model/exp/ | wc -l)" = "0" ]`
  </acceptance_criteria>

  <done>All sixteen bundled modules returned byte-identical, the new module is integrated with the map-driven non-uniform import rewrite as the only edit, it is registered as an appended lakefile root, both builds exit 0, and the build log's own `Built vol_markets.EtaCurvature` line proves the module was actually elaborated rather than skipped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Axiom sweep from a generated file, and the T1'–T31' fidelity diff</name>

  <read_first>
    - lean/vol_markets/EtaCurvature.lean IN FULL — the returned statements, their exact hypotheses, and every docstring in which the prover disclosed an added hypothesis or a route taken
    - scratch/aristotle-eta-curvature-PROMPT.txt — the sha-pinned specification the diff is against. RE-VERIFY its sha256 equals `PROMPT-SHA256` in 12-02-RUN-RECORD.md before diffing, so the comparison is against the bytes actually sent
    - .planning/phases/12-eta-tradeoff-optimum/12-02-RUN-RECORD.md — the T1'–T31' checklist with its OPTIONAL flags
    - the prover's own run summary from the downloaded result — where added hypotheses and chosen routes are disclosed; Phase 11's provers disclosed both, and the T15 disclosure was the finding
    - .planning/phases/11-mev-hazard-inf-program/11-03-FIDELITY.md and 11-05-FIDELITY.md — the record FORMAT to reproduce, and the standard of honesty: `REFUTED`, `OPEN`, `STRENGTHENED-HYPOTHESES` and `OMITTED` are all sanctioned verdicts, and a returned fallback is never written up as satisfying the item it fell back from
  </read_first>

  <files>.planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md</files>

  <action>
1. **Re-verify the prompt bytes**, so the diff is against what was actually sent:
   `[ "$(sha256sum scratch/aristotle-eta-curvature-PROMPT.txt | cut -d' ' -f1)" = "$(grep -m1 '^PROMPT-SHA256:' .planning/phases/12-eta-tradeoff-optimum/12-02-RUN-RECORD.md | awk '{print $2}')" ]`

2. **GENERATE the axiom sweep file by grep** — never hand-author it, so it cannot omit a declaration:
   ```bash
   { echo 'import vol_markets.EtaCurvature'; echo 'open EtaCurvature'
     grep -oE '^(theorem|lemma|noncomputable def|def) [A-Za-z_][A-Za-z0-9_'"'"']*' lean/vol_markets/EtaCurvature.lean \
       | awk '{print "#print axioms EtaCurvature." $2}'
   } > scratch/eta-curvature-axioms.lean
   cd lean && lake env lean ../scratch/eta-curvature-axioms.lean 2>&1 | tee /tmp/eta-axioms.log
   ```
   The run must exit 0 and EVERY line of output must be the clean triple. Record the declaration
   COUNT and confirm it equals the grep count — that equality is what makes the sweep exhaustive.
   If any name fails to resolve, fix the generator (namespace prefix), not the sweep.
   Private declarations cannot be printed individually; if any exist, record their cleanliness as a
   transitive INFERENCE from the clean public prints and LABEL it as an inference.

3. **Write `.planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md`** with:
   - a header carrying the project id, task id, `PROMPT-SHA256` re-verification result, the
     declaration count and the axiom-sweep result (stated by DESCRIPTION — do not write the
     forbidden axiom names into a file whose own acceptance criterion greps for them; that
     self-contradiction has now bitten four consecutive plans);
   - the **byte-identity table**: all 16 input modules, each `IDENTICAL` or with its diff;
   - the **T1'–T31' disposition table**, one row per item, columns
     `| Item | Requested | Returned name | Verdict | Evidence |`, with verdicts drawn from
     `DELIVERED` / `DELIVERED-STRENGTHENED-HYPOTHESES` / `NARROWED` / `REFUTED` / `OMITTED (OPTIONAL)` /
     `ABSENT`. **Every backticked identifier in every row must be grep-verified to be a real
     declaration in the module**, not merely present as text — a row naming a non-existent lemma is
     worse than no row;
   - the **anti-narrowing verdicts**, item by item, each with the evidence that settles it:
     * T4', T7', T11', T12' — `grep -c 'StrictAntiOn\|StrictMonoOn'` in the relevant statements, and
       the explicit confirmation that the non-strict forms were NOT substituted;
     * T13' — the domain in the returned statement is `Set.Icc 0 1`, quoted;
     * T24' — the returned statement is an EQUALITY and `etaStar`'s closed form appears in it;
     * T27' — the maximizer is CONSTRUCTED: `etaStar ...` appears in the statement and there is no
       `IsMaxOn` HYPOTHESIS supplying it. **If it was hypothesized, the verdict is `NARROWED` and
       the contingency is invoked** — it would be a restatement of
       `exp/DynamicsOptimization.optimal_controls`;
     * T29' — the conjunction survives (both `arbLossRatio` and `surplusRatio` in the conclusion);
     * T30' — an explicit conjunction that REFERENCES the prior theorems' statements (the Phase-11
       corner results and T27'), not weakened restatements, with the mandated cross-model-composition
       docstring present;
     * T28'a vs T28'b — separately named, and T28'a's statement/docstring does not claim to
       establish T28'b;
   - the **added-hypotheses record**: every hypothesis the prover added that the prompt did not
     request, with the prover's own stated reason. **Expect at least one** — the `1/κ_φ` pole was
     pre-authorized precisely because the analogous `ptrade` pole falsified two Phase-11 theorems.
     An added hypothesis is a FINDING, not a defect: record it and say what it means;
   - a **NOT ESTABLISHED** section naming every OPTIONAL item that came back absent, each as
     `OPEN`, plus the five standing OPEN items from E8 (the equilibrium transfer, welfare, the
     non-identification with `λ_ARB`, gas, and Phase 11's inherited `Θ_φ`-restricted σ-varying case).
     **If T28'b is absent, state plainly that the user's η-identity decision is PARTIALLY
     discharged — exponent identity PROVEN, factor-share identification OPEN.** Do not write it as
     a full discharge;
   - a **CONTINGENCY** line: whether `12-CONTINGENCY.md` is invoked, and if so which items trigger it.

4. **If a watch-list item came back NARROWED and cannot be repaired without editing a proof, STOP**
   and invoke the contingency rather than accepting it. Record the decision and its reason.
  </action>

  <verify>
    <automated>test -f .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md && test -f scratch/eta-curvature-axioms.lean && (cd lean && lake env lean ../scratch/eta-curvature-axioms.lean) && grep -qF "curvIndex_etaStar" .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md && grep -qF "T31'" .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md && [ "$(grep -cE "^\| *T[0-9]+'" .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md)" -ge 28 ]</automated>
  </verify>

  <acceptance_criteria>
    - `test -f scratch/eta-curvature-axioms.lean` and it was GENERATED: its `#print axioms` line count equals `grep -cE '^(theorem|lemma|noncomputable def|def) ' lean/vol_markets/EtaCurvature.lean`
    - `cd lean && lake env lean ../scratch/eta-curvature-axioms.lean` exits 0 and every output line is the clean triple (confirmed against `/tmp/eta-axioms.log`, not asserted)
    - `test -f .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md`
    - The prompt bytes were re-verified: `grep -qi 'PROMPT-SHA256' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md` and the record states the comparison result
    - Byte identity recorded for all inputs: `[ "$(grep -c 'IDENTICAL' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md)" -ge 16 ]`
    - Every T-item has a row: `[ "$(grep -cE "^\| *T[0-9]+'" .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md)" -ge 28 ]`
    - **IDENTIFIER EXISTENCE:** every backticked identifier in the disposition rows resolves to a real declaration — GUARDED against a vacuous pass: first assert the outer grep is nonempty (`[ -n "$(grep -oE '\`[A-Za-z_][A-Za-z0-9_]*\`' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md)" ] || exit 1`), then `for n in $(grep -oE '\`[A-Za-z_][A-Za-z0-9_]*\`' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md | tr -d '\`' | sort -u); do grep -qE "^(theorem|lemma|noncomputable def|def) $n" lean/vol_markets/EtaCurvature.lean || grep -qrE "^(theorem|lemma|noncomputable def|def) $n" lean/vol_markets/ lean/exp/ || echo "UNRESOLVED: $n"; done` produces no `UNRESOLVED` line for any identifier claimed as a deliverable
    - The anti-narrowing verdicts are all present: `for t in "T4'" "T7'" "T11'" "T12'" "T13'" "T24'" "T27'" "T29'" "T30'" "T28'a" "T28'b"; do grep -qF "$t" .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md || exit 1; done`
    - The strictness claim is evidenced, not asserted: `grep -qF 'StrictAntiOn' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md` and the same strings appear in the module: `grep -qF 'StrictAntiOn' lean/vol_markets/EtaCurvature.lean`
    - T24' is confirmed an equality: the record quotes the returned statement and `grep -qF 'curvIndex_etaStar' lean/vol_markets/EtaCurvature.lean`
    - An added-hypotheses section exists: `grep -qiE 'added hypothes|STRENGTHENED' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md`
    - A NOT ESTABLISHED section exists and names the standing OPEN items: `grep -qi 'NOT ESTABLISHED' …` and `[ "$(grep -c 'OPEN' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md)" -ge 5 ]`
    - If T28'b is absent, the partial-discharge wording is present: `grep -qF 'T28' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md` and either the item is `DELIVERED` or the record matches `grep -qi 'PARTIALLY discharged'`
    - A contingency line exists: `grep -qi 'contingency' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md`
    - No absolute paths: `! grep -rE '/home/|\$HOME' .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md`
    - No Lean file was edited beyond Task 1's integration: `git status --porcelain lean/` still lists exactly the two intended entries
  </acceptance_criteria>

  <done>The axiom sweep ran from a grep-generated file whose declaration count matches the module's, every T1'–T31' item has an explicit verdict diffed against the re-verified prompt bytes, every anti-narrowing watch-list item has evidence rather than an assertion, every added hypothesis is recorded with the prover's stated reason, and every absent OPTIONAL item is recorded as OPEN — with T28'b's absence, if it occurred, stated as a PARTIAL discharge of the user's η-identity decision rather than a full one.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Commit and push to both remotes</name>

  <read_first>
    - .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md (the record being committed alongside the module)
    - .planning/phases/11-mev-hazard-inf-program/11-05-PLAN.md lines 225–235 — the exact push procedure and the fast-forward verification that precedes the mirror push
  </read_first>

  <files>lean/vol_markets/EtaCurvature.lean, lean/lakefile.toml, .planning/phases/12-eta-tradeoff-optimum/12-03-FIDELITY.md</files>

  <action>
1. Final green check before committing anything: `cd lean && lake build` exits 0.

2. Commit this tree:
   ```bash
   git add lean/vol_markets/EtaCurvature.lean lean/lakefile.toml .planning/phases/12-eta-tradeoff-optimum/ scratch/eta-curvature-axioms.lean
   git commit -m "feat(12-03): land EtaCurvature — Capponi curvature transcription, interior η*, de-degeneration"
   ```
   (`scratch/` is gitignored; if the axiom file is excluded, that is expected — its identity lives
   in the fidelity record, as prior bundles' identities did.)

3. Push to origin, then mirror — **verify the mirror is a fast-forward BEFORE pushing**:
   ```bash
   git push origin feat/lean4-spec
   git fetch lean4-spec main
   # confirm the subtree push will fast-forward; if it will not, STOP and escalate
   git subtree push --prefix=lean lean4-spec main
   ```

4. Verify both landed: `git log --oneline -1 origin/feat/lean4-spec` and
   `git ls-remote lean4-spec main`, and record the two shas in the plan summary.

5. Update memory: flip `aristotle-eta-curvature-inflight` to RESOLVED, naming the landed module,
   the declaration count, the headline outcome for T24'/T27', the disposition of the OPTIONAL items,
   and **the queue being FREE for this project**.

6. Do NOT touch `../plank/`, `model/exp/` or `lean/exp/` in this task.
  </action>

  <verify>
    <automated>(cd lean && lake build) && git log --oneline -3 --name-only | grep -q 'EtaCurvature.lean' && git log --oneline -1 origin/feat/lean4-spec >/dev/null && [ "$(git status --porcelain lean/ | wc -l)" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
    - `cd lean && lake build` exits 0 at the final state
    - Committed: `git log --oneline -3 --name-only | grep -q 'lean/vol_markets/EtaCurvature.lean'` and `git log --oneline -3 --name-only | grep -q '12-03-FIDELITY.md'`
    - The working tree is clean for Lean: `[ "$(git status --porcelain lean/ | wc -l)" = "0" ]`
    - Pushed to origin: `git rev-parse HEAD` equals `git rev-parse origin/feat/lean4-spec`
    - Mirrored: `git ls-remote lean4-spec main` returns a sha, recorded in the summary
    - Untouched trees: `[ "$(git status --porcelain model/exp/ lean/exp/ | wc -l)" = "0" ]` and the plank HEAD is unchanged
    - No key leaked: `! git grep -qE 'ARISTOTLE_API_KEY *= *[A-Za-z0-9]' -- . ':!.env'`
    - Memory updated: the `aristotle-eta-curvature-inflight` entry reads RESOLVED and states the queue is free
  </acceptance_criteria>

  <done>The module and its fidelity record are committed, `lake build` is green at the committed state, both remotes carry the change with the mirror push verified a fast-forward beforehand, and memory records the run as resolved with the queue free.</done>
</task>

</tasks>

<verification>
- All 16 bundled input modules returned byte-identical, verified BEFORE integration via the
  map-driven inverse rewrite.
- `lean/vol_markets/EtaCurvature.lean` imports `exp.*` as `exp.*` and `vol_markets.*` as
  `vol_markets.*` — the non-uniform rewrite was applied, not a blanket sed.
- `lean/lakefile.toml` gained `"vol_markets.EtaCurvature"` by appending; nothing was removed and the
  `exp` list is untouched.
- `lake build vol_markets` and `lake build` both exit 0, and the build log contains
  `Built vol_markets.EtaCurvature`.
- The axiom sweep file was generated by grep, its declaration count matches the module's, and the
  sweep run exits 0 with every print clean.
- `12-03-FIDELITY.md` carries a verdict for every T1'–T31' item, evidence for every anti-narrowing
  watch-list item, every added hypothesis with the prover's reason, and an OPEN label for every
  absent OPTIONAL item.
- Both remotes carry the landed module; the mirror push was verified a fast-forward first.
</verification>

<success_criteria>
The curvature layer, the interior optimum, the closed-form `η*` and the de-degeneration are landed,
registered, green and axiom-clean on both remotes, accompanied by a fidelity record that states
exactly what was delivered, exactly what was strengthened, and exactly what remains OPEN — with no
narrowed statement accepted under a requested name and no absent optional item written up as a
success.
</success_criteria>

<output>
After completion, create `.planning/phases/12-eta-tradeoff-optimum/12-03-SUMMARY.md`.
</output>
