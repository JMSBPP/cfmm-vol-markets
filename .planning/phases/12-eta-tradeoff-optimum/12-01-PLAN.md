---
phase: 12-eta-tradeoff-optimum
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md
  - .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh
  - .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md
  - ../plank/notes/VOLATILITY_INSTRUMENTS.md
  - ../plank/todo.md
autonomous: false
requirements: [CTX-CURVDOC, CTX-REVIEW]

must_haves:
  truths:
    - "The curvature-controller spec exists as insert-ready LaTeX blocks E0-E8, authored in THIS tree, minimal prose and maximal math"
    - "η is the PROTECTED symbol and the gate REQUIRES it — the Phase-11 gate, whose Rule 1 forbids η, is not reused and is proven unusable here"
    - "Capponi's k, α, β are remapped to χ, ϱ_I, ϱ_S and his θ, κ_* are ABSORBED into ϖ_* constants; every remap is on a whitelisted notation-map line"
    - "ν is never introduced — the master document already binds it at M6b"
    - "The anchor's results are cited by their CORRECT numbers: Lemma 3, Proposition 5, Proposition 6 — never 'Lemma 1' or 'the curvature proposition'"
    - "No first-order condition appears anywhere: χ* is a KINK (a branch point), and the doc says so"
    - "The η-normalization bridge is stated as TWO separate claims — the provable exponent identity and the interpretive factor-share identification — so a user-approved banner cannot carry an unproven claim"
    - "The doc never asserts curvIndex 1 = 1 or equates η = 1 with χ = 1"
    - "Two reviewers ran in parallel, blind, and every BLOCKER/MAJOR was resolved before user approval was requested"
    - "The user explicitly approved the block before insertion, and the approved bytes are pinned by a recorded sha256 of an END-marker-delimited section"
    - "The plank-owned file is written but NOT committed by this session; agent ul2inqpl is notified"
  artifacts:
    - path: "model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md"
      provides: "The insert-ready curvature-controller LaTeX blocks E0-E8, authoritative in this tree"
      contains: "2103.08842"
      min_lines: 110
    - path: ".planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh"
      provides: "Executable INVERTED notation gate: η required, Capponi externals forbidden, FOC forbidden, whitelist bounded"
      contains: "notation-map"
    - path: ".planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md"
      provides: "Both reviewer verdicts severity-sorted with resolutions, the user's verbatim approval, and the approved-bytes sha256"
      contains: "BLOCKER"
    - path: "../plank/notes/VOLATILITY_INSTRUMENTS.md"
      provides: "The approved E0-E8 block inserted at the user-decided placement (default: the user-authored ## FLAIR & MEV stub) — the Aristotle input document"
      contains: "varrho_I"
  key_links:
    - from: "model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md"
      to: "Capponi-Jia arXiv:2103.08842v4 Lemma 3 / Proposition 5"
      via: "citation beside each transcribed display"
      pattern: "2103\\.08842"
    - from: "model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md"
      to: "VolInstrument.priceEta (the SAME η)"
      via: "the curvature index built from the grid step ratio"
      pattern: "priceEta"
    - from: "model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md"
      to: "MevJointProgram.joint_corner_degeneracy (the M6a contrast)"
      via: "E7's de-degeneration block naming the Phase-11 theorem"
      pattern: "joint_corner_degeneracy"
---

<objective>
Author the curvature-controller mathematical specification (blocks E0–E8) as insert-ready LaTeX,
gate it with two reviewers, obtain HEAVY USER APPROVAL, and insert it into
`../plank/notes/VOLATILITY_INSTRUMENTS.md` at the user-decided placement (default: the
user-authored `## FLAIR & MEV` stub, whose `\eta ...` body it replaces; alternative: end-of-file
after `## JIT` as a new `## ETA` section).

Nothing else in this phase can start. The workflow is doc-driven: the DOC is what goes to Aristotle,
so the doc IS the specification. Phase 11 proved this three times over — every one of its three
reviewer gates found at least one BLOCKER, and two of them were defects the PLAN had introduced and
that every downstream mechanical check would have passed.

This phase's specific exposure is different from Phase 11's and worse in one respect: the phase
headline `η* = ln((1+ϱ_I)/(1+φ))/(Δi²·ln λ)` is obtained by INVERTING a closed form at a KINK. A
single sentence in this document asking for a stationary point would produce a false or vacuous
theorem two plans later, under a banner the user has already approved.

Purpose: produce the single artifact every downstream plan consumes.
Output: `VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` (this tree, authoritative), the inverted notation
gate, the two reviewer verdicts, and the approved block landed in plank's doc.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-eta-tradeoff-optimum/12-CONTEXT.md
@.planning/phases/12-eta-tradeoff-optimum/12-RESEARCH.md
@model/vol_markets/LEAN_TRACEABILITY.md
@model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md
@lean/vol_markets/VolInstrument.lean
@CLAUDE.md

<constants>
ANCHOR PAPER : ../plank/refs/mev/CapponiJiaAdoptionDEX.pdf
               Capponi & Jia, "The Adoption of Blockchain-Based Decentralized Exchanges",
               arXiv:2103.08842v4 [q-fin.TR], 21 Jul 2021.
               THE CORRECT CITATIONS (12-RESEARCH PIT-E2 — 12-CONTEXT.md gets these WRONG):
                 Lemma 3       — the two-sided curvature lemma (arb-loss ratio AND surplus ratio
                                 each strictly decreasing in curvature).  NOT "Lemma 1".
                 Proposition 5 — the interior optimum k* and liquidity-freeze minimization.
                 Proposition 6 — deposit efficiency and welfare.
               Lemma 1 is the unrelated one-token-shock arbitrage-profit result and must not be
               cited for any curvature claim.

THE CLOSED FORMS (12-RESEARCH F1 — transcribe, do NOT re-derive):
  chi_S = 1 - sqrt((1+phi)/(1+varrho_S))          branch point of the arb-loss ratio
  chi_I = 1 - sqrt((1+phi)/(1+varrho_I))          branch point of the surplus ratio; = chi_star
  arbLossRatio(chi) = varpi_A/2 * { (1+varrho_S) - (1+phi)/(1-chi)   on [0, chi_S]
                                  { (1+varrho_S) * chi_S^2 / chi     on [chi_S, 1]
  surplusRatio(chi) =        1/2 * { (1+varrho_I) - (1+phi)/(1-chi)  on [0, chi_I]
                                   { (1+varrho_I) * chi_I^2 / chi    on [chi_I, 1]
  lpExcess: three branches (A.50)-(A.52), increasing on [0, chi_I], decreasing on [chi_I, 1]
  chi_star = chi_I,  interior in (0,1) iff phi < varrho_I

THE BRIDGE (ours; the algebra is VERIFIED against the sources, not assumed):
  VolInstrument.priceEta eta Di i = PosSpec.lam ^ ((i/2) * Di * eta)      [VolInstrument.lean:30]
  step ratio  priceEta eta Di (i+Di) / priceEta eta Di i = lam ^ (Di^2 * eta / 2)  — NO i
  curvIndex eta Di = 1 - lam ^ (-(Di^2 * eta)/2)   — strictly increasing bijection (0,inf) -> (0,1)
  eta_star = ln((1+varrho_I)/(1+phi)) / (Di^2 * ln lam),  lam = 1.0001 so ln lam > 0
  eta_star > 0 <=> phi < varrho_I;  strictly increasing in varrho_I;  STRICTLY DECREASING in phi.

NOTATION RESOLUTIONS (BINDING — 12-RESEARCH F2; the gate INVERTS vs Phase 11):
  eta  -> PROTECTED. It MUST appear. It is never reassigned, never remapped, never "absorbed".
  Capponi k     (curvature index)            -> write  \chi        (Lean: curv / curvIndex)
  Capponi alpha (INVESTOR PRIVATE-USE PREMIUM, NOT a probability) -> write \varrho_I  (premInv)
  Capponi beta  (PRICE-SHOCK MAGNITUDE,       NOT a probability)  -> write \varrho_S  (premShock)
  Capponi f     (proportional trading fee)   -> IDENTIFIED with this document's \varphi (not renamed)
  Capponi theta, kappa_I, kappa_com, kappa_1, kappa_2 -> NEVER NAMED. Absorbed into four constants:
       varpi_A  probability an arbitrage occurs in a period
       varpi_I  probability an investor arrives
       varpi_H  the hold-benchmark coefficient
       varpi_D  the constant subtracted in the LP excess return
  nu is TAKEN — the master doc binds it at M6b (`\nu_t = w_t/D_t`).  NEVER introduce it.
  Glyph availability VERIFIED by grep against the master doc AND lean/vol_markets/:
       \chi 0 hits, \varrho 0 hits, \varpi 0 hits, \nu 5 hits on 3 lines (TAKEN).
  tau is TAKEN — the live doc now binds τ = τ_MEV (blocks M9–M10, LANDED). Capponi's
  Proposition-5 coefficients tau_1/tau_2 therefore get NEW symbols per the binding
  notation-precedence rule: they are written c_1/c_2 (Lean: cOne/cTwo). Freeness VERIFIED by
  grep: `c₁`/`c₂`/`c_1`/`c_2`/`cOne`/`cTwo` have 0 hits in ../plank/notes/VOLATILITY_INSTRUMENTS.md
  and 0 hits in lean/vol_markets/*.lean.

THE LAMBDA OVERLOAD (a pre-existing collision this plan must NOT resolve by renaming):
  In the pricing geometry `\lambda` unsubscripted is the TICK BASE 1.0001 (`PosSpec.lam`), and the
  E-blocks need it inside `\lambda^{...}` exponentials.  In `### MEV` every `\lambda` is a HAZARD
  and is always subscripted (`\lambda_{\text{ARB}}`).  Both usages are legitimate and both appear.
  The gate therefore permits `\lambda` ONLY when (a) immediately followed by `_` (a subscripted
  hazard) or `^` (the tick base raised to a power), OR (b) immediately preceded by `\ln`/`ln`
  (the headline's `\ln\lambda` denominator — patterns `\\ln\s*\\?lambda`, `ln\s*λ`), OR (c) on the
  single E0 tick-base declaration line matching `λ *= *1\.0001`. This extension of Rule 6 is the
  SANCTIONED resolution of the gate-vs-displays conflict (E6's `\ln\lambda` and E0's `λ = 1.0001`
  are the phase's own displays); Rule 11 (no markers below E1) stands unweakened, and E0 must
  declare the tick-base reading explicitly. Do NOT rename either one.

THE WHITELIST MARKER (the ONLY sanctioned way a forbidden glyph may appear):
  A line whose SOLE purpose is to say what a foreign symbol MAPS TO must end with the exact string
  <!-- notation-map -->  and the gate strips marker-tagged lines before applying its glyph rules.
  Permitted ONLY on the header notation sentences and inside block E0 — nowhere else. Marking any
  other line IS weakening the gate, and gate Rule 9 exists so that this weakening is itself caught.

THE END MARKER (new this phase, and load-bearing):
  The addendum and the inserted plank section BOTH end with a line containing exactly
  <!-- END ETA -->
  Every fidelity extraction in plans 12-02 and 12-04 is delimited by `**E0.` .. `<!-- END ETA -->`.
  Phase 11 terminated its extraction on `/^### /`, which is fragile against any later insertion;
  the TAU addendum (`model/vol_markets/VOLATILITY_INSTRUMENTS_TAU_ADDENDUM.md`) has since LANDED
  in the plank file — blocks M9–M10 are inserted and the τ_MEV channel (A) is DECIDED — and the
  live parallel-edit threat is now the JIT addendum (Aristotle project 610bb259 in flight), which
  may be inserted into the same plank file while this phase runs, so the delimiter must be
  explicit.

NO FIRST-ORDER CONDITION, ANYWHERE (12-RESEARCH PIT-E7; user decision in 12-CONTEXT.md):
  chi_star = chi_I is a BRANCH POINT — a KINK, where the investor's trade switches from draining
  the pool to an interior marginal condition. The derivative JUMPS there; it is not zero.
  `lean/exp/DynamicsOptimization.lean` already contains `foc_eta` and `optimal_controls`, which
  HYPOTHESIZE an interior eta* and characterize it by a first-order condition in a DIFFERENT model.
  This phase SUPERSEDES that by CONSTRUCTION (a closed form obtained by inverting a bijection), and
  the document must state that difference — otherwise a reviewer will correctly call the headline a
  duplicate.

THE ETA-IDENTITY DECISION (user, 2026-07-31, BINDING — and it must be split in TWO):
  The user decided that `VolInstrument.priceEta`'s eta and `model/exp/eta.md`'s eta in (0,1) are
  the SAME parameter under different normalizations, and that the normalization bridge is a
  REQUIRED deliverable.  The document must honor that decision by stating the bridge — but it must
  state it as TWO claims, because only one of them is provable algebra:
    (i) THE EXPONENT IDENTITY — provable, and the algebra is already verified:
        priceEta eta Di i = lam ^ ((i/2)*Di*eta)   and   CFMM.Eta.p_eta lam Di e i = lam ^ (i*Di*e)
        hence   priceEta eta Di i = p_eta lam Di (eta/2) i,
        and via the EXISTING `CFMM.Eta.p_eta_eq_P_half_rescaled`,  = P_half lam (Di*eta/2) i.
        The factor 2 IS the normalization: `priceEta`'s sqrt-price convention (`i/2`).
   (ii) THE FACTOR-SHARE IDENTIFICATION — NOT automatic, and must not be smuggled in under (i).
        `model/exp/eta.md`'s eta in (0,1) is the exponent of the weighted-CFMM TRADING FUNCTION
        `CFMM.Eta.L_eta eta X Y = X^eta * Y^(1-eta)` — a FACTOR SHARE on reserves, not a grid
        exponent.  `exp/eta.lean`'s own `P_half` docstring says, in terms: *"eta does not enter the
        tick->price map — it enters at the reserve / impact level."*  So (ii) is a claim ABOUT THE
        MODEL linking a reserve-side share to a grid-side exponent, and it is either derived from
        the weighted-CFMM marginal-price relation or it is labelled OPEN.
  E6 states (i) as the bridge and (ii) as an explicitly separate, explicitly labelled item. The
  user's decision is honored — the bridge IS a deliverable — and no unproven identification rides
  in on an approved banner. If the reviewers or the user judge (ii) provable, it is upgraded in
  12-02; it is never assumed here.

THE KNOWN ASYMMETRY (record it, do not smooth it):
  Capponi's k = 1 is CPMM and CAPS his family. Our eta -> infinity does NOT cap; curvIndex covers
  curvatures beyond his range. This HELPS interiority (eta* is a genuine interior max on (0,inf)),
  but the document may NOT claim "eta = 1 is CPMM": in this project's own convention
  `VolInstrument.priceEta_one` gives eta = 1 = the standard sqrt-price grid, a DIFFERENT
  normalization from Capponi's k = 1.  Any statement that `curvIndex 1 = 1`, or that equates
  eta = 1 with chi = 1, is FORBIDDEN and the gate checks for it.

WHAT MUST BE LABELLED OPEN (12-RESEARCH F5 — honest labels, not softening):
  1. THE EQUILIBRIUM TRANSFER. That the tick-grid AMM's arbitrage/investor equilibrium has
     Capponi's closed forms with `curvIndex eta Di` in the k slot is ASSUMED, not derived. Deriving
     it means re-solving his (A.31)/(A.39) on a discrete grid with per-tick liquidity.
  2. Proposition 6's WELFARE half — bounded to a statement about three given functions under the
     zero-arbitrageur-payoff assumption, or OPEN. Never a welfare theorem about the game.
  3. The relation between Capponi's arb-loss ratio and `MevOptimization.mevMulti` (lambda_ARB):
     different models, different units. NOT IDENTIFIED, stated as forcefully as M0 states that
     lambda_ARB is a summand of lambda_MEV and never a sibling.
  4. GAS. Capponi's Assumption 3 (the arbitrageur pays a gas fee equal to its full profit) is what
     makes the arbitrageur's equilibrium payoff zero. Absorbed, not modelled.
  5. The Theta_phi-restricted sigma-varying MEV comparison — INHERITED OPEN from Phase 11
     (LEAN_TRACEABILITY 7.1, last M6b row). Phase 12 does not touch it and must not appear to.

INSERTION POINT: ../plank/notes/VOLATILITY_INSTRUMENTS.md — the file is now ~813 lines: after
  block M8 (line 748) come `## **M9.` (~757) and `## **M10.` (~770, the landed τ_MEV blocks), then
  the user-authored `## FLAIR & MEV` stub at line 786 whose ENTIRE body is the single line
  `\eta ...` at line 788, then `## JIT` at line 790.
  DEFAULT placement: the user-authored `## FLAIR & MEV` stub — REPLACE its `\eta ...` body with
  the ETA blocks, KEEPING the user's section title. The alternative is end-of-file after `## JIT`
  (as a new `## ETA` section). The placement choice (stub vs end-of-file) MUST be listed as an
  explicit decision item inside the HEAVY USER APPROVAL checkpoint presentation (Task 3).
  Do NOT interleave with the M-blocks: their bytes are sha-pinned in 11-01-REVIEW.md and
  11-02-RUN-RECORD.md, and 11-06 already recorded one intentional pin invalidation. A second
  casual one is not to be created. Block headers follow the existing convention `## **E0. ...**`.
  M-BLOCK INTEGRITY HASH SCOPE: every M-block extraction in this phase runs M0 → end of M8 ONLY
  (`awk '/\*\*M0\./{f=1} f&&/^## \*\*M9\./{f=0} f{print}'` — stop BEFORE `## **M9.`), NEVER
  M0→EOF: that scope is placement-independent and immune to parallel JIT/TAU edits below M8.

FILE OWNERSHIP : ../plank/notes/VOLATILITY_INSTRUMENTS.md and ../plank/todo.md belong to agent
  `ul2inqpl` (CLAUDE.md ownership map). This session may WRITE the approved block after user
  approval; it must NOT `git commit` in the plank worktree. Precedent: 11-01 Task 3.
  `lean/exp/` and `model/exp/` belong to ANOTHER workstream and are READ-ONLY for this entire
  phase — `model/exp/eta.md` is modified and `model/exp/eta_pi_trader_delta_control.md` untracked
  in the working tree right now. Check `claude-peers list_peers` before any exp/ interaction.
</constants>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the curvature addendum (blocks E0–E8) and the INVERTED notation gate</name>

  <read_first>
    - .planning/phases/12-eta-tradeoff-optimum/12-RESEARCH.md IN FULL — especially F1 (the exact closed forms and the branch-agreement checks), F2 (the collision set and the verified glyph availability), F3 (the Option-C decision and the known asymmetry), F5 (what must be OPEN), F7 (the pre-existing lean/exp layer), F8 (the de-degeneration argument), F9 (the E-block layout), and Common Pitfalls PIT-E1..PIT-E12
    - ../plank/refs/mev/CapponiJiaAdoptionDEX.pdf — extract with `pdftotext -layout ../plank/refs/mev/CapponiJiaAdoptionDEX.pdf /tmp/capponi.txt`, then read `sed -n '983,1115p' /tmp/capponi.txt` (section 5.1: the family, Lemma 3, Proposition 5, Proposition 6), `sed -n '3286,3570p' /tmp/capponi.txt` (proof of Lemma 3, (A.31)-(A.43)), `sed -n '3567,3835p' /tmp/capponi.txt` (proof of Proposition 5, (A.44)-(A.55)), `sed -n '3836,3935p' /tmp/capponi.txt` (proof of Proposition 6, (A.56)). TRANSCRIBE FROM THE PDF, not from the research summary — 11-01's reviewer gate was charged with exactly this and it mattered
    - model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md — the FORMAT precedent: numbered blocks, `[NOTATION]`/`[ADDITION]`/`[CAVEATS]` tags, insert-ready `\[ \begin{aligned} ... \end{aligned} \]` displays, minimal prose, the `<!-- notation-map -->` marker convention, and the voice its M8 uses for OPEN items
    - .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh — the script to INVERT. Read it for the mechanism (whitelist strip, POSIX-ERE discipline, the written-out `[^_]|$` alternatives, the bounded-marker rule). Its Rule 1 FORBIDS η, so it cannot be reused; a new script is required
    - model/vol_markets/LEAN_TRACEABILITY.md section 0 (the η reservation and the MMR collision paragraph whose shape the ETA notation paragraph mirrors) and section 6 item (b) (the demand-elasticity gap this phase partly closes) and section 7.1 (the MEV claim rows)
    - lean/vol_markets/VolInstrument.lean lines 28-46 — `priceEta`, `priceEta_pos`, `priceEta_strictMono`, `priceEta_one`; and lean/vol_markets/PosSpec.lean lines 39-46 — `lam = 1.0001`, `one_lt_lam`, `tickPrice`
    - lean/exp/EtaReplication.lean lines 50-85 — `p_eta` and `p_eta_eq_P_half_rescaled`; lean/exp/eta.lean lines 36-40 and 110-116 — `P_half` (READ ITS DOCSTRING: "η does not enter the tick→price map") and `L_eta`. READ-ONLY: do not edit anything under lean/exp/ or model/exp/
    - lean/vol_markets/MevJointProgram.lean lines 36-96 — `joint_corner_degeneracy`, `joint_beta_degeneracy`, `joint_scalarization_degeneracy` and the T22 docstring naming demand response as the escape from the degeneracy (E7 quotes this)
    - ../plank/notes/VOLATILITY_INSTRUMENTS.md lines 564–748 — the `### MEV` M-blocks and the M-block header convention, plus line 692's `\nu_t = w_t/D_t` (the collision that rules ν out); and the tail: M9–M10 (τ_MEV, landed), the `## FLAIR & MEV` stub at 786 (the default insertion target) and `## JIT` at 790
  </read_first>

  <files>model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md, .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh</files>

  <action>
**A. Write `model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`.**

Header, mirroring the MEV addendum's shape. The notation sentences that must DISPLAY the foreign
glyphs in order to retire them carry `<!-- notation-map -->` at end of line:

```
# PROPOSED addendum to `VOLATILITY_INSTRUMENTS.md` — the `## ETA` section: the curvature controller and the interior η*

> STATUS: DRAFT — pending two-reviewer gate and user approval.
> Anchor: Capponi & Jia, *The Adoption of Blockchain-Based Decentralized Exchanges*,
> arXiv:2103.08842v4 (2021-07-21), read from `../plank/refs/mev/CapponiJiaAdoptionDEX.pdf`.
> The curvature results transcribed here are **Lemma 3**, **Proposition 5** and **Proposition 6**.
> Notation: η is PROTECTED and is this document's pricing-kernel exponent throughout. The <!-- notation-map -->
> paper's curvature index `k` is written `χ`; his investor private-use premium `α` is written <!-- notation-map -->
> `ϱ_I` and his price-shock magnitude `β` is written `ϱ_S`; his fee `f` IS this document's `φ`; <!-- notation-map -->
> his arrival and shock probabilities `θ, κ_I, κ_com, κ₁, κ₂` are never named and are absorbed <!-- notation-map -->
> into the four constants `ϖ_A, ϖ_I, ϖ_H, ϖ_D`. <!-- notation-map -->
> Minimal prose; each block is insert-ready LaTeX.
```

Then these NINE blocks in this order, LaTeX-first, at most two sentences of prose each. Take every
formula from the PDF (cross-checked against 12-RESEARCH F1) — transcribe, do not re-derive.

- **E0. [NOTATION]** The remap table, every row on a `<!-- notation-map -->` line — including
  the MANDATED row "Capponi's `τ₁`, `τ₂` → `c₁`, `c₂` (ours: `τ = τ_MEV`, block M9)". Then, on
  UNMARKED lines (so they must be glyph-clean):
  - the four absorbed constants `ϖ_A, ϖ_I, ϖ_H, ϖ_D` with their meanings, each `≥ 0`;
  - the standing hypotheses `0 ≤ φ < ϱ_S ≤ ϱ_I` and `0 < Δ_i`, `1 < λ`;
  - **one sentence declaring the tick-base reading**: in this section an unsubscripted `\lambda`
    inside an exponential is the tick base `λ = 1.0001` (`PosSpec.lam`), never a hazard; the
    hazards of `### MEV` are always subscripted;
  - **one sentence stating explicitly that `ϱ_I` and `ϱ_S` are NOT probabilities** — `ϱ_I` is a
    valuation premium and may exceed 1 (PIT-E3: `12-CONTEXT.md` mis-describes them as arrival
    probabilities, and under that reading `χ* = 1 − √((1+φ)/(1+ϱ_I))` is uninterpretable);
  - **the η convention bridge, in one paragraph, as TWO numbered claims** — (i) the exponent
    identity `priceEta η Δ_i i = p_η(λ, Δ_i, η/2, i) = P_half(λ, Δ_i·η/2, i)`, the factor 2 being
    the sqrt-price convention, and (ii) the factor-share identification with `L_η = X^η Y^{1−η}`,
    marked as a MODELLING claim that E8 lists among the OPEN items unless E6 derives it.

- **E1. [ADDITION] The curvature family and the discrete index.** Capponi's mixture
  `F_χ = (1−χ)·A·F₀ + χ·F₁` with `F₀ = p_A x + p_B y` (linear, zero curvature), `F₁ = xy`
  (constant product), `A = (y_A y_B/(p_A p_B))^{1/2}`; curvature increasing in `χ`. Then OUR
  discrete index, with the enabling algebra displayed:
  \[ \frac{p_{(\eta,\Delta_i)}(i+\Delta_i)}{p_{(\eta,\Delta_i)}(i)} = \lambda^{\Delta_i^2\eta/2}
     \quad\text{(independent of } i\text{)}, \qquad
     \chi(\eta,\Delta_i) \;\coloneqq\; 1 - \lambda^{-\Delta_i^2\eta/2} \]
  with: strictly increasing in `η`; a bijection `(0,∞) → (0,1)`; `→ 0` as `η → 0⁺` (Capponi's
  `χ = 0`, the zero-curvature constant-price grid) and `→ 1` as `η → ∞`. State that this is
  Capponi's OWN definition of curvature ("the rate of change of the marginal exchange rate",
  section 5.1 p. 22) in discrete form. Then the explicit warning, on its own line:
  **`η = 1` is the standard sqrt-price grid (`VolInstrument.priceEta_one`), NOT Capponi's `χ = 1`;
  `χ(1, Δ_i) ≠ 1`, and our `η` range is unbounded above where his `k` caps at 1.**

- **E2. [ADDITION] The arbitrage-loss ratio.** (Lemma 3(1).) Both branches displayed, the branch
  point `χ_S = 1 − √((1+φ)/(1+ϱ_S))`, the branch-agreement value `(1+ϱ_S)(1−s)/2` with
  `s = √((1+φ)/(1+ϱ_S))` so the glued function is continuous, and **strictly decreasing in `χ`**
  on `(0,1]`. One line: `ϱ_S > φ` is Lemma 1's condition for an arbitrage to occur at all.

- **E3. [ADDITION] The investors' surplus ratio.** (Lemma 3(2).) The same shape with `ϱ_I` and
  `χ_I`, **strictly decreasing in `χ`**. One line: `ϱ_I > φ` is Lemma 2's condition for the
  investor to trade. One line: `ϱ_S ≤ ϱ_I ⟺ χ_S ≤ χ_I`, which is the geometrized form of
  Proposition 5's displayed `α > β` hypothesis (his proof consumes it ONLY through `k₁ ≥ k₂`).

- **E4. [ADDITION — THE INTERIOR OPTIMUM]** (Proposition 5.) The LP excess return `D(χ)` in its
  three branches (A.50)–(A.52) with `ϖ_D` and `ϖ_I` free, continuity at BOTH branch points,
  strictly increasing on `[0, χ_I]`, strictly decreasing on `[χ_I, 1]` **under `c₁ > 0`** (the
  paper's `τ₁`, renamed per E0 — `τ` is TAKEN by M9's `τ_MEV`), and
  \[ \chi^{\star} = \chi_I = 1 - \sqrt{\tfrac{1+\varphi}{1+\varrho_I}}, \qquad
     \chi^{\star} \in (0,1) \iff \varphi < \varrho_I \]
  Then, on its own line and in bold: **`χ*` is a BRANCH POINT — a kink, where the investor's trade
  switches from draining the pool to an interior marginal condition. The derivative jumps there.
  There is no first-order condition and none is claimed.** Then the liquidity-freeze corollary
  (`D(χ*) < 0 ⟹ D(χ) < 0` for all `χ`) and one line recording that the `c₁ ≤ 0` case is the flat
  freeze region where the LP payoff is constant in `χ`, so strict single-peakedness is FALSE in
  general and the strict claim is made only under `c₁ > 0`.

- **E5. [ADDITION]** (Proposition 6.) Deposit efficiency (A.56), same two-branch shape, same branch
  point `χ*`, increasing below and decreasing above. Then the welfare half **bounded explicitly**:
  a statement about the three given pieces (LP aggregate payoff, arbitrageur payoff `= 0` by
  Capponi's Assumption 3, investor surplus) or `**OPEN**` — never a welfare theorem about the game.
  One line naming gas as absorbed and as the reason the bound is where it is.

- **E6. [ADDITION — THE BRIDGE]** The headline:
  \[ \eta^{\star} \;=\; \frac{\ln\!\big((1+\varrho_I)/(1+\varphi)\big)}{\Delta_i^{2}\,\ln\lambda},
     \qquad \chi(\eta^{\star},\Delta_i) = \chi^{\star} \]
  obtained by INVERTING the bijection of E1 at `χ*` — say that in words, and say that the inversion
  is `Real.log` algebra, not an existence argument. Then the three comparative statics:
  `η* > 0 ⟺ φ < ϱ_I`; strictly increasing in `ϱ_I`; **strictly decreasing in `φ`**; strictly
  decreasing in `Δ_i²`. Then the two-sided shape in `η` (strictly increasing below `η*`, strictly
  decreasing above). Then the normalization-bridge sub-block, as the TWO claims fixed in
  `<constants>`: (i) the exponent identity, displayed, with the one-sentence integer-tick
  qualifier: the identity (T28'a) holds on integer ticks `i : ℤ`, the domain on which the two
  conventions are compared; (ii) the factor-share identification,
  labelled either as derived (with the derivation displayed) or as OPEN. Finally, one sentence
  distinguishing this from `lean/exp/DynamicsOptimization.lean`: that module HYPOTHESIZES an
  interior `η*` and characterizes it by a first-order condition in a different model; **this block
  proves existence and gives a closed form by construction**, and supersedes rather than
  duplicates it.

- **E7. [ADDITION — THE DE-DEGENERATION]** The contrast with Phase 11, naming the theorem:
  over `Θ_φ`, `MevJointProgram.joint_corner_degeneracy` puts the FLAIR maximum and the ARB minimum
  at the SAME point in every coordinate, robustly to every linear scalarization — no trade-off.
  Over `η` the structure is the opposite and the reason is Lemma 3's two-sidedness: BOTH ratios
  are strictly antitone, so the arb-minimizer sits at `η → ∞` and the surplus-maximizer at
  `η → 0⁺`; **no `η` is simultaneously arb-minimal and surplus-maximal**, and the LP payoff, which
  combines them, peaks strictly between. Hence the joint optimum is (`Θ_φ` CORNER, `η` INTERIOR).
  Then the coupling, as its own display: `η*` is strictly decreasing in `φ`, and Phase 11 put `φ̄`
  at its UPPER corner, so **the fee corner strictly LOWERS the optimal curvature — the two blocks
  are not separable even though one of them is a corner.** One sentence recording that `ϱ_I` IS the
  demand-side parameter that `MevJointProgram`'s degeneracy docstring and `LEAN_TRACEABILITY`
  section 6(b) both name as the missing layer.
  **Write "every linear scalarization" in words — do not name `κ`**, which the gate forbids (it is
  Capponi's absorbed arrival symbol and it collides with the Phase-11 scalarization weight).

- **E8. [CAVEATS]** The five OPEN items of `<constants>`, each labelled `**OPEN**`, in that order.
  Plus: this is Capponi's two-period discrete-shock model, not MMR's fast-block diffusion; the two
  arbitrage objects are NOT identified; gas is absorbed. End the file with a line containing
  exactly `<!-- END ETA -->`.

**B. Write `.planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh`** — executable,
`#!/usr/bin/env bash`, `set -euo pipefail`, `export LC_ALL=C.UTF-8` as the third line. It takes the
file to check as `$1`. Every rule is plain POSIX ERE; do NOT use `([^_]|$)` mid-alternation (a GNU
extension that silently mis-parses elsewhere) — write the alternatives out, as the Phase-11 script
does. Whitelist step first, verbatim in shape:

```bash
STRIPPED="$(mktemp)"; trap 'rm -f "$STRIPPED"' EXIT
grep -v -- '<!-- notation-map -->' "$1" > "$STRIPPED" || true
```

Rules 1–8 as follows (glyph rules against `"$STRIPPED"`, token/structure rules against `"$1"`):

1. **REQUIRE η — the INVERSION.** `grep -qE 'η|\\eta' "$STRIPPED"` must SUCCEED — the grep runs
   against the whitelist-STRIPPED text, NOT against `"$1"`: the Phase-11 MEV addendum DOES contain
   η on its notation-map lines (lines 22/29), so a `"$1"` grep would pass there and void the
   canary. Fail with
   `INVERTED Rule 1: the protected pricing-kernel eta is absent — this is the ETA section`.
   (Sanity: this rule is what makes the script fail on the Phase-11 addendum, as it must — its η
   occurrences all sit on notation-map lines and are stripped.)
2. **FORBID Capponi's curvature `k`.** Against `"$STRIPPED"`, none of these may appear:
   `\(k\)`, `k^{\star}`, `k^*`, `k_1`, `k_2`, `F_k`, `k \in`, `\(k \)`. Use one ERE alternation of
   those literal forms. Fail with `Capponi's k appears unmapped — use \chi`.
3. **FORBID Capponi's α, β unsubscripted** (his premia are `\varrho_I`/`\varrho_S`; the doc's own
   `α_j`, `β_j` are Θ_φ sigmoid parameters and are always subscripted):
   `grep -nE 'α[^_]|α$|\\alpha[^_]|\\alpha$|β[^_]|β$|\\beta[^_]|\\beta$' "$STRIPPED"` must find
   nothing.
4. **FORBID θ, κ and τ entirely** (θ/κ absorbed into `ϖ_*`; `θ` collides with the doc's option
   theta and `κ` with the Phase-11 scalarization weight; **τ is TAKEN by M9's `τ = τ_MEV`** and
   Capponi's `τ₁`/`τ₂` are renamed `c₁`/`c₂` — τ may appear ONLY on `<!-- notation-map -->` lines
   citing M9, which the whitelist strip removes):
   `grep -nE 'θ|\\theta|κ|\\kappa|τ|\\tau' "$STRIPPED"` must find nothing. Capital `\Theta` (as in
   `\Theta_{\varphi}`) is NOT matched by this and is allowed.
5. **FORBID ν, BOTH forms** — TAKEN by M6b: `grep -nE 'ν|\\nu' "$STRIPPED"` must find nothing.
6. **PERMIT λ only as (a) a subscripted hazard or the tick base raised to a power, (b) the
   ln-prefixed tick base, or (c) the E0 tick-base declaration.** Against `"$STRIPPED"`, every
   `λ`/`\lambda` occurrence must be (a) immediately followed by `_` or `^`, OR (b) immediately
   preceded by `\ln`/`ln` (patterns `\\ln\s*\\?lambda`, `ln\s*λ` — E6's headline denominator
   `\ln\lambda`), OR (c) on the single E0 tick-base declaration line matching `λ *= *1\.0001`.
   Implement as: `grep -nE 'λ[^_^]|λ$|\\lambda[^_^]|\\lambda$' "$STRIPPED"` restricted to lines
   that do NOT match `\\ln\s*\\?lambda|ln\s*λ` and do NOT match `λ *= *1\.0001` (at most ONE such
   declaration line) must find nothing. **This extension is the SANCTIONED resolution of the
   Rule-6-vs-displays conflict** — the phase's own E6 display uses `\ln\lambda` and E0 declares
   `λ = 1.0001`; Rule 11 (no markers below E1) stands unweakened.
7. **FORBID a first-order condition.** Two greps: `grep -n -iE 'first.order condition|stationary point' "$1"`
   (case-insensitive, `first.order` covering the hyphen/space variants) plus the CASE-SENSITIVE,
   word-bounded `grep -n -E '\bFOC\b' "$1"` — word-boundary and case-sensitivity so that "focus"
   (and other words containing "foc") does NOT match. Both,
   restricted to lines that do NOT also match `-iE 'no first-order|not a first-order|kink|jumps|superseded|never claimed|none is claimed'`,
   must find nothing. Fail with `PIT-E7: a first-order condition is asserted — chi* is a KINK`.
8. **FORBID the CPMM misidentification.** `grep -n -E '\\chi\(1|curvIndex 1 = 1|\\eta = 1.*\\chi = 1|\\chi = 1.*\\eta = 1' "$1"`
   restricted to lines that do NOT also match `-iE 'NOT|≠|never'` must find nothing.
9. **REQUIRE tokens** (`grep -qF` each): `2103.08842`, `Lemma 3`, `Proposition 5`, `Proposition 6`,
   `\chi`, `\varrho_I`, `\varrho_S`, `priceEta`, `joint_corner_degeneracy`, `OPEN`,
   `<!-- END ETA -->`; and the varpi requirement accepts BOTH forms — `grep -qF '\varpi' "$1" || grep -qF 'ϖ' "$1"`
   (the LaTeX macro OR the Unicode glyph) must succeed.
10. **FORBID absolute paths:** `grep -nE '/home/|\$HOME|~/' "$1"` must find nothing.
11. **BOUND the whitelist.** Marker count `>= 1` and `<= 24`; and the LAST marker line number must
    be strictly LESS than the line number of the `**E1.` header. Fail with
    `notation-map marker used outside the header/E0 whitelist`.

Print `ETA NOTATION GATE: PASS` on success.

**C. Prove the gate is not the Phase-11 gate wearing a new name.** Run BOTH directions and record
both outputs in the task notes:
```bash
bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md   # must PASS
! bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md > /tmp/eta-canary.out 2>&1
grep -q 'INVERTED Rule 1' /tmp/eta-canary.out   # must FAIL WITH THE RULE-1 MESSAGE, not merely nonzero
```
The second is the PIT-E1 canary: a gate that passes on the Phase-11 addendum is not enforcing the
inversion — and the canary must assert the gate's Rule-1 failure MESSAGE appears in the output,
because a bare nonzero exit could equally come from a script crash (a crash is not a canary pass).
Note the MEV addendum DOES contain η on its notation-map lines, which is exactly why Rule 1 greps
the whitelist-stripped text. Fix the ADDENDUM until the first passes. **Do NOT weaken the gate to make it pass, and
do NOT add the marker to any line outside the header/E0 whitelist** — Rule 11 exists so that this
particular weakening is itself caught.

**D. Touch nothing under `lean/`, `model/exp/`, or `../plank/` in this task.**
  </action>

  <verify>
    <automated>bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md && ! bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md > /tmp/eta-canary.out 2>&1 && grep -q 'INVERTED Rule 1' /tmp/eta-canary.out && [ "$(git status --porcelain lean/ model/exp/ | wc -l)" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
    - `test -f model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`
    - `bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` exits 0 and prints `ETA NOTATION GATE: PASS`
    - **THE PIT-E1 CANARY:** `! bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md > /tmp/eta-canary.out 2>&1 && grep -q 'INVERTED Rule 1' /tmp/eta-canary.out` — the Phase-11 addendum MUST fail Rule 1 WITH the Rule-1 message (its η occurrences sit on notation-map lines and are stripped; a bare nonzero exit could be a crash, which is a gate defect, not a canary pass)
    - All nine block headers present: `for b in E0 E1 E2 E3 E4 E5 E6 E7 E8; do grep -q "\*\*$b\." model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md || exit 1; done`
    - The gate pins the locale and implements the whitelist: `grep -q 'LC_ALL=C.UTF-8' .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh` and `grep -q 'notation-map' .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh`
    - The gate uses no GNU-only mid-alternation anchor: `! grep -q '(\[^_\]|\$)' .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh`
    - η is present (the inversion): `grep -qE 'η|\\eta' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`
    - ν is absent outside the whitelist: `! grep -v -- '<!-- notation-map -->' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md | grep -qE 'ν|\\nu'`
    - θ and κ are absent outside the whitelist: `! grep -v -- '<!-- notation-map -->' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md | grep -qE 'θ|\\theta|κ|\\kappa'`
    - The correct anchor results are cited: `grep -qF 'Lemma 3' … && grep -qF 'Proposition 5' … && grep -qF 'Proposition 6' …` on the addendum
    - The WRONG citation is absent: `! grep -qE 'Lemma 1[^0-9]' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` OR every `Lemma 1` hit is on a line also matching `arbitrage occurs|not the curvature|one-token`
    - The closed form for η* is present: `grep -qF '\ln' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` and `grep -qE 'eta\^\{\\star\}|η\^\*|\\eta\^\{\\star\}' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`
    - The kink is stated and no FOC is asserted: `grep -qi 'kink' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` and the gate's Rule 7 passes
    - The CPMM misidentification is explicitly denied: `grep -qi 'NOT Capponi' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` or a line matching `η = 1` also matching `sqrt-price` and `NOT`
    - ϱ_I is declared not-a-probability: `grep -qiE 'not a probability|are not probabilities' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`
    - The bridge is split in two: `grep -qE 'p_eta|p_\{\\eta\}|P_half' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` and `grep -qE 'L_\\?eta|L_\{\\eta\}|X\^\{\\eta\}' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`
    - The Phase-11 contrast names the theorem: `grep -qF 'joint_corner_degeneracy' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`
    - The five OPEN items are present: `[ "$(grep -c 'OPEN' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md)" -ge 5 ]`
    - The end marker exists exactly once and is last: `[ "$(grep -c -- '<!-- END ETA -->' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md)" = "1" ]`
    - The whitelist is bounded to header/E0: `[ "$(grep -n -- '<!-- notation-map -->' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md | tail -1 | cut -d: -f1)" -lt "$(grep -n -- '\*\*E1\.' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md | head -1 | cut -d: -f1)" ]`
    - `[ "$(wc -l < model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md)" -ge 110 ]`
    - `[ "$(git status --porcelain lean/ model/exp/ | wc -l)" = "0" ]`
    - `git status --porcelain` shows NOTHING under `../plank/` (this task writes only in this tree)
  </acceptance_criteria>

  <done>The addendum exists with blocks E0–E8, transcribes Capponi's Lemma 3 / Proposition 5 / Proposition 6 closed forms from the PDF under the remaps χ / ϱ_I / ϱ_S with θ and κ absorbed, protects η, never introduces ν, states χ* as a KINK with no first-order condition anywhere, splits the η-normalization bridge into the provable exponent identity and the separately-labelled factor-share claim, denies the η = 1 ⇔ χ = 1 misidentification, carries five OPEN labels and an END marker, and passes an inverted notation gate that provably FAILS on the Phase-11 addendum.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Two-reviewer gate on the addendum (parallel, blind), resolve BLOCKER/MAJOR</name>

  <read_first>
    - model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md (the artifact under review, as written by Task 1)
    - /home/jmsbpp/.claude/CLAUDE.md section "Two-step reviewer process (specs and plans)" — the binding rule: Reviewer 1 is ALWAYS the Reality Checker; Reviewer 2 is ONE specialist from the AI-agency catalog chosen to fit the task, and the pick must be STATED WITH ITS REASON; both run IN PARALLEL, neither edits the artifact, both return BLOCKER/MAJOR/MINOR
    - .planning/phases/12-eta-tradeoff-optimum/12-RESEARCH.md — Common Pitfalls PIT-E2 (the mis-numbered anchor results), PIT-E3 (α/β are premia, not probabilities), PIT-E4 (the 1/χ pole), PIT-E5 (conflating the two arbitrage objects), PIT-E6 (duplicating lean/exp), PIT-E7 (the FOC), PIT-E8 (presenting the bridge as a derivation), and Open Questions 1–5
    - ../plank/refs/mev/CapponiJiaAdoptionDEX.pdf via `/tmp/capponi.txt` — the reviewers must check the transcription against the SOURCE, not against the research summary. This is what 11-01's and 11-02's gates were charged with and it is where their BLOCKERs came from
    - .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md and 11-04-PROMPT-REVIEW.md — the review-file FORMAT to reproduce, and the evidence that every gate so far has found at least one BLOCKER
  </read_first>

  <files>.planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md, model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md</files>

  <action>
1. **Extract the anchor** so both reviewers check against the paper:
   `pdftotext -layout ../plank/refs/mev/CapponiJiaAdoptionDEX.pdf /tmp/capponi.txt`

2. **Launch BOTH reviewers IN PARALLEL** (one message, two agent calls; if the sub-agent tool is
   unavailable, spawn them as independent OS processes, blind to each other, as 11-04 did — their
   convergence is only evidence if they cannot see each other). Neither edits the file.

   - **Reviewer 1 — Reality Checker** (mandatory, always first). NEEDS-WORK-by-default,
     evidence-based. Specifically charged with:
     (a) verifying every transcribed formula against `/tmp/capponi.txt` — the two branches of both
         ratios, the three branches of `D(χ)`, `χ_S`, `χ_I`, `χ*`, (A.36)/(A.38)/(A.42)/(A.43)/
         (A.50)–(A.52)/(A.56) — and re-deriving the branch-agreement algebra at BOTH branch points
         by hand rather than accepting the research's word for it;
     (b) confirming the results are cited as **Lemma 3 / Proposition 5 / Proposition 6** and that
         no curvature claim cites Lemma 1;
     (c) confirming `ϱ_I`/`ϱ_S` are described as premia and NEVER as probabilities or as lying in
         `[0,1]`;
     (d) hunting for any first-order condition, stationary point, or derivative-based
         characterization of `χ*` or `η*` anywhere in the file;
     (e) checking the `1/χ` pole: does any displayed claim quantify over a region containing
         `χ = 0` on a branch with `χ` in a denominator? This is structurally the same defect as
         `ptrade`'s negative-fee pole, which made Phase 11's T15 AND T17 false as specified;
     (f) **auditing the η-bridge split specifically** — is the exponent identity actually the
         algebra of `VolInstrument.priceEta` and `CFMM.Eta.p_eta` as they exist in the tree
         (check `lean/vol_markets/VolInstrument.lean:30` and `lean/exp/EtaReplication.lean:53`),
         and is the factor-share claim kept separate and labelled, given that `exp/eta.lean`'s
         `P_half` docstring says η does NOT enter the tick→price map?
     (g) verifying every backticked Lean identifier in the file resolves to a real declaration:
         `joint_corner_degeneracy`, `priceEta`, `priceEta_one`, `p_eta_eq_P_half_rescaled`,
         `L_eta`, `P_half`, `lam`. A doc row naming a non-existent lemma is worse than no row.

   - **Reviewer 2 — the specialist.** Pick the closest AI-agency specialist to
     **quantitative finance / market microstructure / AMM economics** — the risk in this artifact is
     economic misstatement of what curvature does to arbitrage and to investor demand, not LaTeX.
     **State the pick and the reason in the review file.** Charged with: is the curvature index a
     defensible discrete analogue of "the rate of change of the marginal exchange rate"? Is the
     two-sidedness (both ratios falling in curvature) stated in a way a market microstructure
     reader would accept, and is the resulting interior optimum argued correctly (the peak comes
     from two-sidedness, NOT from weighting)? Is the claim that the fee corner LOWERS optimal
     curvature economically coherent? Is transporting a two-period continuum equilibrium onto a
     discrete tick grid with per-tick liquidity honestly bounded, or does the doc read as if
     Capponi's results were PROVED for our AMM? Is calling `η` a "substitution elasticity"
     (plank todo #227's phrasing) correct — for a weighted-geometric CFMM the elasticity of
     substitution is 1 and `η` is the FACTOR SHARE, so the loose phrasing should be tightened here
     rather than propagated.

3. **Write `.planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`** with, in order: the
   specialist pick + reason; `## Reviewer 1 — Reality Checker` and `## Reviewer 2 — <name>`
   sections carrying the verbatim severity-sorted findings; then a `## Resolution` table with
   columns `| Severity | Finding | Disposition | Where fixed |`.

4. **Resolve every BLOCKER and MAJOR** by editing the addendum, then re-run BOTH gate directions
   (the addendum must still PASS, the Phase-11 addendum must still FAIL with the Rule-1 message). MINORs may be accepted
   with a one-line reason in the table. **If a reviewer finding contradicts 12-RESEARCH.md, the
   PDF (`/tmp/capponi.txt`) decides** and the disposition records which source won — the same
   doc-over-plan adjudication 11-02 and 11-04 used.

5. If a BLOCKER cannot be resolved without a user decision — in particular anything touching the
   factor-share half of the η identity, which the user has already ruled on at the level of intent
   — do NOT paper over it. Record it as `ESCALATE` and carry it into Task 3's approval request as
   an explicit question.
  </action>

  <verify>
    <automated>test -f .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md && grep -q 'Reviewer 1 — Reality Checker' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md && grep -qE '^## Reviewer 2 — .+' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md && grep -q '## Resolution' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md && ! grep -qiE '^\| *(BLOCKER|MAJOR) *\|.*\| *(open|unresolved|TODO|—|-) *\|' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md && bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`
    - `grep -q 'Reviewer 1 — Reality Checker' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`
    - `grep -qE '^## Reviewer 2 — .+' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md` and the file matches `grep -qiE 'chosen because|reason for this specialist'`
    - `grep -q '## Resolution' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`
    - Findings were actually produced: `grep -qE 'BLOCKER|MAJOR|MINOR' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`
    - No unresolved blocking rows: `! grep -qiE '^\| *(BLOCKER|MAJOR) *\|.*\| *(open|unresolved|TODO|—|-) *\|' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`
    - The reviewers checked against the PDF: `grep -qiE 'capponi\.txt|pdftotext|A\.3[0-9]|A\.4[0-9]|A\.5[0-9]' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`
    - Both gate directions still hold: `bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` exits 0 AND `! bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md > /tmp/eta-canary.out 2>&1 && grep -q 'INVERTED Rule 1' /tmp/eta-canary.out`
    - The whitelist did not grow during resolution: `[ "$(grep -n -- '<!-- notation-map -->' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md | tail -1 | cut -d: -f1)" -lt "$(grep -n -- '\*\*E1\.' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md | head -1 | cut -d: -f1)" ]`
    - `[ "$(git status --porcelain lean/ model/exp/ | wc -l)" = "0" ]`
    - `! grep -rE '/home/|\$HOME|~/' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`
  </acceptance_criteria>

  <done>Both reviewers ran in parallel and blind on the addendum, the specialist pick is named and justified, the transcription was checked against the PDF rather than the research summary, every BLOCKER and MAJOR has a recorded disposition and a fix in the addendum, and both directions of the notation gate still hold with an unweakened whitelist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HEAVY USER APPROVAL of the ETA doc block, then insertion + sha pin + peer handoff</name>

  <read_first>
    - model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md (the artifact being approved)
    - .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md (the verdicts to present, including any ESCALATE rows)
    - model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md — the header form the approved file must match: `STATUS: APPROVED & APPLIED <date> ... per user approval (todo.md ## LEAN4 - MATH)`
    - ../plank/notes/VOLATILITY_INSTRUMENTS.md — the tail of the file (now ~813 lines: M9 ~757, M10 ~770, the user-authored `## FLAIR & MEV` stub at 786 with its `\eta ...` body at 788, `## JIT` at 790); the ETA blocks land at the user-decided placement (default: the stub), and NO M-block byte may change
    - CLAUDE.md ownership map (agent `ul2inqpl` owns the plank worktree; this session writes but does not commit there)
  </read_first>

  <files>model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md, ../plank/notes/VOLATILITY_INSTRUMENTS.md, ../plank/todo.md, .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md</files>

  <action>
This is a BLOCKING user-approval checkpoint. Execute the procedure in `<how-to-verify>` exactly:
record the plank HEAD sha and the pre-insertion M-block bytes, present the addendum and the reviewer
verdicts, ask the verbatim approval question, and WAIT. Write nothing to `../plank/` before the user
replies. On `approved`, perform steps 1–8 of `<how-to-verify>`. On `rejected`, stop and record the
reason under `## User disposition` in `12-01-REVIEW.md`.
  </action>

  <verify>
    <automated>grep -q 'STATUS: APPROVED & APPLIED' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md && grep -qF 'varrho_I' ../plank/notes/VOLATILITY_INSTRUMENTS.md && grep -qF '<!-- END ETA -->' ../plank/notes/VOLATILITY_INSTRUMENTS.md && grep -q '## User disposition' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md && grep -qE '^APPROVED-ETA-SHA256: [0-9a-f]{64}$' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md && [ "$(git status --porcelain lean/ model/exp/ | wc -l)" = "0" ]</automated>
  </verify>

  <what-built>
    A complete, reviewer-gated curvature-controller specification: blocks E0–E8 in insert-ready
    LaTeX, transcribing Capponi–Jia's Lemma 3, Proposition 5 and Proposition 6 under the remaps
    χ / ϱ_I / ϱ_S with his probabilities absorbed into ϖ_* constants, protecting η end-to-end,
    stating χ* as a KINK with no first-order condition anywhere, giving the closed form
    η* = ln((1+ϱ_I)/(1+φ))/(Δi²·ln λ) by inverting the curvature bijection, contrasting the Phase-11
    degeneracy by name, and carrying five explicit OPEN labels. Nothing has been written to the
    plank-owned document yet.
  </what-built>

  <how-to-verify>
    FIRST, before showing anything, capture the pre-state (it is the evidence for step 7):
    ```bash
    git -C ../plank rev-parse HEAD > /tmp/plank-head-before.txt
    awk '/\*\*M0\./{f=1} f&&/^## \*\*M9\./{f=0} f{print}' ../plank/notes/VOLATILITY_INSTRUMENTS.md | sha256sum > /tmp/mblocks-before.txt
    ```
    (M0 → end of M8 ONLY — stop BEFORE `## **M9.`, never M0→EOF: the scope is
    placement-independent and immune to the parallel JIT/TAU edits below M8.)

    Present to the user, in this order:

    1. The **full text** of `model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`.
    2. A **three-line diff summary** of what changes in `../plank/notes/VOLATILITY_INSTRUMENTS.md`:
       the ETA blocks (N lines) land at the proposed placement — DEFAULT: the user-authored
       `## FLAIR & MEV` stub, whose single-line `\eta ...` body is REPLACED while the user's
       section title is kept; the M-block bytes (M0 → end of M8) are unchanged (so the Phase-11
       `APPROVED-DOC-SHA256` M-block diff still holds).
    3. Both reviewer verdicts from `12-01-REVIEW.md`, with every `ESCALATE` row raised as an
       explicit question.
    4. The **four decisions that are hardest to reverse**, each stated as a question:
       - a. The notation map: Capponi `k`→`χ`, `α`→`ϱ_I`, `β`→`ϱ_S`, `f` identified with `φ`, and
            `θ`/`κ_*` NEVER NAMED but absorbed into `ϖ_A, ϖ_I, ϖ_H, ϖ_D`. Approve?
       - b. The transcription route (Option C): Capponi's economics is transcribed as one-dimensional
            algebra over `χ` with all equilibrium content frozen into constants, and OUR bridge
            `χ(η) = 1 − λ^(−Δi²η/2)` transports the interior optimum to `η*`. **The equilibrium
            transfer — that our tick-grid AMM actually HAS Capponi's closed forms — is an
            ASSUMPTION and is labelled OPEN.** Approve shipping it as an assumption?
       - c. **The η-identity split.** Your 2026-07-31 decision (priceEta's η and eta.md's η ∈ (0,1)
            are the same parameter under different normalizations) is honored as a REQUIRED bridge,
            but the block states it as TWO claims: (i) the exponent identity
            `priceEta η Δi i = p_η(λ,Δi,η/2,i)`, which is provable algebra and where the factor 2 IS
            the normalization; and (ii) the factor-share identification with `L_η = X^η Y^{1−η}`,
            which is a modelling claim — `exp/eta.lean`'s own `P_half` docstring says η does NOT
            enter the tick→price map. Approve stating (ii) separately (as derived, or as OPEN)
            rather than folding it into (i)?
       - d. **No first-order condition anywhere.** `χ*` is a branch point where the derivative jumps.
            This SUPERSEDES `lean/exp/DynamicsOptimization.optimal_controls`, which hypothesizes an
            interior η* and characterizes it by an FOC in a different model. Approve?
       - e. **THE PLACEMENT.** DEFAULT: replace the `\eta ...` body of your `## FLAIR & MEV` stub
            (line 786) with the ETA blocks, keeping your section title. ALTERNATIVE: append them
            as a new `## ETA` section at end-of-file, after `## JIT`. Which placement?

    Then ask, verbatim:

      "APPROVE inserting blocks E0–E8 into ../plank/notes/VOLATILITY_INSTRUMENTS.md at the
       placement chosen in (e)? Reply `approved` to insert as-is,
       `approved with changes: <...>` to insert with edits, or `rejected: <reason>` to hold.
       Nothing is written to the plank worktree until you reply."

    ON `approved` (or after applying the requested changes and re-running BOTH gate directions):
    1. INSERT the approved blocks into `../plank/notes/VOLATILITY_INSTRUMENTS.md` at the placement
       the user chose in (e) — default: replacing the `\eta ...` body of the `## FLAIR & MEV` stub
       (the ONLY pre-existing line that may change), keeping the user's section title; alternative:
       appended as a new `## ETA` section after `## JIT`. The inserted blocks end with the
       `<!-- END ETA -->` line. Modify no other existing line.
       Do NOT `git commit` in the plank worktree.
    2. Append a handoff entry under `## LEAN4 - MATH` in `../plank/todo.md` naming this tree's
       addendum as the source of record, noting that committing the plank file is the plank agent's
       action, and cross-referencing **the η note at ../plank/todo.md line 227** (a line number,
       not an item number — the η controller application) as the consumer this phase will answer
       at 12-04.
    3. Update the addendum header to
       `> STATUS: APPROVED & APPLIED <YYYY-MM-DD> — blocks E0–E8 inserted into ../plank/notes/VOLATILITY_INSTRUMENTS.md at the user-decided placement per user approval (todo.md ## LEAN4 - MATH). Committing the plank file is the plank agent's.`
    4. **PROVE THE M-BLOCKS DID NOT MOVE** — the Phase-11 pins must survive:
       ```bash
       awk '/\*\*M0\./{f=1} f&&/^## \*\*M9\./{f=0} f{print}' ../plank/notes/VOLATILITY_INSTRUMENTS.md | sha256sum
       ```
       (M0 → end of M8 ONLY, stopping BEFORE `## **M9.` — never M0→EOF) must equal
       `/tmp/mblocks-before.txt`'s hash, which was taken with the SAME scope; record both. If they
       differ, REVERT the insertion and escalate — an M-block byte change invalidates two Phase-11
       pins and requires re-approval there, not here.
    5. **PIN THE APPROVED BYTES.** Append to `12-01-REVIEW.md` under `## User disposition`:
       - the user's **verbatim** resume-signal reply, in a fenced block;
       - the ETA-section hash, taken from the END-marker-delimited extraction of the PLANK file
         (not the whole file — a parallel workstream may append a TAU section later, and the
         whole-file hash would then mismatch for an irrelevant reason; this is exactly the trap
         11-04 hit and correctly diagnosed):
         ```bash
         awk '/\*\*E0\./{f=1} f{print} /<!-- END ETA -->/{f=0}' ../plank/notes/VOLATILITY_INSTRUMENTS.md \
           | sha256sum | cut -d' ' -f1
         ```
         written on its own line in the exact form `APPROVED-ETA-SHA256: <64 hex chars>` — this
         exact key is what plans 12-02 and 12-04 grep for;
       - `sha256sum model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` in the form
         `APPROVED-ADDENDUM-SHA256: <64 hex chars>`.
       Both hashes are taken AFTER step 3's header edit, i.e. of the final approved bytes.
    6. Run the notation gate against the inserted section itself (NOT the whole plank file — it
       legitimately contains bare `\alpha`, `\beta`, `\theta`, `\kappa` and `\nu` elsewhere):
       ```bash
       awk '/\*\*E0\./{f=1} f{print} /<!-- END ETA -->/{f=0}' ../plank/notes/VOLATILITY_INSTRUMENTS.md > /tmp/eta-section.md
       bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh /tmp/eta-section.md
       ```
       Record which form was used.
    7. Notify agent `ul2inqpl` via `claude-peers send_message` with: the file paths, the recorded
       `APPROVED-ETA-SHA256`, the fact that the insert is uncommitted on their side, that the
       M-block bytes were proven unchanged, and that any further edit to the `## ETA` section
       invalidates the hash and requires re-approval. Confirm `git -C ../plank rev-parse HEAD`
       still equals `/tmp/plank-head-before.txt`.
    8. Commit ONLY this tree:
       `git add model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md .planning/phases/12-eta-tradeoff-optimum/` then
       `git commit -m "docs(12-01): ETA curvature doc spec — Capponi-anchored addendum, reviewer-gated, user-approved"`

    ON `rejected`: stop. Do not write to `../plank/`. Record the reason in `12-01-REVIEW.md` under
    a `## User disposition` heading and end the plan; the phase cannot proceed to 12-02.
  </how-to-verify>

  <resume-signal>Type `approved`, `approved with changes: <...>`, or `rejected: <reason>`</resume-signal>

  <acceptance_criteria>
    - `grep -q 'STATUS: APPROVED & APPLIED' model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md`
    - The section landed: `grep -qF 'varrho_I' ../plank/notes/VOLATILITY_INSTRUMENTS.md` and `grep -qF '2103.08842' ../plank/notes/VOLATILITY_INSTRUMENTS.md` and `grep -qF '<!-- END ETA -->' ../plank/notes/VOLATILITY_INSTRUMENTS.md`
    - The section header of the decided placement exists exactly once: under the DEFAULT (stub) placement `[ "$(grep -c '^## FLAIR & MEV' ../plank/notes/VOLATILITY_INSTRUMENTS.md)" = "1" ]` and its `\eta ...` body is gone; under the end-of-file alternative `[ "$(grep -c '^## ETA' ../plank/notes/VOLATILITY_INSTRUMENTS.md)" = "1" ]`
    - The M-blocks survived byte-identically: the step-4 restricted hash equals the pre-insertion hash (both recorded in `12-01-REVIEW.md`)
    - The pre-existing MEV content is intact: `grep -qF 'lambda_{\text{sandwich}}' ../plank/notes/VOLATILITY_INSTRUMENTS.md`
    - `grep -q 'LEAN4 - MATH' ../plank/todo.md` and that file mentions `VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` and `227`
    - The approved bytes are pinned: `grep -qE '^APPROVED-ETA-SHA256: [0-9a-f]{64}$' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md` and `grep -qE '^APPROVED-ADDENDUM-SHA256: [0-9a-f]{64}$' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md`
    - The recorded ETA hash is the CURRENT plank section: `[ "$(awk '/\*\*E0\./{f=1} f{print} /<!-- END ETA -->/{f=0}' ../plank/notes/VOLATILITY_INSTRUMENTS.md | sha256sum | cut -d' ' -f1)" = "$(grep -m1 '^APPROVED-ETA-SHA256:' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md | awk '{print $2}')" ]`
    - The gate passes on the inserted section: `awk '/\*\*E0\./{f=1} f{print} /<!-- END ETA -->/{f=0}' ../plank/notes/VOLATILITY_INSTRUMENTS.md > /tmp/eta-section.md && bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh /tmp/eta-section.md`
    - This session did NOT commit in the plank worktree: `[ "$(git -C ../plank rev-parse HEAD)" = "$(cat /tmp/plank-head-before.txt)" ]`
    - `grep -q '## User disposition' .planning/phases/12-eta-tradeoff-optimum/12-01-REVIEW.md` and that section contains a fenced block matching `approved|rejected`
    - Committed in THIS tree: `git show --stat --oneline HEAD | grep -q 'VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md'`
    - `[ "$(git status --porcelain lean/ model/exp/ | wc -l)" = "0" ]`
  </acceptance_criteria>

  <done>The user explicitly approved the E0–E8 block; it is inserted as a new `## ETA` section of the plank-owned document (uncommitted there) with the Phase-11 M-block bytes proven unchanged, the approved bytes are pinned by an END-marker-delimited sha256 alongside the user's verbatim reply, the addendum header records APPROVED & APPLIED, `ul2inqpl` is notified, and only this tree was committed.</done>
</task>

</tasks>

<verification>
- `bash .planning/phases/12-eta-tradeoff-optimum/eta-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_ETA_ADDENDUM.md` exits 0.
- The SAME gate FAILS on `model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md` WITH the
  Rule-1 failure message in its output (the PIT-E1 canary — a gate that passes there is not
  enforcing the inversion, and a message-less nonzero exit could be a crash rather than a fail).
- The `<!-- notation-map -->` marker appears only above the `**E1.` header (gate Rule 11).
- No first-order condition, stationary point or FOC characterization appears anywhere (gate Rule 7).
- `ν`, `θ`, `κ` and Capponi's unsubscripted `α`/`β` appear nowhere outside the whitelist.
- The plank file's M-block bytes are unchanged, so both Phase-11 sha pins still hold.
- `git status --porcelain lean/ model/exp/` is empty across every commit of this plan.
- `12-01-REVIEW.md` carries both verdicts, a named specialist with a reason, a Resolution table with
  no unresolved BLOCKER/MAJOR, the user's verbatim disposition, and the two `APPROVED-*-SHA256:`
  lines pinning the approved bytes.
</verification>

<success_criteria>
The curvature-controller specification is APPROVED and present as the `## ETA` section of
`VOLATILITY_INSTRUMENTS.md`; η is protected and Capponi's colliding symbols are mechanically
excluded by a script that was not weakened and that provably rejects the Phase-11 document; the
interior optimum is stated as a KINK with no first-order condition anywhere; the η-normalization
bridge is split into its provable and its modelling halves; five OPEN items are labelled as OPEN;
and the approved bytes are pinned by an END-marker-delimited hash that a later parallel insertion
cannot invalidate.
</success_criteria>

<output>
After completion, create `.planning/phases/12-eta-tradeoff-optimum/12-01-SUMMARY.md`.
</output>
