---
phase: 23-postgres-foundation-byte-exact-schema
plan: 05
type: execute
wave: 5
depends_on: ["23-04"]
files_modified:
  - offchain/test/Main.hs
autonomous: true
requirements: [DB-02, DB-03, DB-04, BYTE-01, BYTE-02, BYTE-05, KEY-07]
user_setup: []

must_haves:
  truths:
    - "cabal test FAILS — never skips — when store-conformance.json is absent, naming the capture command"
    - "Editing a migration .sql without re-capturing reddens the suite, because freshness is RECOMPUTED from the repo's own files rather than read off a timestamp"
    - "The artifact's law-verdict keys are asserted against expected_store_laws in BOTH directions, so a skipped law is a set mismatch rather than a count shortfall"
    - "The recorded negative control shows a SilentlyCorrupted member returning fewer bytes than went in — not merely that something threw"
    - "The jsonb exhibit is asserted to show the two digests DIFFERING; equal digests redden, because equality means the exhibit stopped exercising jsonb"
    - "PGSTORE_DSN and STORE_CONFORMANCE are each registered in advertised_overrides with the consumer proven to fail loudly naming the resolved path"
    - "Every leaf of store-conformance.json is either asserted by a check or pardoned in absorbed_by_design with a written reason"
  artifacts:
    - path: "offchain/test/Main.hs"
      provides: "the Tier-C checks over store-conformance.json, two new OverrideProbes, the fifth swept artifact and its measured floors"
      contains: "store_conformance_is_present_and_fresh"
  key_links:
    - from: "offchain/test/Main.hs"
      to: "offchain/rig/store-conformance.json"
      via: "read through Store.Config.store_conformance_path so STORE_CONFORMANCE can redirect it"
      pattern: "store_conformance_path"
    - from: "offchain/test/Main.hs"
      to: "offchain/migrations/*.sql"
      via: "the freshness oracle recomputes each file's md5 and compares to the recorded list"
      pattern: "migrations_dir"
    - from: "offchain/test/Main.hs"
      to: "swept_artifacts"
      via: "store-conformance.json becomes the fifth artifact the sentinel harness mutates"
      pattern: "STORE_CONFORMANCE"
---

<objective>
Make the committed evidence load-bearing. Ten checks over `store-conformance.json`, two new
override probes, and the fifth entry in the sentinel falsification harness — after which every
value plan 23-04 measured is asserted by something, and anything nobody asserts is REPORTED by
name rather than sitting in the file unread.

Purpose: this is the half of DB-03 the roadmap's `CFMM_REQUIRE_DB` design left entirely open. That
design's failure mode is a TRUNCATED artifact, which nothing catches; this design's failure mode is
a STALE one, which the computed freshness oracle catches. The artifact is COMMITTED, so a fresh
checkout has it, so "FAIL, never skip" costs nothing and is unconditional.

Output: `cabal test` green, with the count MEASURED, and every one of the nineteen research guards
now OBSERVED firing at least once across the phase.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-postgres-foundation-byte-exact-schema/23-RESEARCH.md
@.planning/phases/23-postgres-foundation-byte-exact-schema/23-04-SUMMARY.md
@offchain/rig/store-conformance.json
@offchain/lib/Store/Config.hs
@offchain/lib/Store/Schema.hs
@offchain/test/Main.hs

<interfaces>
<!-- The suite's own instruments. Reuse these shapes; do not invent parallel mechanisms. -->

`read_json_file` — FAIL, never skip, and NAME the command that produces it
(`offchain/test/Main.hs:2022-2035`):

```haskell
read_json_file :: FilePath -> String -> IO (Either String Value)
read_json_file path advice = do
  present <- doesFileExist path
  if not present
    then pure (Left ("no " ++ path ++ " -- " ++ advice))
    else do ...
```

JSON accessors (`Main.hs:1996-2020`): `json_field :: String -> Value -> Either String Value`,
`json_string`, `json_integer`, `json_bool`, `json_array`. `json_field`'s miss message already
lists the present keys.

`OverrideProbe` and the sweep (`Main.hs:3550-3646`). `probe_override` asserts THREE things per
variable: (1) the resolver returns the override VERBATIM; (2) it returns something DIFFERENT from
the unset default — *"otherwise a resolver that ignored its input but happened to name the same
path would pass (1)"*; (3) pointing it at a nonexistent path makes the CONSUMER fail, and the
failure text must NAME the resolved path — *"(1) and (2) alone would be satisfied by a resolver
whose result nothing reads, which is the defect restated one layer down."*

```haskell
data OverrideProbe = OverrideProbe
  { ov_var :: String, ov_resolve :: IO FilePath, ov_probe :: IO (Maybe String) }

advertised_overrides :: [OverrideProbe]
advertised_overrides =
  [ OverrideProbe "RIG_MANIFEST" rig_manifest_path rig_probe
  , OverrideProbe "RIG_PINS" rig_pins_path rig_probe
  , OverrideProbe "RIG_CHEAT_SWAP_PROOF" proof_file (json_probe proof_file proof_command)
  , OverrideProbe "DRIVER_CAPTURE" capture_path (json_probe capture_path driver_capture_command)
  ]
```
The probe path shape is `/nonexistent-override-probe/<VAR>.json` (built inside `probe_override`).

The sentinel harness (`Main.hs:4998-5470`): six `sentinels` (empty-string, numeric-zero,
zero-address, zero-word, git-null-object-id, json-null); `swept_artifacts :: [MutableArtifact]`
with `MutableArtifact { ma_var, ma_resolve, ma_label }`; `absorbed_by_design :: [(String,
[(String, Int)], String)]` — field path, per-sentinel counts, and a WRITTEN REASON;
`sentinel_pair_floor :: Int` (currently 2457, four artifacts);
`artifact_field_floors :: [(String, Int)]`. The harness mutates a scratch COPY outside the tree
and reaches the artifact only through the environment override — `outside_repo` makes a write
inside the tree a hard error.

`normalize_field` turns `steps[3].e3.tick` into `steps[].e3.tick`, so an array-element path is
allowlisted once with a COUNT.

Existing reusable reason constants: `reason_generated_at` (21-02 measured `generatedAt` is not a
regeneration witness), `reason_provenance`, `reason_oracle_output`.

From 23-01, `offchain/lib/Store/Config.hs`: `pgstore_dsn`, `pgstore_dsn_env_var`,
`store_conformance_path`, `store_conformance_env_var`, `migrations_dir`.
From 23-03, `offchain/lib/Store/Schema.hs`: `expected_migrations`, `identity_constraint_name`,
`identity_constraint_columns`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Tier-C checks — the artifact becomes load-bearing</name>
  <files>offchain/test/Main.hs</files>
  <read_first>
    - `offchain/rig/store-conformance.json` in full — the exact key names and shapes plan 23-04
      emitted. Write the checks against the FILE, not against this plan's sketch of it.
    - `offchain/test/Main.hs:1996-2035` — `json_field`, `json_string`, `json_integer`, `json_bool`,
      `json_array`, `read_json_file`.
    - `offchain/test/Main.hs:2091-2137` — `rpin05_capture_is_present_and_fresh`, the idiom the
      freshness check copies, and its haddock explaining why `generatedAt` is NOT a witness.
    - `offchain/lib/Store/Schema.hs` and `offchain/lib/Store/Config.hs`.
    - `.planning/phases/23-postgres-foundation-byte-exact-schema/23-RESEARCH.md`
      §Requirement → Test Map — every Tier-C row.
  </read_first>
  <action>
Add these checks to `offchain/test/Main.hs` and register EVERY one in `core_checks`. All of them
read the artifact through `Store.Config.store_conformance_path`, so `STORE_CONFORMANCE` redirects
them and the sentinel harness in Task 2 can reach them. All of them FAIL — never skip — when the
artifact is absent, naming `bash offchain/rig/capture-store-conformance.sh`. Define
`store_conformance_command :: String` once and use it in every advice string.

**(1) `store_conformance_is_present_and_fresh`** — the freshness oracle, COMPUTED not stamped:
  - the artifact is present and decodes (via `read_json_file`, so absence is a named failure);
  - `sc_complete == True`, failing with *"the capture did not reach the end; it writes sc_complete
    False first and flips it last, so this is a truncated run, not a stale one"*;
  - `sc_law_count == length expected_store_laws`, reporting both numbers;
  - `schema_version == 1`;
  - **and the real instrument:** for every entry in the artifact's `migrations` array, RECOMPUTE
    that file's md5 from `Store.Config.migrations_dir` and compare. Report per-file
    `recorded=<x> recomputed=<y>`. Also assert the recorded filename SET equals
    `map snd expected_migrations` in BOTH directions — a migration added to the repo but absent
    from the artifact is a stale capture, and a migration in the artifact but absent from the repo
    is a deleted file nobody re-captured after.
  Its haddock states, quoting the file's own precedent, that `generatedAt` is NOT a regeneration
  witness in this repository (21-02, `reason_generated_at`), so freshness is COMPUTED from the
  repo's own bytes.

**(2) `store_conformance_verdicts_are_all_pass`** — over `law_verdicts`:
  - every value is the exact string `"pass"`, naming any that is not WITH its recorded message;
  - the verdict KEY SET equals `expected_store_laws`, asserted in BOTH directions: a key with no
    expected law fails, and an expected law with no key fails. This is what makes skip-inflation
    structurally unrepresentable — a skipped law is a MISSING VERDICT, i.e. a set mismatch, not a
    count that came up short. State that reasoning in the haddock.

**(3) `bare_bytestring_is_observed_corrupting_the_artifact`** — the negative control, asserted on
VALUES and never on "an exception was thrown". Over the `corpus` array:
  - every member of `adversarial_corpus` appears, by name, in both directions;
  - each member's recorded `behaviour` equals its `cm_behaviour` in `Store.Types`;
  - `binary_out_len == in_len` and `binary_out_sha256 == in_sha256` for EVERY member — the
    `Binary` path is lossless for all seven;
  - for every member tagged `SilentlyCorrupted`: `bare_outcome == "returned"` AND
    `bare_out_len /= in_len` AND `bare_out_sha256 /= in_sha256`. The failure message must say:
    *"the discriminating member was recorded as having round-tripped, or as having thrown. A
    SqlError is shaped exactly like a dead connection and cannot distinguish the guard firing from
    the database being down; only a WRONG VALUE returned with no complaint can."*
  - for every member tagged `ServerRejects`: `bare_outcome == "SqlError"`;
  - **and the anti-collapse assertion:** at least one member is recorded `"returned"` with a
    differing length. If EVERY member is recorded `SqlError`, the corpus has lost its
    discriminating member and the check reddens with that named diagnosis.

**(4) `store_conformance_digests_match_the_pinned_source_digest`** — recompute, in Haskell, the
sha256 of each `cm_bytes` from `adversarial_corpus` and compare to the artifact's `in_sha256`.
The expected side comes from the CORPUS DEFINITION in source, never from the artifact — a digest
read out of the same file as the thing it digests is the tautology this repo has already shipped.
Also assert `jsonb_exhibit.raw_in_sha256 == volume_path_golden_sha256` and
`jsonb_exhibit.raw_len == volume_path_golden_bytes_len`, both pinned bare in `Store.Types`.

**(5) `jsonb_round_trip_of_the_real_shape_is_exhibited_failing`** — over `jsonb_exhibit`:
  - `raw_out_sha256 == raw_in_sha256` (BYTE-01: the bytea column returned what it was given);
  - `doc_text_sha256 /= raw_out_sha256` (BYTE-02), failing with: *"the exhibit records the jsonb
    projection and the bytea artifact as byte-identical. That does not mean jsonb is safe — it
    means the exhibit has stopped exercising jsonb, and the guard has lost its subject. jsonb does
    not preserve whitespace, key order or duplicate keys and re-renders numbers through numeric;
    MEASURED three times independently. Re-take the capture and investigate before touching this
    check."*;
  - all three digest strings are 64 lowercase hex characters and none begins `0x`.

**(6) `store_conformance_records_a_nonzero_exit_on_checksum_drift`** — `checksum_drift_exit == 1`,
failing with: *"postgresql-migration RETURNS MigrationError \"Checksum mismatch\" and the PROCESS
STILL EXITS 0 — MEASURED. A recorded 0 means run_migrations_or_exit's exitFailure is not on the
path, and DB-01 is unmet."* Also assert `checksum_drift_exit_without_guard` is recorded and shows
the wart, so the reason the guard exists stays legible in the evidence.

**(7) `store_conformance_records_the_second_migrator_applying_nothing`** —
`second_migrator_try_lock == False` and `second_migrator_applied == 0` and
`advisory_lock_key == 872304`, with a haddock recording that `postgresql-migration` 0.2.1.8
contains NO advisory lock (source-read: zero `advisory` hits) so this is the caller's guard.

**(8) `store_conformance_records_two_runs_from_an_empty_database`** — `empty_db_run1` records a
success and `empty_db_run2_applied == 0`. Haddock: `actions/checkout`'s `clean: true` runs
`git clean -ffdx`, so "migrate from a completely empty database" is CI's NORMAL case, not an edge.

**(9) `store_conformance_records_the_pinned_image_and_server_version`** — `image_tag` is exactly
`"postgres:18-alpine"` and `server_version` starts with `"18."`. This is DB-04's assertion: the
provisioning is pinned and the pin was actually honoured by the server that answered.

**(10) `unique_constraint_names_all_three_columns`** — EXTEND the pure check plan 23-03 added so
it also asserts the artifact's `unique_constraint.name == identity_constraint_name` and
`unique_constraint.columns == identity_constraint_columns`. The file half (23-03) and the LIVE
CATALOGUE half (here) are both required: the file half catches a bad DDL, the catalogue half
catches a DDL that was never applied.

**(11) `no_credential_is_present_in_a_tracked_file`** — the `sc3_literal_purge` idiom with its own
POSITIVE CONTROL. Pattern: `postgres://|PGPASSWORD|password=|POSTGRES_PASSWORD=[^\"$]`. Scan
`offchain/` over `.hs`, `.sh`, `.sql`, `.json`. The positive control seeds a temp file with a real
DSN-shaped literal, runs the IDENTICAL argument vector, and asserts grep EXITS 0 and NAMES the
seeded file — absence may not read as success until the pattern has been shown matching. Any
legitimate match (the capture script's `-e POSTGRES_PASSWORD="$GENERATED"` form) must be
constructed so the pattern does NOT match a literal value; if it does, fix the script, not the
pattern.
  </action>
  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/23-05-t1.log && grep -cE '^offchain/[^ ]*:[0-9]+:[0-9]+: warning:' /tmp/23-05-t1.log; cabal test 2>&1 | tee /tmp/23-05-t1-test.log | tail -5; grep -c '^FAIL ' /tmp/23-05-t1-test.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'store_conformance_is_present_and_fresh\|store_conformance_verdicts_are_all_pass\|bare_bytestring_is_observed_corrupting_the_artifact\|store_conformance_digests_match_the_pinned_source_digest\|jsonb_round_trip_of_the_real_shape_is_exhibited_failing\|store_conformance_records_a_nonzero_exit_on_checksum_drift\|store_conformance_records_the_second_migrator_applying_nothing\|store_conformance_records_two_runs_from_an_empty_database\|store_conformance_records_the_pinned_image_and_server_version\|no_credential_is_present_in_a_tracked_file' offchain/test/Main.hs`
      returns at least 20 (ten definitions + ten `core_checks` registrations).
    - `grep -c 'store_conformance_path' offchain/test/Main.hs` returns at least 1 — every read goes
      through the override-resolving path, never a hardcoded constant.
    - `grep -c 'migrations_dir' offchain/test/Main.hs` returns at least 1 — freshness is computed
      from the repo's own `.sql` files.
    - `grep -cE 'generatedAt' offchain/test/Main.hs | head` — confirm by reading that NO Tier-C
      check compares `generatedAt` to anything; it is provenance only.
    - `grep -cE 'Store\.Postgres|connectPostgreSQL|CFMM_REQUIRE_DB' offchain/test/Main.hs` returns
      exactly 0. `cabal test` opens no socket, structurally.
    - `cabal build --enable-tests -j all` exits 0 with zero `offchain/` warning lines.
    - `grep -c '^FAIL ' /tmp/23-05-t1-test.log` returns 0 and `cabal test` exits 0. Record the
      `N/N checks passed` line and its arithmetic against 23-03's.
    - **GUARD #15 (freshness) FIRING, OBSERVED:** append `-- freshness probe` to
      `offchain/migrations/002_byte_corpus.sql`, run `cabal test`, confirm
      `FAIL store_conformance_is_present_and_fresh` reporting `recorded=<x> recomputed=<y>` for
      that file. Remove the line; confirm `sha256sum` restored and the suite green. Verbatim FAIL
      line in the summary.
    - **GUARD #16 (completeness) FIRING, OBSERVED:** copy the artifact to a scratch path OUTSIDE
      the tree, set `sc_complete` to `false` in the copy, run
      `STORE_CONFORMANCE=<copy> cabal test`, confirm
      `FAIL store_conformance_is_present_and_fresh` naming the truncation. Then delete one entry
      from the copy's `law_verdicts` and confirm
      `FAIL store_conformance_verdicts_are_all_pass` reports it as a SET mismatch (a missing
      verdict), not as a count. Confirm the committed artifact's sha256 is UNCHANGED throughout.
    - **ABSENCE FIRING, OBSERVED:** `STORE_CONFORMANCE=/nonexistent-probe/x.json cabal test` FAILS
      — never skips — and the message names
      `bash offchain/rig/capture-store-conformance.sh`. Verbatim line in the summary.
    - **POSITIVE CONTROL OBSERVED** for `no_credential_is_present_in_a_tracked_file`: the seeded
      DSN-shaped literal was matched and its file named by the identical argument vector.
  </acceptance_criteria>
  <done>Ten Tier-C checks assert every value plan 23-04 measured, freshness is recomputed from the repo rather than stamped, the law verdicts are a two-directional SET, and absence, staleness and truncation were each OBSERVED reddening the suite.</done>
</task>

<task type="auto">
  <name>Task 2: The two override probes, the fifth swept artifact, and the re-MEASURED floors</name>
  <files>offchain/test/Main.hs</files>
  <read_first>
    - `offchain/test/Main.hs:3541-3646` — `OverrideProbe`, `advertised_overrides`,
      `every_advertised_override_is_honoured`, `probe_override`, and the haddock recording the
      three measured advertised-and-dead instances (22-03 `RIG_MANIFEST`, 22-04
      `RIG_CHEAT_SWAP_PROOF`, 22-07 `RIG_PINS`).
    - `offchain/test/Main.hs:4986-5074` — `sentinels`, `MutableArtifact`, `swept_artifacts`,
      `unswept_artifacts`, `outside_repo`.
    - `offchain/test/Main.hs:5240-5470` — the `reason_*` constants, `absorbed_by_design`,
      `normalize_field`, `sentinel_pair_floor`, `artifact_field_floors`.
    - `.planning/phases/23-postgres-foundation-byte-exact-schema/23-04-SUMMARY.md` — the recorded
      LEAF COUNT, which is the budget input for this task.
  </read_first>
  <action>
**(a) Two new `OverrideProbe` entries** in `advertised_overrides`:

```haskell
  , OverrideProbe "STORE_CONFORMANCE" store_conformance_path
      (json_probe store_conformance_path store_conformance_command)
  , OverrideProbe "PGSTORE_DSN" <resolver> <probe>
```

`STORE_CONFORMANCE` uses the existing `json_probe` helper unchanged — it resolves a FilePath and a
consumer reads it, which is exactly `DRIVER_CAPTURE`'s shape.

`PGSTORE_DSN` resolves a DSN STRING, not a path, so `ov_resolve :: IO FilePath` does not fit it
directly. Resolve this honestly rather than by contorting the type: EITHER extend `OverrideProbe`
with a variant that carries an `IO String` resolver and a consumer that must fail, OR — simpler
and preferred — give `PGSTORE_DSN` a probe whose consumer is the capture tool's own DSN validation
made available as a pure-ish function in `Store.Config`
(`validate_dsn :: String -> Either String ()`, rejecting a DSN that names a nonexistent path-like
probe value), so the `/nonexistent-override-probe/PGSTORE_DSN.json` value produces a failure whose
text CONTAINS that string. Whichever you choose, all three of `probe_override`'s assertions must
hold for `PGSTORE_DSN`: verbatim resolution, differs-from-default, and a consumer that fails
NAMING the resolved value. **Do not weaken `probe_override` to accommodate it.** If neither
option can honestly satisfy assertion (3), do NOT register `PGSTORE_DSN` — instead record it in a
new, asserted `unprobed_overrides :: [String]` list with a written reason, in the
`unswept_artifacts` idiom (a named gap is honest; a registered-but-vacuous probe is the exact
defect this sweep exists to catch). State which route you took, and why, in the summary.

**(b) The fifth swept artifact.** Add to `swept_artifacts`:

```haskell
  , MutableArtifact "STORE_CONFORMANCE" store_conformance_path "store-conformance.json"
```

Then run `cabal test` and read the harness's report. For EVERY leaf it reports as ABSORBED, either
add a check that asserts it (preferred) or add an `absorbed_by_design` entry with a WRITTEN REASON
in the existing `reason_*` style. Reuse `reason_generated_at` for
`store-conformance.json.generatedAt` and `reason_provenance` where it fits; author new reason
constants for anything else, each explaining WHY the field is legitimately unasserted rather than
merely noting that it is. An entry with no reason, or a reason that says "not needed", is not
acceptable — the whole value of this list is that it is a written record of a decision.

**(c) Re-MEASURE both floors. Never increment by guesswork.**
  - `sentinel_pair_floor`: run the harness and read the ACTUAL pair count it reports with five
    artifacts. Set the constant to that number and update its haddock to say
    "MEASURED at 23-05 with five artifacts" (it currently says four).
  - `artifact_field_floors`: add `("store-conformance.json", <N>)` where `<N>` is the MEASURED
    per-artifact field count from the same run. Re-verify the four existing entries are still
    satisfied and record whether any moved.

**(d) BUDGET THE RUNTIME, and act on it.** Record `cabal test` wall-clock BEFORE and AFTER adding
the fifth artifact. The harness re-runs `core_checks` once per (leaf x 6 sentinels); 23-04's
recorded leaf count times six is the added work. If the AFTER time exceeds **15 minutes**, narrow
the artifact rather than accepting it: drop the per-member `in_sha256`/`binary_out_sha256` pairs
for the `RoundTripsAnyway` members (they are, by their own measured behaviour, the members that
prove nothing), re-run the capture, and re-measure both floors. Record the before/after times and
whether you narrowed. Do NOT solve a runtime problem by removing the `SilentlyCorrupted` member or
by dropping the artifact from `swept_artifacts`.

**(e) Close the phase's guard ledger.** Compile, in the summary, the full nineteen-row table from
23-RESEARCH.md §"Every guard, and the input that makes it fire", with a column naming the plan and
the observation where each was seen firing, and the verbatim evidence line. Any guard with no
observation is a phase-level FINDING and must be named as such — a guard that has never been seen
rejecting is treated as absent.
  </action>
  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/23-05-t2.log && grep -cE '^offchain/[^ ]*:[0-9]+:[0-9]+: warning:' /tmp/23-05-t2.log; /usr/bin/time -f 'WALL %e' cabal test 2>&1 | tee /tmp/23-05-t2-test.log | tail -8; grep -c '^FAIL ' /tmp/23-05-t2-test.log; grep -E 'WALL|checks passed' /tmp/23-05-t2-test.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"STORE_CONFORMANCE"' offchain/test/Main.hs` returns at least 2 (the `OverrideProbe`
      and the `MutableArtifact`).
    - `grep -c '"PGSTORE_DSN"' offchain/test/Main.hs` returns at least 1 — either in
      `advertised_overrides` or in the new `unprobed_overrides` list with a reason. The summary
      states which and why.
    - `grep -c 'store-conformance.json' offchain/test/Main.hs` returns at least 2.
    - `grep -A8 'artifact_field_floors ::' offchain/test/Main.hs` shows FIVE entries, the new one
      naming `store-conformance.json`.
    - `sentinel_pair_floor`'s value differs from `2457` and its haddock says "five artifacts" — a
      floor left at the four-artifact number would be satisfied by the new artifact dropping out
      entirely, which is the substitution the per-artifact floors exist to stop.
    - Every `absorbed_by_design` entry whose field path begins `store-conformance.json.` has a
      reason string that is non-empty and explains WHY, verified by reading each one. Count them
      and record the count.
    - `grep -c '^FAIL ' /tmp/23-05-t2-test.log` returns 0; `cabal test` exits 0. Record the final
      `N/N checks passed` line.
    - The `WALL` figure is recorded before and after adding the fifth artifact. If AFTER exceeds
      900 seconds, the summary states what was narrowed and shows the re-measured figure.
    - **GUARD #13/#14 (override) FIRING, OBSERVED:** `every_advertised_override_is_honoured` passes
      with the new entries, and the summary records — for `STORE_CONFORMANCE` — the verbatim
      consumer failure text showing it CONTAINS
      `/nonexistent-override-probe/STORE_CONFORMANCE.json`.
    - **GUARD #18 (sentinel) FIRING, OBSERVED:** the harness's own negative control
      (`__sentinel_harness_probe`) still passes, and the summary records at least one leaf of
      `store-conformance.json` that the harness reported CAUGHT — proving the new artifact is
      actually reaching the checks and that its "absorbed" verdicts are not vacuous.
    - `git status --porcelain .github src test foundry-scripts Makefile foundry.toml` is EMPTY.
  </acceptance_criteria>
  <done>Both new overrides are registered (or honestly recorded as unprobed with a reason), the conformance artifact is the fifth swept artifact with every leaf asserted or pardoned in writing, both floors are RE-MEASURED at five artifacts, the runtime is budgeted and recorded, and the phase's nineteen-guard ledger is closed.</done>
</task>

</tasks>

<verification>
- `cabal build --enable-tests -j all` exits 0 with ZERO `offchain/` warning lines; `cabal test`
  exits 0 with zero `^FAIL ` lines. The bare `cabal build -j all` is VACUOUS and is never evidence.
- Four independent falsifications were OBSERVED and their verbatim FAIL lines recorded: artifact
  ABSENT (fails, never skips, naming the capture command); artifact STALE (a `.sql` edited without
  re-capture); artifact TRUNCATED (`sc_complete` false); a law verdict MISSING (a set mismatch, not
  a count shortfall). The committed artifact's sha256 is unchanged after all four.
- `grep -cE 'Store\.Postgres|connectPostgreSQL|CFMM_REQUIRE_DB' offchain/test/Main.hs` returns 0.
- The nineteen-guard ledger is complete, or every unobserved guard is named as a phase finding.
- Territory respected: `git status --porcelain .github src test foundry-scripts Makefile foundry.toml`
  is EMPTY.
</verification>

<success_criteria>
1. `cabal test` is GREEN with no database present, and its store checks still discriminate — proven
   by four observed falsifications, not by the suite being green.
2. Freshness is COMPUTED from the repo's own `.sql` files; editing a migration without re-capturing
   reddens the suite.
3. The law verdicts are a SET asserted in both directions, so a skipped law is unrepresentable
   rather than merely detectable.
4. The negative control asserts a WRONG VALUE returned with no complaint, never merely that
   something threw.
5. `store-conformance.json` is the fifth swept artifact with every leaf asserted or pardoned in
   writing, and both sentinel floors are re-MEASURED at five artifacts.
6. All nineteen research guards have a recorded firing observation, or are named as findings.
</success_criteria>

<output>
After completion, create
`.planning/phases/23-postgres-foundation-byte-exact-schema/23-05-SUMMARY.md`, carrying as MEASURED
values:
  - the final `N/N checks passed` line and the arithmetic from 23-01's cold baseline through every
    plan;
  - the verbatim FAIL line from each of the four falsifications (absent / stale / truncated /
    missing verdict), plus the before/after sha256 of `store-conformance.json`;
  - the re-MEASURED `sentinel_pair_floor` and the five `artifact_field_floors` entries;
  - `cabal test` WALL before and after the fifth artifact, and whether the artifact was narrowed;
  - which route `PGSTORE_DSN` took (registered probe vs named gap) and why;
  - **the nineteen-guard ledger**: guard, firing input, plan, verbatim evidence — with any
    unobserved guard named as a phase-level finding rather than omitted.
</output>
