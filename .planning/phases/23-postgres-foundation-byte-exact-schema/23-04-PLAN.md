---
phase: 23-postgres-foundation-byte-exact-schema
plan: 04
type: execute
wave: 4
depends_on: ["23-03"]
files_modified:
  - offchain/app/StoreConformance.hs
  - offchain/rig/capture-store-conformance.sh
  - offchain/rig/volume-path-golden.json
  - offchain/rig/store-conformance.json
  - cfmm-replicationPlank-rpc-api.cabal
autonomous: true
requirements: [DB-01, DB-04, BYTE-01, BYTE-02, BYTE-05, KEY-07]
user_setup: []

must_haves:
  truths:
    - "The adversarial corpus round-trips byte-identically through a real Postgres via the Binary path, and the SAME corpus through the bare-ByteString path is OBSERVED returning 3 bytes where 6 went in, with no error raised"
    - "The real 606-byte volume_path.json is exhibited round-tripping through bytea at its pinned sha256 and FAILING that same comparison through jsonb"
    - "A deliberately corrupted migration checksum makes the runner exit NON-ZERO"
    - "A second migrator, while the first holds the advisory lock, gets f from pg_try_advisory_lock and applies zero migrations"
    - "A row under key_scheme 1 is not found under key_scheme 2 against a real Postgres, and the same (model, key) under scheme 2 inserts"
    - "The live pg_catalogue reports the unique constraint over exactly model, key_scheme, key"
    - "The capture refuses to emit an artifact at all when the database is unreachable — CFMM_REQUIRE_DB lives HERE, not in cabal test"
  artifacts:
    - path: "offchain/app/StoreConformance.hs"
      provides: "the capture executable — runs every law and every DB-requiring observation against Store.Postgres"
      contains: "store_laws"
    - path: "offchain/rig/capture-store-conformance.sh"
      provides: "docker-provisioned Postgres on a non-default port, per-run database, CFMM_REQUIRE_DB enforcement"
      contains: "postgres:18-alpine"
    - path: "offchain/rig/volume-path-golden.json"
      provides: "the real 606-byte GAMS artifact, copied in with provenance"
      min_lines: 1
    - path: "offchain/rig/store-conformance.json"
      provides: "the committed evidence cabal test asserts over"
      contains: "law_verdicts"
  key_links:
    - from: "offchain/app/StoreConformance.hs"
      to: "Store.Laws.store_laws"
      via: "the SAME laws that ran against Memory now run against Postgres"
      pattern: "store_laws"
    - from: "offchain/rig/capture-store-conformance.sh"
      to: "offchain/rig/store-conformance.json"
      via: "the capture writes the artifact the suite reads"
      pattern: "store-conformance\\.json"
---

<objective>
Stand a real Postgres up in Docker, run every DB-requiring observation against it, and commit the
result as evidence `cabal test` can assert over while staying chain- and database-independent.

Purpose: this is the ONLY place in Phase 23 that needs a server. Seven of the nineteen guards can
only be OBSERVED here — the `Binary` wart's silent corruption, the `jsonb` exhibit, the
checksum-drift exit code, the concurrency lock, the double run from an empty database, the
`key_scheme` orphaning against real SQL, and the live catalogue's constraint columns. A guard that
has never been seen rejecting is treated as absent, so every one of them is DRIVEN here rather
than argued.

Output: a capture executable, a capture script, the real golden bytes, and the committed
`store-conformance.json`. `cabal test` gains nothing yet — plan 23-05 is what makes the artifact
load-bearing.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-postgres-foundation-byte-exact-schema/23-RESEARCH.md
@.planning/phases/23-postgres-foundation-byte-exact-schema/23-03-SUMMARY.md
@offchain/lib/Store/Postgres.hs
@offchain/lib/Store/Laws.hs
@offchain/lib/Store/Types.hs
@offchain/lib/Store/Schema.hs
@offchain/rig/capture-cheat-swap-proof.sh
@offchain/lib/Driver/Capture.hs

<interfaces>
<!-- Everything the executor needs. Do not go exploring. -->

From 23-03, `offchain/lib/Store/Postgres.hs`:
```haskell
new_postgres_store             :: Connection -> Store
with_connection                :: String -> (Connection -> IO a) -> IO a
run_migrations_or_exit         :: Connection -> FilePath -> IO ()
with_migration_lock            :: Connection -> IO a -> IO a
try_migration_lock             :: Connection -> IO Bool
server_version                 :: Connection -> IO String
live_identity_constraint_columns :: Connection -> IO [String]
```

From 23-02, `offchain/lib/Store/Laws.hs`:
```haskell
store_laws :: [(String, Store -> IO (Either String ()))]
law_names  :: [String]                        -- 7 names
```

From 23-01, `offchain/lib/Store/Types.hs`:
```haskell
adversarial_corpus           :: [CorpusMember]     -- 7 members, one SilentlyCorrupted
volume_path_golden_sha256    :: String             -- BARE hex, no 0x prefix
volume_path_golden_bytes_len :: Int                -- 606
sha256_hex                   :: BS.ByteString -> String
artifact_sha256              :: Artifact -> String
derived_doc_sha256           :: DerivedDoc -> String
```

The capture-script idiom to replicate — `offchain/rig/capture-cheat-swap-proof.sh:34-62`:
```bash
set -euo pipefail
cd "$(git rev-parse --show-toplevel)"
# --- Preconditions: fail loudly, never default -----------------------------
if [ ! -f "$MANIFEST" ]; then
  echo "CAPTURE FAIL: no manifest at $(realpath -m "$MANIFEST")" >&2
  echo "              stand the rig up first: bash offchain/rig/deploy-rig.sh" >&2
  exit 1
fi
```

The MEASURED facts this capture must reproduce, not assume:
  - `a\101b` = `61 5c 31 30 31 62` (6 bytes) through the BARE path returns `61 41 62` (3 bytes),
    NO error. `0x00` / `0xFF` / `0xC3 0x28` through the bare path raise
    `ERROR: invalid byte sequence for encoding "UTF8"`. CRLF and a trailing newline round-trip
    CORRECTLY through the broken path and prove nothing.
  - `pg_try_advisory_lock(872304)` returns `f` while another session holds it, `t` after release.
  - `postgresql-migration` returns `MigrationError "Checksum mismatch"` and the PROCESS EXITS 0 —
    the non-zero exit is `run_migrations_or_exit`'s `exitFailure`, and the observation is
    `echo $?` == 1.
  - `postgres:18-alpine` was ready in 3 s; `server_version` 18.4;
    `standard_conforming_strings=on`; `bytea_output=hex`.
  - The real `volume_path.json` is 606 bytes, sha256
    `e7b14f384ab4c027be5450218a52040110d45dbaddbbfb0bb7bd5ab707d0d884`, ends `5d 0a 7d 0a`
    (`"]\n}\n"` — trailing newline PRESENT), byte-identical at two independent paths.
</interfaces>
</context>

<constraints>
- **Do not run the capture inside `cabal test`,** and do not let any driver rewrite a tracked
  artifact it is being checked against. The CI workflow already documents why: it converts a
  staleness failure into a silent pass.
- **`.github/` belongs to the CI track and must not be edited by this workstream.** No CI change is
  needed: under the three-tier decision nothing in `cabal test` touches a database, so the gate
  does not need Postgres provisioning for Phase 23 to be green. If a later phase needs it, that is
  a coordination item for the CI track, not a task here.
- **Do not read the golden bytes across worktrees at test time.** Copy them in once, here, with
  provenance recorded. Reading `cfmm-gams` from this tree is the Phase-28 ownership problem
  arriving early.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: The capture executable — every law and every DB-only observation, driven</name>
  <files>offchain/app/StoreConformance.hs, cfmm-replicationPlank-rpc-api.cabal</files>
  <read_first>
    - `offchain/lib/Store/Postgres.hs`, `offchain/lib/Store/Laws.hs`, `offchain/lib/Store/Types.hs`,
      `offchain/lib/Store/Schema.hs`, `offchain/lib/Store/Config.hs` — the whole surface this tool
      drives.
    - `cfmm-replicationPlank-rpc-api.cabal:151-165` — the `executable cheat-swap-proof` stanza,
      the exact shape this new stanza copies.
    - `offchain/lib/Driver/Capture.hs:93-98` — `dr_complete` / `dr_configured_size`, the
      completeness idiom `sc_complete` / `sc_law_count` reproduces.
    - `.planning/phases/23-postgres-foundation-byte-exact-schema/23-RESEARCH.md`
      §"Every guard, and the input that makes it fire" — the seven rows this tool must drive.
  </read_first>
  <action>
Add an `executable store-conformance` stanza to the `.cabal`, copying the `cheat-swap-proof`
stanza's shape (`main-is: StoreConformance.hs`, `hs-source-dirs: offchain/app`,
`import: warnings`), depending on `base`, the library, `aeson`, `bytestring`, `text`,
`postgresql-simple`, `postgresql-migration`, `process`, `directory`, `filepath`.

`aeson` IS permitted in this executable — it writes the JSON REPORT, never the artifact bytes.
State that in the file header: the report is a description of an experiment, the `raw` column is
the experiment, and the two never meet. BYTE-03's grep is scoped to
`offchain/lib/Store/*.hs`, not here, for exactly this reason.

Create `offchain/app/StoreConformance.hs`. It takes the DSN from `Store.Config.pgstore_dsn`, runs
`run_migrations_or_exit` against `Store.Config.migrations_dir`, then performs and RECORDS the
following. Every block is DRIVEN, never asserted from a constant.

**(a) `law_verdicts`** — run all seven `store_laws` against `new_postgres_store con`, each against
a freshly-migrated per-law schema state (truncate both tables between laws so one law's writes
cannot satisfy another's read). Record `{"<law name>": "pass"}` or
`{"<law name>": "FAIL: <the law's own message>"}`. Record `sc_law_count = length store_laws`.

**(b) `corpus`** — for EVERY member of `adversarial_corpus`, drive BOTH paths and record what
happened, so the reader can tell WHICH failure was observed:

```json
{ "name": "octal-escape",
  "behaviour": "SilentlyCorrupted",
  "in_len": 6,
  "in_sha256": "<bare hex>",
  "binary_out_len": 6,
  "binary_out_sha256": "<bare hex, EQUAL to in_sha256>",
  "bare_outcome": "returned",
  "bare_out_len": 3,
  "bare_out_sha256": "<bare hex, DIFFERENT>" }
```

  - the `Binary` path uses `store_put_blob`/`store_get_blob`;
  - the BARE path issues the same insert with the parameter passed as a naked `ByteString` — a
    deliberate, commented, single-use raw query inside this tool, NOT a change to
    `Store.Postgres`. Catch `SqlError` and record `"bare_outcome": "SqlError"` with the message,
    setting `bare_out_len` to `-1`. Never let an exception escape and never let one stand in for
    a value-level observation.
  - `bare_outcome` is one of exactly `"returned"`, `"SqlError"`, `"absent"`.
  - This is the ONLY place the silent-corruption fact is produced. Assert nothing here; record.

**(c) `jsonb_exhibit`** — read `offchain/rig/volume-path-golden.json` (Task 2 puts it there),
insert it as a `model_run` row via `store_put`, and record:

```json
{ "raw_len": 606,
  "raw_in_sha256":  "<bare hex>",
  "raw_out_sha256": "<bare hex — MUST equal raw_in_sha256>",
  "doc_text_sha256": "<bare hex — MUST DIFFER>" }
```

`doc_text_sha256` comes from `store_doc_sha256`, i.e. from `convert_to(doc::text,'UTF8')` — never
from aeson, never from a re-serialization in Haskell. If the two digests come out EQUAL, do NOT
"fix" it: record it and let plan 23-05's check redden, because equal digests mean the exhibit has
stopped exercising `jsonb` and the guard has lost its subject.

**(d) `migration_checks`** — four DRIVEN observations:
  - `empty_db_run1` / `empty_db_run2_applied`: against a database created moments ago, run the
    migrations, then run them AGAIN; record that the first succeeded and the second applied 0.
  - `checksum_drift_exit`: copy `offchain/migrations/` to a scratch directory OUTSIDE the working
    tree, append one comment line (`-- drift probe`) to the copy's `001_model_run.sql`, run the
    migration command against the already-migrated database pointed at the scratch directory as a
    SUBPROCESS, and record its `ExitCode` as an integer plus the stderr line. MUST be 1. The
    subprocess is essential: `MigrationError` is a VALUE, and only a real process exit can
    demonstrate DB-01. Also record `checksum_drift_exit_without_guard` by calling `runMigrations`
    directly in-process and recording that it returned `MigrationError` while the process was
    still alive — the measured wart, in the artifact, so the reason the guard exists is legible.
    **Mutate only the scratch COPY. `offchain/migrations/` in the working tree is never written.**
  - `second_migrator_try_lock` (`false` expected) and `second_migrator_applied` (`0` expected):
    open connection A, take `with_migration_lock`, and from a SECOND connection B call
    `try_migration_lock` and then attempt a migration run; record both. Deterministic — no sleeps,
    no racing.
  - `advisory_lock_key`: the integer `872304`, recorded so a future change to it is visible.

**(e) `unique_constraint`** — from `live_identity_constraint_columns`, i.e. the LIVE catalogue:
`{"name": "model_run_identity", "columns": ["model", "key_scheme", "key"]}`. Reading the live
catalogue rather than the `.sql` file is the point: the file half is already asserted in 23-03,
and this is the half that proves the file was actually applied.

**(f) `migrations`** — `[{"filename": "001_model_run.sql", "md5": "<32 hex>"}, ...]` COMPUTED from
the repo's own `.sql` files at capture time. This is the input to plan 23-05's freshness oracle,
which recomputes the same md5s and reddens on drift. A `generatedAt` timestamp is NOT a
regeneration witness — 21-02 measured that in this repo — so freshness is COMPUTED, never stamped.

**(g) provenance** — `schema_version` (integer `1`), `generatedAt`, `server_version` (from
`Store.Postgres.server_version`), `image_tag` (from the `STORE_PG_IMAGE` env var the script sets),
`sc_complete` (`True` only if the whole run reached the end), `sc_law_count`.

**Writing rules:**
  - `sc_complete` is written `False` FIRST and flipped to `True` only after every block succeeded,
    so a truncated capture is visible as a fact rather than as a shorter file.
  - Write to a temp path and `renameFile` into place — the atomic-write idiom already in
    `Driver.Capture`. A half-written artifact must never be readable.
  - EVERY digest is BARE lowercase hex, never `0x`-prefixed:
    `sc3_literal_purge`'s pattern is `0x[0-9a-fA-F]{64}\b` and a prefixed digest would redden the
    suite on sight.
  - The output path comes from `Store.Config.store_conformance_path` so the `STORE_CONFORMANCE`
    override is honoured by the WRITER as well as the reader.
  - Design the report NARROW. Every leaf costs six full `core_checks` re-runs in the sentinel
    harness (plan 23-05). Record nothing a check will not read.
  </action>
  <verify>
    <automated>cabal build --enable-tests -j all 2>&1 | tee /tmp/23-04-t1.log && grep -cE '^offchain/[^ ]*:[0-9]+:[0-9]+: warning:' /tmp/23-04-t1.log; cabal test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'executable store-conformance' cfmm-replicationPlank-rpc-api.cabal` returns 1 and
      `grep -c 'StoreConformance.hs' cfmm-replicationPlank-rpc-api.cabal` returns 1.
    - `find offchain/lib offchain/app offchain/test -name '*.hs' | wc -l` — every one of these is
      in a component. Verify `StoreConformance.hs` is the `main-is` of the new stanza.
    - `grep -c 'store_laws' offchain/app/StoreConformance.hs` returns at least 1 — the SAME laws,
      not a transcription.
    - `grep -c 'sc_complete\|sc_law_count' offchain/app/StoreConformance.hs` returns at least 4.
    - `grep -c 'renameFile' offchain/app/StoreConformance.hs` returns at least 1 — atomic write.
    - `grep -cE '"0x' offchain/app/StoreConformance.hs` returns 0 — digests are emitted bare.
    - `grep -c '872304' offchain/app/StoreConformance.hs` returns at least 1.
    - `grep -c 'checksum_drift_exit' offchain/app/StoreConformance.hs` returns at least 2 (the
      guarded subprocess exit AND `checksum_drift_exit_without_guard`).
    - `grep -c 'live_identity_constraint_columns' offchain/app/StoreConformance.hs` returns 1 —
      the constraint is read from the LIVE catalogue, not from the `.sql`.
    - `grep -rc 'cfmm-gams' offchain/app/StoreConformance.hs` returns 0 — no cross-worktree read.
    - `cabal build --enable-tests -j all` exits 0 with zero `offchain/` warning lines.
    - `cabal test` exits 0 at 23-03's count, unchanged — this task registers no check.
  </acceptance_criteria>
  <done>The capture executable compiles, drives every DB-only observation rather than asserting it, writes atomically with `sc_complete` flipped last, and emits every digest bare.</done>
</task>

<task type="auto">
  <name>Task 2: The capture script, the real golden bytes, and the committed artifact</name>
  <files>offchain/rig/capture-store-conformance.sh, offchain/rig/volume-path-golden.json, offchain/rig/store-conformance.json</files>
  <read_first>
    - `offchain/rig/capture-cheat-swap-proof.sh` lines 1-70 — the header discipline (WHY THIS FILE
      EXISTS / the gate is a VALUE / WHY THE ARTIFACT IS COMMITTED / USAGE), `set -euo pipefail`,
      `cd "$(git rev-parse --show-toplevel)"`, and the fail-loudly-never-default precondition block.
    - `offchain/rig/deploy-rig.sh` — the same family's conventions.
    - `.planning/phases/23-postgres-foundation-byte-exact-schema/23-RESEARCH.md` §New Measurements
      #3 (the golden bytes, both riders) and §"DB-03 ... The replacement" part 5 (why
      `CFMM_REQUIRE_DB` lives in the capture and nowhere else).
  </read_first>
  <action>
**(a) Copy the golden bytes in.** From
`/home/jmsbpp/cfmms-playground/cfmm-gams/model/mev_tax_model_one/volume_path.json` to
`offchain/rig/volume-path-golden.json`. Then VERIFY, and record:

```bash
wc -c offchain/rig/volume-path-golden.json        # must be 606
sha256sum offchain/rig/volume-path-golden.json    # must be e7b14f38...d0d884
tail -c 4 offchain/rig/volume-path-golden.json | xxd   # must be 5d 0a 7d 0a
```

If ANY of the three disagrees with `Store.Types.volume_path_golden_sha256` /
`volume_path_golden_bytes_len`, STOP and record the disagreement — the source file has moved and
the pin in Haskell source is the authority about what was measured, not the file on disk. Do not
silently update the pin to match.

Record provenance in `offchain/rig/README.md` (append a short section, do not rewrite the file):
the source path, the toolchain that produced it (GAMS 54.1 / CONOPT 4.39.0), the two-run
byte-identical reproducibility, the date, and the fact that the sha256 is pinned in
`offchain/lib/Store/Types.hs` — in HASKELL SOURCE, deliberately not beside the bytes, because a
digest recorded next to the thing it digests is a tautology and this repo has already shipped that
defect once.

**(b) Write `offchain/rig/capture-store-conformance.sh`.** `set -euo pipefail`,
`cd "$(git rev-parse --show-toplevel)"`, and a header in the `capture-cheat-swap-proof.sh` shape
stating: why the file exists, that the gate is a VALUE not a green build, why the artifact is
committed (so `cabal test` can assert over real database evidence while STAYING
database-independent), and the usage line.

Behaviour:

```bash
STORE_PG_IMAGE="${STORE_PG_IMAGE:-postgres:18-alpine}"
STORE_PG_PORT="${STORE_PG_PORT:-55433}"     # NOT 5432: a developer's local Postgres must not be
                                            # able to silently satisfy this connection
DB_NAME="cfmm_store_$$"                     # per-run database name; no shared singleton
```

  - **`CFMM_REQUIRE_DB` lives HERE.** If `docker` is absent, or the container does not become
    ready, or the connection fails: print `CAPTURE FAIL:` naming the exact cause and `exit 1`
    WITHOUT writing any artifact. A truncated artifact is caught by nothing; a STALE one is caught
    by plan 23-05's computed freshness oracle. That is the whole reason the variable moved out of
    `cabal test`, where it failed open.
  - Preconditions, each with its own named failure and its own `exit 1`: `docker` on `PATH`;
    `offchain/rig/volume-path-golden.json` present; `offchain/migrations/*.sql` present and
    non-empty.
  - `docker run -d --rm --name "$DB_NAME" -e POSTGRES_PASSWORD=... -p 127.0.0.1:$STORE_PG_PORT:5432 "$STORE_PG_IMAGE"`,
    then a bounded readiness poll (`pg_isready` or `docker exec ... pg_isready`) with a hard
    timeout that fails loudly rather than hanging. `trap 'docker rm -f "$DB_NAME" >/dev/null 2>&1 || true' EXIT`
    so the container never survives the script.
  - Export `PGSTORE_DSN` pointing at `127.0.0.1:$STORE_PG_PORT/$DB_NAME`. The password is
    generated per run in the script and never written to a tracked file.
  - `cabal run store-conformance` (or `cabal run -v0 store-conformance`).
  - AFTER the tool exits, assert the artifact is present and that
    `jq -r '.sc_complete'` is `true` and `jq -r '.law_verdicts | length'` equals
    `jq -r '.sc_law_count'`; otherwise `exit 1`. The script's own gate is a VALUE, not the tool's
    exit code alone.
  - No password, DSN, host or port LITERAL that could serve as a credential is committed; the two
    env-var defaults above are configuration, and `55433`/`postgres:18-alpine` are deliberate,
    documented, non-secret pins.

**(c) Run it and commit `offchain/rig/store-conformance.json`.**

Then DEMONSTRATE the capture's own refusal (research guard: "the capture exits non-zero naming
`docker` when it is absent — never emits a partial artifact"): run the script with `PATH` stripped
of `docker` (e.g. `env PATH=/usr/bin:/bin` in a shell where docker is elsewhere, or a temp
directory shadowing it), confirm exit 1 and a message NAMING `docker`, and confirm
`sha256sum offchain/rig/store-conformance.json` is UNCHANGED — no partial artifact was written.

**(d) Record the seven DB-only guard firings** from the produced artifact, by VALUE, in the
summary. For each, the recorded number is the observation:

| guard | the recorded field | the value that means it fired |
|---|---|---|
| `Binary` on write (value-level kill) | `corpus[octal-escape].bare_out_len` | `3` against `in_len` `6` with `bare_outcome` `"returned"` |
| `Binary` on write (secondary) | `corpus[nul].bare_outcome` | `"SqlError"` with the UTF-8 message |
| `bytea` authoritative | `jsonb_exhibit.doc_text_sha256` | DIFFERENT from `raw_out_sha256` |
| BYTE-01 round-trip | `jsonb_exhibit.raw_out_sha256` | EQUAL to `raw_in_sha256` and to the Haskell pin |
| checksum drift | `migration_checks.checksum_drift_exit` | `1` (and `..._without_guard` shows the wart) |
| concurrency | `migration_checks.second_migrator_try_lock` | `false`, `second_migrator_applied` `0` |
| empty-db double run | `migration_checks.empty_db_run2_applied` | `0` |
| `key_scheme` orphaning | `law_verdicts.law_key_scheme_orphans_rather_than_matching` | `"pass"` against Postgres |
| live constraint | `unique_constraint.columns` | `["model","key_scheme","key"]` |

If any value does NOT come out as the right-hand column says, that is a FINDING to record, not a
number to adjust. In particular: if `corpus[crlf].bare_out_sha256` EQUALS its input digest, that
is the research's measured `RoundTripsAnyway` behaviour reproducing correctly, not a bug.
  </action>
  <verify>
    <automated>bash offchain/rig/capture-store-conformance.sh && jq -e '.sc_complete == true and (.law_verdicts|length) == .sc_law_count and (.law_verdicts|to_entries|map(select(.value != "pass"))|length) == 0' offchain/rig/store-conformance.json && jq -r '.corpus[] | select(.name=="octal-escape") | "in=\(.in_len) bare=\(.bare_out_len) outcome=\(.bare_outcome)"' offchain/rig/store-conformance.json && jq -e '.jsonb_exhibit.raw_out_sha256 == .jsonb_exhibit.raw_in_sha256 and .jsonb_exhibit.doc_text_sha256 != .jsonb_exhibit.raw_out_sha256' offchain/rig/store-conformance.json && jq -e '.migration_checks.checksum_drift_exit == 1 and .migration_checks.second_migrator_try_lock == false and .migration_checks.second_migrator_applied == 0 and .migration_checks.empty_db_run2_applied == 0' offchain/rig/store-conformance.json && jq -e '.unique_constraint.columns == ["model","key_scheme","key"]' offchain/rig/store-conformance.json</automated>
  </verify>
  <acceptance_criteria>
    - `wc -c < offchain/rig/volume-path-golden.json` returns exactly `606`.
    - `sha256sum offchain/rig/volume-path-golden.json | cut -d' ' -f1` equals
      `e7b14f384ab4c027be5450218a52040110d45dbaddbbfb0bb7bd5ab707d0d884`, which equals
      `Store.Types.volume_path_golden_sha256`.
    - `xxd -p <(tail -c 4 offchain/rig/volume-path-golden.json)` returns `5d0a7d0a`.
    - `grep -c 'postgres:18-alpine' offchain/rig/capture-store-conformance.sh` returns at least 1.
    - `grep -c '5432:5432\|:5432/' offchain/rig/capture-store-conformance.sh` returns 0 — the host
      port is NOT the default; `grep -c '55433' offchain/rig/capture-store-conformance.sh` returns
      at least 1.
    - `grep -c 'set -euo pipefail' offchain/rig/capture-store-conformance.sh` returns 1 and
      `grep -c 'trap ' offchain/rig/capture-store-conformance.sh` returns at least 1.
    - `grep -c 'CFMM_REQUIRE_DB' offchain/rig/capture-store-conformance.sh` returns at least 1, and
      `grep -rc 'CFMM_REQUIRE_DB' offchain/test/Main.hs` returns 0 — the variable lives in the
      capture and nowhere near `cabal test`.
    - `grep -cE '0x[0-9a-fA-F]{8}\b|0x[0-9a-fA-F]{40}\b|0x[0-9a-fA-F]{64}\b' offchain/rig/capture-store-conformance.sh`
      returns 0.
    - The `<automated>` verify command above exits 0 in full — every one of the nine
      value-level observations holds.
    - **CAPTURE-REFUSAL FIRING, OBSERVED:** with `docker` unreachable the script exits 1, the
      message contains the word `docker`, and `sha256sum offchain/rig/store-conformance.json` is
      byte-identical before and after. Both digests recorded in the summary.
    - `cabal test` still exits 0 — this plan does not yet register a check over the artifact.
    - `jq -r 'paths(scalars) | join(".")' offchain/rig/store-conformance.json | wc -l` is recorded
      in the summary as the LEAF COUNT. Plan 23-05 needs it to budget the sentinel harness
      (~6 full `core_checks` re-runs per leaf).
  </acceptance_criteria>
  <done>The real 606-byte artifact is in this workstream with provenance, the capture script provisions Postgres on a non-default port with a per-run database and refuses to emit anything when the database is unreachable, and `store-conformance.json` is committed with all nine DB-only observations at their expected values.</done>
</task>

</tasks>

<verification>
- `cabal build --enable-tests -j all` exits 0 with ZERO `offchain/` warning lines; `cabal test`
  exits 0. The bare `cabal build -j all` is VACUOUS and is never evidence.
- `bash offchain/rig/capture-store-conformance.sh` succeeds from a cold start with no container
  running, and the `jq` gate in the `<automated>` block exits 0.
- The capture was OBSERVED refusing — exit 1 naming `docker`, artifact byte-unchanged.
- No file under `.github/` was modified: `git status --porcelain .github/` is EMPTY.
- No file under `src/`, `test/`, `foundry-scripts/`, `Makefile` or `foundry.toml` was modified:
  `git status --porcelain src test foundry-scripts Makefile foundry.toml` is EMPTY.
</verification>

<success_criteria>
1. `store-conformance.json` is committed, `sc_complete` is `true`, and all seven law verdicts are
   `pass` against a REAL Postgres.
2. The `octal-escape` member is recorded going in at 6 bytes and coming back at 3 through the bare
   path with `bare_outcome == "returned"` — a silent value-level corruption, not an exception.
3. `jsonb_exhibit` shows `raw_out_sha256 == raw_in_sha256` and
   `doc_text_sha256 != raw_out_sha256`.
4. `checksum_drift_exit == 1`, `second_migrator_try_lock == false`,
   `second_migrator_applied == 0`, `empty_db_run2_applied == 0`.
5. `unique_constraint.columns` from the LIVE catalogue is exactly
   `["model","key_scheme","key"]`.
6. The capture refuses and writes nothing when the database is unreachable, and that refusal was
   observed.
</success_criteria>

<output>
After completion, create
`.planning/phases/23-postgres-foundation-byte-exact-schema/23-04-SUMMARY.md`, carrying as MEASURED
values:
  - the nine-row guard table above with the ACTUAL recorded value in each row;
  - `wc -c`, `sha256sum` and the last four bytes of `volume-path-golden.json`;
  - the artifact LEAF COUNT (`jq -r 'paths(scalars)|join(".")' ... | wc -l`) — plan 23-05's budget
    input;
  - the wall-clock of one full capture run;
  - the before/after sha256 of `store-conformance.json` across the docker-absent refusal probe;
  - any value that did NOT come out as predicted, recorded as a finding rather than adjusted.
</output>
