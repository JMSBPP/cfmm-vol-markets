---
phase: 11-mev-hazard-inf-program
plan: 06
type: execute
wave: 6
depends_on: ["11-05"]
files_modified:
  - model/vol_markets/LEAN_TRACEABILITY.md
  - model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CTX-TRACE]

must_haves:
  truths:
    - "LEAN_TRACEABILITY section 0 carries the MEV notation rows, including the three resolved collisions and the λ_ARB / λ_MEV distinction"
    - "LEAN_TRACEABILITY section 7 carries one row per proved MEV claim, each naming a real Lean identifier"
    - "The ARB/FEE row is labelled a bridge identity, not a formalization of the anchor's Theorem 3/4"
    - "LEAN_TRACEABILITY section 6 no longer lists the MEV section as wholly OPEN; what remains open is named precisely"
    - "Every section 7 status word (PROVEN / CORRECTED / REFUTED / OPEN) matches the fidelity records, not the plan's hopes"
    - "ROADMAP Phase 11 carries the minted CTX-* requirement IDs and the real plan list; STATE records the terminal outcome"
    - "The close-out is committed and mirrored to both remotes"
  artifacts:
    - path: "model/vol_markets/LEAN_TRACEABILITY.md"
      provides: "Notation rows and claim rows binding the doc's MEV section to the two new Lean modules"
      contains: "MevOptimization"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 requirements line and plan list, closing the TBD placeholder"
      contains: "CTX-MEVDOC"
    - path: ".planning/STATE.md"
      provides: "The phase's terminal position, decisions and outcome"
      contains: "lambda_MEV"
  key_links:
    - from: "model/vol_markets/LEAN_TRACEABILITY.md"
      to: "lean/vol_markets/MevOptimization.lean"
      via: "section 7 claim rows naming real identifiers"
      pattern: "Theta_lambdaMEV_identification"
    - from: "model/vol_markets/LEAN_TRACEABILITY.md"
      to: "lean/vol_markets/MevJointProgram.lean"
      via: "the joint-program and Angstrom rows"
      pattern: "joint_corner_degeneracy"
    - from: ".planning/ROADMAP.md"
      to: "the CTX-* requirement set"
      via: "the Requirements line replacing TBD"
      pattern: "CTX-INF"
---

<objective>
Close the phase: bind every proved MEV claim to the traceability record, name precisely what stays
open, and update the roadmap and state to what actually happened.

This is the plan that decides whether the phase's record is honest. The degeneracy result and any
OPEN verdict on the volatility-varying Jensen statement must survive into the traceability table
exactly as the fidelity records state them — not softened into a success, not omitted.

Purpose: leave an auditable record that a future consumer can trust without re-deriving it.
Output: updated LEAN_TRACEABILITY sections 0/6/7, the addendum back-annotated with Lean names,
ROADMAP and STATE closed out, both remotes mirrored.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-mev-hazard-inf-program/11-03-FIDELITY.md
@.planning/phases/11-mev-hazard-inf-program/11-05-FIDELITY.md
@.planning/phases/11-mev-hazard-inf-program/11-01-SUMMARY.md
@.planning/phases/11-mev-hazard-inf-program/11-03-SUMMARY.md
@.planning/phases/11-mev-hazard-inf-program/11-05-SUMMARY.md
@model/vol_markets/LEAN_TRACEABILITY.md
@model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md
@lean/vol_markets/MevOptimization.lean
@lean/vol_markets/MevJointProgram.lean
@.planning/ROADMAP.md
@.planning/STATE.md

<constants>
STATUS VOCABULARY (LEAN_TRACEABILITY header, use these words only):
  PROVEN    - sorry-free, axiom-clean
  CORRECTED - the doc claim was wrong; the true statement is proven
  REFUTED   - counterexample proven
  OPEN      - not yet formalized

THE MINTED REQUIREMENT SET (closes ROADMAP's "Requirements: TBD"):
  CTX-MEVDOC   - the lambda_MEV LaTeX spec in VOLATILITY_INSTRUMENTS.md ### MEV, user-approved
  CTX-PTRADE   - the fee-decreasing kernel and its lemmas + the MMR split bridge identity
  CTX-MEVHAZ   - mevHazard / mevMulti discrete functionals + the CPMM instantiation
  CTX-INF      - Theta_{lambda_MEV} identification + the SOLVED infimum program
  CTX-JOINT    - the joint program: degeneracy + the constrained (Jensen) reformulation
  CTX-ANGSTROM - statement-level bridge: tau-rebate + argmin invariance, the Delta-t cadence lever,
                 sandwich nulling
  CTX-TRACE    - traceability rows and the close-out
  CTX-REVIEW   - the two-reviewer gate on every pre-submission spec artifact

SECTION 6 currently reads: "The abstract G_phi group beyond the probOr monoid core, and the MEV
  section (empty in the doc). OPEN." This must be rewritten - the MEV section is no longer empty
  and lambda_MEV is no longer wholly unformalized - but the rewrite must name what REMAINS open
  rather than deleting the entry. NOTE: the clause is WRAPPED across lines in the file, so the
  removal check greps the unwrapped fragment "section (empty in the doc)", not the full sentence.

PUSH: git push origin feat/lean4-spec ; git subtree push --prefix=lean lean4-spec main
  (the subtree push only carries lean/; the model/ and .planning/ changes go to origin only -
   state this in the summary rather than expecting lean4-spec to show them)
</constants>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LEAN_TRACEABILITY sections 0, 6 and 7 + addendum back-annotation</name>

  <read_first>
    - model/vol_markets/LEAN_TRACEABILITY.md IN FULL — the header's status vocabulary, section 0's notation table and its explicit `η` reservation sentence, section 6's OPEN list (note the line-wrapping around the MEV clause), and section 7's row format (`| Doc claim | Lean | Status |`) including the FLAIR rows whose phrasing the MEV rows mirror
    - .planning/phases/11-mev-hazard-inf-program/11-03-FIDELITY.md — the AUTHORITATIVE list of what landed from bundle A, including any Aristotle-renamed theorem, whether `ptrade_strictConvexOn` landed strict, and whether the optional exact-CPMM kernel `ARBoverV_exact` (T19) exists
    - .planning/phases/11-mev-hazard-inf-program/11-05-FIDELITY.md — the AUTHORITATIVE list for bundle B, and the `## T24 verdict` section which determines whether the general constrained claim is PROVEN, REFUTED or OPEN
    - lean/vol_markets/MevOptimization.lean and lean/vol_markets/MevJointProgram.lean — grep the real identifiers; every name written into a section 7 row must exist in one of these files
    - model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md — the approved blocks M0-M8, which this task back-annotates with the Lean lemma names (the precedent format is VOLATILITY_INSTRUMENTS_LEAN_ADDENDUM.md, where each block ends with the backticked lemma names it rests on)
  </read_first>

  <files>model/vol_markets/LEAN_TRACEABILITY.md, model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md</files>

  <action>
1. **Section 0 — notation rows.** Add rows to the notation table for the MEV symbols, and add one
   sentence beneath the existing `η` reservation recording the resolutions:
   - `φ` (the fee) — note that the anchor paper's `γ` is this document's `φ`, and that this
     document's `γ_j` remains the sigmoid steepness.
   - `Δt` — mean interblock time; the anchor's Poisson block rate is written through it precisely
     because this document's `λ` is the hazard rate.
   - `P_trade` -> `MevOptimization.ptrade`.
   - `λ_ARB` -> `MevOptimization.mevMulti` / `mevHazard` — the ARBITRAGE channel, and the object
     every identification and infimum row below is about.
   - `λ_MEV` -> `MevJointProgram.mevTotal` — the TOTAL, `λ_ARB ⊕ λ_sandwich` (doc block M7);
     equal to `λ_ARB` exactly when uniform batch clearing nulls the sandwich channel. **The two
     symbols must not be presented as interchangeable in any row.**
   - `a_t` -> the arbitrage-opportunity weight argument of `mevHazard` (the leading-order LVR),
     distinguished from `flairHazard`'s traded-flow `w_t`.
   - `τ` -> `MevJointProgram.mevNet` / `taxFraction`, a protocol parameter OUTSIDE `Θ_φ`.
   State explicitly that the anchor's composite parameter is deliberately unnamed.

2. **Section 7 — claim rows.** Append rows in the existing three-column format, one per claim,
   each naming REAL identifiers taken from the two modules. Suggested rows (adjust names to what
   actually landed):
   - `λ_ARB` kernel and functionals -> `ptrade`, `mevHazard`, `mevMulti`, `mevMulti_nonneg`,
     `mevWeight_cpmm_pos`
   - `P_trade` antitone / strictly convex / bounded / `Δt`-monotone -> `ptrade_strictAntiOn`,
     `ptrade_strictConvexOn`, `ptrade_convexOn`, `ptrade_mem_Ioc`, `ptrade_eq_one_iff`,
     `ptrade_monotoneOn_dt`, `ptrade_tendsto_atTop`
   - **the ARB/FEE split -> `arb_add_fee_eq_lvr`. This row's text MUST contain the words
     `bridge identity` and MUST state that it is a ring tautology
     (`x·p + x·(1−p) = x`) which lets the anchor's Theorem 3 / Theorem 4 split be written in Lean
     notation — it is NOT a formalization of those theorems, which are asymptotic approximations
     and are NOT formalized anywhere in this phase.** Do not write "MMR Theorem 3/4 -> PROVEN".
   - `Θ_{λ_MEV} ⊂ Θ_φ` identification -> the antitone/isotone block +
     `Theta_lambdaMEV_identification`, with the mandatory note that **there is NO affine
     identification** (the FLAIR mirror breaks here) — this is a `CORRECTED`-flavoured row relative
     to the naive mirror expectation and must say so
   - `inf λ_ARB` solved -> `mevMulti_ge_corner`, `_corner_attained_levels`, `_saturation_limit`,
     `_strict_above_saturation`, `_exists_min_compact`
   - joint program, unconstrained -> `joint_corner_degeneracy`, `joint_beta_degeneracy`,
     `joint_scalarization_degeneracy`; status `PROVEN`, with the row text stating the RESULT: the
     program is degenerate and the shape block is not essential unconstrained — **the phase brief's
     expectation is REFUTED**
   - joint program, constrained -> `mev_ge_flat_under_flair_budget*`; status taken VERBATIM from
     `11-05-FIDELITY.md`'s `## T24 verdict`: `PROVEN` if the volatility-varying statement landed,
     `REFUTED` if `mev_ge_flat_under_flair_budget_false` landed with witnesses, or a `PROVEN` row
     for the const-σ case PLUS a separate `OPEN` row naming the volatility-varying case as
     unformalized
   - Angstrom bridge -> `mevNet`, `mevNet_argmin_invariant`, `taxFraction`, the `Δt` cadence lever
     `mev_mono_dt`, `mevTotal`, `mevTotal_eq_arb_of_sandwich_zero`; the row must record that `τ`
     and `Δt` are outside `Θ_φ` and that the tax constants are parametric because the sources
     disagree
   - the exact-CPMM kernel: if T19 landed, a `PROVEN` row naming `ARBoverV_exact` and its
     hypotheses (including that it is the ONLY object carrying `σ²Δt < 8`); if not, an `OPEN`
     row marked "deliberately optional, non-blocking"

3. **Section 6 — rewrite the MEV entry.** Replace the "MEV section (empty in the doc). OPEN."
   clause with a precise statement of what remains, in the style the FLAIR parenthetical already
   uses. It must name: the continuum path-integral form of `λ_ARB` (the discrete functional is the
   deliverable, as with `λ_FLAIR`); the demand-elasticity / optimal-fee equilibrium layer (which
   belongs to `FeeSchedule`, and whose exact missing term is the anchor's section 7.3 eq. (27));
   the anchor's Theorem 3 / Theorem 4 asymptotics themselves, which are quoted but NOT formalized;
   the volatility-varying constrained statement IF it is open; and the abstract `𝓖_φ` group, which
   stays open regardless. Do not delete the entry.

4. **Back-annotate the addendum.** In
   `model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`, append to each of blocks M1-M7 the
   backticked Lean identifiers that now discharge it, following the
   `VOLATILITY_INSTRUMENTS_LEAN_ADDENDUM.md` precedent (each block ends with its lemma names).
   Update the status header to record that the blocks are LEAN-BACKED as of this commit, and — if
   the T24 verdict is REFUTED or OPEN — amend block M6b's text so the doc does not claim more than
   is proven, and note in the header that block M6b requires a corresponding amendment in the
   plank-owned `VOLATILITY_INSTRUMENTS.md` (hand off to agent `ul2inqpl` via `claude-peers`; do not
   commit in the plank worktree). Note in the header that this back-annotation intentionally
   invalidates the `APPROVED-ADDENDUM-SHA256` recorded in `11-01-REVIEW.md`; the doc-fidelity gates
   of plans 11-02/11-04 have already been consumed, so no downstream check depends on it.

5. **Cross-check before finishing:** every Lean name written into a section 7 row must exist:
   for each backticked identifier in the new rows, `grep -q` it in
   `lean/vol_markets/MevOptimization.lean` or `lean/vol_markets/MevJointProgram.lean`. A row naming
   a non-existent lemma is worse than no row.

6. Do NOT modify any `.lean` file.
  </action>

  <verify>
    <automated>grep -q 'MevOptimization' model/vol_markets/LEAN_TRACEABILITY.md && grep -q 'MevJointProgram' model/vol_markets/LEAN_TRACEABILITY.md && grep -q 'ptrade' model/vol_markets/LEAN_TRACEABILITY.md && grep -qi 'bridge identity' model/vol_markets/LEAN_TRACEABILITY.md && ! grep -q 'section (empty in the doc)' model/vol_markets/LEAN_TRACEABILITY.md && [ "$(git status --porcelain lean/ | wc -l)" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
    - `grep -q 'MevOptimization' model/vol_markets/LEAN_TRACEABILITY.md` and `grep -q 'MevJointProgram' model/vol_markets/LEAN_TRACEABILITY.md`
    - Notation rows added: `grep -q 'ptrade' model/vol_markets/LEAN_TRACEABILITY.md` and `grep -q 'Δt' model/vol_markets/LEAN_TRACEABILITY.md` and `grep -q 'taxFraction' model/vol_markets/LEAN_TRACEABILITY.md`
    - The two hazard symbols are distinguished: `grep -q 'mevTotal' model/vol_markets/LEAN_TRACEABILITY.md` and `grep -qE 'λ_ARB|lambda_ARB|\\lambda_\{\\text\{ARB\}\}' model/vol_markets/LEAN_TRACEABILITY.md`
    - The `η` reservation sentence is intact: `grep -q 'is never reused' model/vol_markets/LEAN_TRACEABILITY.md`
    - Section 6's stale clause is gone (the sentence is line-wrapped in the file, so grep the unwrapped fragment): `! grep -q 'section (empty in the doc)' model/vol_markets/LEAN_TRACEABILITY.md`
    - Section 6 still lists what remains: `grep -qi 'continuum path-integral' model/vol_markets/LEAN_TRACEABILITY.md` and `grep -q '𝓖_φ' model/vol_markets/LEAN_TRACEABILITY.md`
    - Section 7 records the degeneracy as a result: `grep -q 'joint_corner_degeneracy' model/vol_markets/LEAN_TRACEABILITY.md` and `grep -qi 'degenerat' model/vol_markets/LEAN_TRACEABILITY.md`
    - The ARB/FEE row is labelled honestly: `grep -qi 'bridge identity' model/vol_markets/LEAN_TRACEABILITY.md` and the line carrying `arb_add_fee_eq_lvr` also matches `grep -i 'bridge identity\|tautolog'`
    - The no-affine correction is recorded: `grep -qi 'no affine' model/vol_markets/LEAN_TRACEABILITY.md`
    - Every backticked Lean identifier in the new rows exists in a module: `for n in $(grep -oE '`[a-zA-Z_][a-zA-Z0-9_]*`' model/vol_markets/LEAN_TRACEABILITY.md | tr -d '`' | grep -E '^(ptrade|mev|joint|taxFraction|Theta_lambdaMEV|arb_add|ARBoverV)' | sort -u); do grep -qr "$n" lean/vol_markets/MevOptimization.lean lean/vol_markets/MevJointProgram.lean || exit 1; done`
    - Only sanctioned status words used in the new rows: `! grep -nE '\*\*(PARTIAL|MOSTLY|LIKELY|ESSENTIALLY)' model/vol_markets/LEAN_TRACEABILITY.md`
    - The addendum is back-annotated: `grep -q 'MevOptimization' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - `[ "$(git status --porcelain lean/ | wc -l)" = "0" ]`
    - `! grep -rE '/home/|\$HOME' model/vol_markets/LEAN_TRACEABILITY.md`
  </acceptance_criteria>

  <done>The traceability file binds every proved MEV claim to a real Lean identifier with a sanctioned status word taken from the fidelity records, distinguishes λ_ARB from λ_MEV, labels the ARB/FEE row a bridge identity rather than a Theorem 3/4 formalization, records the degeneracy and the absent affine identification as findings, names precisely what remains open, and the addendum carries its Lean back-annotations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ROADMAP requirements + plan list, STATE close-out, memory, push</name>

  <read_first>
    - .planning/ROADMAP.md Phase 11 section — the `**Requirements**: TBD` placeholder, `**Plans:** 0 plans`, and the `- [ ] TBD` line this task fills; also read the Phase 10 section for the exact formatting precedent
    - .planning/STATE.md — the frontmatter (`status`, `stopped_at`, `last_activity`, `progress`), the `## Current Position` block, and the `### Decisions` list; follow the Phase 10 entry format exactly, including its habit of stating negative and partial results explicitly
    - all .planning/phases/11-mev-hazard-inf-program/11-*-SUMMARY.md produced so far — the authoritative record of what happened, including any unmet requirement
    - .planning/phases/11-mev-hazard-inf-program/11-05-FIDELITY.md `## T24 verdict` — the single fact that most shapes the STATE entry
    - .planning/phases/11-mev-hazard-inf-program/11-VALIDATION.md — its per-task verification map and `nyquist_compliant` flag, now resolvable
  </read_first>

  <files>.planning/ROADMAP.md, .planning/STATE.md</files>

  <action>
1. **`.planning/ROADMAP.md`, Phase 11 section:**
   - Replace `**Requirements**: TBD` with the minted set and a one-line gloss each:
     ```
     **Requirements**: CTX-MEVDOC (λ_MEV LaTeX spec into VOLATILITY_INSTRUMENTS.md ### MEV,
     user-approved), CTX-PTRADE (fee-decreasing kernel + the MMR split bridge identity), CTX-MEVHAZ
     (mevHazard/mevMulti discrete functionals + CPMM instantiation), CTX-INF (Θ_{λ_MEV}
     identification + solved infimum), CTX-JOINT (joint program: degeneracy + constrained/Jensen),
     CTX-ANGSTROM (τ-rebate argmin invariance, Δt cadence lever, sandwich nulling), CTX-TRACE
     (traceability + close-out), CTX-REVIEW (two-reviewer gate on every spec artifact)
     ```
   - Amend the Goal paragraph where it is now known to be wrong: it currently asserts that in the
     joint program "the shape block `(β, γ)` becomes essential". If the degeneracy theorem landed,
     that expectation is REFUTED — rewrite the clause to say that the unconstrained joint program
     is degenerate and the trade-off appears only under a fixed FLAIR fee budget. Do not delete the
     original intent; record the correction.
   - Set `**Plans:** 6 plans` and replace `- [ ] TBD` with:
     ```
     Plans:
     - [ ] 11-01-PLAN.md — CTX-MEVDOC/CTX-REVIEW: λ_MEV doc spec, notation gate, two-reviewer gate, user approval
     - [ ] 11-02-PLAN.md — CTX-PTRADE/CTX-MEVHAZ/CTX-INF: bundle A + numbered prompt, gate, submit
     - [ ] 11-03-PLAN.md — integrate bundle A: build, axiom sweep, T1–T19 fidelity, push
     - [ ] 11-04-PLAN.md — CTX-JOINT/CTX-ANGSTROM: bundle B + constrained-program prompt, gate, submit
     - [ ] 11-05-PLAN.md — integrate bundle B: build, axiom sweep, T20–T30 fidelity, T24 verdict, push
     - [ ] 11-06-PLAN.md — CTX-TRACE: traceability rows, ROADMAP/STATE close-out
     ```
   - Verify every minted CTX ID appears in at least one plan's `requirements` frontmatter and that
     no plan carries an ID absent from this list:
     `grep -h '^requirements:' .planning/phases/11-mev-hazard-inf-program/11-0*-PLAN.md`

2. **`.planning/STATE.md`:**
   - Update the frontmatter `status`, `stopped_at`, `last_updated`, `last_activity` and `progress`
     to Phase 11's terminal state, in the Phase-10 entry style.
   - Add a `Plan (11-0N)` paragraph per executed plan under `## Current Position`, each stating what
     was measured or proved rather than what was attempted.
   - Add entries to `### Decisions` for the load-bearing ones, at minimum:
     - `[Phase 11]: 11-01: the three MMR notation collisions resolved as γ→φ, λ→Δt, η never named — enforced by an executable grep gate with a bounded header/M0 whitelist marker, not by convention`
     - `[Phase 11]: 11-01: λ_ARB (arb channel) and λ_MEV (total, λ_ARB ⊕ λ_sandwich) are distinct symbols; the inf program is on λ_ARB and the two coincide only under uniform batch clearing`
     - `[Phase 11]: 11-01: the doc states DEGENERACY and the CONSTRAINED result as two separate claims; the phase brief's "shape block becomes essential" expectation is refuted unconstrained`
     - `[Phase 11]: 11-02/11-04: two serial Aristotle bundles (mirror first, novel content second) rather than one — so a weakened return from A could revise B's premises before they were spent`
     - `[Phase 11]: 11-05: the volatility-varying Jensen statement verdict — <PROVED / REFUTED / OPEN, verbatim from 11-05-FIDELITY.md>`
     - `[Phase 11]: arb_add_fee_eq_lvr is a bridge identity, not a formalization of MMR Theorem 3/4 — the asymptotics themselves remain unformalized`
     - `[Phase 11]: the Angstrom tax stays parametric in τ; the repo snapshot and the live docs disagree on the constants, so no numeric enters a theorem`
   - If ANY requirement is unmet, say so in the status line in the project's established voice —
     Phase 10 recorded an unmet success criterion plainly and that is the standard here.

3. **Update `.planning/phases/11-mev-hazard-inf-program/11-VALIDATION.md`**: fill the per-task
   verification map with the real task ids, plans, waves, requirements and commands, and set
   `nyquist_compliant: true` only if every task in every plan had an `<automated>` verify.

4. **Memory:** record a phase-level entry capturing (a) the λ_ARB kernel is `ptrade` and it is a
   Möbius function of the fee, not a `max(gap − fee, 0)` shape; (b) the unconstrained joint program
   is degenerate and the trade-off lives only under a fee-budget constraint; (c) the two
   protocol levers outside `Θ_φ` are `τ` and `Δt`; (d) the T24 verdict; (e) `arb_add_fee_eq_lvr`
   is a tautology-grade bridge identity, so nobody later cites it as "MMR Theorem 3 formalized".
   Mark both `aristotle-mev-bundle-*-inflight` entries RESOLVED and the queue FREE.

5. **Commit and push:**
   ```bash
   git add model/vol_markets/ .planning/
   git commit -m "docs(11-06): λ_MEV traceability rows, ROADMAP requirements, STATE close-out — PHASE 11 CLOSED"
   git push origin feat/lean4-spec
   git subtree push --prefix=lean lean4-spec main
   ```
   The subtree push carries only `lean/`; note in the summary that `model/` and `.planning/`
   changes reach `origin` only.
  </action>

  <verify>
    <automated>grep -q 'CTX-MEVDOC' .planning/ROADMAP.md && grep -q 'CTX-JOINT' .planning/ROADMAP.md && grep -q '11-06-PLAN.md' .planning/ROADMAP.md && ! grep -A20 '### Phase 11' .planning/ROADMAP.md | grep -q 'Requirements.*TBD' && grep -qi 'Phase 11' .planning/STATE.md && git log --oneline -3 --name-only | grep -q 'ROADMAP.md'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -A25 '### Phase 11' .planning/ROADMAP.md | grep -q 'CTX-MEVDOC'` and the same block matches `CTX-PTRADE`, `CTX-MEVHAZ`, `CTX-INF`, `CTX-JOINT`, `CTX-ANGSTROM`, `CTX-TRACE`, `CTX-REVIEW`
    - `! grep -A25 '### Phase 11' .planning/ROADMAP.md | grep -q 'Requirements.*TBD'`
    - `grep -A40 '### Phase 11' .planning/ROADMAP.md | grep -q '\*\*Plans:\*\* 6 plans'`
    - All six plan lines listed: `for p in 11-01 11-02 11-03 11-04 11-05 11-06; do grep -q "$p-PLAN.md" .planning/ROADMAP.md || exit 1; done`
    - The refuted expectation is corrected in the Goal: `grep -A25 '### Phase 11' .planning/ROADMAP.md | grep -qiE 'degenerat|fee budget|constrained'`
    - Requirement coverage is closed both ways: every CTX id in the ROADMAP line appears in at least one plan's `requirements:` frontmatter (`grep -h '^requirements:' .planning/phases/11-mev-hazard-inf-program/11-0*-PLAN.md`), and no plan carries an id absent from the ROADMAP line
    - STATE updated: `grep -q 'Phase 11' .planning/STATE.md` and `grep -qE 'CTX-INF|λ_MEV|lambda_MEV' .planning/STATE.md`
    - At least six Phase-11 decisions recorded: `[ "$(grep -c '\[Phase 11\]' .planning/STATE.md)" -ge 6 ]`
    - The T24 verdict appears in STATE: `grep -qE 'PROVED|REFUTED|OPEN' .planning/STATE.md`
    - The bridge-identity caveat is recorded in STATE: `grep -qi 'bridge identity' .planning/STATE.md`
    - `grep -q 'nyquist_compliant' .planning/phases/11-mev-hazard-inf-program/11-VALIDATION.md` and the per-task map no longer contains the template placeholder `REQ-{XX}`
    - Committed: `git show --stat --oneline HEAD | grep -q 'ROADMAP.md'` and `git show --stat --oneline HEAD | grep -q 'LEAN_TRACEABILITY.md'`
    - `[ "$(git status --porcelain | wc -l)" = "0" ]` (working tree clean, this tree only)
    - `cd lean && lake build` still exits 0 (nothing in this plan touched Lean, but the phase gate requires a green build at close-out)
    - `! grep -rE '/home/|\$HOME' .planning/ROADMAP.md`
  </acceptance_criteria>

  <done>ROADMAP Phase 11 carries the minted CTX-* set, a corrected goal, and the real six-plan list; STATE records the terminal position and the load-bearing decisions including the T24 verdict and the bridge-identity caveat; the validation map is filled; memory is updated; and everything is committed and pushed.</done>
</task>

</tasks>

<verification>
- `grep -A25 '### Phase 11' .planning/ROADMAP.md` shows the CTX-* set and no `TBD`.
- Every backticked MEV lemma name in LEAN_TRACEABILITY exists in one of the two new Lean modules.
- The `arb_add_fee_eq_lvr` row reads "bridge identity" and does not claim MMR Theorem 3/4.
- `grep -q 'section (empty in the doc)' model/vol_markets/LEAN_TRACEABILITY.md` finds nothing.
- No status word outside {PROVEN, CORRECTED, REFUTED, OPEN} appears in the new rows.
- `cd lean && lake build` exits 0 and the working tree is clean.
- The close-out commit reached `origin/feat/lean4-spec`; `lean/` is mirrored at `lean4-spec/main`.
</verification>

<success_criteria>
The phase's record is complete and honest: every proved claim is traceable to a real Lean
identifier with a sanctioned status, λ_ARB and λ_MEV stay distinct, the ARB/FEE identity is not
oversold as a formalized theorem, the degeneracy finding and any OPEN verdict are recorded as
results rather than softened, the roadmap's TBD requirement placeholder is closed, and both remotes
carry the work.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mev-hazard-inf-program/11-06-SUMMARY.md`.
</output>
</content>
