---
phase: 11-mev-hazard-inf-program
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md
  - .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh
  - .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md
  - ../plank/notes/VOLATILITY_INSTRUMENTS.md
  - ../plank/todo.md
autonomous: false
requirements: [CTX-MEVDOC, CTX-REVIEW]

must_haves:
  truths:
    - "The λ_MEV mathematical spec exists as insert-ready LaTeX with minimal prose, authored in THIS tree"
    - "The three MMR notation collisions (γ→φ, λ→Δt, η→not-named) are resolved mechanically and the resolution is checkable by a script"
    - "The addendum states the DEGENERACY result and the CONSTRAINED result as two separate claims — it never promises a Θ_φ trade-off without a constraint in the same sentence"
    - "Two reviewers (Reality Checker + one specialist) ran in parallel on the addendum and every BLOCKER/MAJOR was resolved before user approval was requested"
    - "The user explicitly approved the doc block before it was inserted into VOLATILITY_INSTRUMENTS.md"
    - "The plank-owned file is written but NOT committed by this session; agent ul2inqpl is notified"
  artifacts:
    - path: "model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md"
      provides: "The insert-ready λ_MEV LaTeX blocks M0–M8, the authoritative record in this tree"
      contains: "P_{\\text{trade}}"
      min_lines: 90
    - path: ".planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh"
      provides: "Executable grep gate enforcing PIT-1/PIT-2/PIT-3 on the addendum"
      contains: "lambda"
    - path: ".planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md"
      provides: "Both reviewer verdicts, severity-sorted, with resolutions"
      contains: "BLOCKER"
    - path: "../plank/notes/VOLATILITY_INSTRUMENTS.md"
      provides: "The approved λ_MEV block inserted under ### MEV — the Aristotle input document"
      contains: "lambda_{\\text{MEV}}"
  key_links:
    - from: "model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md"
      to: "arXiv:2305.14604 Theorem 1"
      via: "citation beside the P_trade display"
      pattern: "2305\\.14604"
    - from: "model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md"
      to: "VolInstrument.multiFee (the SAME Θ_φ)"
      via: "the fee slot of the hazard"
      pattern: "multiFee"
    - from: "../plank/notes/VOLATILITY_INSTRUMENTS.md"
      to: "model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md"
      via: "the inserted block under ### MEV"
      pattern: "Theta_{\\\\lambda_{\\\\text{MEV}}}"
---

<objective>
Author the λ_MEV mathematical specification as insert-ready LaTeX, gate it with two reviewers,
obtain HEAVY USER APPROVAL, and insert it into `VOLATILITY_INSTRUMENTS.md ### MEV`.

Nothing else in this phase can start. The workflow is doc-driven: the DOC is what goes to
Aristotle, so the doc is the specification. A wrong symbol here becomes a wrong theorem two plans
later, and a doc claim the math does not deliver becomes a retraction after user approval was
already spent.

Purpose: produce the single artifact every downstream plan consumes.
Output: `VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md` (this tree, authoritative), the notation gate
script, the two reviewer verdicts, and the approved block landed in plank's doc.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-mev-hazard-inf-program/11-CONTEXT.md
@.planning/phases/11-mev-hazard-inf-program/11-RESEARCH.md
@.planning/phases/11-mev-hazard-inf-program/11-VALIDATION.md
@model/vol_markets/LEAN_TRACEABILITY.md
@model/vol_markets/VOLATILITY_INSTRUMENTS_LEAN_ADDENDUM.md
@lean/vol_markets/FlairOptimization.lean
@CLAUDE.md

<constants>
ANCHOR PAPER   : ../plank/refs/mev/MilionisMoallemiRoughgardenArbProfitsFees.pdf
                 (arXiv:2305.14604 v2, 23 Jul 2025). Theorem 1 (stationary distribution /
                 P_trade), Theorem 3 + eq. (12) (ARB ~= LVR * P_trade), Theorem 4
                 (FEE ~= LVR * (1 - P_trade)), Corollary 2 (exact CPMM, needs sigma^2*dt < 8),
                 section 7.1 (block-time lever), section 7.3 eq. (27) (LP P&L = NT_FEE - ARB).

THE KERNEL     : P_trade = sigma / (sigma + phi * sqrt(2/dt))
                 strictly DECREASING in phi, strictly CONVEX in phi, valued in (0,1],
                 = 1 iff phi = 0, -> 0 as phi -> infinity, increasing in dt and in sigma.

NOTATION RESOLUTIONS (BINDING — 11-RESEARCH F2; three collisions):
  MMR gamma (the fee)          -> write phi        (this doc's gamma_j is sigmoid steepness)
  MMR lambda (Poisson block rate) -> write Delta t  (dt = 1/lambda, MMR's own primitive;
                                                     this doc's lambda is the HAZARD rate)
  MMR eta (the composite)      -> DO NOT NAME IT AT ALL (eta is project-reserved for the
                                  pricing kernel — LEAN_TRACEABILITY section 0)

THE TWO CLAIMS, NEVER MERGED (11-RESEARCH F5/F6, PIT-6):
  (a) DEGENERACY   — unconstrained over Theta_phi, argsup lambda_FLAIR = arginf lambda_MEV at
      EVERY coordinate (level corner top AND beta -> -infinity), robust to any linear
      scalarization. The shape block is NOT essential unconstrained. This REFUTES the phase
      brief's expectation and must be stated as such.
  (b) CONSTRAINED  — at a FIXED FLAIR fee budget, convexity of P_trade makes a FLAT fee the
      lambda_MEV minimizer, and every volatility-responsive schedule with the same fee income is
      strictly worse for MEV. Proven in the aligned-measure (a == w) case; the sigma-varying
      generalization is OPEN and must be labelled OPEN.

FILE OWNERSHIP : ../plank/notes/VOLATILITY_INSTRUMENTS.md and ../plank/todo.md belong to agent
                 `ul2inqpl` (CLAUDE.md ownership map). This session may WRITE the approved block
                 into them after user approval; it must NOT `git commit` in the plank worktree.
                 Precedent: commit 489bb43 (VOLATILITY_INSTRUMENTS_LEAN_ADDENDUM.md blocks A-G).

INSERTION POINT: ../plank/notes/VOLATILITY_INSTRUMENTS.md line 461 `### MEV`; the two existing
                 bullets (Angstrom reference; "literature ... must be saved on refs/{flair,mev}")
                 are KEPT and the LaTeX blocks are appended beneath them.
</constants>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author the λ_MEV addendum (blocks M0–M8) and the notation gate script</name>

  <read_first>
    - .planning/phases/11-mev-hazard-inf-program/11-RESEARCH.md IN FULL — especially F1 (the exact closed forms to transcribe), F2 (the three collisions and their resolutions), F3 (the composition and its properties), F5 (degeneracy), F6 (the constrained/Jensen result and its honest generalization gap), F7/F8 (Angstrom, sandwich), and Common Pitfalls PIT-1..PIT-6
    - model/vol_markets/VOLATILITY_INSTRUMENTS_LEAN_ADDENDUM.md — the FORMAT precedent: numbered blocks, `[CORRECTION]`/`[ADDITION]` tags, insert-ready `\[ \begin{aligned} ... \end{aligned} \]` displays, minimal prose, each block citing the Lean lemma it rests on
    - model/vol_markets/LEAN_TRACEABILITY.md section 0 (the `η` reservation — the single hardest constraint) and section 7 (the Θ_φ = {γ, φ̄, β, α} notation row, and the FLAIR rows whose phrasing the MEV rows will mirror)
    - lean/vol_markets/FlairOptimization.lean — `flairHazard`, `flairMulti`, `pathWeight`, `flairMulti_affine`, `flairMulti_le_corner`, `flairMulti_saturation_limit`, `Theta_lambda_identification`, and the module docstring's demand-elasticity caveat (the shape the MEV blocks mirror, with every inequality reversed)
    - ../plank/notes/VOLATILITY_INSTRUMENTS.md lines 380–470 (the HAZARD RATES / FLAIR / MEV sections — the surrounding LaTeX style and the two `### MEV` bullets to keep)
  </read_first>

  <files>model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md, .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh</files>

  <action>
**A. Write `model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`.**

Header, verbatim in shape (mirror `VOLATILITY_INSTRUMENTS_LEAN_ADDENDUM.md`):

```
# PROPOSED addendum to `VOLATILITY_INSTRUMENTS.md` `### MEV` — the λ_MEV hazard and its infimum

> STATUS: DRAFT — pending two-reviewer gate and user approval.
> Anchor: Milionis–Moallemi–Roughgarden, *Automated Market Making and Arbitrage Profits in the
> Presence of Fees*, arXiv:2305.14604v2 (2025-07-23), read from
> `../plank/refs/mev/MilionisMoallemiRoughgardenArbProfitsFees.pdf`.
> Notation: the paper's fee `γ` is this document's `φ`; the paper's Poisson block rate `λ` is
> written through its own primitive `Δt ≜ λ⁻¹` (this document's `λ` is the hazard rate); the
> paper's composite `η` is deliberately never named (`η` is the project's pricing-kernel eta).
> Minimal prose; each block is insert-ready LaTeX.
```

Then EXACTLY these nine blocks, in this order, LaTeX-first with at most two sentences of prose
each. Take every formula from 11-RESEARCH F1/F3/F5/F6/F7/F8 — transcribe, do not re-derive.

- **M0. [NOTATION]** One display-free paragraph fixing the three substitutions above plus:
  `Δt` = mean interblock time (Angstrom: one bundle per block per pair); `σ` = the same
  `σ(i(t))` already used by `multiFee`; `a_t ≥ 0` = the per-step arbitrage-opportunity weight
  (leading-order LVR); `D_t > 0` = the SAME deployed-capital denominator as `λ_FLAIR`.
  State in one sentence that MMR's `FEE` (arbitrageur fee payments) is a strict sub-flow of
  `λ_FLAIR` (which also carries noise-trader flow) and the two are not identified.

- **M1. [ADDITION] The trade probability.** The display
  \[ P_{\text{trade}}(\varphi,\sigma,\Delta t) \;=\; \frac{\sigma}{\sigma + \varphi\sqrt{2/\Delta t}} \]
  with, on one line each: `∈ (0,1]`; `= 1 ⟺ φ = 0`; strictly decreasing in `φ`; strictly convex
  in `φ`; increasing in `Δt`; increasing in `σ`; `→ 0` as `φ → ∞`. Cite MMR Theorem 1 and record
  that it is independent of the bonding function — only the fee matters.

- **M2. [ADDITION] The MMR split.**
  \[ \mathrm{ARB} \approx \mathrm{LVR}\cdot P_{\text{trade}}, \qquad
     \mathrm{FEE} \approx \mathrm{LVR}\cdot(1-P_{\text{trade}}), \qquad
     \mathrm{ARB}+\mathrm{FEE} \approx \mathrm{LVR} \]
  Cite Theorem 3 / eq. (12) and Theorem 4. State in one sentence that `≈` is the fast-block
  (`Δt → 0`) small-fee leading order, so everything built on it is a leading-order object.

- **M3. [ADDITION] The discrete `λ_MEV`.** Mirror `λ_FLAIR`'s discrete form exactly:
  \[ \lambda_{\text{MEV}} \;=\; \sum_{t<T} P_{\text{trade}}\big(\varphi(\sigma_t),\sigma_t,\Delta t\big)\,\frac{a_t}{D_t} \]
  and its `Θ_φ` specialization with `φ = multiFee(n, γ, β, α, φ̄, u)` — the SAME `Θ_φ` as FLAIR.
  One line on the CPMM instantiation `a_t = (σ_t²/8)·V_t` with the guard `σ_t²Δt < 8`.

- **M4. [ADDITION] Identification `Θ_{λ_MEV}`.** State it as a MONOTONICITY block plus
  CONVEXITY — explicitly NOT as an affine decomposition:
  `λ_MEV` is antitone in `φ̄`, in each `α_j`, and in `u`; isotone in each `β_j`; and convex in the
  fee. One sentence, mandatory: *there is no affine identification analogous to
  `flairMulti_affine`, because `P_trade` is not affine — the level and shape coordinates do not
  separate.* Conclude `Θ_{λ_MEV} = \{\bar\varphi, \alpha, u\}`.

- **M5. [ADDITION] The infimum program.** The uniform lower bound as a SUM (not a
  scalar × pathWeight):
  \[ \lambda_{\text{MEV}} \;\ge\; \sum_{t<T} P_{\text{trade}}\Big(\bar\varphi_{\max} + u_{\max}\textstyle\sum_j \alpha_{\max,j},\,\sigma_t,\,\Delta t\Big)\frac{a_t}{D_t} \]
  attained bang-bang at the level-corner TOP, approached as `β_j → −∞`, strict at every finite
  `β`, with a minimizer existing on any nonempty compact box.

- **M6a. [ADDITION — THE DEGENERACY] The unconstrained joint program.** State plainly:
  `argsup λ_FLAIR = arginf λ_MEV` at EVERY coordinate of `Θ_φ` (level corner top and
  `β → −∞`), and for every scalarization weight `κ ≥ 0`, `sup(λ_FLAIR − κ λ_MEV)` sits at the
  same point. Therefore, **unconstrained, there is no trade-off in `Θ_φ` and the shape block is
  not essential.** Say in one sentence that this REFUTES the expectation recorded in the phase
  brief.

- **M6b. [ADDITION — THE CONSTRAINED PROGRAM] Where the trade-off lives.** With
  `ν_t = w_t/D_t`, `W = Σν_t`, budget `λ_FLAIR = B`, and the aligned-measure hypothesis `a ≡ w`:
  \[ \lambda_{\text{MEV}} \;\ge\; W\cdot P_{\text{trade}}\!\left(\frac{B}{W},\,\sigma_0,\,\Delta t\right),
     \qquad \text{equality} \iff \varphi(\sigma_t)\ \text{constant in}\ t \]
  i.e. **at a fixed FLAIR fee income, a FLAT fee minimizes `λ_MEV`, and any volatility-responsive
  schedule with the same income is strictly worse for MEV.** Immediately beneath, an explicit
  `**OPEN**` line: the display is stated at constant `σ_t ≡ σ_0`; with `σ_t` varying the summands
  are *different* convex functions and the correct statement is a two-measure/covariance one,
  which is NOT claimed here.

- **M7. [ADDITION] Angstrom bridge (parameters outside `Θ_φ`).** Two items, both parametric:
  (i) the recycling/rebate `λ_MEV^{net} = (1-\tau)\,\lambda_{\text{MEV}}`, `τ ∈ [0,1]`, with
  `\tau(k) = k/(k+1)`, `k` FREE — mention `k = 49 ⟹ τ = 0.98` only as a dated worked instance
  (l2-angstrom snapshot 2026-07-30) and state in the same sentence that the live docs disagree
  (`TAXED_GAS` 120,000, a `priorityFeeTaxFloor`, a `jitMEVTaxFactor`, and a creator/protocol/LP
  split), so no numeric constant may enter a claim; and that the rebate rescales the objective
  WITHOUT moving its minimizers — it is outside `Θ_φ`.
  (ii) the batch cadence IS `Δt`: it moves `λ_MEV` monotonically and does not enter `λ_FLAIR`
  at all — the second, genuinely non-degenerate lever.
  One line for the sandwich channel: `λ_MEV = λ_ARB ⊕ λ_sandwich` in the doc's own `⊗_φ`/hazard
  algebra, and uniform-clearing batches give `λ_sandwich = 0 ⟹ λ_MEV = λ_ARB` (cite
  arXiv:2207.11835 for sandwich being a distinct channel; do not model sandwich profit shapes).

- **M8. [CAVEATS]** Three bullets: (1) leading-order — everything rests on eq. (12)'s fast-block
  small-fee asymptotics; (2) no demand elasticity in EITHER functional, with the missing term
  named exactly: MMR section 7.3 eq. (27), `E[LP P&L] = E[NT_FEE] − E[ARB]`, "higher fees reduce
  noise-trader activity ... but also reduce arbitrage profits" — so every corner solution here is
  a property of the formalized objective, not a market-equilibrium claim; (3) the σ-varying
  Jensen statement of M6b is OPEN.

**B. Write `.planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh`** (executable,
`#!/usr/bin/env bash`, `set -euo pipefail`), taking the file to check as `$1` and exiting non-zero
with a named message on each violation:

1. FORBID the pricing-kernel eta: `grep -nE 'η|\\eta' "$1"` must find nothing (PIT-1).
2. FORBID a bare block-rate lambda: `grep -nE '\\lambda([^_]|$)' "$1"` must find nothing — every
   `\lambda` must be subscripted (PIT-2).
3. FORBID the fee-gamma: `grep -nE '\\gamma([^_]|$)' "$1"` must find nothing — every `\gamma` must
   be subscripted `\gamma_j`/`\gamma_{...}` (PIT-3).
4. FORBID the false affine claim (PIT-5): `grep -nE 'lambda_\{\\text\{MEV\}\}[^=]*=[^=]*pathWeight'`
   and a literal `grep -n 'affine'` restricted to lines that do NOT also contain `no affine` /
   `not affine` must find nothing.
5. REQUIRE presence: `P_{\text{trade}}`, `\Delta t`, `\Theta_{\lambda_{\text{MEV}}}`,
   `2305.14604`, `multiFee`, `OPEN`.
6. FORBID absolute paths: `grep -nE '/home/|\$HOME|~/' "$1"` must find nothing.
Print `MEV NOTATION GATE: PASS` on success.

**C. Run it**: `bash .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
and fix the addendum until it passes. Do NOT weaken the gate to make it pass.

**D. Do NOT touch any file under `lean/`, and do NOT write to `../plank/` in this task.**
  </action>

  <verify>
    <automated>bash .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md && git status --porcelain lean/ | wc -l | grep -q '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - `test -f model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - `test -x .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh` (or the file exists and `bash` runs it exit 0)
    - `bash .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md` exits 0 and prints `MEV NOTATION GATE: PASS`
    - All nine block headers present: `for b in M0 M1 M2 M3 M4 M5 M6a M6b M7 M8; do grep -q "\*\*$b\." model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md || exit 1; done`
    - `! grep -nE 'η|\\eta' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - `! grep -nE '\\lambda([^_]|$)' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - `! grep -nE '\\gamma([^_]|$)' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - `grep -q 'P_{\\text{trade}}' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - `grep -q '2305.14604' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - `grep -q 'multiFee' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - The degeneracy is stated: `grep -qiE 'degenerat|REFUT' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - The σ-varying gap is labelled OPEN: `grep -q 'OPEN' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - No hardcoded tax constant inside a claim display: `grep -c '49' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md` returns at most 2 and every hit is on a line also matching `2026-07-30` or `instance` or `snapshot`
    - `wc -l < model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md` is at least 90
    - `git status --porcelain lean/` is EMPTY
    - `git status --porcelain` shows NOTHING under `../plank/` (this task writes only in this tree)
  </acceptance_criteria>

  <done>The addendum exists with blocks M0–M8, transcribes MMR's closed forms under the three resolved substitutions, states degeneracy and the constrained result as separate claims, labels the σ-varying Jensen case OPEN, keeps τ parametric, and passes an executable notation gate that was not weakened.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Two-reviewer gate on the addendum (parallel), resolve BLOCKER/MAJOR</name>

  <read_first>
    - model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md (the artifact under review, as written by Task 1)
    - /home/jmsbpp/.claude/CLAUDE.md section "Two-step reviewer process (specs and plans)" — the binding rule: Reviewer 1 is ALWAYS the Reality Checker; Reviewer 2 is ONE specialist from the AI-agency catalog chosen to fit the task, and the pick must be stated with its reason; both run IN PARALLEL, neither edits the artifact, both return BLOCKER/MAJOR/MINOR findings
    - .planning/phases/11-mev-hazard-inf-program/11-RESEARCH.md — Common Pitfalls PIT-5 (claiming the mirror where it breaks), PIT-6 (over-promising the trade-off), and Open Questions Q1/Q3 (the two claims the reviewers are most likely to catch)
    - ../plank/refs/mev/MilionisMoallemiRoughgardenArbProfitsFees.pdf (extract with `pdftotext -layout <pdf> /tmp/mmr.txt`) — the reviewers must be able to check the transcription against the source, not against the research summary
  </read_first>

  <files>.planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md, model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md</files>

  <action>
1. **Extract the anchor text** so both reviewers check against the paper, not the summary:
   `pdftotext -layout ../plank/refs/mev/MilionisMoallemiRoughgardenArbProfitsFees.pdf /tmp/mmr.txt`

2. **Launch BOTH reviewers IN PARALLEL** (one message, two agent calls) on
   `model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`:
   - **Reviewer 1 — Reality Checker** (mandatory, always first). Brief: NEEDS-WORK-by-default,
     evidence-based. Specifically charged with: verifying every transcribed formula against
     `/tmp/mmr.txt` rather than against 11-RESEARCH.md; flagging any claim asserted without a
     theorem number; flagging any place the addendum promises a Θ_φ trade-off without a constraint
     in the same sentence; flagging any numeric Angstrom constant that has escaped into a claim.
   - **Reviewer 2 — the specialist.** Pick the closest AI-agency specialist to
     **quantitative finance / market microstructure** (per 11-RESEARCH PIT-7: the risk in this
     artifact is economic misstatement of ARB/LVR and of what the Angstrom mechanism does, not
     LaTeX syntax). **State the pick and the reason in the review file.** Brief: is `P_trade`
     the right object for "MEV"? Is the ARB/FEE/LVR split stated correctly? Is conflating MMR's
     `FEE` with `λ_FLAIR` avoided? Is the ToB-auction-as-rebate modelling defensible, and is the
     "searcher bid ↔ priority_fee × gas" assumption disclosed?
   Neither reviewer edits the file. Both return severity-sorted BLOCKER / MAJOR / MINOR.

3. **Write `.planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md`** with, in order:
   the specialist pick + reason; a `## Reviewer 1 — Reality Checker` section and a
   `## Reviewer 2 — <name>` section, each with the verbatim severity-sorted findings; then a
   `## Resolution` table with columns `| Severity | Finding | Disposition | Where fixed |`.

4. **Resolve every BLOCKER and MAJOR** by editing the addendum, then re-run the notation gate.
   MINORs may be accepted with a one-line reason recorded in the Resolution table. If a reviewer
   finding contradicts 11-RESEARCH.md, the paper (`/tmp/mmr.txt`) decides and the disposition
   records which source won.

5. If a BLOCKER cannot be resolved without a user decision (e.g. a claim the user must adjudicate),
   do NOT paper over it — record it in the Resolution table as `ESCALATE` and carry it into the
   Task 3 approval request as an explicit question.
  </action>

  <verify>
    <automated>test -f .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md && grep -q 'Reviewer 1 — Reality Checker' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md && grep -q 'Reviewer 2' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md && ! grep -qE '^\| *(BLOCKER|MAJOR) *\|.*\| *(open|unresolved|TODO) *\|' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md && bash .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md</automated>
  </verify>

  <acceptance_criteria>
    - `test -f .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md`
    - `grep -q 'Reviewer 1 — Reality Checker' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md`
    - `grep -qE '^## Reviewer 2 — .+' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md` and the same file matches `grep -qiE 'chosen because|reason for this specialist'`
    - `grep -q '## Resolution' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md`
    - Every BLOCKER/MAJOR row carries a disposition: `! grep -qE '^\| *(BLOCKER|MAJOR) *\|.*\| *(open|unresolved|TODO|—) *\|' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md`
    - `grep -qE 'BLOCKER|MAJOR|MINOR' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md` (findings were actually produced, not an empty gate)
    - `bash .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md` still exits 0 after the edits
    - `git status --porcelain lean/` is EMPTY
    - `! grep -rE '/home/|\$HOME|~/' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md`
  </acceptance_criteria>

  <done>Both reviewers ran in parallel on the addendum, the specialist pick is named and justified, every BLOCKER and MAJOR has a recorded disposition and a fix in the addendum, and the notation gate still passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HEAVY USER APPROVAL of the λ_MEV doc block, then insertion + peer handoff</name>

  <read_first>
    - model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md (the artifact being approved)
    - .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md (the verdicts to present, including any ESCALATE rows)
    - model/vol_markets/VOLATILITY_INSTRUMENTS_LEAN_ADDENDUM.md (the header the approved file must be updated to match: `STATUS: APPROVED & APPLIED <date> ... per user approval (todo.md ## LEAN4 - MATH)`)
    - ../plank/notes/VOLATILITY_INSTRUMENTS.md around `### MEV` (line ~461) — the exact insertion point and the two bullets to keep
    - CLAUDE.md ownership map (agent `ul2inqpl` owns the plank worktree; this session writes but does not commit there)
  </read_first>

  <files>model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md, ../plank/notes/VOLATILITY_INSTRUMENTS.md, ../plank/todo.md, .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md</files>

  <action>
This is a BLOCKING user-approval checkpoint. Execute the procedure in `<how-to-verify>` below
exactly: present the addendum and the reviewer verdicts, ask the verbatim approval question, and
WAIT. Write nothing to `../plank/` before the user replies. On `approved`, perform steps 1-6 of
`<how-to-verify>` (insert beneath the two existing `### MEV` bullets, append the todo.md handoff,
flip the addendum header to APPROVED & APPLIED, notify `ul2inqpl` via claude-peers, re-run the
notation gate, commit THIS tree only). On `rejected`, stop and record the reason under a
`## User disposition` heading in `11-01-REVIEW.md`.
  </action>

  <verify>
    <automated>grep -q 'STATUS: APPROVED & APPLIED' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md && grep -q 'P_{\\text{trade}}' ../plank/notes/VOLATILITY_INSTRUMENTS.md && grep -q 'Angstrom holds the reference for an implementation' ../plank/notes/VOLATILITY_INSTRUMENTS.md && grep -q '## User disposition' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md && [ "$(git status --porcelain lean/ | wc -l)" = "0" ]</automated>
  </verify>

  <what-built>
    A complete, reviewer-gated λ_MEV mathematical specification: blocks M0–M8 in insert-ready
    LaTeX, with the three MMR↔doc notation collisions resolved mechanically, the DEGENERACY result
    and the CONSTRAINED (Jensen) result stated as two separate claims, the σ-varying generalization
    labelled OPEN, and the Angstrom tax kept parametric in τ. Nothing has been written to the
    plank-owned document yet.
  </what-built>

  <how-to-verify>
    Present to the user, in this order:

    1. The **full text** of `model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`.
    2. A **three-line diff summary** of what changes in `../plank/notes/VOLATILITY_INSTRUMENTS.md`:
       the two existing `### MEV` bullets are KEPT, and N lines of LaTeX are appended beneath them.
    3. Both reviewer verdicts from `11-01-REVIEW.md`, with any `ESCALATE` rows raised as explicit
       questions.
    4. The **three decisions that are hardest to reverse**, each stated as a question:
       - a. The notation substitutions (MMR `γ`→`φ`, `λ`→`Δt`, `η` never named). Approve?
       - b. The addendum REFUTES the phase brief's expectation that `(β, γ)` becomes essential:
            unconstrained, the joint program is degenerate. Approve stating it that way?
       - c. The constrained (Jensen) claim is asserted only at constant `σ_t`, with the σ-varying
            case marked OPEN. Approve shipping the OPEN label rather than a stronger claim?

    Then ask, verbatim:

      "APPROVE inserting blocks M0–M8 into ../plank/notes/VOLATILITY_INSTRUMENTS.md `### MEV`?
       Reply `approved` to insert as-is, `approved with changes: <...>` to insert with edits, or
       `rejected: <reason>` to hold. Nothing is written to the plank worktree until you reply."

    ON `approved` (or after applying the requested changes and re-running the notation gate):
    1. Append the approved blocks to `../plank/notes/VOLATILITY_INSTRUMENTS.md` beneath the two
       existing `### MEV` bullets. Keep those bullets. Do NOT `git commit` in the plank worktree.
    2. Append a handoff entry under `## LEAN4 - MATH` in `../plank/todo.md` naming this tree's
       addendum file as the source of record and stating that committing the plank file is the
       plank agent's action.
    3. Update the addendum header to
       `> STATUS: APPROVED & APPLIED <YYYY-MM-DD> — blocks M0–M8 inserted into ../plank/notes/VOLATILITY_INSTRUMENTS.md per user approval (todo.md ## LEAN4 - MATH). Committing the plank file is the plank agent's.`
    4. Notify agent `ul2inqpl` via `claude-peers send_message` with the file paths and the fact
       that the insert is uncommitted on their side.
    5. Run the notation gate against the PLANK file too:
       `bash .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh ../plank/notes/VOLATILITY_INSTRUMENTS.md`
       — if the pre-existing document legitimately contains a bare `\lambda` or `\gamma` outside
       the MEV block, run the gate against an extracted copy of the `### MEV` section instead and
       record which form was used.
    6. Commit ONLY this tree:
       `git add model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md .planning/phases/11-mev-hazard-inf-program/` then
       `git commit -m "docs(11-01): λ_MEV doc spec — MMR-anchored addendum, reviewer-gated, user-approved"`

    ON `rejected`: stop. Do not write to `../plank/`. Record the reason in `11-01-REVIEW.md` under
    a `## User disposition` heading and end the plan; the phase cannot proceed to 11-02.
  </how-to-verify>

  <resume-signal>Type `approved`, `approved with changes: <...>`, or `rejected: <reason>`</resume-signal>

  <acceptance_criteria>
    - `grep -q 'STATUS: APPROVED & APPLIED' model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - `grep -q 'lambda_{\\text{MEV}}' ../plank/notes/VOLATILITY_INSTRUMENTS.md`
    - `grep -q 'P_{\\text{trade}}' ../plank/notes/VOLATILITY_INSTRUMENTS.md`
    - The pre-existing bullets survive: `grep -q 'Angstrom holds the reference for an implementation' ../plank/notes/VOLATILITY_INSTRUMENTS.md`
    - `grep -q 'LEAN4 - MATH' ../plank/todo.md` and that file mentions `VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`
    - This session did NOT commit in the plank worktree: `git -C ../plank log --oneline -1 --since='1 hour ago' -- notes/VOLATILITY_INSTRUMENTS.md` produces no commit authored by this run (record the plank HEAD sha before and after; they must be identical)
    - `git log --oneline -1` in THIS worktree matches `grep -q '11-01'` and touches `model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md`: `git show --stat --oneline HEAD | grep -q 'VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md'`
    - `git status --porcelain lean/` is EMPTY
    - A `## User disposition` section records the verbatim resume signal: `grep -q '## User disposition' .planning/phases/11-mev-hazard-inf-program/11-01-REVIEW.md`
  </acceptance_criteria>

  <done>The user explicitly approved the λ_MEV block; it is inserted under `### MEV` in the plank-owned document (uncommitted there), the addendum header records APPROVED & APPLIED, `ul2inqpl` is notified, and only this tree was committed.</done>
</task>

</tasks>

<verification>
- `bash .planning/phases/11-mev-hazard-inf-program/mev-notation-gate.sh model/vol_markets/VOLATILITY_INSTRUMENTS_MEV_ADDENDUM.md` exits 0.
- `git status --porcelain lean/` is empty across every commit of this plan (no Lean file touched).
- The plank worktree HEAD sha is unchanged by this plan.
- `11-01-REVIEW.md` carries both reviewer verdicts, a named specialist with a reason, a Resolution
  table with no unresolved BLOCKER/MAJOR, and the user's verbatim disposition.
</verification>

<success_criteria>
The λ_MEV specification is APPROVED and present in `VOLATILITY_INSTRUMENTS.md ### MEV`, notation
collisions are mechanically excluded by a script that was not weakened, the degeneracy and
constrained results are separate claims, and the σ-varying Jensen case is labelled OPEN.
</success_criteria>

<output>
After completion, create `.planning/phases/11-mev-hazard-inf-program/11-01-SUMMARY.md`.
</output>
