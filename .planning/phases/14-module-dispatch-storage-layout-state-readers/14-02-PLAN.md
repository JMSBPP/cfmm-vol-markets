---
phase: 14-module-dispatch-storage-layout-state-readers
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - test/exposure/VegaAccount.t.sol
  - src/modules/exposure/VegaAccountMod.plk
autonomous: true
requirements: [VMOD-05]
must_haves:
  truths:
    - "after one deposit, raw vm.load at the four precomputed slot addresses returns each field's independent value (totalDeposits, totalShares, riskWeightedShares, riskPrice)"
    - "the four slot addresses are pairwise distinct (keccak of four distinct preimage strings)"
    - "the inert admissibility guard's presence/form is verified solely by the raw-checked-cross-product mutant (overflow revert at deposit ~ 2^200)"
    - "slot-constant aliasing, dust-guard deletion, and raw-checked-cross-product mutants each OBSERVED RED at a named assertion, then restored sha256-identical and green"
    - "the unset-price guard deletion is DOCUMENTED equivalence-checked (masked by the lib's zero-price revert), NOT counted as a kill"
  artifacts:
    - path: "test/exposure/VegaAccount.t.sol"
      provides: "VegaAccountSlotDistinctnessTest (raw vm.load at the 4 computed slots) + the mutation-gate in-file documentation"
      contains: "vm.load"
    - path: "src/modules/exposure/VegaAccountMod.plk"
      provides: "mutation target (edited then restored byte-identical — net zero change)"
      contains: "SLOT_RISK_WEIGHTED_SHARES"
  key_links:
    - from: "test/exposure/VegaAccount.t.sol"
      to: "src/modules/exposure/VegaAccountMod.plk"
      via: "test recomputes the 4 slot addresses from the SAME preimage strings the module SLOT_* consts use"
      pattern: "keccak256"
    - from: "vm.load on the never-aliased slot"
      to: "the read-conflation-invisible aliasing mutant"
      via: "raw slot read is the ONLY thing that kills it (d==1 makes readers numerically identical)"
      pattern: "vm.load"
---

<objective>
Prove the FOUR keccak-derived slots are DISTINCT by raw `vm.load` at the precomputed addresses after a deposit (reader-level assertions CANNOT prove this — with d==1 read-conflation is behaviorally invisible), and run the Phase 14 mutation gate: three killable mutants each OBSERVED RED then restored byte-identical, plus the one documented equivalence-checked non-kill. Both live in the SAME `test/exposure/VegaAccount.t.sol` (one file per surface).

Purpose: Discharge VMOD-05 — slot distinctness via vm.load, the inert-admissibility-guard verification (via the cross-product mutant), and the falsifiability gate.
Output: slot-distinctness test contract + mutation-gate documentation in the module test file; the module edited-and-restored (net zero change).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-module-dispatch-storage-layout-state-readers/14-CONTEXT.md

# The module under mutation + its 4 slot preimage strings (from 14-01)
@src/modules/exposure/VegaAccountMod.plk
# The interface file (from 14-01)
@src/interfaces/exposure/VegaAccountInterface.plk
# The test file 14-01 created — EXTEND this same file (one file per surface)
@test/exposure/VegaAccount.t.sol
# The vm.store/vm.load-at-raw-slot technique to mirror
@test/market_state_measurements/RealizedVolatility.diff.t.sol
# deployPlank + roots
@test/PlankTestBase.sol

<interfaces>
<!-- The four slot preimage strings the test must recompute (from the 14-01 module). -->
<!-- Solidity: bytes32 slot = keccak256(bytes("VegaAccountMod.totalDeposits")); -->
<!-- This equals `cast keccak "VegaAccountMod.totalDeposits"` and the module SLOT_* const value. -->
Preimage strings (RESTATE test-side, do not hardcode the hash — recompute so a module/test
preimage mismatch surfaces loudly as vm.load reading 0 where a value was written):
  "VegaAccountMod.totalDeposits"
  "VegaAccountMod.totalShares"
  "VegaAccountMod.riskWeightedShares"
  "VegaAccountMod.riskPrice"

Cheatcodes: `vm.load(address, bytes32 slot) -> bytes32` reads a raw storage word.
`forge clean` clears the compilation cache; there is no fuzz cache to clear for pure-unit mutants
(the mutation anchors below are NON-FUZZ unit tests — cache-independent by construction).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slot-distinctness — raw vm.load at the four precomputed addresses after a deposit</name>
  <read_first>
    - test/exposure/VegaAccount.t.sol (14-01 — EXTEND this file; reuse its deployPlank setUp and IVegaAccount interface)
    - src/modules/exposure/VegaAccountMod.plk (the 4 SLOT_* consts and their `// "VegaAccountMod.<field>"` preimage comments — the test recomputes the SAME strings)
    - test/market_state_measurements/RealizedVolatility.diff.t.sol lines 505-524 (the vm.store/vm.load-at-a-raw-keccak-slot idiom to mirror)
    - test/PlankTestBase.sol (deployPlank)
  </read_first>
  <files>test/exposure/VegaAccount.t.sol</files>
  <action>
Add a new contract `VegaAccountSlotDistinctnessTest is PlankTestBase` to the EXISTING `test/exposure/VegaAccount.t.sol` (do NOT fork a second file). Deploy the module in setUp exactly as the 14-01 contracts do.

Compute the four slot addresses TEST-SIDE from the SAME preimage strings the module uses — recompute with `keccak256`, never hardcode the hash. A mismatch between module preimage and test preimage then shows up LOUDLY as vm.load reading 0 where a value was written:

```
bytes32 constant SLOT_TOTAL_DEPOSITS       = keccak256(bytes("VegaAccountMod.totalDeposits"));
bytes32 constant SLOT_TOTAL_SHARES         = keccak256(bytes("VegaAccountMod.totalShares"));
bytes32 constant SLOT_RISK_WEIGHTED_SHARES = keccak256(bytes("VegaAccountMod.riskWeightedShares"));
bytes32 constant SLOT_RISK_PRICE           = keccak256(bytes("VegaAccountMod.riskPrice"));
```

Contract docblock — state WHY vm.load and not readers: "Reader-level assertions CANNOT prove the four slots are distinct. With d == 1 in v1, totalShares and riskWeightedShares are numerically EQUAL forever, so a reader that conflated them (returned the wrong slot) is behaviorally invisible. Only a raw vm.load on each independently-computed slot address proves each field lives in its OWN slot. This is the SAME infrastructure that kills the 14-02 slot-aliasing mutant."

Tests:
  - `test__unit__fourSlotsAreDistinct`: assert the four bytes32 addresses are pairwise distinct (6 assertTrue(a != b) comparisons). This pins that four distinct preimages give four distinct slots.
  - `test__unit__depositWritesFourIndependentSlots`: choose a stored price making shares != collateral so totalDeposits and totalShares hold DIFFERENT numbers (so a slot that read the wrong neighbour is caught by value, not just by presence). Use setRiskPrice(2 * (1<<96)) and deposit(4) -> shares = floor(4 * 2^96 / 2^97) = floor(2) = 2. Then:
      - assertEq(uint256(vm.load(address(mod), SLOT_RISK_PRICE)), 2 * (1<<96), "riskPrice slot");
      - assertEq(uint256(vm.load(address(mod), SLOT_TOTAL_DEPOSITS)), 4, "totalDeposits slot");
      - assertEq(uint256(vm.load(address(mod), SLOT_TOTAL_SHARES)), 2, "totalShares slot");
      - assertEq(uint256(vm.load(address(mod), SLOT_RISK_WEIGHTED_SHARES)), 2, "riskWeightedShares slot");
    Cross-check each raw slot equals its reader: assertEq(uint256(vm.load(...SLOT_TOTAL_DEPOSITS)), mod.totalDeposits()) for all four. This binds the vm.load addresses to the module's own reader semantics — if the test preimage were wrong, the raw load would be 0 and disagree with the (correct) reader.
    Non-degeneracy: assertTrue(4 != 2) i.e. totalDeposits != totalShares in this fixture, so the totalDeposits/totalShares slots hold distinguishable values (a swap would be caught).
  </action>
  <verify>
    <automated>forge test --match-path 'test/exposure/VegaAccount.t.sol' --match-contract VegaAccountSlotDistinctnessTest --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `VegaAccountSlotDistinctnessTest` passes: the four slot addresses are pairwise distinct, and after `setRiskPrice(2*2^96); deposit(4)` the raw vm.load at each of the four addresses returns 4 / 2 / 2 / 2*2^96 respectively.
    - Each raw vm.load equals its corresponding reader (`vm.load(SLOT_TOTAL_DEPOSITS) == totalDeposits()`, etc.) — binding the test-side preimage to the module.
    - The slot addresses are recomputed via `keccak256(bytes("VegaAccountMod.<field>"))` test-side (grep: 4 `keccak256(bytes(` occurrences), NOT hardcoded hashes.
    - The fixture uses shares != collateral (4 vs 2) so the totalDeposits and totalShares slots hold distinguishable values.
    - Docblock states reader assertions cannot prove distinctness (d==1 read-conflation invisibility) and that vm.load is the only proof.
  </acceptance_criteria>
  <done>Raw vm.load at four independently-computed slot addresses proves the four fields occupy four distinct slots after a deposit — the proof reader assertions structurally cannot give.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation gate — three killable mutants OBSERVED RED then restored byte-identical</name>
  <read_first>
    - src/modules/exposure/VegaAccountMod.plk (the mutation targets: the SLOT_RISK_WEIGHTED_SHARES const, the dust guard `if @evm_iszero(shares)`, the admissibility `require(new_total_deposits >= collateral)`)
    - test/exposure/VegaAccount.t.sol (14-01 smoke/guard contracts + Task 1 slot-distinctness — the assertions that must go RED)
    - .planning/STATE.md Accumulated Context (the cached-fuzz-replay lesson: clear cache/fuzz OR kill via a non-fuzz unit anchor; FFI recompiles the .plk fresh each run so a .plk mutant reaches deployed bytecode with NO make compile-plank needed)
    - .planning/phases/14-module-dispatch-storage-layout-state-readers/14-CONTEXT.md (the mutant catalogue + the read-conflation UNKILLABLE note)
  </read_first>
  <files>src/modules/exposure/VegaAccountMod.plk, test/exposure/VegaAccount.t.sol</files>
  <action>
Add one contract `VegaAccountCrossProductMutantTest is PlankTestBase` whose sole purpose is to make the inert-admissibility-guard's FORM falsifiable (the guard can never fire behaviorally, so ONLY a mutant proves it is present and correctly collapsed). Then execute the full mutation gate as an OBSERVED procedure and record verbatim RED lines in the SUMMARY. The module edits are TEMPORARY — every mutant is restored sha256-identical (net zero change to the module).

First, capture a baseline hash: `sha256sum src/modules/exposure/VegaAccountMod.plk` — record it; every "restored" step must reproduce it.

--- The cross-product test contract (add to the file, permanent) ---
`test__unit__crossProductOverflowsAtLargeDeposit`:
  setRiskPrice(1 << 96);                       // weight-one: shares == collateral
  uint256 big = 1 << 200;                      // ~2^200
  mod.deposit(big);                            // BASELINE must ACCEPT (u256 accumulators unobstructed)
  assertEq(mod.totalDeposits(), big, "baseline accepts a ~2^200 deposit");
  assertEq(mod.totalShares(), big, "shares == collateral at weight-one");
Docblock: "The u128 bound on VegaExposure.exposure is a TYPE-LEVEL doc bound, NOT enforced on the u256 accumulator slots, so this path is unobstructed. The baseline collapsed guard (new_total_deposits >= collateral) has no multiply and accepts. Mutant (c) below replaces it with a raw CHECKED cross-product `collateral * storedRiskPrice` which overflow-reverts here (2^200 * 2^96 = 2^296 > 2^256) — killed on state."

--- MUTATION GATE (observed procedure; run each, record verbatim RED, restore) ---

For each mutant: (1) apply the one-line edit; (2) `forge clean` (clears the compile cache; FFI then recompiles the mutated .plk fresh — no `make compile-plank` needed, and these anchors are non-fuzz so no fuzz-cache replay is possible); (3) run the named test with `--match-path 'test/exposure/VegaAccount.t.sol' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize`; (4) OBSERVE the named assertion go RED and copy the verbatim failing line; (5) restore the file; (6) `sha256sum` MUST match the baseline; (7) re-run and confirm GREEN.

MUTANT (a) — SLOT-CONSTANT aliasing:
  Edit: `const SLOT_RISK_WEIGHTED_SHARES = <its keccak>;`  ->  `const SLOT_RISK_WEIGHTED_SHARES = SLOT_TOTAL_SHARES;`
  Effect: both totalShares and riskWeightedShares writes hit ONE slot -> double-increment (2*shares); the real riskWeightedShares slot address is never written.
  KILLED BY:
    - VegaAccountSmokeTest.test__unit__depositMovesAllThreeAccumulators (totalShares reads 2*shares != shares), AND
    - VegaAccountSlotDistinctnessTest.test__unit__depositWritesFourIndependentSlots (vm.load at the never-written riskWeightedShares slot reads 0 != 2).
  STATED IN THE DOCBLOCK (not hidden): the READ-conflation variant (a reader returns the other slot but the write is correct) is BEHAVIORALLY UNKILLABLE in v1 — with d==1 the two accumulators are numerically equal forever — and is killed ONLY by the raw vm.load on the never-written slot. That is exactly why Task 1 exists.

MUTANT (b) — DUST-GUARD deletion:
  Edit: delete the line `if @evm_iszero(shares) { revert_empty(); }`.
  Effect: a dust deposit (shares == 0) proceeds and banks collateral for zero shares.
  KILLED BY: VegaAccountGuardTest.test__unit__dustDepositReverts — baseline reverts (totalDeposits stays 0); mutant proceeds so totalDeposits() == 1 != 0. Killed on STATE.

MUTANT (c) — RAW CHECKED CROSS-PRODUCT admissibility guard:
  Edit: `require(new_total_deposits >= collateral);`  ->  `require(new_total_deposits >= collateral * storedRiskPrice);`  (ASCII `*` = CHECKED multiply).
  Effect: at collateral == 2^200, storedRiskPrice == 2^96 the checked product 2^296 overflow-reverts.
  KILLED BY: VegaAccountCrossProductMutantTest.test__unit__crossProductOverflowsAtLargeDeposit — baseline accepts (totalDeposits == 2^200); mutant reverts so the deposit reverts and totalDeposits stays 0. Killed on STATE (overflow revert where baseline passes).
  NOTE: `storedRiskPrice` is in scope at the guard site (loaded earlier in the deposit branch); if the mutant needs it and it was shadowed, use the in-scope binding — do not add new reads.

--- DOCUMENTED EQUIVALENCE-CHECKED NON-KILL (NOT counted as a kill) ---
UNSET-PRICE guard deletion: delete `if @evm_iszero(storedRiskPrice) { revert_empty(); }` from deposit. This is PROVEN EQUIVALENT, not a kill: with the guard gone, a deposit before any setRiskPrice has storedRiskPrice == 0, so `issue_shares(collateral, RiskPriceX96 { val: 0 })` reverts via the lib's own mulDiv zero-denominator check — BOTH baseline and mutant revert empty with totalDeposits unchanged. Observe that test__unit__depositBeforeSetRiskPriceReverts stays GREEN under this mutant (same observable), and DOCUMENT it as equivalence-checked defense-in-depth in the SUMMARY and in test__unit__depositBeforeSetRiskPriceReverts's docblock (the CONTEXT designates that test as the equivalence-documentation site). Counting it as a kill would be the false-verification pattern this project catalogues.

After the gate: confirm `sha256sum src/modules/exposure/VegaAccountMod.plk` matches the baseline recorded at the start, and the full `make test-vega-account` is GREEN.
  </action>
  <verify>
    <automated>forge test --match-path 'test/exposure/VegaAccount.t.sol' --skip 'src/modules/protocol_integrations/PriceSetterHook.sol' --via-ir --optimize 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Baseline `sha256sum src/modules/exposure/VegaAccountMod.plk` recorded, and the FINAL module hash matches it byte-for-byte (module restored; net zero change).
    - Mutant (a) slot-aliasing: applied -> `forge clean` -> OBSERVED RED at BOTH `test__unit__depositMovesAllThreeAccumulators` (totalShares 2*shares) AND `test__unit__depositWritesFourIndependentSlots` (vm.load riskWeightedShares == 0); verbatim RED lines recorded in SUMMARY; restored -> GREEN.
    - Mutant (b) dust-guard deletion: applied -> `forge clean` -> OBSERVED RED at `test__unit__dustDepositReverts` (totalDeposits == 1 != 0, killed on state); verbatim RED line recorded; restored -> GREEN.
    - Mutant (c) raw cross-product: applied -> `forge clean` -> OBSERVED RED at `test__unit__crossProductOverflowsAtLargeDeposit` (mutant reverts, totalDeposits stays 0 where baseline == 2^200); verbatim RED line recorded; restored -> GREEN.
    - Unset-price guard deletion documented as equivalence-checked (NOT a kill): `test__unit__depositBeforeSetRiskPriceReverts` stays GREEN under the mutant; equivalence stated in the SUMMARY and in that test's docblock.
    - Final `make test-vega-account` GREEN; the read-conflation UNKILLABLE note is present in the slot-aliasing docblock (stated, not hidden).
  </acceptance_criteria>
  <done>Three killable mutants each OBSERVED RED at a named assertion (cache cleared, non-fuzz anchors) then restored sha256-identical; the inert admissibility guard's form is falsified by the cross-product mutant; the unset-price non-kill is documented equivalence-checked.</done>
</task>

</tasks>

<verification>
- `make test-vega-account` green including VegaAccountSlotDistinctnessTest and VegaAccountCrossProductMutantTest.
- Four slot addresses proven distinct by raw vm.load at test-recomputed keccak preimages; each raw slot equals its reader.
- Three mutants observed RED (verbatim lines in SUMMARY) then restored byte-identical (sha256 matches baseline); unset-price deletion documented equivalence-checked, never counted as a kill.
- Module net-unchanged after the gate; PLANK_SKIP still untouched (exit is Phase 15).
- Every forge run uses `--via-ir --optimize` and `--skip 'src/modules/protocol_integrations/PriceSetterHook.sol'`.
</verification>

<success_criteria>
- VMOD-05: the FOUR keccak-derived slots proven distinct by raw vm.load at precomputed addresses; the inert admissibility guard exists in its collapsed money-side form (Q_M^Σ as post-deposit totalDeposits) and its presence/form is verified SOLELY by the raw-checked-cross-product mutant; the falsifiability gate holds — slot-aliasing, dust-guard deletion, and cross-product mutants each observed RED then restored green, with the read-conflation and unset-price cases honestly documented as unkillable / equivalence-checked.
</success_criteria>

<output>
After completion, create `.planning/phases/14-module-dispatch-storage-layout-state-readers/14-02-SUMMARY.md` recording: the baseline + final module sha256 (must match), the verbatim RED line for each of the three mutants and which named assertion caught it, the equivalence-checked documentation for the unset-price non-kill, and confirmation the read-conflation variant's unkillability is stated in-file.
</output>
