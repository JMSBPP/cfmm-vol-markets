---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 05
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - econometrics/src/Panel/Variance.hs
  - econometrics/app/Main.hs
  - econometrics/package.yaml
  - econometrics/test/Panel/VarianceSpec.hs
  - econometrics/test/fixtures/swap-ticks-sample.csv
  - notes/structural-econometrcics/data/variance.csv
autonomous: true
requirements: [CTX-VAR]
must_haves:
  truths:
    - "σ̂²_t is the realized variance of the within-day tick-implied log-price series from BigQuery Swap ticks"
    - "A SECOND, independently-windowed daily variance estimator σ̃²_t is produced as the EIV instrument (two-noisy-measures IV)"
    - "The variance window uses the SAME daily epoch boundary as the panel; the instrument uses a disjoint intraday sub-window within the same day"
  artifacts:
    - path: "econometrics/src/Panel/Variance.hs"
      provides: "BigQuery swap-tick fetch + int24 tick decode + realized variance + instrument"
      exports: ["realizedVariance", "instrumentVariance"]
    - path: "notes/structural-econometrcics/data/variance.csv"
      provides: "per-epoch σ̂²_t and σ̃²_t joined by epoch"
  key_links:
    - from: "econometrics/src/Panel/Variance.hs"
      to: "BigQuery crypto_ethereum.logs (Swap topic0)"
      via: "mcp bigquery query of pool swap logs"
      pattern: "topic0|Swap"
---

<objective>
Construct the variance regressor and its EIV instrument (CTX-VAR): from the underlying pool's Uniswap v3 Swap ticks (BigQuery), compute the within-day realized variance σ̂²_t AND a second, independently-windowed daily estimate σ̃²_t that instruments σ̂² per the spec's two-noisy-measures errors-in-variables remedy (§4.3).

Purpose: The RHS regressor σ̂²_t (EIV-flagged) and the instrument σ̃²_t that fixes the attenuation bias on υ̂₀.
Output: `econometrics/src/Panel/Variance.hs`, a `variance` CLI stage, and `notes/structural-econometrcics/data/variance.csv` (σ̂²_t, σ̃²_t by epoch), aligned to the panel's daily boundary.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@notes/structural-econometrcics/data/DATA-SOURCES.md
@notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md
@econometrics/src/Econ/Types.hs
</context>

<constraints>
- Haskell ONLY for the variance math. BigQuery access is via the `mcp__bigquery__` MCP tools (query issued at execution; results serialized to CSV that the Haskell code reads — the ESTIMATION and variance math stay in Haskell).
- Use the confirmed dataset + Swap topic0 + pool address from `notes/structural-econometrcics/data/DATA-SOURCES.md`.
- The daily window boundary MUST equal `Panel.Build.dailyEpoch` (RESEARCH Pitfall 4). The instrument window is a DISJOINT intraday sub-window within the same day (e.g. even-vs-odd swaps, or a split half-day) — document which.
- Decode the pool tick from the Swap log `data` (trailing int24, sign-extended). σ̂²_t = realized variance of the within-day tick-implied log-price series.
- No `/home/`, `$HOME`, `~/` paths; no secrets in tracked files.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: BigQuery swap-tick fetch + int24 tick decode</name>
  <files>econometrics/src/Panel/Variance.hs, econometrics/package.yaml, econometrics/test/fixtures/swap-ticks-sample.csv</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Code Examples — BigQuery Swap query + int24 tick decode note; §Common Pitfalls Pitfall 4)
    - notes/structural-econometrcics/data/DATA-SOURCES.md (dataset, Swap topic0, pool address)
  </read_first>
  <action>
    Create `econometrics/src/Panel/Variance.hs` (module `Panel.Variance`). Implement the tick-ingestion half:
      - A documented BigQuery SQL string (as a Haskell value) selecting `block_timestamp, block_number, log_index, data` from the confirmed `crypto_ethereum.logs` (or L2 equivalent) WHERE `address = LOWER('<pool>')` AND `topics[OFFSET(0)] = '<SWAP_TOPIC0>'`, ordered by block_number, log_index. Parameterize pool/topic0/date-range (read from config, not hardcoded).
      - `decodeTick :: ByteString -> Int` — extract the trailing int24 from the Swap `data` word and sign-extend to a signed Int. Unit-test this in Task 2.
      - `loadSwapTicks :: FilePath -> IO [(UTCTime, Int)]` — read the BigQuery-exported CSV (the MCP query result serialized to `notes/structural-econometrcics/data/`), parse via cassava into (timestamp, tick).
    NOTE the execution flow in a comment: the executor runs the SQL via `mcp__bigquery__` and serializes the result to CSV; Haskell then consumes it (keeps estimation in Haskell, satisfies "no Python munging").
    Freeze `econometrics/test/fixtures/swap-ticks-sample.csv` = a small within-day series of (timestamp, raw-data-hex or tick) rows spanning ≥2 days, used by Task 2.
    `stack build` stays green.
  </action>
  <verify>
    <automated>cd econometrics && stack build 2>&1 | tail -5; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack build` exits 0
    - `grep -q 'module Panel.Variance' econometrics/src/Panel/Variance.hs` and `grep -qE 'decodeTick|loadSwapTicks' econometrics/src/Panel/Variance.hs`
    - `grep -qE 'topics\[OFFSET\(0\)\]|topic0|Swap' econometrics/src/Panel/Variance.hs` (uses the confirmed topic0)
    - `test -f econometrics/test/fixtures/swap-ticks-sample.csv`
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/src/Panel/Variance.hs`
  </acceptance_criteria>
  <done>Swap-tick ingestion compiles: SQL builder, int24 decode, CSV loader, and a frozen tick fixture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Realized variance σ̂²_t + disjoint-window instrument σ̃²_t with a golden test + variance.csv</name>
  <files>econometrics/src/Panel/Variance.hs, econometrics/app/Main.hs, econometrics/test/Panel/VarianceSpec.hs, econometrics/package.yaml, notes/structural-econometrcics/data/variance.csv</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Architecture Pattern 2 EIV two-noisy-measures; §Validation → CTX-VAR golden window)
    - notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md (§3.3 M1 EIV; §4.3 the instrument)
    - econometrics/src/Panel/Build.hs (dailyEpoch — reuse the SAME boundary)
  </read_first>
  <behavior>
    - Test 1: `realizedVariance` on a golden within-day tick series equals a hand-computed realized variance of the log-price increments (tol 1e-9).
    - Test 2: `instrumentVariance` on the disjoint sub-window is computed from a DIFFERENT tick subset than σ̂²_t (assert it differs from σ̂²_t on the fixture, and matches its own hand-computed value).
    - Test 3: both estimators bucket by the SAME dailyEpoch boundary as the panel (a tick at the day boundary lands in the expected epoch).
  </behavior>
  <action>
    Extend `Panel.Variance` with:
      - `tickToLogPrice :: Int -> Double` — log-price from tick (log(1.0001^tick) = tick·log 1.0001), consistent with the λ=1.0001 grid.
      - `realizedVariance :: [(UTCTime, Int)] -> Map Epoch Double` — per-day realized variance of the within-day tick-implied log-price increments, bucketed by `Panel.Build.dailyEpoch`.
      - `instrumentVariance :: [(UTCTime, Int)] -> Map Epoch Double` — the SAME estimator on a disjoint intraday sub-window (document the split: even/odd swaps or split half-day).
      - `writeVarianceCsv :: FilePath -> Map Epoch Double -> Map Epoch Double -> IO ()` → `notes/structural-econometrcics/data/variance.csv` with header (epoch, sigma2, sigma2_instrument) and a comment/first-line noting the window definition.
    Wire the `variance` subcommand in `app/Main.hs`.
    Add `econometrics/test/Panel/VarianceSpec.hs` implementing the three behaviors against `swap-ticks-sample.csv`. Register the test.
    Run `stack test`. (variance.csv over LIVE ticks is produced at the estimation run in 09-09; here the writer runs on the fixture so the file exists and is well-formed.)
  </action>
  <verify>
    <automated>cd econometrics && stack test 2>&1 | tail -15; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack test` exits 0 and VarianceSpec cases pass
    - `grep -qE 'realizedVariance|instrumentVariance' econometrics/src/Panel/Variance.hs`
    - `grep -q 'dailyEpoch' econometrics/src/Panel/Variance.hs` (same boundary as the panel)
    - `grep -qi 'variance' econometrics/app/Main.hs`
    - `test -f notes/structural-econometrcics/data/variance.csv` with header containing `sigma2` and `instrument`
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/src/Panel/Variance.hs`
  </acceptance_criteria>
  <done>σ̂²_t and the disjoint-window instrument σ̃²_t are computed on the panel's daily boundary, golden-tested to 1e-9, and written to variance.csv.</done>
</task>

</tasks>

<verification>
- `stack build` and `stack test` green.
- variance.csv exists with σ̂²_t and instrument columns.
- Same daily boundary as the panel; no secrets/home paths.
</verification>

<success_criteria>
Realized variance σ̂²_t and an independently-windowed instrument σ̃²_t are produced from BigQuery Swap ticks, golden-tested, aligned to the panel epoch — supplying the EIV-flagged regressor and its instrument for estimation.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-05-SUMMARY.md`.
</output>
