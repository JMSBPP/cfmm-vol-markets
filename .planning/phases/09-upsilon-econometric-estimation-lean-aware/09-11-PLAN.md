---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 11
type: execute
wave: 6
depends_on: [6, 9]
files_modified:
  - quality_reports/audits/2026-07-19-phase09-audit.md
  - notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md
autonomous: false
requirements: [CTX-AUDIT]
must_haves:
  truths:
    - "audit-econ runs over the analysis output with full data lineage (3 independent agents, Delphi consensus)"
    - "Any Critical/High finding mapping to spec-faithfulness or estimator-correctness FAILs the phase and is fixed + re-audited"
    - "The final audit verdict is PASS with no unresolved Critical/High spec-faithfulness/estimator-correctness findings"
  artifacts:
    - path: "quality_reports/audits/2026-07-19-phase09-audit.md"
      provides: "the audit-econ report (severity-sorted findings + verdict)"
      contains: "verdict"
  key_links:
    - from: "audit-econ skill"
      to: "notes/structural-econometrcics/analysis/ + data/ lineage"
      via: "backward lineage trace from analysis to raw data"
      pattern: "audit"
---

<objective>
Run the audit-econ gate (CTX-AUDIT) on the estimation output and pass it. The audit dispatches 3 independent Opus agents (different-agent rule, satisfied by the skill's design) that trace the analysis backward through the full data lineage and return Delphi-consensus, severity-sorted findings. Any Critical/High spec-faithfulness or estimator-correctness finding FAILs the phase until fixed and re-audited.

Purpose: The phase completion gate — independent verification that the estimation is faithful to the locked spec and the estimator is correct.
Output: `quality_reports/audits/<date>-phase09-audit.md` with a PASS verdict (after any fixes + re-audit).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md
@notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md
</context>

<constraints>
- The audit target is the analysis output; the audit needs the FULL lineage self-describing (subgraph pulls, BigQuery exports, construction steps) — 09-09 must have produced it that way. If lineage is not traceable, fix the analysis output first.
- Invocation: `audit-econ <target> --agents 3` (skill at /home/jmsbpp/.claude/skills/audit-econ/; defaults 3 Opus agents, high effort). Report lands at `quality_reports/audits/<date>-<phase>-audit.md`.
- Gate semantics: any Critical/High mapping to a spec-faithfulness or estimator-correctness DEFECT FAILs the phase → fix → re-audit until PASS. Mid/Low findings are recorded but do not block.
- The GAMS cross-check (09-10) is NON-BLOCKING — a pending cross-check does not fail the audit gate.
- No home paths / secrets introduced by the fixes.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Run audit-econ over the analysis output and its lineage</name>
  <files>quality_reports/audits/2026-07-19-phase09-audit.md</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (section audit-econ Gate — mechanics, input it needs, gate semantics)
    - notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md (the target + its DATA LINEAGE section)
  </read_first>
  <action>
    Confirm the analysis output (`notes/structural-econometrcics/analysis/<date>-upsilon-estimates.md`) exists and its DATA LINEAGE section names every upstream artifact (panel.csv, variance.csv, estimation-panel.csv, DATA-SOURCES.md, the cross-walk) with construction steps. If any lineage link is missing, fix the analysis output so the audit can trace it.
    Invoke the audit: `audit-econ notes/structural-econometrcics/analysis/<date>-upsilon-estimates.md --agents 3` (use the live date). The skill dispatches 3 independent agents, forms Delphi consensus, and writes the severity-sorted report to `quality_reports/audits/<date>-phase09-audit.md`.
    Record the verdict and the count of Critical/High/Mid/Low findings in the 09-11 SUMMARY.
  </action>
  <verify>
    <automated>d=$(date +%Y-%m-%d); ls quality_reports/audits/*phase09*audit.md 2>/dev/null && echo "AUDIT REPORT PRESENT"; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - an audit report exists: `ls quality_reports/audits/*phase09*audit.md` succeeds
    - the report contains a verdict and severity-sorted findings: `grep -qiE 'verdict|critical|high|pass|fail' quality_reports/audits/*phase09*audit.md`
    - the analysis output's DATA LINEAGE section names the upstream CSVs: `grep -qiE 'panel.csv|variance.csv|estimation-panel.csv' notes/structural-econometrcics/analysis/*upsilon-estimates.md`
  </acceptance_criteria>
  <done>audit-econ has run over the analysis output with traceable lineage; the report exists with a verdict + severity-sorted findings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Triage the audit verdict — fix Critical/High spec-faithfulness/estimator findings and re-audit to PASS</name>
  <action>This is a user checkpoint: the executor surfaces the audit-econ verdict. Every Critical/High finding mapping to a spec-faithfulness or estimator-correctness defect must be fixed in the Haskell/analysis artifacts and re-audited until none remain; Mid/Low are recorded but non-blocking. A pending GAMS cross-check (09-10) does not fail this gate. Resume only on a PASS verdict.</action>
  <what-built>
    Task 1 ran `audit-econ --agents 3` and produced `quality_reports/audits/<date>-phase09-audit.md` with a Delphi-consensus, severity-sorted verdict over the estimation output and its lineage.
  </what-built>
  <how-to-verify>
    1. Open `quality_reports/audits/<date>-phase09-audit.md` and review the verdict + findings.
    2. For EACH Critical/High finding that maps to a spec-faithfulness defect (deviation from the locked spec: the estimating equation, the EIV remedy, the three tests, the four alternatives) OR an estimator-correctness defect (sandwich SE, LM fit, IV): fix the underlying Haskell/analysis artifact, re-run the affected `stack test` and the pipeline, and RE-INVOKE `audit-econ` until no Critical/High spec-faithfulness/estimator finding remains.
    3. Mid/Low findings: record for follow-up; they do not block.
    4. A pending GAMS cross-check (09-10) is non-blocking and does not fail this gate.
    5. Confirm the FINAL verdict is PASS.
  </how-to-verify>
  <resume-signal>Reply "audit PASS" once no Critical/High spec-faithfulness/estimator-correctness finding remains, or describe the findings to be fixed.</resume-signal>
</task>

</tasks>

<verification>
- `quality_reports/audits/<date>-phase09-audit.md` exists with a PASS verdict.
- No unresolved Critical/High spec-faithfulness or estimator-correctness finding.
- All `stack test` + `lake build vol_markets` still green after any fixes.
</verification>

<success_criteria>
The estimation output passes the audit-econ gate: independent agents trace it backward through full lineage and return no unresolved Critical/High spec-faithfulness or estimator-correctness finding — the phase's completion gate is green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-11-SUMMARY.md` (record the final verdict + finding counts).
</output>
