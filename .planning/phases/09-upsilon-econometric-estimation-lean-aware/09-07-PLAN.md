---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 07
type: execute
wave: 3
depends_on: [4, 5]
files_modified:
  - econometrics/src/Model/Upsilon.hs
  - econometrics/src/Model/NLS.hs
  - econometrics/src/Model/EIV.hs
  - econometrics/app/Main.hs
  - econometrics/package.yaml
  - econometrics/test/Model/UpsilonSpec.hs
  - econometrics/test/Model/NLSSpec.hs
  - notes/structural-econometrcics/analysis/lean-haskell-crosswalk.md
autonomous: true
requirements: [CTX-EST]
must_haves:
  truths:
    - "Model/Upsilon.hs encodes υ, the moneyness distance, and the tick grid BYTE-FOR-BYTE as the Lean definitions (upsilon finite diff, |i_K−i_t|, λ=1.0001)"
    - "The primary NLS optimizer is hmatrix-gsl Levenberg-Marquardt (Numeric.GSL.Fitting) fitting π = β₀ + υ₀·exp(−κ·d)·σ̂²"
    - "The ad-based hand-rolled Gauss-Newton/LM is retained as a cross-check that recovers the same planted params on synthetic data"
    - "The EIV two-noisy-measures IV/GMM plumbing instruments σ̂² with σ̃² (hand-rolled)"
  artifacts:
    - path: "econometrics/src/Model/Upsilon.hs"
      provides: "the Lean-mirrored model function + moneyness distance + tick grid"
      exports: ["model", "moneyness"]
    - path: "econometrics/src/Model/NLS.hs"
      provides: "GSL-LM primary fit + ad cross-check"
      exports: ["fitGSL", "fitAD"]
    - path: "econometrics/src/Model/EIV.hs"
      provides: "two-noisy-measures IV/GMM using σ̃² as instrument"
    - path: "notes/structural-econometrcics/analysis/lean-haskell-crosswalk.md"
      provides: "Lean name ↔ Haskell name ↔ spec § cross-walk table"
      contains: "upsilon"
  key_links:
    - from: "econometrics/src/Model/NLS.hs"
      to: "Numeric.GSL.Fitting (hmatrix-gsl)"
      via: "Levenberg-Marquardt fit of the exp-moneyness model"
      pattern: "GSL.Fitting|nlFitting|fitModel"
    - from: "econometrics/src/Model/Upsilon.hs"
      to: "lean/vol_markets/Upsilon.lean"
      via: "identical υ / distance / λ=1.0001 formulas"
      pattern: "1.0001|exp"
---

<objective>
Implement the estimator core (CTX-EST): the Lean-mirrored model function, the primary GSL Levenberg-Marquardt NLS fit, an `ad`-based cross-check, and the EIV two-noisy-measures IV/GMM plumbing. The model function `f(θ) = β₀ + υ₀·exp(−κ·|i_K−i_t|)·σ̂²_t` mirrors `Upsilon.upsilon` / `upsilonTickSlope` and the λ=1.0001 grid byte-for-byte, backed by a Lean↔Haskell↔spec cross-walk table (the "formal witness" claim depends on this fidelity).

Purpose: The load-bearing estimation machinery, with the GSL LM as primary optimizer (GSL now installed) and the exact Lean-mirroring that lets κ̂ witness the bridging lemma.
Output: `Model/{Upsilon,NLS,EIV}.hs`, an `estimate` CLI stage, synthetic-recovery tests, and the cross-walk table.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md
@lean/vol_markets/Upsilon.lean
@lean/vol_markets/PosSpec.lean
@econometrics/src/Econ/Types.hs
</context>

<constraints>
- **Haskell ONLY.** Primary NLS = **hmatrix-gsl** `Numeric.GSL.Fitting` Levenberg-Marquardt (GSL now installed system-wide). Keep the `ad`-based Gauss-Newton/LM ONLY as a cross-check golden (both recover the same planted params); do not make `ad` the primary.
- The estimating equation is LOCKED (spec §4.3): `π_it = β₀ + υ₀·exp(−κ·|i_K(i) − i_t|)·σ̂²_t + v_it`. Implement verbatim; do NOT re-derive.
- **Byte-for-byte Lean mirror:** the distance `|i_K − i_t|`, the λ=1.0001 tick grid (PosSpec.lam), and the exp form MUST match the Lean `upsilon`/`upsilonTickSlope`/`ATMOTMNullHypothesis` forms. Keep the cross-walk table current.
- EIV remedy is LOCKED (spec §4.3): instrument σ̂²_t with the second-window σ̃²_t (two-noisy-measures IV). Hand-rolled — no package ships it.
- No `/home/`, `$HOME`, `~/` paths.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model/Upsilon.hs — the Lean-mirrored model function + moneyness + tick grid, with cross-walk table</name>
  <files>econometrics/src/Model/Upsilon.hs, econometrics/test/Model/UpsilonSpec.hs, econometrics/package.yaml, notes/structural-econometrcics/analysis/lean-haskell-crosswalk.md</files>
  <read_first>
    - lean/vol_markets/Upsilon.lean (upsilon, upsilonTickSlope, ATMOTMNullHypothesis — exact formulas)
    - lean/vol_markets/PosSpec.lean (lam = 1.0001, tickPrice)
    - notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md (§4.1, §4.3 functional form)
  </read_first>
  <behavior>
    - Test 1: `model (Theta b0 u0 k) (d, s2)` equals `b0 + u0 * exp(negate k * d) * s2` for sample inputs.
    - Test 2: `moneyness iK it` equals `abs (fromIntegral (iK - it))` (tick distance, matching |i_K − i_t|).
    - Test 3: the tick grid constant equals 1.0001 (matches PosSpec.lam) — assert `tickBase == 1.0001`.
  </behavior>
  <action>
    Create `econometrics/src/Model/Upsilon.hs` (module `Model.Upsilon`). Implement, generic over `Floating a` so `ad` can differentiate it:
      - `model :: Floating a => [a] -> (a, a) -> a` where `model [b0,u0,k] (d,s2) = b0 + u0 * exp (negate k * d) * s2` (verbatim spec §4.3).
      - `moneyness :: Int -> Int -> Double` = `abs (fromIntegral (iK - it))` (the |i_K − i_t| tick distance).
      - `tickBase :: Double` = `1.0001` with a comment `-- mirrors PosSpec.lam`.
      - `modelSplit` for the κ⁺/κ⁻ variant (split the distance by sign of (iK − it) → 4 params), used by the symmetry test in 09-08.
    Write `notes/structural-econometrcics/analysis/lean-haskell-crosswalk.md`: a table with columns [Lean name | Haskell name | spec §], covering at least: `upsilon` ↔ `Model.Upsilon.model` (§4.3), `upsilonTickSlope` ↔ the discrete slope used in the ATM test, `|i_K − i_t|` ↔ `moneyness` (§4.3), `PosSpec.lam`/`tickPrice` ↔ `tickBase` (§2.4), `ATMOTMNullHypothesis` ↔ the κ>0 test (§5). This table is the auditable evidence for the "formal witness" claim.
    Add `Model.UpsilonSpec.hs` implementing the three behaviors; register the test. `stack test` green.
  </action>
  <verify>
    <automated>cd econometrics && stack test 2>&1 | tail -12; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack test` exits 0 and UpsilonSpec passes
    - `grep -q 'module Model.Upsilon' econometrics/src/Model/Upsilon.hs` and `grep -qE 'b0 \+ u0 \* exp' econometrics/src/Model/Upsilon.hs` (verbatim model form)
    - `grep -q '1.0001' econometrics/src/Model/Upsilon.hs` (mirrors PosSpec.lam)
    - `test -f notes/structural-econometrcics/analysis/lean-haskell-crosswalk.md` and `grep -q 'upsilon' notes/structural-econometrcics/analysis/lean-haskell-crosswalk.md`
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/src/Model/Upsilon.hs`
  </acceptance_criteria>
  <done>The model function, moneyness distance, and tick grid mirror the Lean definitions exactly; the cross-walk table documents the mapping.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NLS.hs (GSL-LM primary + ad cross-check) and EIV.hs (two-noisy-measures IV), synthetic recovery</name>
  <files>econometrics/src/Model/NLS.hs, econometrics/src/Model/EIV.hs, econometrics/app/Main.hs, econometrics/test/Model/NLSSpec.hs, econometrics/package.yaml</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Architecture Pattern 1 NLS via GN/LM with ad; §Architecture Pattern 2 EIV two-noisy-measures)
    - notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md (§4.3 the EIV instrument)
    - econometrics/src/Model/Upsilon.hs (the model function from Task 1)
  </read_first>
  <behavior>
    - Test 1: on synthetic data generated from known (β₀,υ₀,κ) with small noise, `fitGSL` recovers the planted params within tolerance (e.g. rel 1e-2).
    - Test 2: `fitAD` (hand-rolled ad Gauss-Newton/LM) recovers the SAME planted params within tolerance — the cross-check.
    - Test 3: `ivFit` (EIV) on synthetic data with a deliberately mismeasured σ̂² and a valid instrument σ̃² recovers υ₀ with LESS attenuation than the naive `fitGSL` on the mismeasured regressor.
  </behavior>
  <action>
    Create `econometrics/src/Model/NLS.hs` (module `Model.NLS`):
      - `fitGSL :: Panel -> Theta` — PRIMARY. Use `Numeric.GSL.Fitting` Levenberg-Marquardt (`nlFitting` / `fitModel`) fitting `Model.Upsilon.model` to `(π_it)` over `(d_it, σ̂²_t)`; supply the residual + (analytic or GSL-numeric) Jacobian. Return the fitted Theta (+ the JᵀJ / covariance handle for 09-08's SE).
      - `fitAD :: Panel -> Theta` — cross-check. Hand-rolled Gauss-Newton/LM: `ad` (Numeric.AD.jacobian) for the exact 3-col Jacobian J; LM update `θ ← θ − (JᵀJ + μI)⁻¹ Jᵀr` via hmatrix solve; adapt μ on the residual-norm trend.
    Create `econometrics/src/Model/EIV.hs` (module `Model.EIV`): `ivFit :: Panel -> Theta` implementing the two-noisy-measures IV/GMM — instrument σ̂²_t with σ̃²_t (from variance.csv), moment `E[Z·v]=0` with Z from σ̃²; for the nonlinear model use the linearized moment or 2-step GMM on the exp-transformed regressor (per spec §4.3). Hand-rolled.
    Wire the `estimate` subcommand in `app/Main.hs` to read panel.csv + variance.csv, run `fitGSL` (and `ivFit`), and print/emit the estimates (the full analysis output is written in 09-09).
    Add `Model.NLSSpec.hs` implementing the three behaviors with a fixed RNG seed for reproducibility. Register the test. `stack test` green.
  </action>
  <verify>
    <automated>cd econometrics && stack test 2>&1 | tail -15; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack test` exits 0 and NLSSpec passes (GSL and ad both recover planted params; IV reduces attenuation)
    - `grep -qE 'Numeric.GSL.Fitting|GSL.Fitting' econometrics/src/Model/NLS.hs` (GSL is the primary optimizer)
    - `grep -q 'fitGSL' econometrics/src/Model/NLS.hs` and `grep -q 'fitAD' econometrics/src/Model/NLS.hs` (cross-check retained)
    - `grep -qE 'ivFit' econometrics/src/Model/EIV.hs` and `grep -qiE 'instrument|sigma2_instr|GMM' econometrics/src/Model/EIV.hs`
    - `grep -qi 'estimate' econometrics/app/Main.hs`
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/src/Model/NLS.hs econometrics/src/Model/EIV.hs`
  </acceptance_criteria>
  <done>GSL-LM is the primary fit and recovers planted params; the ad path cross-checks it; EIV IV/GMM instruments σ̂² with σ̃² and reduces attenuation — all synthetic-tested.</done>
</task>

</tasks>

<verification>
- `stack test` green; GSL and ad both recover planted params; IV reduces attenuation.
- Model mirrors Lean (1.0001 grid, verbatim exp form); cross-walk table exists.
- No home paths.
</verification>

<success_criteria>
The estimator core fits `π = β₀ + υ₀·exp(−κ·d)·σ̂²` via GSL Levenberg-Marquardt (primary) with an `ad` cross-check and a two-noisy-measures EIV remedy, all mirroring the Lean definitions byte-for-byte with an auditable cross-walk table.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-07-SUMMARY.md`.
</output>
