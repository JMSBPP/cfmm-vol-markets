---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 04
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - econometrics/src/Panel/Subgraph.hs
  - econometrics/src/Panel/Build.hs
  - econometrics/app/Main.hs
  - econometrics/package.yaml
  - econometrics/test/Panel/BuildSpec.hs
  - econometrics/test/fixtures/subgraph-sample.json
  - notes/structural-econometrcics/data/panel.csv
autonomous: true
requirements: [CTX-PANEL]
must_haves:
  truths:
    - "The subgraph client fetches positions/premia/strikes for the confirmed pool with pagination"
    - "The panel has one row per tokenId per daily epoch with π_it as the per-epoch DELTA of cumulative premia (not the raw cumulative total)"
    - "i_K is the strike tick (log_λ K, λ=1.0001, matching PosSpec.tickPrice) and i_t the pool tick per epoch"
  artifacts:
    - path: "econometrics/src/Panel/Subgraph.hs"
      provides: "GraphQL client + paginated fetch of TokenId/Leg/AccountBalance"
      exports: ["fetchPositions"]
    - path: "econometrics/src/Panel/Build.hs"
      provides: "cumulative→delta epoch assembly into Panel"
    - path: "notes/structural-econometrcics/data/panel.csv"
      provides: "the tokenId×daily panel (partial: π_it, i_K, i_t; σ̂² joined in 09-05)"
  key_links:
    - from: "econometrics/src/Panel/Build.hs"
      to: "notes/structural-econometrcics/data/panel.csv"
      via: "cassava writeFile of per-epoch deltas"
      pattern: "premiaSettled|delta"
---

<objective>
Build the position-epoch panel (CTX-PANEL): fetch tokenId-level positions, premia, and strikes from the confirmed Panoptic subgraph, snapshot daily, and diff the CUMULATIVE premia into per-epoch flows π_it. One row per tokenId per daily epoch, with strike tick i_K and pool tick i_t on the λ=1.0001 grid that mirrors `PosSpec.tickPrice`.

Purpose: The left-hand side (π_it) and the moneyness inputs (i_K, i_t) of the estimation equation.
Output: `econometrics/src/Panel/{Subgraph,Build}.hs`, a `build-panel` CLI stage, and `notes/structural-econometrcics/data/panel.csv` (σ̂² column joined in 09-05).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@notes/structural-econometrcics/data/DATA-SOURCES.md
@notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md
@lean/vol_markets/PosSpec.lean
@econometrics/src/Econ/Types.hs
</context>

<constraints>
- Haskell ONLY (http-conduit/req + aeson + cassava). No Python for munging.
- π_it MUST be the daily DELTA of `AccountBalance.premiaSettled*Total` (cumulative → per-epoch), NOT the raw cumulative (RESEARCH Pitfall 2).
- Read the confirmed endpoint from `notes/structural-econometrcics/data/DATA-SOURCES.md`; read any `GRAPH_API_KEY` from the worktree `.env` at runtime — NEVER hardcode/print/commit it.
- Define the daily epoch ONCE (block-at-timestamp for a fixed UTC hour); the SAME boundary is reused by 09-05's variance window.
- i_K = log_λ Leg.strike with λ=1.0001 (matches PosSpec.lam / tickPrice); i_t = pool tick per epoch. Same units as Lean.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Subgraph GraphQL client with paginated position/premia fetch</name>
  <files>econometrics/src/Panel/Subgraph.hs, econometrics/package.yaml, econometrics/test/fixtures/subgraph-sample.json</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Code Examples — the verified GraphQL query + entity schema: TokenId, Leg, PremiumSettled, AccountBalance, Collateral)
    - notes/structural-econometrcics/data/DATA-SOURCES.md (confirmed endpoint, pool address, auth requirement)
    - econometrics/src/Econ/Types.hs (Obs record)
  </read_first>
  <action>
    Create `econometrics/src/Panel/Subgraph.hs` (module `Panel.Subgraph`). Implement a POST GraphQL client (http-conduit or req) that reads the endpoint URL from DATA-SOURCES.md-recorded config (pass via CLI arg or an `econometrics/config` value; do NOT hardcode) and, if required, an auth header from `GRAPH_API_KEY` in the environment (`System.Environment.lookupEnv`). Parse responses with aeson.
    Implement paginated fetch (subgraph `first`/`skip` or block-cursor) of, for the confirmed `underlyingPool`:
      - TokenId { id, pool, legs { strike, width, isLong, tokenType, optionRatio } }
      - AccountBalance { premiaSettled0Total, premiaSettled1Total, ...InEthTotal, ...InUsdTotal } snapshotted with block/timestamp
      - the pool tick per snapshot (from the subgraph pool entity if available; else leave a hook for 09-05/BigQuery to supply i_t)
    Export `fetchPositions :: Endpoint -> PoolAddr -> IO [RawPosition]` returning a typed record set.
    Add a frozen fixture `econometrics/test/fixtures/subgraph-sample.json` = a small real (or realistically-shaped) response with ≥2 tokenIds and ≥2 daily snapshots each (used by Task 2's test). Add `aeson`/`http-conduit`/`req` to package.yaml if not already present.
    `stack build` must stay green.
  </action>
  <verify>
    <automated>cd econometrics && stack build 2>&1 | tail -5; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack build` exits 0
    - `grep -q 'module Panel.Subgraph' econometrics/src/Panel/Subgraph.hs` and `grep -q 'fetchPositions' econometrics/src/Panel/Subgraph.hs`
    - `grep -q 'lookupEnv' econometrics/src/Panel/Subgraph.hs` (key/endpoint read from env, not hardcoded)
    - `test -f econometrics/test/fixtures/subgraph-sample.json` and `grep -q 'premiaSettled' econometrics/test/fixtures/subgraph-sample.json`
    - `! grep -qIE 'GRAPH_API_KEY *= *["'\'']|/home/[a-z0-9_-]+/' econometrics/src/Panel/Subgraph.hs` (no inline key, no home path)
  </acceptance_criteria>
  <done>Subgraph client compiles, paginates, reads endpoint+key from config/env, and a representative JSON fixture is frozen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cumulative→delta epoch assembly with a fixture-driven test + panel.csv writer</name>
  <files>econometrics/src/Panel/Build.hs, econometrics/app/Main.hs, econometrics/test/Panel/BuildSpec.hs, econometrics/package.yaml, notes/structural-econometrcics/data/panel.csv</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Architecture Pattern 3 cumulative→delta; Pitfall 2)
    - notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md (§4.3 the estimating equation; π_it definition)
    - lean/vol_markets/PosSpec.lean (lam = 1.0001, tickPrice — the i_K grid)
    - econometrics/test/fixtures/subgraph-sample.json (from Task 1)
  </read_first>
  <behavior>
    - Test 1: two consecutive daily snapshots of `premiaSettled*Total` diff to the correct per-epoch π_it (delta), NOT the cumulative total.
    - Test 2: i_K = round(log(strike)/log(1.0001)) matches the expected tick for a known strike in the fixture.
    - Test 3: one Panel row is emitted per (tokenId, epoch); a tokenId with N daily snapshots yields N−1 delta rows.
  </behavior>
  <action>
    Create `econometrics/src/Panel/Build.hs` (module `Panel.Build`). Implement:
      - `dailyEpoch :: UTCTime -> Int` — a fixed-UTC-hour daily bucket (define the boundary ONCE; document it; 09-05 reuses it).
      - `deltaPremia :: [Snapshot] -> [(Epoch, Double)]` — sort by time, diff consecutive cumulative `premiaSettled*Total` into per-epoch flows.
      - `strikeToTick :: Double -> Int` — `round (log strike / log 1.0001)` (λ=1.0001, mirrors PosSpec.lam).
      - `assemble :: [RawPosition] -> Panel` — join into `Econ.Types.Obs` rows (obsSigma2 left as NaN/0 placeholder — filled by 09-05).
      - `writePanelCsv :: FilePath -> Panel -> IO ()` via cassava, writing `notes/structural-econometrcics/data/panel.csv` with a header row and self-describing columns (tokenId, epoch, premium_delta, strike_tick, pool_tick, sigma2_placeholder).
    Wire the `build-panel` subcommand in `app/Main.hs` to fetch → assemble → writePanelCsv.
    Add `econometrics/test/Panel/BuildSpec.hs` implementing the three behaviors above against `subgraph-sample.json` (parse fixture → assemble → assert deltas, tick, row count). Register the test in package.yaml.
    Run `stack test`. (panel.csv against LIVE data is produced at the estimation run in 09-09; here the CSV writer is exercised on the fixture so the file exists and is well-formed.)
  </action>
  <verify>
    <automated>cd econometrics && stack test 2>&1 | tail -15; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack test` exits 0 and the BuildSpec cases pass
    - `grep -q 'module Panel.Build' econometrics/src/Panel/Build.hs` and `grep -qE 'deltaPremia|strikeToTick' econometrics/src/Panel/Build.hs`
    - `grep -q '1.0001' econometrics/src/Panel/Build.hs` (λ mirrors PosSpec.lam)
    - `grep -qi 'build-panel' econometrics/app/Main.hs`
    - `test -f notes/structural-econometrcics/data/panel.csv` with a header line (`head -1` non-empty)
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/src/Panel/Build.hs`
  </acceptance_criteria>
  <done>Panel assembly diffs cumulative premia to per-epoch flows, computes i_K on the λ=1.0001 grid, passes the fixture test, and writes a well-formed panel.csv.</done>
</task>

</tasks>

<verification>
- `stack build` and `stack test` green.
- panel.csv exists with per-epoch delta rows and λ=1.0001 strike ticks.
- No inline keys / home paths.
</verification>

<success_criteria>
A tokenId×daily panel is produced with π_it as per-epoch premium deltas, i_K on the λ=1.0001 tick grid mirroring PosSpec, and i_t per epoch — ready for the σ̂² join in 09-05 and estimation in 09-07/09-09.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-04-SUMMARY.md`.
</output>
