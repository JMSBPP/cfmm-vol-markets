---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - econometrics/package.yaml
  - econometrics/stack.yaml
  - econometrics/src/Econ/Types.hs
  - econometrics/app/Main.hs
  - econometrics/test/Spec.hs
  - econometrics/test/Golden/SandwichFixture.hs
autonomous: true
requirements: [CTX-EST]
must_haves:
  truths:
    - "econometrics/ Stack project builds green and links hmatrix + hmatrix-gsl"
    - "A test suite exists and runs via stack test with a hand-computed sandwich-SE golden fixture"
    - "No home-absolute paths (/home/, $HOME, ~/) appear in any econometrics/ source"
  artifacts:
    - path: "econometrics/package.yaml"
      provides: "Haskell dependency manifest incl. hmatrix-gsl"
      contains: "hmatrix-gsl"
    - path: "econometrics/stack.yaml"
      provides: "Stackage LTS pin for reproducibility"
    - path: "econometrics/test/Golden/SandwichFixture.hs"
      provides: "hand-computed 2-cluster/3-obs toy panel + expected CR0 covariance V"
  key_links:
    - from: "econometrics/package.yaml"
      to: "system GSL + BLAS/LAPACK"
      via: "stack build linking hmatrix-gsl"
      pattern: "hmatrix-gsl"
---

<objective>
Scaffold the `econometrics/` Haskell Stack project (the ONLY estimation language for this phase — hard user directive) and stand up the test harness with the hand-computed cluster-robust sandwich-SE golden fixture. This is the Wave-0 build gate: everything downstream compiles and tests against this scaffold.

Purpose: Give every later plan a green `stack build` / `stack test` surface, and lock the numeric stack (hmatrix + hmatrix-gsl + ad, no Python/R anywhere).
Output: A buildable `econometrics/` project with an LTS pin, a shared types module, an `optparse` CLI skeleton, and a runnable test suite carrying the sandwich-SE golden fixture data.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-VALIDATION.md
</context>

<constraints>
- **Haskell ONLY.** No Python/R/other language files anywhere in the pipeline, not even for data munging.
- **GSL is now installed** (`gsl-config --version` → 2.8; libs `-L/usr/lib -lgsl -lgslcblas -lm`; headers `/usr/include/gsl/`). `hmatrix-gsl` is ALLOWED and PREFERRED. Do NOT add a `! grep hmatrix-gsl` prohibition anywhere.
- No `/home/`, `$HOME`, `~/` paths in any tracked file (Phase-1 rule) — applies to Haskell sources, cabal/yaml, and comments.
- Outputs of later stages land in `notes/structural-econometrcics/{data,analysis}/`, NOT under `econometrics/` (skill layout).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold econometrics/ Stack project with pinned LTS and the full numeric dependency set</name>
  <files>econometrics/package.yaml, econometrics/stack.yaml, econometrics/src/Econ/Types.hs, econometrics/app/Main.hs</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Standard Stack, §Architecture Patterns — project structure; §Open Questions 3 = LTS pin)
    - CLAUDE.md (Lean4+math session boundary; no home-absolute paths)
  </read_first>
  <action>
    From the repo root run `stack new econometrics` (or hand-create the tree). Set the project layout to:
      econometrics/
        package.yaml, stack.yaml
        src/Econ/Types.hs
        app/Main.hs
        test/Spec.hs  test/Golden/SandwichFixture.hs
    In `package.yaml` declare these dependencies (library + executable + test stanzas as needed):
      hmatrix, hmatrix-gsl, ad, statistics, math-functions, vector, containers, cassava, aeson, req, http-conduit, bytestring, text, optparse-applicative
      test deps: hspec, hspec-discover
    Pick a Stackage LTS that ships `hmatrix`. Run `stack --resolver <lts> ls dependencies | grep -E 'hmatrix|hmatrix-gsl|ad |statistics|cassava|aeson'` to confirm versions. If `hmatrix-gsl` is NOT in the chosen LTS, add it (and any needed companion) to `stack.yaml` `extra-deps:` with the EXACT version string, and record that version in a comment in stack.yaml. Confirm the system GSL is found (`gsl-config --version` prints 2.8).
    `src/Econ/Types.hs`: define the shared panel/observation record the rest of the pipeline uses — module `Econ.Types`, exporting at minimum:
      data Obs = Obs { obsTokenId :: !Text, obsEpoch :: !Int, obsPremium :: !Double, obsStrikeTick :: !Int, obsPoolTick :: !Int, obsSigma2 :: !Double, obsSigma2Instr :: !Double } deriving (Show, Eq)
      type Panel = [Obs]
      data Theta = Theta { b0 :: !Double, u0 :: !Double, kappa :: !Double } deriving (Show, Eq)
    `app/Main.hs`: an `optparse-applicative` CLI skeleton with subcommands `fetch | build-panel | variance | estimate | test` — each a stub that prints "not yet implemented" and exits 0 for now (later plans fill them in). Do NOT hardcode any absolute path.
    Verify a clean build: `cd econometrics && stack build`.
  </action>
  <verify>
    <automated>cd econometrics && stack build 2>&1 | tail -5; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack build` exits 0
    - `grep -q 'hmatrix-gsl' econometrics/package.yaml` succeeds
    - `grep -q 'hmatrix' econometrics/package.yaml` and `grep -q 'ad' econometrics/package.yaml` succeed
    - `test -f econometrics/stack.yaml` and it names a concrete `resolver: lts-*` (or `snapshot:`)
    - `grep -q 'module Econ.Types' econometrics/src/Econ/Types.hs` and `grep -q 'data Obs' econometrics/src/Econ/Types.hs`
    - `! grep -rIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/` (no home-absolute paths)
  </acceptance_criteria>
  <done>econometrics/ builds green under a pinned LTS with hmatrix-gsl linked; Econ.Types and the CLI skeleton compile; no home paths.</done>
</task>

<task type="auto">
  <name>Task 2: Stand up the hspec test suite with the hand-computed sandwich-SE golden fixture</name>
  <files>econometrics/test/Spec.hs, econometrics/test/Golden/SandwichFixture.hs, econometrics/package.yaml</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Don't Hand-Roll → "Cluster-robust sandwich SE" formula; §Validation Architecture — test map)
    - econometrics/src/Econ/Types.hs (the Obs/Panel/Theta records from Task 1)
  </read_first>
  <action>
    Wire `stack test` via hspec. In `package.yaml` add a test stanza `econometrics:test:unit` with `main: Spec.hs`, `hspec-discover` driver, deps hspec + the library.
    Create `test/Golden/SandwichFixture.hs` (module `Golden.SandwichFixture`) holding a FROZEN, hand-computed toy panel and its expected CR0 cluster-robust covariance. Use a 2-cluster, 3-observation panel (tokenId "A" with 2 obs, tokenId "B" with 1 obs). Provide, as top-level values with literal numbers computed BY HAND and documented in comments (show the arithmetic in the comment):
      - `toyJacobianRows :: [[Double]]`  -- each row = ∂f/∂θ = [∂/∂b0, ∂/∂u0, ∂/∂k] at that obs
      - `toyResiduals :: [Double]`       -- v_it per obs
      - `toyClusters :: [Text]`          -- tokenId per obs, e.g. ["A","A","B"]
      - `expectedV :: [[Double]]`        -- 3×3 CR0 sandwich V = (JᵀJ)⁻¹ [Σ_g (Σ J·v)(Σ J·v)ᵀ] (JᵀJ)⁻¹, hand-computed
      - `expectedSE :: [Double]`         -- sqrt of the diagonal of expectedV
    Choose small integer-ish J and v so the by-hand arithmetic is checkable; document each matrix step in comments so an auditor can retrace it.
    In `test/Spec.hs`, add a placeholder `describe "sandwich golden fixture"` spec that asserts the fixture is internally consistent (dimensions line up: length toyResiduals == length toyJacobianRows == length toyClusters; expectedV is 3×3; expectedSE == map sqrt (diagonal expectedV)). The ACTUAL SandwichSE implementation is built and tested against this fixture in plan 09-08 — this task only freezes the golden truth and proves the harness runs.
    Run `cd econometrics && stack test`.
  </action>
  <verify>
    <automated>cd econometrics && stack test 2>&1 | tail -15; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack test` exits 0
    - `grep -q 'expectedV' econometrics/test/Golden/SandwichFixture.hs` and `grep -q 'expectedSE' econometrics/test/Golden/SandwichFixture.hs`
    - `grep -q 'toyClusters' econometrics/test/Golden/SandwichFixture.hs`
    - `grep -Eq 'test:unit|tests:' econometrics/package.yaml` (a test stanza exists)
    - `! grep -rIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/test/`
  </acceptance_criteria>
  <done>`stack test` runs green; the sandwich-SE golden fixture is frozen with hand-computed V and SE and documented arithmetic, ready for 09-08 to implement against.</done>
</task>

</tasks>

<verification>
- `cd econometrics && stack build` exits 0 (hmatrix-gsl linked).
- `cd econometrics && stack test` exits 0.
- No home-absolute paths in econometrics/.
</verification>

<success_criteria>
The `econometrics/` Stack project builds green under a pinned LTS with the full numeric stack (hmatrix, hmatrix-gsl, ad) linked, exposes `Econ.Types` and an `optparse` CLI skeleton, and runs an hspec suite carrying the hand-computed sandwich-SE golden fixture.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-01-SUMMARY.md`.
</output>
