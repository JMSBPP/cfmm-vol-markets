---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 08
type: execute
wave: 4
depends_on: [7]
files_modified:
  - econometrics/src/Model/SandwichSE.hs
  - econometrics/src/Tests/Specification.hs
  - econometrics/app/Main.hs
  - econometrics/package.yaml
  - econometrics/test/Model/SandwichSpec.hs
  - econometrics/test/Tests/SpecificationSpec.hs
autonomous: true
requirements: [CTX-EST, CTX-TEST]
must_haves:
  truths:
    - "The cluster-robust (by tokenId) CR0 sandwich SE matches the hand-computed golden fixture to 1e-9"
    - "The three committed tests are computed: υ₀>0 (one-sided), κ>0 (one-sided, the null test), κ⁺=κ⁻ (Wald on the 4-param split)"
    - "Test statistics use the clustered covariance, not naive OLS SEs"
  artifacts:
    - path: "econometrics/src/Model/SandwichSE.hs"
      provides: "hand-rolled tokenId-clustered CR0 sandwich covariance"
      exports: ["clusterSandwich"]
    - path: "econometrics/src/Tests/Specification.hs"
      provides: "υ₀>0, κ>0, κ⁺=κ⁻ Wald/t statistics + p-values"
      exports: ["testUpsilonPos", "testKappaPos", "testSymmetry"]
  key_links:
    - from: "econometrics/src/Model/SandwichSE.hs"
      to: "econometrics/test/Golden/SandwichFixture.hs"
      via: "clusterSandwich toyJacobianRows toyResiduals toyClusters == expectedV"
      pattern: "clusterSandwich|expectedV"
---

<objective>
Implement the hand-rolled cluster-robust sandwich SE (the one estimator artifact no Haskell package ships) and the three committed specification tests (CTX-EST SE + CTX-TEST). The sandwich SE is golden-tested to 1e-9 against the fixture frozen in 09-01; the tests (υ₀>0, κ>0, κ⁺=κ⁻) use the clustered covariance.

Purpose: Robust inference for the estimates — the tokenId-clustered SEs and the sign/equality restrictions that ARE the null-hypothesis test (κ>0) and the symmetry check.
Output: `Model/SandwichSE.hs`, `Tests/Specification.hs`, a `test`/`estimate` CLI wiring, and passing golden + specification tests.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md
@econometrics/test/Golden/SandwichFixture.hs
@econometrics/src/Model/Upsilon.hs
</context>

<constraints>
- **Hand-rolled** cluster-robust sandwich SE — no package provides clustered NLS SEs (locked decision). Must match the 09-01 golden fixture to 1e-9.
- The three tests are LOCKED (spec §5): υ₀>0 (one-sided t/Wald), κ>0 (one-sided t/Wald — THE null test), κ⁺=κ⁻ (Wald on the 2×2 sub-block of the 4-param split model). Do NOT add the deliberately-excluded tests (EIV overid J-test, β₀=0).
- CR0 formula (RESEARCH §Don't Hand-Roll): `V = (JᵀJ)⁻¹ [Σ_g (Σ_{it∈g} J_it·v_it)(Σ_{it∈g} J_it·v_it)ᵀ] (JᵀJ)⁻¹`. Decide + document any finite-sample correction (G/(G−1)·(N−1)/(N−k)).
- p-values from `statistics` distributions. No home paths.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SandwichSE.hs — tokenId-clustered CR0 covariance, golden-tested to 1e-9</name>
  <files>econometrics/src/Model/SandwichSE.hs, econometrics/test/Model/SandwichSpec.hs, econometrics/package.yaml, econometrics/app/Main.hs</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Don't Hand-Roll → the CR0 sandwich formula + test strategy)
    - econometrics/test/Golden/SandwichFixture.hs (toyJacobianRows, toyResiduals, toyClusters, expectedV, expectedSE — the frozen golden truth)
    - econometrics/src/Econ/Types.hs
  </read_first>
  <behavior>
    - Test 1: `clusterSandwich toyJacobianRows toyResiduals toyClusters` equals `expectedV` (3×3) elementwise to 1e-9.
    - Test 2: `map sqrt (diagonal (clusterSandwich ...))` equals `expectedSE` to 1e-9.
    - Test 3: with a SINGLE observation per cluster (no within-cluster correlation), the result reduces to the heteroskedasticity-robust (HC0) sandwich (sanity check).
  </behavior>
  <action>
    Create `econometrics/src/Model/SandwichSE.hs` (module `Model.SandwichSE`). Implement with hmatrix:
      - `clusterSandwich :: [[Double]] -> [Double] -> [Text] -> Matrix Double` — build J (rows = per-obs gradient ∂f/∂θ), residual vector v, and cluster labels; compute `bread = inv (tr J <> J)`; group obs by cluster, form each cluster's score sum `s_g = Σ_{it∈g} J_it·v_it`, meat `= Σ_g s_g s_gᵀ`; `V = bread <> meat <> bread`. Apply and document the chosen finite-sample correction.
      - `standardErrors :: Matrix Double -> [Double]` = sqrt of the diagonal.
    Add `Model.SandwichSpec.hs` implementing the three behaviors against the 09-01 fixture. Register the test. Wire SE reporting into the `estimate` CLI stage (gradients come from `Model.Upsilon.model` via `ad` at the fitted θ).
    `stack test` green.
  </action>
  <verify>
    <automated>cd econometrics && stack test 2>&1 | tail -12; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack test econometrics:test:unit` (or the suite) exits 0 with SandwichSpec passing
    - `grep -q 'clusterSandwich' econometrics/src/Model/SandwichSE.hs` and `grep -qE 'bread|meat|inv' econometrics/src/Model/SandwichSE.hs`
    - the SandwichSpec asserts equality to `expectedV`/`expectedSE` at tolerance 1e-9 (`grep -qE '1e-9|1.0e-9' econometrics/test/Model/SandwichSpec.hs`)
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/src/Model/SandwichSE.hs`
  </acceptance_criteria>
  <done>The cluster-robust CR0 sandwich SE matches the hand-computed golden to 1e-9 and reduces to HC0 with singleton clusters.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specification.hs — the three committed tests (υ₀>0, κ>0, κ⁺=κ⁻)</name>
  <files>econometrics/src/Tests/Specification.hs, econometrics/test/Tests/SpecificationSpec.hs, econometrics/package.yaml, econometrics/app/Main.hs</files>
  <read_first>
    - notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md (§5 Specification Tests — the exact three restrictions + the deliberately-excluded ones)
    - econometrics/src/Model/SandwichSE.hs (the clustered covariance the tests consume)
    - econometrics/src/Model/NLS.hs (fitGSL / the split-model fit for κ⁺,κ⁻)
  </read_first>
  <behavior>
    - Test 1: `testUpsilonPos` on a fit with υ̂₀ > 0 and small SE returns a one-sided statistic > 0 and a small p-value; with υ̂₀ ≈ 0 returns a large p-value.
    - Test 2: `testKappaPos` (the null test) similarly reflects κ̂ > 0 vs κ̂ ≈ 0.
    - Test 3: `testSymmetry` (Wald κ⁺=κ⁻ on the 2×2 sub-block) rejects when κ⁺ and κ⁻ are planted far apart and fails to reject when equal.
  </behavior>
  <action>
    Create `econometrics/src/Tests/Specification.hs` (module `Tests.Specification`). Implement, consuming a fitted Theta + its clustered covariance:
      - `testUpsilonPos :: Theta -> Matrix Double -> TestResult` — one-sided t/Wald on υ̂₀ > 0 (robust, clustered).
      - `testKappaPos :: Theta -> Matrix Double -> TestResult` — one-sided t/Wald on κ̂ > 0 (THE null-hypothesis test; H₀: κ=0 vs H₁: κ>0).
      - `testSymmetry :: Theta4 -> Matrix Double -> TestResult` — Wald test κ⁺=κ⁻ using the 2×2 sub-block of the split-model (4-param) covariance; rejection ⇒ put/call-side skew.
      - `data TestResult = TestResult { statistic :: Double, pValue :: Double, reject :: Bool }` with p-values from `statistics` (Normal/χ²/t).
    Add `Tests.SpecificationSpec.hs` implementing the three behaviors on constructed Theta+covariance inputs. Register the test. Wire the three tests into the `estimate` CLI output.
    `stack test` green.
  </action>
  <verify>
    <automated>cd econometrics && stack test 2>&1 | tail -12; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack test` exits 0 with SpecificationSpec passing
    - `grep -qE 'testUpsilonPos|testKappaPos|testSymmetry' econometrics/src/Tests/Specification.hs`
    - `grep -qi 'one-sided\|one_sided\|kappa' econometrics/src/Tests/Specification.hs` (the κ>0 null test present)
    - the deliberately-excluded tests are absent: `! grep -qiE 'J-test|overid|beta0 = 0|zero-intercept' econometrics/src/Tests/Specification.hs`
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/src/Tests/Specification.hs`
  </acceptance_criteria>
  <done>The three committed tests compute clustered t/Wald statistics + p-values; the excluded tests are not present.</done>
</task>

</tasks>

<verification>
- `stack test` green; sandwich SE matches golden to 1e-9; the three tests behave correctly on constructed inputs.
- No home paths; excluded tests absent.
</verification>

<success_criteria>
Cluster-robust (tokenId) sandwich SEs match the hand-computed golden to 1e-9, and the three locked tests — υ₀>0, κ>0 (the null test), κ⁺=κ⁻ — are computed against the clustered covariance.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-08-SUMMARY.md`.
</output>
