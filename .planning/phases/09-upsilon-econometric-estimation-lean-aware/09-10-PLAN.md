---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 10
type: execute
wave: 6
depends_on: [9]
files_modified:
  - notes/structural-econometrcics/data/gams-xcheck-handoff.md
  - notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md
autonomous: false
requirements: [CTX-XCHECK]
must_haves:
  truths:
    - "A handoff note specifies the exact 3-variable NLS objective and points to the exported estimation-panel CSV plus the Haskell point estimates for comparison"
    - "The handoff is sent to the GAMS-development session via claude-peers and the send is recorded"
    - "The numeric agreement check (relative 1e-3 per parameter) is written into the analysis output WHEN GAMS replies; it is NON-BLOCKING for phase completion"
  artifacts:
    - path: "notes/structural-econometrcics/data/gams-xcheck-handoff.md"
      provides: "CSV path + verbatim NLS objective + Haskell point estimates for the GAMS cross-check"
      contains: "beta0"
  key_links:
    - from: "notes/structural-econometrcics/data/gams-xcheck-handoff.md"
      to: "GAMS-development session (claude-peers)"
      via: "mcp__claude-peers__send_message"
      pattern: "send_message|handoff"
---

<objective>
Coordinate the GAMS point-estimate cross-check (CTX-XCHECK) WITHOUT writing any `.gms` (GAMS is owned by the GAMS-development session, PID 175812, per CLAUDE.md). This session exports the final estimation panel + the exact NLS objective + the Haskell point estimates, sends the handoff to the GAMS session via claude-peers, and records the agreement check as a non-blocking addendum to the analysis output when GAMS replies.

Purpose: An independent differential cross-check of the Haskell fit against a GAMS solve of the identical 3-variable NLS.
Output: `notes/structural-econometrcics/data/gams-xcheck-handoff.md` (handoff), the send recorded, and a non-blocking agreement addendum to the analysis output on reply.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md
@notes/structural-econometrcics/data/estimation-panel.csv
@notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md
</context>

<constraints>
- **This session does NOT write `.gms` files.** GAMS work belongs to the GAMS-development session (CLAUDE.md ownership map). This plan is a coordination handoff only.
- The objective is verbatim: minimize over (beta0, upsilon0, kappa) of `sum_i (pi_i - beta0 - upsilon0*exp(-kappa*d_i)*sig2_i)^2`.
- The cross-check is NON-BLOCKING for phase completion. If GAMS has not replied by the time the audit gate (09-11) runs, record "GAMS cross-check pending" and proceed — the agreement addendum lands whenever the reply arrives.
- Discover the GAMS peer via `mcp__claude-peers__list_peers` (scope repo); the durable identity is the GAMS-development ROLE (PID 175812 is not stable across restarts).
- No home paths / secrets in the handoff note.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the GAMS cross-check handoff (CSV path + verbatim objective + Haskell point estimates)</name>
  <files>notes/structural-econometrcics/data/gams-xcheck-handoff.md</files>
  <read_first>
    - notes/structural-econometrcics/data/estimation-panel.csv (the exported panel columns — from 09-09)
    - notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md (the Haskell point estimates to hand over)
    - CLAUDE.md (the GAMS ownership boundary)
  </read_first>
  <action>
    Write `notes/structural-econometrcics/data/gams-xcheck-handoff.md` containing:
      - The path to the estimation panel: `notes/structural-econometrcics/data/estimation-panel.csv`, and its self-describing column header (tokenId, epoch, pi, sigma2, sigma2_instrument, distance).
      - The verbatim NLS objective for the GAMS session to solve:
          minimize over (beta0, upsilon0, kappa):  sum_i ( pi_i - beta0 - upsilon0 * exp( -kappa * d_i ) * sig2_i )^2
        where pi_i = column `pi`, d_i = column `distance`, sig2_i = column `sigma2`.
        Note it is the primary (non-IV) point-fit for comparability; suggest starting values from the Haskell estimates.
      - The Haskell point estimates (beta0-hat, upsilon0-hat, kappa-hat) for the agreement comparison, and the agreement tolerance: relative 1e-3 per parameter.
      - A request that the GAMS session report back (beta0, upsilon0, kappa) from its solve.
    No `.gms` content is authored here — only the handoff spec. No home paths.
  </action>
  <verify>
    <automated>test -f notes/structural-econometrcics/data/gams-xcheck-handoff.md && grep -qiE 'beta0|upsilon0|kappa' notes/structural-econometrcics/data/gams-xcheck-handoff.md && grep -qi 'estimation-panel.csv' notes/structural-econometrcics/data/gams-xcheck-handoff.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f notes/structural-econometrcics/data/gams-xcheck-handoff.md`
    - it contains the verbatim objective: `grep -qi 'exp( *-kappa' notes/structural-econometrcics/data/gams-xcheck-handoff.md` (or the un-spaced form `exp(-kappa`)
    - it references `estimation-panel.csv` and the Haskell point estimates and the 1e-3 tolerance
    - no `.gms` file was created by this session: `! git status --porcelain | grep -E '\.gms$'`
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' notes/structural-econometrcics/data/gams-xcheck-handoff.md`
  </acceptance_criteria>
  <done>The handoff note fully specifies the CSV, the verbatim NLS objective, the Haskell point estimates, and the agreement tolerance — no .gms authored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Send the handoff to the GAMS session and record the agreement check (non-blocking)</name>
  <action>This is a coordination checkpoint: the executor discovers the GAMS-development peer via mcp__claude-peers__list_peers (scope repo) and sends the handoff (CSV path, verbatim NLS objective, Haskell point estimates, 1e-3 tolerance) via mcp__claude-peers__send_message, then pauses. The numeric-agreement addendum is recorded non-blockingly when GAMS replies; phase completion does NOT wait on the reply.</action>
  <what-built>
    Task 1 produced `notes/structural-econometrcics/data/gams-xcheck-handoff.md` with the exported panel path, the verbatim 3-variable NLS objective, the Haskell point estimates, and the 1e-3 agreement tolerance.
  </what-built>
  <how-to-verify>
    1. Discover the GAMS peer: `mcp__claude-peers__list_peers` (scope repo) — find the GAMS-development session (role = GAMS development; PID nominally 175812).
    2. Send the handoff via `mcp__claude-peers__send_message` to that peer: include the CSV path, the verbatim objective, the Haskell point estimates, and the request to report (beta0, upsilon0, kappa). Record the send (peer id + timestamp) in the 09-10 SUMMARY.
    3. NON-BLOCKING: if the GAMS session replies with its solve, append a short "GAMS cross-check" addendum to the analysis output (`notes/structural-econometrcics/analysis/<date>-upsilon-estimates.md`) reporting the GAMS (beta0, upsilon0, kappa), the relative differences vs Haskell, and PASS/FAIL at relative 1e-3 per parameter. If no reply yet, record "GAMS cross-check pending (non-blocking)" and proceed — do NOT block phase completion on the reply.
  </how-to-verify>
  <resume-signal>Reply "handoff sent" (and optionally "GAMS results: beta0=.., upsilon0=.., kappa=.." to record the agreement), or "skip xcheck" to defer.</resume-signal>
</task>

</tasks>

<verification>
- `gams-xcheck-handoff.md` exists with CSV path + verbatim objective + Haskell estimates + tolerance.
- Handoff sent to the GAMS peer via claude-peers; send recorded.
- No `.gms` authored by this session; no home paths/secrets.
- Agreement addendum recorded on reply, or marked pending (non-blocking).
</verification>

<success_criteria>
The GAMS point-estimate cross-check is set up as a clean coordination handoff (no `.gms` authored here): the exported panel, the verbatim NLS objective, and the Haskell estimates are handed to the GAMS session, with the numeric-agreement addendum recorded non-blockingly when GAMS replies.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-10-SUMMARY.md` (record the peer id + send timestamp; note the addendum status).
</output>
