---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 06
type: execute
wave: 2
depends_on: [3]
files_modified:
  - lean/vol_markets/Upsilon.lean
  - .gitignore
autonomous: true
requirements: [CTX-BRIDGE]
must_haves:
  truths:
    - "exp_family_witnesses_ATMOTM is proved by a single serial Aristotle task (new project) and integrated"
    - "lake build vol_markets is sorry-free across the lib after integration"
    - "#print axioms on the bridging lemma shows only [propext, Classical.choice, Quot.sound]"
  artifacts:
    - path: "lean/vol_markets/Upsilon.lean"
      provides: "the PROVED bridging lemma (sorry removed)"
      contains: "theorem exp_family_witnesses_ATMOTM"
  key_links:
    - from: "Aristotle returned archive"
      to: "lean/vol_markets/Upsilon.lean"
      via: "RequestProject.→vol_markets. import rewrite + copy back"
      pattern: "exp_family_witnesses_ATMOTM"
---

<objective>
Discharge the bridging lemma via the phase's SINGLE serial Aristotle task (Aristotle-heavy rule): submit `exp_family_witnesses_ATMOTM` (stated + sorry'd in 09-03) to a NEW Aristotle project, poll to completion with one in-flight task, integrate the returned proof, and verify the lib is sorry-free and axiom-clean. This makes a fitted κ̂ > 0 a formal witness of the Lean conjecture.

Purpose: The headline Lean-awareness artifact — data (κ̂ > 0) witnessing a machine-checked Lean theorem.
Output: `lean/vol_markets/Upsilon.lean` with the proof integrated (sorry removed), `lake build vol_markets` green and axiom-clean.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-05-SUMMARY.md
@lean/vol_markets/Upsilon.lean
</context>

<constraints>
- **STRICTLY SERIAL — exactly ONE in-flight Aristotle task at all times** (memory: aristotle-no-queue). NEVER queue/parallel `aristotle continue`. Poll `aristotle tasks` every ~5 min; only submit/act when nothing is in flight. Queuing overwrites the prior upload.
- **NEW project** (not the 08-05 project). Reuse the 08-05 bundle RECIPE (scratch layout, toolchain manifest, `RequestProject.→vol_markets.` import rewrite, `aristotle tasks` watcher).
- **API key from the worktree `.env`** via `--api-key` — NEVER print, echo, or commit the key. The bundle dir lives under `scratch/` (already gitignored) — confirm it stays gitignored.
- Depends on 09-03: submit only AFTER the corrected conjunct-3 statement builds locally green with the one sorry.
- This is the ONLY Aristotle task in the entire phase.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Bundle and submit the bridging lemma to a new Aristotle project (single in-flight)</name>
  <files>.gitignore, scratch/ (gitignored bundle)</files>
  <read_first>
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-05-SUMMARY.md (the working bundle layout, CLI invocations, the two false-start lessons, the `aristotle tasks` 5-min watcher)
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Bridging Lemma — "Recommended bridging lemma", the β = exp(−c) normal-form hint, and the Option A fallback)
    - lean/vol_markets/Upsilon.lean (the sorry'd exp_family_witnesses_ATMOTM + corrected ATMOTMNullHypothesis from 09-03)
  </read_first>
  <action>
    Confirm nothing is in flight: `aristotle tasks` (via `--api-key "$ARISTOTLE_API_KEY"` sourced from `.env`; never echo the value). If any task is in flight, WAIT.
    First verify the local gate: `cd lean && lake build vol_markets` is green with the single sorry on `exp_family_witnesses_ATMOTM` (from 09-03).
    Build the bundle under `scratch/aristotle-panoptic-upsilon-bridge/` per the 08-05 recipe: toolchain manifest matching the repo's Lean/Mathlib, the RequestProject sources containing `Upsilon.lean` (and its imports) with imports rewritten, NO home-absolute paths. Include a short bundle note steering the prover: the algebraic normal form `β := exp(−c) = exp(−κ·Δi) ∈ (0,1)`, the slope magnitudes `|slope(iK+n)| = υ₀·βⁿ·(1−β)/Δi` and `|slope(iK−1−n)| = υ₀·βⁿ·(1−β)/Δi`, the peak at the pair {iK−1, iK}, and the target bound `βⁿ ≤ exp(−c·g(i))` with `g(i)=max(i−iK, −(i−iK)−1)`; cite the useful Mathlib lemmas (`Real.exp_le_exp`, `Real.exp_nonneg`, `abs_le`, monotonicity of `exp`).
    Confirm `scratch/` is gitignored (it is per 08-05; verify `grep -q 'scratch/' .gitignore`, add if missing).
    Submit to a NEW project: `aristotle <submit-cmd> --api-key "$ARISTOTLE_API_KEY" ...` per the 08-05 invocation. Record the new project id + task id in the SUMMARY (not the key). Do NOT submit anything else while this is in flight.
  </action>
  <verify>
    <automated>grep -q 'scratch/' .gitignore && echo "scratch gitignored"; git check-ignore scratch/aristotle-panoptic-upsilon-bridge 2>/dev/null && echo "bundle ignored" || echo "bundle dir not yet created (ok pre-submit)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'scratch/' .gitignore` (bundle dir is gitignored)
    - The SUMMARY records a NEW project id + task id distinct from 08-05's `c30c6ae3`
    - `! git ls-files scratch/ | grep .` (no bundle files tracked)
    - No key value appears in any tracked file: `! grep -rIE 'arstl_|ARISTOTLE_API_KEY=[^ ]' lean/ .planning/phases/09-* .gitignore`
  </acceptance_criteria>
  <done>Bundle built (gitignored), single-in-flight invariant held, one task submitted to a new project; ids recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Poll, integrate the returned proof, and verify sorry-free + axiom-clean</name>
  <files>lean/vol_markets/Upsilon.lean</files>
  <read_first>
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-05-SUMMARY.md (download → copy-back → `RequestProject.→vol_markets.` rewrite → rebuild; the `#print axioms` scratch check)
    - lean/vol_markets/Upsilon.lean
  </read_first>
  <action>
    Poll `aristotle tasks` every ~5 min until the task is COMPLETE (use the `aristotle tasks` watcher, not `aristotle show` — 08-05 noted `show` takes the project id positionally and the streaming UI times out).
    On COMPLETE: `aristotle download --destination <scratch dir>`; copy the proved `Upsilon.lean` back into `lean/vol_markets/` applying the `RequestProject.→vol_markets.` import rewrite. The statement of `exp_family_witnesses_ATMOTM` and `ATMOTMNullHypothesis` MUST be unchanged — only the `sorry` is replaced by a proof.
    If Aristotle returns UNPROVABLE or cannot close Option B: switch conjunct 3 to the Option A fallback recorded in 09-03 (`Real.exp (-c * (max 0 (|(i:ℝ)-(iK:ℝ)| - 1)))`), rebuild locally, and resubmit (still single-in-flight, same project or a new one — never queue).
    Verify: `cd lean && lake build vol_markets` exits 0 with ZERO sorries across the lib; run a `#print axioms exp_family_witnesses_ATMOTM` scratch check and confirm only `[propext, Classical.choice, Quot.sound]`.
  </action>
  <verify>
    <automated>cd lean && lake build vol_markets 2>&1 | tail -6; echo "EXIT=$?"; echo "sorries in Upsilon.lean:"; grep -c 'sorry' lean/vol_markets/Upsilon.lean</automated>
  </verify>
  <acceptance_criteria>
    - `cd lean && lake build vol_markets` exits 0
    - `grep -c 'sorry' lean/vol_markets/Upsilon.lean` == 0 (the bridging lemma is proved)
    - `grep -q 'theorem exp_family_witnesses_ATMOTM' lean/vol_markets/Upsilon.lean` (statement preserved)
    - `#print axioms exp_family_witnesses_ATMOTM` prints only `[propext, Classical.choice, Quot.sound]` (record the output in the SUMMARY)
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' lean/vol_markets/Upsilon.lean`
  </acceptance_criteria>
  <done>The bridging lemma is proved and integrated; lib is sorry-free and axiom-clean; κ̂ > 0 is now a formal witness of ATMOTMNullHypothesis.</done>
</task>

</tasks>

<verification>
- `cd lean && lake build vol_markets` exits 0, zero sorries.
- `#print axioms` on the bridging lemma: only the standard permitted set.
- Single-in-flight Aristotle invariant held; no key leaked; bundle gitignored.
</verification>

<success_criteria>
`exp_family_witnesses_ATMOTM` is machine-checked (single serial Aristotle task, new project), integrated sorry-free and axiom-clean — the exponential-moneyness family with κ > 0 formally witnesses the Lean ATM/OTM conjecture.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-06-SUMMARY.md` (record the new project/task ids and the `#print axioms` output).
</output>
