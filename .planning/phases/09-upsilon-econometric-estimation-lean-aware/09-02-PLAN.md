---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - notes/structural-econometrcics/data/DATA-SOURCES.md
autonomous: false
requirements: [CTX-PANEL, CTX-VAR]
user_setup:
  - service: the-graph
    why: "Mainnet/L2 Panoptic subgraph may require a Graph gateway API key (only the Sepolia endpoint is documented)"
    env_vars:
      - name: GRAPH_API_KEY
        source: "The Graph Studio → API Keys (only if the discovered mainnet/L2 endpoint requires gateway auth)"
must_haves:
  truths:
    - "A concrete, reachable mainnet or production-L2 Panoptic subgraph endpoint for a deep ETH/USDC market is confirmed and recorded (not the Sepolia testnet endpoint)"
    - "BigQuery is reachable from the exec session and the Uniswap v3 Swap topic0 for the target pool is computed and verified against real rows"
    - "The chosen pool/chain/address, subgraph endpoint, auth requirement, and BigQuery dataset are all recorded in a self-describing data-sources note"
  artifacts:
    - path: "notes/structural-econometrcics/data/DATA-SOURCES.md"
      provides: "Confirmed endpoints, chain, pool address, Swap topic0, dataset names, auth handling"
      contains: "underlyingPool"
  key_links:
    - from: "notes/structural-econometrcics/data/DATA-SOURCES.md"
      to: "Panoptic subgraph + BigQuery crypto_ethereum"
      via: "confirmed endpoint URL + dataset id"
      pattern: "endpoint|dataset"
---

<objective>
Resolve the two live data-access unknowns that gate the whole pipeline: (1) a MAINNET or production-L2 Panoptic subgraph endpoint for a deep ETH/USDC market (the docs publish only a Sepolia testnet endpoint), and (2) BigQuery reachability + the exact Uniswap v3 Swap topic0 for the target pool. This is a Wave-0 discovery GATE — because a Graph gateway API key may be required and executors cannot ask the user mid-task, this plan ends in a checkpoint that surfaces the discovered endpoint(s) and requests a key / endpoint confirmation if needed.

Purpose: Prevent building the "deepest ETH/USDC, full history" panel on testnet-only data, and confirm the σ̂² source before variance work.
Output: `notes/structural-econometrcics/data/DATA-SOURCES.md` recording the confirmed endpoint, chain, pool/address, auth handling, BigQuery dataset, and computed Swap topic0.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-CONTEXT.md
</context>

<constraints>
- **Docs-first**: fetch `https://panoptic.xyz/docs/subgraph/schema` and `.../subgraph/queries` FIRST. Do not guess subgraph IDs.
- If a gateway key is needed, it goes into the worktree `.env` as `GRAPH_API_KEY` (same as `ARISTOTLE_API_KEY`) — NEVER print or commit the key. The DATA-SOURCES.md note records only that auth IS required and where the key lives, not the key value.
- BigQuery access is via the `mcp__bigquery__` MCP tools (use ToolSearch to load them). No Dune (no Dune MCP in this session).
- No `/home/`, `$HOME`, `~/`, or secret values in the tracked note.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Discover and probe a mainnet/L2 Panoptic subgraph endpoint for a deep ETH/USDC market</name>
  <files>notes/structural-econometrcics/data/DATA-SOURCES.md</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Common Pitfalls Pitfall 1; §Open Questions 1; §Code Examples — the verified GraphQL query shape and entity schema)
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-CONTEXT.md (Data access route; Market & sample)
  </read_first>
  <action>
    Fetch `https://panoptic.xyz/docs/subgraph/schema` and `https://panoptic.xyz/docs/subgraph/queries`. Enumerate every endpoint the docs declare and their chain coverage. The documented worked endpoint is Sepolia (Goldsky `panoptic-subgraph-sepolia`) — that is NOT acceptable for the panel.
    Probe for a mainnet/production-L2 sibling: try the Goldsky naming pattern (`panoptic-subgraph-mainnet` / `-optimism` / `-base` / `-arbitrum`) and The Graph Explorer/Studio for a published Panoptic subgraph. For each candidate, POST the verified `GetOpenPanopticPositionsByAccountAndUniswapPool` query shape (from RESEARCH §Code Examples) and confirm it returns real entities with an `underlyingPool` that matches a known mainnet ETH/USDC Uniswap v3 pool (e.g. the 0.05% or 0.3% ETH/USDC pool) and block numbers in the mainnet range.
    Identify the DEEPEST ETH/USDC Panoptic market the reachable subgraph covers (highest activity / most positions on the largest ETH/USDC Uniswap pool). Record its panopticPool + underlyingPool address, chain, fee tier, and observed history depth (earliest/latest block or timestamp).
    Write `notes/structural-econometrcics/data/DATA-SOURCES.md` documenting: the chosen subgraph endpoint URL, chain, whether gateway auth is required (and that the key, if any, lives ONLY in the worktree `.env` as `GRAPH_API_KEY`), the chosen pool/panopticPool addresses, fee tier, and history depth. If NO mainnet/L2 endpoint is reachable without a key, record all candidate endpoints and flag "needs GRAPH_API_KEY" for the checkpoint.
    Do NOT write any key value into the note.
  </action>
  <verify>
    <automated>test -f notes/structural-econometrcics/data/DATA-SOURCES.md && grep -qiE 'underlyingPool|panopticPool' notes/structural-econometrcics/data/DATA-SOURCES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f notes/structural-econometrcics/data/DATA-SOURCES.md`
    - `grep -qiE 'endpoint' notes/structural-econometrcics/data/DATA-SOURCES.md`
    - `grep -qiE '0x[0-9a-fA-F]{40}' notes/structural-econometrcics/data/DATA-SOURCES.md` (a concrete pool address recorded)
    - `! grep -qiE 'sepolia' notes/structural-econometrcics/data/DATA-SOURCES.md || grep -qiE 'mainnet|optimism|base|arbitrum' notes/structural-econometrcics/data/DATA-SOURCES.md` (a non-Sepolia target is present)
    - `! grep -rIE 'GRAPH_API_KEY=|/home/[a-z0-9_-]+/' notes/structural-econometrcics/data/DATA-SOURCES.md` (no key value, no home paths)
  </acceptance_criteria>
  <done>A concrete non-Sepolia Panoptic subgraph endpoint + deep ETH/USDC market (pool address, chain, history depth) is recorded, or all candidates + the key requirement are recorded for the checkpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm BigQuery reachability and compute the Uniswap v3 Swap topic0 for the target pool</name>
  <files>notes/structural-econometrcics/data/DATA-SOURCES.md</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (§Code Examples — BigQuery Swap query; §Open Questions 2; the `bigquery-public-data.crypto_ethereum` note + L2 caveat)
    - notes/structural-econometrcics/data/DATA-SOURCES.md (the pool address + chain from Task 1)
  </read_first>
  <action>
    Load the `mcp__bigquery__` tools (ToolSearch). Run `list-tables` / `describe-table` on `bigquery-public-data.crypto_ethereum.logs` to confirm connectivity and the `topics` (array) + `data` + `address` + `block_timestamp` columns.
    Compute the Uniswap v3 Swap event topic0 = keccak256 of `Swap(address,address,int256,int256,uint160,uint128,int24)`. (Do this deterministically — e.g. a tiny Haskell/keccak call, or confirm against a known reference value; record the hex.)
    Run a cheap dry-run: `SELECT ... FROM bigquery-public-data.crypto_ethereum.logs WHERE address = LOWER('<POOL_ADDR from Task 1>') AND topics[OFFSET(0)] = '<SWAP_TOPIC0>' LIMIT 100` and confirm it returns rows (non-empty) for the mainnet ETH/USDC pool.
    If the chosen market is on an L2 (not mainnet), locate the corresponding public dataset (e.g. an Optimism/Base/Arbitrum crypto export) and confirm the same Swap logs are queryable there; record the dataset name. If no L2 public dataset exists, flag it for the checkpoint (may force a mainnet market choice).
    Append to `notes/structural-econometrcics/data/DATA-SOURCES.md`: BigQuery dataset name(s), the confirmed Swap topic0 hex, the `logs` columns used, and confirmation the dry-run returned rows for the target pool.
  </action>
  <verify>
    <automated>grep -qiE '0x[0-9a-fA-F]{64}' notes/structural-econometrcics/data/DATA-SOURCES.md && grep -qiE 'crypto_ethereum|bigquery' notes/structural-econometrcics/data/DATA-SOURCES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qiE 'crypto_ethereum|bigquery-public-data' notes/structural-econometrcics/data/DATA-SOURCES.md`
    - `grep -qiE '0x[0-9a-fA-F]{64}' notes/structural-econometrcics/data/DATA-SOURCES.md` (Swap topic0 recorded)
    - `grep -qiE 'topic0|Swap' notes/structural-econometrcics/data/DATA-SOURCES.md`
    - the note states the LIMIT-100 dry-run returned rows for the target pool
  </acceptance_criteria>
  <done>BigQuery connectivity confirmed, Swap topic0 computed and validated against real rows for the target pool, dataset(s) recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the endpoint choice and provide GRAPH_API_KEY if the gateway requires auth</name>
  <action>This is a user checkpoint: the executor pauses here. The discovered endpoints/BigQuery findings from Tasks 1-2 are surfaced to the user, who confirms the mainnet/L2 endpoint choice and, if the gateway requires auth, adds GRAPH_API_KEY to the worktree .env (gitignored). Do not proceed to Wave-2 panel/variance work until the endpoint is confirmed.</action>
  <what-built>
    Task 1 discovered and probed candidate mainnet/L2 Panoptic subgraph endpoints and identified the deepest ETH/USDC market; Task 2 confirmed BigQuery + the Swap topic0. All findings are recorded in `notes/structural-econometrcics/data/DATA-SOURCES.md`.
  </what-built>
  <how-to-verify>
    1. Open `notes/structural-econometrcics/data/DATA-SOURCES.md` and review the chosen subgraph endpoint, chain, pool address, and history depth.
    2. If the note flags "needs GRAPH_API_KEY": create/obtain a key at The Graph Studio and add it to the worktree `.env` as `GRAPH_API_KEY=...` (this file is gitignored; do NOT commit it). Confirm the executor can then re-run the Task-1 probe query and get entities.
    3. If multiple candidate endpoints exist, state which one to use for the panel (the deepest ETH/USDC market).
    4. If no mainnet/L2 endpoint is reachable at all, decide the fallback (e.g. accept an L2 market with a confirmed BigQuery dataset, or authorize a specific gateway subscription).
  </how-to-verify>
  <resume-signal>Reply "endpoint confirmed: &lt;url/chain&gt;" (and "GRAPH_API_KEY added" if a key was required), or describe the endpoint to use.</resume-signal>
</task>

</tasks>

<verification>
- `notes/structural-econometrcics/data/DATA-SOURCES.md` exists and records: non-Sepolia subgraph endpoint, pool address, chain, BigQuery dataset, Swap topic0.
- No key value or home-absolute path in the tracked note.
- User has confirmed the endpoint (and provided GRAPH_API_KEY to `.env` if required).
</verification>

<success_criteria>
A confirmed, reachable data path exists for BOTH sides of the panel: a non-Sepolia Panoptic subgraph for positions/premia/strikes and a BigQuery dataset + Swap topic0 for the underlying pool's swap ticks — all recorded self-describingly, with any gateway key held only in `.env`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-02-SUMMARY.md`.
</output>
