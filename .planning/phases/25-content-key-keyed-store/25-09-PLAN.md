---
phase: 25-content-key-keyed-store
plan: 09
type: execute
wave: 9
depends_on: ["25-08"]
files_modified:
  - offchain/test/Main.hs
  - .planning/phases/25-content-key-keyed-store/25-VALIDATION.md
autonomous: true
requirements: [KEY-02, STORE-02]

must_haves:
  truths:
    - "The echoed-field cross-check — carried forward by phase 24 with a standing assertion and no mutation — is OBSERVED REJECTING, and the rejection is EchoMismatch and not a freshness failure"
    - "The freshness conjunct is shown INCAPABLE of catching this mutant, because the stub writes a fresh file"
    - "Every one of the 50 research guards is reconciled: OBSERVED with its firing input, or reported BY NAME as a standing finding with the reason"
    - "The three other carried guards from phase 24 are re-reported by name rather than silently dropped"
    - "The phase-gate numbers are MEASURED cold at the end, not inherited from any earlier plan"
    - "STORE-07 is recorded as closing PARTIAL — event_tx and block_no are structurally present and always Nothing, owed to Phase 27 — rather than being reported closed on a run log that cannot carry two of its four named fields"
  artifacts:
    - path: "offchain/test/Main.hs"
      provides: "the echoed-field mutant check, registered in core_checks"
    - path: ".planning/phases/25-content-key-keyed-store/25-VALIDATION.md"
      provides: "the fifty-row guard ledger, the requirement traceability, and the Per-Task Verification Map RECONCILED against what the nine summaries record"
  key_links:
    - from: "offchain/test/Main.hs"
      to: "Gams.Run.EchoMismatch"
      via: "a stub echoing a token the argv did not send"
      pattern: "EchoMismatch"
---

<objective>
Close the one guard phase 24 handed forward that this phase can close, reconcile all fifty, and take
the phase-gate measurements cold.

Purpose: 24-06's closing ledger names FOUR guards with a **standing assertion and no mutation**. One
of them — the echoed-field cross-check — has its firing input here: it is KEY-02's own mutant. A
stub that echoes a token the argv did not send must produce `Aborted (EchoMismatch …)`, and the
**freshness conjunct cannot catch it** because the stub writes a fresh file, which is precisely why
24-03's exercise of this guard PASSED and proved nothing about the guard.

A guard never OBSERVED rejecting is treated as ABSENT. This plan is where that rule is settled for
the whole phase, and where any guard that could not be fired is reported BY NAME rather than
omitted — 23-05's guard #13 precedent, honoured at 24-06.

Output: one check, the fifty-row ledger, the three re-reported carry-forwards, and the phase-gate
numbers.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-content-key-keyed-store/25-RESEARCH.md
@.planning/phases/25-content-key-keyed-store/25-VALIDATION.md
@.planning/phases/24-gams-invocation-toolchain-identity/24-06-SUMMARY.md
@.planning/phases/25-content-key-keyed-store/25-08-SUMMARY.md
@offchain/lib/Gams/Run.hs
@offchain/test/Main.hs

<interfaces>
<!-- Extracted from the tree at plan time. Use these directly; do not go exploring. -->

`offchain/lib/Gams/Run.hs:372-396` — the conjunct whose guard is being closed, verbatim:

```haskell
echo_conjunct :: [String] -> ProverArtifact -> Either (String, String, String) ()
echo_conjunct argv artifact = do
  _ <- one "sqrtPriceX96" "--sqrtPriceX96=" (pa_sqrt_price_x96_text artifact)
  one "liquidityRaw" "--liquidityRaw=" (pa_liquidity_text artifact)
-- AbortReason: EchoMismatch String String String  -- the field, the token SENT, the value echoed
```

`offchain/test/Main.hs:9105-9126` — the stub builder that takes the two echoed values as SHELL
EXPRESSIONS, which is the whole mechanism this plan needs:

```haskell
stub_writes_artifact :: String -> String -> String
stub_writes_artifact echo_sqrt echo_liquidity = …  -- "$S" gives the honest echo;
                                                   -- a literal gives the mutant
```

`tier_b_shock` sends `--sqrtPriceX96=79228162514264337593543950336`, so a stub built with
`stub_writes_artifact "0$S" "$L"` echoes `079228162514264337593543950336` — numerically identical,
textually different, written to a FRESH file at the current time. MEASURED at 24-02: that leading
zero is exactly the token whose presence changes the real artifact's digest from `e7b14f38…07d0d884`
to `d64a7b32…14b9e650`.

The four guards phase 24 handed forward (24-06's ledger), and what this phase owes each:

| Guard | Handed over as | Owed here |
|---|---|---|
| #21 echoed-field cross-check | STANDING, never observed REJECTING; 24-03 exercised it and it PASSED | **CLOSE IT** — the firing input is this plan's mutant |
| #11 `conopt_parse_is_position_independent` | never falsified; its firing input is CODE | RE-REPORT as a standing finding; no subject in this phase |
| #23 the 2 MB stderr drain | firing input is a deadlock; no mutation removed the drain | RE-REPORT |
| #28/#30 empty hostile-variable set; fewer-than-16-of-16 | asserted every run, never mutated | RE-REPORT |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The echoed-field mutant — carried guard #21, OBSERVED rejecting</name>
  <files>offchain/test/Main.hs</files>

  <read_first>
    - `offchain/lib/Gams/Run.hs:331-396` — `conjuncts` in order, so the observation can distinguish
      WHICH conjunct rejected: the freshness conjunct precedes the echo conjunct and both would
      report an abort.
    - `offchain/test/Main.hs:9100-9230` — `stub_writes_artifact`, `stub_clean` and
      `stub_exit_codes_drive_the_verdict`, whose shape this check copies.
    - `.planning/phases/25-content-key-keyed-store/25-RESEARCH.md` `### The four guards Phase 24
      handed forward`.
  </read_first>

  <behavior>
    `an_echoed_field_that_disagrees_with_the_argv_aborts_the_run`:
    - a stub built with `stub_writes_artifact "0$S" "$L"` -> `Aborted (EchoMismatch "sqrtPriceX96"
      "79228162514264337593543950336" "079228162514264337593543950336") 0 _`
      — the constructor AND all three payload strings asserted, so a freshness abort or a decode
      abort cannot satisfy it
    - a stub built with `stub_writes_artifact "$S" "1$L"` -> `EchoMismatch "liquidityRaw" …`
      — the SECOND field, because a conjunct that only ever checked the first would pass the first
      arm
    - the POSITIVE CONTROL, `stub_clean`, -> `Produced`, so "it aborted" is not satisfied by a stub
      that aborts for any reason at all
    - the artifact the mutant stub wrote is asserted to be FRESH — its modification time is at or
      after the run marker's — which is the evidence that the FRESHNESS conjunct could not have been
      what caught it
  </behavior>

  <action>
    Add `an_echoed_field_that_disagrees_with_the_argv_aborts_the_run` to `offchain/test/Main.hs` in
    the `with_tier_b_scratch` idiom, and register it in `core_checks`.

    The freshness clause matters and must be asserted rather than argued: 24-03 exercised this guard
    and it PASSED, and the *freshness* conjunct is what did the catching in the case that was
    exercised. Here the stub writes a fresh file, so freshness cannot fire — and the check asserts
    that by reading the artifact's modification time out of the run directory before it is torn
    down, or by asserting the abort reason is `EchoMismatch` and NOT `StaleArtifact`. The second
    form is simpler and stronger: `StaleArtifact` and `EchoMismatch` are different constructors, so
    naming the expected one excludes the other by construction. Do BOTH if the run directory is
    still reachable at assertion time; do the constructor form at minimum.

    Record in the summary that this CLOSES carry-forward guard #21, with the verbatim assertion and
    the verbatim `EchoMismatch` payload the run produced.
  </action>

  <verify>
    <automated>start=$(date +%s); cabal test &gt; /tmp/25-09-t1.log 2&gt;&amp;1; ec=$?; echo "WALL=$(( $(date +%s) - start ))s"; tail -5 /tmp/25-09-t1.log; grep -c '^FAIL ' /tmp/25-09-t1.log; exit $ec</automated>
  </verify>

  <acceptance_criteria>
    - `cabal test` FAIL count `0`; total ≥ 207 (151 baseline + 8 + 10 + 3 + 8 + 7 + 7 + 6 + 6 + 1).
    - `grep -c 'an_echoed_field_that_disagrees_with_the_argv_aborts_the_run' offchain/test/Main.hs`
      is `>= 2` (definition + `core_checks`).
    - `grep -c 'EchoMismatch' offchain/test/Main.hs` is `>= 1`.
    - The check asserts BOTH echoed fields and a `Produced` control — verified by reading the check
      body, and the three-part `EchoMismatch` payload for the `sqrtPriceX96` arm is quoted in the
      summary.
    - Both structural greps still `0`.
  </acceptance_criteria>

  <done>Carry-forward guard #21 has been OBSERVED rejecting, on both echoed fields, with `EchoMismatch` named rather than "an abort happened".</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile all fifty guards and the verification map, and re-report the three guards that have no subject here</name>
  <files>.planning/phases/25-content-key-keyed-store/25-VALIDATION.md</files>

  <read_first>
    - `.planning/phases/25-content-key-keyed-store/25-RESEARCH.md` `### Every guard, and the input
      that makes it fire` — all fifty rows.
    - `.planning/phases/25-content-key-keyed-store/25-0{1..8}-SUMMARY.md` — every mutation
      observation recorded across the phase.
    - `.planning/phases/24-gams-invocation-toolchain-identity/24-06-SUMMARY.md` — the shape of a
      phase-closing ledger and of a named standing finding.
  </read_first>

  <action>
    **RECONCILE `25-VALIDATION.md`'s Per-Task Verification Map — do NOT author it.** The map was
    POPULATED during the phase-25 revision pass, from the twenty-eight tasks across the nine plans;
    it carries no not-yet-filled marker and there is no template placeholder to replace. (This task
    greps that file for the marker's literal spelling and requires zero, so neither that document
    nor this paragraph spells it — prose is inside a grep's blast radius.) This task's job is to check
    it against what the nine SUMMARY files actually record, and to correct any row where the plan
    and the execution disagree: a task that gained or lost a guard, a `<verify>` command that
    changed during execution, a tier that turned out to be different from the one planned. Every
    correction is a correction OF RECORD — say what the row said, what the execution showed, and
    which is now written. A map that agrees with the plans and disagrees with the tree is worse than
    no map.

    **No task may cite the without-tests build form.** The grep that checks this is itself an
    instance of the prose-inside-a-grep hazard, and the plan-time filter did NOT survive it.
    MEASURED at revision time, over the nine PLAN files: filtered **0**, unfiltered **10** — the
    eight prohibition sentences in 25-01 through 25-08, plus the two lines of THIS task that quote
    the grep, both of which embed the literal `The bare` inside their own pattern and are therefore
    invisible to their own filter. The unfiltered count also GROWS once nine SUMMARY files exist,
    because each summary that records the sign-off restates the prohibition.

    **The count-shaped criterion is therefore DROPPED and the scan is SCOPED instead.** A total that
    changes whenever anyone writes the sentence is not a measurement of anything. Scope the scan to
    the only places a real citation could hide — `<automated>` commands and `<action>` bodies — and
    require ZERO. Two details are load-bearing and both were MEASURED at revision time:

    - **The awk range must be written so the `<automated>` arm does not open a range.**
      `awk '/A/ || /B/,/C/'` parses as the single range `(A||B) , C`, so one `<automated>` line puts
      awk in-range until the next `</action>` and swallows the verification prose — MEASURED: it
      reported **10**, i.e. it scoped nothing. The flag form below reported **2**.
    - **The pattern is BUILT FROM FRAGMENTS**, so this command's own text does not contain the
      literal it searches for. Without that, the scanner is its own only match and can never read
      zero — the `aeson_bait_source` idiom (`offchain/test/Main.hs:7842`), applied to a grep instead
      of to a bait file.

    ```
    P="cabal build"; P="$P -j all"
    awk '/<\/action>/{a=0} a || /<automated>/ {print} /<action>/{a=1}' \
      .planning/phases/25-content-key-keyed-store/25-0*-PLAN.md | grep -c "$P"     # must be 0
    ```

    **Run the POSITIVE CONTROL first, or the zero is about nothing:** write a scratch file outside
    the tree containing an `<action>` body that DOES cite the form, run the identical scan over it,
    and require **1**. MEASURED at revision time: control 1, phase 0.

    Report the unfiltered whole-file count as an OBSERVATION with its lines listed, never as a pass
    condition, and confirm by READING each listed line that it is prose prohibiting the form rather
    than a citation of it.

    Then write the FIFTY-ROW LEDGER into the same file: for each research guard, its number, its
    name, the plan that drove it, and either the VERBATIM `FAIL` line it produced or the reason it
    could not be fired. A guard with no observation is a **named standing finding**, never an
    omission.

    **The ledger needs THREE dispositions, not two, and rows 48/49/50 are why.** Guard **48** (the
    sentinel harness reporting an unread leaf as ABSORBED), guard **49** (`sc3_literal_purge` over
    any `0x`-prefixed 64-hex digest in a new `.hs`/`.sh`/`.sql`) and guard **50** (the DB-free and
    GAMS-free structural greps) are not fired by a mutation belonging to any one plan: they run in
    **every** plan's acceptance criteria, and 25-01, 25-03 and 25-07 each constrain new work so as
    not to trip them — 25-03 and 25-07 require every ABSORBED leaf to be asserted or entered in
    `absorbed_by_design` with a written reason, and 25-01 requires digests written BARE. Under a
    two-bucket ledger they fit neither slot and an executor will file them as un-fireable standing
    findings, which is false: they ARE exercised, continuously, by every wave. Give them the third
    disposition — **STANDING-AND-EXERCISED** — naming, for each, the plans whose gates run it and
    the constraint the phase adopted to keep it green. A guard held green by design is a different
    fact from a guard that could not be fired, and the ledger must not blur them.

    Re-report the three carried guards phase 24 handed forward that have no subject in this phase —
    #11, #23, #28/#30 — by NAME, each with the reason and the phase that would own it. Guard #21 is
    reported CLOSED with the observation from task 1.

    Cross-check the requirement traceability: all fourteen IDs, each with the plans that address it
    and the checks that assert it. Any ID whose only evidence is a Tier-C artifact leaf must say so
    explicitly.

    **STORE-07 closes PARTIAL, and the table must say so in those words.** The requirement names
    four fields — `(timestamp, key, event tx, block)`. Migration `004` creates `event_tx` and
    `block_no`; plan 25-04 carries `re_event_tx` and `re_block_no` on `RunEvent` and the Postgres
    writer names both columns, so the writer EXISTS and is exercised. But both are ALWAYS `Nothing`
    in this phase, because CHAIN-01 and CHAIN-02 are blocked upstream until Phase 27 and a
    fabricated zero word would pass every hex-shape guard in the tree. The row therefore reads:
    **timestamp and key — CLOSED, asserted; event tx and block — STRUCTURALLY PRESENT, SEMANTICALLY
    EMPTY, owed to Phase 27.** Name Phase 27 as the owner, and record that no check asserts a
    non-`Nothing` value — deliberately, because an assertion over a field nothing populates is the
    standing-assertion-with-no-mutation shape this whole plan exists to reconcile.

    **STORE-05's Tier-C row is CLOSED here, not deviated.** The research map's
    `store_conformance_records_the_pin_column_and_its_default` was the only one of its 47 rows no
    plan delivered at revision time; plan 25-07's `pin_and_reset` block delivers it. Record that in
    the ledger with the block name, so the map reconciles at 47 of 47.
  </action>

  <verify>
    <automated>P="cabal build"; P="$P -j all"; printf '  &lt;action&gt;\n    x %s y\n  &lt;/action&gt;\n' "$P" &gt; /tmp/25-09-bait.md; echo -n 'CONTROL(want 1): '; awk '/&lt;\/action&gt;/{a=0} a || /&lt;automated&gt;/ {print} /&lt;action&gt;/{a=1}' /tmp/25-09-bait.md | grep -c "$P"; echo -n 'PHASE(want 0): '; awk '/&lt;\/action&gt;/{a=0} a || /&lt;automated&gt;/ {print} /&lt;action&gt;/{a=1}' .planning/phases/25-content-key-keyed-store/25-0*-PLAN.md | grep -c "$P"; echo -n 'OBSERVATION(no expected value): '; grep -rc "$P" .planning/phases/25-content-key-keyed-store/25-0*-PLAN.md | awk -F: '{s+=$2} END{print s}'</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'PENDING' .planning/phases/25-content-key-keyed-store/25-VALIDATION.md` is `0`, and
      the Per-Task Verification Map's row count equals the number of tasks actually executed
      (twenty-eight as planned; any deviation stated in words).
    - The ledger has 50 numbered rows; every row carries a verbatim `FAIL` line, a written reason it
      could not be fired, or the **STANDING-AND-EXERCISED** disposition with the plans whose gates
      run it. Rows **48**, **49** and **50** carry that third disposition by name — filing any of
      them as un-fireable is a reconciliation error, not a finding.
    - The SCOPED scan is `0` and its POSITIVE CONTROL is `1`, in that order: the without-tests build
      form appears in no `<automated>` command and in no `<action>` body across the nine PLAN files,
      and the scan was SHOWN finding one that was seeded.
    - The unfiltered whole-file count is recorded as an OBSERVATION with every matching line quoted,
      each confirmed by reading to be a sentence prohibiting the form. **It is not a pass condition
      and no expected value is stated for it** — at revision time it was 10 over the PLAN files
      alone, and it grows with each SUMMARY.
    - All four phase-24 carry-forwards appear by name, one CLOSED and three RE-REPORTED.
    - All fourteen requirement IDs appear in the traceability table; STORE-07's row states PARTIAL
      closure in words, naming Phase 27 as the owner of `event_tx` and `block_no`; and every row of
      the research's 47-row requirement→test map is accounted for, delivered or deviated by name.
  </acceptance_criteria>

  <done>Every guard is either observed or named; no guard is silently absent; the validation document describes what actually happened.</done>
</task>

<task type="auto">
  <name>Task 3: The phase gate, measured cold</name>
  <files>offchain/test/Main.hs</files>

  <read_first>
    - `.planning/phases/25-content-key-keyed-store/25-VALIDATION.md` `## Validation Sign-Off` — the
      seven boxes this task ticks with measurements.
    - `.planning/phases/25-content-key-keyed-store/25-RESEARCH.md` `### Sampling Rate` — the phase
      gate's definition.
  </read_first>

  <action>
    Take every phase-gate number COLD, in one session, and record each with the command that
    produced it. **Treat every number inherited from an earlier plan or from the research as a
    hypothesis** — two phase-24 summaries carried stale floors into a plan brief (55/63 against a
    disk that read 58/67) and the discrepancy was only visible because the executor re-measured.

    1. `cabal build --enable-tests -j all` — exit code and warning count.
    2. `time cabal test` — total, FAIL count, wall. Compare against the research's pre-phase
       baseline of **151/151 at 152.9 s** and against the 900 s ceiling.
    3. Both structural greps — `Store.Postgres|CFMM_REQUIRE_DB|connectPostgreSQL` and
       `Gams.Invoke|CFMM_REQUIRE_GAMS|/usr/gams` over `offchain/test/Main.hs`, each **0**.
    4. `purge_file_floor` and `credential_scan_floor` — two SEPARATE `find … | wc -l` invocations,
       run independently, zero slack against the printed values. The tree started this phase at
       **59** and **68**.
    5. `sentinel_pair_floor` and all six `artifact_field_floors` — raised until the harness NAMES
       each; never arithmetic.
    6. `bash offchain/rig/capture-store-conformance.sh` re-run from a fresh container: exit 0, every
       verdict `pass`, and the committed artifact's sha256 recorded before and after (identical, or
       the difference explained).
    7. `git status --porcelain src test foundry-scripts Makefile foundry.toml .github` — EMPTY.

    If any constant moved, fix it in the same task and re-run, so the recorded numbers describe the
    tree as it is committed and not as it was mid-plan.
  </action>

  <verify>
    <automated>start=$(date +%s); cabal test &gt; /tmp/25-09-t3.log 2&gt;&amp;1; ec=$?; echo "WALL=$(( $(date +%s) - start ))s"; tail -6 /tmp/25-09-t3.log; grep -c '^FAIL ' /tmp/25-09-t3.log; git status --porcelain src test foundry-scripts Makefile foundry.toml .github | wc -l; exit $ec</automated>
  </verify>

  <acceptance_criteria>
    - `cabal build --enable-tests -j all` exits 0 with zero warning lines.
    - `cabal test` FAIL count `0`, total recorded, wall recorded and under 900 s.
    - Both structural greps print `0`.
    - Both `find | wc -l` outputs quoted and equal to the constants in `Main.hs`.
    - The harness's naming output for `sentinel_pair_floor` and all six `artifact_field_floors`
      quoted.
    - `bash offchain/rig/capture-store-conformance.sh` exits 0 with every verdict `pass`.
    - `git status --porcelain src test foundry-scripts Makefile foundry.toml .github | wc -l` is `0`.
  </acceptance_criteria>

  <done>Every phase-gate number is a measurement taken at the committed tree, and the territory is clean.</done>
</task>

</tasks>

<verification>
- `cabal build --enable-tests -j all` — exit 0, ZERO warnings. The bare without-tests build form
  appears nowhere in this phase's plans or summaries except in the sentences prohibiting it.
- `cabal test` — FAIL 0; total ≥ 207, and both the total and the wall recorded, wall under 900 s.
- Both structural greps 0: the suite is DB-free AND GAMS-free.
- `bash offchain/rig/capture-store-conformance.sh` — exit 0, all verdicts `pass`.
- `git status --porcelain src test foundry-scripts Makefile foundry.toml .github` is EMPTY.
</verification>

<success_criteria>
1. Carried guard #21 is CLOSED by an observation, on both echoed fields, with `EchoMismatch` named
   and the freshness conjunct shown incapable of catching it.
2. All fifty research guards are reconciled — observed with a verbatim `FAIL` line, or reported by
   name with a reason.
3. The three other phase-24 carry-forwards are re-reported by name rather than dropped.
4. All fourteen requirement IDs are traced to the plans and checks that address them, with
   STORE-07 recorded as closing PARTIAL and Phase 27 named as the owner of its two chain fields.
5. Every phase-gate number is measured cold at the committed tree, with the command that produced it.
</success_criteria>

<output>
After completion, create `.planning/phases/25-content-key-keyed-store/25-09-SUMMARY.md` recording:
the `EchoMismatch` payloads verbatim; the fifty-row ledger's totals (observed vs named); the four
carry-forward dispositions; every phase-gate measurement with its command; and any guard reported as
a standing finding, by name.
</output>
