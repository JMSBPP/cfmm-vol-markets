---
phase: 10-consolidated-spec
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v2-controller/C2-PROOF-CASE.md
autonomous: true
requirements: [SPEC-03]
must_haves:
  truths:
    - "A reader sees the full C2 (σ_target -> Δi⋆) pipeline named across all four stages: SymPy, Lean, Plank, gamsDiff"
    - "The SymPy stage gives the exact closed-form root and the exact-rational WAD/Q64.96 quantization recipe"
    - "The Lean stage names the proven existence/positivity theorem sigma_xs_poly_target_exists"
    - "The Plank stage states it consumes the SPEC-02 fixed-point sqrt and lists the on-chain primitives"
    - "The gamsDiff stage states fixture equality with an explicit tolerance"
    - "The feasibility preconditions n>=2 and strict σ_target > d² are stated"
  artifacts:
    - path: ".planning/research/v2-controller/C2-PROOF-CASE.md"
      provides: "Concrete SPEC-03 end-to-end proof-case spec for controller C2"
      contains: "σ_target"
  key_links:
    - from: "C2-PROOF-CASE.md"
      to: "ON-CHAIN-REALIZATION.md"
      via: "Plank stage depends on the SPEC-02 fixed-point sqrt primitive"
      pattern: "sqrt"
---

<objective>
Author the concrete end-to-end PROOF-CASE spec (SPEC-03) for controller C2
(σ_xs cross-section variance-target -> Δi⋆). This is the single example that
exercises every layer of the pipeline (theory -> design tool -> proof -> EVM -> diff)
on an already-proven inversion, so the implementation milestone can build it without
further research.

Purpose: pin the exact closed-form, the exact quantization, the exact proof obligation,
the exact on-chain primitives, and the exact differential-test contract for C2.
Output: `.planning/research/v2-controller/C2-PROOF-CASE.md`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2-controller/MILESTONE.md
@.planning/milestones/v2-controller/REQUIREMENTS.md
@.planning/milestones/v2-controller/ROADMAP.md
@.planning/research/v2-controller/CONTROLLERS.md
@.planning/research/v2-controller/TOOLING-CONTROL-DSL.md
@.planning/research/v2-controller/STATIC-CONTROL-KERNEL-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SymPy derivation + exact-rational quantization stage, with feasibility preconditions</name>
  <files>.planning/research/v2-controller/C2-PROOF-CASE.md</files>
  <read_first>
    - .planning/research/v2-controller/C2-PROOF-CASE.md (the file being written; create if absent)
    - .planning/research/v2-controller/CONTROLLERS.md (C2 entry: closed form, discriminant, coefficients, regime/feasibility n>=2 and strict σ_target > d²)
    - .planning/research/v2-controller/TOOLING-CONTROL-DSL.md (§4 pipeline: SymPy solve -> exact Rational -> WAD/Q64.96 quantization; the concrete 5-step workflow)
  </read_first>
  <action>
    Create the doc with an H1 title and an intro line naming the four pipeline stages
    (SymPy -> Lean -> Plank -> gamsDiff). Then write:

    "## Feasibility preconditions" — state both, exactly: `n >= 2` (positive leading
    coefficient) and STRICT `σ_target > d²` (Aristotle-narrowed; non-strict lets the
    root collapse to 0), with `d := i₋ - i_μ`. Note C2 is proven for `sigma_xs_poly`
    with `#` = `n` FREE, valid only on a Δi-interval where the tick count `#` is
    constant (full-σ_xs lift is gap G3).

    "## Stage 1 — SymPy derivation + quantization" — write the exact closed-form root:
      `Δi⋆(n, d, σ_target) = (d·n(n-1) + sqrt(disc)) / (n(n-1)(2n-1)/3)`
      with `disc = c₁² - 4·c₂·(d² - σ_target)`, `c₂ = n(n-1)(2n-1)/6`,
      `c₁ = -d·n(n-1)`.
    State the SymPy recipe (cite TOOLING-CONTROL-DSL §4): `solve(sigma_xs_poly(n,d,Δi)
    - σ_target, Δi)` reproduces this root; keep results as exact `Rational`; quantize
    deterministically to WAD 1e18 and Q64.96 from the exact rationals (the GDX->fixture
    pattern at EPS≈1e-15); emit the constant table via `cse`+`ccode`. State SymPy is the
    computational check, Lean the proof authority.
  </action>
  <verify>
    <automated>grep -q "σ_target > d²\|sigma_target > d\|σ_target > d\^2" .planning/research/v2-controller/C2-PROOF-CASE.md && grep -qE "n *>= *2|n ≥ 2" .planning/research/v2-controller/C2-PROOF-CASE.md && grep -qi "sympy" .planning/research/v2-controller/C2-PROOF-CASE.md && grep -q "disc" .planning/research/v2-controller/C2-PROOF-CASE.md && grep -qi "rational" .planning/research/v2-controller/C2-PROOF-CASE.md</automated>
  </verify>
  <done>Doc names all four stages, states feasibility preconditions (n>=2, strict σ_target > d²), and gives the Stage-1 SymPy closed-form root with discriminant/coefficients and the exact-rational WAD/Q64.96 quantization recipe.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Lean, Plank, and gamsDiff stages</name>
  <files>.planning/research/v2-controller/C2-PROOF-CASE.md</files>
  <read_first>
    - .planning/research/v2-controller/C2-PROOF-CASE.md (continue the file from Task 1)
    - .planning/research/v2-controller/CONTROLLERS.md (C2: theorem name sigma_xs_poly_target_exists at eta.lean:560; EVM cost = one fixed-point sqrt + few mulDiv)
    - .planning/research/v2-controller/TOOLING-CONTROL-DSL.md (§4: gamsDiff-style fixtures, forge --via-ir, EPS tolerance)
    - .planning/research/v2-controller/ON-CHAIN-REALIZATION.md (the SPEC-02 sqrt/signed-mulDiv signatures the Plank stage consumes — may not exist yet if plan 10-01 not run; cite by name regardless)
  </read_first>
  <action>
    Append three stage sections, each concrete:

    "## Stage 2 — Lean existence/positivity" — name the proven theorem
    `sigma_xs_poly_target_exists` (`eta.lean:560`) and state exactly what it guarantees:
    existence and positivity of the Δi⋆ root under the feasibility preconditions. State
    SymPy's closed form must equal this proven statement (computational check vs proof
    authority). Note the GAMS σ identity in `eta_sigma_xs_realized_connection.md` is the
    numeric ground-truth side.

    "## Stage 3 — Plank fixed-point evaluation" — state the on-chain evaluation consumes
    the SPEC-02 fixed-point `sqrt` (for `disc`) plus a few `mulDiv`; the `n`-dependent
    coefficients `c₁, c₂` and `n(n-1)(2n-1)/3` are integer constants computable on-chain
    or shipped. State the saturate-never-revert obligation applies (guard `disc >= 0`
    before sqrt; clamp the result into the admissible int24 Δi band). Reference
    ON-CHAIN-REALIZATION.md for the primitive signatures.

    "## Stage 4 — gamsDiff fixture equality" — state the differential-test contract:
    a `gamsDiff`-style fixture `{inputs (n, d, σ_target), reference Δi⋆}` produced from
    the SymPy exact-rational reference; `forge --via-ir` runs the Plank evaluation over
    the same inputs and compares to a STATED tolerance (EPS≈1e-15 for the rational
    reference; state a wider fixed-point tolerance for the Plank quantized result and
    justify it as round-toward-zero accumulation). State equality-within-tolerance is
    the pass condition.

    Close with a one-line "## Pipeline summary" table: stage | tool | artifact | check.
  </action>
  <verify>
    <automated>grep -q "sigma_xs_poly_target_exists" .planning/research/v2-controller/C2-PROOF-CASE.md && grep -qi "plank" .planning/research/v2-controller/C2-PROOF-CASE.md && grep -qi "gamsdiff\|gamsDiff" .planning/research/v2-controller/C2-PROOF-CASE.md && grep -qi "tolerance" .planning/research/v2-controller/C2-PROOF-CASE.md && grep -qi "sqrt" .planning/research/v2-controller/C2-PROOF-CASE.md</automated>
  </verify>
  <done>Doc has the Lean stage (names sigma_xs_poly_target_exists existence/positivity), the Plank stage (consumes SPEC-02 sqrt + mulDiv, saturate-never-revert), and the gamsDiff stage (fixture equality with explicit tolerance), plus a pipeline-summary table.</done>
</task>

</tasks>

<verification>
- `C2-PROOF-CASE.md` exists and names all four pipeline stages.
- Closed-form root, discriminant, and coefficients are written explicitly.
- Feasibility preconditions n>=2 and strict σ_target > d² are present.
- Lean theorem `sigma_xs_poly_target_exists` is named.
- gamsDiff stage states an explicit tolerance.
</verification>

<success_criteria>
SPEC-03 satisfied: a single end-to-end proof case (C2: σ_target -> Δi⋆) is specified
across every layer — SymPy derivation + exact-rational quantization -> Lean existence
(sigma_xs_poly_target_exists) -> Plank fixed-point evaluation -> gamsDiff fixture
equality within a stated tolerance — including the feasibility preconditions.
</success_criteria>

<output>
After completion, create `.planning/milestones/v2-controller/phases/10-consolidated-spec/10-02-SUMMARY.md`.
</output>
