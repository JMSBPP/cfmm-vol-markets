---
phase: 10-consolidated-spec
plan: 04
type: execute
wave: 3
depends_on: ["10-03"]
files_modified:
  - .planning/milestones/v2-controller/phases/10-consolidated-spec/10-04-REVIEW.md
autonomous: false
requirements: [SPEC-04]
must_haves:
  truths:
    - "The mandatory two-step review of the finalized spec is tracked as an explicit, owed gate"
    - "The review is marked DEFERRED / blocked-on-user (user choice 2026-06-28) and is NOT auto-run"
    - "When un-blocked, the gate spawns exactly the two required reviewers: Reality Checker + Solidity Smart Contract Engineer, in parallel"
  artifacts:
    - path: ".planning/milestones/v2-controller/phases/10-consolidated-spec/10-04-REVIEW.md"
      provides: "The captured two-step review findings (created only when the user un-blocks)"
  key_links:
    - from: "10-04 review gate"
      to: "STATIC-CONTROL-KERNEL-SPEC.md + ON-CHAIN-REALIZATION.md + C2-PROOF-CASE.md"
      via: "the three reviewed artifacts handed to both reviewers unchanged"
      pattern: "STATIC-CONTROL-KERNEL-SPEC"
---

<objective>
Track the mandatory two-step review of the finalized static-control-kernel spec
(SPEC-04) as an explicit, owed gate — WITHOUT auto-running it. The user deferred this
review on 2026-06-28 (proceed to requirements/roadmap/plan first); per the project's
CLAUDE.md two-step reviewer process, no pre-commit spec artifact is "ready to execute /
commit" until it passes Reviewer 1 (Reality Checker) + Reviewer 2 (Solidity Smart
Contract Engineer) run IN PARALLEL.

Purpose: keep the owed review visible and unambiguous, so an execution commit is not
made before it runs, while honoring the user's defer decision.
Output: a checkpoint that pauses for the user; on un-block, `10-04-REVIEW.md` with
consolidated severity-sorted findings.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2-controller/MILESTONE.md
@.planning/milestones/v2-controller/REQUIREMENTS.md
@.planning/research/v2-controller/STATIC-CONTROL-KERNEL-SPEC.md
@.planning/research/v2-controller/ON-CHAIN-REALIZATION.md
@.planning/research/v2-controller/C2-PROOF-CASE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: DEFERRED two-step review gate (blocked-on-user)</name>
  <status>DEFERRED — blocked on user (deferred 2026-06-28). Do NOT auto-run.</status>
  <what-built>
    The finalized spec set: STATIC-CONTROL-KERNEL-SPEC.md (SPEC-01), its
    ON-CHAIN-REALIZATION.md (SPEC-02) and C2-PROOF-CASE.md (SPEC-03) siblings.
  </what-built>
  <decision>
    The user must explicitly un-block this gate before it runs. The user chose on
    2026-06-28 to defer the mandatory two-step review until after requirements/roadmap/
    planning. This task exists only to TRACK that owed review; it must not be executed
    autonomously.
  </decision>
  <how-to-run-when-unblocked>
    Per CLAUDE.md two-step reviewer process, spawn BOTH reviewers IN PARALLEL on the
    same three artifacts (reviewers do not edit them):
      1. Reviewer 1 — Reality Checker (ALWAYS): evidence-based, NEEDS-WORK-by-default
         scrutiny of fantasy claims / unverified assumptions / production-readiness gaps
         in the on-chain realization and proof-case specs.
      2. Reviewer 2 — Solidity Smart Contract Engineer (the catalog specialist closest
         to this EVM/fixed-point spec): scrutinize the matvec layout, the G5 primitive
         signatures (sqrt / signed mulDiv / clamp), the saturate-never-revert rule, and
         int24 bounds/rounding for on-chain correctness.
    Both return severity-sorted findings (BLOCKER / MAJOR / MINOR). Capture a
    consolidated summary in 10-04-REVIEW.md, resolve BLOCKERs/MAJORs, and only then is
    the spec cleared for an execution commit.
  </how-to-run-when-unblocked>
  <resume-signal>User types "run spec review" (or "approve deferral to next milestone") to un-block or keep deferred.</resume-signal>
</task>

</tasks>

<verification>
- This plan is `autonomous: false` and its sole task is a blocking, deferred checkpoint.
- The owed two-step review is tracked but NOT auto-run.
- The gate names exactly Reality Checker + Solidity Smart Contract Engineer, in parallel.
</verification>

<success_criteria>
SPEC-04 represented: the mandatory two-step review is tracked as an explicit owed gate,
marked deferred/blocked-on-user, naming the two required reviewers — satisfying the
requirement to track-but-not-auto-run per the user's 2026-06-28 defer decision.
</success_criteria>

<output>
On un-block only, create `.planning/milestones/v2-controller/phases/10-consolidated-spec/10-04-REVIEW.md`
with the consolidated severity-sorted reviewer findings, then
`.planning/milestones/v2-controller/phases/10-consolidated-spec/10-04-SUMMARY.md`.
</output>
