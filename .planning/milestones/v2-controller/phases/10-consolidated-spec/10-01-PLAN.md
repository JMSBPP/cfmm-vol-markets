---
phase: 10-consolidated-spec
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/v2-controller/ON-CHAIN-REALIZATION.md
autonomous: true
requirements: [SPEC-02]
must_haves:
  truths:
    - "A reader can see the exact on-chain object: one banded Toeplitz/circulant matvec u = -K x with K[i,j]=κ(i-j), truncated to n<=3-4 by spatial decay"
    - "Each EVM-ready controller (C1, C2, C3, C5, C7, C9) has its concrete per-position set-point formula written as the matvec/set-point entries"
    - "The three fixed-point primitives to BUILD (G5) have explicit signatures: fixed-point sqrt, signed mulDiv, clamp/saturating add"
    - "The saturate-never-revert rule is stated as a hard rule with its DoS rationale"
    - "int24 bounds and rounding mode per inversion are specified (MIN/MAX_TICK, admissible Delta-i band, round-toward-zero, Plank coordinate bridge)"
  artifacts:
    - path: ".planning/research/v2-controller/ON-CHAIN-REALIZATION.md"
      provides: "Concrete SPEC-02 on-chain realization section"
      contains: "## On-chain realization"
  key_links:
    - from: "ON-CHAIN-REALIZATION.md"
      to: "EVM-CONTROL-PRIMITIVES-MAP.md"
      via: "explicit citation of the G5 primitive gaps and the saturate-never-revert rule"
      pattern: "EVM-CONTROL-PRIMITIVES-MAP"
---

<objective>
Author the concrete ON-CHAIN REALIZATION section (SPEC-02) for the static control
kernel. The draft consolidated spec (§4) only gestures at this; this plan produces a
standalone, implementation-grade sibling doc the consolidated spec will reference and
the future implementation milestone will build from.

Purpose: turn "one banded matvec + some primitives" into exact layout, exact
set-point formulas, exact primitive signatures, exact bounds/rounding — no
hand-waving, so a Plank implementer needs no further research.
Output: `.planning/research/v2-controller/ON-CHAIN-REALIZATION.md`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v2-controller/MILESTONE.md
@.planning/milestones/v2-controller/REQUIREMENTS.md
@.planning/milestones/v2-controller/ROADMAP.md
@.planning/research/v2-controller/EVM-CONTROL-PRIMITIVES-MAP.md
@.planning/research/v2-controller/LIT-LATTICE-CONTROL.md
@.planning/research/v2-controller/CONTROLLERS.md
@.planning/research/v2-controller/STATIC-CONTROL-KERNEL-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the matvec layout + per-position set-point entries</name>
  <files>.planning/research/v2-controller/ON-CHAIN-REALIZATION.md</files>
  <read_first>
    - .planning/research/v2-controller/ON-CHAIN-REALIZATION.md (the file being written; create if absent)
    - .planning/research/v2-controller/LIT-LATTICE-CONTROL.md (§A1/A2 + "Recommended theoretical frame": Toeplitz/circulant, DFT decoupling, exponential tap decay, n<=3-4 band)
    - .planning/research/v2-controller/CONTROLLERS.md (C1, C2, C3, C5, C7, C9 closed forms + the readiness ranking table)
    - .planning/research/v2-controller/EVM-CONTROL-PRIMITIVES-MAP.md (§4 cost: n^2 mulDiv + n^2 signed adds)
  </read_first>
  <action>
    Create the doc with a top H1 title and a "## On-chain realization" section. Write
    two subsections with concrete content (no "align/match" placeholders):

    (A) "### Banded Toeplitz/circulant matvec layout" — state the on-chain object as
    the fixed shift-invariant law `u = -K x` with `K[i,j] = kappa(i-j)` over the tick
    lattice `i in [-120, 120]`. State it is banded/truncated to `n <= 3-4` taps
    justified by the exponential spatial tap decay of the Bamieh-Paganini-Dahleh frame
    (cite LIT-LATTICE-CONTROL §A1/A2). State the circulant variant for the
    periodic/wrapped grid (diagonalized by the lattice DFT, synthesis pushed off-chain).
    State the EVM cost explicitly: `n^2` `mulDiv` + `n^2` signed adds per evaluation
    (cite EVM-CONTROL-PRIMITIVES-MAP §4); for `n=3` ~9 `mulDiv`. State that `A,B,K`
    are compile-time constants precomputed off-chain (GAMS/SymPy), never solved on-chain.

    (B) "### Per-position set-point entries" — a table mapping each EVM-ready controller
    to the closed-form it contributes as a set-point entry of the operator, with the
    actual formula:
    - C1 (zero-slippage): rational target `P_half = L̄/(L̄-Δᴵ)` via one `mulDiv`, then
      price->tick via `getTickAtSqrtRatio`; Δi⋆ = log(L̄/(L̄-Δᴵ))/(logλ·i) shipped
      precomputed if log is avoided. Note Plank bridge `Δi⋆_Plank = 2·Δi⋆_Lean`.
    - C2 (σ_xs variance-target): quadratic root (full formula deferred to plan 10-02);
      here just record it needs `1 sqrt + few mulDiv`.
    - C3 (band-min): `Δi = Δi_min` — pure clamp to band floor, no payoff eval.
    - C5 (small-signal gain): `lim π_½/Δᴵ² = P²(P-1)²`, `P = λ^{i·Δi}` via
      `getSqrtRatioAtTick` + two squarings.
    - C7 (η-split): `P_η(i) = P_½(i₋)·P_½(i₊)`, `i₋ = ⌊η·i⌋`, `i₊ = i - i₋`; two
      `getSqrtRatioAtTick` + one `mulDiv`.
    - C9 (realized variance): `σ_xs = #·σ_realized - (#-1)d² - 2dΔi·#(#-1)` closed form,
      no per-node loop.
    Each row: controller id, set-point formula, primitives used, source theorem name.
  </action>
  <verify>
    <automated>grep -q "## On-chain realization" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md && grep -q "Toeplitz" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md && grep -qE "K\[i,j\]|kappa\(i-j\)" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md && grep -q "set-point" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md</automated>
  </verify>
  <done>Doc has the "## On-chain realization" section with the banded matvec layout (K[i,j]=kappa(i-j), n<=3-4, n^2 mulDiv cost) and a per-position set-point table covering C1/C2/C3/C5/C7/C9 with their formulas and source theorems.</done>
</task>

<task type="auto">
  <name>Task 2: Specify the required fixed-point primitives, saturate-never-revert rule, and int24 bounds/rounding</name>
  <files>.planning/research/v2-controller/ON-CHAIN-REALIZATION.md</files>
  <read_first>
    - .planning/research/v2-controller/ON-CHAIN-REALIZATION.md (continue the file from Task 1)
    - .planning/research/v2-controller/EVM-CONTROL-PRIMITIVES-MAP.md (§3 MISSING primitives; §4 numerical pitfalls: signedness, WAD scale discipline, overflow/saturation, truncation, denom==0)
    - .planning/research/v2-controller/CONTROLLERS.md (per-controller regime/feasibility + admissible Δi band; the "saturate, never revert" non-negotiable)
  </read_first>
  <action>
    Append three more subsections, all concrete:

    (C) "### Required fixed-point primitives to build (gap G5)" — give explicit Plank
    signatures (u256 substrate, two's-complement signed) for each missing helper and
    cite EVM-CONTROL-PRIMITIVES-MAP §3:
    - fixed-point `sqrt`: `sqrt(x: u256) -> u256` over a stated scale (WAD or Q-fixed),
      built on the `tick_math` / `mostSignificantBit` MSB machinery; state rounding
      (toward zero) and the scale convention.
    - signed `mulDiv`: `signedMulDiv(a: u256, b: u256, denom: u256) -> u256` — operate
      on magnitudes via the existing 512-bit `mulDiv`, recover sign via XOR of operand
      sign bits (`@evm_slt`), re-apply via two's-complement negate. State this is the
      #1 correctness hazard.
    - clamp / saturating add: `clamp(x, lo, hi)` and `satAdd(x, y)` (signed-saturating),
      since only unsigned `min/max` + revert-on-overflow casts exist today.

    (D) "### Saturate-never-revert rule" — state as a HARD RULE: the controller must
    saturate, never revert, because a reverting `beforeSwap` hook DoSes the swap. Spell
    out the consequences: clamp BEFORE any value can overflow rather than relying on
    checked `*`; guard any controller-derived denominator against 0 BEFORE `mulDiv`
    (which reverts on `denom==0`); use one consistent WAD scale and divide-by-WAD per
    multiply (a missing `/WAD` blows scale by 1e18 per multiply). Cite the real
    `CESLongPayoff.plk:42` `*%` overflow at ~2^192.

    (E) "### int24 bounds + rounding mode per inversion" — a table: per controller
    (C1/C2/C3/C5/C7/C9) give the int24 admissibility (`MIN_TICK = -887272`,
    `MAX_TICK = 887272`), the admissible spacing band (`Δi in [1,200]` GAMS abort
    guard; fixtures use `Δi=1`; C7 requires `IsInt24 i₋, i₊`), the rounding mode
    (round-toward-zero for `</`/`mulDiv`; keep intermediate scale high Q96/Q128, downcast
    once mirroring tick_math's Q128.128->Q96 final shift), and the Plank coordinate
    bridge `Δi⋆_Plank = 2·Δi⋆_Lean` where it applies (C1).
  </action>
  <verify>
    <automated>grep -q "signed mulDiv\|signedMulDiv" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md && grep -q "saturate, never revert\|saturate-never-revert\|Saturate-never-revert" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md && grep -q "int24" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md && grep -qi "sqrt" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md && grep -q "887272" .planning/research/v2-controller/ON-CHAIN-REALIZATION.md</automated>
  </verify>
  <done>Doc specifies the three G5 primitive signatures (sqrt, signed mulDiv, clamp/saturating add), states the saturate-never-revert hard rule with DoS + WAD-scale rationale, and gives an int24-bounds/rounding-per-inversion table with MIN/MAX_TICK and the Plank coordinate bridge.</done>
</task>

</tasks>

<verification>
- `ON-CHAIN-REALIZATION.md` exists and contains the "## On-chain realization" section.
- All five required content blocks present: matvec layout, set-point entries, G5
  primitive signatures, saturate-never-revert rule, int24 bounds/rounding table.
- Every controller named is EVM-ready per CONTROLLERS.md (C1/C2/C3/C5/C7/C9).
- The doc cites EVM-CONTROL-PRIMITIVES-MAP.md for the primitive gaps.
</verification>

<success_criteria>
SPEC-02 satisfied: the on-chain realization is specified concretely — banded
Toeplitz/circulant matvec + per-position set-points + required fixed-point primitives
(sqrt, signed mulDiv, clamp) + saturate-never-revert rule + int24 bounds/rounding per
inversion — with no unresolved "align/match" hand-waving.
</success_criteria>

<output>
After completion, create `.planning/milestones/v2-controller/phases/10-consolidated-spec/10-01-SUMMARY.md`.
</output>
