import types::Liquidity:LiquidityStock;
import types:Nominal:Nominal;
import std::addr::addr;

const SLOT_TICK_MIN =
const SLOT TICK_MAX =
const SLOT_MARKET_TOTAL_LIQUIDITY =
const SLOT_TICK =
const SLOT_MARKET_CONTROLLER_ID =


// ===== state fetchers =====
const tickMin = fn() u256 {
      @evm_sload(SLOT_TICK_MIN);
};

const tickMax = fn() u256 {};
const tick = fn () u256 {};
const marketTotalLiquidity = fn() LiquidityStock {};
const marketControllerId = fn() addr {};

//= =======


// known at run time 
const marketExposure = fn() Nominal {};


// ==== state - updates ===
const updateTickMin = fn(_tickMinCand:u256) {};
const updateTickMax = fn(_tickMaxCand:u256) {};