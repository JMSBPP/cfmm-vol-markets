import types::Liquidity::Liquidity;

const GridGeometry = struct {
      liquidityScaling: Liquidity,
      tickSpacing:u256,
      elasticity:u256
};

// @dev: updates the run time values of a grdiGeometry
const update = fn(comptime T, val: T) GridGeometry {};

