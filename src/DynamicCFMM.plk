import v3::


   //  init state
    /**********************************************************************/
    /* (di = 20 ,i = 100, i_l = -120, i_u = 120, L(i) = 1e18, Y = 100e18) */
    /**********************************************************************/


const return_runtime = fn () never {
    let buf = @malloc_uninit(@runtime_length());
    @evm_codecopy(buf, @runtime_start_offset(), @runtime_length());
    @evm_return(buf, @runtime_length());
};

const INIT_STATE = struct {
      tickSpacing: u256,
      tick: u256,
      tickLower: u256,
      tickUpper:u256,
      cashStock:u256
};



init {
}

run {}