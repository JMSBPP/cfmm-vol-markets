---
phase: 01-ground-truth-notation-and-the-rulings-triage
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md
autonomous: true
requirements: [NOT-02, NOT-06, NOT-07, NOT-09]

must_haves:
  truths:
    - "Every collision has one chosen reading AND its rejected reading written down, and both come from a user ruling or a pinned source — never from this plan's discretion."
    - "The vol axis carries the glyph `L_σ ≡ ΔQ_v★`; plain `L`/`L̄` is the price axis; the axis rule covers `L(i_K)` and `ℓ` by name."
    - "`UNITS_AND_SCALES.md:114`'s `L(i_K) = ΔQ_v★·ℓ` is reconciled with `L(i_K) = L̄·ℓ` by its own `mint SIZING` label, so no later reader can revive the `τ*=1` route from it."
    - "`∂φ/∂ν` is on the record as determinate and strictly positive under three citable conditions, distinguished from the two behavioral estimands."
    - "No symbol enters a later artifact without appearing in the symbol register with a pinned source or a recorded user ruling."
  artifacts:
    - path: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md"
      provides: "Four collision resolutions, the SETTLED two-axis record with the UNITS:114 reconciliation, the ∂φ/∂ν determinacy record, and the symbol register"
      min_lines: 160
      contains: "SETTLED"
  key_links:
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md"
      to: "control/.planning/PROJECT.md USER NOTATION RULINGS 2026-08-08"
      via: "rulings R1 (L_σ) and R2 (rename the source side) are cited, not re-derived"
      pattern: "871e387"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md"
      to: "plank/notes/UNITS_AND_SCALES.md:70 and :114"
      via: "quoted normative unit line plus the mint-SIZING chain reconciliation"
      pattern: "mint SIZING"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md"
      to: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md"
      via: "C-1's glyph comes from the §4 checkpoint ruling; C-2/C-3 cite §3.1/§3.3"
      pattern: "RULINGS-TRIAGE.md"
---

<objective>
Write the notation map: one chosen reading per collision with the rejected reading stated, the
two-axis liquidity distinction recorded as SETTLED under the `L_σ` glyph, `∂φ/∂ν` classified as
a determinate strictly positive factor, and a symbol register that makes `NOT-06` enforceable
rather than aspirational.

Purpose: `NOT-02`, `NOT-06`, `NOT-07`, `NOT-09`. This is the artifact that stops a proof from
being about the wrong object.

Output: `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md

**Path glossary.** `WT` = `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller`. `PLANK` =
`/home/jmsbpp/cfmms-playground/cfmm-wt/plank` (PEER-OWNED, READ-ONLY). `SRC` =
`WT/notes/VOLATILITY_INTRUMENTS_MEV.md` (**this project's own file**). `DOC` =
`PLANK/notes/VOLATILITY_INSTRUMENTS.md`. `UNITS` = `PLANK/notes/UNITS_AND_SCALES.md`.

**Hard prohibitions.** Do not modify anything under `PLANK/`, `LEAN/` or `WT/.planning/`. Do
not edit `SRC`. `grep` dispatches to `ugrep` — use `-F` for every LaTeX literal.

**TWO BINDING USER RULINGS govern this plan** (`PROJECT.md`, block
`USER NOTATION RULINGS 2026-08-08`, commit `871e387`). Read them before writing a word:

- **R1 — `L_{\sigma} \equiv \Delta Q_v^{\star}`.** Vol-axis liquidity takes the glyph
  `L_{\sigma}`, identified with the **starred, stored** target vega
  (`UNITS_AND_SCALES.md:70`). Plain `L` / `\bar L` remain the **price axis**. This
  **SUPERSEDES the `L^{\text{pool}}` / `L^{\text{vol}}` superscript tags an earlier draft of
  this plan proposed — those tags are DELETED and must not appear anywhere.** The subscript
  carries the axis and both letters come from the doc's own symbols. Not the unstarred
  `\Delta Q_v` (`UNITS_AND_SCALES.md:72`), which is the lens readout in collateral base units
  per Algebra vol unit, never stored — a sensitivity, not a liquidity. **The star is
  load-bearing.**
- **R2 — the `\pi^{\varphi}` collision is DISSOLVED, not renamed.** `DOC` KEEPS `\pi^{\varphi}`
  for its numbered Definition 25. **`SRC` no longer contends for the glyph at all**: commit
  `c521af5` restructured `SRC` and deleted the composite's abbreviation, and `cf386de`
  restructured it again — verify with
  `grep -cF 'pi^{\varphi}' notes/VOLATILITY_INTRUMENTS_MEV.md` → **0**.
  `Definition 32 (Event-time plant) @ 33af6a85` writes the state vector's fourth component out in
  full as `\pi^{\phi} - \pi^{\text{LVR}}` (locate it with
  `grep -nF 'pi^{\phi} - \pi^{\text{LVR}}'`). **Cite the BLOCK, never a line** — `SRC` is 14
  numbered blocks as of `cf386de` and its line numbers have moved at every commit.
  **Consequences, binding:** (a) NO glyph is minted — not `\Pi^{\text{pv}}` (an earlier draft's
  autonomous mint, which renamed the wrong side), not the three candidates plan 01-02 once
  proposed, not anything. (b) C-1 records a collision that is CLOSED at the source, with both
  readings still stated so a reader of the old text can resolve it. (c) `\pi^{\varphi}` in any
  artifact this project writes means `DOC` Definition 25's portfolio value function and
  nothing else.

**Ownership.** `INVENTORY.md` §10 assigns each topic one normative home. Citation form (§5),
pins (§2), `SRC` errata (§9) belong to `INVENTORY.md`; the 13 rulings belong to
`RULINGS-TRIAGE.md`. Point with `> Authoritative detail: <artifact> §N`; never restate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the four collisions with chosen AND rejected readings</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md` (the `USER NOTATION RULINGS 2026-08-08` block — R1 and R2 verbatim)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md` (§3.1, §3.3 supply C-2/C-3 evidence; §4 Item 1 supplies C-1's glyph)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` — **ALL 190 lines.** In particular `Convention 8 (Liquidity axis)`, `Definition 32 (Event-time plant)`, `Rule 13`, `Theorem 31` and `Proposition 13`. The `\pi^{\phi}` algebra this task used to read at lines 100-125 was **DELETED at `cf386de`**
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md` — locate by content: `grep -nF 'Definition 25 (Portfolio value function)'`, `grep -nF 'Definition 26 (LVR rate)'`, `grep -nF 'I_ord'` or `grep -nF 'Protocol Input'`, `grep -nF 'standing'`
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` (§2 pins, §5 citation rules, §9 errata)
  </read_first>
  <action>
Create `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md`.

Header, verbatim except the date:

```
# NOTATION MAP

**Requirements:** NOT-02, NOT-06, NOT-07, NOT-09
**Written:** <YYYY-MM-DD>
**Governing rulings:** `PROJECT.md`, `USER NOTATION RULINGS 2026-08-08` (commit `871e387`) —
R1 (`L_{\sigma} \equiv \Delta Q_v^{\star}`) and R2 (rename the SOURCE side of the
`\pi^{\varphi}` collision), plus the glyph ruled at `RULINGS-TRIAGE.md` §4 Item 1.

> Authoritative detail: citation form and pins — `INVENTORY.md` §5, §2. `SRC` errata —
> `INVENTORY.md` §9. The 13 blocking decisions — `RULINGS-TRIAGE.md`. This document restates
> none of them.

**Binding rule.** Every collision below is resolved with BOTH the chosen reading and the
rejected reading stated. A later artifact that uses a rejected reading is a defect, not a
variant. Where a rejected reading must still be referred to, it is referred to by the
DISAMBIGUATED name assigned here, never by the bare glyph.
```

Then `## 1. Collisions`, four subsections in this exact shape:

```
### C-<n> — <glyph or phrase>

**Sources in conflict:** <each, cited per INVENTORY.md §5>
**Authority:** <the user ruling or pinned source that decides it — never "this plan's judgment">
**CHOSEN reading:** <written out>
**REJECTED reading:** <written out>
**Disambiguated name for the rejected reading:** <the name later artifacts must use, or "none needed">
**Why:** <one paragraph>
**Defects routed (not fixed here):** <if any, else "none">
```

**C-1 — `\pi^{\varphi}`.**
Sources: the pre-`c521af5` `SRC:28` (**that line no longer exists, and the region around it was
deleted again at `cf386de`**; at the old pin `0ec30184` it gave
`\pi^{\varphi} \equiv \pi^{\phi} - \pi^{\text{LVR}}`); `DOC Definition
25 (Portfolio value function)` gives `\pi^{\varphi}` as the on-curve valuation of the RESERVES
`(Q_X^L(p_{\varphi}), Q_M^L(p_{\varphi}))`, the conic-dual object of the trading function; `DOC`
states the standing `\phi`/`\varphi` split explicitly (locate it — at plan time around
`DOC:919`).
Authority: **R2** (commit `871e387`), **executed in the source at commit `c521af5`**.
CHOSEN: **`DOC` Definition 25's reading keeps the glyph `\pi^{\varphi}`.**
REJECTED: the old `SRC:28 @ 0ec30184` composite keeping the glyph (that display was struck at `c521af5`).
Disambiguated name: **NONE IS NEEDED — the collision is dissolved.** Write verbatim:
`**RESOLVED AT SOURCE.** \`SRC\` no longer uses \`\pi^{\varphi}\`
(\`grep -cF 'pi^{\varphi}'\` → 0 at \`33af6a85\`). \`Definition 32 (Event-time plant) @ 33af6a85\`
writes the composite out in full as \`\pi^{\phi} - \pi^{\text{LVR}}\`. No glyph is minted, and the
entry is retained so a reader of the pre-\`c521af5\` text can resolve the old abbreviation.`
**Under no circumstances invent a glyph in this task.**
Why: the standing precedence is that doc notation is preserved and the CONFLICTING symbol gets
the new glyph. `DOC`'s use is numbered, load-bearing and peer-owned; `SRC`'s is this project's
own and is the one that moves.
Defects routed: **the circularity R2 existed to break is now GONE at the source — record that,
do not record a live defect.** `DOC Definition 26 (LVR rate)` computes `\pi^{\mathrm{LVR}}` with
the second derivative "well-defined on Definition 25's object", i.e. FROM `\pi^{\varphi}`. The
old `SRC:28 @ 0ec30184` therefore placed the same glyph on both sides of its own definition once
Definition 26 was imported. Writing the composite out in full breaks the circle: `DOC`'s
`\pi^{\varphi}` is the primitive; `SRC`'s composite is a derived quantity with no abbreviation.
Record this as **RESOLVED-BEFORE-PHASE-1, fixed at `c521af5` and still absent at `cf386de`**,
not as an erratum needing a read-through tag.

**C-2 — `\nu` vs `u`.** Authority: `RULINGS-TRIAGE.md` §3.1 (already closed). CHOSEN `\nu`;
REJECTED `u` as a distinct object (there is no distinct object). Disambiguated name: none —
instead the rule that every cross-document citation states which glyph the cited document uses.
Point at §3.1 for the evidence rather than re-quoting it; add here only the downstream rule and
the `u_{\text{ex}}` / `u_{\text{en}}` collision warning.

**C-3 — leg pairing in `\pi^{\phi}`.** Authority: `RULINGS-TRIAGE.md` §3.3, which as of
`cf386de` records this as **RESOLVED AT SOURCE**. CHOSEN: `M` leg fee'd by `\phi_M` against
`\Delta Q_M`, `X` leg by `\phi_X` against `\Delta Q_X`, price factor `p_{(\eta,\Delta_i)}` on the
`X` term — whose **sole normative home is now `DOC Rule 6 (Per-leg fee split) @ 0c5ce574`**,
because `SRC` no longer defines `\pi^{\phi}` at all. Verify:
`grep -cF 'phi_M \, \Delta Q_M' notes/VOLATILITY_INTRUMENTS_MEV.md` → **0**.
REJECTED: the two dissenting pairings that once sat at `SRC:106`/`SRC:118` (`0ec30184`),
re-pinned to `SRC:111`/`SRC:123` at `45aeba4c` and **STRUCK at `cf386de`**. Disambiguated name:
none — the rejected displays no longer exist, so there is nothing to refer to by name.
**Defects routed:** none live. `> Authoritative detail: INVENTORY.md §9.1, §9.2` (both closures)
and `INVENTORY.md` §8's 2026-08-09 STRUCK table.
**Binding, and the reason this entry is kept at all:** a reader of the pre-`cf386de` text will
find those displays and must be able to resolve them. **No artifact may re-pin `SRC:111` or
`SRC:123` at their current offsets** — different material sits there now.

**C-4 — `\Delta Q_v^{\star}` vs `\Delta Q_{\upsilon}`.**
Sources: `DOC`'s Protocol Input display (locate it; at plan time `DOC:672`, an UNNUMBERED
display — cite per `INVENTORY.md` §5 Rule 2's second branch, naming the nearest enclosing
numbered item) gives
`\mathcal{I}_{\text{ord}} = \{\sigma^2_K, \#_{\sigma}, s_{\upsilon}, \Delta Q_v^{\star}\}`;
`Definition 32 (Event-time plant) @ 33af6a85`'s `\Theta_{\sigma}` slot gives the same four-slot
block.
**Check the fourth slot's glyph yourself, BY CONTENT not by line** — `SRC`'s line numbers moved
at `c521af5` and again at `cf386de`, and the old `sed -n '37p'` probe this entry used to carry
now lands inside a different display. Run both and record the outputs:

```
grep -nF 'Theta_{\sigma} = \begin{bmatrix}' notes/VOLATILITY_INTRUMENTS_MEV.md
grep -cF 'Delta Q_{\upsilon}' notes/VOLATILITY_INTRUMENTS_MEV.md
```

At plan time the first returns one hit inside `Definition 32` carrying `\Delta Q_v^{\star}`, and
the second returns **0** — the retired glyph is absent from `SRC` entirely. If that is what you
measure, C-4 is a **CLOSED** collision: record both historical readings so a reader of the
pre-`c521af5` text can resolve `\Delta Q_{\upsilon}`, and mark the entry
`RESOLVED AT SOURCE (c521af5, unchanged at cf386de)`. If `\Delta Q_{\upsilon}` is present, the
collision is live and C-4 stands as written. **Do not assert the closure without both outputs.**
Authority: **R1** — it identifies `L_{\sigma}` with the STARRED object, which forces the star.
CHOSEN: **`\Delta Q_v^{\star}`**. REJECTED: `\Delta Q_{\upsilon}` as a distinct object.
Disambiguated name: none — `\Delta Q_{\upsilon}` is RETIRED, permitted only inside a verbatim
quotation of the pre-`c521af5` source (`SRC:23 @ 0ec30184`) and always followed by
`(= \Delta Q_v^{\star})`.
Add the caution verbatim: "`UNITS_AND_SCALES.md` distinguishes a THIRD object — `\Delta Q_v`
WITHOUT the star (`UNITS_AND_SCALES.md:72`), the *greek*: collateral base units per Algebra vol
unit, a sensitivity, the lens readout, never stored. `\Delta Q_v` and `\Delta Q_v^{\star}` are
NOT the same object and **the star is never optional**."
Then, as a HISTORICAL note (the correction has already landed — verify with
`grep -nF 'ENTRY_POINT.md:672' control/.planning/ROADMAP.md control/.planning/REQUIREMENTS.md`,
expect no output):

```
**Historical note.** An earlier `ROADMAP.md` cited this collision to `ENTRY_POINT.md:672`;
`ENTRY_POINT.md` is 64 lines and the actual locus is in `DOC`. **Corrected at commit
`98c2891`.** Recorded here so the correction's reason survives the correction.
> Authoritative detail: INVENTORY.md §6 (Finding INV-3).
```
  </action>
  <acceptance_criteria>
    - `NOTATION-MAP.md` exists and contains exactly four `### C-` headings.
    - `grep -c '^\*\*CHOSEN reading:\*\*'` returns 4; same for `REJECTED reading` and `**Authority:**`.
    - It contains `871e387` and the literal string `Definition 25` and `Definition 26`.
    - The deleted glyphs `\Pi^{\text{pv}}`, `L^{\text{pool}}` and `L^{\text{vol}}` are never USED as notation: **every line containing any of them must also contain one of `DELETED`, `BANNED`, `Use instead`, or `superseded`.** (They legitimately reappear in Task 3's §4.3 banned-glyph table; what is forbidden is using them to denote an object.)
    - It contains `the star is never optional` and `UNITS_AND_SCALES.md:72`.
    - It contains `98c2891` and the literal string `Historical note.`
    - It contains `> Authoritative detail: INVENTORY.md §9`.
    - `grep -nF 'ENTRY_POINT.md:672'` over ROADMAP.md and REQUIREMENTS.md returns nothing (the historical note is genuinely historical).
    - **C-1 matches the ruling by byte-comparison, not by disjunction.** Extract `RULINGS-TRIAGE.md` §4 Item 1's `**RULING:**` line. If it contains `DECLINED`, `NOTATION-MAP.md` MUST contain `**UNNAMED.**` and MUST NOT name a glyph. Otherwise, the first backtick-delimited token on that RULING line MUST appear verbatim inside C-1's span, and `**UNNAMED.**` MUST NOT appear. A check that passes when `RULINGS-TRIAGE.md` merely contains the string `Item 1` is not a check — that string is mandated to exist.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md && T=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md && test -f $F && test $(grep -c '^### C-' $F) -eq 4 && test $(grep -c '^\*\*CHOSEN reading:\*\*' $F) -eq 4 && test $(grep -c '^\*\*REJECTED reading:\*\*' $F) -eq 4 && test $(grep -c '^\*\*Authority:\*\*' $F) -eq 4 && for s in '871e387' 'Definition 25' 'Definition 26' 'the star is never optional' 'UNITS_AND_SCALES.md:72' '98c2891' 'Historical note.' '> Authoritative detail: INVENTORY.md §9'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && for g in '\Pi^{\text{pv}}' 'L^{\text{pool}}' 'L^{\text{vol}}'; do test $(grep -F -- "$g" $F | grep -cvE 'DELETED|BANNED|Use instead|superseded') -eq 0 || { echo "DELETED GLYPH USED AS NOTATION: $g"; exit 1; }; done && ! grep -qF 'ENTRY_POINT.md:672' control/.planning/ROADMAP.md control/.planning/REQUIREMENTS.md && R=$(awk '/^### Item 1 /,0' $T | grep -m1 '^\*\*RULING:\*\*') && test -n "$R" && if printf '%s' "$R" | grep -qi 'DECLINED'; then grep -qF '**UNNAMED.**' $F; else G=$(printf '%s' "$R" | sed -n 's/.*`\([^`]*\)`.*/\1/p' | head -1) && test -n "$G" && awk '/^### C-1/,/^### C-2/' $F | grep -qF -- "$G" && ! grep -qF '**UNNAMED.**' $F; fi && echo PASS</automated>
  </verify>
  <done>Four collisions each carry an AUTHORITY (a user ruling or pinned source, never this plan's discretion), a chosen and a rejected reading; `Π^pv` and the `L^pool`/`L^vol` tags appear only as deletions/bans, never as notation; C-1 either carries the user-ruled glyph or is explicitly UNNAMED; C-4's citation correction is recorded as history.</done>
</task>

<task type="auto">
  <name>Task 2: Two-axis liquidity SETTLED under L_σ, with the UNITS:114 reconciliation; and ∂φ/∂ν</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md` (the file you are appending to)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/UNITS_AND_SCALES.md` — **ALL 139 lines.** In particular line 70 (the `ΔQ_v★` unit cell), line 72 (the unstarred greek), and the §4 conversion-chain row at line 114 (`ΔQ_v★ → the position (mint SIZING, dimension (ii))`). Locate them by content: `grep -nF 'RAW LIQUIDITY units'`, `grep -nF 'mint SIZING'`.
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md` (`USER RULING 2026-08-08` items 1-3 AND `USER NOTATION RULINGS 2026-08-08` R1)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md` (all 64 lines — the boxed `∂φ/∂ν` you classify)
  </read_first>
  <action>
Append `## 2. Two-axis liquidity — SETTLED (NOT-07)`.

```
**STATUS: SETTLED by user ruling 2026-08-08, refined by the notation ruling R1 at commit
`871e387`.** Not an open notation question; not re-litigated by any later artifact — but see
the reconciliation in §2.1 BEFORE relying on that foreclosure.

**The factorization (price axis) — NOW CARRIED BY THE SOURCE ITSELF.**

At `45aeba4c` this factorization existed only in `PROJECT.md`'s user ruling. **Commit `cf386de`
added `Convention 8 (Liquidity axis) [M18]` to `SRC`, which states BOTH axes and the derivative
identity outright.** Quote it verbatim from the file — locate it by content, never by line:

```
grep -nF 'Convention 8 (Liquidity axis)' notes/VOLATILITY_INTRUMENTS_MEV.md
grep -nF 'L_{\sigma} \; &\equiv \; \Delta Q_v^{\star}' notes/VOLATILITY_INTRUMENTS_MEV.md
grep -nF 'partial L(i_K)}{\partial \pi^{\phi}}' notes/VOLATILITY_INTRUMENTS_MEV.md
```

`Convention 8 @ 33af6a85` carries, in order: `L_{\sigma} \equiv \Delta Q_v^{\star}` tagged
`(volatility axis)` and `L, \bar L` tagged `(price axis)`; the vol-axis ladder identity
`L_{\sigma}(i_K) = L_{\sigma}\,\ell(\xi^{\star},\iota;i_K)` with `\sum_{i_K} L_{\sigma}(i_K) =
\Delta Q_v^{\star}` and `\sum_{i_K}\ell = 1`; and the price-axis pair

    L(i_K) = \bar L \cdot \ell(\xi, \iota; i_K)
    \partial L(i_K)/\partial \pi^{\phi} = \ell(\xi,\iota;i_K) \cdot \partial \bar L/\partial \pi^{\phi}

`\ell(\xi,\iota;i_K)` is **geometry — invariant to the fee payoff**. Only aggregate `\bar L`
responds. `\iota` is the ladder resolution (`TEX:212`), the same object `SRC` calls
`\#_{\sigma}`.

**Record the upgrade explicitly, and say what it buys.** Ruling R1 is no longer this project's
gloss on a peer document: **the source states the two-axis distinction in its own numbered
block, in R1's own glyph `L_{\sigma}`.** Cite `Convention 8 @ 33af6a85` as the primary source for
§2 from here on, with `PROJECT.md`'s R1 (commit `871e387`) as the ruling that authorised the
glyph and `UNITS_AND_SCALES.md:70 @ a46ba298` as the normative unit. **§2's `Normative source`
column changes accordingly** — do not leave the axis table citing only `PROJECT.md`.

**O3 — `\phi_X`'s `\nu`-dependence: CLOSED AT SOURCE (`cf386de`). Do NOT record it as OPEN.**
Record this before the axis table, because it changes what `\phi_X` IS — and because the
previous version of this plan instructed the executor to write `**O3 — OPEN.**`, which is now a
false statement about the source.

Measure both sides, do not trust either description, and locate `Rule 13` by content:
```
grep -nF 'Rule 13 (Fee schedule and standing assumptions)' /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md
grep -nF 'Phi \, (\Theta_{\phi};' /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md
grep -nF 'Definition 18 (Dynamic fee schedule)' /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md
```

At plan time `Rule 13 @ 33af6a85` reads

    \phi_X (t) \; = \; \Phi \, (\Theta_{\phi}; \sigma (i (t)), \nu (t))

— **`\nu` IS an argument, and the volatility argument is `\sigma`, not `\sigma^2`.** At the
superseded pin `45aeba4c` the same rule read `\Phi(\Theta_{\phi}; \sigma^2(i(t)))`, with no `\nu`
and with `\sigma^2`. **Both arguments changed at `cf386de`.** `DOC Definition 18 (Dynamic fee
schedule)` reads
`\phi(\sigma(i(t));t) \equiv \bar\phi + \big(\sum_j \alpha_j/(1+\exp(\gamma_j(\beta_j - \sigma(i(t)))))\big)\cdot\alpha_R/(1+\exp(\gamma_R(\beta_R - \varphi_{(1/2,\,0)}(i_K;\Delta Q,0;t)/\varphi_{(1/2,\,0)}(i_K;0,L;t))))`
— a volatility surcharge TIMES a utilization gate, and that gate's argument is exactly `\nu`
(C-2). **`Rule 13` and `DOC` Definition 18 now agree on both arguments.**

Record verbatim:

```
**O3 — CLOSED AT SOURCE (`cf386de`).** `\phi_X` carries `\nu` (user-confirmed; `DOC`
Definition 18's gate factor), and `Rule 13 @ 33af6a85` now says so: it reads
`\phi_X(t) = \Phi(\Theta_{\phi}; \sigma(i(t)), \nu(t))`. At the superseded pin `45aeba4c` it read
`\Phi(\Theta_{\phi}; \sigma^2(i(t)))` and omitted `\nu` entirely, which is what O3 recorded.
**The source adopted the reading this project was going to flag.** O3 is therefore closed as a
signature defect, and **no artifact may still describe `Rule 13` as `\nu`-free.**

**The CONSEQUENCE survives O3's closure and is what actually matters downstream.** Because
`\phi_X` depends on `\nu`, `\partial\phi_X/\partial\tau_{\text{MEV}}` is not zero through the
schedule: `\phi_X` appears in `Proposition 13 (Corrected law) @ 33af6a85`'s numerator as
`(1-\phi_X)` AND, through `\nu`, inside the denominator's `\partial\phi/\partial\nu`. The law is
self-referential in `\phi_X` as well as in `\nu` — which is exactly what `Proposition 13`'s own
rider states ("*All of `\phi_X`, `\partial\phi/\partial\nu`, `\partial\nu/\partial\tau_{\text{MEV}}`
evaluated at `\nu(\tau^{\star}_{\text{MEV}})`*"). Carried to `HND-01` as a STANDING PROPERTY of
the law, not as a defect. `FINDING-REGISTER.md` CF-30 records the closure.

**O4 is NOT closed and its statement MOVED.** The `\sigma`/`\sigma^2` mismatch is now INTERNAL to
`SRC`: `Rule 13` takes `\sigma(i(t))` while `Definition 32 (Event-time plant)`'s `u_{\text{ex}}`
third slot carries `\sigma^2(i(t))`. Verify both in one pass:

    grep -nF 'Phi \, (\Theta_{\phi};' notes/VOLATILITY_INTRUMENTS_MEV.md
    grep -nF 'sigma^2 \, (i (t))' notes/VOLATILITY_INTRUMENTS_MEV.md

`> Authoritative detail: UNITS-LEDGER-EXTENSION.md §2b` (`NOT-05`, plan 01-05) — that is the
artifact that settles O4. This map records the mismatch and does not resolve it.
```

**Source-side confirmation (revised 2026-08-09).** `SRC @ 33af6a85` carries the plant in
numbered blocks; the vol-axis quantity appears in `Definition 32 (Event-time plant)`'s
`\Theta_{\sigma}` slot — locate it with `grep -nF 'Theta_{\sigma} = \begin{bmatrix}'`. Quote that
display and confirm which glyph it uses before writing the table below. **Do not use the old
`sed -n '37p'` probe** — that line number was valid at `45aeba4c` only.

**The glyphs (R1, binding).**

| Axis | Glyph | Meaning | Normative source | Source-side carrier |
|---|---|---|---|---|
| Volatility | `L_{\sigma}` `\equiv` `\Delta Q_v^{\star}` | the STARRED, STORED target vega — RAW LIQUIDITY units, the Uniswap `L` dimension | `UNITS_AND_SCALES.md:70 @ a46ba298` | **`Convention 8 (Liquidity axis) @ 33af6a85`**, tagged `(volatility axis)` |
| Price | `L`, `\bar L` | pool liquidity on the underlying's price tick | `UNITS_AND_SCALES.md` §3 (a SECTION reference, not a line citation — Check A does not apply) | **`Convention 8 @ 33af6a85`**, tagged `(price axis)` |
| Neither (a sensitivity) | `\Delta Q_v` (UNSTARRED) | collateral base units per Algebra vol unit; the lens readout; never stored | `UNITS_AND_SCALES.md:72 @ a46ba298` | **none — absent from `SRC`; `grep -cF 'Delta Q_v}'` confirms** |

`UNITS_AND_SCALES.md:70` is normative and reads:

> <quote line 70's unit cell verbatim, beginning "RAW LIQUIDITY units — the Uniswap L
> dimension">

(cited `UNITS_AND_SCALES.md:70 @ a46ba298` — Check A requires the pin)

Pricing a volatility level assigns it a tick; liquidity on the volatility tick is a DIFFERENT
object from liquidity on the price tick. **The "L overload" is two assets, not one ambiguous
symbol.**
```

Then `### 2.1 — Reconciliation with `UNITS_AND_SCALES.md:114` (READ THIS BEFORE CITING §2)`.
This is mandatory: a reviewer reading `:114` in isolation concluded the two-axis ruling might be
false and the `τ*=1` route alive. Quote the row and defuse it:

```
`UNITS_AND_SCALES.md` §4's conversion-chain table carries, at line 114, a row whose FIRST CELL
reads:

> ΔQ_v★ → the position (mint SIZING, dimension (ii))

and whose integer form is `L(i_K) = ΔQ_v★ · ℓ(ξ⋆, ι; i_K)` per leg, with `Σ L(i_K) = ΔQ_v★`
exact by `Σℓ = 1`.

**This does NOT compete with `L(i_K) = \bar L · \ell`. It corroborates the two-axis ruling.**
The row is labelled **`mint SIZING`** — it is the chain that sizes a MINT on the VOLATILITY
axis, where the aggregate is `L_{\sigma} = \Delta Q_v^{\star}` by R1. Read with the axis
labels attached, the two statements are:

    vol axis:    L_{\sigma}(i_K) = L_{\sigma} · \ell(\xi^{\star}, \iota; i_K),   \sum_{i_K} L_{\sigma}(i_K) = L_{\sigma} = \Delta Q_v^{\star}
    price axis:  L(i_K)          = \bar L      · \ell(\xi, \iota; i_K)

— the SAME geometric kernel `\ell` applied to TWO different aggregates on TWO different axes.
That is precisely the ruling's content. `PROJECT.md`'s R1 says so in terms: the `:114` chain is
"labelled **mint SIZING** and is therefore the vol axis, corroborating the two-axis ruling
rather than competing with it."

**Consequence for the dead routes:** because `:114` is a VOL-axis identity, it constrains
nothing on the price axis, exactly as Rule 9 does not. It cannot revive the
`\tau^{\star}_{\text{MEV}} = 1` route and cannot revive `\partial L(i_K)/\partial\pi^{\phi}
\equiv 0`. A later artifact citing `:114` against the price axis is making an axis error.
```

Then the consequences block:

```
**Consequences, binding.**
1. **Rule 9 is an identity on the VOL axis and constrains NOTHING on the price axis.**
2. The `\tau^{\star}_{\text{MEV}} = 1` refutation route (Rule 9 zeroing the bracket) is **DEAD**
   — marked SUPERSEDED in the finding register and never carried forward.
3. The claim `\partial L(i_K)/\partial\pi^{\phi} \equiv 0` ("channel death") is **DEAD**. Also
   SUPERSEDED.
4. `\partial \bar L/\partial \pi^{\phi} > 0` is **BEHAVIORAL** — an LP-supply estimand observed
   from add/remove-liquidity events, not a theorem. A named typed hypothesis (`PRF-03`),
   **NEVER sent to Aristotle**. Same status for
   `\bar{\mathcal{G}}_{(\nu,\lambda_{\text{MEV}})} := \partial\nu/\partial\lambda_{\text{MEV}}`.
   Magnitudes are `EST-01` (v2).

**AXIS-NAMING RULE (binding on every later artifact).** The rule covers FOUR symbol forms, not
one — the channel factor `\partial L(i_K)/\partial\pi^{\phi}` contains two of them:

| Form | Vol axis | Price axis | Bare form permitted? |
|---|---|---|---|
| aggregate | `L_{\sigma}` (`= \Delta Q_v^{\star}`) | `\bar L` | no |
| per-strike | `L_{\sigma}(i_K)` | `L(i_K)` | no |
| geometric kernel | `\ell(\xi^{\star}, \iota; i_K)` | `\ell(\xi, \iota; i_K)` | no — the `\xi` argument carries the axis and must be written |
| sensitivity (neither axis) | — | — | `\Delta Q_v` only, and only as the lens readout |

A bare `L`, `\bar L`, `L(i_K)` or `\ell` with its arguments suppressed is permitted ONLY inside
a verbatim quotation of a source, and every such quotation is immediately followed by a
parenthetical naming the axis, e.g. `(axis: price)`.
```

Then `## 3. `\partial\phi/\partial\nu` is a determinate, strictly positive factor (NOT-09)`.

Quote the boxed form from `ENTRY_POINT.md:15-60 @ db70caf2` verbatim in a fenced block. Then:

```
**Classification: DETERMINATE.** Every quantity in the boxed form is a schedule parameter
(`\alpha_R, \gamma_R, \beta_R`, the families `\alpha_j, \gamma_j, \beta_j`) or a trading-function
evaluation (`\varphi_{(1/2,0)}(i_K;\Delta Q,0;t)` over `\varphi_{(1/2,0)}(i_K;0,L;t)` — the
utilization ratio, `\nu` itself). Nothing in it is an estimand. It therefore enters the
channel's factor list on a DIFFERENT footing from `\partial \bar L/\partial\pi^{\phi}` and
`\bar{\mathcal G}_{(\nu,\lambda_{\text{MEV}})}`, which are estimands, and must never be grouped
with them.

**Sign: STRICTLY POSITIVE under the following conditions, declared here AS CONDITIONS and not
asserted as facts.**

| Label | Condition | Status |
|---|---|---|
| (S1) | `\alpha_R > 0` | OPEN — no normative artifact fixes it |
| (S2) | `\gamma_R > 0` | OPEN — no normative artifact fixes it |
| (S3) | `\alpha_j \ge 0` for all `j`, with `\alpha_j > 0` for at least one `j` | OPEN — no normative artifact fixes it |

Under (S1)-(S3): the trailing quotient `\exp(z)/[1+\exp(z)]^2` is the logistic derivative shape,
strictly positive for every real `z`; the bracketed sum
`\sum_j \alpha_j/(1+\exp(\gamma_j(\beta_j-\sigma)))` is a sum of non-negative terms with at
least one strictly positive; and `\alpha_R\gamma_R > 0`. The product is strictly positive.

**Position in the channel.** In the stated chain
`\partial\widehat\pi^\sigma/\partial\tau_{\text{MEV}} = (\partial\widehat\pi^\sigma/\partial L_{\sigma})(\partial L/\partial\pi^{\phi})(\partial\pi^{\phi}/\partial\phi)(\partial\phi/\partial\nu)(\partial\nu/\partial\tau_{\text{MEV}})`
this is the FOURTH factor. Recording it as determinate and strictly positive settles the sign of
THAT ONE FACTOR and **settles nothing about the chain itself** — whether the chain is the only
path is obligation `PRF-02` (Phase 4), and this map takes no position on it. Note that the
chain as written mixes axes (`\partial\widehat\pi^\sigma/\partial L_{\sigma}` against
`\partial L/\partial\pi^{\phi}`); flagging that is `PRF-02`'s work, not this map's, but the
axis tags are applied here so Phase 4 inherits the question rather than the ambiguity.

**(S1)-(S3) are OPEN ITEMS**, carried to the gap register (`HND-01`) unless a normative
artifact fixes the signs first. A later artifact using the strict positivity MUST cite them by
label.
```
  </action>
  <acceptance_criteria>
    - `NOTATION-MAP.md` contains `STATUS: SETTLED by user ruling 2026-08-08`.
    - **§2 cites the source-side carrier:** it contains `Convention 8 (Liquidity axis) @ 33af6a85` at least twice, and the axis table has a `Source-side carrier` column.
    - It pastes the three `Convention 8` locating greps and their output, and quotes `Convention 8`'s displays verbatim.
    - It contains `**O3 — CLOSED AT SOURCE (`cf386de`).**` and `no artifact may still describe `Rule 13` as `\nu`-free`, and does NOT contain `**O3 — OPEN.**` or `is therefore INCOMPLETE as written`.
    - It contains `**O4 is NOT closed and its statement MOVED.**` and routes O4 to `UNITS-LEDGER-EXTENSION.md §2b`.
    - It quotes `Rule 13`'s CURRENT signature `\Phi \, (\Theta_{\phi}; \sigma (i (t)), \nu (t))` **and** its superseded one `\Phi(\Theta_{\phi}; \sigma^2(i(t)))`, the latter labelled `45aeba4c`.
    - It contains `L_{\sigma}` at least 6 times and `\Delta Q_v^{\star}` at least 4 times.
    - It contains `### 2.1` with a heading including `Reconciliation`, the literal string `mint SIZING` at least twice, and the literal string `corroborates the two-axis ruling`.
    - It contains `A later artifact citing `:114` against the price axis is making an axis error`.
    - It contains `AXIS-NAMING RULE` and a table containing all four forms: `L_{\sigma}(i_K)`, `L(i_K)`, `\ell(\xi^{\star}, \iota; i_K)`, `\ell(\xi, \iota; i_K)`.
    - It contains `RAW LIQUIDITY units` (the quoted normative text) and `UNITS_AND_SCALES.md:70`.
    - It contains `SUPERSEDED` at least twice and `NEVER sent to Aristotle`.
    - It contains `Classification: DETERMINATE`, all of `(S1)`, `(S2)`, `(S3)`, and `settles nothing about the chain itself`.
    - It does NOT contain `L^{\text{pool}}` or `L^{\text{vol}}`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md && for s in "STATUS: SETTLED by user ruling 2026-08-08" "### 2.1" "Reconciliation" "corroborates the two-axis ruling" 'A later artifact citing `:114` against the price axis is making an axis error' "AXIS-NAMING RULE" 'L_{\sigma}(i_K)' 'L(i_K)' '\ell(\xi^{\star}, \iota; i_K)' '\ell(\xi, \iota; i_K)' "RAW LIQUIDITY units" "UNITS_AND_SCALES.md:70" "NEVER sent to Aristotle" "Classification: DETERMINATE" "(S1)" "(S2)" "(S3)" "settles nothing about the chain itself" 'Source-side carrier' '**O3 — CLOSED AT SOURCE (`cf386de`).**' '**O4 is NOT closed and its statement MOVED.**' 'UNITS-LEDGER-EXTENSION.md §2b' '\Phi \, (\Theta_{\phi}; \sigma (i (t)), \nu (t))'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && for bad in '**O3 — OPEN.**' 'is therefore INCOMPLETE as written'; do grep -qF "$bad" $F && { echo "STALE O3 CLAIM: $bad"; exit 1; }; done; test $(grep -cF 'Convention 8 (Liquidity axis) @ 33af6a85' $F) -ge 2 && test $(grep -oF 'L_{\sigma}' $F | wc -l) -ge 6 && test $(grep -oF '\Delta Q_v^{\star}' $F | wc -l) -ge 4 && test $(grep -cF 'mint SIZING' $F) -ge 2 && test $(grep -cF 'SUPERSEDED' $F) -ge 2 && ! grep -qF 'L^{\text{pool}}' $F && ! grep -qF 'L^{\text{vol}}' $F && echo PASS</automated>
  </verify>
  <done>Two-axis liquidity is SETTLED under R1's `L_σ` glyph with the normative unit line quoted; §2.1 reconciles `UNITS_AND_SCALES.md:114` by quoting its own `mint SIZING` label and forecloses the axis-error misreading; the axis rule covers all four symbol forms including `L(i_K)` and `ℓ`; `∂φ/∂ν` is determinate and strictly positive under a citable `(S1)-(S3)` table.</done>
</task>

<task type="auto">
  <name>Task 3: Symbol register, banned glyphs, and a real NOT-06 enforcement check</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md` (§1-§3 name every symbol you must register)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` — **ALL 190 lines / all 14 numbered blocks.** `Definition 32` is the plant and the source of most registered symbols; `Convention 8`, `Theorem 29-31`, `Proposition 12-13`, `Hypothesis (H2)` and `Definition 36` are NEW at `cf386de` and every glyph they introduce needs a §4.6 row
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md` (Constraints — the binding notation rules you transcribe)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md` — locate Proposition 10 by content to confirm the `\chi_{X/M}` weight parameter exists and is legitimate
  </read_first>
  <action>
Append `## 4. Symbol register (NOT-06)`.

```
**Enforcement rule.** No symbol appears in ANY artifact this project writes unless it is either
(a) present in a pinned source with the same meaning, or (b) listed in §4.2 with a stated
reason AND a recorded user ruling. A symbol in neither is a defect and is removed, not
grandfathered. **This project does not mint glyphs autonomously** — §4.2's `Authority` column
must name a ruling, never a plan.
```

`### 4.1 — Inherited symbols` — table `Symbol | Meaning | Source (cited per INVENTORY.md §5) |
Notes`. At minimum, each with a citation you confirm by opening the file:

`\phi`, `\varphi`, `\phi_M`, `\phi_X`, `\bar\phi_M`, `\Theta_{\phi}`, `\beta_j`, `\gamma_j`,
`\alpha_j`, `\alpha_R`, `\beta_R`, `\gamma_R`, `\nu`, `\tau_{\text{MEV}}`,
`\lambda_{\text{MEV}}`, `\pi^{\phi}`, `\pi^{\varphi}`, `\pi^{\mathrm{LVR}}`, `\pi^{\sigma}`,
`\widehat\pi^{\sigma}`, `\pi^{l}`, `x`, `y`, `u_{\text{ex}}`, `u_{\text{en}}`, `\Delta Q_M`,
`\Delta Q_X`, `\Delta Q_v^{\star}`, `\Delta Q_v`, `L_{\sigma}`, `L`, `\bar L`, `L(i_K)`,
`\ell`, `\sigma^2(i(t))`, `\sigma_K^2`, `\#_{\sigma}`, `\iota`, `\xi`, `s_{\upsilon}`, `i_K`,
`p_{(\eta,\Delta_i)}`, `p_{\varphi}`, `\Delta t`, `\Theta_{\sigma}`,
`\mathcal{I}_{\text{ord}}`, `\bar{\mathcal{G}}_{(\nu,\lambda_{\text{MEV}})}`, `e^{\sigma}`,
`\phi_{\text{total}}`, `\varphi_{(1/2,\,0)}`, `Q_X^L`, `Q_M^L`, `\sigma(i_K;\Theta_{\sigma})`.

**`\phi_{\text{total}}` is the one that must not be missed.** It appears in this project's own
CF-01 and CF-03 headlines and in `ROADMAP.md` Phase 4 Success Criterion 2, but
**`grep -cF 'phi_{\text{total}}' notes/VOLATILITY_INTRUMENTS_MEV.md` returns 0 — it is absent
from `SRC` entirely.** Its only pinned homes are the unnumbered monoid displays
`\phi_{\text{total}} \leftarrow \phi_M \otimes_{\phi} \phi_X \otimes_{\phi} \tau_{\text{MEV}}`
at `DOC:1053` and `TEX:1036` — cite per `INVENTORY.md` §5 Rule 2's second branch (line + pin +
nearest enclosing numbered item). Verify all three counts yourself. Without this row, §4's
enforcement rule instructs a Phase 2 agent to DELETE the symbol the Phase 4 branch gate turns
on.

Notes cells that MUST be present: `\#_{\sigma}` — "= `\iota` (`TEX:212`), same object, two
documents"; `\pi^{\varphi}` — "`DOC` Definition 25's object per R2 — NOT the source-side
composite once abbreviated at `SRC:28 @ 0ec30184` and struck at `c521af5`; see C-1"; `L_{\sigma}` — "≡ `\Delta Q_v^{\star}` per R1; vol axis"; `\bar L` — "price axis;
bare use is quotation-only per §2's AXIS-NAMING RULE"; `\Delta Q_v` — "UNSTARRED: the lens
readout sensitivity, `UNITS_AND_SCALES.md:72`. Not a liquidity."

`### 4.2 — MINTED symbols` — table `Symbol | Meaning | Reason | Authority (ruling + commit) |
Where used`. Under R1/R2 this table has AT MOST ONE row: the glyph the user ruled at
**Under the 2026-08-08 re-triage this table is EMPTY.** The composite's glyph question was
dissolved at `c521af5`, not answered by a mint: `SRC` no longer uses `\pi^{\varphi}` and writes
`\pi^{\phi} - \pi^{\text{LVR}}` out in full inside `Definition 32`. Write exactly:
`No symbol was minted by this project. The composite is written out in full at the source
(commit \`c521af5\`, \`Definition 32\`); see C-1.` If a later ruling DOES mint something, the
row's `Authority` cell must name that ruling's date or commit sha — never a plan.

State explicitly, verbatim: `Two glyphs an earlier draft of this plan proposed —
`\Pi^{\text{pv}}` and the `L^{\text{pool}}`/`L^{\text{vol}}` superscript tags — were
autonomous mints and are DELETED. `\Pi^{\text{pv}}` renamed the wrong side of the C-1
collision; the superscript tags are superseded by R1's `L_{\sigma}` subscript, which draws both
letters from the doc's own symbols.`

`### 4.3 — BANNED glyphs` — table `Banned | Reason | Use instead`:
- `\pi^{\tilde\phi}` (the `π^φ̃` mint) — a self-inflicted notation violation in an earlier
  planning artifact, corrected at commit `fcb6d10`. Use `\pi^{\phi}` or C-1's ruled glyph.
- `\Pi^{\text{pv}}` — BANNED: an autonomous mint that renamed the wrong side. Use instead:
  `\pi^{\varphi}` (which `DOC` keeps) and, for the composite, the full form
  `\pi^{\phi} - \pi^{\text{LVR}}` as `Definition 32` writes it.
  **Every §4.3 row must literally contain `BANNED` or `Use instead:`** — the deleted-glyph
  check in Task 1 requires it, and a row reading only "Use `X`" fails it.
- `L^{\text{pool}}`, `L^{\text{vol}}` — BANNED: superseded by R1. Use instead: `\bar L` /
  `L_{\sigma}`.
- `\chi` **as curvature** — curvature is `\kappa_{\varphi}`. **Carve-out:** `\chi_{X/M}` is a
  LEGITIMATE inherited symbol (the CFMM weight parameter, `\chi_{X/M} = 1/2`, in `DOC`'s
  Proposition 10 — locate and cite it). The ban covers `\chi` for curvature only.
- `\Delta Q_{\upsilon}` — retired by C-4. Use `\Delta Q_v^{\star}`; permitted only inside a
  verbatim quotation of the pre-`c521af5` source (`SRC:23 @ 0ec30184`), always followed by
  `(= \Delta Q_v^{\star})`. **It is absent from `SRC` at `33af6a85`** — `grep -cF` returns 0.
- bare `L` / `\bar L` / `L(i_K)` / `\ell` outside a quotation — use the §2 axis table.
- `u` for the utilization gate value — use `\nu` (C-2). **Carve-out:** `u` remains legitimate
  as the plant's input letter in `u_{\text{ex}}` / `u_{\text{en}}`.

`### 4.4 — Standing notation constraints carried from PROJECT.md`, verbatim:

```
- `\phi` (fee) and `\varphi` (quote/trading function) are DISTINCT and must never be collapsed
  or decorated into one another.
- Curvature is `\kappa_{\varphi}` — never `\chi` (see §4.3's `\chi_{X/M}` carve-out).
- Tilde `\tilde\lambda` denotes the INCIDENCE OPERATOR; plain `\lambda` denotes a HAZARD.
- Probabilities are written `\mathbb{P}_{\text{event}}`, e.g. `\mathbb{P}_{\Delta_{\text{ARB}}}`.
- These constraints bind Aristotle prompts exactly as they bind documents.
```

`### 4.6 — RETROACTIVE reconciliation of symbols minted before this register existed`.

**This is a burden `NOT-06` creates and the roadmap re-baseline made explicit, and it GREW at
`cf386de`.** Two waves of source blocks were minted before this register existed:

- **Wave 1 (commit `c521af5`)** — `Convention 7`, `Definitions 32-35`, `Rule 13`.
- **Wave 2 (commit `cf386de`)** — **eight more blocks**: `Convention 8 (Liquidity axis)`,
  `Theorem 29 (The monoid path is direct)`, `Theorem 30 (Path decomposition)`,
  `Proposition 12 (Kernel differentiation)`, `Theorem 31 (The section sum is ill-posed)`,
  `Hypothesis (H2)`, `Definition 36 (Tax program)`, `Proposition 13 (Corrected law)`. The same
  commit also **CHANGED `Rule 13`'s signature** (see §2's `INV-C1` / O3 record) and **DELETED**
  the whole `\pi^{\phi}` algebra region and the boxed `\tau^{\star}`.

Plus **every Lean identifier across blocks M11-M24 in the two returned Aristotle bundles**. All
were minted BEFORE `NOTATION-MAP.md` existed. `NOT-06` forbids exactly that. They cannot be
un-minted, so they are reconciled retroactively and the irregularity is recorded rather than
hidden. **Enumerate the source blocks by measurement, not from this list:**

```
grep -nE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' notes/VOLATILITY_INTRUMENTS_MEV.md
```

Expect **14** blocks at `33af6a85`. Every one gets a row. **Glyphs that entered `SRC` for the
first time in wave 2 need particular attention** — at minimum `L_{\sigma}`, `\ell`, `\xi^{\star}`,
`\iota`, `\kappa`-free curvature usage, `\bar{\mathcal{G}}_{(\nu,\lambda_{\text{MEV}})}`,
`\pi^{l}`, and the `(A1)` label in `Proposition 12`. Note that `L_{\sigma}` entering the SOURCE
is a *ratification* of ruling R1, not a fresh mint — record it that way.

Build a table `Symbol / identifier | Origin | Introduced by | Disposition`:

- **Origin** ∈ {`SOURCE` (`SRC` block), `BUNDLE` (a returned `.lean`), `USER RULING`,
  `PEER DOC`}.
- **Introduced by** — the commit or bundle, e.g. `c521af5`, `MevTaxControl.lean` (M11-M18),
  `MevTaxProgram.lean` (M19-M24).
- **Disposition** ∈ {`REGISTERED §4.1` (inherited, already in a pinned source),
  `REGISTERED §4.2` (a genuine mint — record the reason and note that it PRECEDED the
  register), `RENAME REQUIRED` (collides with a registered symbol — say what it must become),
  `LEAN-ONLY` (an identifier that never enters a document, so `NOT-06` does not bind it —
  state that explicitly)}.

Harvest the identifiers rather than listing them from memory:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
grep -ohE '^(theorem|lemma|def|noncomputable def) [A-Za-z0-9_]+' control/aristotle/tax-result/project_aristotle/RequestProject/MevTaxControl.lean control/aristotle/tax2-result/project_aristotle/RequestProject/MevTaxProgram.lean | awk '{print $NF}' | sort -u
grep -oE '\\[A-Za-z]+' notes/VOLATILITY_INTRUMENTS_MEV.md | sort -u
```

Every harvested item gets a row. **The `LEAN-ONLY` disposition is the expected majority** —
`NOT-06` governs symbols in DOCUMENTS, and a Lean declaration name is not a document symbol.
Say so once, in the table's preamble, so the table does not read as 90 violations.

**What must NOT be recorded as fine:** any glyph appearing in a `SRC` block that is not in
§4.1/§4.2. Those are real `NOT-06` breaches and each needs a disposition, not an exemption.

`### 4.5 — NOT-06 self-check (run it, record the output)`. The register must satisfy its own
rule. Run and paste:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX
# every backslash-glyph used in this document's own prose
grep -oE '\\[A-Za-z]+(\^|_)?\{?[A-Za-z\\{}^_ ,;()\\\\-]*\}?' NOTATION-MAP.md | sort -u > /tmp/used.txt
wc -l /tmp/used.txt
```

Then, for each distinct symbol in that list, confirm by inspection that it appears in §4.1,
§4.2 or §4.3, and record any that do not as a table `Unregistered glyph | Disposition
(register / remove)`. Resolve every one before finishing — the disposition column may not
contain `TBD`.

**Citation-form check (this is a real check, not a recording step).** Run and paste:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX
# Standing Check A (INVENTORY.md §10.1) — EVERY line citation, not just SRC's
grep -oE '(SRC|UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' NOTATION-MAP.md | sort -u
# any that lack the pin:
grep -oE '(SRC|UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' NOTATION-MAP.md | grep -v ' @ ' | sort -u
# Standing Check B — no LIVE line citation of SRC survives (INVENTORY.md §10.1, revised at cf386de)
grep -oE 'SRC:[0-9]+(-[0-9]+)?' NOTATION-MAP.md | grep -vE '^SRC:(2|4-10)$' | grep -v ' '
# Standing Check B' — struck loci are annotated where they appear at all
grep -nE 'SRC:(111|113|115|123|209|226)' NOTATION-MAP.md | grep -vE 'was |STRUCK|0ec30184|45aeba4c'
```

**The second, third and fourth commands MUST return nothing.** Record all three outputs. This is why
§2, §3 and §4.1 must write `UNITS_AND_SCALES.md:70 @ a46ba298`, `:72 @ a46ba298`,
`ENTRY_POINT.md:15-60 @ db70caf2`, `TEX:212 @ c8cbd3c0` and so on — an unpinned line citation
of ANY artifact fails Check A, not just an unpinned `SRC:`.

**Glyph-form consistency (`m-4`).** `L_σ` (Unicode) and `L_{\sigma}` (LaTeX) denote the same
object. **Normative statements use the LaTeX form**; the Unicode form is permitted only in
prose headings and running text. Record the counts of each and confirm no §4 table cell uses
the Unicode form — §4.5's backslash-based extractor cannot see it, so an unregistered Unicode
glyph would slip the self-check.

**Two-step review, THEN commit.** Run Reality Checker plus one named specialist in parallel
over `NOTATION-MAP.md`, resolve BLOCKER/MAJOR findings, record the review date. Then:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
git add spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md
git commit -m "docs(01): notation map — collisions, L_sigma two-axis SETTLED, symbol register

Closes NOT-02, NOT-06, NOT-07, NOT-09. Four collisions resolved with the
rejected reading stated and an authority named for each (rulings R1/R2 at
871e387 plus the 01-02 checkpoint). Two-axis liquidity SETTLED under
L_sigma = DeltaQ_v-star; UNITS_AND_SCALES.md:114 reconciled via its own
'mint SIZING' label. d-phi/d-nu determinate and strictly positive under
(S1)-(S3). No glyph minted autonomously.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

Stage only that path. Never `git add -A`.
  </action>
  <acceptance_criteria>
    - `NOTATION-MAP.md` contains headings `### 4.1`, `### 4.2`, `### 4.3`, `### 4.4`, `### 4.5`, `### 4.6`.
    - §4.6 contains `RETROACTIVE`, `LEAN-ONLY`, `RENAME REQUIRED`, the pasted identifier harvest, and names `c521af5`, `MevTaxControl.lean` and `MevTaxProgram.lean`.
    - It contains `Enforcement rule.`, `is a defect and is removed, not grandfathered`, and `This project does not mint glyphs autonomously`.
    - **M3 — claim hygiene on the struck derivative, not merely citation hygiene.** The struck LHS `\partial\pi^{\phi}/\partial\phi` must NEVER appear on a line that also contains `=`: `grep -E 'pi\^\{.?phi\}(\}\{.?partial|/.?partial)' <artifact> | grep -c '='` returns **0** (both the `\frac{}{}` and the `/` spellings). The expression `\partial\phi_M/\partial\phi\,\Delta Q_M + p_{(\eta,\Delta_i)}\,\partial\phi_X/\partial\phi\,\Delta Q_X` may be quoted ONLY as `Theorem 31 @ 33af6a85`'s no-section-independent-value object. **A `was `/`STRUCK` annotation is citation hygiene and does NOT license asserting the derivative** — that is the claim inversion the re-pin exists to prevent. (The address history in `INVENTORY.md` §8 is the one place the old equation may be shown; it is exempt, and it owns that record.)
    - §4.1's table has ≥ 50 DATA rows: `awk` between `### 4.1` and `### 4.2`, count lines matching `^|`, exclude separator rows matching `^\|[- :|]*$`, subtract the 1 header row. (Counting `'^| '` is separator-style-dependent and gives different answers for `|---|` vs `| --- |`.)
    - §4.1 contains all of: `\widehat\pi^{\sigma}`, `\pi^{l}`, `u_{\text{en}}`, `\Delta Q_v^{\star}`, `\bar{\mathcal{G}}_{(\nu,\lambda_{\text{MEV}})}`, `\iota`, `L_{\sigma}`, `\ell`, `\phi_{\text{total}}`, `\varphi_{(1/2,\,0)}`, `Q_X^L`, `Q_M^L`, `\sigma(i_K;\Theta_{\sigma})`.
    - The `\phi_{\text{total}}` row cites `DOC:1053` or `TEX:1036` and states it is absent from `SRC`.
    - §4.2 contains EITHER the literal string `No symbol was minted by this project`, OR at least one DATA row (excluding header and separator) whose text contains a commit sha (`[0-9a-f]{7,40}`) or an ISO date (`20NN-MM-DD`). **Matching the bare word `Authority` is not sufficient — that is the column header and is unconditionally present.**
    - §4.3 contains `\Pi^{\text{pv}}`, `L^{\text{pool}}`, `\chi_{X/M}`, `\Delta Q_{\upsilon}`, and `remains legitimate as the plant's input letter`.
    - §4.4 contains `\kappa_{\varphi}`, `\tilde\lambda`, `\mathbb{P}_{\text{event}}`, `bind Aristotle prompts`.
    - §4.5 contains the pasted output of ALL THREE commands, the unregistered-glyph table contains no `TBD`, and both the "lacking a pin" and the "lacking an ERRATUM tag" outputs are empty.
    - The file does NOT contain the string `PEER-ADJACENT`.
    - No §4 table cell contains the Unicode `L_σ` form (normative statements use `L_{\sigma}`).
    - **Live re-run of Standing Check A:** `grep -oE '(SRC|UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' NOTATION-MAP.md | grep -v ' @ '` returns nothing.
    - **Live re-run of Standing Check B:** every `SRC:NNN` occurrence in `NOTATION-MAP.md` is `SRC:2`, `SRC:4-10`, or carries `was ` / `STRUCK` / `0ec30184` / `45aeba4c` on the same line — i.e. it is address-history, not a live citation. **`SRC` is cited BY NUMBERED BLOCK.**
    - `NOTATION-MAP.md` is ≥ 160 lines; `git log -1 --name-only` lists it and no `.planning/` path; live plank porcelain diffs clean against the sentinel baseline.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md && test $(wc -l < $F) -ge 160 && for s in '### 4.1' '### 4.2' '### 4.3' '### 4.4' '### 4.5' '### 4.6' 'RETROACTIVE' 'LEAN-ONLY' 'RENAME REQUIRED' 'MevTaxControl.lean' 'MevTaxProgram.lean' 'Convention 8' 'Theorem 29' 'Theorem 30' 'Theorem 31' 'Proposition 12' 'Proposition 13' 'Hypothesis (H2)' 'Definition 36' 'Enforcement rule.' 'is a defect and is removed, not grandfathered' 'This project does not mint glyphs autonomously' '\widehat\pi^{\sigma}' '\pi^{l}' 'u_{\text{en}}' '\bar{\mathcal{G}}_{(\nu,\lambda_{\text{MEV}})}' '\iota' 'L_{\sigma}' '\ell' '\Pi^{\text{pv}}' 'L^{\text{pool}}' '\chi_{X/M}' '\Delta Q_{\upsilon}' "remains legitimate as the plant's input letter" '\kappa_{\varphi}' '\tilde\lambda' '\mathbb{P}_{\text{event}}' 'bind Aristotle prompts'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && { awk '/^### 4\.2/,/^### 4\.3/' $F | grep -qF 'No symbol was minted by this project' || awk '/^### 4\.2/,/^### 4\.3/' $F | grep '^|' | grep -vE '^\|[- :|]*$' | tail -n +2 | grep -qE '[0-9a-f]{7,40}|20[0-9]{2}-[0-9]{2}-[0-9]{2}'; } && test $(awk '/^### 4\.5/,0' $F | grep -cF 'TBD') -eq 0 && test -z "$(grep -oE '(SRC|UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' $F | grep -v ' @ ')" && test $(grep -E 'SRC:[0-9]+' $F | grep -vE 'SRC:(2|4-10)([^0-9-]|$)' | grep -cvE 'was |STRUCK|0ec30184|45aeba4c') -eq 0 && test $(grep -E 'pi\^\{.?phi\}(\}\{.?partial|/.?partial)' $F | grep -c '=') -eq 0 && ! grep -qF 'PEER-ADJACENT' $F && test $(awk '/^### 4\.1/,/^### 4\.2/' $F | grep -c '^|') -ge 52 && for g in '\phi_{\text{total}}' '\varphi_{(1/2,\,0)}' 'Q_X^L' 'Q_M^L'; do grep -qF -- "$g" $F || { echo "MISSING SYMBOL: $g"; exit 1; }; done && git log -1 --name-only --format="" | grep -q 'NOTATION-MAP.md' && ! git log -1 --name-only --format="" | grep -q '^\.planning/' && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && echo PASS</automated>
  </verify>
  <done>The symbol register lists every inherited symbol with a pinned source, mints nothing without a named ruling, bans the two autonomously-minted glyphs by name, and passes its own NOT-06 self-check and a live citation-form check that fails if any `SRC:` citation lacks its 8-hex pin.</done>
</task>

</tasks>

<verification>
1. Every citation resolves at its measured locus against the §2 pin, and every `SRC:` citation
   carries an 8-hex pin (the §4.5 check, re-run live).
2. `NOT-06` self-check: every backslash-glyph in the document's own prose appears in §4.1,
   §4.2 or §4.3.
3. Deleted glyphs appear ONLY in §4.3: `grep -n '\\Pi^{\\text{pv}}'` and
   `grep -n 'L^{\\text{pool}}'` return hits only inside §4.3.
4. `UNITS_AND_SCALES.md:114` is reconciled before §2's foreclosure is relied on — §2.1 precedes
   the "Consequences, binding" block in document order.
5. Peer non-interference: live plank porcelain diffs clean against `INVENTORY.md`'s sentinel
   block; `git -C WT status --porcelain .planning/` shows only the two pre-existing untracked
   dirs.
</verification>

<success_criteria>
- `NOT-02` closed: four collisions, each with chosen AND rejected reading and a named authority.
- `NOT-06` closed and ENFORCEABLE: a symbol register, a no-autonomous-minting rule, a self-check
  that ran, and a citation-form check that can fail.
- `NOT-07` closed: two-axis liquidity SETTLED under `L_σ`, the `UNITS_AND_SCALES.md:114`
  misreading foreclosed by quoting its own `mint SIZING` label, the axis rule covering
  `L(i_K)` and `ℓ`, and the two dead routes marked SUPERSEDED.
- `NOT-09` closed: `∂φ/∂ν` determinate and strictly positive under a citable `(S1)-(S3)` table,
  positioned as the fourth factor, taking no position on whether the chain is the only path.
</success_criteria>

<output>
After completion, create
`/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/phases/01-ground-truth-notation-and-the-rulings-triage/01-03-SUMMARY.md`
</output>
