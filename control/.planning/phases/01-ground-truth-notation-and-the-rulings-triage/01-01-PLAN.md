---
phase: 01-ground-truth-notation-and-the-rulings-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md
  - spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md
autonomous: true
requirements: [NOT-08]

must_haves:
  truths:
    - "A reader can name every normative artifact this project must obey, each with a content sha and a one-line statement of what it is normative for."
    - "ENTRY_POINT.md is git-tracked and can no longer be destroyed by a stray clean."
    - "Every later citation has exactly one legal form, and a pin that moves has a written re-pin procedure rather than silent drift."
    - "The record states that VOLATILITY_INSTRUMENTS_MEV.tex is NOT a superset of SRC, on evidence produced by a command that cannot silently emit a false zero."
    - "The plank worktree's pre-phase git state is captured between sentinels before any artifact is written, so 'unchanged by this phase' is machine-checkable."
    - "Every known SRC erratum is on the record with its CLOSURE — all three were fixed at source — and the one live consequence of the cf386de restructure, Rule 13's changed signature, is recorded as a correction with O3 closed and O4 re-located."
  artifacts:
    - path: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md"
      provides: "Sweep, pin register, citation protocol, re-pin procedure, SRC errata, artifact-ownership table, plank baseline"
      min_lines: 130
      contains: "UNITS_AND_SCALES.md"
    - path: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md"
      provides: "The boxed ∂φ/∂ν — now git-tracked"
      contains: "\\frac{\\partial\\phi}{\\partial\\nu}"
  key_links:
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md"
      to: "plank/notes/UNITS_AND_SCALES.md"
      via: "content sha pin + normative-for line"
      pattern: "a46ba298"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md"
      to: "plank worktree git state"
      via: "sentinel-delimited baseline block"
      pattern: "PLANK-BASELINE"
    - from: "git index"
      to: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md"
      via: "git add — file becomes tracked"
      pattern: "git ls-files"
---

<objective>
Produce the inventory sweep — the FIRST artifact this project writes — that names every
normative on-disk artifact, pins each by content sha, states what each is normative *for*,
fixes exactly one legal citation form (BY NUMBERED BLOCK — `SRC` is now 14 blocks and nothing
else) with a re-pin procedure behind it, and records the SRC errata as CLOSED-AT-SOURCE together
with the `Rule 13` signature correction that closes open item O3 and re-locates O4.

Purpose: `NOT-08`. Nothing else this project writes may cite a source that has not been pinned
here. This artifact must be dated before every other artifact this project produces.

Output: `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md`, plus
`spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md` moved from untracked to git-tracked.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/STATE.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/CLAUDE.md

**Path glossary used by every plan in this phase.** `WT` =
`/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller` (this worktree; `files_modified` paths
are relative to it). `PLANK` = `/home/jmsbpp/cfmms-playground/cfmm-wt/plank` (PEER-OWNED,
READ-ONLY). `LEAN` = `/home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec` (PEER-OWNED,
READ-ONLY). `SPECDIR` = `WT/spec/VOLATILITY_INSTRUMENTS_MEV_TAX`.

**OWNERSHIP — read this before writing any "peer-owned" claim.** `WT/CLAUDE.md` assigns
`ul2inqpl` exactly: `src/`, `script/`, `foundry.toml`, `remappings.txt`, and Plank `.plk`.
It does **not** assign `evm-controller/notes/`. Therefore
`WT/notes/VOLATILITY_INTRUMENTS_MEV.md` (`SRC`) is **THIS PROJECT'S OWN DOCUMENT**. The string
`PEER-ADJACENT` must never be used to describe it.

**Hard prohibitions, in force for every task.**
- Do NOT create, modify or delete anything under `PLANK/` or `LEAN/`.
- Do NOT create, modify or delete anything under `WT/.planning/` (the repo-root planning dir
  is a DIFFERENT, unrelated GSD project). This project's planning dir is
  `WT/control/.planning/` only.
- Do NOT edit `SRC`. It is this project's own file, but it is the pinned citation target:
  editing it invalidates every `SRC:NNN` pin issued before the edit.
- Do NOT produce any `.plk` or `.sol` artifact, and attach no on-chain cost claim to anything.

**MEASUREMENT DISCIPLINE (standing).** Three separate errors in this project's history came
from carrying a hardcoded count that had drifted. Every numeric fact in this plan is stated
with the command that produces it. **Run the command, record ITS output, and if your measured
value differs from the plan-time value quoted here, record BOTH and note the drift.** Never
copy a plan-time number into an artifact without re-measuring.

**`grep` IS A SHELL FUNCTION IN THIS ENVIRONMENT** that dispatches to `ugrep`. Patterns
containing `\h`, `\w`, `\p`, `\d` etc. are parsed as character classes and **error out**.
Verified: `grep -c -- '\widehat\pi' file` emits `ugrep: error: ... invalid character class`,
not a count. **Every literal-string search in this phase MUST use `-F`** (`grep -cF`,
`grep -qF`). A bare `grep -c` on a LaTeX pattern will produce an error message that an
inattentive executor may record as a zero.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the peer baseline, sweep the three directories, write the pin register</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md` (Phase 1, Success Criterion 1 — read it verbatim; it is the spec for this task)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md` (NOT-08)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/CLAUDE.md` (the ownership map — it decides every `Owner` cell you write)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/UNITS_AND_SCALES.md` (lines 55-80 at minimum — the vega-axis table quoted downstream)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md` (all 64 lines)
  </read_first>
  <action>
**STEP 0 — CAPTURE THE PEER BASELINE FIRST, BEFORE WRITING ANYTHING ELSE.** Every later
peer-safety check in this phase compares against this block, so it must be taken before this
phase has had any chance to touch the peer tree.

Create `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md`
whose FIRST content after the header is `## 7. Peer-worktree baseline`. (Section numbering is
by topic, not by writing order — §7 is written first deliberately.)

Run:
```
git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain
git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank rev-parse HEAD
git -C /home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec status --porcelain
```

Write the plank porcelain output into **ONE** fenced block delimited by HTML-comment
sentinels, in exactly this shape and with nothing else between the sentinels:

```
<!-- PLANK-BASELINE -->
​```
 M lib/bunni-v2
 M lib/panoptic-v2-core
 M lib/plankified-univ3
?? notes/DERIVED_VOLATILITY_INSTRUMENTS.md
?? notes/types/
​```
<!-- /PLANK-BASELINE -->
```

(Remove the zero-width characters before the backticks — they are here only so this plan can
show a fence inside a fence. Use your MEASURED output, not the five lines above, if they
differ; the five lines above are the plan-time value.)

The `lean4-spec` porcelain goes in a SEPARATE block delimited by `<!-- LEAN-BASELINE -->` /
`<!-- /LEAN-BASELINE -->`, same rules: one fenced block, nothing else between sentinels.

Then, outside the sentinels, state verbatim:

```
**The peer tree is NOT clean at baseline** — three modified submodule pointers and two
untracked paths, none of them this project's doing. The Phase 1 success criterion is therefore
**`git -C plank status --porcelain` is byte-identical to the sentinel-delimited block above**,
not "empty". Any later plan checking peer non-interference extracts the block between the
sentinels and requires `diff` to SUCCEED — a check that compares plank's status against itself
is a tautology and is not a check.

Extraction command (this exact form, used by every later plan):

    sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d'
```

**STEP 1 — the sweep.** Run all three and paste their raw output under `## 1. Sweep output`,
each preceded by the command that produced it:

```
ls -la /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/ /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/*/
ls -la /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/
ls -la /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/ /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/types/
```

**STEP 2 — the pins.** Use `git hash-object` (a blob hash pins content whether or not the file
is committed, and works on peer-owned files without touching them):

```
git -C /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller hash-object notes/VOLATILITY_INTRUMENTS_MEV.md spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md
git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank hash-object notes/VOLATILITY_INSTRUMENTS.md notes/UNITS_AND_SCALES.md notes/VOLATILITY_INSTRUMENTS_MEV.tex
wc -l /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/UNITS_AND_SCALES.md /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS_MEV.tex
```

Plan-time values (2026-08-08) — **re-measure and use YOUR values; record drift if any differs**:

| Alias | Path (absolute) | Pin @ plan time | Lines @ plan time |
|---|---|---|---|
| `SRC` | `WT/notes/VOLATILITY_INTRUMENTS_MEV.md` | `33af6a85a9eb7e17fa19b4f92a78ee413353196e` | 200 |
| `ENTRY_POINT` | `WT/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md` | `db70caf2244718a6b3d68e685174080f20b40fe2` | 64 |
| `DOC` | `PLANK/notes/VOLATILITY_INSTRUMENTS.md` | `0c5ce5744e146cd022620007d2af5b576c1bc8b8` | 1639 |
| `UNITS` | `PLANK/notes/UNITS_AND_SCALES.md` | `a46ba298d8b48fa851e817bf6f46e580b350878e` | 139 |
| `TEX` | `PLANK/notes/VOLATILITY_INSTRUMENTS_MEV.tex` | `c8cbd3c094d620280954fc3a737c5fbda599eae9` | 1063 |

Note in `DOC`'s row: **`PROJECT.md` describes `DOC` as 1636 lines; it measures 1639 today.**
Record the drift; it does not invalidate any numbered-item citation, only line-number ones —
which is exactly why §5 rule 2 exists.

Write `## 2. Pin register`: a table with columns
`Alias | Path | Lines (measured) | Pin (git hash-object) | Owner | Normative FOR`. `Normative
FOR` is exactly one line; use these verbatim:

- `SRC` — "The controller derivation under adjudication. Carries the plant (`x`, `u_ex`, `u_en`, `y`, `Θ_σ`), the `∇φ` display, the path decomposition, and the CORRECTED set-point law. THE citation target for every derivation step. **As of commit `cf386de` it is composed ENTIRELY of numbered blocks — 14 of them — plus a two-line preamble and the numbering note. Cite BY NUMBERED BLOCK plus pin; a line-only citation of `SRC` is now a defect, exactly as for `DOC`. The boxed `τ*_MEV` it once carried has been STRUCK and replaced by `Proposition 13 (Corrected law)`.**"
- `ENTRY_POINT` — "Normative for the boxed `∂φ/∂ν` only. Its boxed form is correct and is the determinate, strictly positive factor entering the channel's factor list (NOT-09)."
- `DOC` — "Normative for every numbered Definition / Rule / Theorem / Proposition the derivation leans on."
- `UNITS` — "NORMATIVE units table. Binding on every unit claim this project makes. Extended by proposed diff only (NOT-05) — NEVER edited in place."
- `TEX` — "A complementary NUMBERED treatment, not a superset of SRC (see §4). Normative for `ι` = the ladder resolution = our `#_σ`, defined at `TEX:212`."

`Owner` cells, verbatim:
- `SRC` and `ENTRY_POINT` — `THIS PROJECT (evm-controller/notes/ and spec/ are not assigned to any peer by CLAUDE.md, which gives ul2inqpl src/, script/, foundry.toml, remappings.txt and .plk)`
- `DOC`, `UNITS`, `TEX` — `PEER ul2inqpl (READ-ONLY)`

Add below the table: `**Commit state of SRC:** committed at
`0fc821aec2c68f5709749c23c8ac32ca54a943b4` ("docs(notes): Proposition 13 splits conditional from
unconditional"); working tree clean; blob identical to the pin above. **The pin chain, oldest
first, is `0ec30184` (`78381d4`) → `45aeba4c` (`c521af5`) → `04bac0a5` (`cf386de`) → `33af6a85`
(`0fc821a`). Only the last is current; the other three are superseded and are retained wherever
lineage is recorded** — see §8's THREE re-pin sweeps. Verify with
`git -C WT status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` (expect empty) and
`git -C WT log --oneline -4 -- notes/VOLATILITY_INTRUMENTS_MEV.md` (expect `0fc821a`, `cf386de`,
`c521af5`, `78381d4` in that order).`

**Also record the block index, MEASURED — it is what makes the by-block citation form usable:**

```
grep -nE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' notes/VOLATILITY_INTRUMENTS_MEV.md
```

Plan-time output at `33af6a85` (re-measure; record drift). **All 14 heading LINE NUMBERS are unchanged from `04bac0a5`; only `Proposition 13`'s heading TEXT changed (its rider gained the `(H2)` clause) — so the index below is a line index, not a content index:**

| Block | Line |
|---|---|
| `Convention 7 (Event time) [M11]` | 12 |
| `Convention 8 (Liquidity axis) [M18]` | 21 |
| `Definition 32 (Event-time plant) [M11]` | 48 |
| `Definition 33 (Replication residual) [M19]` | 71 |
| `Definition 34 (State-space representation) [M11]` | 79 |
| `Definition 35 (Monoid gradient) [M12]` | 91 |
| `Rule 13 (Fee schedule and standing assumptions) [M11]` | 99 |
| `Theorem 29 (The monoid path is direct) [M12]` | 109 |
| `Theorem 30 (Path decomposition) [M20]` | 118 |
| `Proposition 12 (Kernel differentiation) [M20]` | 130 |
| `Theorem 31 (The section sum is ill-posed) [M13]` | 147 |
| `Hypothesis (H2) [M18]` | 160 |
| `Definition 36 (Tax program) [M19]` | 168 |
| `Proposition 13 (Corrected law) [M24]` | 178 |

**These line numbers are NAVIGATION ONLY.** They are recorded so a reader can find a block; they
are NOT a citation form. Cite `Definition 35 @ 33af6a85`, never `SRC:95`.

**STEP 3 —** write `## 3. The three artifacts the discovery failure missed`: a bulleted list
naming exactly `plank/notes/UNITS_AND_SCALES.md`,
`plank/notes/VOLATILITY_INSTRUMENTS_MEV.tex`, and
`spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md`, each with its pin inline and one sentence
on why missing it mattered. For `ENTRY_POINT.md` add: "UNTRACKED at sweep time — at risk of
destruction by any `git clean`. Tracked by Task 3 of this plan."

Write nothing about the superset question here — that is Task 2.
  </action>
  <acceptance_criteria>
    - `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` exists and is ≥ 80 lines at end of this task.
    - It contains exactly one `<!-- PLANK-BASELINE -->` and one `<!-- /PLANK-BASELINE -->`, and exactly one `<!-- LEAN-BASELINE -->` / `<!-- /LEAN-BASELINE -->` pair.
    - The sentinel extraction command returns a non-empty block that is byte-identical to live `git -C plank status --porcelain`.
    - It contains all five artifact filenames: `UNITS_AND_SCALES.md`, `VOLATILITY_INSTRUMENTS_MEV.tex`, `ENTRY_POINT.md`, `VOLATILITY_INSTRUMENTS.md`, `VOLATILITY_INTRUMENTS_MEV.md`.
    - It contains the measured pin for `UNITS` (plan-time value `a46ba298d8b48fa851e817bf6f46e580b350878e`) and the literal string `TEX:212`.
    - It contains the literal string `Normative FOR` and the literal string `PEER ul2inqpl (READ-ONLY)` at least three times.
    - It does NOT contain the string `PEER-ADJACENT` anywhere.
    - It contains the literal string `THIS PROJECT (evm-controller/notes/`.
    - It contains the literal string `cf386de` (the CURRENT pin's commit) AND the literal string `c521af5` (the superseded one, retained as history).
    - It contains the literal string `The peer tree is NOT clean at baseline` and the literal string `is a tautology and is not a check`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md && test -f $F && test $(wc -l < $F) -ge 80 && test $(grep -cF '<!-- PLANK-BASELINE -->' $F) -eq 1 && test $(grep -cF '<!-- /PLANK-BASELINE -->' $F) -eq 1 && test $(grep -cF '<!-- LEAN-BASELINE -->' $F) -eq 1 && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' $F | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && test -n "$B" && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && for s in UNITS_AND_SCALES.md VOLATILITY_INSTRUMENTS_MEV.tex ENTRY_POINT.md VOLATILITY_INSTRUMENTS.md VOLATILITY_INTRUMENTS_MEV.md "TEX:212" "Normative FOR" "78381d4" "THIS PROJECT (evm-controller/notes/" "The peer tree is NOT clean at baseline" "is a tautology and is not a check"; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && test $(grep -cF "PEER ul2inqpl (READ-ONLY)" $F) -ge 3 && ! grep -qF "PEER-ADJACENT" $F && echo PASS</automated>
  </verify>
  <done>The plank and lean baselines are captured between sentinels as the first thing written, the extraction command reproduces live porcelain exactly, the five-row pin register carries measured pins/lines and correct `Owner` cells, and no `PEER-ADJACENT` claim is made about `SRC`.</done>
</task>

<task type="auto">
  <name>Task 2: The superset finding, the five-rule citation protocol, the re-pin procedure, and the historical corrections</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` (the file you are appending to)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS_MEV.tex` lines 205-220 (the `ι` definition you quote)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md` lines 668-676 (the `I_ord` block at `DOC:672` — you must determine its nearest enclosing numbered item)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md` Phase 1 Success Criterion 1 (already corrected — read the CURRENT text)
  </read_first>
  <action>
**A — `## 4. VOLATILITY_INSTRUMENTS_MEV.tex is NOT a superset of SRC`.**

Run the probe battery. **Use `-cF`. Do not use bare `-c`** — see the `grep` warning in
`<context>`:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/plank
for p in 'u_{\text{ex}}' 'x_{t+1}' '\hat{\pi}' '\widehat\pi' '\frac{\partial' '\partial'; do printf "%s : " "$p"; grep -oF -- "$p" notes/VOLATILITY_INSTRUMENTS_MEV.tex | wc -l; done
grep -nF -- '\partial' notes/VOLATILITY_INSTRUMENTS_MEV.tex
```

The SIXTH probe is bare `\partial`, and it exists to STOP you overclaiming: at plan time
`\frac{\partial` returned 0 but bare `\partial` returned **4 occurrences, on lines 455 and
789**. Paste the raw output of BOTH commands, wrapped in `<!-- PROBE-OUTPUT -->` /
`<!-- /PROBE-OUTPUT -->` sentinels so the error-scan can be scoped to it. It MUST be six count
lines each ending in a digit, and **no line between those sentinels may contain the word
`error`**. If any line contains `error`, the battery did not run — fix the command and
re-run. A recorded `error` line, or a count you did not actually see, is a fabricated finding
on the phase's headline claim.

At plan time all five returned `0`. Record a table `Probe | Hits in TEX` and then state verbatim:

```
**Finding INV-1 — TEX is a complementary numbered treatment, not a superset of SRC.**
The first five probes, drawn from the controller derivation, return ZERO hits in TEX: the
exogenous input `u_{\text{ex}}`, the event-time successor `x_{t+1}`, both spellings of the
realized liquidity-kernel payoff `\hat{\pi}` / `\widehat\pi`, and `\frac{\partial`.

**Stated precisely — TEX carries no FRACTION-FORM partial-derivative display.** The sixth
probe shows bare `\partial` DOES occur, 4 times across two lines, and both are accounted for:
`TEX:455` (Definition 14, the curvature `\kappa_{\varphi}`) and `TEX:789`
(`p_{\varphi} \equiv \partial_{Q_X}\varphi \,/\, \partial_{Q_M}\varphi`, the marginal price).
**Neither is a `∂`-partition entry.** Inferring that TEX contains no partials at all would be
FALSE. What is true, and what the non-superset finding rests on, is that TEX contains no
`\frac{\partial\cdot}{\partial\cdot}` display and therefore none of the `∂`-partition, the
5-factor channel, or the boxed `τ*_MEV`.

**Method note.** The probes were run with a fixed-string matcher. With a bare `grep -c` in this
environment (where `grep` is a shell function dispatching to `ugrep`) probes 3-5 fail with
`invalid character class` rather than returning `0`, because `\h`, `\w` and `\p` parse as
character classes. A zero recorded from a bare `-c` run would be fabricated.

**Consequence (binding):** `SRC` remains the citation target for the controller derivation.
```

**B — `## 5. Citation protocol (binding on every later artifact)`.** Five rules, verbatim:

```
**Rule 1 — pin form, and `SRC`'s MIXED convention.** A pin is written INLINE as the first 8 hex
characters of the artifact's `git hash-object` blob, e.g. `Definition 35 @ 33af6a85`. The full
40-character value appears exactly once, in §2. The tokens `SRC_SHA`, `DOC_SHA`, `UNITS_SHA`,
`TEX_SHA`, `ENTRY_POINT_SHA` are NAMES FOR §2 ROWS and must never be written literally inside a
citation.

**`SRC` IS NOW FULLY BLOCKED as of commit `cf386de`, and BOTH earlier rationales are FALSE.**
This rule has been wrong twice and the history is recorded so it is not got wrong a third time:

- At pin `0ec30184` the rule read *"`SRC` carries no numbered Definitions or Rules, so a line is
  the only address it has."* Commit `c521af5` falsified that by adding six numbered blocks.
- At pin `45aeba4c` the rule read *"`SRC` is MIXED — cite blocked content by block, the
  unblocked remainder (the `\pi^{\phi}` algebra, the boxed `\tau^\star`) by line."* Commit
  `cf386de` falsified THAT: the unblocked remainder was **DELETED**, not merely re-flowed. There
  is no unblocked remainder left.

At `33af6a85`, `SRC` is 190 lines consisting of: a two-line preamble (`:1-2`), the numbering
note (`:4-10`), and **14 numbered blocks** continuing the shared-corpus sequence of
`VOLATILITY_INSTRUMENTS.md` — `Convention 7` (Event time), `Convention 8` (Liquidity axis),
`Definition 32` (Event-time plant), `Definition 33` (Replication residual), `Definition 34`
(State-space representation), `Definition 35` (Monoid gradient), `Rule 13` (Fee schedule and
standing assumptions), `Theorem 29` (The monoid path is direct), `Theorem 30` (Path
decomposition), `Proposition 12` (Kernel differentiation), `Theorem 31` (The section sum is
ill-posed), `Hypothesis (H2)`, `Definition 36` (Tax program), `Proposition 13` (Corrected law).
`SRC:4-10` states the reservation explicitly. Therefore:

- **Every citation of `SRC` content is BY NUMBERED BLOCK plus pin** — `Definition 35 @ 33af6a85`,
  `Theorem 31 @ 33af6a85`. **A line-only citation of `SRC` is a defect**, exactly as for `DOC`.
  `Hypothesis (H2)` is unnumbered and is cited by that exact name plus pin.
- **The only two line-addressable loci** are the preamble import at `SRC:2 @ 33af6a85` and the
  numbering note at `SRC:4-10 @ 33af6a85`, neither of which carries mathematics.
- **Where a specific display inside a block must be pinpointed, use a CONTENT ANCHOR, not a
  line number** — e.g. ``Rule 13 @ 33af6a85, the `\phi_X` schedule line
  (`grep -nF 'Phi \, (\Theta_{\phi};'`)``. Line numbers inside `SRC` have moved at every one of
  its three commits; block numbers have not moved once.
- Locate the block boundaries by content, never by remembered offsets:
  `grep -nE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' notes/VOLATILITY_INTRUMENTS_MEV.md`.
  Record that command's output in §2 so a reader can see where each block starts.

**Rule 2 — `DOC`.** Where the locus IS a numbered item, cite by numbered item plus pin:
`DOC Definition 25 @ 0c5ce574`. Where the locus is NOT numbered — prose, or an unnumbered
display such as the `I_ord` block at `DOC:672` — cite by LINE plus pin AND name the nearest
enclosing numbered item: `DOC:672 @ 0c5ce574 (unnumbered Protocol Input display; nearest
enclosing numbered item: <the one you find by reading upward>)`. Line-only citation of `DOC` is
a defect. `DOC` measures 1639 lines today against `PROJECT.md`'s 1636 — line numbers in this
document move, numbered items do not.

**Rule 3 — `TEX`.** Same two-branch rule as Rule 2.

**Rule 4 — `UNITS`.** By line plus pin: `UNITS_AND_SCALES.md:70 @ a46ba298`. The table is a
line-addressed normative object and has no numbered items.

**Rule 5 — `ENTRY_POINT`.** By line plus pin: `ENTRY_POINT.md:15-60 @ db70caf2`.
```

**C — `## 8. Re-pin procedure`** (in the FINISHED document `## 6` precedes `## 7` and `## 8`; §6 is authored last only because it depends on §4's measurements). Verbatim:

```
A pinned artifact that changes does NOT get its §2 row edited. It gets a NEW dated row appended
to §2; old rows are the historical record and are never rewritten.

The plan that discovers the change then runs a **re-verification sweep** over every citation of
that artifact issued under the old pin, and records the result here as a
`### Re-pin sweep <YYYY-MM-DD> — <alias> <old8> → <new8>` block listing, per citation: the
citation, whether it still resolves, and the new address if it moved.

**An artifact citing a superseded pin with no sweep entry covering it is a DEFECT.**

### Re-pin sweep 2026-08-08 — SRC `0ec30184` → `45aeba4c` (commit `c521af5`) — SUPERSEDED

**Recorded as history, not as a live address table.** This sweep re-derived every citation from
`0ec30184` onto `45aeba4c`. **Every one of its "new locus" values was itself superseded 24 hours
later by `cf386de`.** It is retained because §8's own rule says old rows are the historical
record and are never rewritten — but **no artifact may resolve a citation through this table.**
Resolve through the 2026-08-09 sweep below.

| Old citation @ `0ec30184` | Content | Locus @ `45aeba4c` | Now |
|---|---|---|---|
| `SRC:2` | `import [MEV](...)` | `SRC:2` | see 2026-08-09 sweep |
| `SRC:23` | `u_{\text{en}}`, `\Theta_{\sigma}` blocks | `SRC:37` | see below |
| `SRC:28` | `\pi^{\varphi} \equiv \pi^{\phi} - \pi^{\text{LVR}}` | **DELETED at `c521af5`** | still deleted |
| `SRC:56` | the `\nabla\phi` display | `SRC:65` | see below |
| `SRC:70` | the `(\beta,\gamma)` non-existent-theorem sentence | **DELETED at `c521af5`** | still deleted |
| `SRC:106` | `\pi^{\phi} \equiv \phi_M \Delta Q_M + p \phi_X \Delta Q_M` | `SRC:111` | **DELETED at `cf386de`** |
| `SRC:108` | `\partial\pi^{\phi}/\partial\phi` chain rule | `SRC:113` | **DELETED at `cf386de`** (RHS survives — see below) |
| `SRC:110` | the two-section sum | `SRC:115` | **DELETED at `cf386de`** |
| `SRC:118` | `(\partial\pi^{\sigma}/\partial\phi_M, \partial\pi^{\sigma}/\partial\phi_X)` | `SRC:123` | **DELETED at `cf386de`** |
| `SRC:221-229` | the boxed fee-Jacobian bracket | `SRC:226-234` | **DELETED at `cf386de`** |
| `SRC:222-228` | — | — | never valid; dropped |

### Re-pin sweep 2026-08-09 — SRC `45aeba4c` → `04bac0a5` (commit `cf386de`) — **AUTHORITATIVE**

**This is the sweep every citation resolves through.** `SRC` was restructured into **14 numbered
blocks** and SHRANK 240 → 190 lines. **The restructure DELETED content; it did not merely move
it.** Two dispositions exist and they are not interchangeable:

- **RE-PINNED** — the content survives; it now sits inside a named block and is cited BY BLOCK.
- **STRUCK** — the content was removed FOR CAUSE. **A struck citation has no new line number.**
  Re-pinning it at whatever now occupies its old offset would silently point it at a different
  claim. Where the struck material's PURPOSE is served by a replacement block, this table names
  that block explicitly as a RE-AIM; where nothing replaces it, the table says so.

**Re-derive every row by content match against the file on disk. Do not trust this table
blindly, and do not use offset arithmetic — the deltas are not uniform** (+30 through `Rule 13`,
undefined thereafter because the region was deleted).

**RE-PINNED — content survives (10 rows).** Verify each with the `grep -nF` given, and paste its
output:

| Citation @ `45aeba4c` | Content anchor (`grep -nF`) | Now inside | Line @ `04bac0a5` |
|---|---|---|---|
| `SRC:2` | `import [MEV]` | preamble (not a block) | 2 |
| `SRC:4-7` | `**Numbering.**` | numbering note (not a block) | **4-10** (the note GREW; the range is no longer 4-7) |
| `SRC:23` | `\begin{bmatrix}` (first) | **`Definition 32`** | 53 |
| `SRC:26-27` | `\pi^{\phi} - \pi^{\text{LVR}}` | **`Definition 32`** | 56-57 |
| `SRC:28` (as re-pinned at `c521af5`) | `\end{bmatrix} \, \quad \t\tu_{\text{ex}}=` | **`Definition 32`** | 58 |
| `SRC:37` | `u_{\text{en}} = \, \begin{bmatrix} \tau_{\text{MEV}}` | **`Definition 32`** | 67 |
| `SRC:41-47` | `e^{\sigma} \, &= \, \bigl|\pi^{\sigma}` | **`Definition 33`** | 71-77 |
| `SRC:56` | `x_{t+1} &= \, \partial_{(t+1,t)}` (`:84`, UNIQUE) — **the `\end{cases}` anchor this row used is RETIRED: at `33af6a85` it matches twice (`:86`, `:198`)**. `SRC:56 @ 45aeba4c` was the `\end{cases}` at `:86`; locate the block by `:84` and take its display close | **`Definition 34`** | 86 |
| `SRC:65` | `nabla \phi \; \equiv` | **`Definition 35`** | 95 |
| `SRC:73` | `Phi \, (\Theta_{\phi};` | **`Rule 13`** | 103 |

**`SRC:73` changed CONTENT as well as address — read it before citing it.** At `45aeba4c` it read
`\phi_X(t) = \Phi(\Theta_{\phi}; \sigma^2(i(t)))`. At `33af6a85` it reads
`\phi_X (t) = \Phi \, (\Theta_{\phi}; \sigma (i (t)), \nu (t))`. **Both arguments changed:
`\nu` was ADDED (open item **O3**) and `\sigma^2` became `\sigma` (open item **O4**).** Record
this in §9 as `INV-C1` (a CORRECTION at source, not an erratum) and route it: O3 is now closed at
source; O4 is now a mismatch between `Rule 13`'s `\sigma` and `Definition 32`'s `\sigma^2`
disturbance slot, which is `NOT-05`'s to settle.

**STRUCK — deleted for cause, NO new line number (9 rows).** Confirm each deletion with the
`grep -cF` given, which MUST return `0`:

| Citation @ `45aeba4c` | Content | Deletion probe (expect `0`) | Why struck | RE-AIM target |
|---|---|---|---|---|
| `SRC:110` | `\begin{aligned}` fragment inside the struck region | — | it sat inside the deleted `\pi^{\phi}` algebra | **NONE — a generic LaTeX fragment. A content match finds `\begin{aligned}` elsewhere in the file; that match is SPURIOUS. Treat as dead.** |
| `SRC:111` | `\pi^{\phi} \equiv \phi_M \Delta Q_M + p \phi_X \Delta Q_M` | `grep -cF 'phi_M \, \Delta Q_M'` | duplicate definition — `\pi^{\phi}` is owned by **`DOC` Rule 6 (Per-leg fee split)** | **`DOC` Rule 6 @ `0c5ce574`** |
| `SRC:113` | `\partial\pi^{\phi}/\partial\phi = \partial\phi_M/\partial\phi\,\Delta Q_M + p\,\partial\phi_X/\partial\phi\,\Delta Q_X` | `grep -cF 'partial \pi^{\phi}}{\partial \phi}'` | the LHS is gone; **the RHS survives at `Theorem 31` with the OPPOSITE claim attached** — it is no longer "the derivative of `\pi^{\phi}`", it is the expression proved to have **no section-independent value** | **`Theorem 31 (The section sum is ill-posed) @ 33af6a85`** — cite it as the ill-posed object, NEVER as a derivative |
| `SRC:115` | the fee-Jacobian bracket `\Delta Q_M/((1-\tau)(1-\phi_X)) + p\Delta Q_X/((1-\tau)(1-\phi_M))` | `grep -cF '(1 -  \tau_{\text{MEV}})(1 - \phi_X)'` | ruled **ILL-POSED** by the M24 audit (`Theorem30_composed_fee_submersion_section_sum_ill_posed`) | **`Theorem 31 @ 33af6a85`** |
| `SRC:123` | `(\partial\pi^{\sigma}/\partial\phi_M, \partial\pi^{\sigma}/\partial\phi_X) = (p\Delta Q_X, \Delta Q_M)` | `grep -cF 'partial \pi^{\sigma}}{\partial \phi_M}'` | struck: `Theorem 31` proves the section sum has **no section-independent value**, so neither partial is defined | **`Theorem 31 @ 33af6a85`** |
| `SRC:209` | "Thus, solving for `\tau_{\text{MEV}}` on the minimization" + "note: This needs verification" | `grep -cF 'This needs verification'` | the root-vs-argmin ambiguity was RESOLVED: the program is now stated explicitly as a minimisation with replication as a **constraint** | **`Definition 36 (Tax program) @ 33af6a85`** |
| `SRC:221` | `\sum_{i_K}` fragment inside the boxed `\tau^\star` | — | it sat inside the deleted boxed law | **NONE — a generic LaTeX fragment. `\sum_{i_K}` matches elsewhere; that match is SPURIOUS. Treat as dead.** |
| `SRC:222` / `SRC:226` / `SRC:226-234` | the boxed `\tau^{\star}_{\text{MEV}}` and its fee-Jacobian bracket | `grep -cF 'tau_{\mathrm{MEV}}^\star'` | **REFUTED factor by factor by the M24 audit.** Three factors MISSING, two SPURIOUS, one ILL-POSED | **`Proposition 13 (Corrected law) @ 33af6a85`** |
| `SRC:111-115`, `SRC:113-115`, `SRC:106-110` | the whole `\pi^{\phi}` algebra region | — | **the entire unblocked remainder was deleted** | see the three rows above; **no range citation of `SRC` is valid any more** |

**NEW at `cf386de` — eight blocks that did not exist at `45aeba4c`.** Record them so a later
artifact does not "queue" a block that is already written:
`Convention 8 (Liquidity axis)`, `Theorem 29 (The monoid path is direct)`,
`Theorem 30 (Path decomposition)`, `Proposition 12 (Kernel differentiation)`,
`Theorem 31 (The section sum is ill-posed)`, `Hypothesis (H2)`,
`Definition 36 (Tax program)`, `Proposition 13 (Corrected law)`.

**Verification requirement:** for every RE-PINNED row, run the `grep -nF` that finds the content
and paste its output. For every STRUCK row, run the `grep -cF` and paste the `0`. A row asserted
without its command is not a re-verification.
**A STRUCK row that acquires a line number is a defect** — that is the failure mode this sweep
exists to prevent. Write that sentence on ONE line; an acceptance criterion greps it verbatim.

### Re-pin sweep 2026-08-09 (later same day) — SRC `04bac0a5` → `33af6a85` (commit `0fc821a`) — **CURRENT, and ZERO-RELOCATION**

**Not a single citation required relocation, and the reason is the mechanism this document
adopted one sweep earlier: `SRC` is cited BY NUMBERED BLOCK.** The previous move (`cf386de`)
invalidated nineteen line citations and struck nine of them. This move changed `Proposition 13`
and nothing else, and because no artifact addresses `SRC` by line, **no artifact needed editing
beyond the pin token itself.** That is the by-block citation form justifying itself on its first
test, and it belongs on the record where the next person deciding how to cite `SRC` will read it.

**What changed:** `Proposition 13 (Corrected law) [M24]` splits its conditional from its
unconditional content. The non-degeneracy guard `[(∂φ/∂ν)(∂ν/∂τ_MEV) ≠ 0]` is stated inline on
the equivalence; the domain lines move into a second display under an explicit
`0 < ∂φ/∂ν, ∂ν/∂τ_MEV < 0, φ_X < 1 ⟹ {…}` guard; the rider gains
`∂ν/∂τ_MEV < 0 rests on (H2) — UNDISCHARGED`. 190 → 200 lines.

**Measurements — reproduce every one; do not copy these numbers:**

```
git show 04bac0a5 > /tmp/src_old.md
diff <(grep -nE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' /tmp/src_old.md) \
     <(grep -nE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' notes/VOLATILITY_INTRUMENTS_MEV.md)
diff /tmp/src_old.md notes/VOLATILITY_INTRUMENTS_MEV.md | grep -E '^[0-9]'
grep -nF '\end{cases}' notes/VOLATILITY_INTRUMENTS_MEV.md
```

| Measurement | Result |
|---|---|
| Numbered blocks | **14 → 14**, and all 14 heading LINE NUMBERS identical |
| Block heading TEXT | **one changed** — `Proposition 13`'s rider. The block index is a LINE index, not a content index; **do not report it as "identical"** |
| Diff hunks | `178c178` and `184,188c184,198` — **all change at or after `:178`**, below every other block |
| RE-PINNED anchors from the previous sweep | **9 of 10 unmoved and still unique; 1 unmoved but NO LONGER UNIQUE** — see the caveat |
| STRUCK deletion probes | **8 of 8 still return `0`** |
| Banned `1 −` absolute-value reading | still absent |
| Citations requiring relocation | **ZERO** |

**CAVEAT — one content anchor lost its uniqueness, which is the same hazard class the STRUCK
rows exist to prevent.** `\end{cases}` matched once at `04bac0a5` (`:86`, closing
`Definition 34`'s display). At `33af6a85` it matches **twice**: `:86` and `:198`, the new one
inside `Proposition 13`'s conjunct-2 guard. **The previous sweep's `SRC:56` row used
`\end{cases}` as its anchor and would now resolve ambiguously.** Disambiguate on
`x_{t+1} &= \, \partial_{(t+1,t)}` (`:84`, unique, inside `Definition 34`) and record the
`\end{cases}` anchor as retired. **An anchor unique at one pin is not guaranteed unique at the
next: re-check UNIQUENESS, not just presence, on every sweep.**
```

**D — `## 6. Historical record — corrections this sweep's measurements forced`.** Both entries
describe corrections **already applied**; write them as history, not as instructions. Verify
first that they are applied:
```
grep -cF '88 Definitions' /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md
grep -nF 'ENTRY_POINT.md:672' /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md
```
Both returned nothing at plan-verification time. Then measure the item counts:
```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/plank
for p in 'textbf{Definition' 'textbf{Theorem' 'textbf{Proposition'; do printf "%s : " "$p"; grep -oF -- "$p" notes/VOLATILITY_INSTRUMENTS_MEV.tex | wc -l; done
grep -oE 'textbf\{Definition [0-9]+' notes/VOLATILITY_INSTRUMENTS_MEV.tex | sort -t' ' -k2 -n | tail -1
grep -oE 'textbf\{Theorem [0-9]+' notes/VOLATILITY_INSTRUMENTS_MEV.tex | sort -t' ' -k2 -n | tail -1
grep -oE 'textbf\{Proposition [0-9]+' notes/VOLATILITY_INSTRUMENTS_MEV.tex | sort -t' ' -k2 -n | tail -1
```
Plan-time: 29 / 21 / 9 occurrences; highest numbered Definition 31, Theorem 21, Proposition 10.

Then write, verbatim (substituting your measured values):

```
**Finding INV-2 (HISTORICAL RECORD).** An earlier planning record described TEX as carrying
"88 Definitions / 46 Theorems / 23 Propositions". Measured against the pin: 29 / 21 / 9
occurrences of `\textbf{Definition` / `\textbf{Theorem` / `\textbf{Proposition`, highest
numbered items Definition 31, Theorem 21, Proposition 10. **The 88/46/23 figure was corrected
in `ROADMAP.md` and `REQUIREMENTS.md` at commit `98c2891` ("fix: correct .tex item counts
(29/21/9 not 88/46/23) and DOC:672 miscitation").** This section records the measurement that
forced that correction, and the current measurement re-confirming it.
`grep -cF '88 Definitions'` over `ROADMAP.md` returns 0 today.

**Finding INV-3 (HISTORICAL RECORD).** An earlier `ROADMAP.md` Phase 1 Success Criterion 3
cited the `ΔQ_v★`/`ΔQ_υ` glyph collision to `ENTRY_POINT.md:672`. `ENTRY_POINT.md` is 64 lines
long; the actual locus is `DOC:672`, the unnumbered Protocol Input display
`\mathcal{I}_{\text{ord}} = \{\sigma^2_K, \#_{\sigma}, s_{\upsilon}, \Delta Q_v^{\star}\}`.
**Corrected at commit `98c2891`.** This section records the measurement that forced it;
`grep -nF 'ENTRY_POINT.md:672'` over `ROADMAP.md` and `REQUIREMENTS.md` returns nothing today.
Note that `DOC:672` is a LINE citation of an UNNUMBERED locus and is therefore governed by §5
Rule 2's second branch.
```
  </action>
  <acceptance_criteria>
    - `INVENTORY.md` contains `Finding INV-1`, `Finding INV-2 (HISTORICAL RECORD)`, `Finding INV-3 (HISTORICAL RECORD)`.
    - The `<!-- PROBE-OUTPUT -->` sentinel block contains no line containing the string `error`, and contains exactly six count lines each ending in a digit. The scan is scoped to those sentinels — the surrounding prose legitimately discusses the word `error`.
    - It contains `TEX:455`, `TEX:789`, and the literal string `Neither is a `∂`-partition entry.`
    - It does NOT contain the string `no partial-derivative display at all`.
    - It contains all five probe strings: `u_{\text{ex}}`, `x_{t+1}`, `\hat{\pi}`, `\widehat\pi`, `\frac{\partial`.
    - It contains the literal string `Method note.` and the literal string `would be fabricated`.
    - It contains `**Rule 1 —`, `**Rule 2 —`, `**Rule 3 —`, `**Rule 4 —`, `**Rule 5 —` (exactly five rules under §5).
    - It contains the literal string `Definition 35 @ 33af6a85` (the by-block form Rule 1 now mandates) and the literal string `must never be written literally inside a citation`.
    - **`SRC` line citations are dead:** **in §1-§7** the file contains NO occurrence of `SRC:` followed by a number OTHER THAN `SRC:2` and `SRC:4-10`. §8 (the two sweep tables) and §9 (the erratum headings, which name their `45aeba4c` loci) are **address-history, not citations, and are deliberately exempt** — the automated check scopes itself to the span before `## 8.` for exactly that reason, and this criterion says so rather than claiming whole-file coverage it does not have.
    - The tokens `SRC_SHA`, `DOC_SHA`, `UNITS_SHA`, `TEX_SHA`, `ENTRY_POINT_SHA` appear ONLY inside §5 Rule 1 (where they are named as forbidden) — nowhere else in the file.
    - It contains `## 8. Re-pin procedure`, the literal string `is a DEFECT`, and ALL THREE sweep headings: `### Re-pin sweep 2026-08-08 — SRC` (marked `SUPERSEDED`), `### Re-pin sweep 2026-08-09 — SRC `45aeba4c` → `04bac0a5`` (marked `AUTHORITATIVE`) and `### Re-pin sweep 2026-08-09 (later same day) — SRC `04bac0a5` → `33af6a85`` (marked `CURRENT, and ZERO-RELOCATION`).
    - **The zero-relocation sweep states the mechanism, not just the result:** it contains `Citations requiring relocation | **ZERO**` and the sentence beginning `Not a single citation required relocation, and the reason is the mechanism`.
    - It records the anchor caveat: `\end{cases}` matches TWICE at `33af6a85`, the `SRC:56` row is re-anchored on `x_{t+1} &= \, \partial_{(t+1,t)}`, and the file contains `re-check UNIQUENESS, not just presence, on every sweep`.
    - **The block index is described as a LINE index, not a content index** — it contains `do not report it as "identical"` — because `Proposition 13`'s heading text changed while its line number did not.
    - **The pin chain is recorded in full and in order:** the file contains `0ec30184`, `45aeba4c`, `04bac0a5` and `33af6a85`, and states that only `33af6a85` is current.
    - The 2026-08-09 sweep contains a `RE-PINNED` table with 10 rows and a `STRUCK` table with 9 rows, and the literal string `A STRUCK row that acquires a line number is a defect`.
    - Every `STRUCK` row names a RE-AIM target or the literal string `NONE —`; no `STRUCK` row carries a line number in its last column.
    - The 2026-08-09 sweep names all four re-aim targets: `DOC` Rule 6, `Theorem 31`, `Definition 36`, `Proposition 13`.
    - It names all eight blocks NEW at `cf386de`: `Convention 8`, `Theorem 29`, `Theorem 30`, `Proposition 12`, `Theorem 31`, `Hypothesis (H2)`, `Definition 36`, `Proposition 13`.
    - It records `INV-C1` — `Rule 13`'s signature changed at `cf386de` from `\Phi(\Theta_{\phi}; \sigma^2(i(t)))` to `\Phi(\Theta_{\phi}; \sigma(i(t)), \nu(t))` — and states that **O3 is closed at source** while **O4 is now a `Rule 13` vs `Definition 32` mismatch**.
    - It contains the literal string `98c2891` at least twice.
    - `grep -cF '88 Definitions' control/.planning/ROADMAP.md` returns 0 and `grep -nF 'ENTRY_POINT.md:672'` over ROADMAP.md and REQUIREMENTS.md returns nothing — i.e. INV-2/INV-3 are genuinely historical.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md && for s in "Finding INV-1" "Finding INV-2 (HISTORICAL RECORD)" "Finding INV-3 (HISTORICAL RECORD)" 'u_{\text{ex}}' 'x_{t+1}' '\hat{\pi}' '\widehat\pi' '\frac{\partial' "Method note." "would be fabricated" '**Rule 1 —' '**Rule 2 —' '**Rule 3 —' '**Rule 4 —' '**Rule 5 —' 'Definition 35 @ 33af6a85' 'must never be written literally inside a citation' '## 8. Re-pin procedure' 'is a DEFECT' '### Re-pin sweep 2026-08-08 — SRC' '### Re-pin sweep 2026-08-09 — SRC' 'AUTHORITATIVE' 'CURRENT, and ZERO-RELOCATION' 'Citations requiring relocation | **ZERO**' 're-check UNIQUENESS, not just presence, on every sweep' '0ec30184' '45aeba4c' '04bac0a5' '33af6a85' 'A STRUCK row that acquires a line number is a defect' 'INV-C1' 'Convention 8' 'Theorem 29' 'Theorem 30' 'Proposition 12' 'Theorem 31' 'Hypothesis (H2)' 'Definition 36' 'Proposition 13'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && test $(awk '/^### Re-pin sweep 2026-08-09/,0' $F | grep -c '^| *`SRC:') -ge 19 && test -z "$(awk '/^## 8\./{exit} {print}' $F | grep -oE 'SRC:[0-9]+(-[0-9]+)?' | grep -vE '^SRC:(2|4-10)$')"  && test $(grep -cF '98c2891' $F) -ge 2 && P=$(awk '/^## 4\./,/^## 5\./' $F) && test $(printf '%s\n' "$P" | grep -ciF 'error') -eq 0 && for t in SRC_SHA DOC_SHA UNITS_SHA TEX_SHA ENTRY_POINT_SHA; do test $(grep -cF "$t" $F) -le 1 || { echo "LEAKED TOKEN: $t"; exit 1; }; done && test $(grep -cF '88 Definitions' control/.planning/ROADMAP.md) -eq 0 && ! grep -qF 'ENTRY_POINT.md:672' control/.planning/ROADMAP.md control/.planning/REQUIREMENTS.md && echo PASS</automated>
  </verify>
  <done>The superset finding rests on a `-F` battery whose pasted output is provably error-free; §5 fixes exactly five citation rules with an inline 8-hex pin form and no literal placeholder tokens anywhere; §8 gives a re-pin procedure with a worked precedent; INV-2 and INV-3 are written as historical records of corrections already landed at `98c2891`.</done>
</task>

<task type="auto">
  <name>Task 3: SRC errata, the artifact-ownership table, and tracking ENTRY_POINT.md</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md, spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` (the file you are appending to)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` — **ALL 190 lines.** The displays this section used to errata (`\pi^{\phi}`'s definition, its derivative, the `(\partial\pi^{\sigma}/\partial\phi_M,\cdot)` pair, the boxed `\tau^{\star}`) were **DELETED at `cf386de`**. Read the file to confirm their absence before writing a word of §9 — do not trust a remembered range and do not re-pin a deleted display
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md` (all 64 lines — you are tracking it; its CONTENT must not change)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/CLAUDE.md` (ownership map)
  </read_first>
  <action>
**A — `## 9. Errata and corrections against SRC`.** Open with the rationale, verbatim:

```
**Why an erratum is RECORDED and not APPLIED.** `SRC` is THIS PROJECT'S OWN document —
`CLAUDE.md` assigns `ul2inqpl` `src/`, `script/`, `foundry.toml`, `remappings.txt` and `.plk`,
and does not assign `evm-controller/notes/`. Nothing forbids this project from editing it. What
forbids it is this: **`SRC` is the pinned citation target, and editing it invalidates every
citation issued before the edit.** That is not hypothetical — it has now happened twice
(`c521af5`, `cf386de`) and the second time it deleted content rather than moving it.

**ERRATUM IDs live in THIS document's namespace, not the finding register's.** `INVENTORY.md`
§10 assigns `SRC` errata to `INVENTORY.md` §9, so the IDs are `INV-E<n>` (errata) and `INV-C<n>`
(corrections made AT SOURCE by the author) — assigned HERE, complete on the day they are
written. There is no `CF-TBD` and nothing is backfilled from another plan. `FINDING-REGISTER.md`
may CITE them; it does not number them.

**BINDING READ-THROUGH RULE — RETIRED, AND WHY.** At pin `45aeba4c` this document carried a rule
requiring every citation of an errata'd line to be followed on the same line by
`[ERRATUM INV-E<n>: read <corrected form>]`. **As of `33af6a85` there is no live erratum to tag.**
All three errata were fixed at source. The rule is retired and REPLACED by the stronger rule
below, which is what the current state actually needs:

**BINDING STRUCK-LOCUS RULE.** The loci the three errata sat on were **DELETED**, not corrected
in place. A later artifact that re-pins one of them at whatever now occupies its old offset
points the citation at a DIFFERENT claim. Therefore: **no artifact this project writes may issue
a live line citation of `SRC` at all** (§5 Rule 1), and the struck loci
`SRC:111`, `SRC:113`, `SRC:115`, `SRC:123`, `SRC:209`, `SRC:226-234` may appear ONLY inside §8's
sweep tables or with an explicit `(STRUCK at cf386de — see §8)` annotation on the same line.
```

Then a THIN table — `ID | Locus @ 45aeba4c | Verdict (one clause) | Analysis →` — whose
`Analysis →` cell points at a `### 9.N` section. **Do not put a multi-reading analysis in a table
cell**; a markdown cell cannot hold a display. Populate by reading the file — do not trust the
plan's quotes. Four rows, then four sections. **Every row's Verdict is now a closure**; there is
no `STILL LIVE` row left.

**`### 9.1 — INV-E1 (the `\pi^{\phi}` definition, `SRC:111` @ `45aeba4c`) — RESOLVED AT SOURCE
(`cf386de`)`**

**This defect no longer exists. Do NOT write a live erratum for it and do NOT issue a
read-through tag.** At `45aeba4c` the line read
`\pi^{\phi} \equiv \phi_M \Delta Q_M + p_{(\eta,\Delta_i)} \phi_X \Delta Q_M`, whose second
`\Delta Q_M` should have been `\Delta Q_X` — contradicted by the very next display, its own
derivative, which carried `\Delta Q_X`. **Commit `cf386de` deleted the whole `\pi^{\phi}` algebra
region.** Verify before writing:

    grep -cF 'phi_M \, \Delta Q_M' notes/VOLATILITY_INTRUMENTS_MEV.md          # expect 0
    grep -cF 'partial \pi^{\phi}}{\partial \phi}' notes/VOLATILITY_INTRUMENTS_MEV.md   # expect 0

**Where `\pi^{\phi}` is now defined:** it is not defined in `SRC` at all. `SRC` uses it as a
state component (`Definition 32`) and as a differentiation variable (`Proposition 12`,
`Theorem 31`). **Its definition is owned by `DOC Rule 6 (Per-leg fee split) @ 0c5ce574`**, which
carries the correct pairing. Record that as the RE-AIM target and state verbatim, **on one
line**:

    Any artifact needing the definition of \pi^{\phi} cites DOC Rule 6, never SRC.

Status:
`RESOLVED AT SOURCE — the erratum's locus was deleted, and the object it mis-defined has a
normative home elsewhere.`

**`### 9.2 — INV-E2 (the `(\partial\pi^{\sigma}/\partial\phi_M, \partial\pi^{\sigma}/\partial\phi_X)`
pair, `SRC:123` @ `45aeba4c`) — RESOLVED AT SOURCE (`cf386de`), AND THE QUESTION IS ANSWERED`**

**Do NOT reproduce the three-reading analysis this section used to carry.** At `45aeba4c` the
display read
`(\frac{\partial \pi^{\sigma}}{\partial \phi_M}, \frac{\partial \pi^{\sigma}}{\partial \phi_X}) = (p_{(\eta\,\Delta_i)}\Delta Q_X, \Delta Q_M)`
and this document enumerated three candidate readings (glyph typo / intended `\pi^{\sigma}` /
`\widehat\pi^{\sigma}` intended) without adopting one. **Commit `cf386de` struck the display and
replaced it with a block that answers the question outright.** Verify:

    grep -cF 'partial \pi^{\sigma}}{\partial \phi_M}' notes/VOLATILITY_INTRUMENTS_MEV.md   # expect 0

**RE-AIM: `Theorem 31 (The section sum is ill-posed) @ 33af6a85`.** Quote it verbatim from the
file. It states that `(\phi_M, \phi_X, \tau_{\text{MEV}}) \mapsto \phi` is a submersion
`\mathbb{R}^3 \to \mathbb{R}`, hence `\partial\phi_M/\partial\phi` and `\partial\phi_X/\partial\phi`
are **not simultaneously defined**, hence the section sum **has no section-independent value**.

**State the disposition precisely, and do not overstate it:** `Theorem 31` settles the SECTIONING
question (no reading of the pair is privileged, because the sections themselves are not
well-defined). It does **not** retroactively adjudicate which glyph the author meant at
`SRC:123`. That question is **MOOT** — the display it lived in no longer exists and nothing
downstream depends on it. Record it as MOOT rather than as answered. Status:
`RESOLVED AT SOURCE — locus struck; the sectioning question it raised is settled by Theorem 31;
the glyph question is MOOT.`

**`### 9.3 — INV-E3 (the `(\beta,\gamma)` non-existent theorem, `SRC:70` @ `0ec30184`) —
RESOLVED-BEFORE-PHASE-1 (`c521af5`)`**

**This defect no longer exists. Do NOT write a live erratum for it.** At the old pin
`0ec30184`, `SRC:70` cited "the theorem that syays that `(\beta_j,\gamma_j)` does not control
for `\lambda_{\text{MEV}}`"; no such theorem exists and `MevOptimization.lean`'s T12
`mevMulti_mono_beta` proves the converse for `\beta`. **Commit `c521af5` removed the sentence
and restated the freeze as a DECLARED ASSUMPTION in `Rule 13`; `cf386de` left that restatement
in place.** Verify before writing:

    grep -ciE 'theorem that (says|syays)' notes/VOLATILITY_INTRUMENTS_MEV.md    # expect 0
    grep -cF 'does not control' notes/VOLATILITY_INTRUMENTS_MEV.md              # expect 0
    sed -n '/Rule 13 (Fee schedule/,/^\\]/p' notes/VOLATILITY_INTRUMENTS_MEV.md

Record the row as `RESOLVED-BEFORE-PHASE-1`, name `c521af5` as the fixing commit, quote
`Rule 13`'s replacement text **as it stands at `33af6a85`**, and state that **no read-through tag
is issued for `INV-E3`** — there is no live citation to tag. `FINDING-REGISTER.md` CF-05 records
the historical finding; this section records that it is closed.

**`### 9.4 — INV-C1 (`Rule 13`'s schedule signature) — CORRECTED AT SOURCE (`cf386de`) —
THIS IS THE ONE THAT CHANGES DOWNSTREAM WORK`**

**This is a CORRECTION, not an erratum: the author changed the mathematics, and two of this
project's standing open items move as a result.** Quote both forms verbatim.

At `45aeba4c`, `Rule 13` read:

    \phi_X (t) \; = \; \Phi \, (\Theta_{\phi}; \sigma^2 (i (t)))

At `33af6a85` it reads (locate it — `grep -nF 'Phi \, (\Theta_{\phi};'`):

    \phi_X (t) \; = \; \Phi \, (\Theta_{\phi}; \sigma (i (t)), \nu (t))

**Two arguments changed, and they route to two different requirements. Do not conflate them.**

- **`\nu` was ADDED. Open item O3 is CLOSED AT SOURCE.** O3 said `\phi_X` carries
  `\nu`-dependence (`DOC` Definition 18's utilization gate) while `Rule 13`'s signature omitted
  it, so every downstream `\partial\phi_X/\partial\cdot` might be taken along the wrong section.
  `Rule 13` now carries `\nu` explicitly and agrees with `DOC` Definition 18. Record O3 as
  **CLOSED — the source adopted the reading this project was going to flag**, name `cf386de`,
  and state that **no artifact may still describe `Rule 13` as `\nu`-free.** The consequence
  survives O3's closure and must be written down: `\phi_X` appears in `Proposition 13`'s
  numerator as `(1-\phi_X)` AND, through `\nu`, in its denominator — so the corrected law is
  self-referential in `\phi_X` as well as in `\nu`.
- **`\sigma^2` became `\sigma`. Open item O4 is STILL OPEN and its statement CHANGED.** O4 was
  "`DOC` Definition 18 takes `\sigma`, the plant's `u_{\text{ex}}` carries `\sigma^2`". The
  schedule side has now moved to `\sigma`, so the mismatch is no longer schedule-vs-`DOC` but
  **`Rule 13`'s `\sigma` against `Definition 32`'s `\sigma^2` disturbance slot, inside the same
  document.** Verify both sides in one command and paste it:

      grep -nF 'sigma^2 \, (i (t))' notes/VOLATILITY_INTRUMENTS_MEV.md
      grep -nF 'Phi \, (\Theta_{\phi};' notes/VOLATILITY_INTRUMENTS_MEV.md

  Route it to `NOT-05` (plan 01-05 §2b) as the requirement that settles it, and state verbatim:
  `**O4 is UNCHANGED in substance and CHANGED in locus.** \`\Theta_{\phi}\`'s centers are in
  \sigma-units and the disturbance is in \sigma^2-units, now WITHIN a single document. A
  regression that mixes them is still wrong and is still dimensionally invisible.`

**Do not record INV-C1 as an erratum.** An erratum is a defect in a source this project must
read through; INV-C1 is the author correcting the source. The distinction matters because an
erratum gets a read-through rule and a correction gets a re-verification of everything that cited
the old form.

**B — `## 10. Artifact ownership — one normative home per topic`.** Verbatim opener:

```
**Rule.** Every normative statement this project makes lives in EXACTLY ONE artifact and
section. Every other mention is a pointer of the form
`> Authoritative detail: <artifact> §N`, never a restatement. A statement restated in two
artifacts is a defect: the two copies WILL drift.
```

Then the table `Topic | Owning artifact & section | Everyone else`:

| Topic | Owning artifact & section | Everyone else |
|---|---|---|
| Content pins, citation form, re-pin procedure | `INVENTORY.md` §2, §5, §8 | pointer |
| `SRC` errata, corrections, and the struck-locus rule | `INVENTORY.md` §9 | pointer |
| Peer baseline / non-interference | `INVENTORY.md` §7 | pointer |
| The 13 blocking decisions and their rulings | `RULINGS-TRIAGE.md` §2-§4 | pointer |
| Glyph collisions, chosen/rejected readings | `NOTATION-MAP.md` §1 | pointer |
| Two-axis liquidity, `L_σ`, axis naming | `NOTATION-MAP.md` §2 | pointer |
| `∂φ/∂ν` determinacy and conditions (S1)-(S3) | `NOTATION-MAP.md` §3 | pointer |
| Symbol register and minting rules | `NOTATION-MAP.md` §4 | pointer |
| Finding IDs, statuses, supersessions | `FINDING-REGISTER.md` §1-§4 | pointer |
| DO-NOT-CITE ruling | `FINDING-REGISTER.md` §5 | pointer |
| Unit rows for controller symbols | `UNITS-LEDGER-EXTENSION.md` §2 | pointer |
| Review history and the gate | `REVIEW-REGISTER.md` | pointer |
| Historical corrections to ROADMAP/REQUIREMENTS | `INVENTORY.md` §6 | pointer |
| The closure statement over the 13 decisions | `RULINGS-TRIAGE.md` §5 | pointer |

**B2 — the two standing self-checks every artifact in this phase must pass.** Write them into
`## 10` as a `### 10.1 Standing artifact checks` block, verbatim, because every later plan
re-runs them:

```
**Check A — every line citation carries its pin (§5).**

    grep -oE '(SRC|UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' <artifact> | grep -v ' @ '

MUST return nothing.

**Check B — NO artifact issues a live line citation of `SRC` (§5 Rule 1, §8).**

    grep -oE 'SRC:[0-9]+(-[0-9]+)?' <artifact> | grep -vE '^SRC:(2|4-10)$'

MUST return nothing, in every artifact EXCEPT `INVENTORY.md` §8's two sweep tables, which are
address-history rather than citations. `SRC:2` (the preamble import) and `SRC:4-10` (the
numbering note) are the only two line-addressable loci left; everything else is a numbered block
and is cited as `Definition 35 @ 33af6a85`.

**Why this replaced the old Check B.** At `45aeba4c` Check B read
`grep -nE 'SRC:(111|123)\b' <artifact> | grep -v '\[ERRATUM '` — every citation of an errata'd
line had to carry a read-through tag. **Both of those loci were DELETED at `cf386de`.** A tag
check on a deleted locus passes trivially and would have let a re-pinned citation of struck
material through. The check that actually catches the current failure mode is the one above.

**Check B′ — struck loci are annotated where they appear at all.**

    grep -nE 'SRC:(111|113|115|123|209|226)' <artifact> | grep -vF '(STRUCK at cf386de'

MUST return nothing outside `INVENTORY.md` §8/§9.
```

Run both against `INVENTORY.md` now and paste the outputs. Fix any hit before continuing —
including in this document's own §2, §4 and §6.

**C — track `ENTRY_POINT.md`.** Content must NOT change; this is a tracking operation only.

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
git hash-object spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md   # record BEFORE
git add spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md
git hash-object spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md   # MUST equal BEFORE
```

**Do NOT commit `SRC`** — it is already committed at `0fc821a` and its working tree is clean.
Confirm with `git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` (expect empty output).

**D — two-step review, THEN commit.** Per `ROADMAP.md` Standing constraints and the project's
`HND-05` obligation, this artifact passes the two-step review BEFORE its commit. Run Reality
Checker plus one named specialist in parallel over `INVENTORY.md`, resolve BLOCKER/MAJOR
findings, and record the review date — plan 01-05 Task 3 will require `review_date <
commit_date` for this artifact. Then:

```
git add spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md
git commit -m "docs(01): inventory sweep, pin register, citation protocol, SRC errata

Closes NOT-08. Pins SRC/DOC/UNITS/TEX/ENTRY_POINT by git hash-object.
Records that VOLATILITY_INSTRUMENTS_MEV.tex is not a superset of SRC
(five -F probes, zero hits, error-free output). Fixes five citation rules
plus a re-pin procedure. Records all three SRC errata as CLOSED AT SOURCE (their loci were deleted
at cf386de) plus INV-C1, Rule 13's changed signature, which closes open
item O3 and re-locates O4. SRC is this project's own file and is not
edited because editing it would invalidate every citation issued against
it -- which has now happened twice.
ENTRY_POINT.md moves from untracked to tracked, content unchanged.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

Then append the commit sha to §2 as `**Commit that pinned this register:** <sha>` and make a
second small commit for that line.

**Do NOT** `git add` anything under `WT/.planning/`, `PLANK/`, `LEAN/`, `TODO.md`, or
`spec/01_STATE_DELTA_ELASTICITY_CONTROLLER/`. Stage paths explicitly; never `git add -A`.
  </action>
  <acceptance_criteria>
    - `INVENTORY.md` contains `## 9. Errata and corrections against SRC`, `## 10. Artifact ownership`, and is ≥ 130 lines.
    - It contains the literal string `BINDING STRUCK-LOCUS RULE` and the literal string `BINDING READ-THROUGH RULE — RETIRED, AND WHY.`
    - It does NOT contain the string `[ERRATUM INV-E` anywhere — **there is no live erratum left to tag, and issuing a tag for a deleted locus is the exact defect §9 now exists to prevent.**
    - It contains the literal string `editing it invalidates every` (the correct no-fix rationale).
    - §9 is a THIN table (`ID | Locus @ 45aeba4c | Verdict | Analysis →`) plus `### 9.1`, `### 9.2`, `### 9.3`, `### 9.4` sections; no §9 table row exceeds 200 characters.
    - It contains `INV-E1`, `INV-E2`, `INV-E3`, `INV-C1` and does NOT contain the string `CF-TBD`.
    - **No §9 row is statused `STILL LIVE`**: the string `STILL LIVE` appears nowhere in the file.
    - §9.1 names `DOC Rule 6` as the RE-AIM target and contains `Any artifact needing the definition of \pi^{\phi} cites DOC Rule 6, never SRC.`
    - §9.2 names `Theorem 31` as the RE-AIM target, contains the literal string `MOOT`, and does NOT contain `Reading A`, `Reading B` or `Reading C` — the three-reading analysis is withdrawn, not merely unused.
    - §9.3 is marked `RESOLVED-BEFORE-PHASE-1` and names `c521af5`; the file does NOT contain the string `[ERRATUM INV-E3`.
    - §9.4 (`INV-C1`) quotes BOTH `Rule 13` signatures, states `O3` is **CLOSED AT SOURCE** naming `cf386de`, states `O4` is **STILL OPEN** and re-located to `Rule 13` vs `Definition 32`, and routes O4 to `NOT-05`.
    - The §8 2026-08-09 sweep contains all of `SRC:111`, `SRC:113`, `SRC:115`, `SRC:123`, `SRC:209`, `SRC:226-234` in its `STRUCK` table, and `Definition 32`, `Definition 33`, `Definition 35`, `Rule 13`, `Theorem 31`, `Definition 36`, `Proposition 13` as blocks; the literal string `STRUCK` appears at least four times.
    - It contains `### 10.1 Standing artifact checks`, `Check A —`, `Check B —` and `Check B′ —`.
    - **Check A passes live:** `grep -oE '(UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' INVENTORY.md | grep -v ' @ '` returns nothing.
    - **Check B passes live:** every `SRC:NNN` occurrence outside the `## 8`…`## 10` span is `SRC:2` or `SRC:4-10`.
    - It contains the literal string `> Authoritative detail:` and at least 14 rows in the §10 table.
    - It does NOT contain the string `PEER-ADJACENT`.
    - `git ls-files spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md` prints that path.
    - `git ls-files spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` prints that path.
    - `git hash-object spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md` returns `db70caf2244718a6b3d68e685174080f20b40fe2` (content unchanged by tracking).
    - `git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` prints nothing, and `git log -1 --name-only` does NOT list `notes/VOLATILITY_INTRUMENTS_MEV.md` (this plan did not commit SRC).
    - `git status --porcelain .planning/` prints only `?? .planning/milestones/` and `?? .planning/research/`.
    - Live `git -C plank status --porcelain` still diffs clean against the §7 sentinel block.
  </acceptance_criteria>
  <verify>
    <automated>F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md && test $(wc -l < $F) -ge 130 && for s in '## 9. Errata and corrections against SRC' '## 10. Artifact ownership' 'BINDING STRUCK-LOCUS RULE' 'BINDING READ-THROUGH RULE — RETIRED, AND WHY.' 'INV-E1' 'INV-E2' 'INV-E3' 'INV-C1' 'editing it invalidates every' 'MOOT' 'DOC Rule 6' 'Theorem 31' '> Authoritative detail:' '### 9.1' '### 9.2' '### 9.3' '### 9.4' '### 10.1 Standing artifact checks' 'Check A —' 'Check B —' 'Check B′ —'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && for bad in 'PEER-ADJACENT' 'CF-TBD' '[ERRATUM INV-E' 'STILL LIVE' 'Reading A' 'Reading B' 'Reading C'; do grep -qF "$bad" $F && { echo "FORBIDDEN: $bad"; exit 1; }; done; test $(awk '/^## 10\./,0' $F | grep -c '^|') -ge 15 && test $(awk '/^## 9\./,/^### 9\.1/' $F | awk 'length>200' | wc -l) -eq 0 && test $(grep -cF 'STRUCK' $F) -ge 4 && test -z "$(grep -oE '(UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' $F | grep -v ' @ ')" && test -z "$(sed -n '/^## 10\./,$p' $F | grep -oE 'SRC:[0-9]+(-[0-9]+)?' | grep -vE '^SRC:(2|4-10)$')" && test "$(git ls-files spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md)" = "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md" && test "$(git ls-files spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md)" = "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md" && test "$(git hash-object spec/VOLATILITY_INSTRUMENTS_MEV_TAX/ENTRY_POINT.md)" = "db70caf2244718a6b3d68e685174080f20b40fe2" && test -z "$(git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md)" && ! git log -1 --name-only --format="" | grep -q 'notes/VOLATILITY_INTRUMENTS_MEV.md' && test -z "$(git status --porcelain .planning/ | grep -v '^?? .planning/milestones/$' | grep -v '^?? .planning/research/$')" && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' $F | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && echo PASS</automated>
  </verify>
  <done>The three SRC errata are recorded as CLOSED AT SOURCE with the struck-locus rule replacing the retired read-through rule, `INV-C1` records `Rule 13`'s changed signature with O3 closed and O4 re-located to `NOT-05`, the ownership table gives every topic exactly one normative home, `ENTRY_POINT.md` is tracked with byte-identical content, `SRC` was not re-committed, and the peer trees plus repo-root `.planning/` are untouched.</done>
</task>

</tasks>

<verification>
1. `INVENTORY.md` is the oldest artifact under `SPECDIR` except `ENTRY_POINT.md`
   (`ls -t --time=birth`).
2. Every pin in §2 re-verifies via `git hash-object`, and every `Lines` cell re-verifies via
   `wc -l`.
3. Sentinel extraction: the §7 block diffs clean against live `git -C plank status --porcelain`.
4. No placeholder token leak: `SRC_SHA` / `DOC_SHA` / `UNITS_SHA` / `TEX_SHA` /
   `ENTRY_POINT_SHA` each appear at most once, inside §5 Rule 1.
5. Repo-root planning untouched: `git -C WT status --porcelain .planning/` shows only the two
   pre-existing untracked directories.
</verification>

<success_criteria>
- `NOT-08` closed: the sweep names, pins and characterises every normative artifact, including
  the three the discovery failure missed.
- `ENTRY_POINT.md` returns from `git ls-files`.
- The superset finding rests on a command that provably ran (`-F`, no `error` line).
- One legal citation form exists, with a re-pin procedure and a worked precedent behind it.
- `SRC` is correctly described as this project's own file everywhere; its three errata are
  recorded as CLOSED AT SOURCE with the struck-locus rule preventing any re-pin of a deleted
  display, and `INV-C1` records the `Rule 13` signature change with O3 closed and O4 re-located.
- Every topic has exactly one normative home, so the five artifacts cannot drift against
  each other.
</success_criteria>

<output>
After completion, create
`/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/phases/01-ground-truth-notation-and-the-rulings-triage/01-01-SUMMARY.md`
</output>
