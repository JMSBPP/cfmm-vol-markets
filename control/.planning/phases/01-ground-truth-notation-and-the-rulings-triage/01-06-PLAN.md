---
phase: 01-ground-truth-notation-and-the-rulings-triage
plan: 06
type: execute
wave: 6
depends_on: ["01-01", "01-02", "01-03", "01-04", "01-05"]
files_modified:
  - spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md
  - notes/VOLATILITY_INTRUMENTS_MEV.md
autonomous: false
requirements: [NOT-10]

must_haves:
  truths:
    - "Every landed result is EITHER reconciled in §0 as already written in SRC, OR carries a queue row saying whether it may be written now and, if not, exactly what must return before it may. No result is queued as writable when its block already exists."
    - "The eight blocks cf386de wrote are recorded as DELIVERED OUT OF ORDER — the NOT-10 cadence ran for each of them, ahead of the GSD plan that encodes it — and are reconciled retroactively rather than re-approved."
    - "Proposition 13's domain lines are audited against Proposition16_corrected_law's binders rather than assumed legitimate."
    - "Every queued block declares its conditionality — unconditional, or naming the hypothesis it rests on — before anything is written."
    - "The register's form is fixed in writing: heading, math, stop; no prose rationale in the file."
    - "The next available number in every block class is MEASURED against the shared corpus, not assumed."
    - "No block mints a symbol; the notation map governs."
    - "At most ONE block is written by this plan, and only against an explicit per-block user approval."
  artifacts:
    - path: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md"
      provides: "The §0 DELIVERED-OUT-OF-ORDER reconciliation of the eight cf386de blocks, the §0.1 Proposition 13 sign audit, the ordered block queue with gate reasons, the conditionality table, the register spec with measured numbering, the no-mint rule, and the worked precedent"
      min_lines: 140
      contains: "GATED ON PHASE 6b"
  key_links:
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md"
      to: "control/aristotle/*-result/**/*.lean"
      via: "every queue row names a declaration verified to exist in a returned bundle"
      pattern: "Theorem29_monoid_path_is_direct"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md"
      to: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md"
      via: "the no-mint rule delegates every symbol question to the map"
      pattern: "NOTATION-MAP.md"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md"
      to: "notes/VOLATILITY_INTRUMENTS_MEV.md"
      via: "the worked precedent cites Convention 7, Definitions 32-35, Rule 13 as the cadence already run"
      pattern: "Convention 7"
---

<objective>
Establish the `SRC` block programme: the ordered queue of landed results awaiting transcription,
each row's conditionality and gate, the register's exact form, the measured numbering, and the
prohibition on minting — then demonstrate the cadence by writing **at most one** block under an
explicit user approval.

Purpose: `NOT-10`. This plan does NOT transcribe the backlog. Every block needs a user approval
that an executor cannot supply, so the deliverable is the *programme*, plus one worked block.

Output: `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md`, and — only if the user
approves a specific block at the checkpoint — one new numbered block in
`notes/VOLATILITY_INTRUMENTS_MEV.md`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md

**Path glossary.** `WT` = `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller`. `SRC` =
`WT/notes/VOLATILITY_INTRUMENTS_MEV.md` (**this project's own file**; pin `33af6a85`, commit
`0fc821a`, **200 lines, 14 numbered blocks**). `DOC` = `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md`
(PEER-OWNED, READ-ONLY). `B1` =
`WT/control/aristotle/tax-result/project_aristotle/RequestProject/MevTaxControl.lean`. `B2` =
`WT/control/aristotle/tax2-result/project_aristotle/RequestProject/MevTaxProgram.lean`.

**Hard prohibitions.** Do not modify `DOC`, `PLANK/`, `LEAN/`, or `WT/.planning/`. `grep`
dispatches to `ugrep` — use `-F` for literals. Citation form and pins are owned by
`INVENTORY.md` §5/§2; symbols by `NOTATION-MAP.md` §4.

**`NOT-10`, the governing text — read it in `REQUIREMENTS.md` before writing.** Its binding
clauses:
- **Register:** heading, math, stop. Minimal prose, maximal math. **Rationale goes to the user
  in conversation, never into the file.**
- **HEAVY USER APPROVAL, one block at a time.** Each block is presented to the user *in full*
  before it is written; the user approves or revises; only then is it committed. **No batch
  writes, and no block written without an explicit approval for that block.**
- **Conditionality:** unconditional results written plainly; results resting on `H1`, `H2`,
  `hclearing` or single-crossing **name that hypothesis inside the block**.
- **Sign-gated blocks are NOT written until Phase 6 returns** `EST-03`'s verdict.
- **No symbol may be minted by a block** — `NOT-02`/`NOT-06` govern.

**This plan writes AT MOST ONE block.** That is a hard cap, enforced by an acceptance
criterion. A plan that transcribes the queue is a batch write and violates `NOT-10` directly.

**THE QUEUE'S PREMISE CHANGED AT `cf386de` — READ THIS BEFORE TASK 1.** When this plan was
written, `SRC` held six blocks and eleven results awaited transcription. Commit `cf386de` wrote
**eight more blocks into `SRC`** — `Convention 8 (Liquidity axis)`, `Theorem 29 (The monoid path
is direct)`, `Theorem 30 (Path decomposition)`, `Proposition 12 (Kernel differentiation)`,
`Theorem 31 (The section sum is ill-posed)`, `Hypothesis (H2)`, `Definition 36 (Tax program)`,
`Proposition 13 (Corrected law)` — and **struck** the `\pi^{\phi}` algebra and the boxed
`\tau^{\star}`. **Four of the seven WRITABLE-NOW rows and one of the four GATED rows are
therefore already written.** Task 1 opens with a reconciliation, not a queue: a row that says
"write this" for a block that exists is a fabricated commitment of exactly the kind Step 1's
declaration check exists to prevent.

**Those eight blocks were DELIVERED OUT OF ORDER, not written outside the cadence.** `NOT-10`
is not a new rule they escaped — it **codifies an existing practice and names the blocks already
written under it**: read its closing sentence in `REQUIREMENTS.md`, *"This is the cadence already
used for `Convention 7`, `Definitions 32–35` and `Rule 13`."* That sentence is the artifact
evidence and it is checkable.

**How the `cf386de` eight were produced is recorded as TESTIMONY, not as evidence, and must be
written that way.** Per a **user ruling of 2026-08-09**, they were produced the same way — a
working session in which each block was presented in full and individually approved, revised or
rejected. **No approval log exists for them; this plan is what creates one.** So `## 0` attributes
the account (`per user ruling 2026-08-09`) rather than asserting it flat. **The cadence ran; the
GSD plan that encodes it had not executed yet.** This is the pattern `ROADMAP.md` already records for Phases 4 and 5 — delivered
out of order by direct submission, ahead of Phases 1–3 — and it does not treat that as a
violation. `## 0` therefore **records them retroactively; it does not re-approve them and does
not put a governance question to the user.**

**The stale-commitment problem is real and is separate.** Whatever cadence produced them, this
plan's queue still lists four WRITABLE-NOW rows and one GATED row for blocks that now exist. A
row that says "write this" for a block already on the page is a fabricated commitment. That is
what `## 0` reconciles.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile what cf386de already wrote, then build the queue — writable-now vs gated, with a reason per row</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md` — `NOT-10` in full, and `EST-03` (whose verdict gates the sign rows)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/aristotle/tax-result/project_aristotle/ARISTOTLE_SUMMARY.md` and the tax2 equivalent
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/aristotle/tax2-result/project_aristotle/RequestProject/TAX2_BUNDLE_NOTES.md` (the M24 audit table)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md` — the `USER RULING 2026-08-08 — the objective is EXPOSURE MINIMIZATION` block added at commit `acebde3`
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md` (CF-26…CF-30 already carry these results)
  </read_first>
  <action>
Create `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md`.

Header, verbatim except the date:

```
# SRC BLOCK PROGRAMME

**Requirement:** NOT-10
**Written:** <YYYY-MM-DD>
**Target file:** `notes/VOLATILITY_INTRUMENTS_MEV.md` (`SRC`) @ `33af6a85`, commit `0fc821a` —
**200 lines, 14 numbered blocks.** Eight of those blocks were **delivered out of order** at
`cf386de`: `NOT-10`'s cadence ran for each of them, ahead of the plan that encodes it. See
`## 0`. **This header is the first thing a reader sees — it must not describe them as written
outside the cadence.**

> Authoritative detail: citation form and pins — `INVENTORY.md` §5, §2. Symbols —
> `NOTATION-MAP.md` §4. Finding IDs — `FINDING-REGISTER.md`. This document restates none.

**This is a QUEUE, not a transcription.** `NOT-10` requires HEAVY USER APPROVAL, one block at a
time, each presented in full before it is written. Nothing below is written by building this
queue. **At most one block is written by plan 01-06, at its checkpoint, against an explicit
approval naming that block.**
```

**Step 1 — verify every declaration exists before you queue it.** A queue row naming a
declaration that is not in a returned bundle is a fabricated commitment. Run and paste:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
for d in Theorem29_monoid_path_is_direct Theorem30_composed_fee_submersion_section_sum_ill_posed \
         Theorem32_hazard_strictAntiOn_tau Theorem33_path_decomposition \
         Theorem36_no_interior_root_off_the_band tau_to_nu_strictAntiOn_under_H2 \
         Theorem34_opposed_signs Proposition16_corrected_law Proposition16_audit_justification \
         Proposition15_level_reading_second_order_undetermined M19_FOC_is_not_feasibility; do
  printf "%-56s lean_files=%s\n" "$d" "$(grep -rlF "$d" --include='*.lean' control/aristotle | wc -l)"
done
```

At plan time every one returned ≥ 1. **Any row returning 0 is DROPPED from the queue with the
reason recorded** — do not queue it and do not silently rename it.

**Step 1b — `## 0. ALREADY WRITTEN — DELIVERED OUT OF ORDER, reconciled against `cf386de``
(NEW, and it comes first).** Measure what is in `SRC` before queueing anything:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
grep -nE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' notes/VOLATILITY_INTRUMENTS_MEV.md
git log --oneline -3 -- notes/VOLATILITY_INTRUMENTS_MEV.md
```

Expect **14** blocks at `33af6a85` and `0fc821a` as the tip. Record the count verbatim as
`**pre-task block count: <N>**` — Task 3's cap check reads that string.

Then a table `Block (as written in SRC) | Bundle tag | Corresponding declaration | Was queued as
| Cadence`:

| Block | Was queued as | Note |
|---|---|---|
| `Convention 8 (Liquidity axis) [M18]` | **not queued** | ratifies user ruling R1 in the source; carries the two axes and `\partial L(i_K)/\partial\pi^{\phi} = \ell\,\partial\bar L/\partial\pi^{\phi}` |
| `Theorem 29 (The monoid path is direct) [M12]` | **W1** | `Theorem29_monoid_path_is_direct` |
| `Theorem 30 (Path decomposition) [M20]` | **W3** | `Theorem33_path_decomposition` — **note the doc number and the Lean identifier's number DIFFER (doc 30, Lean `Theorem33_*`). Record that pairing explicitly; a later reader must not infer `Theorem 33` exists in the doc.** |
| `Proposition 12 (Kernel differentiation) [M20]` | **not queued** | carries assumption `(A1)` and the three `\widehat\pi^{\sigma}` derivatives |
| `Theorem 31 (The section sum is ill-posed) [M13]` | **W2** | `Theorem30_composed_fee_submersion_section_sum_ill_posed` — **doc 31, Lean `Theorem30_*`. Same number-mismatch warning.** |
| `Hypothesis (H2) [M18]` | **not queued** | UNNUMBERED block; states `\bar{\mathcal{G}}_{(\nu,\lambda_{\text{MEV}})} := \partial\nu/\partial\lambda_{\text{MEV}} > 0` |
| `Definition 36 (Tax program) [M19]` | **W6** | `M19_FOC_is_not_feasibility`; objective `\min_{\tau_{\text{MEV}}\in[0,1]}(\partial\widehat\pi^\sigma/\partial\tau_{\text{MEV}})^2` s.t. `\pi^\sigma = \widehat\pi^\sigma` |
| `Proposition 13 (Corrected law) [M24]` | **G3, unconditional half** | `Proposition16_corrected_law` — **doc 13, Lean `Proposition16_*`** |

For every row, quote the block verbatim from `SRC` and state whether what is on the page matches
the declaration it is attributed to. **Where it does not, that is a defect against the block, not
a note.**

Then, verbatim:

```
**DELIVERED OUT OF ORDER — the cadence ran, the plan had not.** All eight blocks above were
written into `SRC` at commit `cf386de` under `NOT-10`'s own cadence: **per user ruling
2026-08-09**, each was presented to the user in full, one block at a time, and approved, revised
or rejected individually. **That account is testimony, not an artifact — no approval log exists
for those eight, because this document is what creates one — and it is recorded here as an
attributed ruling rather than as evidence.** What had not happened yet is the GSD plan that
ENCODES that cadence — this document. `NOT-10` codifies an existing
practice rather than introducing a new one, and says so in its own closing sentence: *"This is
the cadence already used for `Convention 7`, `Definitions 32–35` and `Rule 13`."*

**This is the same disposition `ROADMAP.md` records for Phases 4 and 5** — delivered out of
order by direct submission, ahead of Phases 1-3, with the inversion stated rather than treated
as a violation. The eight blocks are therefore **recorded here retroactively and are NOT
re-approved**. No governance question arises from them and none is put to the user.

**What this section IS for:** reconciling the queue. Four rows this programme carried as
WRITABLE NOW and one carried as GATED name blocks that already exist. Those rows are retired in
§1 and §2. **A queue row that commits to writing a block already on the page is a fabricated
commitment**, and that is a defect regardless of how the block was produced. (Write that bolded
sentence on ONE line — an acceptance criterion greps it verbatim.)
```

**Step 1c — `## 0.1 The `Proposition 13` hypothesis audit (do this before §2 is written)`.**

**FIRST: check the live pin. `SRC` moved three times while this plan was being written and the
block this section audits is the one most likely to have moved again.**

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
git hash-object notes/VOLATILITY_INTRUMENTS_MEV.md
git log --oneline -3 -- notes/VOLATILITY_INTRUMENTS_MEV.md
sed -n '/Proposition 13 (Corrected law)/,/^\\]/p' notes/VOLATILITY_INTRUMENTS_MEV.md
```

**If the blob is not `33af6a85`, the audit below still applies but its EXPECTED FINDING does
not.** Record the new pin, run `INVENTORY.md` §8's re-pin procedure for it, and classify against
**the text you just printed** — never against the quotation in this plan. Known at plan time: a
later commit may have split `Proposition 13`'s conditional and unconditional parts, which is
remedy **option 1** below and would settle this audit at source. **If that is what you find,
§0.1's job is to RECORD the ruling, not to re-raise the question** — write the classification,
name the commit, mark the defect closed, and carry nothing to the checkpoint but the record.

`Proposition 13` carries three lines plus one rider **at pin `04bac0a5`**. Quote all four **as
they stand at the live pin** and classify each:

    ∂π̂^σ/∂τ_MEV = 0  ⟺  τ*_MEV = 1 + (1−φ_X)/((∂φ/∂ν)(∂ν/∂τ_MEV))
    τ*_MEV < 1
    τ*_MEV > 0  ⟺  1−φ_X < |(∂φ/∂ν)(∂ν/∂τ_MEV)|
    rider: "All of φ_X, ∂φ/∂ν, ∂ν/∂τ_MEV evaluated at ν(τ*_MEV)."

**The gated absolute-value READING (`τ* = 1 − (1−φ_X)/|·|`) does NOT appear as the law.** Confirm
with `grep -cF 'tau^{\star}_{\text{MEV}} \; = \; 1 \; - \;' notes/VOLATILITY_INTRUMENTS_MEV.md`
→ 0. Record that much of the G3 split as honoured at source.

**Now read the binders. Do not classify from the doc alone.** Open
`control/aristotle/tax2-result/project_aristotle/RequestProject/MevTaxProgram.lean` at
`Proposition16_corrected_law` (`grep -nF 'theorem Proposition16_corrected_law'`) and **quote the
signature verbatim**, including its conjunct structure. At plan time it reads:

```
theorem Proposition16_corrected_law
    (A phiM tauMEV phiXv dphidnu dnudtau : ℝ)
    (hA : A ≠ 0) (hM : phiM ≠ 1) (hg : dphidnu * dnudtau ≠ 0) :
    (totalDeriv … = 0 ↔ tauMEV = 1 + (1 - phiXv) / (dphidnu * dnudtau))        -- CONJUNCT 1
    ∧ (0 < dphidnu → dnudtau < 0 → phiXv < 1 →                                  -- CONJUNCT 2, GUARDED
        ((totalDeriv … = 0 ↔ tauMEV = 1 - (1 - phiXv) / |dphidnu * dnudtau|)
         ∧ (tauMEV = 1 + … → tauMEV < 1)
         ∧ (tauMEV = 1 + … → (0 < tauMEV ↔ 1 - phiXv < |dphidnu * dnudtau|))))
```

**THREE-WAY classification. A binary (a)/(b) is WRONG here and an earlier draft of this plan
carried one — it is recorded so the error is not repeated:** that draft said case (a) meant the
domain lines "inherit its binders and are as unconditional as line 1", which routed to no defect
at all. **Line 1 is conjunct 1; lines 2-3 are conjunct 2. They do not share a hypothesis set.**
Classify each line into exactly one of:

- **(a) CARRIED UNGUARDED** — the line is conjunct 1, or is otherwise in the statement with no
  antecedent the doc drops. No defect.
- **(b) NOT CARRIED** — the line is a doc-side addition with no counterpart in the declaration.
  A claim on the page with no carrier: defect, `## 3` entry, checkpoint question.
- **(c) CARRIED UNDER A DROPPED ANTECEDENT** — the line IS in the declaration, but under an
  antecedent the doc block does not state. **This is a defect too, and it is the one the binary
  hid.** It routes to the `## 3` entry and the checkpoint question exactly as (b) does. Being
  "in the statement" is not a discharge; being *unconditionally* in the statement is.

**At pin `04bac0a5` the expected classification was (c) for lines 2 and 3** — they sat in
conjunct 2, under the same guard as the absolute-value reading that §2's G3 row bans as
*"smuggles a sign claim into the doc as an identity"*, while the block stated no antecedent at
all. **That expectation is pin-specific. Verify against the binders AND the live block text; do
not inherit this sentence.** If the block now states the antecedents explicitly, the correct
classification is **(a) for every line** and the defect is CLOSED AT SOURCE — record it that way
with the fixing commit named.

**Record the dropped antecedents in TWO separate families — they route differently:**

| Family | Antecedents | Why it matters | Routes to |
|---|---|---|---|
| **Sign** | `0 < dphidnu`, `dnudtau < 0` | these are the M21 signs `EST-03` is asked to establish; a doc line resting on them is a **G2-class sign claim ahead of its gate** | §2 G-rows / `EST-03` (Phase 6b) |
| **Non-sign** | `phiXv < 1` (conjunct 2); `hA : A ≠ 0`, `hM : phiM ≠ 1`, `hg : dphidnu*dnudtau ≠ 0` (whole theorem) | **NOT sign claims and NOT `EST-03`'s business.** `phiXv < 1` is what `τ* < 1` actually needs. `hg` is load-bearing on **line 1 as well**: without it the `⟺` fails at a zero gate, so even the "unconditional" line is Lean-conditional | `HND-01` gap register |

**Residual after a ruling that states the antecedents (record it, do NOT escalate it).** If the
block now carries `hg` inline and the three conjunct-2 antecedents in a guard, then `hA : A ≠ 0`
and `hM : phiM ≠ 1` are the only binders still unstated. They are **structural non-degeneracies
of the plant, not sign claims**, so the doc remains *marginally* weaker-hypothesised than the
declaration — a much smaller gap than the one just closed. Add it to `HND-01` as a **MINOR** in
exactly that framing, alongside the non-sign family above. **Do not escalate it and do not put it
to the user.**

**State the net finding plainly, in these terms:** `Proposition 13` as written states **no
hypotheses at all** beyond the evaluation-point rider — not the signs, not `phiXv < 1`, not
`hA`/`hM`/`hg`. **The doc block is weaker-hypothesised than the declaration it transcribes, which
is the dangerous direction:** a reader can discharge the doc's conditions and still not have the
theorem's. Record that sentence verbatim in `## 3`.

**This audit RECORDS; it does not remedy, and it does not touch `SRC`.**
`Proposition 13` is a `NOT-10` block and **is the user's to rule on**. If the audit finds the
defect still open, write it into `## 3` as a defect entry against `Proposition 13`, carry it to
`HND-01`, and put it at Task 3's checkpoint with the remedy options **as options, not as a
decision**. If the audit finds it already settled at source, write the closure into `## 3`
instead — naming the commit and which option the ruling took — and **put nothing to the
checkpoint**. The options:

1. attach the missing antecedents to the block explicitly (the `01-06` §2 G3 row already names
   this remedy — "attach the M21 sign precondition to them explicitly");
2. strike lines 2-3 until `EST-03` returns and keep line 1 with `hg` stated;
3. leave as written and record the gap in `HND-01`.

**Do NOT edit `SRC` in this task**, and do not pre-empt the ruling by writing a preferred option
into the artifact. If the user rules a revision, that revision is its own block-turn under
`NOT-10`'s cadence; §0.1 records the ruling and §6's approval log carries it.

**Step 2 — `## 1. WRITABLE NOW`.** Table:
`# | Block (proposed heading) | Class | Landed declaration(s) | Conditionality | Finding ID`.
**Only rows whose block is NOT already in `SRC` may appear here.** Rows (verify each; the order
is the proposed write order):

| # | Block | Class | Declaration | Conditionality |
|---|---|---|---|---|
| W4 | The arbitrage hazard is strictly antitone in `\tau_{\text{MEV}}` | Theorem | `Theorem32_hazard_strictAntiOn_tau` | check — read the signature | CF-26 |
| W5 | No interior root off the band | Theorem | `Theorem36_no_interior_root_off_the_band` | check — read the signature | CF-22 |
| W7 | Audit of the source's boxed `\tau^\star_{\text{MEV}}` | Proposition | `Proposition16_audit_justification` | unconditional **MINUS the `\partial\nu/\partial\tau` row** | CF-28 |

**W1, W2, W3 and W6 are RETIRED from this queue** — their blocks are `Theorem 29`, `Theorem 31`,
`Theorem 30` and `Definition 36`, already in `SRC` at `cf386de`. Record them in `## 0` with their
reconciliation, and **state in §1 verbatim, on one line**:

    Four rows an earlier version of this queue carried as WRITABLE NOW (W1, W2, W3, W6) were delivered out of order at cf386de. They are reconciled in §0 and are NOT re-queued. Queueing a block that already exists would be a fabricated commitment.

**W7 needs a number and the numbering question is now settled — see §4B.** The audit is a
`Proposition`; the next unused doc Proposition after `SRC`'s `Proposition 12` and `Proposition 13`
is **14**, subject to §4B's re-measurement and the orphaned-12-to-14 note.

For W7, state explicitly: **the M24 audit table is written MINUS the `\partial\nu/\partial\tau`
`SIGN CORRECTED` row**, which is queued in §2. Eight rows now, one gated. A block that writes
all nine is a `NOT-10` violation.

**For each of W4, W5, W7, open the declaration in its `.lean` file and read its hypotheses.**
If it takes `H1`, `H2`, `hclearing` or a single-crossing hypothesis, it is CONDITIONAL and the
block must name that hypothesis inside itself — record which. Do not write "unconditional"
without having read the signature.

**Step 3 — `## 2. GATED ON PHASE 6b (`EST-03`'s sign verdict)`.** Same columns plus
`Gate reason` and `Unblocked when`. **`EST-03` lives in Phase 6b** after the 2026-08-09 split of
Phase 6 into 6a (`NEC-*`) and 6b (`LIT-*` + `EST-*`) — the section heading says `PHASE 6b` for
that reason. Keep `EST-03` as the gate identifier; it did not move.

| # | Block | Declaration | Gate reason | Unblocked when |
|---|---|---|---|---|
| G1 | `\tau \to \nu` is strictly antitone under `H2` | `tau_to_nu_strictAntiOn_under_H2` | the statement is CONDITIONAL ON `H2`, and `H2` is a sign hypothesis on the behavioral gain — an estimand, not a proposition (`PRF-03`) | `EST-03` (Phase 6b) returns a sign verdict for `\partial\nu/\partial\lambda_{\text{MEV}}` |
| G2 | Opposed signs | `Theorem34_opposed_signs` | the whole content IS a sign claim; writing it before `EST-03` asserts on the page what the project has declared unproved | as G1 |
| G3 | The corrected law's `1 -` sign READING | `Proposition16_corrected_law` | **the law's FORM is UNCONDITIONAL, was written at `cf386de` as `Proposition 13`, and is NOT gated** — see the split below and the §0.1 audit | as G1, for the reading only |
| G4 | The `\partial\nu/\partial\tau` `SIGN CORRECTED` audit row | `Proposition16_audit_justification` | it is a sign verdict, so it inherits `EST-03` | as G1 |

**Note that `Hypothesis (H2)` is already in `SRC`, and that this does NOT unblock G1.** The
source states the hypothesis; `EST-03` is what discharges or refutes it. A block asserting a
consequence OF `H2` is still gated. Say so verbatim in G1's row.

**G3 needs its split written out explicitly — this is the row most likely to be got wrong.**
State verbatim:

```
**G3 is a SPLIT row, not a whole-block gate, and its unconditional half is ALREADY WRITTEN.**
The corrected law has two parts and only one is gated:

- **UNCONDITIONAL — WRITTEN at `cf386de` as `Proposition 13 (Corrected law) [M24]`.** The
  signed-denominator form, exactly as `Proposition16_corrected_law` states it:

      ∂π̂^σ/∂τ_MEV = 0  ⟺  τ*_MEV = 1 + (1-φ_X)/((∂φ/∂ν)(∂ν/∂τ_MEV))

  with a **`+`** and a **signed** denominator. This carries no sign claim; it is an identity.
  Verify it is on the page: `grep -nF 'tau^{\star}_{\text{MEV}} \; = \; 1 \; + \;'` → 1 hit.

- **GATED — not written until `EST-03` returns.** The absolute-value reading
  `τ*_MEV = 1 - (1-φ_X)/|(∂φ/∂ν)(∂ν/∂τ_MEV)|`. It is equivalent to the form above ONLY IF
  `∂ν/∂τ_MEV < 0`, which is exactly what `EST-03` is being asked to establish. **Writing the
  `1 -` reading before that verdict smuggles a sign claim into the doc as an identity.** Verify
  it is NOT on the page: `grep -cF 'tau^{\star}_{\text{MEV}} \; = \; 1 \; - \;'` → 0.

The DOMAIN lines (`τ*_MEV < 1`; `τ*_MEV > 0` iff `1-φ_X < |(∂φ/∂ν)(∂ν/∂τ_MEV)|`) **are also on
the page as of `cf386de`, and whether that was legitimate is settled by the §0.1 binder audit,
not asserted here.**
```

**Step 4 — `## 3. Not queued (and why)`.** Every landed result NOT in §1 or §2 gets a row with
its reason. Include at minimum
`Proposition15_level_reading_second_order_undetermined` — it is a **negative** result (the FOC
root is not established to be the minimiser; the settling single-crossing hypothesis is used
but never discharged), so it belongs in the gap register (`CF-29`, `HND-01`), not as a doc
block asserting something. State that reasoning.
  </action>
  <acceptance_criteria>
    - `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md` exists and is ≥ 100 lines at end of this task.
    - It contains the headings `## 0. ALREADY WRITTEN`, `## 0.1`, `## 1. WRITABLE NOW`, `## 2. GATED ON PHASE 6b`, `## 3. Not queued`.
    - **§0 reconciles all eight `cf386de` blocks by name:** `Convention 8`, `Theorem 29`, `Theorem 30`, `Proposition 12`, `Theorem 31`, `Hypothesis (H2)`, `Definition 36`, `Proposition 13`; and records `**pre-task block count: 14**` (or the measured value).
    - §0 contains `DELIVERED OUT OF ORDER — the cadence ran, the plan had not.`, quotes `NOT-10`'s closing sentence containing `This is the cadence already used for`, and contains `A queue row that commits to writing a block already on the page is a fabricated commitment`.
    - **The file NOWHERE frames the eight blocks as a violation — header included.** `grep -niE 'outside (this|the|its)( [a-z]+)? cadence|cadence bypass|bypassed the cadence|Back-dating'` returns nothing. The header at the top of the file is checked by the same pattern, because the earlier `outside this cadence` ban was slipped by the variant `outside this programme's cadence`. (The bare word `bypassing` is NOT banned — `Theorem 29`'s own content is that `τ_MEV` reaches the fee *bypassing* `ν`, and the queue rows legitimately say so.)
    - §0 records the doc-number / Lean-identifier mismatches: the file contains `doc 30, Lean `Theorem33_*``, `doc 31, Lean `Theorem30_*`` and `doc 13, Lean `Proposition16_*``.
    - **§0.1 exists and quotes `Proposition 13`'s three lines plus the rider, AND the verbatim signature of `Proposition16_corrected_law` including its two-conjunct structure.** It contains `grep -cF 'tau^{\star}_{\text{MEV}} \; = \; 1 \; - \;'` with its recorded `0`.
    - **§0.1 classifies THREE ways, not two:** it contains `(a) CARRIED UNGUARDED`, `(b) NOT CARRIED` and `(c) CARRIED UNDER A DROPPED ANTECEDENT`, and states that being in the statement is not a discharge. Every one of lines 1-3 carries exactly one classification.
    - §0.1 separates the dropped antecedents into a **Sign** family (`0 < dphidnu`, `dnudtau < 0`) routed to `EST-03`, and a **Non-sign** family (`phiXv < 1`, `hA`, `hM`, `hg`) routed to `HND-01`; it names `phiXv < 1` and `hg` explicitly and states that `hg` is load-bearing on line 1 too.
    - §0.1 records the `hA`/`hM` residual as an `HND-01` **MINOR** framed as structural non-degeneracies, not sign claims, and explicitly **not escalated and not put to the user**.
    - §0.1 records the net finding verbatim: `The doc block is weaker-hypothesised than the declaration it transcribes, which is the dangerous direction`.
    - **§0.1 records and does not remedy:** it contains `This audit RECORDS; it does not remedy, and it does not touch `SRC`.` and presents three remedy options as options. `git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` prints nothing at the end of this task.
    - **§1 has exactly 3 data rows (`W4`, `W5`, `W7`) and contains NONE of `W1`, `W2`, `W3`, `W6` as queue rows**; it contains the retirement sentence `were delivered out of order at cf386de. They are reconciled in §0 and are NOT re-queued.`. §2 has exactly 4 (`G1`…`G4`); count `^|` lines excluding separator rows matching `^\|[- :|]*$`, minus the header.
    - §2's `G1` row states that `Hypothesis (H2)` being in `SRC` does **not** unblock it.
    - Every §2 row's `Gate reason` cell is non-empty and every row names `EST-03`.
    - It contains the declaration-verification command's pasted output, and every declaration named in §1/§2 shows `lean_files` ≥ 1.
    - It contains the literal string `G3 is a SPLIT row, not a whole-block gate.`
    - It contains the literal string `1 + (1-φ_X)/((∂φ/∂ν)(∂ν/∂τ_MEV))` AND the literal string `smuggles a sign claim into the doc as an identity`.
    - W7's row states `MINUS the `\partial\nu/\partial\tau`` — the audit block is 8 rows, not 9.
    - §3 contains `Proposition15_level_reading_second_order_undetermined` with `HND-01`.
    - `git -C WT status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` prints nothing — no block was written by this task.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md && test -f $F && test $(wc -l < $F) -ge 100 && for s in '## 0. ALREADY WRITTEN' '## 0.1' '## 1. WRITABLE NOW' '## 2. GATED ON PHASE 6b' '## 3. Not queued' 'G3 is a SPLIT row, not a whole-block gate' '1 + (1-φ_X)/((∂φ/∂ν)(∂ν/∂τ_MEV))' 'smuggles a sign claim into the doc as an identity' 'Proposition15_level_reading_second_order_undetermined' 'HND-01' 'Theorem29_monoid_path_is_direct' 'tau_to_nu_strictAntiOn_under_H2' 'DELIVERED OUT OF ORDER — the cadence ran, the plan had not.' 'This is the cadence already used for' 'per user ruling 2026-08-09' '(a) CARRIED UNGUARDED' '(b) NOT CARRIED' '(c) CARRIED UNDER A DROPPED ANTECEDENT' 'phiXv < 1' 'hg' 'The doc block is weaker-hypothesised than the declaration it transcribes, which is the dangerous direction' 'A queue row that commits to writing a block already on the page is a fabricated commitment' 'pre-task block count:' 'Convention 8' 'Theorem 29' 'Theorem 30' 'Proposition 12' 'Theorem 31' 'Hypothesis (H2)' 'Definition 36' 'Proposition 13' 'doc 30, Lean `Theorem33_*`' 'doc 31, Lean `Theorem30_*`' 'doc 13, Lean `Proposition16_*`' 'were delivered out of order at cf386de. They are reconciled in §0 and are NOT re-queued.'; do grep -qF -- "$s" $F || { echo "MISSING: $s"; exit 1; }; done && ! grep -qiE 'outside (this|the|its)( [a-z'"'"']+)? cadence|cadence bypass|bypassed the cadence|Back-dating' $F; test $(awk '/^## 1\. WRITABLE NOW/,/^## 2\./' $F | grep '^|' | grep -vcE '^\|[- :|]*$') -eq 4 && test $(awk '/^## 1\. WRITABLE NOW/,/^## 2\./' $F | grep -cE '^\| *W[1236] ') -eq 0 && test $(awk '/^## 2\. GATED/,/^## 3\./' $F | grep '^|' | grep -vcE '^\|[- :|]*$') -eq 5 && test $(awk '/^## 2\. GATED/,/^## 3\./' $F | grep -cF 'EST-03') -ge 4 && test -z "$(git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md)" && echo PASS</automated>
  </verify>
  <done>§0 reconciles the eight blocks `cf386de` delivered out of order — the cadence ran ahead of the plan that encodes it, exactly as Phases 4 and 5 were delivered ahead of Phases 1-3 — and retires the four stale WRITABLE-NOW rows and one stale GATED row that named them; §0.1 audits `Proposition 13`'s domain lines against `Proposition16_corrected_law`'s binders rather than assuming; the queue carries THREE writable rows (W4, W5, W7) and four gated rows each naming `EST-03`; the G3 form/reading split is written out with the unconditional half recorded as already on the page; every declaration is verified present in a returned bundle; `SRC` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: The register spec — form, measured numbering, conditionality, no-mint, precedent</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` — the numbering note at `SRC:4-10` (it GREW at `cf386de`; the old `SRC:4-7` range is stale), and **all 14 blocks** as written
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md` (READ ONLY) — to measure the shared corpus's current maxima
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md` §4 (the symbol register the no-mint rule delegates to)
  </read_first>
  <action>
Append `## 4. Register spec`.

**A — the form.** Verbatim:

```
**Heading, math, stop.** A block is: a bold numbered heading with a short parenthetical name
and its bundle tag; the mathematics in a display; nothing else. Minimal prose, maximal math.
**Rationale goes to the user in conversation and NEVER into the file.** If a block needs a
paragraph of justification to be understood, that paragraph belongs in the checkpoint message,
not in `SRC`.

A block MAY carry: a one-line `*Status:*` or conditionality clause naming its hypothesis, and a
bracketed bundle tag (`[M11]`, `[M19]`, …) as the existing blocks do. A block MAY NOT carry:
motivation, discussion of alternatives, or a proof sketch.
```

**B — the numbering, MEASURED.** Do not trust any list of "next available" numbers, including
the one in `NOT-10` and the one in `SRC:4-10`. Run and paste:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
echo "SRC:"; grep -oE '\*\*(Convention|Definition|Theorem|Proposition|Rule) [0-9]+' notes/VOLATILITY_INTRUMENTS_MEV.md | sort -u
echo "DOC:"; P=/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md
for k in Convention Definition Theorem Proposition Rule; do printf "%-12s: " "$k"; grep -oE "\*\*$k [0-9]+" $P | grep -oE '[0-9]+' | sort -n | uniq | tr '\n' ' '; echo; done
```

Plan-time measurement at `33af6a85` / `0c5ce574`: `SRC` holds `Convention 7`, `Convention 8`,
`Definitions 32-36`, `Rule 13`, `Theorems 29-31`, `Propositions 12-13`, and one unnumbered
`Hypothesis (H2)`. `DOC` holds `Convention` ≤ 6, `Definition` ≤ 31 (16 absent), `Theorem` ≤ 28
(7 absent), `Proposition` 2-11, `Rule` ≤ 12. Record a
`Class | max in DOC | held in SRC | NEXT AVAILABLE` table from YOUR measurement.

**The numbering discrepancy this section used to carry has been RESOLVED AT SOURCE. Record the
resolution; do not re-raise it as OPEN.** Verbatim:

```
**RESOLVED AT SOURCE (`cf386de`).** At pin `45aeba4c` the numbering note reserved
`Proposition 15+`, which disagreed with the corpus: `DOC`'s Propositions run 2…11, so the next
unused number is **12**. This programme's earlier draft recorded that as OPEN, listed two
readings — (a) `12-14` deliberately reserved by the peer, or (b) the note off by three — and
instructed the executor not to pick a number.

**Reading (b) was adopted at source.** The numbering note at `SRC:4-10 @ 33af6a85` now reads
`Next unused: Convention 8, Definition 36, Theorem 29, Proposition 12, Rule 14` and states the
correction in its own words: *"(`Proposition` corrected from an earlier `15+` reservation: the
entry-point doc's Propositions run 2–11, so 12 is next and 12–14 were orphaned by the error.)"*
`SRC` then consumed `Proposition 12` and `Proposition 13`. Quote the note verbatim.

**What remains, and it is smaller:** the note says 12–14 "were orphaned by the error" while the
source has since used 12 and 13. Whether 14 is free is not stated.
**Do not pick 14 for W7 without confirming.** (One line — an acceptance criterion greps it.)
The question is a one-line ask to `ul2inqpl` and rides on plan 01-05's
Message A; carry it to `HND-01` if unanswered. The returned bundles' `Proposition15_*` and
`Proposition16_*` are LEAN IDENTIFIER names, are not doc numbers, and reserve nothing.

**Also record, because it is the same class of hazard:** three `SRC` blocks carry doc numbers
that differ from their Lean identifiers' numbers — doc `Theorem 30` ← `Theorem33_*`, doc
`Theorem 31` ← `Theorem30_*`, doc `Proposition 13` ← `Proposition16_*`. **A doc number is never
inferred from a Lean identifier.** See §0.
```

**C — conditionality.** A table `Hypothesis | What it asserts | Blocks that must name it`,
covering `H1`, `H2`, `hclearing`, and the single-crossing hypothesis. Locate each in the
bundles by content (`grep -rn 'H1\|H2\|hclearing' --include='*.lean' control/aristotle`) and
quote the statement it stands for. Then, verbatim:

```
**Rule.** A block resting on a hypothesis NAMES that hypothesis inside itself, in the block's
own math, not in a footnote and not in the surrounding prose. A reader must not be able to
quote the block's display without also quoting its hypothesis. An unconditional block is
written plainly and carries no such clause — and "unconditional" is asserted only after the
declaration's signature has been read.
```

**D — the no-mint rule.** Verbatim:

```
**No block mints a symbol.** Every symbol a block uses must already be in `NOTATION-MAP.md`
§4.1 (inherited) or §4.2 (minted, with a named ruling). If a block needs a symbol that is in
neither, the block is BLOCKED: the symbol goes to the user as a notation question first, the
map is updated, and only then is the block written. `NOT-10` sits behind `NOT-02`/`NOT-06` for
exactly this reason. A block that introduces a glyph is a defect in both requirements.

Check before presenting any block:

    grep -oE '\\[A-Za-z]+' <the block's draft text> | sort -u
    # every result must appear in NOTATION-MAP.md §4.1/§4.2/§4.3
```

**E — `## 5. Worked precedent — the FORM already demonstrated (but not the cadence)`.**
Fourteen blocks exist in `SRC`: six from `c521af5`, eight from `cf386de`. Quote **TWO** in full
and annotate the parts — the heading form, the bundle tag, the display, the absence of prose:

- `Definition 35 (Monoid gradient) [M12]` — the `c521af5` cohort, a pure display.
- `Proposition 13 (Corrected law) [M24]` — the `cf386de` cohort, and the ONLY block carrying a
  one-line conditionality rider (*"All of `\phi_X`, `\partial\phi/\partial\nu`,
  `\partial\nu/\partial\tau_{\text{MEV}}` evaluated at `\nu(\tau^{\star}_{\text{MEV}})`"*). It is
  the worked example of §4C's rule that a conditional block names its condition **inside itself**.

Also quote `Hypothesis (H2) [M18]` and record the one form question it raises: **it is the only
UNNUMBERED block**, so `§4A`'s "bold numbered heading" rule has an exception on the page.
Record the exception; do not silently generalise the rule.

State the cadence as five steps: **draft → present in full → user approves or revises → write the
single block → commit that block alone.** Then, verbatim:

```
**Retroactive note, recorded not hidden.** All fourteen blocks in `SRC` were written under this
cadence and **none of them under this document** — the practice preceded the plan that encodes
it, which is what `NOT-10`'s closing sentence records. What this programme adds is not the
cadence but its **written form, its queue, its gates and its approval log**.

One genuine retroactivity remains, and it is about symbols rather than approval: the `c521af5`
six (`Convention 7`, `Definitions 32-35`, `Rule 13`) predate `NOTATION-MAP.md`, so their glyphs
were never checked against a register that did not yet exist, and the `cf386de` eight introduced
further glyphs (`L_{\sigma}`, `\ell`, `\xi^{\star}`, the `(A1)` label) under the same condition.
That is `NOT-06`'s burden, not `NOT-10`'s, and plan 01-03 §4.6 carries it.
`> Authoritative detail: NOTATION-MAP.md` §4.6 (retroactive symbol reconciliation) and `## 0`
above (block-level reconciliation).
```
  </action>
  <acceptance_criteria>
    - `BLOCK-PROGRAMME.md` contains `## 4. Register spec` and `## 5. Worked precedent`, and is ≥ 140 lines.
    - It contains `Heading, math, stop.` and `NEVER into the file`.
    - It contains a measured numbering table with a `NEXT AVAILABLE` column, and the pasted output of BOTH measurement commands.
    - It contains `RESOLVED AT SOURCE (`cf386de`).` and quotes the numbering note's own correction sentence containing `orphaned by the error`; it does NOT contain `OPEN — the Proposition sequence has a gap`.
    - It contains `Do not pick 14 for W7 without confirming.` and `A doc number is never` together with `inferred from a Lean identifier`.
    - It contains all four hypothesis names `H1`, `H2`, `hclearing`, and `single-crossing`.
    - It contains `No block mints a symbol.` and `NOTATION-MAP.md`.
    - §5 quotes `Definition 35`, `Proposition 13` **and** `Hypothesis (H2)`, contains the five-step cadence, the literal string `Retroactive note, recorded not hidden.`, and the statement `the practice preceded the plan that encodes it`.
    - §5's retroactivity is scoped to **symbols** (`NOT-06`, plan 01-03 §4.6) and does NOT claim any block was written without approval.
    - §5 records that `Hypothesis (H2)` is the only UNNUMBERED block and that §4A's heading rule has that exception on the page.
    - `git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` still prints nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md && test $(wc -l < $F) -ge 140 && for s in '## 4. Register spec' '## 5. Worked precedent' 'Heading, math, stop.' 'NEVER into the file' 'NEXT AVAILABLE' 'RESOLVED AT SOURCE (`cf386de`).' 'orphaned by the error' 'Do not pick 14 for W7 without confirming.' 'inferred from a Lean identifier' 'H1' 'H2' 'hclearing' 'single-crossing' 'No block mints a symbol.' 'NOTATION-MAP.md' 'Definition 35' 'Proposition 13' 'Hypothesis (H2)' 'Retroactive note, recorded not hidden'; do grep -qF -- "$s" $F || { echo "MISSING: $s"; exit 1; }; done && ! grep -qF 'OPEN — the Proposition sequence has a gap' $F && test -z "$(git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md)" && echo PASS</automated>
  </verify>
  <done>The register spec fixes the form, records measured numbering with the Proposition gap raised rather than resolved, tabulates the four hypotheses with the naming rule, states the no-mint rule with its check, and quotes `Definition 35` as the worked precedent with the retroactive note.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Approve the programme, and write AT MOST ONE block</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md, notes/VOLATILITY_INTRUMENTS_MEV.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md` (§1-§5 — what you are presenting)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` (`Definition 35` and `Rule 13` — the cadence and the insertion point)
    - The `.lean` file of whichever block the user selects, to quote its statement exactly
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md` §4 (run the no-mint check against the draft)
  </read_first>
  <decision>Two decisions in one checkpoint: (1) approve or revise the QUEUE — its membership, its order, and the writable/gated split; (2) name AT MOST ONE block from §1 to write now, or decline to write any.</decision>
  <context>
`NOT-10` requires heavy user approval one block at a time. This checkpoint therefore does two
distinct things and must not blur them: it approves the *programme*, and it authorises *one*
write as the worked demonstration.

Present the queue as a table the user can read in one screen: block, class, declaration,
conditionality, gate. Then present the ONE block you propose writing first — **in full, exactly
as it would appear in `SRC`** — with its rationale in the message body, since the rationale may
not enter the file.

**The default proposal an earlier version of this plan carried — W1,
`Theorem29_monoid_path_is_direct` — is MOOT.** That block was written at `cf386de` as
`Theorem 29 (The monoid path is direct) [M12]`. Proposing it would be proposing a block that
already exists.

Default proposal: **W4, `Theorem32_hazard_strictAntiOn_tau`**, PROVIDED its signature check in
Task 1 Step 2 returned unconditional or a nameable hypothesis. It takes `Theorem 32` — `DOC`
maxes at 28, `SRC` holds 29, 30 and 31, so 32 is unambiguously next and needs no number from the
Proposition sequence. If W4's signature makes it conditional on `H2`, it is **not** a §1 row at
all: move it to §2 and present `write none` as the default instead, saying why.

**Do not present W7 or anything numbered `Proposition` until §4B's residual question — whether
`Proposition 14` is free — is answered.** Say so when presenting.

**ONE thing MUST be presented at this checkpoint that is not a block proposal**, because it is a
consequence of the source move and the user is the only one who can dispose of it:

1. **The `## 0.1` `Proposition 13` hypothesis audit.** If the binder read returned **case (b) OR
   case (c)** for the domain lines `\tau^\star < 1` and `\tau^\star > 0 \iff \dots` — at plan time
   (c) is expected, because they sit in `Proposition16_corrected_law`'s **guarded conjunct 2**
   alongside the absolute-value reading §2's G3 row already bans — then a **G2-class sign claim
   is on the page ahead of its gate.** Present the finding with **both antecedent families named
   separately**: the sign family (`0 < dphidnu`, `dnudtau < 0` → `EST-03`) and the non-sign
   family (`phiXv < 1`, `hA`, `hM`, `hg` → `HND-01`, with `hg` load-bearing on line 1 too).
   Present the three remedy options from §0.1 **as options**. **Do not decide this without the
   user, and do not edit `SRC`** — `Proposition 13` is a `NOT-10` block and the ruling is theirs.
   §0.1 must be able to record whatever they rule, including "leave as written".

**`## 0` is NOT presented as a question.** The eight `cf386de` blocks were delivered out of
order under `NOT-10`'s own cadence; §0 records them and retires the stale queue rows. Present
§0 as information — what exists, and which four WRITABLE-NOW rows and one GATED row it retires
— and **do not ask the user to ratify or re-approve blocks they already approved.** Manufacturing
a governance question at a blocking checkpoint is the review gate pointed the wrong way.
  </context>
  <options>
    <option id="approve-and-write-w4">
      <name>Approve the queue; write W4 (`Theorem 32`) now</name>
      <pros>Demonstrates the cadence end to end on the first block that is genuinely unwritten; `Theorem 32` is unambiguously next and needs no number from the Proposition sequence.</pros>
      <cons>Commits the shared corpus's `Theorem 32` before the rest of the queue is reviewed; and if W4's signature carries `H2` it belongs in §2, not §1.</cons>
    </option>
    <option id="approve-queue-only">
      <name>Approve the reconciled queue; write NO new block this turn</name>
      <pros>Settles §0's retirements and, if §0.1 returned case (b), the `Proposition 13` domain-line question, without adding a fifteenth block on the same turn.</pros>
      <cons>The programme's written form is still not exercised end to end against a live block.</cons>
    </option>
    <option id="approve-write-nothing">
      <name>Approve the queue; write NO block yet</name>
      <pros>The programme is established and reviewable; every write then happens in its own turn with full attention.</pros>
      <cons>`NOT-10`'s cadence is specified but never exercised, so a defect in the register form would not surface until Phase 6.</cons>
    </option>
    <option id="revise-queue">
      <name>Revise the queue before anything is written</name>
      <pros>Membership, order or the writable/gated split can be corrected while it costs nothing.</pros>
      <cons>Delays the worked demonstration.</cons>
    </option>
  </options>
  <action>
**Step 1 — present.** In one message: the §1/§2 queue tables; the §4B numbering measurement and
the Proposition gap; then the proposed block in full, formatted exactly as it would appear in
`SRC`, with its rationale in the message body only.

Before presenting the draft, run the no-mint check from §4D over it and report the result in
the message. **If any glyph is unregistered, do not present the block** — present the notation
question instead.

**Step 2 — record the decision.** Append `## 6. Approval log` to `BLOCK-PROGRAMME.md`:

```
### <YYYY-MM-DDTHH:MM:SS±ZZ:ZZ> — programme approval
**Presented:** §0 reconciliation (<K> blocks delivered out of order, <R> queue rows retired —
information, not a question), §0.1 sign audit, queue §1 (<N> rows), §2 (<M> rows), numbering
measurement, the `Proposition 14` residual
**RULING:** <the user's words, quoted, not paraphrased>
**Ruling on the `Proposition 13` domain lines (§0.1 case <a|b|c>):** <the user's words, quoted —
option 1 / 2 / 3 from §0.1, or their own; or `N/A — case (a): carried unguarded, no defect`.
**`SRC` is NOT edited by this plan on any branch**; a ruled revision is its own block-turn.>
**Queue changes:** <any, or "none">

### <timestamp> — block <ID>
**Block presented in full:** <yes/no>
**No-mint check:** <output>
**RULING:** APPROVED / REVISED (<the revision, in the user's words>) / DECLINED
**Written to SRC:** <yes + the new pin, or no>
```

Quote the user. If the user declines to write any block, record
`**Written to SRC:** no. The programme is approved; no block was written. NOT-10's cadence is
established and exercised at the first Phase 6 write.` — that is a complete outcome, not a
failure.

**Step 3 — write AT MOST ONE block, only if approved.** If and only if the user approved a
specific block:

- Insert it in the correct position in `SRC` using the register form from §4A.
- **One block. Not two. Not "the two unconditional ones."** The cap is enforced below.
- Re-pin: `git hash-object notes/VOLATILITY_INTRUMENTS_MEV.md`, and follow `INVENTORY.md` §8 —
  a new dated row in §2 and a re-verification sweep over every `SRC` citation, since inserting
  a block MOVES LINE NUMBERS. This is not optional; the last re-pin moved every citation in the
  project.
- Two-step review of `BLOCK-PROGRAMME.md` (Reality Checker + one named specialist, in parallel)
  before commit, per `HND-05`; record the ISO timestamps in `REVIEW-REGISTER.md` §2.

Then commit:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
git add spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md
# and, ONLY if a block was approved and written:
git add notes/VOLATILITY_INTRUMENTS_MEV.md
git commit -m "docs(01): SRC block programme — queue, gates, register spec

Closes NOT-10's programme requirement. Reconciles the eight blocks cf386de
delivered into SRC out of order -- NOT-10's cadence ran for each of them,
ahead of the GSD plan that encodes it, the same pattern the roadmap records
for Phases 4 and 5 -- and retires the four WRITABLE-NOW rows and one GATED
row that named them. Queues what remains (W4, W5, W7) into WRITABLE-NOW and
GATED-ON-PHASE-6 (EST-03's sign verdict), with the corrected law's
form/reading split recorded -- its unconditional half is already on the
page as Proposition 13 -- and audits that block's domain lines against
Proposition16_corrected_law's binders. Records the Proposition numbering
correction as resolved at source (reservation 15+ was wrong; 12 is next)
and carries the Proposition-14 residual rather than picking a number.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

Stage only those paths. Never `git add -A`.
  </action>
  <acceptance_criteria>
    - `BLOCK-PROGRAMME.md` contains `## 6. Approval log` with a `**RULING:**` line for the programme approval, and both timestamps in full ISO-8601 form matching `[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}`.
    - The approval log contains `**Ruling on the `Proposition 13` domain lines`, non-empty.
    - The approval log's `**Presented:**` line describes §0 as `information, not a question`; there is **no** ratification ruling for the `cf386de` blocks, because none was sought.
    - The file contains NO `### Item`-style proposal for `W1`, `W2`, `W3` or `W6` — all four are already written and re-proposing one is a fabricated commitment.
    - **AT MOST ONE BLOCK WAS ADDED TO `SRC`:** the count of `^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ` headings in `SRC` is at most the pre-task count **plus one**. **This regex MUST be byte-identical to §0's baseline regex** — the earlier cap check dropped `Hypothesis` and required a trailing `[0-9]+`, so it counted 13 against a baseline of 14 and would have passed TWO new numbered blocks while an unnumbered one (`Hypothesis (H2)` is the precedent) was invisible to it. Record the pre-task count in the approval log so this is checkable.
    - If a block was written, `SRC`'s `git hash-object` differs from `33af6a85` AND `INVENTORY.md` §2 carries a NEW dated pin row AND §8 carries a new `### Re-pin sweep` block.
    - If no block was written, `git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` prints nothing and the log says so explicitly.
    - The approval log records the no-mint check output.
    - `git log -1 --name-only` lists `BLOCK-PROGRAMME.md`, lists no path under `.planning/`, and lists `notes/VOLATILITY_INTRUMENTS_MEV.md` only if a block was approved.
    - Live `git -C plank status --porcelain` diffs clean against `INVENTORY.md`'s `PLANK-BASELINE` sentinel block.
  </acceptance_criteria>
  <resume-signal>Approve or revise the queue; then name ONE block to write now, or say "write none".</resume-signal>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/BLOCK-PROGRAMME.md && grep -q '^## 6\. Approval log' $F && grep -qE '[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}' $F && grep -qF '**RULING:**' $F && grep -qF '**Ruling on the `Proposition 13` domain lines' $F && grep -qF 'information, not a question' $F && PRE=$(grep -oF 'pre-task block count:' $F | wc -l) && test $PRE -ge 1 && NOW=$(grep -cE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' notes/VOLATILITY_INTRUMENTS_MEV.md) && BASE=$(sed -n 's/.*pre-task block count: *\([0-9]*\).*/\1/p' $F | head -1) && test -n "$BASE" && test $NOW -le $((BASE+1)) && { test -z "$(git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md)" || { grep -q '### Re-pin sweep' spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md && test "$(git hash-object notes/VOLATILITY_INTRUMENTS_MEV.md)" != "33af6a85a9eb7e17fa19b4f92a78ee413353196e"; }; } && git log -1 --name-only --format="" | grep -q 'BLOCK-PROGRAMME.md' && ! git log -1 --name-only --format="" | grep -q '^\.planning/' && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && echo PASS</automated>
  </verify>
  <done>The programme is approved with the user's words quoted; at most one block was written, against an explicit approval naming it; if a block was written the re-pin sweep ran and `INVENTORY.md` carries the new pin; if none was written that is recorded as a complete outcome.</done>
</task>

</tasks>

<verification>
0. Reconciliation integrity: every block returned by
   `grep -nE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' notes/VOLATILITY_INTRUMENTS_MEV.md`
   has a `## 0` row, and **no block that appears in that output also appears as a §1 queue row.**
1. Queue integrity: every declaration named in §1/§2 returns ≥ 1 from
   `grep -rlF <decl> --include='*.lean' control/aristotle`.
2. Gate integrity: no §2 row is writable, and every §2 row names `EST-03` as its unblocking
   condition. The G3 split is present and the unconditional `+` form is NOT gated.
3. Block cap: `SRC`'s numbered-heading count increased by at most 1.
4. Numbering: the Proposition correction is recorded as RESOLVED AT SOURCE with the note quoted,
   and the residual (`is Proposition 14 free?`) is carried, not guessed.
5. No-mint: every glyph in any written block appears in `NOTATION-MAP.md` §4.
6. Peer non-interference: live plank porcelain diffs clean against the sentinel baseline;
   `git -C WT status --porcelain .planning/` shows only the two pre-existing untracked dirs.
</verification>

<success_criteria>
- `NOT-10` closed as a PROGRAMME: a §0 reconciliation of what `cf386de` already wrote, an
  ordered queue over what remains, a writable/gated split with a reason per row, a conditionality
  table, a register spec, measured numbering, the no-mint rule, and a worked precedent.
- The eight blocks `cf386de` delivered are recorded as **DELIVERED OUT OF ORDER** — the cadence
  ran ahead of the plan encoding it, as Phases 4 and 5 ran ahead of Phases 1-3 — and are
  reconciled, not re-approved. **No governance question is manufactured for the checkpoint.**
- The four stale WRITABLE-NOW rows and one stale GATED row that named already-written blocks are
  retired; no queue row commits to writing a block that exists.
- The gated set is exactly the four sign-dependent items; the corrected law's unconditional form
  is on the page as `Proposition 13` and is not held hostage to its gated reading; and whether
  its domain lines crossed the gate is decided by reading binders, not by assumption.
- The Proposition numbering correction is recorded from the source; the `Proposition 14`
  residual is raised, not resolved by guess.
- At most one block written, under an explicit per-block approval — no batch write.
</success_criteria>

<output>
After completion, create
`/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/phases/01-ground-truth-notation-and-the-rulings-triage/01-06-SUMMARY.md`
</output>
