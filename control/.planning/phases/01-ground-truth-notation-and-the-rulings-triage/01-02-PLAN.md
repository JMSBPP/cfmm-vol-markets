---
phase: 01-ground-truth-notation-and-the-rulings-triage
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md
autonomous: false
requirements: [NOT-01]

must_haves:
  truths:
    - "All 13 blocking decisions carry a bucket; the bucket sizes sum to exactly 13 and none is left untriaged."
    - "Every ALREADY-ANSWERED and AGENT-ANSWERABLE item names the evidence that closes it, in a section that can hold a display — not squeezed into a table cell."
    - "Only the NEEDS-THE-AUTHOR bucket reaches the user; under the re-triage it is EMPTY, and the emptiness is recorded with a closure citation for every item that once sat in it rather than manufactured into a question."
    - "The `π^φ` leg-pairing 'collision' is on the record as an errata artifact, with four agreeing sources and two dissenters named."
    - "No statement owned by another artifact is restated here — only pointed at."
  artifacts:
    - path: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md"
      provides: "The three-bucket triage of the 13 blocking decisions, per-item evidence sections, and the recorded user rulings for the NEEDS-THE-AUTHOR bucket"
      min_lines: 150
      contains: "NEEDS THE AUTHOR"
  key_links:
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md"
      to: "control/.planning/research/SUMMARY.md lines 116-131"
      via: "the 13 items quoted verbatim from their source list"
      pattern: "Blocking User Decisions"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md"
      to: "control/.planning/PROJECT.md USER NOTATION RULINGS 2026-08-08"
      via: "ruling R2 fixes which side of the π^varphi collision is renamed"
      pattern: "871e387"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md"
      to: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md"
      via: "citation form and SRC errata are pointed at, not restated"
      pattern: "Authoritative detail:"
---

<objective>
Triage the 13 blocking decisions into four named buckets so that the only ones consuming the
user's time are the ones that genuinely require the author. **Under the re-triage against `SRC`
@ `33af6a85` the NEEDS-THE-AUTHOR bucket is EMPTY**: the glyph question an earlier draft
escalated was dissolved at source, item 8's sectioning question is machine-answered, and item 10
is a routing rather than a question.

Purpose: `NOT-01`. This is the phase's highest-leverage, lowest-cost gate, and it supplies the
evidence and the glyph ruling that plan 01-03 consumes.

Output: `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research/SUMMARY.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md

**Path glossary.** `WT` = `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller`. `PLANK` =
`/home/jmsbpp/cfmms-playground/cfmm-wt/plank` (PEER-OWNED, READ-ONLY). `SPECDIR` =
`WT/spec/VOLATILITY_INSTRUMENTS_MEV_TAX`. `SRC` = `WT/notes/VOLATILITY_INTRUMENTS_MEV.md`
(**THIS PROJECT'S OWN FILE** — never call it peer-owned or `PEER-ADJACENT`). `DOC` =
`PLANK/notes/VOLATILITY_INSTRUMENTS.md`. `UNITS` = `PLANK/notes/UNITS_AND_SCALES.md`.

**Hard prohibitions.** Do not modify anything under `PLANK/`, `LEAN/`
(`/home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec`), or `WT/.planning/`. Do not edit `SRC`.

**`grep` is a shell function dispatching to `ugrep`** — LaTeX patterns need `-F`
(`grep -cF`, `grep -qF`). A bare `grep -c` on `\widehat\pi` errors rather than returning 0.

**Citation form and SRC errata are OWNED BY `INVENTORY.md`** (§5, §8, §9 — see its §10
ownership table). Use the pin form it fixes — **BY NUMBERED BLOCK**, e.g. `Definition 35 @
33af6a85`; as of `cf386de` `SRC` is 14 numbered blocks and a line-only citation of it is a
defect. Do not restate the rules or the errata here. Where you need them, write
`> Authoritative detail: INVENTORY.md §9` and point.

**Re-measure everything.** Plan-time values below are from 2026-08-08 and the trees move. Run
the command, record its output, note drift.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bucket all 13 as a thin index, with the evidence in sections that can hold displays</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research/SUMMARY.md` lines 116-131 (the source list of the 13 — quote it verbatim)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md` Phase 1 Success Criterion 2 (the spec for this task)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md` (BOTH the `USER RULING 2026-08-08` block and the `USER NOTATION RULINGS 2026-08-08` block added at commit `871e387`)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` (§2 pins, §5 citation rules, §9 SRC errata, §10 ownership)
  </read_first>
  <action>
Create `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md`.

Header, verbatim except the date (`date -I`):

```
# RULINGS TRIAGE — The 13 Blocking Decisions

**Requirement:** NOT-01
**Triaged:** <YYYY-MM-DD>
**Source list:** `control/.planning/research/SUMMARY.md`, section "Blocking User Decisions
(13 items, collected from all four documents)", lines 116-131.

> Authoritative detail: citation form — `INVENTORY.md` §5. `SRC` errata — `INVENTORY.md` §9.
> Pins — `INVENTORY.md` §2. This document restates none of them.

**FOUR statuses, nothing untriaged.** An earlier version had three; the user's ruling on item
10 forced a fourth, because "deferred to a later discipline" is neither answered nor a question
for the author:
- **ALREADY ANSWERED ON DISK** — the answer exists in a normative artifact; closed here by
  citing the locus that answers it.
- **AGENT-ANSWERABLE** — no artifact states the answer outright, but it follows from artifacts
  on disk or from this project's own scope decisions; closed here with the answer AND the
  evidence.
- **NEEDS THE AUTHOR** — a genuine modelling, intent, or naming question that no artifact
  settles. **Only this bucket is put to the user.**
- **DEFERRED** — the question is real, is NOT answered on disk, and is NOT the author's to
  answer: it belongs to a later discipline with the method to settle it. A `DEFERRED` row names
  the receiving phase and requirement and states why it cannot close here. **`DEFERRED` is not
  a soft `NEEDS THE AUTHOR`** — it is not put to the user at all.
```

Then `## 1. The 13, verbatim` — reproduce the 13 numbered items exactly as they appear in
`SUMMARY.md` lines 118-130, including their parenthetical researcher attributions.

Then `## 2. Bucket index (thin — evidence lives in §3)`. This is a NAVIGATION table only. Four
narrow columns, nothing that needs a display or a blockquote:

`# | Item (short) | Bucket | Evidence →`

The `Evidence →` cell is a pointer such as `§3.4` or `§4.1`. **No citation, quotation or
argument goes in this table** — markdown cells cannot hold a display, and a 180-word ruling
crammed into a cell is unreadable and unreviewable.

Bucket assignment (your starting position — for each row, open the cited file and CONFIRM
before accepting it; if a citation does not resolve, move the item to `NEEDS THE AUTHOR` and
say why):

| # | Item | Bucket | Evidence → |
|---|---|---|---|
| 1 | `π^{\varphi}` symbol collision | **ALREADY ANSWERED ON DISK** (closed at `c521af5`) | §3.11 |
| 2 | Leg pairing in `π^φ` | ALREADY ANSWERED ON DISK | §3.3 |
| 3 | Schedule's gate argument missing from display | AGENT-ANSWERABLE | §3.6 |
| 4 | `ν` vs `u` | ALREADY ANSWERED ON DISK | §3.1 |
| 5 | τ-vs-λ substitution / bridge legitimacy | AGENT-ANSWERABLE | §3.7 |
| 6 | Which relation the boxed form solves | **ALREADY ANSWERED ON DISK** (Aristotle bundle 2) | §3.8 |
| 7 | The `L` overload — ladder vs aggregate pool | ALREADY ANSWERED ON DISK | §3.4 |
| 8 | How `∂π^φ/∂φ` is sectioned | **ALREADY ANSWERED ON DISK** (`Theorem30`: ILL-POSED) | §3.12 |
| 9 | `e^σ`'s status — constraint or objective | AGENT-ANSWERABLE | §3.9 |
| 10 | `Δt` — exogenous constant or endogenous | **DEFERRED → Phase 6 / `EST-02`** | §3.13 |
| 11 | Grid map vs marginal price for leg conversion | ALREADY ANSWERED ON DISK | §3.2 |
| 12 | Entry-point-doc section/tag choice | AGENT-ANSWERABLE | §3.10 |
| 13 | Import-or-assume decision on demand elasticity | ALREADY ANSWERED ON DISK | §3.5 |

**Section numbers are NOT item numbers.** `§3.N` is a document ordinal; the item it resolves is
named in the section's own heading as `— item M`. Never infer `§3.N ≡ item N`: §3.2 is
Proposition 10, which is item 11.

Then a `### Count check` block: the three bucket sizes and their sum, asserting the sum is 13.
**Starting position, re-triaged 2026-08-08 against `SRC @ 33af6a85` and the two returned
Aristotle bundles, and the user's ruling on item 10:** ALREADY ANSWERED ON DISK = **8** (items
1, 2, 4, 6, 7, 8, 11, 13); AGENT-ANSWERABLE = **4** (items 3, 5, 9, 12); **DEFERRED = 1**
(item 10); **NEEDS THE AUTHOR = 0**. Sum 13. If verification moves any item, state the move,
its reason, and the revised sizes.

**The NEEDS-THE-AUTHOR bucket is EMPTY, and that is the correct outcome.** All thirteen close
on disk, on an agent-derivable answer, or by deferral to a discipline that can settle them. The
checkpoint records this and passes; it does not manufacture a question.

**Three items closed since the previous triage — record WHY each closed, not just that it
did:**
- **Item 1** closed by `c521af5`: `SRC` no longer uses the glyph `\pi^{\varphi}` at all
  (`grep -cF 'pi^{\varphi}' notes/VOLATILITY_INTRUMENTS_MEV.md` → 0) and writes the composite
  out in full as `\pi^{\phi} - \pi^{\text{LVR}}` inside `Definition 32 @ 33af6a85`. `DOC`
  keeps the glyph for its Definition 25, per ruling R2. **No glyph needs minting.**
- **Item 6** closed by Aristotle bundle 2 (`MevTaxProgram.lean`, blocks M19-M24):
  `M19_FOC_is_not_feasibility` separates the FOC `\partial\widehat\pi^\sigma/\partial\tau_{\text{MEV}} = 0`
  from feasibility `\mathcal{E} = 0`, and `Proposition16` audits the box term by term.
- **Item 8** closed by `Theorem30` in the same bundle: the fee-Jacobian bracket is
  **ILL-POSED** — it has no section-independent value. The question "which section was
  intended" has a machine-proved answer: none of them is privileged.
- **Item 10** DEFERRED by user ruling: **`\Delta t`'s exogeneity is an econometric question,
  not a doc ruling.** It routes to the structural-econometrics discipline in Phase 6
  (`EST-02`). `\Delta t` is the proposed INSTRUMENT, so its exogeneity AND its dispersion are
  jointly the identification question — a property of the estimator's design, not a modelling
  choice the author can declare. **Not answered by the user, not answered on disk: routed.**

**Evidentiary classes are NOT interchangeable — label each closure.** Items 1, 7 and 13 close
on a **USER RULING** recorded in `PROJECT.md`. Items 6 and 8 close on a **MACHINE-PROVED
RESULT** in a returned Aristotle bundle. Items 4 and 11 close on a **PEER DOCUMENT**. Write the
class in each `### 3.N` block; a later reader must not mistake a machine result for a ruling or
vice versa.

Then open `## 3. Evidence` and write a `### 3.N` block for EVERY item in the first two buckets
(§3.1 … §3.10 — ten blocks; §3.1/3.2/3.3 are Task 2's, so write §3.4 … §3.10 here).
**Every heading MUST carry `— item M`** so a reader never has to infer the mapping. Each block:

```
### 3.N — item M: <item title>

**Bucket:** <bucket>
**Evidence:** <the locus, in INVENTORY.md §5 form, with the text quoted in a blockquote or a
fenced display — this is why these are sections and not table cells>
**RULING:** <the answer>
**STATUS: CLOSED BY THIS PHASE — not put to the user.**
```

The seven you write here:

- **§3.6 — item 3** — the gate's argument is written out in full at `DOC Theorem 1` (locate its
  numbered item and its line; at plan time the display sits around `DOC:622`) as
  `u = \alpha_R/(1+\exp(\gamma_R(\beta_R - \varphi_{(1/2,\,0)}(i_K;\Delta Q,0;t)/\varphi_{(1/2,\,0)}(i_K;0,L;t))))`
  — **note the thin space `\,` inside the subscript; `DOC` writes `\varphi_{(1/2,\,0)}`, and a
  quote that drops it is not verbatim** — and again inside the boxed `\partial\phi/\partial\nu`
  at `ENTRY_POINT.md:15-60 @ db70caf2`. RULING:
  `SRC`'s display omits the argument as an ABBREVIATION, not a semantic gap; the argument is
  the utilization ratio and is recoverable verbatim. Note `DOC:620`'s own "**Signature note:**
  the `t` argument extends Definition 12's `(i_K; ΔQ, L)` signature — flagged, not silently
  repaired", which shows the peer document already treats this as a signature question, not a
  missing object.
- **§3.7 — item 5** — **not a user decision.** It is a first-class proof obligation: `PRF-05`,
  adjudicated at `ROADMAP.md` Phase 4 Success Criterion 3, which additionally requires any
  composition of named Lean declarations to carry a written closure check. RULING: closed as a
  scheduled obligation, not a ruling.
- **§3.8 — item 6** — **not a user decision.** `PRF-04`, adjudicated at `ROADMAP.md` Phase 5
  Success Criterion 1, which requires the ledger to name which of {level
  `\pi^\sigma \equiv^R \widehat\pi^\sigma`, vega-matching, as-written
  `\partial\widehat\pi^\sigma/\partial\tau_{\text{MEV}} = \Delta Q_v^\star`} the box was
  adjudicated against, with the rejected alternates named. RULING: closed as a scheduled
  obligation.
- **§3.4 — item 7** — **SETTLED.** `PROJECT.md` Context, block `USER RULING 2026-08-08`, items
  1-2, refined by `USER NOTATION RULINGS 2026-08-08` (commit `871e387`) which fixes the glyph
  `L_{\sigma} \equiv \Delta Q_v^{\star}` for the vol axis and leaves plain `L` / `\bar L` on
  the price axis. RULING: two assets, not one ambiguous symbol.
  `> Authoritative detail: NOTATION-MAP.md §2` — do not restate the factorization here.
- **§3.9 — item 9** — `PROJECT.md` Out of Scope: "Closed-loop feedback law over
  `e^σ = |π^σ − π̂^σ|` — the control target is an *optimal set-point*". `ROADMAP.md` Phase 2
  Success Criterion 3 instructs that `e^σ` be "declared an equality constraint rather than an
  objective so a regulator cannot creep in". RULING: **equality constraint.** Phase 2 owns the
  write-up; the decision is closed here.
  **The root-vs-argmin tension `FRAME.md` W4 raised is now RESOLVED AT SOURCE — do not record it
  as live.** W4 observed that `SRC` said "solving for `\tau_{\text{MEV}}` on the
  **minimization**" while displaying a closed form obtained from the **equation**. That sentence
  and that boxed form were both **STRUCK at `cf386de`**; confirm with
  `grep -cF 'This needs verification' notes/VOLATILITY_INTRUMENTS_MEV.md` → 0. **RE-AIM:
  `Definition 36 (Tax program) @ 33af6a85`**, which states the program explicitly as
  `\min_{\tau_{\text{MEV}} \in [0,1]} (\partial\widehat\pi^{\sigma}/\partial\tau_{\text{MEV}})^2`
  **subject to** `\pi^{\sigma} = \widehat\pi^{\sigma}`, with the FOC
  `\partial\widehat\pi^{\sigma}/\partial\tau_{\text{MEV}} = 0` written beneath it. Quote it
  verbatim. This corroborates the ruling twice over: the objective is the squared FOC residual
  and replication is a **constraint**, exactly as `e^\sigma`-as-equality-constraint requires.
  Record the W4 finding's disposition as `RESOLVED AT SOURCE (cf386de)` and hand that
  disposition to plan 01-04, which carries it as `CF-21`. **`\equiv^R` no longer appears in
  `SRC` either** (`grep -cF 'equiv^{R}'` → 0); `Definition 36` writes plain `=`.
- **§3.11 — item 1** — **closure class: USER RULING + source restructure.** Verify first:
  `grep -cF 'pi^{\varphi}' notes/VOLATILITY_INTRUMENTS_MEV.md` → 0, and quote
  `Definition 32 (Event-time plant) @ 33af6a85` — locate it by content
  (`grep -nF 'pi^{\phi} - \pi^{\text{LVR}}'`) — which carries the state vector's fourth
  component as `\pi^{\phi} - \pi^{\text{LVR}}` written out. RULING: the collision is **dissolved, not
  renamed** — `DOC` keeps `\pi^{\varphi}` for its Definition 25 (ruling R2, commit `871e387`)
  and `SRC` no longer contends for the glyph. **No glyph is minted and none is needed.**
  `> Authoritative detail: NOTATION-MAP.md C-1`.
- **§3.12 — item 8** — **closure class: MACHINE-PROVED.** `Theorem30` in
  `control/aristotle/tax2-result/project_aristotle/RequestProject/MevTaxProgram.lean` proves
  the fee-Jacobian bracket `[\Delta Q_M/(1-\phi_X) + p_{(\eta,\Delta_i)}\Delta Q_X/(1-\phi_M)]`
  is **ILL-POSED** — no section-independent value. Quote the declaration name and the M24
  verdict row verbatim from `TAX2_BUNDLE_NOTES.md`. RULING: the question "which section did the
  author intend" is answered **none is privileged**; the section sum is not a derivative
  of anything. This is a machine result, NOT a user ruling — say so in the block.
  **RE-AIM (`cf386de`): the sum's old locus `SRC:115` is STRUCK.** The claim now lives in `SRC`
  itself as `Theorem 31 (The section sum is ill-posed) @ 33af6a85`, which states the submersion,
  the non-simultaneous-definability of `\partial\phi_M/\partial\phi` and
  `\partial\phi_X/\partial\phi`, and the no-section-independent-value conclusion. **Cite
  `Theorem 31`, never `SRC:115`** — that line number now holds different material. Quote
  `Theorem 31` verbatim from the file alongside the Lean declaration.
  `> Authoritative detail: INVENTORY.md` §8 (the 2026-08-09 STRUCK table).
- **§3.10 — item 12** — `ENTRY_POINT.md` lives inside this project's own spec namespace and was
  git-tracked by plan 01-01. No peer coordination is required to choose sections or tags inside
  it. The `DOC`-side placement question is **moot**: this project never edits `DOC`
  (`ROADMAP.md` Phase 6 Success Criterion 4). RULING: closed — this project owns the file and
  its sectioning.
- **§3.5 — item 13** — **ASSUME, not import.** `PROJECT.md` Context, `USER RULING 2026-08-08`
  item 3: `\partial\bar L/\partial\pi^{\phi}` and
  `\bar{\mathcal{G}}_{(\nu,\lambda_{\text{MEV}})}` are LP-supply estimands observed from
  add/remove-liquidity events, formalized as named typed hypotheses and never sent to Aristotle.
  `REQUIREMENTS.md` `PRF-03`; magnitudes deferred to `EST-01` (v2).

**Before assigning item 10**, check whether `DOC` fixes a `Δt` convention:
`grep -nF 'Delta t' /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md | head -40`.
Write item 10 up as `### 3.13 — item 10`, in this shape:

```
### 3.13 — item 10: `\Delta t` exogenous or endogenous — DEFERRED

**Bucket:** DEFERRED → Phase 6 / `EST-02`
**Closure class:** USER RULING (deferral), 2026-08-08
**RULING:** `\Delta t`'s exogeneity is an **econometric** question for the structural-
econometrics discipline in Phase 6, not a doc ruling. Not settled here; not put to the author.
**Why it cannot close here:** `\Delta t` is the proposed INSTRUMENT. Its exogeneity and its
dispersion are together the identification question — a property of the estimator's design,
which no reading of `SRC` or `DOC` can decide.
**Receiving requirement:** `EST-02` (Phase 6). Also carried to `HND-01`.
**STATUS: DEFERRED — not closed by this phase, and not put to the user.**
```

Record the `DOC` grep's output inside the block: if `DOC` fixes `\Delta t` as an exogenous
block time in a NUMBERED item, that CONSTRAINS the estimator and belongs in the record — but
**do not use it to re-close item 10**, because the ruling concerns the identification question,
which a definitional convention does not settle. Do not conflate `\Delta t` with the
endogenous-maturity `t^\star` machinery — that is the vol instrument's maturity, a different
object.
  </action>
  <acceptance_criteria>
    - `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md` exists and is ≥ 150 lines.
    - It contains all three bucket names: `ALREADY ANSWERED ON DISK`, `AGENT-ANSWERABLE`, `NEEDS THE AUTHOR`.
    - §2's index table has exactly 13 data rows: `awk` between `^## 2\.` and `^### Count check` counting lines matching `^| *[0-9]+ *|` returns 13.
    - §2's table contains NO line longer than 200 characters (it is a thin index, not a ruling store).
    - It contains `### Count check` and the literal string `sum is 13`.
    - It contains at least 8 `### 3.` evidence blocks. Every one contains `**RULING:**`, and each carries EITHER `STATUS: CLOSED BY THIS PHASE` (the ten closed) OR `STATUS: DEFERRED` (item 10).
    - It contains all FOUR status names including `DEFERRED`, plus `EST-02` and `is the proposed INSTRUMENT`.
    - It contains `871e387`, `c521af5`, and `L_{\sigma} \equiv \Delta Q_v^{\star}`.
    - It contains `Theorem30`, `M19_FOC_is_not_feasibility`, and `closure class:` at least three times.
    - The Count check states ALREADY ANSWERED ON DISK = 8, AGENT-ANSWERABLE = 4, NEEDS THE AUTHOR = 1 (or revised sizes, each move justified).
    - It contains the literal string `> Authoritative detail:` at least twice.
    - It contains `PRF-04` and `PRF-05`.
    - It does NOT contain the string `PEER-ADJACENT` and does NOT contain `4 of 4`.
    - Every `### 3.` heading matches `^### 3\.[0-9]+ — item [0-9]+:` — the item number is stated, never inferred.
    - **Standing Check A** (`INVENTORY.md` §10.1): every `SRC:`/`UNITS_AND_SCALES.md:`/`ENTRY_POINT.md:`/`TEX:` line citation carries an 8-hex pin.
    - **Standing Check B** (`INVENTORY.md` §10.1): the file issues NO live line citation of `SRC` — every `SRC:NNN` occurrence is `SRC:2`, `SRC:4-10`, or carries `was ` / `STRUCK` on the same line.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md && test -f $F && test $(wc -l < $F) -ge 150 && for s in "ALREADY ANSWERED ON DISK" "AGENT-ANSWERABLE" "NEEDS THE AUTHOR" "### Count check" "sum is 13" "871e387" "c521af5" "Theorem30" "M19_FOC_is_not_feasibility" 'L_{\sigma} \equiv \Delta Q_v^{\star}' "PRF-04" "PRF-05"; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && test $(awk '/^## 2\./,/^### Count check/' $F | grep -cE '^\| *[0-9]+ *\|') -eq 13 && test $(awk '/^## 2\./,/^### Count check/' $F | awk 'length>200' | wc -l) -eq 0 && test $(grep -c '^### 3\.' $F) -ge 8 && test $(grep -c '^\*\*RULING:\*\*' $F) -ge 8 && test $(( $(grep -cF 'STATUS: CLOSED BY THIS PHASE' $F) + $(grep -cF 'STATUS: DEFERRED' $F) )) -ge 8 && grep -qF 'DEFERRED' $F && grep -qF 'EST-02' $F && grep -qF 'is the proposed INSTRUMENT' $F && test $(grep -cF '> Authoritative detail:' $F) -ge 2 && ! grep -qF 'PEER-ADJACENT' $F && ! grep -qF '4 of 4' $F && test $(grep -c '^### 3\.' $F) -eq $(grep -cE '^### 3\.[0-9]+ — item [0-9]+:' $F) && test -z "$(grep -oE '(SRC|UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' $F | grep -v ' @ ')" && test $(grep -E 'SRC:(111|123)\b' $F | grep -vc '\[ERRATUM ') -eq 0 && echo PASS</automated>
  </verify>
  <done>All 13 items are quoted verbatim; §2 is a thin navigation index with 13 rows and no oversized cells; every closed item has a `### 3.N` section carrying quotable evidence, a ruling and a CLOSED marker; the bucket sizes sum to 13.</done>
</task>

<task type="auto">
  <name>Task 2: The three mandated ALREADY-ANSWERED evidence blocks</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md` (the file you are appending to)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md` — locate by content, not by remembered line: `grep -nF 'Rule 6 (Per-leg fee split)'`, `grep -nF 'Theorem 1 (Fee Envelope)'`, `grep -nF 'Proposition 10'`
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` — **ALL 190 lines.** The leg-pairing displays and the boxed bracket were **DELETED at `cf386de`**; read the file to confirm their absence, and read `Theorem 31` and `Definition 36`, which are what replaced them
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` §9 (the SRC errata this block points at)
  </read_first>
  <action>
Append `### 3.1`, `### 3.2` and `### 3.3` — the three blocks `ROADMAP.md` Phase 1 Success
Criterion 2 names by hand. Same block shape as Task 1's.

**`### 3.1 — item 4: `ν` vs `u``**

Locate `DOC`'s utilization-gate paragraph and Theorem 1 by content
(`grep -nF 'utilization ratio'`, `grep -nF 'Theorem 1 (Fee Envelope)'`). Quote verbatim in a
blockquote: the sentence beginning "The gate's argument is the **utilization ratio**" (ending
"Theorem 1's \(u\) is the gate's value"), and Theorem 1's opening carrying
`u = \alpha_R/(1+\exp(\gamma_R(\beta_R - ...)))`. **Copy both quotes byte-exactly — `DOC` writes
the subscript with a thin space, `\varphi_{(1/2,\,0)}`.** Cite per `INVENTORY.md` §5 Rule 2 —
this is PROSE, so it is a line citation plus pin plus the nearest enclosing numbered item
(Theorem 1), and the citation must say so explicitly, e.g.
`DOC:620-622 @ 0c5ce574 (prose; nearest enclosing numbered item: Theorem 1 (Fee Envelope))`.
Then:

```
**RULING: `ν` and `u` denote the SAME object — the utilization gate's value.** `DOC` names it
`u`; `SRC` and `ENTRY_POINT.md` name it `\nu`. Both write the same expression. This is a glyph
difference across documents, not two objects. **Chosen glyph: `\nu`**, because `SRC` is this
project's citation target and `ENTRY_POINT.md`'s boxed `\partial\phi/\partial\nu` — the
determinate factor entering the channel — is already written in `\nu`.

**Collision warning that makes the choice necessary:** `u` is ALSO the letter carrying the
plant's inputs, `u_{\text{ex}}` and `u_{\text{en}}`, in `Definition 32 (Event-time plant) @
33af6a85`. Reusing `u` for the gate value would overload it inside the very equations the plant
lives in.

**STATUS: CLOSED BY THIS PHASE — not put to the user.**
```

**`### 3.2 — item 11: Proposition 10 is DECIDED`**

Locate Proposition 10 by content and quote its statement verbatim (the sentence asserting the
grid map and the marginal price are DISTINCT objects and that `p_{(\eta,\Delta_i)} \equiv
p_{\varphi}` is NOT admissible). Cite by numbered item plus pin. Then:

```
**RULING: DECIDED, not open.** The identification `p_{(\eta,\Delta_i)} \equiv p_{\varphi}` is
NOT admissible; the grid enters the marginal price as the inverse product of adjacent grid
values plus the leg orientation.

**Nuance that must not be lost:** `DOC` marks the relation "**unproved in-tree**" (a cheap
Aristotle rider on the Proposition 7 bundle). The DECISION is made; the PROOF is not. A later
artifact may cite the decision as settled and **MUST NOT cite it as a landed Lean result.**

**STATUS: CLOSED BY THIS PHASE — not put to the user.**
```

**`### 3.3 — item 2: the `π^φ` leg-pairing "collision" — RESOLVED AT SOURCE (`cf386de`)`**

**Read this before writing a word.** At pin `45aeba4c` this section was a six-source
adjudication: four displays agreeing on the pairing, two dissenting, both dissenters carrying
errata read-through tags. **Commit `cf386de` deleted FIVE of the six loci.** Reproducing the old
table would cite struck material. Verify the deletions first and paste every count:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
grep -cF 'phi_M \, \Delta Q_M' notes/VOLATILITY_INTRUMENTS_MEV.md            # was SRC:111 -> expect 0
grep -cF '(1 -  \tau_{\text{MEV}})(1 - \phi_X)' notes/VOLATILITY_INTRUMENTS_MEV.md  # was SRC:115 -> expect 0
grep -cF 'partial \pi^{\sigma}}{\partial \phi_M}' notes/VOLATILITY_INTRUMENTS_MEV.md # was SRC:123 -> expect 0
grep -cF 'tau_{\mathrm{MEV}}^\star' notes/VOLATILITY_INTRUMENTS_MEV.md       # the boxed law -> expect 0
grep -nF 'frac{\partial \phi_M}{\partial \phi}' notes/VOLATILITY_INTRUMENTS_MEV.md   # was SRC:113 -> expect TWO hits, both inside Theorem 31
```

**If any of the first four returns non-zero, STOP** — the source is not at `33af6a85` and this
whole section's premise is wrong.

**The fifth probe returns TWO hits and they carry DIFFERENT CLAIMS. Quoting the wrong one is
exactly the mis-quotation this row exists to prevent.** Both are inside
`Theorem 31 (The section sum is ill-posed) @ 33af6a85`; at plan time they measured:

| Line @ `04bac0a5` | What it claims | Use it for |
|---|---|---|
| `:153` | `\frac{\partial \phi_M}{\partial \phi}, \; \frac{\partial \phi_X}{\partial \phi}` **not simultaneously defined** — a statement about the two SECTIONS | the sectioning verdict (§3.12, item 8) |
| `:154` | `\frac{\partial \phi_M}{\partial \phi}\,\Delta Q_M \, + \, p_{(\eta,\Delta_i)}\,\frac{\partial \phi_X}{\partial \phi}\,\Delta Q_X` **has no section-independent value** — a statement about the SUM | **this row: the surviving RHS of the struck `SRC:113`** |

**Only `:154` is the re-pin target for `SRC:113`.** `:153` is a different proposition and is NOT
what `SRC:113` used to say. Disambiguate by content, not by taking the first hit:
`grep -nF 'Delta Q_M \, + \, p_{(\eta,\Delta_i)}'` returns `:154` alone.

Build a table `Source | Status @ 33af6a85 | Display (verbatim, only if it survives) | Pairing`
with these FOUR rows and nothing else:

| Source | Status |
|---|---|
| the boxed fee-Jacobian bracket (was `SRC:226-234 @ 45aeba4c`) | **STRUCK** — REFUTED factor by factor by the M24 audit; the surviving law is `Proposition 13 (Corrected law) @ 33af6a85` and it contains **no** fee-Jacobian bracket |
| the `(1-\tau)` two-section sum (was `SRC:115 @ 45aeba4c`) | **STRUCK** — ruled ILL-POSED; RE-AIM to `Theorem 31 @ 33af6a85` |
| the `\partial\pi^{\phi}/\partial\phi` chain rule (was `SRC:113 @ 45aeba4c`) | **PARTLY SURVIVES** — the LHS is gone, the RHS `\frac{\partial \phi_M}{\partial \phi}\,\Delta Q_M + p_{(\eta,\Delta_i)}\,\frac{\partial \phi_X}{\partial \phi}\,\Delta Q_X` is now the object `Theorem 31 @ 33af6a85` proves has **no section-independent value**. Quote it FROM `Theorem 31` and say what it is now — **do not quote it as a derivative** |
| `DOC Rule 6 (Per-leg fee split) @ 0c5ce574` | **ALIVE and now the SOLE normative home of the pairing.** Quote it verbatim |

**The two dissenters no longer exist.** State this verbatim rather than reproducing them:

```
**Both dissenting displays were DELETED at `cf386de`.** The `\pi^{\phi}` definition whose second
factor read `\Delta Q_M` where `\Delta Q_X` was meant (`INV-E1`, was `SRC:111 @ 45aeba4c`) and
the `(\partial\pi^{\sigma}/\partial\phi_M, \partial\pi^{\sigma}/\partial\phi_X)` pair (`INV-E2`,
was `SRC:123 @ 45aeba4c`) are both gone from `SRC`. **No read-through tag is issued for either**
— there is no live citation to tag, and tagging a deleted locus would imply the material is
still there to be read through.
> Authoritative detail: INVENTORY.md §9.1, §9.2 (closures) and §8's 2026-08-09 STRUCK table.
```

Then:

```
**RULING: there is no leg-pairing collision in `\pi^{\phi}`, and as of `cf386de` there is no
contest left to have.** The pairing is the `M` leg fee'd by `\phi_M` against `\Delta Q_M` and the
`X` leg by `\phi_X` against `\Delta Q_X`, with `p_{(\eta,\Delta_i)}` on the `X` term. Its sole
normative home is now `DOC Rule 6 (Per-leg fee split) @ 0c5ce574`; `SRC` no longer defines
`\pi^{\phi}` at all. **Any artifact needing the definition cites `DOC` Rule 6, never `SRC`.**

**The errata that made this an adjudication are CLOSED AT SOURCE, not recorded-and-not-applied.**
The earlier wording — "these errata are RECORDED, NOT APPLIED, because editing `SRC` would
invalidate every pin issued against it" — described a live defect. There is no live defect: the
author deleted the displays. What survives is the STRUCK-LOCUS RULE, which is stronger: a later
artifact must not re-pin `SRC:111`, `SRC:113`, `SRC:115` or `SRC:123` at whatever now occupies
those offsets.
> Authoritative detail: INVENTORY.md §9 and §5 Rule 1 (`SRC` is cited BY NUMBERED BLOCK only).

**STATUS: CLOSED BY THIS PHASE — not put to the user.**
```
  </action>
  <acceptance_criteria>
    - `RULINGS-TRIAGE.md` contains `### 3.1`, `### 3.2`, `### 3.3` headings.
    - It contains `RULING: `ν` and `u` denote the SAME object`, `RULING: DECIDED, not open`, and `RULING: there is no leg-pairing collision`.
    - It contains `unproved in-tree` AND `MUST NOT cite it as a landed Lean result`.
    - **§3.3 records the source move, not the old six-source adjudication:** it contains `RESOLVED AT SOURCE (`cf386de`)`, `DOC Rule 6 (Per-leg fee split) @ 0c5ce574`, `Theorem 31 @ 33af6a85`, `Proposition 13 (Corrected law) @ 33af6a85`, and the literal string `Both dissenting displays were DELETED at `cf386de`.`
    - **§3.3 pastes the five deletion probes and their counts**, and the four `expect 0` probes all show `0`.
    - It does NOT contain `RECORDED, NOT APPLIED` — that phrase described a live defect and there is none.
    - It does NOT contain `[ERRATUM INV-E` anywhere: **no read-through tag is issued for a struck locus.**
    - It contains `> Authoritative detail: INVENTORY.md §9`, `INV-E1` and `INV-E2` (as CLOSED references), and the STRUCK-LOCUS rule is honoured — every occurrence of `SRC:111`, `SRC:113`, `SRC:115`, `SRC:123` or `SRC:226-234` carries `was ` or `STRUCK` on the same line.
    - It contains `The differentiated object is `\pi^{\sigma}`, not `\pi^{\phi}`` **only if** that phrase appears inside a `was `-annotated historical clause; it may not describe a live display.
    - **M3 — claim hygiene on the struck derivative, not merely citation hygiene.** The struck LHS `\partial\pi^{\phi}/\partial\phi` must NEVER appear on a line that also contains `=`: `grep -E 'pi\^\{.?phi\}(\}\{.?partial|/.?partial)' <artifact> | grep -c '='` returns **0** (both the `\frac{}{}` and the `/` spellings). The expression `\partial\phi_M/\partial\phi\,\Delta Q_M + p_{(\eta,\Delta_i)}\,\partial\phi_X/\partial\phi\,\Delta Q_X` may be quoted ONLY as `Theorem 31 @ 33af6a85`'s no-section-independent-value object. **A `was `/`STRUCK` annotation is citation hygiene and does NOT license asserting the derivative** — that is the claim inversion the re-pin exists to prevent. (The address history in `INVENTORY.md` §8 is the one place the old equation may be shown; it is exempt, and it owns that record.)
    - The Theorem 1 quote contains the thin space: the literal string `\varphi_{(1/2,\,0)}` is present.
    - The `DOC:620-622` citation names its nearest enclosing numbered item on the same line (`Theorem 1`).
    - **§3.9 records the root-vs-argmin resolution:** it contains `Definition 36 (Tax program) @ 33af6a85`, the literal string `RESOLVED AT SOURCE (cf386de)`, and states that `\equiv^R` no longer appears in `SRC`.
    - It does NOT contain the string `SRC:222-228` (the superseded range) and does NOT contain any `SRC:2[23][0-9]` boxed-bracket citation — **the boxed bracket is struck.**
    - `git -C WT status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` prints nothing — `SRC` was not edited.
    - Live `git -C plank status --porcelain` diffs clean against `INVENTORY.md`'s `PLANK-BASELINE` sentinel block (extracted, not re-derived from the same command).
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md && for s in '### 3.1' '### 3.2' '### 3.3' 'denote the SAME object' 'RULING: DECIDED, not open' 'unproved in-tree' 'MUST NOT cite it as a landed Lean result' 'there is no leg-pairing collision' 'RESOLVED AT SOURCE (`cf386de`)' 'DOC Rule 6 (Per-leg fee split) @ 0c5ce574' 'Theorem 31 @ 33af6a85' 'Proposition 13 (Corrected law) @ 33af6a85' 'Both dissenting displays were DELETED at `cf386de`.' 'Definition 36 (Tax program) @ 33af6a85' 'RESOLVED AT SOURCE (cf386de)' '> Authoritative detail: INVENTORY.md §9' 'INV-E1' 'INV-E2'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && for bad in 'RECORDED, NOT APPLIED' '[ERRATUM INV-E' 'SRC:222-228'; do grep -qF "$bad" $F && { echo "FORBIDDEN: $bad"; exit 1; }; done; ! grep -qE 'SRC:2[23][0-9]' $F && test $(grep -E 'SRC:(111|113|115|123|226)' $F | grep -cvE 'was |STRUCK') -eq 0 && test $(grep -E 'pi\^\{.?phi\}(\}\{.?partial|/.?partial)' $F | grep -c '=') -eq 0 && grep -qF '\varphi_{(1/2,\,0)}' $F && grep -F 'DOC:620-622' $F | grep -qF 'Theorem 1' && test -z "$(grep -oE '(UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' $F | grep -v ' @ ')" && test -z "$(git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md)" && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && test -n "$B" && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && echo PASS</automated>
  </verify>
  <done>Sections 3.1-3.3 carry verbatim quoted evidence cited BY NUMBERED BLOCK; §3.3 records that five of its six former loci were STRUCK at `cf386de`, pastes the deletion probes, names `DOC` Rule 6 as the pairing's sole normative home and `Theorem 31` / `Proposition 13` as the re-aim targets, and issues no read-through tag for a deleted locus; §3.9 records the root-vs-argmin tension as resolved by `Definition 36`; `SRC` is unmodified and the peer tree diffs clean against the sentinel baseline.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Put the NEEDS-THE-AUTHOR bucket to the user — expected EMPTY, recorded not manufactured</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md` (§2's index — the NEEDS-THE-AUTHOR rows are what you present)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md` (`USER NOTATION RULINGS 2026-08-08`, commit `871e387` — ruling 2 settles WHICH side is renamed and states the glyph is not yet minted)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md` — the `\pi^{\text{WORD}}` family, to source candidate glyphs from the doc's own symbols: `grep -noF 'pi^{\text{' notes/VOLATILITY_INSTRUMENTS.md | sort -u | head -20` and `grep -noF 'pi^{\mathrm{' ... `
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` lines 25-30 and 100-125 (the `π^varphi` definition; the sectioning displays)
  </read_first>
  <decision>The rulings on the NEEDS-THE-AUTHOR bucket. **Under the 2026-08-08 re-triage that bucket is EMPTY** — items 1 and 8 closed on disk (`c521af5`; `Theorem30`), and item 10 is DEFERRED to Phase 6 / `EST-02` by user ruling, which is a routing, not a question. Expected outcome: present nothing, record the empty bucket and the reason, pass. Present a question ONLY if Task 1's verification actually moved an item into NEEDS THE AUTHOR — and then only that item.</decision>
  <context>
Ten of the thirteen "blocking decisions" have been closed without the user's time — five were
already answered on disk and five follow from artifacts or from this project's own scope
decisions. Only the residue is escalated. Present ONLY those items. Do not re-open a closed
item and do not ask the user to confirm the closures.

**Item 1 is a NAMING decision, not an analysis question.** `PROJECT.md`'s `USER NOTATION
RULINGS 2026-08-08` (commit `871e387`) already settled that `DOC` KEEPS `\pi^{\varphi}` for its
numbered Definition 25 and that **the source-side composite `\pi^{\phi} - \pi^{\text{LVR}}` (once
abbreviated `\pi^{\varphi}` at `SRC:28 @ 0ec30184`, STRUCK at `c521af5`) is the side that gets
renamed** — following the standing precedence that doc notation is preserved and
the conflicting symbol gets the new glyph. The ruling states explicitly that the replacement
glyph is **not yet minted** and must be built from the doc's own symbols and put to the user
before use. That is what this checkpoint does.

**No artifact may use any candidate glyph before the user rules.** Plan 01-03 depends on this
ruling.
  </context>
  <options>
    <option id="item-10-exogenous">
      <name>Item 10 — `Δt` is an exogenous block time</name>
      <pros>Matches `ptrade`'s use as a parameter; keeps `Δt` out of the actuator set.</pros>
      <cons>Forecloses `Δt` as a lever, which a phase-11 carry-forward named as one of the two levers outside `Θ_φ`.</cons>
    </option>
    <option id="item-10-endogenous">
      <name>Item 10 — `Δt` is endogenous</name>
      <pros>Preserves it as a lever.</pros>
      <cons>Adds a state the plant's `u_en = [τ_MEV, φ_M, φ_X]^T` does not carry, so Phase 2's `∂`-partition must be re-derived.</cons>
    </option>
  </options>
  <action>
**No glyph harvest is required.** An earlier version of this task built three candidate glyphs
for the composite. **The abbreviation was STRUCK at `c521af5` and the whole surrounding region
was restructured again at `cf386de`**; the composite is written out in full inside
`Definition 32 (Event-time plant) @ 33af6a85` as the state vector's fourth component. Confirm
before presenting anything:

```
grep -cF 'pi^{\varphi}' /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md
```

must return **0**. If it returns non-zero the closure is wrong — stop and re-escalate item 1.


Present the escalated items to the user in one message. After the user rules, append
`## 4. NEEDS-THE-AUTHOR — questions put and rulings recorded`. For each item:

```
### Item <N> — <short title>

**Question as posed:** <verbatim from the SUMMARY.md source list>
**What is already settled (NOT re-opened):** <the rulings and machine results bearing on it>
**Displays that create it:** <verbatim quotes at pinned citations>
**Put to the user:** <YYYY-MM-DD>
**RULING:** <the user's words, quoted, not paraphrased>
**Consequences recorded:** <which downstream artifacts/phases change, named>
```

If the user declines an item: `**RULING:** DECLINED — <the user's words>` then
`**Default adopted:** <option>` with the reason.

**If the bucket is EMPTY after Task 1's verification**, write §4 as the heading followed by
`**EMPTY.** All 13 decisions closed on disk or by agent-derivable answer at the 2026-08-08
re-triage. No question was put to the user. The three items an earlier triage escalated —
item 1 (glyph), item 8 (sectioning), item 10 (Δt) — closed at <cite each>.` and skip the
checkpoint. **Do not manufacture a question to justify the checkpoint.**

Then `## 5. Closure statement`, verbatim with actual counts:

```
All 13 blocking decisions are triaged. <A> closed by citation to a normative artifact, <B>
closed by agent-derivable answer with evidence, <C> put to the user and ruled (or explicitly
declined with a stated default). `NOT-01` is satisfied: none is untriaged, and the user's time
was spent on <C> items, not 13.
```

**Two-step review, THEN commit.** Run Reality Checker plus one named specialist in parallel
over `RULINGS-TRIAGE.md`, resolve BLOCKER/MAJOR findings, record the review date (plan 01-05
requires `review_date < commit_date`). Then:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
git add spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md
git commit -m "docs(01): triage the 13 blocking decisions into three buckets

Closes NOT-01. All 13 closed without user time: 8 on disk, 4
agent-derivable, 1 (Delta-t) DEFERRED to Phase 6b / EST-02. Records
nu-vs-u, Proposition 10 DECIDED (decided but unproved in-tree), and the
leg-pairing collision as RESOLVED AT SOURCE -- five of its six loci were
struck at cf386de and DOC Rule 6 is now the pairing's sole normative home.
Records the root-vs-argmin tension as resolved by Definition 36. Items 1
and 8 are closed on disk (c521af5; Theorem30 / SRC Theorem 31) and are NOT
escalated; the NEEDS-THE-AUTHOR bucket is EMPTY.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

Stage only that one path. Never `git add -A`.
  </action>
  <acceptance_criteria>
    - `RULINGS-TRIAGE.md` contains a `## 4.` section whose heading includes `NEEDS-THE-AUTHOR`.
    - The number of `### Item ` headings under §4 equals the NEEDS-THE-AUTHOR bucket size **as counted from §2's own index table** (`awk` the §2 table, count rows whose Bucket cell contains `NEEDS THE AUTHOR`) — not a hardcoded 3, because Task 1 is permitted to re-bucket item 10.
    - Every `### Item` block contains `**Question as posed:**`, `**Put to the user:**` and `**RULING:**`.
    - §4 contains NO `### Item 1` and NO `### Item 8` block — both closed on disk, in §3.11 / §3.12.
    - The file does NOT contain `\pi^{\text{net}}`, `\pi^{\text{LP}}` or `\pi^{\phi-\mathrm{LVR}}` — the candidate glyphs are WITHDRAWN, not merely unused.
    - If §4 says `**EMPTY.**` it contains no `### Item ` heading and the Count check's NEEDS-THE-AUTHOR size is 0.
    - It contains `## 5.` with `Closure statement`, and the literal string `NOT-01` is satisfied`.
    - `git log -1 --name-only` lists `RULINGS-TRIAGE.md` and no path under `.planning/`.
    - Live `git -C plank status --porcelain` diffs clean against `INVENTORY.md`'s sentinel baseline.
  </acceptance_criteria>
  <resume-signal>Confirm the EMPTY NEEDS-THE-AUTHOR bucket, or name any item Task 1's verification moved into it and rule on that item only. No glyph is being requested: the composite is written out in full at the source (`Definition 32 @ 33af6a85`) and `grep -cF 'pi^{\varphi}'` over `SRC` returns 0.</resume-signal>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md && grep -qE '^## 4\..*NEEDS-THE-AUTHOR' $F && N=$(grep -c '^### Item ' $F) && B=$(awk '/^## 2\./,/^### Count check/' $F | grep -cF 'NEEDS THE AUTHOR') && test $N -eq $B && ! grep -q '^### Item 1$' $F && ! grep -q '^### Item 8$' $F && for g in '\pi^{\text{net}}' '\pi^{\text{LP}}' '\pi^{\phi-\mathrm{LVR}}'; do grep -qF -- "$g" $F && { echo "WITHDRAWN GLYPH: $g"; exit 1; }; done; test $(grep -c '^\*\*RULING:\*\*' $F) -eq $N && test $(grep -c '^\*\*Question as posed:\*\*' $F) -eq $N && test $(grep -c '^\*\*Put to the user:\*\*' $F) -eq $N && for s in '871e387' 'Closure statement'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && { test $N -eq 0 || grep -qF '**What is already settled (NOT re-opened):**' $F; } && git log -1 --name-only --format="" | grep -q 'RULINGS-TRIAGE.md' && ! git log -1 --name-only --format="" | grep -q '^\.planning/' && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && echo PASS</automated>
  </verify>
  <done>The NEEDS-THE-AUTHOR bucket — under the 2026-08-08 re-triage, item 10 only — was put to the user, or recorded EMPTY if verification closed it; every escalated item carries the question as posed and the user's ruling or an explicit decline; the withdrawn glyph candidates appear nowhere.</done>
</task>

</tasks>

<verification>
1. Bucket arithmetic: §2's four bucket sizes sum to 13, and `### Item` blocks in §4 equal the
   NEEDS-THE-AUTHOR size (expected: 0).
2. Every citation in §3 resolves at its NUMBERED BLOCK against the §2 pin. **No `SRC` line
   citation is live** — `grep -oE 'SRC:[0-9]+' RULINGS-TRIAGE.md` returns only loci annotated
   `was ` or `STRUCK`.
3. No restatement: no normative statement owned by `INVENTORY.md` or `NOTATION-MAP.md` per
   `INVENTORY.md` §10 appears here except as a `> Authoritative detail:` pointer.
4. `SRC` unmodified; peer tree diffs clean against the sentinel baseline;
   `git -C WT status --porcelain .planning/` shows only the two pre-existing untracked dirs.
5. `grep -F "4 of 4"` and `grep -F "PEER-ADJACENT"` both return nothing.
</verification>

<success_criteria>
- `NOT-01` closed: all 13 triaged into exactly four buckets, none untriaged.
- The three roadmap-mandated records are present with verbatim evidence, in sections that can
  hold displays, cited BY NUMBERED BLOCK against the `33af6a85` pin.
- **Zero items consumed the user's time**, and the empty NEEDS-THE-AUTHOR bucket is recorded with
  a closure citation for each item that once sat in it — not manufactured into a question.
- The `SRC` errata are recorded as CLOSED AT SOURCE and pointed at their owning section; no
  read-through tag is issued for a struck locus, and no struck locus is re-pinned.
</success_criteria>

<output>
After completion, create
`/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/phases/01-ground-truth-notation-and-the-rulings-triage/01-02-SUMMARY.md`
</output>
