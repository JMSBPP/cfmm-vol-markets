---
phase: 01-ground-truth-notation-and-the-rulings-triage
plan: 05
type: execute
wave: 5
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified:
  - spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md
  - spec/VOLATILITY_INSTRUMENTS_MEV_TAX/REVIEW-REGISTER.md
autonomous: true
requirements: [NOT-05, HND-05]

must_haves:
  truths:
    - "The unit ledger is a PROPOSED DIFF that would actually merge — same column count, same enforcement-cell convention as the base table."
    - "The peer worktree is provably unchanged: live porcelain diffs clean against the sentinel-delimited baseline, with no fallback that can pass on drift."
    - "The extension was routed to its owner, and the routing outcome is recorded — silence as silence, never as consent."
    - "The message to the peer contains only findings that are actually theirs."
    - "The review register is honest: three retroactive first entries, and a per-row gate-satisfied cell rather than a heading that asserts compliance for everyone."
  artifacts:
    - path: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md"
      provides: "The proposed §6 extension to UNITS_AND_SCALES.md — mergeable shape, routed not applied"
      min_lines: 90
      contains: "PROPOSED DIFF"
    - path: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/REVIEW-REGISTER.md"
      provides: "The two-step review register with three retroactive first entries and a per-row gate cell"
      min_lines: 100
      contains: "RETROACTIVE"
  key_links:
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md"
      to: "plank/notes/UNITS_AND_SCALES.md"
      via: "pinned base sha, proposed as a new §6 — never applied in the peer tree"
      pattern: "a46ba298"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md"
      to: "peer ul2inqpl"
      via: "claude-peers send_message, attempt and outcome recorded"
      pattern: "ul2inqpl"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/REVIEW-REGISTER.md"
      to: "git history"
      via: "b5f5e82 / 9658375 / d3b226a recorded as retroactive entries"
      pattern: "b5f5e82"
---

<objective>
Ship the unit ledger as a proposed diff that would actually merge into a pinned peer-owned
table, route it to its owner with only the findings that are theirs, and stand up a review
register honest about the three artifacts committed before their review ran.

Purpose: `NOT-05`, `HND-05`. This is the phase's last plan; it also closes the phase's
peer-non-interference proof.

Output: `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md` and
`spec/VOLATILITY_INSTRUMENTS_MEV_TAX/REVIEW-REGISTER.md`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/CLAUDE.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md

**Path glossary.** `WT` = `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller`. `PLANK` =
`/home/jmsbpp/cfmms-playground/cfmm-wt/plank` (PEER-OWNED — owner is the Plank-development
session, `claude-peers` agent id `ul2inqpl`). `UNITS` = `PLANK/notes/UNITS_AND_SCALES.md`.
`SRC` = `WT/notes/VOLATILITY_INTRUMENTS_MEV.md` (**this project's own file**).

**Hard prohibitions.** Do not create, modify or delete anything under `PLANK/`, `LEAN/`, or
`WT/.planning/`. **`UNITS_AND_SCALES.md` is NOT edited by this plan.** `grep` dispatches to
`ugrep` — use `-F` for literals.

**OWNERSHIP — decides what goes in the peer message.** `CLAUDE.md` assigns `ul2inqpl` exactly
`src/`, `script/`, `foundry.toml`, `remappings.txt` and Plank `.plk`. It does NOT assign
`evm-controller/notes/`. **The `SRC` errata are THIS PROJECT'S OWN and must NOT be sent to
`ul2inqpl` as their defects.** At the `33af6a85` pin **all three are CLOSED**: `INV-E3` (the old
`SRC:70`) was fixed at `c521af5`; `INV-E1` (was `SRC:111 @ 45aeba4c`) and `INV-E2` (was
`SRC:123 @ 45aeba4c`) had their loci **STRUCK at `cf386de`**. A fourth entry, `INV-C1`, records
that `cf386de` also **changed `Rule 13`'s signature**. All four live at `INVENTORY.md` §9. **None
of them goes in Message A.** What DOES go to the peer is the `\sigma`/`\sigma^2` units question
(O4), because it turns on `DOC` Definition 18's convention — see Step 2 item (c).

**THE BASE TABLE'S CONVENTIONS — read them before proposing a row.** At the pin, `UNITS`
carries at `UNITS_AND_SCALES.md:11-13` the convention: *"every enforcement cell is marked
**[FACT]** (exists in code today, cite) or **[PLANNED — increment]** (forward commitment, named
owner). A row that mixes the two is a defect."* Its symbol tables have **FOUR** columns:
`Symbol | Storage | Unit (CANONICAL) | Enforced`. Sections present: `## 0` Fixed-point
conventions, `## 1` The volatility axis, `## 2` The vega axis, `## 3` The price axis, `## 4`
The conversion chains, `## 5` Rules. The extension proposes a NEW `## 6.` and reopens nothing.

**`L_σ` stays here.** Ruling R1's `L_{\sigma}` glyph is this project's notation, recorded at
`NOTATION-MAP.md` §2. It is NOT proposed into the peer's table — the peer's table already fixes
the unit at `:70` under the name `ΔQ_v★`, and proposing a rename would be a change to an
existing row, which this extension does not do.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author a §6 extension that would actually merge</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/UNITS_AND_SCALES.md` — **ALL 139 lines.** Especially `:11-13` (the enforcement-cell convention), the four-column table headers, `:70` and `:72`, and `## 4`'s conversion chains including `:114`.
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md` (§2 the axis rule and `L_σ`, §4.1 the symbol register, C-4 the `ΔQ_υ` retirement, C-1's ruled glyph)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` (§2 pin, §5 citation rules)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md` (`NOT-05` — the six symbols; and `EVM-01b` for the on-chain-referent fact)
  </read_first>
  <action>
Confirm the six symbols are genuinely absent from the base table (use `-F`):

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/plank
for s in 'nu' 'tau' 'MEV' 'LVR' 'ν' 'τ' 'π^' 'pi^' 'phi' 'φ' 'fee' 'eta'; do printf "%-6s: " "$s"; grep -cF -- "$s" notes/UNITS_AND_SCALES.md; done
grep -nF 'π^' notes/UNITS_AND_SCALES.md
grep -nF 'eta' notes/UNITS_AND_SCALES.md
```

Plan-time, re-measured: `tau`, `MEV`, `LVR`, `ν`, `τ`, `pi^`, `phi`, `φ`, `fee` → **0**;
`nu` → 4 and `eta` → 3, **all substring noise inside ordinary words** (`eta` matches
*deleverage delta*, *reinterpretation*, *meta*); **`π^` → 2, at lines 59 and 63**, both inside
§2's prose about `π^σ`, not a symbol row. An earlier draft of this plan recorded `π^` as 0 —
that was wrong, and the correction is recorded rather than silently applied. The operative
conclusion is unchanged: **none of the six symbols has a row, and the base table fixes no fee
scale and no `η` scale.** If any of the six is genuinely present, DROP it and
record why: `NOT-05` says the extension adds what the table LACKS and re-derives nothing.

Create `.../spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md`.

Header, verbatim except the date:

```
# UNITS LEDGER EXTENSION — PROPOSED DIFF

**Requirement:** NOT-05
**Written:** <YYYY-MM-DD>
**Base:** `plank/notes/UNITS_AND_SCALES.md` @ `a46ba298...` (139 lines) — re-measure with
`git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank hash-object notes/UNITS_AND_SCALES.md` and
record your measured value.
**Owner:** the Plank-development session, `claude-peers` agent id `ul2inqpl`.
**Status:** PROPOSED DIFF — NOT APPLIED. This project does not edit the peer tree.

**Merge-shape discipline.** The base table's symbol tables have FOUR columns —
`Symbol | Storage | Unit (CANONICAL) | Enforced` — and `UNITS_AND_SCALES.md:11-13` requires
every enforcement cell to be `[FACT]` or `[PLANNED — increment]`, stating that "a row that
mixes the two is a defect". **The proposed section below obeys both.** Provenance for each row
(which pinned source fixed the unit) is NOT a fifth column — it lives in a `**Sources**`
sub-list beneath the table, so the block can be lifted into the base file verbatim.

**Scope discipline.** This is an EXTENSION. It proposes exactly one new section, `## 6. The
controller axis`, appended after `## 5.`. It reopens NOTHING in §0-§5, re-derives no conversion
chain, restates no rule, and proposes no change to any existing row.
```

Then `## 1. Absence check` — the grep battery output with a one-line verdict per symbol.

Then `## 2. Proposed addition`. Inside a fenced ```markdown block (so it lifts verbatim):

`## 6. The controller axis` — a FOUR-column table with **exactly seven unit rows** (the five
`NOT-05` symbols plus the two `\sigma`/`\sigma^2` rows O4 requires):

1. `ν` | not stored | **DIMENSIONLESS** — a ratio of two evaluations of the same trading
   function, `\varphi_{(1/2,0)}(i_K;\Delta Q,0;t)` over `\varphi_{(1/2,0)}(i_K;0,L;t)` |
   `no enforcement — symbol has no on-chain referent (live grep recorded in §1; EVM-01b)`
2. `τ_MEV` | not stored | **DIMENSIONLESS fraction in the fee monoid's carrier `[0,1]`** — it
   composes with the leg fees through the Rule 12 monoid, whose carrier `DOC` Theorem 14 fixes
   as `[0,1]`. **Do NOT cite a `φ_X` scale row in the base table: it has none.** Verify —
   `grep -cF 'phi'`, `grep -cF 'φ'`, `grep -cF 'fee'` over `UNITS_AND_SCALES.md` all return
   **0** at the pin. State that explicitly: "the base table fixes no fee scale; the carrier is
   fixed by `DOC` Rule 12 / Theorem 14, cited by numbered item." |
   `no enforcement — symbol has no on-chain referent (live grep recorded in §1; EVM-01b)`
3. `π^l` | not stored | **`[π^l] = [π̂^σ]/[L]` on the vol axis** — from
   `π̂^σ = Σ_{i_K} L(i_K)·π^l(σ(i_K;Θ_σ))`; the `L` here is vol-axis liquidity, `ΔQ_v★` in this
   table's name and `L_σ` in the controller spec's | `no enforcement — symbol has no on-chain referent`
4. `π^φ` | not stored | **collateral base units** — `φ_M ΔQ_M + p_{(η,Δ_i)} φ_X ΔQ_X`: a
   dimensionless fee times a token quantity, plus a price times a dimensionless fee times a
   token quantity. **The base table's §3 does not carry `p_{(η,Δ_i)}`** — `grep -cF 'eta'`
   finds only substring noise (*deleverage delta*, *reinterpretation*, *meta*) and there is no
   `η` row. Cite `SRC`'s own use plus `DOC` Proposition 10 for the grid-vs-marginal
   distinction, and state that the base table fixes no scale for it. |
   `no enforcement — symbol has no on-chain referent`
5b. `σ(i(t))` | not stored | **VOLATILITY — the argument of `DOC` Definition 18's sigmoids; `Θ_φ`'s centers `β_j` are in these units** | `no enforcement — schedule argument, not a stored field`
5c. `σ²(i(t))` | not stored | **VARIANCE — the third slot of the plant's `u_ex` (`Definition 32`). NOT interchangeable with `σ(i(t))`; see §2b** | `no enforcement — disturbance input, not a stored field`
5. `π^LVR` | not stored | **same unit as `π^φ`** — imposed as a CONSISTENCY CONSTRAINT, not
   derived: `Definition 32 (Event-time plant) @ 33af6a85` carries the state component
   `\pi^{\phi} - \pi^{\text{LVR}}`, so the units must agree. **The abbreviated identity that
   once sat at `SRC:28 @ 0ec30184` no longer exists** — removed at `c521af5`, and the whole
   surrounding region was restructured again at `cf386de`. **Cite the numbered block, never a
   line** | `no enforcement — symbol has no on-chain referent`

**Enforcement-cell semantics — do NOT write `no enforcement — symbol has no on-chain referent`.** `[FACT]` means
"exists in code today, cite" (`UNITS_AND_SCALES.md:11-13`); using it to assert ABSENCE inverts
the token. The base table already models this third case twice and neither uses `[FACT]` for
absence: its not-stored row at `:72` uses **free text** (`lens L1/L2 tests`), and its `:70` row
writes `[PLANNED — V2-01/02] … NOTHING exists today`. **Adopt the `:72` free-text form** for
all five rows: `no enforcement — symbol has no on-chain referent`. Then raise the gap in
Message A: the base table's binary convention has no token for "deliberately absent", and the
owner may want one. Do not write "nothing today" either — that is neither token nor free text.

**Re-measure the absence claim live; do not carry `REQUIREMENTS.md`'s stored result:**
```
grep -rin "tau\|mev\|utilization" /home/jmsbpp/cfmms-playground/cfmm-wt/plank/src/ | wc -l
```
Record the command, its output and the date beside the rows that rely on it.

Beneath the table, still inside the fenced block, a `**Sources**` sub-list with one bullet per
row giving the pinned citation the unit came from. **Every `SRC` citation is BY NUMBERED BLOCK —
`SRC` is 14 numbered blocks as of `cf386de` and a line citation of it is a defect:**

- `ν` → `DOC` Theorem 1 (Fee Envelope) @ `0c5ce574`.
- `τ_MEV` → `Definition 32 (Event-time plant) @ 33af6a85` (the `u_{\text{en}}` slot) and
  `Definition 35 (Monoid gradient) @ 33af6a85`; carrier `[0,1]` from `DOC` Rule 12 / Theorem 14.
  The program's box constraint is written explicitly in `Definition 36 (Tax program) @ 33af6a85`
  as `\min_{\tau_{\text{MEV}} \in [0,1]}` — cite that, it is the strongest source-side statement
  of the carrier.
- `π^l` → `Proposition 12 (Kernel differentiation) @ 33af6a85`, whose first display is
  `\widehat\pi^{\sigma} = \sum_{i_K} L(i_K)\,\pi^{l}(\sigma(i_K;\cdot))` — locate it with
  `grep -nF 'sum_{i_K} L (i_K) \, \pi^{l}'` — plus `UNITS_AND_SCALES.md:70 @ a46ba298`.
- `π^φ` → **`DOC Rule 6 (Per-leg fee split) @ 0c5ce574`.** This is a RE-AIM and the reason must
  be recorded: at `45aeba4c` this bullet cited `SRC:113-115`, the `\pi^{\phi}` definition and its
  derivative. **That entire region was STRUCK at `cf386de`** — confirm with
  `grep -cF 'phi_M \, \Delta Q_M' notes/VOLATILITY_INTRUMENTS_MEV.md` → **0**. `SRC` no longer
  defines `\pi^{\phi}`; it only *uses* it (as a state component in `Definition 32` and as a
  differentiation variable in `Proposition 12` and `Theorem 31`). **The unit's provenance is
  therefore `DOC` Rule 6, which is normative for the per-leg split and is peer-owned.** Add the
  leg pairing per `NOTATION-MAP.md` C-3.
  `> Authoritative detail: INVENTORY.md §8` (the 2026-08-09 STRUCK table) and `§9.1`.
- `π^LVR` → `Definition 32 (Event-time plant) @ 33af6a85` (the state component
  `\pi^{\phi} - \pi^{\text{LVR}}`) and `DOC` Definition 26 @ `0c5ce574`.
- `σ(i(t))` → `Rule 13 (Fee schedule and standing assumptions) @ 33af6a85` and `DOC`
  Definition 18 @ `0c5ce574`.
- `σ²(i(t))` → `Definition 32 (Event-time plant) @ 33af6a85`, the `u_{\text{ex}}` third slot.

**Do NOT write `SRC:106-110`, `SRC:111-115` or `SRC:113-115` anywhere.** All three are ranges
into a region that no longer exists.

Then, still inside the fenced block, a prose **alias note** — NOT a table row:

```
**Alias note (adds no unit).** `ΔQ_υ` (as written in the controller source's `Θ_σ` block) and
`ΔQ_v★` (as written in this table at line 70 and in the doc's `I_ord` display) occupy the same
slot of the same four-slot parameter block and are the same object. The glyph `ΔQ_υ` is
retired. The unit is the one this table already fixes at line 70 and is **not re-derived**.
Caution: `ΔQ_v` WITHOUT the star (line 72) is a THIRD, different object — the lens readout
sensitivity in collateral base units per Algebra vol unit, never stored.
```

Then, OUTSIDE the fenced block, add a note on the `π^LVR` row:
`**Flag carried, not resolved:** \`DOC\` Definition 26 computes \`π^LVR\` from Definition 25's
object (\`π^{\varphi}\`). Importing it therefore requires that object's unit. Ruling R2 (commit
\`871e387\`) keeps \`π^{\varphi}\` for \`DOC\`'s Definition 25 and renames the SOURCE side, which
breaks the circularity — and as of commit `c521af5` the source no longer uses the glyph at all,
so the circularity is DISSOLVED, not merely renamed.
> Authoritative detail: NOTATION-MAP.md C-1.`

Then `## 2b. O4 — `\sigma` versus `\sigma^2`: two axes the ledger MUST separate`.

**This is the units ledger's single most consequential row for Phase 6.** Measure both sides:

```
grep -nF 'Definition 18 (Dynamic fee schedule)' /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md
grep -nF 'Phi \, (\Theta_{\phi};' /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md
grep -nF 'sigma^2 \, (i (t))' /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md
```

**Do NOT use the old `sed -n '37p'` probe.** That line number was valid at pin `45aeba4c` only;
`cf386de` moved every line and deleted a third of the file.

At plan time: `DOC` Definition 18's sigmoid argument is **`\sigma(i(t))`** — a VOLATILITY, and
its centers `\beta_j` therefore live in `\sigma`-units. **`Rule 13 (Fee schedule and standing
assumptions) @ 33af6a85` now agrees**: as of `cf386de` it reads, **verbatim from the file —
copy it byte-exactly, spacing included**:

    \phi_X (t) \; &= \; \Phi \, (\Theta_{\phi}; \sigma (i (t)), \nu (t))

where at `45aeba4c` it read `\Phi(\Theta_{\phi}; \sigma^2(i(t)))`. The plant's `u_{\text{ex}}` third slot
(`Definition 32 (Event-time plant) @ 33af6a85`) is still **`\sigma^2(i(t))`** — a VARIANCE.

**This CHANGES O4's statement and makes this section MORE load-bearing, not less.** O4 used to be
a schedule-versus-`DOC` disagreement that a peer could settle by fiat. It is now a
**`Rule 13`-versus-`Definition 32` disagreement INSIDE one document** — the fee schedule reads a
volatility, the plant's disturbance carries a variance, and nothing converts between them. Record
that framing explicitly. Add BOTH to the proposed `## 6.` as separate rows, and state verbatim:

```
**`\sigma` and `\sigma^2` are different quantities on different scales and are NOT
interchangeable.** `\Theta_{\phi}`'s sigmoid centers `\beta_j` are in `\sigma`-units
(`DOC` Definition 18); the disturbance input's third slot is in `\sigma^2`-units
(`Definition 32`). Any expression pairing a `\Theta_{\phi}` parameter with the disturbance
must state which it is on and convert explicitly. **A regression that mixes them silently is
wrong, and it is wrong in a way that no unit check downstream will catch** — both are
positive reals and the substitution is dimensionally invisible without this row.
```

Add a `Conversion` note: `\sigma^2` is the square of `\sigma`, so a center stated in one is
NOT the square/root of a center stated in the other unless the schedule was re-parameterized;
say that the conversion is a MODELLING question and record it as OPEN if `SRC` does not fix it.
Route it to `HND-01` and raise it in Message A.

Then `## 3. What this extension deliberately does NOT do`, verbatim:

```
- It does not re-derive any unit `UNITS_AND_SCALES.md` §0-§5 already fixes.
- It does not restate the conversion chains of §4 or the rules of §5.
- It does not propose any change to an existing row — including no rename of `ΔQ_v★` to this
  project's `L_σ`, which is THIS PROJECT'S notation and stays in this project's tree.
- It attaches NO on-chain cost, gas or storage claim — the EVM track is v2 (`EVM-01a`/`EVM-01b`).
```
  </action>
  <acceptance_criteria>
    - `UNITS-LEDGER-EXTENSION.md` exists and is ≥ 90 lines.
    - It contains `PROPOSED DIFF — NOT APPLIED`, `a46ba298`, `ul2inqpl`, and `## 6. The controller axis`.
    - The proposed table's header row inside the fenced block has exactly 4 columns.
    - The proposed table has exactly 7 DATA rows (five `NOT-05` symbols + `σ` + `σ²`). **Count `^|` lines, exclude separator rows matching `^\|[- :|]*$`, then subtract the header.** Counting `'^| '` is separator-style-dependent: with the compact `|---|---|---|---|` it under-counts by one (5 correct rows score 6 and FAIL an `-eq 7` test) while 6 wrong rows score 7 and PASS.
    - Every `Enforced` cell is free text beginning `no enforcement` or is `[PLANNED — <owner>]`; `nothing today` appears nowhere.
    - It contains a **live** `grep -rin "tau\|mev\|utilization" …/plank/src/` command with its recorded output and date — not a value carried from `REQUIREMENTS.md`.
    - No `Enforced` cell uses `[FACT]` to assert ABSENCE: the string `[FACT] — no on-chain referent` appears nowhere.
    - It states that the base table fixes **no fee scale and no `η` scale**, with the measured zero counts.
    - It contains `## 2b. O4` and the literal string `A regression that mixes them silently is`, and both `σ(i(t))` and `σ²(i(t))` appear as separate rows in the proposed `## 6.`.
    - **The `\pi^{\phi}` provenance is RE-AIMED, not re-pinned:** it contains `DOC Rule 6 (Per-leg fee split) @ 0c5ce574` and does NOT contain `SRC:113-115`, `SRC:106-110` or `SRC:111-115` — all three are ranges into a region struck at `cf386de`.
    - It cites `Proposition 12 (Kernel differentiation) @ 33af6a85` for `π^l`, `Definition 36 (Tax program) @ 33af6a85` for `τ_MEV`'s `[0,1]` carrier, and `Rule 13 ... @ 33af6a85` for `σ(i(t))`.
    - It issues NO live `SRC` line citation: every `SRC:NNN` occurrence is annotated `was `/`STRUCK`/`0ec30184`/`45aeba4c`.
    - §2b states that O4's mismatch is now **internal to `SRC`** (`Rule 13` vs `Definition 32`) and quotes `Rule 13`'s current signature `\Phi \, (\Theta_{\phi}; \sigma (i (t)), \nu (t))`.
    - It contains `**Sources**` and `**Alias note (adds no unit).**`.
    - It contains `CONSISTENCY CONSTRAINT`, `not re-derived`, `does NOT do`, and `EVM track is v2`.
    - It contains `L_σ` together with `stays in this project's tree`.
    - `git -C plank hash-object notes/UNITS_AND_SCALES.md` still returns `a46ba298d8b48fa851e817bf6f46e580b350878e`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md && test -f $F && test $(wc -l < $F) -ge 90 && for s in "PROPOSED DIFF — NOT APPLIED" "a46ba298" "ul2inqpl" "## 6. The controller axis" "no enforcement — symbol has no on-chain referent" "**Sources**" "**Alias note (adds no unit).**" "CONSISTENCY CONSTRAINT" "not re-derived" "does NOT do" "EVM track is v2" "stays in this project's tree"; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && ! grep -qF 'nothing today' $F && ! grep -qF '[FACT] — no on-chain referent' $F && grep -q '^## 2b\. O4' $F && grep -qF 'A regression that mixes them silently is' $F && for s in 'DOC Rule 6 (Per-leg fee split) @ 0c5ce574' 'Proposition 12 (Kernel differentiation) @ 33af6a85' 'Definition 36 (Tax program) @ 33af6a85' '\Phi \, (\Theta_{\phi}; \sigma (i (t)), \nu (t))'; do grep -qF "$s" $F || { echo "MISSING RE-AIM: $s"; exit 1; }; done && for bad in 'SRC:113-115' 'SRC:106-110' 'SRC:111-115' 'PEER-ADJACENT'; do grep -qF "$bad" $F && { echo "FORBIDDEN: $bad"; exit 1; }; done; test $(grep -E 'SRC:[0-9]+' $F | grep -cvE 'was |STRUCK|0ec30184|45aeba4c') -eq 0 && H=$(awk '/^## 6\. The controller axis/,0' $F | grep -m1 '^| *Symbol') && test $(printf '%s' "$H" | tr -cd '|' | wc -c) -eq 5 && D=$(awk '/^## 6\. The controller axis/,/^\*\*Sources\*\*/' $F | grep '^|' | grep -vE '^\|[- :|]*$') && test $(printf '%s\n' "$D" | wc -l) -eq 8 && test $(printf '%s\n' "$D" | tail -n +2 | grep -c 'no enforcement\|\[PLANNED —') -eq 7 && test "$(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank hash-object notes/UNITS_AND_SCALES.md)" = "a46ba298d8b48fa851e817bf6f46e580b350878e" && echo PASS</automated>
  </verify>
  <done>The extension proposes a four-column `## 6.` with five unit rows whose enforcement cells all use the base table's `[FACT]`/`[PLANNED — owner]` convention, provenance in a `**Sources**` sub-list rather than a fifth column, `ΔQ_υ` demoted to a prose alias note that adds no unit, and `L_σ` explicitly kept out of the peer's table.</done>
</task>

<task type="auto">
  <name>Task 2: Route to ul2inqpl and prove peer non-interference against the sentinel baseline</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md` (the content you are sending)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/CLAUDE.md` (the ownership map — decides what belongs in the message)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md` §7 (the sentinel baseline) and §9 (the SRC errata that must NOT be sent)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md` §5 (the DO-NOT-CITE proposal — a SEPARATE message)
  </read_first>
  <action>
**Step 1 — find the peer.** Call `claude-peers` `list_peers` at `repo` scope; record the raw
result. The durable identity is the agent id `ul2inqpl`; PIDs are not stable.

**Step 2 — send MESSAGE A to `ul2inqpl`.** It carries ONLY what is theirs:

1. What this is: a PROPOSED extension to `notes/UNITS_AND_SCALES.md`, authored at
   `evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md`, against the
   pin `a46ba298...`. Nothing applied; the plank tree is untouched. State that the proposed
   block keeps the four-column shape and the `[FACT]`/`[PLANNED — owner]` enforcement
   convention from `UNITS_AND_SCALES.md:11-13`.
2. The proposed `## 6. The controller axis` block, verbatim.
3. TWO informational items — the only findings that touch their tree:
   `\`VOLATILITY_INSTRUMENTS.md\` Definition 25 defines \`π^{\varphi}\` as the portfolio value
   function and Definition 26 computes \`π^LVR\` from it. The controller source's line 28
   independently uses \`π^{\varphi}\` for the composite \`π^φ − π^LVR\`, which put the glyph on
   both sides once Definition 26 is imported. **This is resolved on OUR side, not yours** —
   user ruling R2 (\`871e387\`) keeps \`π^{\varphi}\` for your Definition 25, and commit
   \`c521af5\` deleted our abbreviation entirely. No action required.`
   (b) `**RESOLVED on our side — informational only, no action needed.** Our \`Rule 13\`
   previously stated \`φ_X(t) = Φ(Θ_φ; σ²(i(t)))\` — a function of σ ALONE — while your
   **Definition 18** schedule is \`φ̄ + (Σ_j sigmoid_j(σ))·gate(ν)\`, σ AND ν. As of our commit
   \`cf386de\` our \`Rule 13\` reads \`Φ(Θ_φ; σ(i(t)), ν(t))\` and the two agree. We record the
   consequence: our corrected law is self-referential in \`φ_X\` as well as in \`ν\`. Recorded as
   \`CF-30\`, closed.`
   (c) `**Units question, and a direct input to our Phase 6 regression.** Your **Definition
   18**'s sigmoid argument is \`σ(i(t))\` — a volatility — so \`Θ_φ\`'s centers \`β_j\` are in
   σ-units. Our plant's disturbance slot is \`σ²(i(t))\` — a variance. We are carrying BOTH in
   the proposed §6 and stating that they are not interchangeable. **If your table has a
   preferred convention for which axis \`Θ_φ\` is quoted on, that decides it for us**;
   otherwise we record it as open and convert explicitly at every use.`
   **Also flag the numbered-block reservation — the ask has GROWN since the last message was
   drafted.** Our source now continues your sequence with **fourteen** blocks, not six:
   \`Convention 7\`, \`Convention 8\`, \`Definitions 32-36\`, \`Rule 13\`, \`Theorems 29-31\`,
   \`Propositions 12-13\`, and one unnumbered \`Hypothesis (H2)\`. Measured against your document
   at \`0c5ce574\` (\`Convention\` ≤ 6, \`Definition\` ≤ 31, \`Theorem\` ≤ 28, \`Proposition\` 2-11,
   \`Rule\` ≤ 12) none of these collides — **but our earlier draft reserved \`Proposition 15+\`,
   which was wrong; the correct next unused Proposition is 12, and 12-14 were orphaned by that
   error.** Our numbering note now records the correction. Ask them to confirm no collision with
   anything queued, and to say whether they want 12-14 reserved or released. **Re-measure both
   sides before sending** — do not send the numbers above unverified:

       grep -nE '^\*\*(Convention|Definition|Rule|Theorem|Proposition|Hypothesis) ' notes/VOLATILITY_INTRUMENTS_MEV.md
       for k in Convention Definition Theorem Proposition Rule; do printf "%-12s: " "$k"; grep -oE "\*\*$k [0-9]+" /home/jmsbpp/cfmms-playground/cfmm-wt/plank/notes/VOLATILITY_INSTRUMENTS.md | grep -oE '[0-9]+' | sort -n | uniq | tr '\n' ' '; echo; done
4. An explicit ask: `no action required to unblock us — this is routed for your adjudication.
   A reply is recorded; silence is recorded as silence.`

**DO NOT include any `SRC` erratum.** `SRC` is
`evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` — this project's own file, not assigned to
`ul2inqpl` by `CLAUDE.md`. Sending them would misattribute this project's defects to a peer.
**All three (`INV-E1`, `INV-E2`, `INV-E3`) are CLOSED anyway** — `INV-E3` at `c521af5`, the other
two by the `cf386de` strike — and `INV-C1` is a correction the author made, not a defect. They
are recorded at `INVENTORY.md` §9 and carried to `HND-01`. **The message must contain no
`SRC:NNN` line citation at all**: `SRC` is cited by numbered block.

**Step 3 — MESSAGE B (the DO-NOT-CITE proposal) is SEPARATE and has a different addressee.**
It concerns `.planning/research/v2-controller/`, which belongs to the repo-root planning
project, not to `ul2inqpl`. Determine from `list_peers` whether any peer owns that tree. If
one does, send it there. **If none does, do not send it to `ul2inqpl` as a consolation** —
record verbatim: `**MESSAGE B: NO ADDRESSEE.** No peer in \`list_peers\` (repo scope) owns
\`.planning/research/v2-controller/\`. The DO-NOT-CITE proposal at \`FINDING-REGISTER.md\` §5
therefore binds only this project's artifacts and is carried to \`HND-01\` and \`HND-03\` as an
unrouted item. Absence of an owner is recorded as absence of an owner.`

**Step 4 — record the outcome.** Append `## 4. Routing record and peer non-interference proof`:

```
### 4.1 Routing
**Tool:** `claude-peers` `send_message`
**list_peers (repo scope) result:** <raw>

**MESSAGE A — units extension + the Definition 25/26 note**
**Target:** `ul2inqpl`
**Sent:** <timestamp from `date`>
**Send result:** <raw>
**Reply:** <the reply verbatim, OR the literal line `NO REPLY AT TIME OF WRITING — recorded as
silence, not as consent.`>

**MESSAGE B — the DO-NOT-CITE proposal**
**Target:** <the owner of `.planning/research/v2-controller/`, or `NO ADDRESSEE`>
**Sent / not sent:** <...>
**Reply:** <verbatim, or the silence line, or the NO ADDRESSEE block>
```

If `list_peers` returns nothing or `send_message` fails, record the failure verbatim and write:
`**Routing not achieved.** The extension remains a proposed diff in this project's tree. Carried
to the gap register (\`HND-01\`) and to \`HND-03\`, where peer agreement must be OBTAINED and
EVIDENCED. Failure to route is recorded as failure to route.` Never treat an un-sent message as
sent.

```
### 4.2 Peer non-interference proof
```
Extract the baseline from `INVENTORY.md` §7 **between its sentinels** — do not re-derive it and
do not compare plank's status against itself:

```
BASE=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' ../INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d')
NOW=$(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain)
diff <(printf '%s\n' "$BASE") <(printf '%s\n' "$NOW") && echo "PLANK UNCHANGED" || echo "PLANK DRIFT — INVESTIGATE"
```

**The `diff` must SUCCEED. There is no fallback.** A per-line `grep` fallback cannot detect a
REMOVED baseline line and matches text anywhere in the file; it is not a check. If `diff`
fails, stop and investigate — do not paper over it.

**Do NOT paste the baseline body into this artifact.** `INVENTORY.md` §10 owns it and its own
rule says two copies WILL drift — a copy here would be a second source of truth for the very
fact this section certifies. Record instead: (a) the extraction COMMAND verbatim, (b) the live
`NOW` output, (c) the `diff` exit status and any output, (d) the `UNITS` pin then and now (must
be equal), (e) the same three for `lean4-spec` against the `LEAN-BASELINE` sentinels, and
(f) `git -C WT status --porcelain .planning/` (must be only the two pre-existing untracked
dirs). Point at the baseline: `> Authoritative detail: INVENTORY.md §7`.
  </action>
  <acceptance_criteria>
    - `UNITS-LEDGER-EXTENSION.md` contains `### 4.1 Routing` with `**MESSAGE A`, `**MESSAGE B`, `**Tool:**`, `**Target:**`, `**Reply:**`.
    - Both `**Reply:**` fields are non-empty — each is a quoted reply, or contains `recorded as silence, not as consent`, or contains `NO ADDRESSEE`.
    - It contains `### 4.2 Peer non-interference proof`, the literal string `There is no fallback`, and `> Authoritative detail: INVENTORY.md §7`.
    - §4.2 records the extraction COMMAND and the `diff` exit status but does NOT reproduce the baseline body — the five baseline lines (` M lib/bunni-v2` etc.) appear in `INVENTORY.md` only.
    - **The MESSAGE A block** (from `**MESSAGE A` to `**MESSAGE B`) contains no `SRC:NNN` line citation and no `INV-E<n>` identifier — this project's own errata are not routed to a peer, and all three are closed in any case.
    - The MESSAGE A block flags the **fourteen**-block reservation (not six) and asks about `Proposition 12-14`.
    - It contains `This is resolved on OUR side, not yours` and `**RESOLVED on our side — informational only, no action needed.**`.
    - Live: the sentinel-extracted baseline diffs clean against live `git -C plank status --porcelain`.
    - `git -C plank hash-object notes/UNITS_AND_SCALES.md` returns `a46ba298d8b48fa851e817bf6f46e580b350878e`.
    - `git -C WT status --porcelain .planning/` returns only `?? .planning/milestones/` and `?? .planning/research/`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md && grep -q '^### 4\.1 Routing' $F && grep -q '^### 4\.2 Peer non-interference proof' $F && for s in '**MESSAGE A' '**MESSAGE B' '**Tool:**' '**Target:**' '**Reply:**' 'There is no fallback' 'This is resolved on OUR side, not yours' '> Authoritative detail: INVENTORY.md §7'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && ! grep -qF ' M lib/bunni-v2' $F && MA=$(awk '/^\*\*MESSAGE A/,/^\*\*MESSAGE B/' $F) && for bad in 'SRC:70' 'SRC:111' 'SRC:113' 'SRC:115' 'SRC:123' 'INV-E1' 'INV-E2' 'INV-E3'; do printf '%s' "$MA" | grep -qF "$bad" && { echo "MESSAGE A CONTAINS OWN-PROJECT ERRATUM: $bad"; exit 1; }; done; test $(grep -cF '**Reply:**' $F) -ge 2 && test "$(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank hash-object notes/UNITS_AND_SCALES.md)" = "a46ba298d8b48fa851e817bf6f46e580b350878e" && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && test -n "$B" && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && test -z "$(git status --porcelain .planning/ | grep -v '^?? .planning/milestones/$' | grep -v '^?? .planning/research/$')" && echo PASS</automated>
  </verify>
  <done>Message A carried the extension plus the one item that is genuinely the peer's, with this project's own `SRC` errata excluded; Message B either found its owner or is recorded as having none; the non-interference proof compares live porcelain against the sentinel-extracted baseline with a `diff` that must succeed and no fallback.</done>
</task>

<task type="auto">
  <name>Task 3: The review register — retroactive first entries and a per-row gate cell</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/REVIEW-REGISTER.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md` (`HND-05`, including the `SPEC-04` precedent)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md` Phase 1 SC 5 and Phase 6 SC 5
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/CLAUDE.md` (the two-step reviewer process: Reality Checker always, plus one named specialist, in parallel)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/STATE.md` (Blockers/Concerns — the review-gate debt entry)
  </read_first>
  <action>
Gather the founding-commit facts:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
for c in b5f5e82 9658375 d3b226a fcb6d10 02b4543 98c2891 871e387 78381d4; do git log -1 --format="%h | %ai | %s" $c; done
for c in b5f5e82 9658375 d3b226a; do echo "--- $c ---"; git show --stat --format="" $c | head -8; done
```

Plan-time cross-check: `b5f5e82` 2026-08-08 13:53:07 (the five `control/.planning/research/*.md`);
`9658375` 2026-08-08 13:58:32 (`control/.planning/REQUIREMENTS.md`); `d3b226a` 2026-08-08
14:07:06 (`REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`). Re-measure.

Create `.../spec/VOLATILITY_INSTRUMENTS_MEV_TAX/REVIEW-REGISTER.md`.

Header, verbatim except the date:

```
# REVIEW REGISTER

**Requirement:** HND-05
**Opened:** <YYYY-MM-DD>

**The gate.** Every pre-commit artifact — spec, plan, sub-plan, roadmap, design doc — passes a
two-step review run with two reviewers IN PARALLEL before it is committed or executed: Reviewer
1 is ALWAYS the Reality Checker; Reviewer 2 is ONE named specialist chosen to fit the artifact,
and the choice is justified in the entry. Reviewers do not edit the artifact. BLOCKER and MAJOR
findings are resolved before the commit.

**The invariant this register makes checkable:** for every entry other than the retroactive
first three, `review_date < commit_date`. **Both are recorded as full ISO-8601 timestamps
(`date -Is`, e.g. `2026-08-08T16:42:07-04:00`), matching §1's `git log %ai` format — NOT
dates.** Every commit in this phase lands on 2026-08-08, so a day-granularity row reads
`2026-08-08 < 2026-08-08` = false and a compliant row is indistinguishable from a violation.
A reader can verify each row against `git log --format=%ai`.

**Precedent.** The v2-controller milestone deferred its `SPEC-04` review and still owes it.
This register exists so that does not repeat — and is honest that it did not fully succeed at
the outset.
```

Then `## 1. RETROACTIVE first entries — committed BEFORE their review ran`, opening verbatim:

```
**These three artifacts violated the gate.** They were committed before their two-step review
ran. They are recorded as RETROACTIVE entries rather than back-dated into compliance: the
commit date stands, the review is recorded as having run AFTER it, and the review's findings are
listed as entries against them. **Back-dating any of these rows is itself a defect.**
```

Subsections `### R-01` … `### R-05`. `R-01`-`R-03` are the founding commits. **`R-04` and
`R-05` are the two returned Aristotle bundles, retroactive for the same reason:**

- **`R-04`** — bundle 1, `MevTaxControl.lean` (855 lines, blocks M11-M18).
- **`R-05`** — bundle 2, `MevTaxProgram.lean` (1130 lines, blocks M19-M24).

Both were **submitted, returned, integration-gated and committed WITHOUT a two-step review of
the returned Lean.** The integration gate DID run — `sorry` sweep, `#print axioms`, dependency
byte-identity, forbidden-declaration checks — and it is a real gate, but it is **not** the
two-step review (Reality Checker + one named specialist, in parallel) that `CLAUDE.md` and
`ROADMAP.md`'s Standing constraints require before commit. Record the distinction verbatim:
`**Integration gate:** RUN — <checks and results>. **Two-step review:** NOT RUN before commit —
retroactive.`

**O1 — the axiom sweep was never independently verified.** Record it in both `R-04` and `R-05`,
verbatim:

```
**O1 — `#print axioms` NOT independently verified.** The bundle notes assert axiom-cleanliness
(`propext`, `Classical.choice`, `Quot.sound` only). **This project has not run `#print axioms`
against a built environment**; the claim is the bundle's own, unreproduced here. A `grep` for
zero `sorry` and zero `axiom` DECLARATIONS is NOT an axiom sweep — it cannot see an axiom
reached transitively through a dependency. Recorded as an open item (`HND-01`) and as a
precondition on any later citation of these results as axiom-clean.
```

This is the same rule already stated for `PRF-08`: `0 sorry + 0 axiom declarations` may not be
presented as an axiom sweep. It applies here directly. The integration gate must not be allowed to stand in for the two-step gate, and
neither entry is back-dated.

Each of the five uses this shape:

```
### R-0N — <commit short sha> — RETROACTIVE
**Artifact(s):** <paths from `git show --stat`; for R-04/R-05 the bundle `.lean` paths>
**Integration gate (R-04/R-05 only):** <checks run and results, quoted from the bundle notes>
**Committed:** <full timestamp>
**Commit sha:** <full 40-char>
**Two-step review ran:** AFTER the commit — the gate was not satisfied at commit time.
**Reviewer 1:** Reality Checker
**Reviewer 2:** <named specialist, or `NOT RUN` if it was not>
**Findings recorded against this artifact:** <each with BLOCKER/MAJOR/MINOR and its disposition:
RESOLVED (naming the artifact and commit that resolved it) / OPEN (carried to HND-01)>
**Status:** RETROACTIVE — never back-dated.
```

Findings to place, **each against its correct commit — an earlier draft of this plan swapped
two of them**:

- **`fcb6d10`** ("fix: correct vol_markets file count and restore source varphi glyph in
  PROJECT.md") resolved TWO findings: the 37-vs-23 Lean-file count AND the self-inflicted
  `π^φ̃` mint. Verify: `git show fcb6d10 -- control/.planning/PROJECT.md | grep -F '37 files'`
  and `... | grep -F 'π^φ̃'`. Both findings get their own row and both name `fcb6d10`.
- **`02b4543`** ("fix: (beta,gamma) freeze is an assumption, not a theorem (contradicted by
  T12)") resolved the misquoted `(β,γ)` non-control theorem — a DIFFERENT finding, its own row,
  naming `02b4543`. The record side is `FINDING-REGISTER.md` CF-05. **The source text is now
  corrected too** — `c521af5` removed the sentence and restated the freeze as a declared
  assumption in `Rule 13`, so CF-05 is `RESOLVED-BEFORE-PHASE-1` and is NOT carried open.
  Record both commits: `02b4543` fixed the planning record, `c521af5` fixed the source.
- **`98c2891`** resolved the `.tex` item counts and the `ENTRY_POINT.md:672` miscitation
  (`INVENTORY.md` §6).
- Still OPEN, carried to `HND-01`: the "4 of 4" independence claim (superseded at
  `FINDING-REGISTER.md` CF-14 — a supersession, so record it as RESOLVED-by-supersession with
  the artifact named), the Rule-9 `τ*=1` and channel-death findings (superseded, CF-11/CF-12),
  the no-differential-calculus claim (superseded, CF-13), the three missed normative artifacts
  (closed at `INVENTORY.md` §3), and the `REQUIREMENTS.md` commit message saying "31 across 6
  categories" against the current count — **check the current count yourself** and record it.

Then `## 2. Phase 1 entries` — note the heading is deliberately NOT "gate satisfied"; whether
the gate was satisfied is a PER-ROW fact:

Table `Entry | Artifact | Reviewer 2 (named + why chosen) | Review timestamp (ISO) | Commit timestamp (ISO) | Commit sha | Gate satisfied? | BLOCKER/MAJOR findings | Disposition`.
Both timestamp columns carry full ISO-8601 values with offset; a bare date is a defect.

One row per artifact this phase wrote: `INVENTORY.md`, `RULINGS-TRIAGE.md`, `NOTATION-MAP.md`,
`FINDING-REGISTER.md`, `UNITS-LEDGER-EXTENSION.md`, `REVIEW-REGISTER.md`. Each plan in this
phase runs its artifact's two-step review before its commit, so each row should read
`Gate satisfied? = YES` with `review_date < commit_date`. **Fill from the actual record.** If
any artifact's review date does not precede its commit date, the cell reads
`NO — GATE VIOLATION, carried to HND-01` and the reason is stated. Do not fabricate a review
that did not happen and do not leave a row blank.

Then `## 3. Standing rule for Phases 2-6`, verbatim:

```
Every artifact this project writes from here on gets a row in §2's table BEFORE it is
committed, with `review_date < commit_date`, Reviewer 2 named and justified, and the
`Gate satisfied?` cell filled from the record rather than assumed. Phase 6 Success Criterion 5
checks this register against `git log` for every artifact under `spec/`. The three retroactive
rows in §1 remain retroactive.
```

**Two-step review, THEN commit.** Run Reality Checker plus one named specialist in parallel
over BOTH `UNITS-LEDGER-EXTENSION.md` and `REVIEW-REGISTER.md`, resolve BLOCKER/MAJOR findings,
record the review dates in §2 (including this file's own row). Then:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
git add spec/VOLATILITY_INSTRUMENTS_MEV_TAX/UNITS-LEDGER-EXTENSION.md spec/VOLATILITY_INSTRUMENTS_MEV_TAX/REVIEW-REGISTER.md
git commit -m "docs(01): unit ledger extension (proposed diff) and review register

Closes NOT-05, HND-05. The ledger extends plank/notes/UNITS_AND_SCALES.md
at a pinned sha with seven unit rows plus an alias note, in the base table's
four-column shape and [FACT]/[PLANNED] enforcement convention, routed to
ul2inqpl -- never applied; the peer tree is proven unchanged against a
sentinel-delimited baseline. pi^phi's provenance is re-aimed at DOC Rule 6
because cf386de struck SRC's pi^phi algebra entirely; O4 is restated as a
Rule 13 vs Definition 32 mismatch internal to SRC. The review register records b5f5e82, 9658375
and d3b226a as RETROACTIVE first entries with findings attached to their
correct remediating commits (fcb6d10 for the file count and the pi-phi-tilde
mint; 02b4543 for the (beta,gamma) theorem), never back-dated.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

Stage only those two paths. Never `git add -A`.
  </action>
  <acceptance_criteria>
    - `REVIEW-REGISTER.md` exists and is ≥ 100 lines.
    - It contains `b5f5e82`, `9658375`, `d3b226a`, `fcb6d10`, `02b4543`, `98c2891`.
    - It contains exactly five `### R-0` headings, each containing `RETROACTIVE`.
    - `R-04`/`R-05` name `MevTaxControl.lean` and `MevTaxProgram.lean`, each carrying `**Integration gate` AND `Two-step review:** NOT RUN before commit`.
    - Both carry `O1 — `#print axioms` NOT independently verified` and the literal string `is NOT an axiom sweep`.
    - It contains `review_date < commit_date`, `Back-dating any of these rows is itself a defect`, `SPEC-04`.
    - **Every §2 row's review and commit timestamp cells match `[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}`** — day-granularity dates cannot distinguish compliance from violation when every commit lands on the same day.
    - `Reality Checker` appears at least four times.
    - The line containing `02b4543` also contains `beta` or `β`; the line containing `fcb6d10` also contains `count` or `37` or `φ̃` — i.e. each commit is attached to its correct finding.
    - §2's heading is exactly `## 2. Phase 1 entries` (NOT containing `gate satisfied`), and its header row contains `Gate satisfied?`.
    - §2 contains a row naming each of `INVENTORY.md`, `RULINGS-TRIAGE.md`, `NOTATION-MAP.md`, `FINDING-REGISTER.md`, `UNITS-LEDGER-EXTENSION.md`, `REVIEW-REGISTER.md`.
    - No §2 row has an empty `Gate satisfied?` cell — every one reads `YES` or begins `NO —`.
    - It contains `Standing rule for Phases 2-6`.
    - `git log -1 --name-only` lists both new artifacts and no `.planning/` path.
    - Live plank porcelain diffs clean against the sentinel baseline.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/REVIEW-REGISTER.md && test -f $F && test $(wc -l < $F) -ge 100 && for s in b5f5e82 9658375 d3b226a fcb6d10 02b4543 98c2891 'review_date < commit_date' 'Back-dating any of these rows is itself a defect' 'SPEC-04' 'Standing rule for Phases 2-6' 'Gate satisfied?' INVENTORY.md RULINGS-TRIAGE.md NOTATION-MAP.md FINDING-REGISTER.md UNITS-LEDGER-EXTENSION.md REVIEW-REGISTER.md; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && test $(grep -c '^### R-0' $F) -eq 5 && grep -qF 'MevTaxControl.lean' $F && grep -qF 'MevTaxProgram.lean' $F && test $(grep -cF 'Two-step review:** NOT RUN before commit' $F) -ge 2 && test $(grep -cF 'NOT independently verified' $F) -ge 2 && grep -qF 'is NOT an axiom sweep' $F && test $(grep -cF 'RETROACTIVE' $F) -ge 4 && test $(grep -cF 'Reality Checker' $F) -ge 4 && grep -F '02b4543' $F | grep -qE 'beta|β' && grep -F 'fcb6d10' $F | grep -qE 'count|37|φ̃' && grep -qx '## 2. Phase 1 entries' $F && test $(awk '/^## 2\. Phase 1 entries/,/^## 3\./' $F | grep '^|' | grep -vE '^\|[- :|]*$' | grep -cE '\| *(YES|NO —)') -ge 6 && test $(awk '/^## 2\. Phase 1 entries/,/^## 3\./' $F | grep '^|' | grep -vE '^\|[- :|]*$' | tail -n +2 | grep -cE '[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}') -ge 6 && git log -1 --name-only --format="" | grep -q 'REVIEW-REGISTER.md' && git log -1 --name-only --format="" | grep -q 'UNITS-LEDGER-EXTENSION.md' && ! git log -1 --name-only --format="" | grep -q '^\.planning/' && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && echo PASS</automated>
  </verify>
  <done>The register records three RETROACTIVE first entries with every finding attached to its correct remediating commit, a per-row `Gate satisfied?` cell filled from the record rather than a heading asserting compliance, the `review_date < commit_date` invariant, and the standing rule for Phases 2-6.</done>
</task>

</tasks>

<verification>
1. Merge shape: the proposed `## 6.` block has the base table's four columns and every
   enforcement cell is `[FACT]` or `[PLANNED — owner]`; `nothing today` appears nowhere.
2. Peer non-interference: the sentinel-extracted baseline diffs clean against live porcelain,
   with no fallback path; `git -C plank hash-object notes/UNITS_AND_SCALES.md` unchanged.
3. Message hygiene: no `SRC:NNN` line citation and no `INV-E<n>` identifier appears in the
   MESSAGE A block; all four `SRC` errata/corrections live only at `INVENTORY.md` §9. The
   message DOES carry the `\sigma`/`\sigma^2` units question (O4), which is genuinely a
   `DOC` Definition 18 convention question and therefore theirs.
4. Routing honesty: both `**Reply:**` fields are non-empty; a missing owner is recorded as
   `NO ADDRESSEE`, never re-targeted at `ul2inqpl`.
5. Register honesty: exactly five `### R-0` rows (`R-01`-`R-03` the founding commits, `R-04`/
   `R-05` the two Aristotle bundles); no retroactive row's review date precedes its commit
   date; every §2 row's `Gate satisfied?` cell is filled.
</verification>

<success_criteria>
- `NOT-05` closed: a proposed diff that would merge, adding exactly what the table lacks,
  re-deriving nothing, routed to `ul2inqpl`, with the peer tree provably unchanged.
- `HND-05` closed: an honest register — three retroactive entries, correct commit attribution,
  a checkable invariant, and a per-row gate cell.
- No on-chain cost, gas or storage claim appears in either artifact.
- Silence from a peer, and absence of a peer, are each recorded as themselves.
</success_criteria>

<output>
After completion, create
`/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/phases/01-ground-truth-notation-and-the-rulings-triage/01-05-SUMMARY.md`
</output>
