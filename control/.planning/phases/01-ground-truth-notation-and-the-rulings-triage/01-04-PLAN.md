---
phase: 01-ground-truth-notation-and-the-rulings-triage
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md
autonomous: true
requirements: [NOT-03, HND-04]

must_haves:
  truths:
    - "One identifier scheme replaces the four research taxonomies, and a reverse index resolves every legacy ID — including ones that split across two CF rows."
    - "No finding is used downstream until it has been re-opened at its cited locus against a pinned sha."
    - "Findings the 2026-08-08 rulings and later measurement killed are SUPERSEDED and cannot be carried by accident."
    - "Every count in the register was measured at execution time, not copied from the plan."
    - "No legacy ID whose subject a ROADMAP success criterion names can fall into NOT-CARRIED."
    - "A reader reaching for LEAN-MAP.md or EVM-CONTROL-PRIMITIVES-MAP.md finds a do-not-cite ruling naming the specific stale claim, without this project having edited another project's files."
  artifacts:
    - path: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md"
      provides: "The CF-NN scheme, the reverse index, per-finding re-verification, the SUPERSEDED set, the honest P2 count, and the do-not-cite ruling"
      min_lines: 200
      contains: "SUPERSEDED"
  key_links:
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md"
      to: "notes/VOLATILITY_INTRUMENTS_MEV.md"
      via: "each carried finding re-verified at its NUMBERED BLOCK against the pin"
      pattern: "Definition 35"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md"
      to: "lean4-spec/lean/vol_markets/MevOptimization.lean"
      via: "the (β,γ) misquotation checked against the declaration that contradicts it"
      pattern: "mevMulti_mono_beta"
    - from: "spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md"
      to: ".planning/research/v2-controller/LEAN-MAP.md"
      via: "do-not-cite ruling recorded here rather than as a header in the other project's file"
      pattern: "DO-NOT-CITE"
---

<objective>
Replace four non-aligned research taxonomies with one identifier scheme, re-verify every
finding this project intends to carry against a pinned locus, mark the findings later rulings
and measurements killed as SUPERSEDED, state the honest independence count for P2, and record
the do-not-cite ruling on the two stale v2-controller documents.

Purpose: `NOT-03`, `HND-04`. Without this, a later phase cites `B6` and adjudicates a claim the
user already killed.

Output: `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research/SUMMARY.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/RULINGS-TRIAGE.md
@/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md

**Path glossary.** `WT` = `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller`. `PLANK` =
`/home/jmsbpp/cfmms-playground/cfmm-wt/plank` (PEER-OWNED, READ-ONLY). `LEAN` =
`/home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec` (PEER-OWNED, READ-ONLY). `RESEARCH` =
`WT/control/.planning/research` (this project's own — readable). `V2RES` =
`WT/.planning/research/v2-controller` (the OTHER project's directory — **READ-ONLY, NEVER
WRITTEN**). `SRC` = `WT/notes/VOLATILITY_INTRUMENTS_MEV.md` (**this project's own file**).

**Hard prohibitions.** Do not create, modify or delete anything under `PLANK/`, `LEAN/`, or
`WT/.planning/` — the last includes `V2RES`. Reading `V2RES` is required; writing to it is
forbidden. Do not edit `SRC`.

**`grep` dispatches to `ugrep`** — use `-F` for every LaTeX or backslash literal.

**MEASUREMENT DISCIPLINE — read this twice.** This register has now been the site of THREE
separate hardcoded-count errors (37→23 Lean files; 88/46/23 `.tex` items; 128 `deriv`). Every
count in this artifact is produced by a command run at execution time. **A plan-time number
quoted below is a CROSS-CHECK, never a value to copy.** Where your measurement differs, record
both, name the drift, and use yours. An acceptance criterion below requires the recorded count
to equal the live command output.

**Ownership.** `INVENTORY.md` §10 assigns topics. Citation form (§5), pins (§2) and `SRC`
errata (§9) belong to `INVENTORY.md`. **Errata carry `INV-E<n>` IDs in `INVENTORY.md`'s own
namespace — this register does NOT number them and there is nothing to backfill.** `CF-05` is
the FINDING that `SRC:70` misquotes a theorem; `INV-E3` is the ERRATUM and supplies the
read-through tag. Cite `INV-E<n>`; never re-issue it. Point with
`> Authoritative detail: <artifact> §N`; never restate.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the CF-NN scheme, the seed crosswalk, and a complete reverse index</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research/CLAIMS.md`
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research/PITFALLS.md`
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research/FRAME.md` (especially the `W1`-`W7` block around lines 105-120 and the confidence table around line 270)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research/ARCHITECTURE.md`
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/ROADMAP.md` — Phase 1 SC 4, **and every other phase's success criteria** (you need them for the no-orphan rule)
  </read_first>
  <action>
Create `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md`.

Header, verbatim except the date:

```
# FINDING REGISTER — One Scheme, Re-Verified

**Requirements:** NOT-03, HND-04
**Written:** <YYYY-MM-DD>

> Authoritative detail: pins and citation form — `INVENTORY.md` §2, §5. `SRC` errata and the
> closures for `INV-E1`/`INV-E2` (both loci STRUCK at `cf386de`) — `INVENTORY.md` §9. The 13
> blocking decisions —
> `RULINGS-TRIAGE.md`. Notation rulings — `NOTATION-MAP.md`. This document restates none of
> them.

**Scheme.** Every finding this project carries has one identifier, `CF-NN`. The four research
taxonomies (`P1–P4` / `C-P#-#` / `A#`; `B#` / `M#` / `N#` / `R#` / `S#`; `FINDING A/B` / `W#`;
`ARCHITECTURE`'s direct `P1–P4` references) are RETIRED. A later artifact citing a retired ID
is a defect — §1.2's reverse index is how a retired ID is resolved.

**Status vocabulary — every row carries exactly one of SEVEN.**
| Status | Meaning |
|---|---|
| `CARRIED-VERIFIED` | Re-opened at its cited locus against the pin; the cited text is present and says what the finding says it says. Usable downstream. |
| `DROPPED` | Re-opened and **citation does not verify**. Not usable. Reason recorded. Every `DROPPED` row must contain the literal phrase `citation does not verify` — if it does not, the row is mis-statused. |
| `SUPERSEDED` | Killed by a later ruling or a later measurement. Not usable, never revived. The superseding authority is named. |
| `RESOLVED-BEFORE-PHASE-1` | The finding was CORRECT and was already fixed before this phase began — **either on the planning record, or AT SOURCE by a `SRC` commit that struck or corrected the cited material.** Nothing left to carry, but it is NOT a failed citation and it is NOT `DROPPED`. The remediating commit is named. |
| `HYPOTHESIS-DEPENDENT` | Truth turns on a named typed hypothesis. **May not be logged as a refutation** (`PRF-09`). |
| `CLOSED-BY-PHASE-1` | Superseded by an artifact THIS phase wrote (the triage or the notation map). Cite that artifact instead. |
| `NOT-CARRIED` | In scope for no later phase. No re-verification performed; reason recorded. |

**Re-verification rule.** No finding may be used by any later phase in a status other than
`CARRIED-VERIFIED`. Re-verification means: open the cited file at the cited locus, confirm the
quoted text is present, and record the pin the check was made against. A finding carried
without this check is a hypothesis, not a finding.

**No-orphan rule.** A legacy ID may NOT be routed to §1.2 as `NOT-CARRIED` if any `ROADMAP.md`
success criterion, in any phase, names its subject. Such an ID gets a `CF-NN` row.
```

Then `## 1. Crosswalk`. Enumerate the source IDs first:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research
for f in CLAIMS.md PITFALLS.md FRAME.md ARCHITECTURE.md; do echo "=== $f ==="; grep -oE '\b(C-P[0-9]-[0-9]+|A[0-9]{1,2}|B[0-9]{1,2}|M[0-9]{1,2}|N[0-9]{1,2}|R[0-9]{1,2}|W[0-9]{1,2}|NC-[0-9]|S[0-9])\b' "$f" | sort -u | tr '\n' ' '; echo; done
cat CLAIMS.md PITFALLS.md FRAME.md ARCHITECTURE.md | grep -oE '\b(C-P[0-9]-[0-9]+|A[0-9]{1,2}|B[0-9]{1,2}|M[0-9]{1,2}|N[0-9]{1,2}|R[0-9]{1,2}|W[0-9]{1,2}|NC-[0-9]|S[0-9])\b' | sort -u > /tmp/legacy-ids.txt
wc -l /tmp/legacy-ids.txt
```

Paste the raw output. Plan-time cross-check: 36 / 37 / 8 / 3 per document, **79 unique overall**.
Note that `CLAIMS.md` also references `M2` and `M9` (PITFALLS IDs) — the per-document lists
overlap, which is exactly why the union is the thing to count.

`### 1.1 — Seed crosswalk`. Table with SEVEN columns, in this order:
`CF-NN | Headline (one line) | CLAIMS | PITFALLS | FRAME | ARCHITECTURE | Status`.
Use `—` where a document has no corresponding ID. Seed rows (verify each in Task 2):

| CF | Headline | Sources | Provisional status |
|---|---|---|---|
| CF-01 | The direct monoid path: `\tau_{\text{MEV}}` reaches `\phi_{\text{total}}` without passing through `\nu` — **`Definition 35 (Monoid gradient) @ 33af6a85`** carries the entry `(1-\phi_X)(1-\phi_M)`, so P2's "no other path" clause is false. **Machine-proved: `T29` (direct monoid path) in `MevTaxControl.lean`, and as of `cf386de` TRANSCRIBED INTO THE SOURCE as `Theorem 29 (The monoid path is direct) @ 33af6a85` and `Theorem 30 (Path decomposition) @ 33af6a85`.** | PITFALLS B2; CLAIMS C-P2-*; FRAME FINDING A; ARCHITECTURE | CARRIED-VERIFIED |
| CF-02 | The box solved `\partial\widehat\pi^\sigma/\partial\tau_{\text{MEV}} = \Delta Q_v^\star`, not the stated `\pi^\sigma \equiv^R \widehat\pi^\sigma`; `(\sigma^2-\sigma_K^2)^+` was absent from the boxed form | PITFALLS B1 | **RESOLVED-BEFORE-PHASE-1** — the boxed form, the `\equiv^R` relation and the `\pi^\sigma = \Delta Q_v^\star(\sigma^2-\sigma_K^2)^+` definition were ALL struck at `cf386de`; `Definition 36 (Tax program) @ 33af6a85` now states the objective and the constraint separately. Re-verify against the STRUCK probes, not against a line |
| CF-03 | Two incompatible sections in the `\pi^{\phi}` algebra (was `SRC:110 @ 0ec30184` → `SRC:115 @ 45aeba4c`, **STRUCK at `cf386de`**) — **machine-proved as `Theorem30`, verdict ILL-POSED**: `(\phi_M,\phi_X,\tau)\mapsto\phi_{\text{total}}` is a submersion `\mathbb{R}^3\to\mathbb{R}` with no inverse. **RE-AIM: the claim is now IN the source as `Theorem 31 (The section sum is ill-posed) @ 33af6a85`** | PITFALLS B5 | CARRIED-VERIFIED (against `Theorem 31`, never against a line) |
| CF-04 | The "closed form" is an implicit fixed point — `\tau` on both sides via `\partial\nu/\partial\tau_{\text{MEV}}` | PITFALLS B4 | CARRIED-VERIFIED |
| CF-05 | The old `SRC:70` cited a theorem that does not exist: no result says `(\beta_j,\gamma_j)` fail to control `\lambda_{\text{MEV}}`; T12 `mevMulti_mono_beta` proves the OPPOSITE for `\beta` | PITFALLS M2; CLAIMS A1; PROJECT.md | **RESOLVED-BEFORE-PHASE-1** — the sentence was removed and the freeze restated as a declared assumption in `Rule 13` at commit `c521af5`; recorded at `INVENTORY.md` §9.3 |
| CF-06 | Three non-differentiabilities sit where the derivation differentiates; the OTM branch has no interior solution | PITFALLS B7 | CARRIED-VERIFIED |
| CF-07 | `arb_add_fee_eq_lvr` is a `ring`-grade bridge identity, never "MMR Theorem 3 formalized" | CLAIMS A3; PITFALLS R8, M10 | CARRIED-VERIFIED |
| CF-08 | The event-clock question: does `t` index swaps or blocks; may event-averaged `\Delta Q_M,\Delta Q_X` be combined with time-averaged `\pi^{\mathrm{LVR}}\Delta t, \sigma^2, \lambda`; PASTA/ASTA argued not to hold in a CFMM | FRAME; PITFALLS | CARRIED-VERIFIED → Phase 2 `FRM-03` |
| CF-09 | The not-primary-verified citation set is ≈13, not 5 | FRAME W-series; REQUIREMENTS FRM-04 | CARRIED-VERIFIED → Phase 2 `FRM-04` |
| CF-10 | `\partial\nu/\partial\lambda_{\text{MEV}}` substituted for `\partial\nu/\partial\tau_{\text{MEV}}`; composing was argued to flip the sign and drive `\tau^\star>1` | PITFALLS B3; CLAIMS C-P4-3; ARCHITECTURE | **HYPOTHESIS-DEPENDENT** |
| CF-11 | The Rule-9 route to `\tau^\star_{\text{MEV}} = 1` | PITFALLS B6 (part); CLAIMS C-P4-* | **SUPERSEDED** |
| CF-12 | `\partial L(i_K)/\partial\pi^{\phi} \equiv 0` — the channel is dead | PITFALLS B6 (part) | **SUPERSEDED** |
| CF-13 | "The tree contains essentially no differential-calculus infrastructure" | CLAIMS §2.4, A-series | **SUPERSEDED** |
| CF-14 | "4 of 4 researchers converged" on P2 | SUMMARY.md | **SUPERSEDED** |
| CF-15 | `LEAN-MAP.md` is stale — "exactly one Lean source file" | ARCHITECTURE | CARRIED-VERIFIED → `HND-04` |
| CF-16 | `EVM-CONTROL-PRIMITIVES-MAP.md` is stale | ARCHITECTURE | CARRIED-VERIFIED → `HND-04` |
| CF-17 | The four notation collisions | CLAIMS NC-1..4; PITFALLS M7 | **CLOSED-BY-PHASE-1** — `NOTATION-MAP.md` §1 |
| CF-18 | `PROJECT.md`'s Lean-file count was wrong (claimed 37) | CLAIMS | **RESOLVED-BEFORE-PHASE-1** — see Task 2's live re-measure |
| CF-19 | The self-inflicted `\pi^{\tilde\phi}` mint | PITFALLS M7 | **RESOLVED-BEFORE-PHASE-1** — corrected at commit `fcb6d10`; glyph banned at `NOTATION-MAP.md` §4.3 |
| CF-20 | The two-step review deferral debt from v2-controller `SPEC-04` | ARCHITECTURE; PITFALLS | CARRIED-VERIFIED → plan 01-05 (`HND-05`) |
| CF-21 | **Root vs argmin.** At `45aeba4c` `SRC` said "solving for `\tau_{\text{MEV}}` on the **minimization**" while displaying a closed form obtained from the **equation** `\pi^\sigma \equiv^R \widehat\pi^\sigma`. **RESOLVED-AT-SOURCE at `cf386de`:** both the sentence and the boxed form were struck, and `Definition 36 (Tax program) @ 33af6a85` now states `\min_{\tau_{\text{MEV}} \in [0,1]} (\partial\widehat\pi^\sigma/\partial\tau_{\text{MEV}})^2` **subject to** `\pi^\sigma = \widehat\pi^\sigma`, with the FOC written beneath — objective and feasibility separated exactly as the finding demanded | FRAME W4 | **RESOLVED-BEFORE-PHASE-1** (`cf386de`) — record the closure, do not carry it open |
| CF-22 | **Admissibility / projection.** `\tau_{\text{MEV}} \in [0,1]` is forced by the fee monoid being closed on `[0,1]`. At `45aeba4c` the boxed form was `1 - (\text{bracket})` with **nothing bounding the bracket**. **PARTLY RESOLVED at `cf386de`:** the box is struck, `Definition 36 @ 33af6a85` writes the box constraint into the program (`\min_{\tau_{\text{MEV}} \in [0,1]}`), and `Proposition 13 (Corrected law) @ 33af6a85` states the domain — `\tau^\star < 1` always, `\tau^\star > 0` **iff** `1-\phi_X < \lvert(\partial\phi/\partial\nu)(\partial\nu/\partial\tau_{\text{MEV}})\rvert`. **What SURVIVES and is still carried:** the source states the domain but **still specifies no PROJECTION and no infeasibility handling** for the `\tau^\star \le 0` branch. Re-verify against `Definition 36` and `Proposition 13`, and record precisely which half closed | FRAME W5 | CARRIED-VERIFIED (narrowed) → Phase 5 SC 3 |
| CF-23 | **Accumulator states ⟹ no steady state.** `\lambda_{\text{ARB}}`, `\lambda_{\text{FLAIR}}`, `\tilde\lambda_{\text{JIT}}` are explicitly running sums in the source; whether `\partial_{(t+1,t)}` is the zero matrix turns on whether `\lambda_{\text{MEV}}` enters the state through `\nu` | FRAME W1 | CARRIED-VERIFIED → Phase 2 SC 1 |
| CF-24 | **Dimensional consistency and back-substitution** of the boxed display | FRAME W6, W7 | CARRIED-VERIFIED → Phase 3 SC 3 (`PRF-09`) and `NOT-05` |
| CF-26 | **`T32` — `\lambda_{\text{ARB}}` is `StrictAntiOn` in `\tau`** (`MevTaxControl.lean`) | — | — | — | — | CARRIED-VERIFIED → Phase 4 (`PRF-05`) |
| CF-27 | **`M19_FOC_is_not_feasibility`** — the FOC `\partial\widehat\pi^\sigma/\partial\tau_{\text{MEV}} = 0` and feasibility `\mathcal{E} = 0` are DISTINCT: FOC ⟺ `\mathcal{E} = 0` fails in both directions. The source's "target replication relation" objective is SUPERSEDED by exposure minimization | — | — | — | — | CARRIED-VERIFIED → Phase 5 SC 1 (`PRF-04`) |
| CF-28 | **`Proposition16` (M24) — the term-by-term audit of the boxed law.** Nine verdicts; the corrected law is `Proposition16_corrected_law` | — | — | — | — | CARRIED-VERIFIED → Phase 5 (`PRF-04`, `SAL-01`) |
| CF-29 | **`Proposition15_level_reading_second_order_undetermined`** — the FOC root is NOT established to be the minimiser. The settling hypothesis (single crossing from below of the total derivative) is stated and USED in `Proposition15_single_crossing_gives_minimum` but **never discharged** | — | — | — | — | CARRIED-VERIFIED → `HND-01` gap register |
| CF-30 | **`\phi_X`'s ν-dependence — CLOSED AT SOURCE.** At `45aeba4c` `Rule 13` stated `\phi_X(t) = \Phi(\Theta_{\phi}; \sigma^2(i(t)))` — `\sigma` alone, no `\nu` — while `DOC` Definition 18's schedule is `\bar\phi + (\sum_j \text{sigmoid}_j(\sigma))\cdot\text{gate}(\nu)`. **At `33af6a85` `Rule 13` reads `\Phi(\Theta_{\phi}; \sigma(i(t)), \nu(t))`** — `\nu` added, `\sigma^2` corrected to `\sigma`. **Open item O3 is CLOSED (`cf386de`).** What is CARRIED is the CONSEQUENCE, not the defect: the corrected law is self-referential in `\phi_X` as well as in `\nu`, exactly as `Proposition 13`'s own rider states. **A NEW sub-finding splits out: `CF-31`** — `Rule 13`'s `\sigma` now disagrees with `Definition 32`'s `\sigma^2` disturbance slot INSIDE one document (open item **O4**) | — | — | — | — | **RESOLVED-BEFORE-PHASE-1** (`cf386de`) for O3; the consequence → `HND-01` |
| CF-31 | **`\sigma` vs `\sigma^2` inside `SRC` (open item O4).** `Rule 13 @ 33af6a85` takes `\sigma(i(t))`; `Definition 32 (Event-time plant) @ 33af6a85`'s `u_{\text{ex}}` third slot carries `\sigma^2(i(t))`. `\Theta_{\phi}`'s centers `\beta_j` are therefore in `\sigma`-units and the disturbance is in `\sigma^2`-units. Split out of CF-30 because CF-30's O3 half closed at `cf386de` and its O4 half did not | — | — | — | — | CARRIED-VERIFIED → `NOT-05` (plan 01-05 §2b), then `HND-01` |
| CF-25 | **The Implicit Function Theorem is the actual well-posedness theorem** — define the signed residual and discharge existence / uniqueness / `C¹`-dependence by IFT + IVT + strict monotonicity, not by a controllability Gramian or a Riccati equation (`FRAME.md:103`) | FRAME W3 | CARRIED-VERIFIED → Phase 4 SC 1 (`PRF-01`) |

**`W2` disposition (explicit, not silent).** `FRAME.md:101` — "Right-invertibility of the
steady-state map at the operating point", Brockett–Mesarović (1965) / Silverman (1969). Apply
the no-orphan rule: read `ROADMAP.md` Phase 2 SC 2 and Phase 4 SC 1 and decide whether either
names right-invertibility or the rank of the steady-state map. If yes, seed `CF-26`; if no,
give `W2` an explicit `NOT-CARRIED` row in §1.2 with the reason written out. **Either way it
must appear — an ID with no disposition is the defect this section exists to prevent.**

Add any further `CF-` rows the no-orphan rule forces. **Before finishing §1.1, apply the
no-orphan rule explicitly**: for each remaining legacy ID, ask whether any `ROADMAP.md` success
criterion names its subject; if yes, it gets a row.

`### 1.2 — Reverse index (legacy ID → CF-NN)`. **This is NOT a column scan of §1.1** — a single
legacy ID may map to two `CF` rows (`B6` deliberately splits across CF-11 and CF-12), and a
column scan resolves such an ID wrongly. Build a table `Legacy ID | Document | → CF-NN(s) or
NOT-CARRIED | Note`, with **one row for every ID in `/tmp/legacy-ids.txt`** — all 79 (or your
measured count). `B6` maps to `CF-11, CF-12`. `NOT-CARRIED` rows carry a one-line reason
(typical: "EVM-track finding, moved to v2 by the 2026-08-08 scope ruling"; "process
observation, no downstream dependency"; "subsumed by CF-NN").

Close §1.2 with the totality statement:
`**Totality:** <N> legacy IDs enumerated; <M> rows in this index; N = M. Verified by the
command in this section.`
  </action>
  <acceptance_criteria>
    - `spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md` exists.
    - It contains ≥ 30 distinct `CF-NN` identifiers (CF-26…CF-30 carry the two returned Aristotle bundles and the two gap items; **CF-31 carries open item O4, split out of CF-30 when `cf386de` closed CF-30's O3 half**).
    - It contains `T29`, `Theorem30`, `T32`, `M19_FOC_is_not_feasibility`, `Proposition16_corrected_law`, `Proposition15_level_reading_second_order_undetermined`, and `Rule 13`.
    - **It cites the four `SRC` blocks that replaced struck material, by FULL name and pin:** `Theorem 29 (The monoid path is direct) @ 33af6a85`, `Theorem 31 (The section sum is ill-posed) @ 33af6a85`, `Definition 36 (Tax program) @ 33af6a85`, `Proposition 13 (Corrected law) @ 33af6a85`. It also cites `Convention 8 (Liquidity axis) @ 33af6a85` in CF-12's supersession.
    - **CF-30 states O3 CLOSED naming `cf386de` and quotes BOTH `Rule 13` signatures; CF-31 exists and states O4 OPEN, routed to `NOT-05`.**
    - It contains all SEVEN status tokens including `RESOLVED-BEFORE-PHASE-1`.
    - **Every row or block statused `DROPPED` contains the literal phrase `citation does not verify`** — enforced by requiring `citation does not verify` occurrences ≥ `DROPPED` occurrences outside the status-vocabulary table.
    - It contains `Re-verification rule.`, `is a hypothesis, not a finding`, and `No-orphan rule.`
    - §1.1's table header row has exactly 7 columns — verified by counting `|` characters in the header line (8 pipes ⇒ 7 columns), NOT by eyeballing. A 4-column crosswalk puts the four taxonomies back into one cell, which is precisely what `NOT-03` exists to kill.
    - It contains `CF-25` and the literal string `W2` disposition (explicit, not silent).`
    - §1.2 exists and contains a row for EVERY id in the live enumeration — checked by looping the enumeration against **§1.2's span only**, with **word-boundary anchoring** so `A1` is not satisfied by `A10`, nor `M1` by `M10`, nor `W2` by `W20`.
    - The `B6` row in §1.2 names BOTH `CF-11` and `CF-12`.
    - It contains `CF-21`, `CF-22`, `CF-23`, `CF-24` and the literal strings `Root vs argmin`, `Admissibility / projection`, `Accumulator states`.
    - It contains `Totality:` and the literal string `N = M`.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md && R=control/.planning/research && test -f $F && test $(grep -oE 'CF-[0-9]{2}' $F | sort -u | wc -l) -ge 30 && for d in 'T29' 'Theorem30' 'T32' 'M19_FOC_is_not_feasibility' 'Proposition16_corrected_law' 'Proposition15_level_reading_second_order_undetermined' 'Rule 13'; do grep -qF -- "$d" $F || { echo "MISSING DECL: $d"; exit 1; }; done && for s in CARRIED-VERIFIED DROPPED SUPERSEDED RESOLVED-BEFORE-PHASE-1 HYPOTHESIS-DEPENDENT CLOSED-BY-PHASE-1 NOT-CARRIED "Re-verification rule." "is a hypothesis, not a finding" "No-orphan rule." "CF-21" "CF-22" "CF-23" "CF-24" "Root vs argmin" "Admissibility / projection" "Accumulator states" "Totality:" "N = M"; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && grep -qF 'CF-25' $F && grep -qF '`W2` disposition (explicit, not silent).' $F && H=$(awk '/^### 1\.1/,/^### 1\.2/' $F | grep -m1 '^| *CF') && test $(printf '%s' "$H" | tr -cd '|' | wc -c) -eq 8 && cat $R/CLAIMS.md $R/PITFALLS.md $R/FRAME.md $R/ARCHITECTURE.md | grep -oE '\b(C-P[0-9]-[0-9]+|A[0-9]{1,2}|B[0-9]{1,2}|M[0-9]{1,2}|N[0-9]{1,2}|R[0-9]{1,2}|W[0-9]{1,2}|NC-[0-9]|S[0-9])\b' | sort -u > /tmp/legacy-ids.txt && IDX=$(awk '/^### 1\.2/,0' $F) && MISS=0 && while read -r id; do printf '%s\n' "$IDX" | grep -qE "(^|[^A-Za-z0-9-])$(printf '%s' "$id" | sed 's/[].[^$*\\]/\\&/g')([^A-Za-z0-9-]|$)" || { echo "ORPHAN LEGACY ID: $id"; MISS=1; }; done < /tmp/legacy-ids.txt && test $MISS -eq 0 && awk '/^### 1\.2/,0' $F | grep -F '| B6 ' | grep -qF 'CF-11' && awk '/^### 1\.2/,0' $F | grep -F '| B6 ' | grep -qF 'CF-12' && echo PASS</automated>
  </verify>
  <done>One `CF-NN` scheme with a seven-token status vocabulary, a seven-column seed crosswalk seeded to 24 rows including the four FRAME findings a ROADMAP criterion names, and a reverse index with a row for every one of the live-enumerated legacy IDs — `B6` resolving to both of its CF rows.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify every carried finding; record the SUPERSEDED set and the honest P2 count</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md`
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/notes/VOLATILITY_INTRUMENTS_MEV.md` — **ALL 190 lines / 14 numbered blocks.** The `∇φ` display is `Definition 35`; the sectioning step is now `Theorem 31`; the minimization sentence and the boxed form were **STRUCK at `cf386de`** and replaced by `Definition 36` and `Proposition 13`. **Locate every block by content (`grep -nF`), never by line — the old ranges 104-122 and 200-235 no longer contain what this task used to read there**
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec/lean/vol_markets/MevOptimization.lean` around T12 — locate by content: `grep -nF 'mevMulti_mono_beta'`
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/spec/VOLATILITY_INSTRUMENTS_MEV_TAX/NOTATION-MAP.md` §2 (the SETTLED record superseding CF-11/CF-12, now corroborated by `Convention 8 @ 33af6a85`) and `INVENTORY.md` §9 (the `INV-E1`/`INV-E2` closures and `INV-C1`)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/research/SUMMARY.md` lines 14, 77-78, 106-113 (the "4 of 4" claims being retracted)
  </read_first>
  <action>
Append `## 2. Re-verification record`. One block per `CARRIED-VERIFIED` finding:

```
### CF-NN — <headline>

**Cited at:** <locus in INVENTORY.md §5 form — for `SRC` this is BY NUMBERED BLOCK, e.g. `Definition 35 @ 33af6a85`; a `SRC:NNN` line citation is a defect>
**Quoted text found at that location:**
> <verbatim, copied from the file you just opened>
**Verdict:** VERIFIED  (or: DROPPED — citation does not verify: <reason>)
**Carried to:** <the later phase / requirement ID that consumes it, or "record only">
```

Specific checks you MUST perform and record:

- **CF-01** — locate `Definition 35 (Monoid gradient) @ 33af6a85` by content
  (`grep -nF 'nabla \phi'`); confirm the `\nabla\phi` display's third bracket entry is
  `(1-\phi_X)(1-\phi_M)` — an entry with NO `\tau_{\text{MEV}}` factor beside two that carry
  it. Quote the whole display line. **This is the single checkable fact the Phase 4 branch gate
  is sized on**; if it does not verify, say so loudly and mark it DROPPED. **Cite it as
  `Definition 35 @ 33af6a85`, never as `SRC:56` or `SRC:65`** — those addresses belong to two
  superseded pins. Then ALSO quote `Theorem 29 (The monoid path is direct) @ 33af6a85`, which
  `cf386de` added: the finding is now stated in the source itself, not only inferable from the
  gradient's third entry.
- **CF-02 — the object this finding was about NO LONGER EXISTS. Verify the deletion; do not
  hunt for the bracket.** Run and paste all four:

      grep -cF 'tau_{\mathrm{MEV}}^\star' notes/VOLATILITY_INTRUMENTS_MEV.md   # the boxed law -> 0
      grep -cF 'equiv^{R}' notes/VOLATILITY_INTRUMENTS_MEV.md                   # the R-relation -> 0
      grep -cF 'sigma^2(i(t))-\sigma_K^2' notes/VOLATILITY_INTRUMENTS_MEV.md    # the payoff -> 0
      grep -nF 'min_{\tau_{\text{MEV}} \in [0,1]}' notes/VOLATILITY_INTRUMENTS_MEV.md  # Definition 36 -> 1 hit

  **RE-AIM: `Definition 36 (Tax program) @ 33af6a85`.** Quote it verbatim. It states the
  objective as the squared FOC residual and replication as a **constraint** — which is the
  separation CF-02 said the box conflated. Status `RESOLVED-BEFORE-PHASE-1`, not `CARRIED-VERIFIED`:
  the finding was correct and the source fixed it. **Do NOT mark it `DROPPED`** — `DROPPED` means
  the citation does not verify, which is a different thing from the cited object having been
  deliberately removed.
- **CF-03** — the two incompatible sections were at `SRC:113`/`SRC:115 @ 45aeba4c` and are
  **STRUCK**. Confirm with `grep -cF '(1 -  \tau_{\text{MEV}})(1 - \phi_X)'` → 0. **RE-AIM:** quote
  `Theorem 31 (The section sum is ill-posed) @ 33af6a85` in full — it carries the surviving
  expression `\frac{\partial \phi_M}{\partial \phi}\,\Delta Q_M + p_{(\eta,\Delta_i)}\,\frac{\partial \phi_X}{\partial \phi}\,\Delta Q_X`
  under the conclusion `has no section-independent value`. **Quote it as the ill-posed object,
  NOT as a derivative** — that is the claim inversion the re-pin exists to prevent.
- **CF-05 — DO NOT write a live finding block for this; it belongs in §3 as
  `RESOLVED-BEFORE-PHASE-1`.** Verify the closure first:
  `grep -ciE 'theorem that (says|syays)' notes/VOLATILITY_INTRUMENTS_MEV.md` → 0 and
  `grep -cF 'does not control' …` → 0 at `33af6a85`. Quote `Rule 13`'s replacement text (the
  freeze as a DECLARED ASSUMPTION) and name `c521af5` as the fixing commit. **No `SRC:70`
  citation and no `INV-E3` read-through tag is issued** — there is nothing live to tag. Then,
  for the historical record only, locate T12 in `LEAN` by content and quote both the docstring
  `/-- T12. Raising positive-slope sigmoid centers raises the arbitrage hazard. -/` and the
  `theorem mevMulti_mono_beta` signature, with its MEASURED line number. Pin with
  `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec hash-object lean/vol_markets/MevOptimization.lean`
  (plan-time cross-check: `3bcaf940`). Then state verbatim:
  `**Routing:** \`SRC\` is THIS PROJECT'S OWN document — \`CLAUDE.md\` does not assign
  \`evm-controller/notes/\` to any peer. This erratum is therefore NOT routed to \`ul2inqpl\`.
  It is recorded at \`INVENTORY.md\` §9 with a read-through rule and carried to the gap
  register (\`HND-01\`). It is not applied to \`SRC\`, because \`SRC\` is the pinned citation
  target and editing it would invalidate every \`SRC:NNN\` pin issued before the edit.`
- **CF-07** — locate `arb_add_fee_eq_lvr`
  (`grep -rnF "arb_add_fee_eq_lvr" /home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec/lean/`),
  quote its statement and proof body, record whether the body is a one-liner.
- **CF-21 — RESOLVED-BEFORE-PHASE-1 (`cf386de`); verify the closure rather than the defect.** The "minimization"
  sentence and its "note: This needs verification" rider were struck. Confirm with
  `grep -cF 'This needs verification'` → 0 and `grep -cF 'solving for'` → 0. **RE-AIM: quote
  `Definition 36 (Tax program) @ 33af6a85` verbatim** and state that objective and feasibility
  are now separated on the page: the objective is
  `(\partial\widehat\pi^\sigma/\partial\tau_{\text{MEV}})^2`, the constraint is
  `\pi^\sigma = \widehat\pi^\sigma`, and the FOC is written beneath. Record the coincidence
  condition as HISTORY (they agreed only if the residual attained zero inside `[0,1]`) and note
  that `Definition 36` makes the question moot by writing both objects down.
- **CF-22 — half closed, half open; say WHICH.** The boxed `1 - (\text{bracket})` form is struck
  (`grep -cF 'tau_{\mathrm{MEV}}^\star'` → 0). Quote `Definition 36 @ 33af6a85`'s
  `\min_{\tau_{\text{MEV}} \in [0,1]}` (the box constraint is now IN the program) and
  `Proposition 13 (Corrected law) @ 33af6a85`'s domain lines (`\tau^\star < 1` always;
  `\tau^\star > 0` iff the gate dominates). Then state verbatim: `**What CLOSED:** the source now
  states the admissible set and the domain of the root. **What REMAINS OPEN:** the source
  specifies no PROJECTION rule and no infeasibility handling for the \`\tau^\star \le 0\` branch,
  which is what FRAME W5 actually asked for. CF-22 is carried NARROWED, not closed.`

Then `## 2b. The two returned Aristotle bundles (CF-26 … CF-30)`.

**Both bundles are on disk and are the register's first machine-verified content.** Verify and
record, live:

```
B1=/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/aristotle/tax-result/project_aristotle/RequestProject/MevTaxControl.lean
B2=/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/aristotle/tax2-result/project_aristotle/RequestProject/MevTaxProgram.lean
for f in $B1 $B2; do printf "%s lines=%s sorry=%s\n" "$(basename $f)" "$(wc -l <$f)" "$(grep -c 'sorry' $f)"; done
git -C /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller hash-object $B1 $B2
```

Plan-time cross-check: `MevTaxControl.lean` 855 lines (blocks M11-M18), `MevTaxProgram.lean`
1130 lines (blocks M19-M24), both 0 `sorry`. Record the axiom-sweep result and the
dependency byte-identity result from `TAX2_BUNDLE_NOTES.md`, quoting them rather than
asserting them.

**CRITICAL — quote the corrected law in its LITERAL Lean form, not a paraphrase.**
`TAX2_BUNDLE_NOTES.md` states it as

    ∂π̂^σ/∂τ_MEV = 0  ⟺  τ*_MEV = 1 + (1-φ_X)/((∂φ/∂ν)(∂ν/∂τ_MEV))

with a **`+`** and a **signed** denominator. The informal restatement "`τ* = 1 − (1−φ_X)/|·|`"
is equivalent ONLY because `∂ν/∂τ_MEV < 0` (the M24 row `∂ν/∂τ_MEV` — **SIGN CORRECTED**), which
makes the quotient negative. **Copy the `+` form from `Proposition16_corrected_law` and derive
the `1 −` reading beside it, showing the sign step.** Writing an absolute value the Lean does
not carry is precisely the class of defect this project exists to catch. Record the domain
verbatim too: `τ*_MEV < 1` always; `τ*_MEV > 0` **iff** `1-φ_X < |(∂φ/∂ν)(∂ν/∂τ_MEV)|`.

Reproduce the **M24 audit table** verbatim from `TAX2_BUNDLE_NOTES.md` — all nine rows, with
the four verdict classes as written (`SURVIVES`, `SPURIOUS`, `ILL-POSED`, `SIGN CORRECTED`,
`MISSING`). Do not summarise it; Phase 5 consumes it row by row.

Then `## 3. SUPERSEDED and RESOLVED-BEFORE-PHASE-1`. Blocks in this shape:

```
### CF-NN — <headline> — SUPERSEDED
**What it claimed:** <the claim>
**Superseded by:** <authority, named and located>
**Why it is dead:** <one paragraph>
**Standing instruction:** NEVER CARRIED FORWARD. A later artifact asserting this is a defect.
```

The four `— SUPERSEDED` blocks (the heading line must end with the literal ` — SUPERSEDED`):

- **CF-11** — the Rule-9 `\tau^\star_{\text{MEV}} = 1` route. Superseded by the user ruling of
  2026-08-08 (`PROJECT.md`), by `NOTATION-MAP.md` §2 / §2.1, and — as of `cf386de` — by
  `Convention 8 (Liquidity axis) @ 33af6a85`, which separates the two axes in the source. Note
  additionally that the boxed form the route attacked **no longer exists**: `Proposition 13
  (Corrected law) @ 33af6a85` replaced it. Why: Rule 9 is an identity on
  the VOL axis; `\bar L` is the price axis; a vol-axis identity constrains nothing on the price
  axis. `> Authoritative detail: NOTATION-MAP.md §2.1` — do not re-derive the reconciliation.
- **CF-12** — `\partial L(i_K)/\partial\pi^{\phi} \equiv 0`. Same authority, and **now
  corroborated by the source itself**: `Convention 8 (Liquidity axis) @ 33af6a85`, added at
  `cf386de`, writes the price-axis identity
  `\frac{\partial L(i_K)}{\partial \pi^{\phi}} = \ell(\xi,\iota;i_K)\,\frac{\partial \bar L}{\partial \pi^{\phi}}`
  into `SRC` as a numbered block. Quote it verbatim (locate with
  `grep -nF 'partial L(i_K)}{\partial \pi^{\phi}}'`). `\partial\bar L/\partial\pi^{\phi}` is a
  behavioral estimand, not a zero. **The supersession is no longer this project's inference from
  a user ruling; the source states the factorization.**
- **CF-13** — "no differential-calculus infrastructure". Superseded by direct measurement.
  **Measure it now, and record the METHOD beside every number:**
  ```
  F=/home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec/lean/vol_markets/CapponiEmbed.lean
  grep -c  '\bderiv\b' $F            # LINES containing the token
  grep -o  '\bderiv\b' $F | wc -l    # OCCURRENCES of the token
  grep -oF 'HasDerivAt' $F | wc -l
  grep -oF 'Differentiable' $F | wc -l
  grep -cF 'HasDerivAt.div' $F
  grep -cF 'hasDerivAt_rpow_const' $F
  ```
  Plan-time cross-check: `deriv` = **128 lines / 306 occurrences**; `HasDerivAt` 132
  occurrences; `Differentiable` 19; `HasDerivAt.div` 1; `hasDerivAt_rpow_const` 32.

  **This is a UNIT-AMBIGUITY finding, NOT a count error — record it as such.** The figure
  "128 `deriv`" on the planning record is CORRECT: it is the LINE count (`grep -c`). The
  occurrence count (`grep -o | wc -l`) is 306. Both are right and they measure different
  things; what was missing was the UNIT. `REQUIREMENTS.md` `PRF-06` and `ROADMAP.md` Phase 3
  Success Criterion 2 now state the method explicitly (commit `2053308`). **Do not record 128
  as a non-reproducing number** — an earlier draft of this plan did, and that was wrong.
  Record instead: *"every count of a Lean token in this project carries its method —
  `grep -c` (lines) or `grep -o | wc -l` (occurrences) — because on this file the two differ
  by more than a factor of two."* Then state verbatim:
  `**The real gap is narrower:** no derivative lemmas exist for the five named schedule
  functions \`logistic\`, \`sigmoidR\`, \`multiFee\`, \`probOr\`, \`ptrade\`. Any derivative
  layer is priced against \`CapponiEmbed.lean\` as in-tree precedent, not from scratch
  (\`PRF-06\`).`
- **CF-14** — "4 of 4 researchers converged". Superseded by this register. State verbatim:
  ```
  **Honest independence count for P2: two independent derivations, one restatement, one
  invalid. RETRACTED: "4 of 4".** All four researchers read a `PROJECT.md` that rewarded
  refutation — a shared prior, not independence. One vote analysed `\pi^\sigma`, which carries
  NO `\tau_{\text{MEV}}` dependence, instead of `\widehat\pi^\sigma`, and is therefore invalid;
  one restated another's result rather than deriving it. **This count sizes any later gate.**
  The Phase 4 branch gate is sized on ONE checkable fact — `Definition 35 (Monoid gradient) @
  33af6a85`'s third entry `(1-\phi_X)(1-\phi_M)` (CF-01), now also stated outright by
  `Theorem 29 @ 33af6a85` — and on nothing about researcher counts.
  ```
  Then classify each of the four documents as independent derivation / restatement / invalid
  **by method**, citing the method each used. Where a document's class is not determinable from
  its own text, write `UNDETERMINED from the document's own text` — an undetermined class does
  not become an independent derivation.

Then the `— RESOLVED-BEFORE-PHASE-1` blocks. **There are now SIX.** Three were repaired on the
planning record (`fcb6d10`, `02b4543`) or at `c521af5`; **three more were repaired AT SOURCE by
`cf386de`**, which struck the material they were findings about:

- **CF-02** — the box solved the wrong relation. The boxed form, the `\equiv^R` relation and the
  `\pi^\sigma` payoff definition are all struck; `Definition 36 (Tax program) @ 33af6a85`
  separates objective from constraint. Name `cf386de`; paste the four deletion probes.
- **CF-21** — root vs argmin. Same commit; `Definition 36` writes both objects down. Name
  `cf386de`; paste `grep -cF 'This needs verification'` → 0.
- **CF-30** — `\phi_X`'s `\nu`-dependence (open item **O3**). `Rule 13 @ 33af6a85` now reads
  `\Phi(\Theta_{\phi}; \sigma(i(t)), \nu(t))`. Name `cf386de`; quote both signatures. **State
  that the CONSEQUENCE is carried on (the law is self-referential in `\phi_X`) and that the
  `\sigma`/`\sigma^2` half split out as `CF-31`, which is NOT closed.**

The three planning-record ones:

- **CF-05** — the `(\beta,\gamma)` non-existent theorem. Fixed at `c521af5`: the sentence is
  gone (`grep -ciE 'theorem that (says|syays)'` → 0) and the freeze is a DECLARED ASSUMPTION in
  `Rule 13`. Quote `Rule 13`'s text. The FINDING was correct — T12 `mevMulti_mono_beta` does
  prove the converse for `\beta` — and the source now states the freeze honestly. `PROJECT.md`
  had already been corrected at `02b4543`; `c521af5` closed the source side.
  **Standing instruction:** no `INV-E3` read-through tag is issued and no live erratum is
  recorded. `> Authoritative detail: INVENTORY.md §9.3`.

- **CF-18** — the Lean-file count. **This is a live-measurement row.** Run:
  ```
  ls /home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec/lean/vol_markets/*.lean | wc -l
  cat /home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec/lean/vol_markets/*.lean | wc -l
  ```
  Record the output as `**Measured today:** <N> files, <L> lines`. Then record the drift chain
  verbatim, filling from history and your measurement:
  `**Drift chain:** \`PROJECT.md\` originally claimed 37 files. CLAIMS measured 23 files /
  10,651 lines and the correction landed at commit \`fcb6d10\`. **Today the tree measures <N>
  files / <L> lines** — the peer has added files since \`fcb6d10\`. The FINDING was correct and
  was remediated before this phase; the COUNT it was remediated to has itself drifted.`
  Then: `**Standing instruction:** the Lean file count is a LIVE MEASUREMENT, never a stored
  constant. Any artifact stating it must state the command and the date. This is the third
  hardcoded-count defect in this project's history; it is closed by making the value
  self-measuring, not by writing a better number.`
  **Status is `RESOLVED-BEFORE-PHASE-1`, not `DROPPED`** — the citation verified, the finding
  was true, and it was fixed. `DROPPED` means the citation does not verify, which is not what
  happened here.
- **CF-19** — the `\pi^{\tilde\phi}` mint. **Corrected at commit `fcb6d10`** — verify:
  `git show fcb6d10 -- control/.planning/PROJECT.md | grep -F 'π^φ̃'` should show the removal.
  Note that `02b4543` is a DIFFERENT commit — it fixed the `(\beta,\gamma)` non-existent-theorem
  claim (CF-05's record side), not the glyph. Record both commits against their correct
  findings; an earlier draft of this plan swapped them.

Then `## 4. HYPOTHESIS-DEPENDENT` — the CF-10 block:

```
### CF-10 — the sign flip driving `\tau^\star > 1` — HYPOTHESIS-DEPENDENT

**What it claimed:** substituting `\partial\nu/\partial\lambda_{\text{MEV}}` for
`\partial\nu/\partial\tau_{\text{MEV}}` and composing with proved results makes the true factor
negative, flipping the sign of the whole box and driving `\tau^\star_{\text{MEV}} > 1`, outside
the fee-monoid carrier `[0,1]`.

**Why it is not a refutation.** Its truth turns on the SIGN of
`\bar{\mathcal G}_{(\nu,\lambda_{\text{MEV}})}`, which the user ruling of 2026-08-08 made a
named typed HYPOTHESIS — an LP-supply estimand, not a proposition. `PRF-09` is explicit: a sign
or range violation whose value depends on a hypothesis is recorded as `HYPOTHESIS-DEPENDENT`
and **may not be logged as a refutation**.

**What survives and is carried:** the SUBSTITUTION itself is obligation `PRF-05` (Phase 4),
independent of the gain's sign. The ADMISSIBILITY question — what happens when the root leaves
`[0,1]` — survives independently as CF-22, which does not depend on the gain's sign at all.

**Standing instruction:** any later artifact citing CF-10 must enumerate the hypotheses its
number depends on beside the number.
```
  </action>
  <acceptance_criteria>
    - `## 2` contains ≥ 8 `### CF-` blocks, each with `**Cited at:**`, `**Quoted text found at that location:**` and `**Verdict:**`.
    - It contains `(1-\phi_X)(1-\phi_M)` and `mevMulti_mono_beta`.
    - CF-05's block contains `is THIS PROJECT'S OWN document` and `NOT routed to `ul2inqpl``, and does NOT contain `PEER-ADJACENT`.
    - Exactly four headings match `^### CF-.*— SUPERSEDED$`.
    - Exactly SIX headings match `^### CF-.*— RESOLVED-BEFORE-PHASE-1$` — CF-05, CF-18, CF-19 (planning record / `c521af5`) and **CF-02, CF-21, CF-30 (struck or corrected at source by `cf386de`)**. Each of the last three names `cf386de` and pastes its deletion or signature probe.
    - `## 2b` exists and contains `MevTaxControl.lean`, `MevTaxProgram.lean`, the literal string `1 + (1-φ_X)/((∂φ/∂ν)(∂ν/∂τ_MEV))`, the literal string `SIGN CORRECTED`, and all four other verdict tokens `SURVIVES`, `SPURIOUS`, `ILL-POSED`, `MISSING`.
    - The M24 table in `## 2b` has 9 data rows.
    - The file does NOT assert the corrected law in an absolute-value-only form: any line containing `1 −` or `1 -` next to `(1-φ_X)` is accompanied by the `+`-form and the sign step.
    - `NEVER CARRIED FORWARD` appears at least four times.
    - It contains `two independent derivations, one restatement, one invalid` and `RETRACTED: "4 of 4"`.
    - Every line containing `4 of 4` also contains `RETRACTED`, the STANDALONE word `not`, `superseded`, or `honest` (case-insensitive, word-anchored — an unanchored `not` is satisfied by *notable*, *nothing* and *cannot*).
    - It contains `logistic`, `sigmoidR`, `multiFee`, `probOr`, `ptrade`.
    - **CF-18 is self-measuring:** its recorded `**Measured today:** <N> files` value equals the live output of `ls .../vol_markets/*.lean | wc -l`, and the file contains no bare assertion that the tree has 23 files.
    - It contains `fcb6d10` and `02b4543`, and the line containing `02b4543` also contains `beta` or `\beta` or `(β` (i.e. it is attached to the right finding).
    - `## 4` exists with `HYPOTHESIS-DEPENDENT` and `may not be logged as a refutation`.
    - `git -C lean4-spec status --porcelain` unchanged; `git -C WT status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md` empty.
    - **Standing Check A** (`INVENTORY.md` §10.1): every `SRC:`/`UNITS_AND_SCALES.md:`/`ENTRY_POINT.md:`/`TEX:` line citation carries its 8-hex pin.
    - **Standing Check B**: the file issues NO live line citation of `SRC` — every `SRC:NNN` occurrence is `SRC:2`, `SRC:4-10`, or carries `was `/`STRUCK`/`0ec30184`/`45aeba4c` on the same line. **No `[ERRATUM INV-E` tag appears anywhere**: all three errata closed at source and tagging a struck locus is now itself the defect.
    - **M3 — claim hygiene on the struck derivative, not merely citation hygiene.** The struck LHS `\partial\pi^{\phi}/\partial\phi` must NEVER appear on a line that also contains `=`: `grep -E 'pi\^\{.?phi\}(\}\{.?partial|/.?partial)' <artifact> | grep -c '='` returns **0** (both the `\frac{}{}` and the `/` spellings). The expression `\partial\phi_M/\partial\phi\,\Delta Q_M + p_{(\eta,\Delta_i)}\,\partial\phi_X/\partial\phi\,\Delta Q_X` may be quoted ONLY as `Theorem 31 @ 33af6a85`'s no-section-independent-value object. **A `was `/`STRUCK` annotation is citation hygiene and does NOT license asserting the derivative** — that is the claim inversion the re-pin exists to prevent. (The address history in `INVENTORY.md` §8 is the one place the old equation may be shown; it is exempt, and it owns that record.)
    - It contains `2053308` and `UNIT-AMBIGUITY`, and does NOT contain any claim that 128 fails to reproduce.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md && test $(awk '/^## 2\./,/^## 3\./' $F | grep -c '^### CF-') -ge 8 && test $(awk '/^## 2\./,/^## 3\./' $F | grep -c '^\*\*Cited at:\*\*') -ge 8 && test $(awk '/^## 2\./,/^## 3\./' $F | grep -c '^\*\*Verdict:\*\*') -ge 8 && for s in '(1-\phi_X)(1-\phi_M)' 'mevMulti_mono_beta' "is THIS PROJECT'S OWN document" 'two independent derivations, one restatement, one invalid' 'RETRACTED: "4 of 4"' 'logistic' 'sigmoidR' 'multiFee' 'probOr' 'ptrade' 'may not be logged as a refutation' 'fcb6d10' '02b4543'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && test $(grep -cE '^### CF-.*— SUPERSEDED$' $F) -eq 4 && test $(grep -cE '^### CF-.*— RESOLVED-BEFORE-PHASE-1$' $F) -eq 6 && for s in 'Theorem 29 (The monoid path is direct) @ 33af6a85' 'Theorem 31 (The section sum is ill-posed) @ 33af6a85' 'Definition 36 (Tax program) @ 33af6a85' 'Proposition 13 (Corrected law) @ 33af6a85' 'Convention 8 (Liquidity axis) @ 33af6a85' 'CF-31' 'cf386de'; do grep -qF "$s" $F || { echo "MISSING RE-AIM: $s"; exit 1; }; done && test $(grep -E 'SRC:[0-9]+' $F | grep -vE 'SRC:(2|4-10)([^0-9-]|$)' | grep -cvE 'was |STRUCK|0ec30184|45aeba4c') -eq 0 && test $(grep -E 'pi\^\{.?phi\}(\}\{.?partial|/.?partial)' $F | grep -c '=') -eq 0 && ! grep -qF '[ERRATUM INV-E' $F && grep -q '^## 2b' $F && for s in 'MevTaxControl.lean' 'MevTaxProgram.lean' '1 + (1-φ_X)/((∂φ/∂ν)(∂ν/∂τ_MEV))' 'SIGN CORRECTED' 'SURVIVES' 'SPURIOUS' 'ILL-POSED' 'MISSING'; do grep -qF -- "$s" $F || { echo "MISSING 2b TOKEN: $s"; exit 1; }; done && test $(grep -cF 'NEVER CARRIED FORWARD' $F) -ge 4 && test $(grep -F '4 of 4' $F | grep -civE 'retracted|\bnot\b|supersed|honest') -eq 0 && ! grep -qF 'PEER-ADJACENT' $F && N=$(ls /home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec/lean/vol_markets/*.lean | wc -l) && grep -qF "**Measured today:** $N files" $F && grep -F '02b4543' $F | grep -qE 'beta|\\beta|\(β' && grep -qE '^## 4\..*HYPOTHESIS-DEPENDENT' $F && test -z "$(grep -oE '(UNITS_AND_SCALES\.md|ENTRY_POINT\.md|TEX):[0-9]+(-[0-9]+)?( @ [0-9a-f]{8})?' $F | grep -v ' @ ')" && grep -qF '2053308' $F && grep -qF 'UNIT-AMBIGUITY' $F && test -z "$(git status --porcelain notes/VOLATILITY_INTRUMENTS_MEV.md)" && echo PASS</automated>
  </verify>
  <done>Every carried finding is re-opened at its cited locus with the found text quoted; four findings are SUPERSEDED and two are RESOLVED-BEFORE-PHASE-1 with the correct remediating commits; CF-18's Lean count is measured live and its recorded value provably equals the live command output; the honest P2 count replaces "4 of 4"; CF-05's erratum is correctly routed to this project, not to a peer.</done>
</task>

<task type="auto">
  <name>Task 3: Record the DO-NOT-CITE ruling on the two stale v2-controller documents</name>
  <files>spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/.planning/research/v2-controller/LEAN-MAP.md` lines 1-20 (READ ONLY)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/.planning/research/v2-controller/EVM-CONTROL-PRIMITIVES-MAP.md` lines 1-15 and 145-160 (READ ONLY)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/REQUIREMENTS.md` (`HND-04`; and `EVM-01a` in v2, which names the counter-evidence)
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/PROJECT.md` (Constraints — "never write to the repo-root `.planning/`")
  </read_first>
  <action>
Append `## 5. DO-NOT-CITE ruling (HND-04)`. Open with the deviation statement, verbatim:

```
**Deviation from the roadmap's literal wording, stated rather than applied silently.**
`ROADMAP.md` Phase 1 Success Criterion 4 says these two documents "each carry a do-not-cite
header". They live at `.planning/research/v2-controller/` — inside the REPO-ROOT `.planning/`,
a DIFFERENT, unrelated GSD project that `PROJECT.md`'s Constraints and `ROADMAP.md`'s Standing
constraints both declare read-only to this project. Writing a header into them would violate a
higher-priority constraint to satisfy a lower-priority one.

**The ruling is therefore recorded HERE, and it binds this project's artifacts.** A later
artifact of this project citing either document is a defect, whether or not the document itself
carries a header. The proposed header text is below and is offered to that project's owner —
noting that `list_peers` may return no owner for it, in which case the offer is recorded as
having no addressee rather than as accepted.
```

Then a table `Document | Generated | Specific stale claim (quoted at its line) | Counter-evidence
(measured now) | Ruling`, with exactly these two rows — open each file and quote verbatim:

- `.planning/research/v2-controller/LEAN-MAP.md` — dated 2026-06-28 — the claim at
  `LEAN-MAP.md:9`, quoted verbatim (it asserts there is exactly one Lean source file in the
  entire repo and names `lean/exp/eta.lean`). Counter-evidence: **measure it now** —
  `ls /home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec/lean/vol_markets/*.lean | wc -l` and
  `ls /home/jmsbpp/cfmms-playground/cfmm-wt/lean4-spec/lean/exp/eta.lean 2>&1`. Record the
  measured file count and whether `eta.lean` still exists at that path. Ruling: **DO NOT CITE.**
- `.planning/research/v2-controller/EVM-CONTROL-PRIMITIVES-MAP.md` — dated 2026-06-28 — the
  claim at `EVM-CONTROL-PRIMITIVES-MAP.md:156`, quoted verbatim (it asserts there is no
  `exp`/`ln`/general `pow` on WAD, only tick-basis 1.0001 via `tick_math`). Counter-evidence:
  `REQUIREMENTS.md` `EVM-01a` records that `FixedPointMath.plk` ports solady `rpow` and
  `AdaptiveFee.plk` carries a Taylor `exp`. Ruling: **DO NOT CITE.** Add: the whole EVM track
  moved to v2 by the 2026-08-08 scope ruling, so nothing in this project should be reaching for
  this document; the ruling exists so a later reader cannot.

Then the standing instruction, verbatim:

```
**Standing instruction.** Neither document may appear in a `<context>` block, an `@` reference,
a citation, or an import-closure derivation of any artifact this project writes. Where the
module import closure is needed (`PRF-10`), it is re-derived from `lean/lakefile.toml` at
assembly time, never from `LEAN-MAP.md`.
```

**Two-step review, THEN commit.** Run Reality Checker plus one named specialist in parallel
over `FINDING-REGISTER.md`, resolve BLOCKER/MAJOR findings, record the review date. Then:

```
cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller
git add spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md
git commit -m "docs(01): finding register — one scheme, re-verified, 4 superseded

Closes NOT-03, HND-04. Retires four research taxonomies for CF-NN with a
complete legacy reverse index (B6 splits across CF-11/CF-12). Re-verifies
every carried finding at its cited locus. Marks the Rule-9 tau*=1 route,
the channel-death claim, the no-derivative-infrastructure claim and
'4 of 4' as SUPERSEDED. Adds CF-21..24 for the FRAME findings a roadmap
criterion names, and CF-31 for open item O4. Records CF-02, CF-21 and
CF-30 as RESOLVED-BEFORE-PHASE-1 because cf386de struck or corrected the
material they were about, re-aiming each at the block that replaced it
(Definition 36, Proposition 13, Theorem 31, Theorem 29, Convention 8).
The Lean file count is now a live measurement, not a stored constant.
DO-NOT-CITE ruling recorded here, not written into the other project's
tree.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

Stage only that path. Never `git add -A`.
  </action>
  <acceptance_criteria>
    - `FINDING-REGISTER.md` contains a `## 5.` heading including `DO-NOT-CITE`.
    - It contains `LEAN-MAP.md:9` and `EVM-CONTROL-PRIMITIVES-MAP.md:156`.
    - It contains `DO NOT CITE` at least twice.
    - It contains `Deviation from the roadmap's literal wording, stated rather than applied silently` and `recorded as having no addressee rather than as accepted`.
    - It contains `re-derived from` together with `lakefile.toml`.
    - It is ≥ 200 lines.
    - `git status --porcelain .planning/` prints ONLY `?? .planning/milestones/` and `?? .planning/research/` — the other project's tree is byte-untouched.
    - `git log -1 --name-only` lists `FINDING-REGISTER.md` and no `.planning/` path.
    - Live plank porcelain diffs clean against `INVENTORY.md`'s sentinel baseline.
  </acceptance_criteria>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller && F=spec/VOLATILITY_INSTRUMENTS_MEV_TAX/FINDING-REGISTER.md && test $(wc -l < $F) -ge 200 && grep -qE '^## 5\..*DO-NOT-CITE' $F && for s in 'LEAN-MAP.md:9' 'EVM-CONTROL-PRIMITIVES-MAP.md:156' "Deviation from the roadmap's literal wording, stated rather than applied silently" 'recorded as having no addressee rather than as accepted' 'lakefile.toml' 're-derived from'; do grep -qF "$s" $F || { echo "MISSING: $s"; exit 1; }; done && test $(grep -cF 'DO NOT CITE' $F) -ge 2 && test -z "$(git status --porcelain .planning/ | grep -v '^?? .planning/milestones/$' | grep -v '^?? .planning/research/$')" && git log -1 --name-only --format="" | grep -q 'FINDING-REGISTER.md' && ! git log -1 --name-only --format="" | grep -q '^\.planning/' && B=$(sed -n '/<!-- PLANK-BASELINE -->/,/<!-- \/PLANK-BASELINE -->/p' spec/VOLATILITY_INSTRUMENTS_MEV_TAX/INVENTORY.md | sed -n '/^```$/,/^```$/p' | sed '1d;$d') && diff <(printf '%s\n' "$B") <(git -C /home/jmsbpp/cfmms-playground/cfmm-wt/plank status --porcelain) && echo PASS</automated>
  </verify>
  <done>The DO-NOT-CITE ruling names both stale documents, quotes each stale claim at its line, supplies freshly measured counter-evidence, and binds this project's artifacts — recorded in this project's own tree, with the deviation stated and the other project's files byte-untouched.</done>
</task>

</tasks>

<verification>
1. Reverse-index totality: re-run the legacy-ID enumeration and confirm every ID appears in
   §1.2 (this is enforced in Task 1's `<automated>`, not merely here).
2. Every `**Cited at:**` in §2 resolves at its locus against the pin.
3. Self-measuring counts: CF-13's and CF-18's recorded values equal the live command outputs.
4. No superseded finding leaks: search every artifact under `SPECDIR` for the `τ*=1` route and
   confirm hits occur only inside §3 here and `NOTATION-MAP.md` §2/§2.1, both of which mark it
   dead.
4b. No struck locus is re-pinned: `grep -E 'SRC:[0-9]+' FINDING-REGISTER.md` returns only
   `SRC:2`, `SRC:4-10`, or lines annotated `was `/`STRUCK`/`0ec30184`/`45aeba4c`. Every `SRC`
   citation of substance is BY NUMBERED BLOCK.
5. Peer and other-project non-interference: live plank porcelain diffs clean against the
   sentinel baseline; `git -C LEAN status --porcelain` unchanged; `git -C WT status --porcelain
   .planning/` shows only the two pre-existing untracked directories.
</verification>

<success_criteria>
- `NOT-03` closed: one `CF-NN` scheme, four taxonomies retired, and a reverse index that
  resolves every legacy ID including the ones that split.
- Every carried finding is `CARRIED-VERIFIED` at a pinned locus; nothing else is usable
  downstream; `DROPPED` is reserved for citations that genuinely do not verify.
- The findings killed by the 2026-08-08 rulings and by measurement are `SUPERSEDED`; the two
  already-remediated findings are `RESOLVED-BEFORE-PHASE-1` against their correct commits.
- The Lean file count is a live measurement, closing the third instance of this error class.
- The honest P2 independence count is on the record and the Phase 4 gate is sized on
  `Definition 35 @ 33af6a85`'s `(1-\phi_X)(1-\phi_M)` entry, corroborated by `Theorem 29`.
- `HND-04` closed without writing into the repo-root `.planning/`.
</success_criteria>

<output>
After completion, create
`/home/jmsbpp/cfmms-playground/cfmm-wt/evm-controller/control/.planning/phases/01-ground-truth-notation-and-the-rulings-triage/01-04-SUMMARY.md`
</output>
