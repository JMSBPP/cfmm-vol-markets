# Summary of changes for run edb0b6a0-6f68-49ed-bfd9-9d6c1fc3ade7
Delivered the corrected MEV-tax program (M19–M24) as a new module `RequestProject/MevTaxProgram.lean` (namespace `MevTaxProgram`, ~1130 lines), with a plain-language companion `RequestProject/TAX2_BUNDLE_NOTES.md`. The project builds; the new module has **zero `sorry`** and every result is axiom-clean (`propext`, `Classical.choice`, `Quot.sound` only). Bundle 1's results are cited, never redone; (H1), (H2) are typed hypotheses and are never discharged; the composed fee is kept in `[0,1)` wherever a fee limit is taken; the strike kink is branched, not differentiated; no symbol was minted.

**M19 — the program.** `Definition33_CorrectedTaxProgram` states: minimize the exposure `𝓔(τ) = (∂π̂^σ/∂τ_MEV)²` over `τ_MEV ∈ [0,1]` subject to `π^σ = π̂^σ` as a feasibility region, with FOC `∂π̂^σ/∂τ_MEV = 0`. `M19_FOC_is_not_feasibility` proves FOC ⟺ `𝓔 = 0` (and then global minimality of `𝓔`), and exhibits witnesses: FOC without feasibility, feasibility without FOC, and the superseded `= ΔQ_v^⋆` condition holding while both fail.

**M20 — the total derivative is a sum.** `Theorem33_path_decomposition`: `∂π̂^σ/∂τ_MEV = A(1-φ_M)(1-φ_X) + A(1-φ_M)(1-τ_MEV)(∂φ/∂ν)(∂ν/∂τ_MEV)`, with exhaustiveness of the two routes argued from the plant's structure. `Theorem33_five_factor_product_is_one_summand`: the source's product **is** the gate summand (reading `∂φ/∂ν` as the composed-fee derivative), is off by the monoid Jacobian `(1-φ_M)(1-τ_MEV)` under the Definition-18 reading, and equals the total derivative iff the direct path vanishes.

**M21 — the paths oppose.** `Theorem34_opposed_signs` ((P-direct) > 0, (P-gate) < 0); `Theorem34_omitting_direct_can_reverse_sign` gives explicit data where the gate-only answer is negative while the true derivative is positive — the omission can flip the sign. The boxed `∂φ/∂ν` is transcribed (`dphidnuBoxed`), proved to be the ν-derivative of Definition 18's fee level and strictly positive under `α_R, γ_R > 0`, `α_j ≥ 0` not all zero. `Theorem34_signs_from_H1_H2` reads the signs off (H1), (H2) and the cited `tau_to_nu_strictAntiOn_under_H2`.

**M22 — existence and domain.** `Theorem35_interior_root` (IVT sufficiency; necessary *and* sufficient plus uniqueness under strict monotonicity; no absolute value used). `Theorem35_root_iff_gate_dominates_at_zero`: `D(1) > 0` unconditionally (the gate path carries `(1-τ_MEV)`), so existence reduces to gate dominance at zero tax, `|(∂φ/∂ν)(∂ν/∂τ_MEV)| > 1-φ_X`. `Theorem36_no_interior_root_off_the_band`: no interior root on exact saturation, and — quantitatively — none whenever the utilization stays off the responsive band `ν ∈ (β_R - W/γ_R, β_R + W/γ_R)` with `α_R γ_R (∑_j α_j) N e^{-W} < m`. A supporting lemma records that exact saturation is a *parameter* degeneracy (a responsive logistic never vanishes), so the band is the operative domain condition.

**M23 — second order, honestly split.** Under the exposure reading, any FOC root is a global minimiser and the SOC is `𝓔'' = 2(D')² > 0` iff the crossing is transversal. Under the *level* reading the SOC is **not settled**: two plants of the Theorem 33 shape with identical first-order data at `τ* = 1/2` (both fee-guarded in `[0,1)`) have second derivatives of opposite sign. The structure that would settle it — single crossing from below — is stated and used in `Proposition15_single_crossing_gives_minimum`, and never assumed.

**M24 — audit and corrected law.** `auditTable` gives one verdict per factor: leading `1 -` SURVIVES; `1/ΔQ_v^⋆` SPURIOUS (an artefact of the RHS `ΔQ_v^⋆`; the FOC is scale-invariant); ladder bracket SPURIOUS in the law (a positive common factor, still present in the derivative); fee-Jacobian bracket ILL-POSED; `∂φ/∂ν` SURVIVES (relocated to the denominator); `∂ν/∂τ_MEV` SIGN CORRECTED (negative); the direct monoid path, the monoid Jacobian and the self-reference of `τ*` MISSING — justified by `Proposition16_audit_justification`. `Proposition16_corrected_law`: `∂π̂^σ/∂τ_MEV = 0 ⟺ τ*_MEV = 1 + (1-φ_X)/((∂φ/∂ν)(∂ν/∂τ_MEV)) = 1 - (1-φ_X)/|(∂φ/∂ν)(∂ν/∂τ_MEV)|`, **implicit, not closed** (all right-hand data is evaluated at `ν(τ*)`), with `τ* < 1` always and `τ* > 0` iff the gate dominates; closed-form case and the ITM/OTM branch structure in `Proposition16_closed_form_and_branch_structure`.

**Named as unsettled:** the level-reading second derivative (needs single crossing from below); strict negativity of `∂ν/∂τ_MEV` at a point (strict antitonicity yields only `≤ 0` — a counterexample is given — so `< 0` is carried as a typed hypothesis); and (H1), (H2) themselves.