# Summary of changes for run 96a7116b-fcb9-4279-8a8a-88f84b0d98f9
All of `RequestProject/TAX_ADDENDUM.md` blocks M11–M18 are formalized in the new file `RequestProject/MevTaxControl.lean` (namespace `MevTaxControl`). The whole project builds, the file contains no `sorry`, and every declaration is axiom-clean (`propext`, `Classical.choice`, `Quot.sound` only). `RequestProject/TAX_BUNDLE_MAP.md` gives the block-by-block traceability table, the witnesses, the guards honoured, and the scope notes.

One declaration per numbered item:

- **M11 · Definition 32** — `EventPlant`, transcribing the state `x = [φ, ν, π^φ, π^φ − π^LVR]` (the fourth slot written out in full, no shorthand), `u_ex`, `u_en`, `y = [π^σ, π̂^σ]`, `Θ_σ` and `Θ_φ`, with `π^σ` reusing the existing `Panoptic.volOptionPayoff` carrier and `L_σ` kept as the volatility-axis ladder distinct from the price-axis `L̄`. The strike kink is branched (`piSigma_itm`, `piSigma_otm`). The standing assumptions (`φ_M ≡ φ̄_M`, `(β_j, γ_j)` frozen, `φ_X(t) = Φ(Θ_φ; σ²)`) are carried by `StandingAssumptions` as declared modelling hypotheses, never as results.
- **M12 · Theorem 29** — `Theorem29_monoid_path_is_direct`: `∂φ_total/∂τ_MEV = (1−φ_M)(1−φ_X) > 0` with `τ_MEV ↦ φ_total` strictly increasing, a channel that never mentions `ν`.
- **M12 · Corollary — REFUTED with witness** — `Corollary29_five_factor_product_not_total_derivative`: the universally quantified five-factor identity is false; witness has all outer maps the identity, `φ_M = 0`, `ν ≡ 0`, `τ_MEV = 0`, giving total derivative `1` against product `0`.
- **M12 · second question settled** — `M12_conditioned_partial_and_total_derivative_incompatible`: conditioning on `λ_MEV` zeroes `∂ν/∂τ_MEV` and kills the product, while the unrestricted total derivative is `c(1−φ_M)(1−φ_X) ≠ 0`; they cannot both satisfy the identity.
- **M13 · Theorem 30** — `Theorem30_composed_fee_submersion_section_sum_ill_posed`: the differential of `(φ_M, φ_X, τ_MEV) ↦ φ_total` is surjective (submersion), the map is not injective, and two sections agreeing at a point induce different values of `(∂φ_M/∂φ)ΔQ_M + p(∂φ_X/∂φ)ΔQ_X`.
- **M14 · Proposition 12 — REFUTES the source** — `Proposition12_boxed_law_solves_a_different_equation`: the boxed law is algebraically equivalent to `∂π̂^σ/∂τ_MEV = ΔQ_v^⋆`, and a witness shows one `τ_MEV` satisfying it at two different strikes with different `π^σ`, at which replication fails — the `(σ² − σ_K²)^+` factor is absent.
- **M14 · Proposition 13** — `Proposition13_implicit_not_closed`: closed exactly when `∂φ/∂ν` is `τ_MEV`-free (unique solution `1 − Kc`); otherwise a fixed-point equation, with witnesses having zero and infinitely many solutions.
- **M15 · Proposition 14** — `Proposition14_signed_residual_root` (IVT + strict monotonicity give a unique root of the signed residual on `[0,1]`) and `Proposition14_error_not_differentiable_at_minimiser` (the `|·|` error is non-differentiable at its interior minimiser, so no first-order condition).
- **M16 · Theorem 31** — `Theorem31_admissibility_and_projection`: root admissible iff `π̂^σ(0) ≤ π^σ ≤ π̂^σ(1)`; ITM branch above the carrier gives infeasibility with projection `τ_MEV = 1`; OTM branch gives `π^σ = 0`, no solution under a positive kernel payoff, projection `τ_MEV = 0`.
- **M17 · Theorem 32 (new)** — `Theorem32_hazard_strictAntiOn_tau`: `λ_ARB` is strictly decreasing in `τ_MEV` on `[0,1]`, proved directly on the hazard sum via the joint-action identity `probOr_multiFee_joint` (`φ̄ ↦ φ̄(1−τ)+τ` and `α ↦ α(1−τ)` simultaneously). It composes neither of the two declarations the addendum ruled out, and cites nothing tautological. A corollary `tau_to_nu_strictAntiOn_under_H2` shows the `τ → λ` substitution flips the sign: under (H2) and an explicitly carried uniform-clearing hypothesis, `ν` is strictly *decreasing* in `τ_MEV`.
- **M18** — (H1) `H1_dLbar_dpiPhi_pos` and (H2) `H2_dnu_dlamMEV_pos` are definitions used only as typed hypothesis arguments and are never discharged. `ladder_sum_eq_dQvStar` records `∑ L_σ(i_K) = ΔQ_v^⋆`, and `M18_axis_error_refuted` shows the volatility-axis invariance coexists with a strictly positive price-axis response under (H1), so the `τ*_MEV = 1` conclusion fails.

Guards: every fee limit keeps the composed fee in `[0,1)`, away from the Möbius pole of `ptrade`; the three kinks are branched rather than differentiated through; sign claims use `StrictMono`/`StrictAntiOn`/`IsMinOn`, with derivative machinery introduced explicitly only where the source's own claim is a derivative identity. Points not settled, and the hypotheses that would settle them (unconditional sign of `∂ν/∂τ_MEV`, existence for the non-closed fixed point, strict monotonicity of `π̂^σ` in `τ_MEV`), are stated explicitly in the scope notes of the map file rather than left looking proved.