# Summary of changes for run 9dbcc76f-3ca0-4f62-8f04-79485539dcf9
Formalized blocks M33–M35 of `RequestProject/TAX5_ADDENDUM.md` in one new module, `RequestProject/MevShockInput.lean` (namespace `MevTaxShock`), with a companion write-up in `RequestProject/TAX5_RETURN.md`. The whole project builds; the new module contains no `sorry`, and every theorem below is `#print axioms`-clean (`propext`, `Classical.choice`, `Quot.sound` only). `H1_dLbar_dpiPhi_pos`, `H2_dnu_dlamMEV_pos` and `MevTaxChannels.ScaleHomogeneous` are used by name as typed hypotheses only; nothing already proved elsewhere is redone, and every claim uses the bare `∂φ_X/∂ν` slot with the monoid Jacobian written out separately (standing ban 4).

**The model.** Reserves parameterized by the marginal price with the constant price-impact elasticity `a = |ε_{p/X}|` of DOC Definition 14 (`reserveX`, `reserveM`); `reserveX_constant_elasticity` shows the model realizes that observable exactly, `liquidityUnit` shows the pool capital in liquidity units is the scale `L̄` at every price, and at Rule 5 (`a = 2`, `κ_φ = 1/2`) the curve is the balanced Cobb–Douglas member. A shock `s = Δp/p` sends the pool price to the band edge, ratio `u = (1+s)(1-φ)`, engaged iff `u > 1` (`engaged_iff`).

**Theorem 45 (M33) — HOLDS, `g` explicit.** `Theorem45_shock_driven_utilization`: `ΔQ = L̄·g` and `ν = g` with `g(s,φ,κ_φ) = |u^m − u^{−m}|`, `m = (1−κ_φ)/(4κ_φ) = 1/(2|ε_{p/X}|)` (`expOfCurv_curvOfImpact`); the core identity is that the signed leg product is exactly `−(L̄·(u^m−u^{−m}))²`. The mandatory consistency proof `Theorem45_consistency_with_Theorem39` confirms `MevTaxChannels.Theorem39_arb_side_does_not_close`: the shock flow model is scale 1-homogeneous and does **not** close in `(σ, φ, Δt, ε_{p/X})`, while the ratio does — the missing primitive `L̄` is now the explicit factor. `Theorem45_benign_factorization_is_a_demand_elasticity` shows a benign factorization of the same shape would *be* the `[M8]` missing demand elasticity; none is supplied.

**Theorem 46 (M34) — HOLDS for the domain; the sign reading is a further ruling.** Shock-induced legs are nonzero with strictly negative signed product (`Theorem46_shock_flow_is_two_legged`), and the legs vanish together, so the one-sided configuration is unreachable (`Theorem46_one_sided_is_unreachable`). But `Theorem46_sign_reading` proves the signed legs make Rule 5's geometric mean undefined (its Lean value collapses to `0`, reproducing the 38(a) degeneracy) while the unsigned legs give `ν = g > 0`: adopting the unsigned reading is a second ruling, recorded as the new OPEN `UNSIGNED-LEGS`. Under it Theorem 38(a) recovers (`Theorem46_recovers_Theorem38a`).

**Theorem 47 (M35) — the two channels are one channel, and BOTH control laws are artifacts.** Under the shock input the expected utilization is `ℙ_{Δ_ARB}(φ,σ,Δt)·g(s,φ,κ_φ)`, so routes (i) and (ii) are two summands of one product rule over the single edge `∂φ/∂τ_MEV` (`Theorem47_shared_driver_is_one_edge`); the project's own taxed hazard depends on the tax only through the composed fee (`Theorem47_no_exogenous_hazard_input`), so the loop system's exogenous input is `i = 0`. Then `P·(1 − loop) = (1−φ_M)(1−φ_X) ≠ 0`: the FOC has **no root at any tax** (`Theorem47_shared_driver_leaves_no_root`, also obtained by invoking `Theorem40d` in `Theorem47_loop_law_is_vacuous`), and under the M26 signs the derivative is strictly positive on `[0,1]`, so the optimum is a boundary point. The single-channel law is not the survivor either — it is the FOC of a system that uses the gate response in the FOC and omits it in the flow response, with an explicit witness (`Theorem47_single_channel_root_is_not_a_root`). Benign flow does not restore an independent path: it enters the loop gain, not an exogenous input (`Theorem47_benign_residual_is_not_an_exogenous_path`), and no demand elasticity is supplied. What the shared driver does buy is that `ε` becomes a closed form and is strictly negative (`Theorem47_epsilon_closes`) — but only usable under the reading just refuted, so O2 is recorded as resolved-empty (no interior stationary point to classify) rather than closed.