# Summary of changes for run e4b4b5bf-16d6-43fa-aff2-e6249c2b9b05
Delivered Theorems 53–55 (blocks M41–M43 of `RequestProject/TAX7_ADDENDUM.md`) as three new Lean modules, all building clean, free of `sorry`, and `#print axioms`-clean (only `propext`, `Classical.choice`, `Quot.sound`):

- `RequestProject/MevGateDerivative.lean` (namespace `MevTaxGate`) — Theorem 54
- `RequestProject/MevLawEquating.lean` (namespace `MevTaxEquating`) — Theorem 53
- `RequestProject/MevPhiStarComputability.lean` (namespace `MevTaxCompute`) — Theorem 55
- `RequestProject/TAX7_RETURN.md` — verdict table, slot bookkeeping, and the OPEN items

**M41 / Theorem 53 — the equating.**
(a) The author's reduction is CONFIRMED, not assumed: `τ* = 1 + (1−φ_X)/S` iff `S = −(1−φ_X)/(1−τ*)`, and via the monoid identity that equals `−(1−φ_M)(1−φ_X)²/(1−φ*)`. It IS the `MevLVRCancellation` bracket zero restated — but only in the COMPOSED slot; in the BARE slot used by `Proposition16_corrected_law` the literal bracket and the FOC core differ by `(1−(1−φ_M)(1−τ))·(∂φ_X/∂ν)(∂ν/∂τ)`, and coincide for all slopes only when `(1−φ_M)(1−τ) = 1`.
(b) Resolved, and the answer depends on and only on the ν-reading. It is NOT an identity. Off-loop (free `∂ν/∂τ`): **(ii) codimension-one** — the FOC core is affine in the slope with nonzero coefficient, exactly one slope per parameter point; witnesses on the locus (slope product `9/8·(−1)`, both laws give `1/3`) and off it (`1·(−1)`: `τ*_law = 1/4 ≠ 1/3`), at a parameter point verified to be a genuine interior optimum of the extended model (`φ* = 1/2`, `q = 5/4 > 0`). Loop-consistent (the corpus's own ν, SRC Theorem 37): **(iii) impossible** — the FOC core equals `(1−φ_X)/ℱ > 0` at every tax, so `τ*_law` has no root at all.
(c) The calibration pinning is proved conditionally, with the Theorem 54(a) gate derivative in the denominator; under reading (iii) its hypothesis is unsatisfiable at every `τ < 1`, so the pinning is vacuous — that is what dies.
(d) Answered honestly: the flag is load-bearing. A block-type-weighted realized ν with an exogenous transactional leg admits a root where the arb-only ν has none. **A further author ruling is required, and none was made here.**

**M42 / Theorem 54 — the gate derivative.** (a) `∂φ_X/∂ν = γ_R(φ_X−φ̄)(1−u/α_R)` verified as a genuine derivative, plus the composed form carrying the monoid Jacobian. (b) The first guard conjunct is now a theorem — `0 < ∂φ_X/∂ν` iff `φ̄ < φ_X` and `u < α_R`, with `0 < u < α_R` automatic and saturation only a limit; at the degenerate point `τ*_law` ceases to exist. (c) The second conjunct is discharged and the feedback question answered: the sign SURVIVES `ℱ_{φ→ν→φ}` — `ℱ > 1`, so the loop damps but never flips, `∂ν/∂τ < 0`. (d) The uniform bound `(γ_R α_R/4)·Σ_j α_j` is proved and shown sharp (attained at the gate midpoint).

**M43 / Theorem 55 — computability of φ\*.** (a) Decided: the FOC is an exponential against a QUADRATIC; Lambert form is available exactly in the degenerate `c = 0` case (`φ* = (1 − W(−e·Lvr/K))/α`). A full impossibility proof for `c ≠ 0` is left as a stated OPEN with its reason (it is a differential-Galois statement, and the machinery is not available). (b) — the item flagged as most valuable — DELIVERED: the iteration `T x = x + Φ(x)/M` with fixed denominator (no derivative division), proved to have a unique root on a bracket inside Theorem 52's `q > 0` guard region, to map the bracket into itself, and to satisfy the explicit geometric bound `|T^[n]x₀ − φ*| ≤ (1 − m/M)^n(b−a)` with `m`, `M` given in closed form and the `K`-independent ratio `1 − e^{−α(b−a)}q_lo/(2σ+2cb)`; non-vacuity is exhibited by a witness bracket where the ratio is at most `10/13`. (c) The iterate and its limit are monotone in σ (consistent with Theorem 50's pro-cyclicality), under the stated pointwise hypothesis `αφ ≤ 1`. (d) Three numeral-free corner predicates — shutdown, corner at zero, interior — the last also producing the induced tax in `(0,1)` and reproducing `φ*` through the top-up law.

Standing bans respected throughout: `α_transactional` and `δ_transactional` remain free typed parameters; numerals occur only inside witnesses; every equation states whether it uses the bare or the composed gate-derivative slot. No prior result was redone — existing declarations in `MevTaxProgram`, `MevTaxLVR`, `MevTaxShock`, `MevTaxControl` and `MevTaxTransactional` are reused by name. All deliverables are registered in the Properties table with their final statuses, and all work is committed and pushed.