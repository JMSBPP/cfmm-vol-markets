# Summary of changes for run 15dfd38f-c93c-480b-beac-9559a25b21fd
Formalized M25–M27 of `RequestProject/TAX3_ADDENDUM.md` in two new axiom-clean, `sorry`-free modules — `RequestProject/MevLVRCancellation.lean` (namespace `MevTaxLVR`, M25) and `RequestProject/MevChannelClosure.lean` (namespace `MevTaxChannels`, M26–M27) — plus a verdict note, `RequestProject/TAX3_RETURN.md`. Every new declaration `#print axioms`-reports only `propext`, `Classical.choice`, `Quot.sound`. `H1_dLbar_dpiPhi_pos` and `H2_dnu_dlamMEV_pos` are used by name as typed hypotheses and never discharged; prior results are cited by declaration name and file (`Theorem29_monoid_path_is_direct`, `Theorem32_hazard_strictAntiOn_tau`, `tau_to_nu_strictAntiOn_under_H2` in `MevTaxControl.lean`; `Theorem33_path_decomposition`, `totalDeriv`, `dnudtau_nonpos_of_strictAntiOn`, `Proposition16_corrected_law` in `MevTaxProgram.lean`; `ptrade` and its properties in `MevOptimization.lean`).

M25. `Theorem37_LVR_cancellation` proves the factorization: carrying the LVR channel gives `∂π̂^σ/∂τ_MEV = K·[bracket]`, where the bracket is *identically* the previously proved two-path total derivative, so the LVR term enters only through the scalar `K`. `Theorem37_K_pos` gives `K > 0` under H1 (via `priceAxisSum_pos_of_H1`), `π^LVR > 0` and a strictly decreasing arb-trade probability. `Corollary37_root_invariance` gives zero-set invariance and the root of Proposition 13, restating the guarded domain lines only under `0 < ∂φ/∂ν`, `∂ν/∂τ < 0`, `φ_X < 1`. So the proposal to restore an interior optimum by carrying `π^LVR` is futile by construction. Two refutations are returned with witnesses: (i) read with Definition 18's bare gate derivative, the addendum's literal bracket is **not** the bracket of the total derivative — it drops the monoid Jacobian on the gate summand (`Theorem37_literal_bracket_refuted`); (ii) answering the falsification question, `ℙ_ARB` is strictly decreasing in the fee only where `σ > 0`, and at `σ = 0` both factors of `K` vanish, so root invariance fails there (`Corollary37_root_invariance_fails_at_K_zero`).

M26. 38(a) is **refuted as stated**: on a one-sided flow the operative Rule 5 member (the geometric mean) is identically zero, so `∂ν/∂ΔQ = 0`. It holds along scaling rays of a two-legged strictly positive flow; the property required is 1-homogeneity plus strict positivity at the flow direction, and the CES family supplies it on the boundary only for a positive substitution exponent — while the pool in use sits on the exponent-zero slice (`Theorem38a_boundary_behaviour_is_epsilon_dependent`). Which case is operative is the open flow-domain ruling, named as such. 38(b) and 38(c) hold; route (i) reached by name from H2 delivers only `≤ 0` (`Theorem38c_route_one_from_H2`), whereas route (ii) is strict. 38(d): route (ii)'s sign carries no H2 binder, but the replacement premise (positive flow response together with downward-sloping demand) is proved **logically independent** of H2 in both directions — not weaker; it trades the behavioural gain for a demand slope on total flow, whose benign component the entry-point document records as absent. Answering the falsification target, the two routes are independent paths and running both closes a feedback loop, so the total is the naive sum divided by one minus a negative loop gain: the sign survives but the plain sum overstates the magnitude (`Theorem38_two_routes_close_a_loop`).

M27. Theorem 39 is **refuted**: for any arb-flow model 1-homogeneous in the pool scale, a closed form for `∂ΔQ^ARB/∂φ` in `(σ, φ, Δt, ε_{p/X})` forces that response to vanish identically, and the same holds with a witness for every leading-order product-shape model. The named missing primitive is the **pool scale** — the reserve level, equivalently the portfolio value function — a level, while the four candidates are all scale-free; the obstruction is dimensional, so neither the leading-order nor the quasi-static caveat is what blocks closure. What does close is proved separately: the participation probability and its fee derivative in `(σ, φ, Δt)`, and the fee *elasticity* of arb flow. So the arbitrage side is available in observables up to one multiplicative pool-level constant that the pool already measures.

Open points are stated with reasons in `TAX3_RETURN.md` rather than narrowed away, and the Properties table records each result with its status.